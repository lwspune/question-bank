# Question Bank — project notes

Free, public past-year-question paper builder for Indian entrance exams. Anyone can filter the public bank by exam/chapter/subtopic/difficulty/PYQ year, then download a Question Paper + Answer Key as Word files. Admins (org members) upload Excel batches privately, then flip individual questions PUBLIC. Sibling app `MHT_CET_AI` posts finalized mocks to `/api/sync/mock` for cross-app dedup. Currently MHT-CET + NDA; IPMAT, CUET, NEET, JEE Main on the roadmap.

**Live bank size (2026-05-27):** **9,578 PUBLIC** questions across 13 subjects — MHT-CET 4,718 (Maths 1,588, Physics 1,577, Chemistry 1,553) + NDA 4,860 (Mathematics 2,160, English 900, Physics 449, Geography 345, Chemistry 262, History 260, Biology 190, Current Affairs 180, Polity 90, Economics 24).

**Content audit status (2026-05-27):** **NDA bank-wide closed** at probe-flagged surface — all 10 NDA subjects, 4,860 / 4,860 q audited (~51% of bank). 5 preserved paper defects across NDA; ~126 wrong-key flips total. **MHT-CET pending** (4,718 q across 79 chapters — primary remaining audit frontier). Per-subject audit register in [[content-audit-progress]] memory; per-row narratives in the Decisions log below.

**All 13 subjects fully cleaned** at chapter + subtopic level — bank-wide cleanup pass closed. (The cleanup-pass snapshot numbers below are point-in-time; subjects have grown via new uploads since their original cleanup; NDA Maths was re-Phase-D'd on 2026-05-17; all 9 other NDA subjects (English + Physics + Chemistry + Biology + Geography + History + Polity + Economics + Current Affairs) on 2026-05-18 to fold in fresh auto-creates; MHT-CET Maths re-Phase-D'd on 2026-05-23 after fresh uploads grew it 795 → 1,588 q; MHT-CET Physics fully cleaned for the first time 2026-05-23 at 1,577 q; MHT-CET Chemistry fully cleaned for the first time 2026-05-27 at 1,553 q — closes the bank-wide cleanup.) **10 NDA strategy guides shipped** at /guide/nda-{maths,english,physics,chemistry,biology,geography,history,polity,economics,current-affairs} (economics + current-affairs are thin single-page landings — economics because the bank's that thin at 24 q · 1 ch, current-affairs because the content half-life is too short for a multi-route fact-memorise guide; CA's single page is **Template D — theme-prep-checklist**, the first guide where the strand axis is recurrence-strength rather than skill or strategic priority). **4 chapters of self-sufficient /notes content across 2 subjects** (NDA Maths Statistics + Vectors + Probability + **NDA Physics Sound**) — foundations → worked → self-check → Level 1 practice → per-subtopic mastery checkpoint, plus per-chapter interactive visualizations + a collapsible chapter revision sheet; rendered by registry-driven shared page components. As of 2026-05-30 the `ConceptUnit` type is a **discriminated union** (`kind: "formula" | "reference"`) — formula variant carries an authored worked example, reference variant carries a flat `ReferenceTable` in the same slot — letting Recall-strand chapters teach via named-fact lookups (Sound is the first user; 7 of its 13 concepts are formula variants, 6 are reference variants). Definitions render `**bold**` key terms + `- ` bullet lists (via `RichText`, 2026-05-29); featured-PYQ + checkpoint cards now show the set-bound passage **context** and a `[Q# · month · year]` provenance citation. **Tier 1 IA + cross-linking shipped 2026-05-18** — primary nav (Bank · Guides · Notes) + cookie-backed exam pill in AppHeader; per-question backlink chips on /browse (chapter→playbook + subtopic→notes + principle/concept tag overrides); `/nda` exam home; two-way notes ↔ guide cross-links; empty-state filter recipes; click-to-reveal answer; in-app **question reports** (migration 0024) with admin triage at /dashboard/reports.

## Stack

- **Next.js 14** App Router · TypeScript · Tailwind 3 · shadcn-style UI primitives
- **Supabase** Auth + Postgres + RLS, accessed via `@supabase/supabase-js` (no Prisma, no ORM)
- **Vitest 2** for tests · `tsx` for scripts
- **Word export:** `temml` (LaTeX → MathML) → `mathml2omml` → `docx` + `jszip`
- **Browser math preview:** `katex` + `react-katex`
- **Deploy:** Vercel, git-integrated

## Multi-tenancy + visibility model

Per-school orgs, RLS-enforced. Three tiers of access:
- **Service role** (seed scripts, server-only admin actions, `/api/admin/members`) bypasses RLS by design — never expose to client code.
- **ADMIN** can do everything in own org: insert + update + delete questions, write options, upload Excel batches, auto-create chapters/subtopics, flip visibility, triage reports, manage members.
- **TEACHER** (as of migration 0025 / 2026-05-26) can **edit** question content — text, options, taxonomy moves, difficulty, PYQ metadata, images, concept/principle tags, leave-set — in own org. Cannot upload Excel, delete questions, flip PUBLIC/PRIVATE, auto-create chapters/subtopics, triage reports, or manage members. Server-side guard in `applyEdit` rejects `payload.visibility` changes from TEACHER (`kind: "forbidden_field"` → HTTP 403) so a tampered request can't bypass the hidden UI toggle.
- **anon** (no JWT) can read PUBLIC questions/options + all taxonomy. Cannot write anything.

Each question has a `visibility` enum (`PUBLIC | PRIVATE`, default **PUBLIC** as of migration 0022 / 2026-05-17 — was PRIVATE prior). Read RLS is the union of two permissive policies: `(visibility = 'PUBLIC')` for everyone, plus `(org_id = private.current_user_org_id())` for authenticated org members on their own org's PRIVATE rows. Authenticated users see PUBLIC across all orgs + their own PRIVATE; anon sees only PUBLIC. Admins flip per-question visibility from the edit page.

RLS helpers live in the **`private` schema** (not exposed by PostgREST): `private.current_user_org_id()`, `private.current_user_is_admin()`, `private.current_user_can_edit_questions()`. New org-scoped policies should reference these. Use `current_user_is_admin()` for destructive / privileged ops (DELETE, upload, visibility flip, reports triage, member management); use `current_user_can_edit_questions()` for content edits that TEACHER should be allowed to do.

## File layout

```
src/
├── app/
│   ├── layout.tsx                         next/font (Inter + Source Serif 4), sonner Toaster, Vercel Analytics, no-FOUC theme bootstrap, full SEO Metadata + Twitter card
│   ├── opengraph-image.tsx                edge-runtime ImageResponse — dynamic 1200x630 OG image with the headline + LWS Pune mark
│   ├── robots.ts                          MetadataRoute.Robots — allow public routes (incl. /guide and /notes), disallow /dashboard /upload /questions /api
│   ├── sitemap.ts                         MetadataRoute.Sitemap — entries:
│   │       + /browse + /login
│   │       + /guide + /guide/nda landing
│   │       + nda-maths landing + 5 sections + **11 principle deep-dive pages**
│   │       + nda-english landing + 5 sections + **16 playbook detail pages** pulled from PLAYBOOK_SLUGS
│   │       + nda-physics landing + 6 sections + **14 chapter playbook detail pages** pulled from PHYSICS_PLAYBOOK_SLUGS
│   │       + nda-chemistry landing + 6 sections + **12 chapter playbook detail pages** pulled from CHEMISTRY_PLAYBOOK_SLUGS
│   │       + nda-biology landing + 6 sections + **9 chapter playbook detail pages** pulled from BIOLOGY_PLAYBOOK_SLUGS
│   │       + nda-geography landing + 5 sections + **7 chapter playbook detail pages** pulled from GEOGRAPHY_PLAYBOOK_SLUGS
│   │       + nda-history landing + 5 sections + **4 chapter playbook detail pages** pulled from HISTORY_PLAYBOOK_SLUGS
│   │       + nda-polity landing + 5 sections + **4 chapter playbook detail pages** pulled from POLITY_PLAYBOOK_SLUGS
│   │       + **nda-economics landing only** (single-page, no sub-routes, priority 0.7)
│   │       + **nda-current-affairs landing only** (single-page Template D, no sub-routes, priority 0.6)
│   │       + every /notes route (chapter index landing + 3 chapter landings + 14 subtopic pages, pulled from STATISTICS_SLUGS + VECTORS_SLUGS + PROBABILITY_SLUGS so future notes auto-register)
│   ├── not-found.tsx                      branded 404 with AppHeader + Footer + "Browse questions" CTA
│   ├── error.tsx                          global error boundary with "Try again" + "Browse questions"; logs error.digest
│   ├── login/page.tsx                     email + password sign-in (client) — split-screen layout
│   ├── nda/                               public exam-home (Tier 1 Phase 2 ship). Hero + live `getExamHomeStats` total + dual CTA (Build a paper · All nine guides) + Strategy-guides grid (9 compact cards) + Teaching-notes grid (Statistics + Vectors) + closing "back to bank" banner. ISR `revalidate = 3600`, cookie-free anon client, full Metadata + `GuideJsonLd` CollectionPage. `/nda/opengraph-image` is an edge-runtime ImageResponse (purple gradient distinct from /guide's blue). Sitemap entry priority 0.95.
│   ├── dashboard/                         page.tsx + loading.tsx — stat cards, by-exam bars, recent uploads (admin). Redirects TEACHER → /browse (admin-only territory; non-org-member signed-in users still see the "Account not linked" card). Admin Quick-Actions grid bumps to 4 cols on lg: Browse · Upload · **Question reports** card with live red open-count badge (head-count via `count: "exact"` on `question_reports.org_id + status='open'`) · **Members** (links to /dashboard/members, 2026-05-26).
│   ├── dashboard/reports/                 admin triage page for `question_reports` (migration 0024). Server Component, admin-only via `getSessionMember` → 404 for non-admins. Status filter chips (All / Open / In review / Resolved / Won't fix / Duplicate) routed via `?status=...`. Per-report card: question breadcrumb + severity-coloured category chip + details quoted callout + Open-question link + `ReportRowActions` client island (status `<select>` → PATCH `/api/reports/[id]` → toast + router.refresh, resolution-note field visible on terminal statuses).
│   ├── dashboard/members/                 admin member-management UI (2026-05-26): page.tsx (Server Component, admin-only — TEACHER redirected to /browse) hands an initial `MemberRow[]` list to `MembersClient.tsx`. Add form: name (required) + email (required, becomes username) + password (≥8 chars) + role dropdown (TEACHER default, ADMIN). Per-row: role `<Select>` (disabled for self), `Reset password` (opens dialog with new-password input), `Remove` (opens confirm dialog; auth user kept, only `org_members` row deleted). Last-admin protect + self-protect enforced server-side. All actions POST to /api/admin/members.
│   ├── upload/                            admin-only: Stepper → Dropzone (file pick) → preview summary bar → animated success
│   ├── browse/                            page.tsx + loading.tsx, composed from:
│   │       + Hero — lean: headline + sub-line + total-q chip + "Available now / Coming soon" exam rows; "Coming soon" hidden below sm; no pill row — guides + notes discoverability lives in PrimaryNav + Footer + `/nda` per 2026-05-21.
│   │       + **FilterRecipes** — Tier 2 empty-state only, derived from `recipes.ts` pure helper: 🎯 HARD only · 📅 Last 2 PYQ years · 🔥 HARD from latest year, scoped via cookie-backed `loadActiveExamContext`.
│   │       + FilterBar — sections rendered in a `mode`-driven order: `live` for desktop sidebar with current-order, `staged` for mobile sheet with difficulty + PYQ year hoisted above chapter accordions; PYQ presets Last 3 / Last 5 / All years backed by `selectRecentYears`; chapter and subtopic rows render as `Name (count)` sorted by q-count descending, zero-count entries hidden, counts context-aware (shrink when other filters narrow the bank).
│   │       + MobileFilters — Sheet owns a staged Filters buffer + sticky bottom bar with Reset / Show results; cascading fields auto-commit to refresh option lists while non-cascading fields stage until Apply.
│   │       + ActiveFilterChips — horizontal-scrolls on mobile, no-wrap; chapter/subtopic chips render as "Chapter: Name (count)"; right edge has a Copy-link chip backed by `buildShareUrl`.
│   │       + QuestionList — groups consecutive set-siblings under a passage banner via groupBySet; takes `includeExam` to surface the exam in each card's breadcrumb when no exam filter is active; threads `isLoggedIn` + `resourceTags: Map<questionId, ResourceTags>` from a batched `getResourceTagsForQuestions` fetch.
│   │       + QuestionCard — expand button and cart-toggle are sibling buttons (never nested); breadcrumb built via `buildBreadcrumb` helper; Q-badge hidden on mobile with `#N` inline; expanded body has `overflow-x-auto [&_.katex]:max-w-full` so wide math scrolls within the card; ZoomableImage dialog has an explicit round close button overlay.
│   │            · **Backlink chip row** between header and expand panel — chapter→playbook + subtopic→notes chips via `getQuestionResources`, overridden by principle / concept tag-specific chips when DB tags exist.
│   │            · **Click-to-reveal answer** — every viewer (incl. ADMIN) sees plain options + "Tap an option to check your answer" hint; clicking an option reveals correctness (green border + "Correct" on right answer; red border + "Your pick" on wrong pick). Picked state resets on card collapse. Admins who need to verify answers without clicking go to `/questions/[id]/edit`. (The 2026-05-18 `isAdmin` short-circuit was reversed 2026-05-25 — see Decisions log.)
│   │            · **ReportQuestionDialog** at the bottom of the expanded view — Radix Dialog; logged-in form (category + details) POSTs to `/api/questions/[id]/reports`; anon gets sign-in prompt + mailto fallback.
│   │            · **Footer row (expanded card)** — `Edit question` link on the left (rendered when `canEdit` — ADMIN OR TEACHER as of 2026-05-26; gate widened from `isAdmin`, the page-level prop was renamed to match) + `[Q# · disambiguator · year]` provenance bracket in the middle (composed by `formatProvenance`, NDA uses month / others use note) + `ReportQuestionDialog` on the right. `flex-wrap` on mobile when the bracket pushes a line.
│   │       + Pagination — compact `Prev / Page X of Y / Next` on mobile, numbered jumper on sm+; click triggers smooth scroll-to-top; client component.
│   │       + DownloadDialog — max-h-[90dvh] with sticky footer; two distinct download buttons (Question Paper + Answer Key), one HTTP request per file with `kind` selector.
│   │       + CartPill — floating pill with pill-in entrance + count-pulse on add + Sheet panel (per-row remove, sort toggle Added-order/By-chapter, Clear via branded confirm dialog, Download); bottom respects `env(safe-area-inset-bottom)` for iOS home indicator; CartItem renders math via `KatexRenderer` with `safeSnippet`-truncated text.
│   ├── questions/[id]/edit/               Editor page (ADMIN + TEACHER as of migration 0025 / 2026-05-26): two-column, sticky save bar, Edit/Preview tab, dropzone-style image slots. Question diagram inline under the Context field; textareas (question + options A–D) accept clipboard image paste. Set-bound questions get a "Set · N questions" chip + propagation hint + "Remove from set" button (POST /api/questions/[id]/leave-set — editor, not admin). Save returns to the previous page (router.back()). **TEACHER-gated chrome:** the `Delete` button in the save bar and the entire `Visibility` section are hidden when `isAdmin === false`; the visibility change is also rejected server-side via `applyEdit`'s `forbidden_field` guard. Non-org-member redirected to /browse.
│   ├── uploads/page.tsx                   admin-only upload index (Server Component): paginated full list of upload_jobs for the org (20/page, newest first), reuses browse Pagination; `?page=` clamped beyond the last page. "View all" link on dashboard points here.
│   ├── uploads/[id]/                      admin-only upload-detail: page.tsx (server) lists questions linked via upload_job_id, with header card; UploadHeaderActions (whole-upload delete dialog), PyqMetadataControl (3-field bulk-set: Year select + Month + Comment with confirm-on-overwrite dialog), QuestionListItem (per-row Edit + Delete)
│   ├── api/auth/callback/                 Supabase OAuth code exchange
│   ├── api/upload/{preview,commit}/       two-stage admin upload (preview accepts optional examId — auto-detected from Excel "Course" column via detectCourse + resolveExam if blank — and pyqYear/pyqMonth/pyqNote on formData; runs propagateSetContext to fill Set siblings from the first row's context; stages everything on upload_jobs.staged_rows; commit passes through to commitStaged)
│   ├── api/questions/[id]/                PUT (editor — ADMIN or TEACHER via `requireEditor`) + DELETE (admin only via `requireAdmin`): edit text+options+taxonomy+image paths; delete cascades options + cleans storage objects. PUT also fans out a context change to every sibling sharing the same set_id (only context propagates; everything else stays row-local). PUT stamps `last_edited_by` + `last_edited_at` on the question via `applyEdit` (migration 0025), and threads `member.role` so `applyEdit` can reject TEACHER visibility changes with HTTP 403 (`forbidden_field`).
│   ├── api/questions/[id]/leave-set/      POST (editor — ADMIN or TEACHER via `requireEditor`): clears set_id on this row only. Detaches the question from its passage group; siblings untouched. Idempotent. Teachers can use this as a content correction (the screenshot's Q2-not-in-Set-1 case).
│   ├── api/questions/[id]/reports/        POST (JSON, signed-in user — anon → 401). Body: `{ category, details? }`. Validates category enum + details ≤ 2000 chars, calls `createReport` helper which resolves question.org_id and denormalizes onto the report row. Returns 201 + `{id}` on success, 409 on duplicate-open-report (partial unique index), 404 not-found, 422 invalid input.
│   ├── api/reports/[id]/                  PATCH (JSON, admin only via `requireAdmin`). Body: `{ status, resolutionNote? }`. Calls `updateReport` helper — RLS scopes to admin's own-org reports; auto-stamps `resolved_at` + `resolved_by` on terminal statuses (resolved / wont-fix / duplicate). Reverting to open / in-review clears resolution stamps.
│   ├── api/admin/members/                 POST (JSON, admin only via `requireAdmin`). Single endpoint with action discriminator: `{ action: "list" | "create" | "reset" | "remove" | "update_role", ... }`. Calls into `src/lib/members/admin.ts` helpers using the service-role admin client (auth.admin.* needs it; `org_members` reads gate on admin RLS anyway). Returns `{ ok, ...extras }` on success or `{ error }` on failure. Handles `already_member` / `email_taken_other_org` 409s, `would_remove_last_admin` / `cannot_remove_self` / `cannot_change_own_role` 403s, validator-rejection 400s.
│   ├── api/uploads/[id]/                  DELETE + PATCH (JSON, admin only). DELETE removes all questions linked via upload_job_id + their storage objects + the upload_jobs row. PATCH bulk-updates pyq_year/pyq_month/pyq_note across the upload's questions; partial fields honoured, explicit null clears.
│   ├── api/sync/mock/                     POST (JSON): receives a finalized mock from a sibling app (e.g. MHT_CET_AI). Bearer-token auth via SYNC_SHARED_SECRET. Idempotent + content-hash dedup + attempt-stats merging.
│   ├── api/cart/preview/                  POST (JSON): { ids } → { items }. Slim per-question metadata (taxonomy + question_number + source_row + text) for the cart panel. RLS-scoped, capped at 200 ids.
│   ├── api/export/                        POST → single .docx. Body must include `kind: "paper" | "key"` plus either { filters, options } or { questionIds, options } (mutually exclusive). Returns one Word file per request (filename `QP_<title>.docx` for paper, `Answers_<title>.docx` for key) — no ZIP wrapper. Cart-mode preserves caller's id order. Rate-limited: 20 anon/hr/IP, 200 authed/hr/user (doubled vs the original to keep one teacher-export = two requests neutral).
│   ├── guide/                             Public, anon-friendly, Google-indexable strategy guides. page.tsx 307-redirects to /guide/nda (the NDA exam-picker landing — flipped 2026-05-18; was → /guide/nda-maths). opengraph-image.tsx is a section-wide OG image (NDA-Maths-themed gradient) inherited by /guide/* children that don't override; /guide/nda-english/opengraph-image.tsx overrides with a purple English-themed gradient for that subtree. nda-maths/page.tsx landing also renders cross-link banners pointing into /notes/nda-maths/statistics AND /guide/nda-english.
│   │   ├── _components/                   Shared `/guide` primitives:
│   │   │       · GuideShell — accepts optional `landingHref` prop (defaults to items[0].href) so each guide's Overview link de-activates correctly on sub-routes
│   │   │       · GuideSideNav — same `landingHref` prop (was hard-coded to nda-maths)
│   │   │       · GuideHero · StatBlock · BrowseLink (resolves to /browse via buildBrowseUrl) · PrevNextNav · ChapterCard · CompoundCard
│   │   │       · DriftTable — accepts `rowLabel` prop (defaults "Principle"; english trends passes "Chapter")
│   │   │       · TrapBox · SignFlipHeatmap · PrincipleCard · CollapsibleDomain
│   │   │       · WorkedExampleCard — accepts `presentMode` prop that scales fonts for digital-board projection (used by /notes Present mode)
│   │   │       · RelatedPrinciples (nda-maths)
│   │   │       · RelatedPlaybooks (nda-english) · RelatedPhysicsPlaybooks · RelatedChemistryPlaybooks · RelatedBiologyPlaybooks · RelatedGeographyPlaybooks · RelatedHistoryPlaybooks · RelatedPolityPlaybooks — each parallel to the others, bound to its own guide's PLAYBOOKS, kept separate per "link path is hard-coded per guide" precedent (**8 parallels now**)
│   │   │       · FormulaSheet — single-page formula compendium renderer; physics-only; plain-text + unicode formulas grouped by chapter, no KaTeX pipeline to keep the page a server component
│   │   │       · CommonCompoundsTable — single-page chemical name↔formula↔use reference renderer; chemistry-only; plain-text/unicode formulas in 3-column tables grouped by themed cluster, same server-component constraint
│   │   │       · BiologyReferenceTables — multi-domain named-fact reference renderer; biology-only; 4 themed clusters each with own column headers since Biology spans 4 named-fact domains (diseases↔pathogens, vitamins↔deficiencies, hormones↔glands, scientists↔discoveries)
│   │   │       · GeographyReferenceTables — parallel; multi-domain; 4 themed clusters (Indian Rivers / Mountain Peaks / Mineral & Crop Producer States / Local Winds + Climate Zones)
│   │   │       · HistoryReferenceTables — parallel; multi-domain; 5 themed clusters (Era timeline / Rulers↔dynasty / Reformers↔movement↔text / Scholars↔texts / Viceroys+British Acts↔year)
│   │   │       · PolityReferenceTables — parallel; multi-domain; 4 themed clusters (Key Articles↔Subject / Constitutional Amendments↔Year↔Theme / Constitutional Bodies↔Function↔Article / Parts↔Schedules↔Content); same plain-text + server-component constraint (**4 multi-domain parallels now**)
│   │   │       · GuideJsonLd — emits schema.org Article or CollectionPage as inline `<script type="application/ld+json">`
│   │   ├── nda/                           Exam-picker landing — 10 cards (NDA Mathematics · 2,160 q / NDA English · 900 q / NDA PART B Physics · 449 q / NDA PART A Geography · 345 q / NDA PART B Chemistry · 262 q / NDA PART A History · 260 q / NDA PART B Biology · 190 q / NDA Current Affairs · 180 q · single-page Template D / NDA PART A Polity · 90 q / NDA PART A Economics · 24 q — single-page guide) on a `sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4` grid (was xl:grid-cols-5 with 5 cards; 2×3 layout for 6 cards on 2026-05-18; bumped to xl:grid-cols-4 for 8 cards on 2026-05-19 Polity; stays xl:grid-cols-4 for 9 cards on 2026-05-19 Economics; stays xl:grid-cols-4 for 10 cards on 2026-05-19 Current Affairs = 4+4+2 layout) linking into the ten guide subtrees. Single page.tsx, no _data — extensible when an eleventh subject ships a guide.
│   │   ├── nda-maths/                     17 indexable pages: landing + 5 sections (strategy, principles index, compound-tricks, trends, traps) + **11 principle deep-dive pages** at /principles/[slug] pre-rendered via generateStaticParams (was 20 pre-Phase-3; 9 single-chapter principles dropped 2026-05-17 after strict cross-chapter prune). **Template A** (principles-first).
│   │   │       · `_data/` holds editorial content as static TS modules. `nda-maths.ts` exports `OVERVIEW` + `ROUTES` + **`CHAPTER_TABLE`** (31-row chapter-level shape — qCount, pctTotal, pctHard, focus subtopics — rendered on the overview page).
│   │   │       · **TOP_11 principles** (in `principles.ts`) carry only `slug + name + summary + pctHard` — editorial `pctHard` here is back-stop only; principles index page derives pctHard + chapter spread live via inline `loadPrincipleStats` against `question_principle_tags`.
│   │   │       · **Long-tail principles** inside `DOMAINS` (no slug) keep the original static `drill: Array<{ chapter, subtopic? }>` + `qCount` pattern (resolved via `resolveTaxonomy` → subtopic UUIDs). Per-principle stories + worked-example UUIDs in `principle-details.ts`. COMPOUNDS, DRIFT_ROWS, TRAPS, STRATEGY are unaffected by the prune.
│   │   │       · The principles index TOP table sorts by live #qs descending (not array order) so the highest-leverage principles surface first. BrowseLink CTAs for TOP_11 emit `?principle=<slug>` (DB-backed); for long-tail, they emit `?subtopics=...&extras=...` (TS-backed) — same `/browse` filter machinery.
│   │   ├── nda-english/                   25 indexable pages: landing + 5 sections (strategy, playbooks index, vocab-families, trends, traps) + **16 playbook detail pages** at /playbooks/[slug] pre-rendered via generateStaticParams.
│   │   │       · Structurally distinct from nda-maths because NDA English has no cross-chapter principles axis (largest English lever is S-V agreement at 27 q across Grammar + Errors; everything else is sealed within one chapter) and %HARD is flat (only Sentence Rearrangement carries real difficulty). Replaced principles + compound-tricks with playbooks (one per major subtopic, grouped by Recall / Rule / Reason buckets in `strategy.ts`).
│   │   │       · `_data/` editorial modules:
│   │   │           - `nda-english.ts` — OVERVIEW + ROUTES + 8-row CHAPTER_TABLE
│   │   │           - `strategy.ts` — 3 STRATEGY_BUCKETS + TEST_DAY_PLAN + TIME_BUDGET; Recall = Vocab + Idioms / Rule = Errors + Grammar / Reason = RC + Rearrangement + Cloze + FIB
│   │   │           - `playbooks.ts` — 16 entries (`slug + chapter + subtopics[] + qCount + pctHard + bucket`)
│   │   │           - `playbook-details.ts` — per-slug trigger + story + sub-skills + traps + worked-example UUIDs
│   │   │           - `vocab-families.ts` — 13 themed clusters of real PYQ-tested words, regex-mined from `\(\underline{\text{…}}\)` stems in the live Synonym/Antonym subtopics
│   │   │           - `trends.ts` — 10-yr DRIFT_ROWS + 4 callouts (Grammar exploded post-2024 from 0→40 q/yr, Spotting Errors went silent 2024–25)
│   │   │           - `traps.ts` — 9 English-specific trap shapes bucketed by Recall/Rule/Reason; meaning-based not numeric so no SIGN_FLIP_CELLS analog
│   │   │       · No DB-backed `question_playbook_tags` table — cross-chapter lever in English is too thin (max 27 q) to justify the infra; per-playbook drills use `subtopicIds` from `resolveTaxonomy` at request time.
│   │   ├── nda-physics/                   22 indexable pages: landing + 6 sections (strategy, playbooks index, formulas, trends, traps — and the **new `/formulas` page kind**) + **14 chapter playbook detail pages** at /playbooks/[slug] pre-rendered via generateStaticParams.
│   │   │       · **Template C** (Decisions log 2026-05-18): chapter-playbooks + skill-strand strategy + formula compendium — distinct from BOTH nda-maths (Template A, principles-first) and nda-english (Template B, playbooks-only).
│   │   │       · Cross-chapter physics lever maxes at 12 q × 6 chapters (ratio-proportional, fails Template A's ≥40 q × ≥4 ch gate) AND %HARD is mid-spread (5 of 14 chapters > 15% HARD, so flat-Template-B is wrong too). Playbooks are 1:1 with chapters (subtopics too fine — 50 of them with several at 1–3 q).
│   │   │       · Strategy strands: Recall (Sound + Modern + Astronomy + Energy + Units = 79 q) / Apply (Light + Mechanics + Gravity + Oscillations = 215 q) / Reason (E&M + Heat + Fluids = 155 q), with per-chapter **DrillPosture** overlay (drill-all / drill-all-target-hard / cherry-pick-easy-mod / skim) so strategy reflects both the strand AND the HARD-pool concentration.
│   │   │       · `_data/` editorial modules:
│   │   │           - `nda-physics.ts` — OVERVIEW + ROUTES + 14-row CHAPTER_TABLE
│   │   │           - `strategy.ts` — 3 STRATEGY_STRANDS + TEST_DAY_PLAN + TIME_BUDGET; STRATEGY_HEADLINE per 25-q PART B Physics paper
│   │   │           - `playbooks.ts` — 14 entries (slug + chapter + ALL subtopics in the chapter + qCount + pctHard + bucket)
│   │   │           - `playbook-details.ts` — per-slug trigger + story + sub-skills + traps + 2 worked-example UUIDs per chapter (most-recent + HARD where available)
│   │   │           - `formulas.ts` — FORMULA_GROUPS, 32 essential formulas grouped by chapter, plain-text + unicode, each with name + formula + symbol legend + optional trap-notes
│   │   │           - `trends.ts` — 10-yr DRIFT_ROWS + HARD_BY_YEAR table + 4 DRIFT_CALLOUTS (headline: paper hardened 22× per question, 2% in 2021 to 44% in 2026 NDA-1)
│   │   │           - `traps.ts` — 11 physics-specific trap shapes bucketed by Recall/Apply/Reason; formula-application-confusion shapes: sign-flip, TIR direction, mass-in-pendulum, CGS/SI mix, series/parallel ratio inversion, latent-heat omission, density-mean confusion, planet-scaling ratio, PV-process variant
│   │   │       · No DB-backed `question_playbook_tags` table — same rationale as English; per-playbook drills use `chapterIds` from `resolveTaxonomy` at request time.
│   │   ├── nda-chemistry/                 21 indexable pages: landing + 6 sections (strategy, playbooks index, common-compounds, trends, traps — and the **new `/common-compounds` page kind**) + **12 chapter playbook detail pages** at /playbooks/[slug] pre-rendered via generateStaticParams.
│   │   │       · **Template B variant** (Decisions log 2026-05-18): playbooks-only like English, BUT with a Recall/Rule/**Calculate** strand split (not Recall/Rule/Reason) because the bank has a small but distinct numeric strand (Mole Concept 9 q) and no chain-reasoning lever.
│   │   │       · Cross-chapter lever maxes at Acid-base/pH 26 q × 9 ch (inflated by common-word false positives, still <40 q gate for Template A); %HARD is the FLATTEST of any NDA subject (0 chapters >12% HARD, 4 chapters at 0% HARD, max 11.1% Mole Concept) so Template C's DrillPosture overlay would have nothing to overlay. Playbooks 1:1 with chapters (same rationale as physics — subtopics well-cleaned but thin).
│   │   │       · Strategy strands: Recall (Carbon + Matter + Industrial + Metals + Hydrogen + Everyday Life + Practical = 144 q · 55%) / Rule (Atomic Structure + Acids/Bases + Reactions + Bonding = 109 q · 42%) / Calculate (Mole Concept = 9 q · 3%) — uneven bucket sizes by design (bank is 63% recall).
│   │   │       · `_data/` editorial modules:
│   │   │           - `nda-chemistry.ts` — OVERVIEW + ROUTES + 12-row CHAPTER_TABLE
│   │   │           - `strategy.ts` — 3 STRATEGY_STRANDS + TEST_DAY_PLAN + TIME_BUDGET; STRATEGY_HEADLINE per 25-q PART B Chemistry paper (note: no DrillPosture overlay, simpler card render than physics)
│   │   │           - `playbooks.ts` — 12 entries (slug + chapter + subtopics[] + qCount + pctHard + bucket)
│   │   │           - `playbook-details.ts` — per-slug trigger + story + sub-skills + traps + 2 worked-example UUIDs per chapter
│   │   │           - `common-compounds.ts` — COMPOUND_CLUSTERS, 53 chemical name↔formula↔use pairs in 6 themed clusters (Acids / Salts+Bases / Gases / Fuels+Organic / Allotropes+Elements / Alloys); each entry carries optional `playbookSlug` + amber-noted trap-aware row
│   │   │           - `trends.ts` — 10-yr DRIFT_ROWS + HARD_BY_YEAR table + 4 DRIFT_CALLOUTS (headline: paper has NOT hardened, UNLIKE Physics; oscillates 0–14% HARD with no trend, drill all 10 years equally)
│   │   │           - `traps.ts` — 12 chemistry-specific trap shapes bucketed by Recall/Rule/Calculate; identity-confusion shapes: acid-source swap, diamond-graphite property flip, scientist-discovery swap, alloy composition swap, periodic-trend reversal, reducing-vs-oxidising agent flip, oxide-classification error, hardness-type removal swap, Lewis-vs-Arrhenius base, ionic-vs-covalent EN cutoff, equivalent-weight polyprotic, molecule-vs-atom count
│   │   │       · No DB-backed `question_playbook_tags` table — same rationale as English/Physics; per-playbook drills use `chapterIds` from `resolveTaxonomy` at request time.
│   │   ├── nda-biology/                   16 indexable pages: landing + 6 sections (strategy, playbooks index, reference-tables, trends, traps — and the **new `/reference-tables` page kind**) + **9 chapter playbook detail pages** at /playbooks/[slug] pre-rendered via generateStaticParams.
│   │   │       · **Template B variant** (Decisions log 2026-05-18): playbooks-only like English/Chemistry, BUT with a Recall/Apply/**Verify** strand split (not Recall/Rule/Reason or Recall/Rule/Calculate) because Biology is overwhelmingly recall (82% of questions by execution shape are single-fact-recall), has NO numeric strand (no analogue of Mole), and has only 4 chain-reasoning HARDs across 190 q — too thin for a Reason strand. INSTEAD the third strand captures the bank's second-largest execution mode: multi-statement true/false evaluation (24 q).
│   │   │       · Cross-chapter lever max is Diseases/Pathogens 19 q × 3 ch (skewed to MD) — fails Template A's 40 q × ≥4 ch gate. %HARD is the FLATTEST of any NDA subject — flatter than Chemistry: max 7.7% on a 13-q chapter (1 HARD), only 4 HARDs across 190 q (2.1%), 4 of 9 chapters at ZERO HARD across the entire 10-year window — so Template C's DrillPosture overlay has nothing to overlay. Playbooks 1:1 with chapters (subtopics thin: max 13 q).
│   │   │       · Strategy strands: Recall (Human Physiology + Cell Biology + Microbiology + Biodiversity + Genetics = 132 q · 69%) / Apply (Plant Biology + Reproduction = 42 q · 22% — carries 4 of the bank's 5 HARDs; mechanism-tracing: photosynthesis, transpiration, osmosis, inheritance) / Verify (Ecology + Biochemistry = 16 q · 8% — multi-statement evaluation chapters).
│   │   │       · `_data/` editorial modules:
│   │   │           - `nda-biology.ts` — OVERVIEW + ROUTES + 9-row CHAPTER_TABLE
│   │   │           - `strategy.ts` — 3 STRATEGY_STRANDS + TEST_DAY_PLAN + TIME_BUDGET; STRATEGY_HEADLINE per 25-q PART B Biology paper
│   │   │           - `playbooks.ts` — 9 entries (slug + chapter + subtopics[] + qCount + pctHard + bucket)
│   │   │           - `playbook-details.ts` — per-slug trigger + story + sub-skills + traps + 2 worked-example UUIDs per chapter
│   │   │           - `reference-tables.ts` — REFERENCE_CLUSTERS: **52 named-fact PAIRS in 4 themed clusters with DOMAIN-SPECIFIC column headers** per cluster (Diseases ↔ Pathogens / Vitamins ↔ Deficiencies / Hormones ↔ Glands / Scientists ↔ Discoveries); each entry carries optional `playbookSlug` + amber-noted trap-aware row
│   │   │           - `trends.ts` — 10-yr DRIFT_ROWS + HARD_BY_YEAR table + 4 DRIFT_CALLOUTS (headline: paper has NOT hardened, only 4 HARDs total across 190 q over 10 yrs, drill all 10 years equally)
│   │   │           - `traps.ts` — 13 biology-specific trap shapes bucketed by Recall/Apply/Verify; paired-fact-swap shapes: disease↔pathogen swap, vitamin↔deficiency swap, hormone↔gland swap, RNA-vs-DNA virus identity, scientist↔discovery pair swap, monocot↔dicot trait swap, kingdom misclassification, osmosis direction flip, photosynthesis input/output flip, double-fertilisation ploidy swap, transpiration experimental misread, multi-statement partial-credit, antibiotics-treat-viral, all-cells-have-nucleus universal-claim trap
│   │   │       · New `BiologyReferenceTables` component (parallel to CommonCompoundsTable but multi-domain — each cluster has its own column headers, since 4 different fact domains). New `RelatedBiologyPlaybooks` component (5th parallel — link path hard-coded per guide). No DB-backed `question_playbook_tags` table — same rationale as English/Physics/Chemistry; per-playbook drills use `chapterIds` from `resolveTaxonomy` at request time.
│   │   ├── nda-geography/                 15 indexable pages: landing + 5 sections (strategy, playbooks index, reference-tables, trends, traps) + **7 chapter playbook detail pages** at /playbooks/[slug] pre-rendered via generateStaticParams.
│   │   │       · **Template B variant 3** (Decisions log 2026-05-18): playbooks-only like English/Chemistry/Biology with Recall/Apply/Verify strands, BUT %HARD is NON-flat (4 of 7 chapters >15% HARD — Climatology 28%, IG Economy 24%, Earth's Structure 20%, Earth in Space 18%). Disqualifies pure Template B's "≤2 chapters > 15% HARD" gate.
│   │   │       · AND HARD is NOT concentrated in 1–2 subtopics per chapter the way Physics is (Climatology HARD spread 4-3-3-3-2-1 across 6 subs; Earth's Structure 5-4-3-2-1-0-0) — so Template C's `DrillPosture` overlay has weak leverage. Variant: strand-level %HARD calibration (Apply strand carries 23% avg HARD = the densest-HARD strand) instead of per-chapter DrillPosture. Cross-chapter principle axis dead (max lever rivers-water 27 q × 4 ch fails A's 40 q × ≥4 ch gate).
│   │   │       · Strategy strands: Recall (IG Economy + IG Physical + World/Human = 173 q · 50%) / Apply (Climatology + Earth's Structure = 131 q · 38% — densest HARD) / Verify (Earth in Space + Oceanography = 41 q · 12% — multi-statement evaluation dominant). Largest chapter is Indian Geography Economy at 81 q · 24% HARD — densest-HARD recall chapter in any guide.
│   │   │       · `_data/` editorial modules:
│   │   │           - `nda-geography.ts` — OVERVIEW + ROUTES + 7-row CHAPTER_TABLE
│   │   │           - `strategy.ts` — 3 STRATEGY_STRANDS + TEST_DAY_PLAN + TIME_BUDGET; STRATEGY_HEADLINE per 19-q PART A Geography paper (note: PART A not PART B, max marks ≈ 76, target 50+)
│   │   │           - `playbooks.ts` — 7 entries (slug + chapter + subtopics[] + qCount + pctHard + bucket)
│   │   │           - `playbook-details.ts` — per-slug trigger + story + sub-skills + traps + 2 worked-example UUIDs per chapter
│   │   │           - `reference-tables.ts` — REFERENCE_CLUSTERS: **62 named-fact PAIRS in 4 themed clusters with DOMAIN-SPECIFIC column headers** per cluster (Indian Rivers & Tributaries / Indian Mountain Peaks & Ranges / Mineral & Crop Producer States / Local Winds + Climate Zones); each entry carries optional `playbookSlug` + amber-noted trap-aware row
│   │   │           - `trends.ts` — 10-yr DRIFT_ROWS + HARD_BY_YEAR table + 4 DRIFT_CALLOUTS (headline: paper has NOT consistently hardened, %HARD bounces 6%→42% with no monotonic trajectory; 2018 outlier high, 2025 also high, drill all 10 years equally)
│   │   │           - `traps.ts` — 11 geography-specific trap shapes bucketed by Recall/Apply/Verify; MIXED genre: paired-fact-swap shapes for Recall (state↔river misalignment, mineral↔state swap, peak↔range swap, crop↔season swap, world river/canal identity, scheme purpose over-inclusion); mechanism-direction flips for Apply (Coriolis flip, warm/cold current swap, plate-boundary outcome flip, S-wave-through-liquid, rock-classification error, temperature-inversion misread); multi-statement traps for Verify (partial-credit, terrestrial↔Jovian planet trait swap, universal-claim, spring-tide season confusion, Hawaii-on-ridge)
│   │   │       · New `GeographyReferenceTables` component (6th parallel — same multi-domain shape as BiologyReferenceTables, link path hard-coded per guide). New `RelatedGeographyPlaybooks` component (6th parallel — link path hard-coded per guide). No DB-backed `question_playbook_tags` table — same rationale as English/Physics/Chemistry/Biology; per-playbook drills use `chapterIds` from `resolveTaxonomy` at request time.
│   │   ├── nda-history/                   10 indexable pages: landing + 5 sections (strategy, playbooks index, timeline-and-pairs, trends, traps) + **4 chapter playbook detail pages** at /playbooks/[slug] pre-rendered via generateStaticParams.
│   │   │       · **Template B variant 4** (Decisions log 2026-05-19): the FIRST tier-style strand split within Template B variants. Bank: 260 q · 4 ch · 22 sub · 29% HARD bank-wide. ALL 4 chapters > 15% HARD (Modern 34%, Medieval 28%, Ancient 27%, World 20%) — disqualifies pure Template B.
│   │   │       · HARD top-2 concentration: 63% (Modern) / 40% (Medieval, DIFFUSE 3-3-3-3-2-1) / 67% (Ancient) / 63% (World) — BUT Modern's "concentrated HARD pool" IS Freedom Movement (56 q · 46% of chapter), cannot legitimately be skipped → DrillPosture lever fails on the dominant chapter. AND execution modes are UNIFORM across chapters (multi-statement 70 q × 4 ch · 43% HARD; date-anchored 61 q × 4 ch · 43% HARD — cut UNIFORMLY) → a Bio/Geo-style R/Apply/Verify strand split would force lopsided cuts.
│   │   │       · Strands group chapters by **STRATEGIC PRIORITY**, not execution mode: Cornerstone (Modern India alone = 122 q · 47% · 34% HARD — no other NDA subject has one chapter at this share) / Foundation Recall (Ancient + Medieval = 97 q · 37% · 28% HARD avg — 75% + 64% pure recall, dates rare) / Quick-Win (World History = 41 q · 16% · 20% HARD — date-anchored 39%, lowest %HARD). Cross-chapter topical lever max is Reformers/Movements 27 q × 2 ch (fails A's 40×4 gate).
│   │   │       · History-specific subject artefact = `/timeline-and-pairs` (analogue of /vocab-families /formulas /common-compounds /reference-tables) — **multi-domain** like Bio/Geo: 5 themed clusters · ~95 entries (Era timeline ~42 absolute dates BCE → 1947+ · Rulers ↔ dynasty · Reformers ↔ movement ↔ text · Scholars ↔ texts · Viceroys + British Acts ↔ year).
│   │   │       · `_data/` editorial modules:
│   │   │           - `nda-history.ts` — OVERVIEW + ROUTES + 4-row CHAPTER_TABLE
│   │   │           - `strategy.ts` — 3 STRATEGY_STRANDS + TEST_DAY_PLAN + TIME_BUDGET; STRATEGY_HEADLINE per 14-q PART A History paper (note: PART A not PART B, max marks ≈ 56, target 38+)
│   │   │           - `playbooks.ts` — 4 entries 1:1 with chapters (slug + chapter + subtopics[] + qCount + pctHard + bucket cornerstone|foundation|quickwin)
│   │   │           - `playbook-details.ts` — per-slug trigger + story + sub-skills + traps + 2 worked-example UUIDs per chapter
│   │   │           - `timeline-and-pairs.ts` — REFERENCE_CLUSTERS, 95 entries across 5 themed clusters
│   │   │           - `trends.ts` — 10-yr DRIFT_ROWS + HARD_BY_YEAR table + 4 DRIFT_CALLOUTS (headline: paper has NOT consistently hardened, %HARD bounces 19%→42%→10%; secondary callout: Ancient surged 2022–24 from ~2 q/yr baseline to 9–10 q/yr, Modern dominated 2017–20 then faded)
│   │   │           - `traps.ts` — 15 history-specific trap shapes bucketed by **GENRE** not tier: paired-fact swap (reformer↔movement, ruler↔dynasty, scholar↔text, Harappan site↔feature, Act↔year, Mahamatta function); chronology-sequence (traveller-by-era, Gandhian satyagraha order, EIC dates, IR invention chronology); multi-statement Verify (partial-credit, universal-claim, match-list misalignment, WWI impact). Bucketing by genre because the FIX (memorise pair table / anchor on absolute dates / judge each statement independently) maps to the genre regardless of chapter.
│   │   │       · New `HistoryReferenceTables` component (7th parallel — multi-domain like Bio/Geo). New `RelatedHistoryPlaybooks` component (7th parallel — link path hard-coded per guide). No DB-backed tag table — same rationale as English/Physics/Chemistry/Biology/Geography; per-playbook drills use `chapterIds` from `resolveTaxonomy` at request time.
│   │   ├── nda-polity/                    12 indexable pages: landing + 5 sections (strategy, playbooks index, reference-tables, trends, traps) + **4 chapter playbook detail pages** at /playbooks/[slug] pre-rendered via generateStaticParams.
│   │   │       · **Template B variant 5** (Decisions log 2026-05-19): the SECOND tier-style strand split, but with the THIRD tier INVERTED vs History — Polity's smallest chapter is also the HARDEST (not easiest like History's World History). Bank: 90 q · 4 ch · 14 sub · 19% HARD bank-wide.
│   │   │       · SMALLEST GAT section (avg 5 q/paper, range 2–10) — strategic-priority labels more actionable than skill-bucket labels at this scale. Cross-chapter lever max is article-citation 28 q × 3 ch (fails A's 40 q × ≥4 ch gate). %HARD spread 13.6 / 15.0 / 16.7 / 41.7 — only World Polity (41.7%) is real outlier, and the chapter is too small (12 q) for DrillPosture to add signal. Multi-statement partition is partial (4.5% / 15% / 30.6% / 50% across chapters — Govt Structure has both heavy multi-stmt AND single-fact recall, doesn't fit one execution-mode bucket).
│   │   │       · Strands: **Cornerstone** (Government Structure = 36 q · 40% · 17% HARD — carries 6 of 17 bank HARDs) / **Foundation Recall** (Indian Constitution + FR/DPSP/Local Governance = 42 q · 47% · 14% HARD avg — constitutional-content recall: Articles, Amendments, FRs, DPSP, Parts/Schedules) / **Specialist Wildcard** (World Polity = 12 q · 13% · 42% HARD · 50% multi-statement — INVERTED Quick-Win: smallest AND hardest).
│   │   │       · Polity-specific subject artefact = `/reference-tables` — **multi-domain** like Bio/Geo/History: 4 themed clusters · 80 entries (Key Articles ↔ Subject 32 anchors — bank's #1 cross-chapter lever / Constitutional Amendments ↔ Year ↔ Theme 18 / Constitutional Bodies ↔ Function ↔ Article 19 / Parts ↔ Schedules ↔ Content 11).
│   │   │       · `_data/` editorial modules:
│   │   │           - `nda-polity.ts` — OVERVIEW + ROUTES + 4-row CHAPTER_TABLE
│   │   │           - `strategy.ts` — 3 STRATEGY_STRANDS + TEST_DAY_PLAN + TIME_BUDGET; STRATEGY_HEADLINE per 5-q PART A Polity paper (note: PART A not PART B, max marks ≈ 20, target 14+)
│   │   │           - `playbooks.ts` — 4 entries 1:1 with chapters (bucket cornerstone|foundation|specialist)
│   │   │           - `playbook-details.ts` — per-slug trigger + story + sub-skills + traps + 2 worked-example UUIDs per chapter
│   │   │           - `reference-tables.ts` — REFERENCE_CLUSTERS, 80 entries across 4 themed clusters
│   │   │           - `trends.ts` — 10-yr DRIFT_ROWS + HARD_BY_YEAR table + 4 DRIFT_CALLOUTS (headline: paper has NOT consistently hardened, %HARD bounces 0%→50% with noise inherent to small per-paper samples; secondary callouts: 2026 NDA-1 was hardest + most Govt-Structure-heavy 7-of-10, 2021 FR/DPSP outlier 7-of-14, World Polity absent 2018–20 then steady 1–3 q/yr)
│   │   │           - `traps.ts` — 11 polity-specific trap shapes bucketed by **GENRE**: paired-fact swap (Article↔subject, Amendment↔year, body↔function, HC↔jurisdiction) + **PROCEDURAL CONFUSION** (Money Bill vs Finance Bill, Speaker vs President powers, original vs appellate jurisdiction, constitutional vs statutory body — Polity-UNIQUE genre, no analogue in other NDA subjects) + multi-statement Verify (partial-credit, universal-claim, match-list misalignment)
│   │   │       · New `PolityReferenceTables` component (8th parallel — multi-domain like Bio/Geo/History). New `RelatedPolityPlaybooks` component (8th parallel — link path hard-coded per guide). No DB-backed tag table — same rationale as English/Physics/Chemistry/Biology/Geography/History; per-playbook drills use `chapterIds` from `resolveTaxonomy` at request time.
│   │   ├── nda-economics/                 1 indexable page (single-page landing — no sub-routes).
│   │   │       · **Override of the same-day 2026-05-19 (post-Polity) defer** (Decisions log 2026-05-19 (later)): user opted for a thin one-page artefact over no-ship after analysis re-confirmed the bank shape (24 q · 1 ch · 3 sub · 1.5 q/paper · ~6 marks max · 41.7% HARD — densest HARD of any NDA subject).
│   │   │       · NOT a Template A/B/C variant — deliberately thinner terminal node where the bank's sole lever (Five Year Plans timeline, 18 of 24 PUBLIC q · 75% of bank) IS the recall anchor. No /strategy, no /playbooks, no /trends, no /traps, no /principles, no /reference-tables, no per-playbook detail pages, no STRATEGY_HEADLINE (the numbers would read as parody), no DB tags table (cross-chapter axis impossible at 1 chapter), no new components (timeline renders as inline `<table>`).
│   │   │       · Single page contains: GuideHero with 4-stat StatBlock (24 q · ~1.5 q/paper · ~6 max marks · 41.7% HARD) + honest-stance callout naming the ~1%-of-GAT cap explicitly + 3-row SUBTOPIC_TABLE (FYP 18 · 33% HARD / Govt Schemes 4 · 50% HARD / Intl Trade 2 · 100% HARD) + 12-Plan + 3-Plan-gap chronological timeline (1951 → 2017) with tagline + strategic emphasis per row + POST_PLAN_NOTE (NITI Aayog 2015) + 4-step exam-day playbook ("attempt the easy 1, be cautious on the second, skip current-affairs spillover, target 3–4 of 6") + 2 BrowseLink CTAs (all 24 + HARD-only) + "why this guide is one page" closing section.
│   │   │       · `_data/nda-economics.ts` exports OVERVIEW + SUBTOPIC_TABLE + FIVE_YEAR_PLANS + POST_PLAN_NOTE. Sitemap entry priority 0.7 (vs 0.8 for full landings — flags the thinness). questionResources.ts maps NDA Economics chapter → /guide/nda-economics (Template A pattern — single landing, label "NDA Economics strategy") so /browse cards still get a backlink chip.
│   │   └── nda-current-affairs/           1 indexable page (single-page landing — no sub-routes).
│   │           · **Template D — Theme-Prep Checklist for short-half-life subjects** (Decisions log 2026-05-19 (later, post-Economics)). Bank: 180 q · 8 ch · 31 sub · ~10 q/paper · ~40 max marks · 12.2% HARD.
│   │           · NOT a Template A/B/C variant — the strand axis is RECURRENCE STRENGTH (not skill, not strategic priority), reflecting the bank's empirically-short content half-life: 90% of explicit-year mentions in CA questions are within 12 months of the paper, individual exercise / scheme / award names almost never recur across years. So the guide explicitly does NOT teach facts — it teaches the recurring question SHAPES that DO recur even when specific facts rotate.
│   │           · Strand split:
│   │               - **Anchor themes** (5+ year recurrence · 86 q · ~5 q/paper coverage) — 8 themes including India ↔ partner-country agreements, Cricket tournaments, Military Exercises ↔ partner country, Governance/Policy/UT reform, Health/Education/Welfare schemes, International Orgs, Space Tech, Other Sports
│   │               - **Recurring themes** (3–4 year recurrence · 74 q · ~3 q/paper) — 16 themes
│   │               - **Occasional themes** (1–2 year recurrence · 20 q · drill-if-time-permits) — 7 themes
│   │           · Each anchor theme card carries: question SHAPE (one sentence, durable across years) + prep CHECKLIST (3–6 fact-categories to harvest from this year's news, also durable) + optional durable-anchors callout (G7 nations, UN agency HQs, etc. — the ~15–20% structural facts that DO stay stable) + drill BrowseLink.
│   │           · Single page contains: GuideHero with 4-stat StatBlock + half-life directive callout (the load-bearing framing: "Drill this bank for SHAPE, not for FACTS") + 8-row CHAPTER_TABLE + 8 anchor-theme cards + 16-row recurring themes table + 7-entry occasional themes chip grid + test-day plan callout (target 24 of ~40 marks, attempt 7 skip 3) + 3 BrowseLink CTAs (all 180 + last 3 years + HARD only) + "why this guide is one page" closing section.
│   │           · `_data/nda-current-affairs.ts` exports OVERVIEW + CHAPTER_TABLE + ANCHOR_THEMES + RECURRING_THEMES + OCCASIONAL_THEMES + HALF_LIFE_NOTICE + TEST_DAY_PLAN. Sitemap entry priority 0.6 (thinner than Economics's 0.7 — flags the unusual template shape; the page is shape-calibration not memorise-this). questionResources.ts maps all 8 NDA Current Affairs chapters → /guide/nda-current-affairs (Template A/single-page pattern — single landing, label "NDA Current Affairs strategy") so /browse cards still get a backlink chip.
│   │           · **No DB tags table** (cross-chapter axis impossible — each chapter is a different subject domain). **No new components** — anchor theme cards render as inline JSX `<article>` (same pattern as nda-economics's inline FYP table). `tests/guide-nda-current-affairs-themes.test.ts` locks the invariants — slug uniqueness across all 3 buckets + qCount sums to 180 + recurrence definitions per bucket + every theme's chapter and drillSubtopics resolve under live taxonomy (catches silent broken-CTA failure mode after a rename).
│   └── notes/                             Public, anon-friendly, Google-indexable per-subtopic teaching notes. Dual-purpose: digital-board teaching (Present mode) AND student self-study (Read mode). page.tsx is a 307 redirect to /notes/nda-maths (chapter index landing); nda-maths/page.tsx + nda-physics/page.tsx are real CollectionPages listing every shipped chapter under that subject as cards with live per-chapter PYQ counts via `getNotesTaxonomy`. ISR-cached (`export const revalidate = 3600` on every page; uses cookie-free `createSupabaseAnonClient` so caching isn't broken by request-scoped cookies). Prod warm-state ~200ms.
│       ├── _types.ts                      `SubtopicNote = { subtopicName, title, oneLineDefinition, whyItMatters, concepts: ConceptUnit[], related? }`. **`ConceptUnit` is a discriminated union** (`ConceptUnitFormula | ConceptUnitReference`, gated by `kind: "formula" | "reference"`) — both variants share `slug, name, intuition, definition, pyqExampleId?, selfCheckExample? (independent attempt, full solution behind one reveal), practiceSet? (PracticeProblem[] — Level 1 mastery reps: prompt + answer + optional one-line method), visualizationSlug? (one of 44 SVG diagrams — static or interactive), traps?` and differ only on the core teaching slot: **formula variant** carries `formula?` (FormulaSpec) + required `authoredExample`; **reference variant** carries required `table: ReferenceTable` (2–5 columns × ≥1 row, optional per-row `noteAmber` + `pyqExampleId` for `[Q]` chips, optional table-level `caption`). **Per-concept drill lists are NOT on this type** — they live in `question_concept_tags` (migration 0021) and are resolved at request time via `loadResolvedDrills`. `Slide` discriminated union drives Present mode: title → why → per concept (intro → visualization? → **authored OR reference-table** → self-check? → practice-set? → pyq? → trap* → concept-drill?) → drill. **Five fields render WITHOUT KaTeX** (plain text + SEO metadata): `title`, `oneLineDefinition`, `whyItMatters`, `concept.name`, `formula.label` — keep LaTeX out of them; gated by `npm run notes:latex`. Reference variant adds **`table.columns[]`** to the plain-text-no-LaTeX list (notes-latex-audit enforces). (The faded-example rung was removed 2026-05-29 — it was broken, with the answer always visible.)
│       ├── _components/                   **`NotesChapterLanding` + `NotesSubtopicPage`** — chapter-agnostic page renderers driven by a `NotesChapterRegistration`; the per-chapter route files are thin wrappers that delegate here. Each also exports a `build{Chapter,Subtopic}Metadata` helper for the wrapper's metadata. · `ConceptUnitCard` (read-mode workhorse — branches on `concept.kind`: intuition → definition → formula? → visualization? → **worked example OR reference table** → **collapsed "Practice this concept" disclosure wrapping self-check + Level 1 reps** → bank PYQ → traps → "Drill N more →") · `WorkedExampleAuthored` (formula variant, always-visible worked steps) · `ReferenceTableBlock` (reference variant, bordered N-column table; `compact` prop for revision-sheet use; amber-tinted rows when `noteAmber` is set; per-row `[Q]` chip when row carries `pyqExampleId`) · `SelfCheckCard` (sky; prompt + one Show-solution reveal) · `PracticeSet` (violet; Level 1 reps with per-rep answer+method reveal) · `SubtopicMasteryCheckpoint` (emerald; 5 interleaved bank PYQs via WorkedExampleCard + `pickInterleavedCheckpoint`) · `SubtopicSummary` (per-subtopic auto-derived formula + reference-table + trap recap via `deriveSummary` — reference tables surface in a "Reference tables (N)" section as per-concept disclosures, the full table inline) · `ChapterRevisionSheet` (collapsible chapter-wide sheet, same three-section structure as SubtopicSummary, auto-derived) · `FormulaBlock` · `TrapCallout` · `NotePresenter` (Present-mode overlay) · `visualizations/` — **44 hand-rolled SVG diagrams** dispatched by `concept.visualizationSlug` (shipped in 4 batches — original 9 + High-tier 13 + Medium-tier 17 + Sound-chapter 5; see Decisions log 2026-05-30). **All SVG, no 3D library** (three.js was evaluated + rejected for bundle size / mobile perf / visual consistency). Static figures are server components; interactive + rotatable ones are `"use client"` with `useState`. Rotatable "3-D" uses an orthographic turntable projection that **follows the cursor** (`az -= dx`, `el += dy`). Conventions: indigo wrapper + `max-w-md mx-auto`; SVG `fill` must use a standard opacity step (a non-generated step like `/8` silently falls back to SVG-default black). **Add one:** build the component → add its slug to the `VisualizationSlug` union in `_types.ts` → add a `case` in `renderVisualization` (ConceptUnitCard) → set `visualizationSlug` on the concept in its `_data`. ConceptUnitCard renders the `definition` field via `RichText` (bullets + bold), other prose via `KatexRenderer` (now bold-aware).
│       ├── nda-maths/statistics/          First chapter — 4 subtopics, SSG. `page.tsx` + `[subtopicSlug]/page.tsx` are **thin wrappers** (resolve `getNotesChapterBySlug("nda-maths","statistics")`, delegate to the shared renderers). `_data/chapter.ts` defines `STATISTICS_CHAPTER`; `_data/{central-tendency,dispersion,regression-correlation,frequency-distributions}.ts` each export a SubtopicNote; `_data/index.ts` exposes `STATISTICS_NOTES` + `STATISTICS_SLUGS`. **31 concept units** (6 foundations in central-tendency + 25 bank-anchored) with the full ramp — worked → self-check → Level 1 practiceSet (29 concepts carry one, ~116 reps; the 2 purely-conceptual foundations what-is-data/types-of-data are skipped) → featured PYQ → traps. 12 concepts carry visualizations (originals regression-line-fit / variance-squared-deviations / histogram-bin-slider + High + Medium batch additions). Per-concept drill UUIDs in `question_concept_tags` (**166 tag rows (question × concept pairs) = 100% PUBLIC coverage** of 160 PYQs across 25 bank-anchored concepts; 6 PYQs are dual-tagged).
│       ├── nda-maths/vectors/             Second chapter (reshaped 2026-05-22 as teach-from-scratch with a foundation layer; brought to full parity with Statistics 2026-05-29). 5 subtopics, SSG, same thin-wrapper structure. `_data/index.ts` exposes `VECTORS_NOTES` + `VECTORS_SLUGS`; `subtopicOrder` starts with `position-vectors-section` so foundations precede magnitude/dot/cross/geometry. **28 concept units** (6 foundations in Position Vectors + 22 bank-anchored). Same full ramp — self-check + Level 1 practiceSet (27 concepts, 108 reps; the "what is a vector" foundation is skipped as conceptual) — plus 17 visualizations (Vectors carries the most, including the rotatable 3-D figures: right-hand rule, unit normal, direction cosines, torque, triple-product box). Position Vectors title shows "Foundations: Vectors, Operations, and Position" while `subtopicName` stays "Position Vectors and Section" so DB taxonomy + concept tags resolve. Per-concept drill UUIDs in `question_concept_tags` (**96 tag rows = 96/97 PUBLIC coverage** across 22 bank-anchored concepts; 1 PYQ still untagged).
│       ├── nda-maths/probability/          Third chapter (shipped 2026-05-29, **pedagogy-first** — the bank was reshaped to match the teaching arc, not vice-versa; see [[notes-structure-pedagogy-first]]). 5 subtopics, SSG, same thin-wrapper structure. `_data/index.ts` exposes `PROBABILITY_NOTES` + `PROBABILITY_SLUGS`. **24 concept units** across the arc *define → count → add → multiply → condition → bound*: Classical Probability & Counting (foundations + 5 counting techniques) · Event Algebra & the Addition Rule · Independent Events · Conditional/Total/Bayes · Bounds on Probability. 10 visualizations (originals dice-sum-grid / venn-two-events / probability-tree + High + Medium batch additions). **Bank Phase-D merge** folded the dice/coins/arrangements/balls subtopics into "Probability via Counting" (162 q preserved, NDA Maths subtopics 113 → 109) so the notes teach one coherent Classical page instead of four repetitive ones. Per-concept drill UUIDs in `question_concept_tags` (**162 tag rows = 100% PUBLIC coverage** of 162 PYQs across 24 concepts; the mis-filed "sock" combinations question is cross-tagged to the Classical page).
│       └── nda-physics/sound/              Fourth chapter (shipped 2026-05-30, **first reference-variant chapter** + **first non-Maths /notes subject**). 5 subtopics, SSG, same thin-wrapper structure under a new subject route `/notes/nda-physics`. `_data/index.ts` exposes `SOUND_NOTES` + `SOUND_SLUGS`. **13 concept units** — 7 formula-variant (mechanical-longitudinal-wave, pitch-loudness-quality, wave-equation-and-units, speed-medium-only, beats-formation, echo-and-minimum-distance, reverberation-multiple-reflections) + 6 reference-variant (sound-properties-checklist, frequency-bands-and-scales, ultrasonic-band, sonar-and-applications, sound-transducers, ear-and-instruments). Pedagogical split: Sound's Recall-strand bank tests both technique-driven concepts (v=fλ, beats, echo distance) AND named-fact recall (frequency bands, SONAR, transducer chain, ear anatomy, properties checklist), so each subtopic mixes the two variants. **5 new visualizations** (compression-rarefaction-wave, beats-envelope, ear-anatomy, frequency-spectrum-strip, echo-geometry) — all static SVG, server components. Per-concept drill UUIDs in `question_concept_tags` (**34 tag rows = 100% PUBLIC coverage** of 34 PYQs across 13 concepts — the entire chapter bank, no dual-tags). Two questions had a degenerate set_id (grouped only by Statement-I/II format, no shared passage) — `set_id` unset to standalone during the tagging session.
├── lib/
│   ├── supabase/{client,server,middleware,admin}.ts    four supabase-js variants. `server.ts` exports BOTH `createSupabaseServerClient` (cookie-aware, default for /browse + /dashboard + admin routes) AND `createSupabaseAnonClient` (cookie-free anon client — opts the caller out of forced dynamic rendering, used by /notes pages so `revalidate=3600` ISR actually caches).
│   ├── auth.ts                            getSessionUser, getSessionMember, requireAdmin, **requireEditor** (ADMIN or TEACHER — added 2026-05-26 for migration 0025), HttpError
│   ├── seed.ts                            taxonomy upsert (used by scripts/seed.ts)
│   ├── questions/{filters,query,edit,applyEdit,deleteQuestion,dirty,applyPartial,selectRecentYears,buildShareUrl,facets,formatProvenance}.ts   browse filters ↔ URL (Filters carries `extraIds: string[]`, round-tripped as `?extras=u1,u2,...` with UUID-shape validation; powers principle-drill links so cross-chapter principles return their full reach) · Supabase query builder (queryQuestions sorts `created_at DESC, source_row ASC, id ASC` so within-upload Excel order is preserved; when both subtopicIds and extraIds are set, composes via PostgREST `.or('subtopic_id.in.(...),id.in.(...)')` — other filters AND-narrow on top; queryQuestionsByIds + queryQuestionPreviewsByIds for cart-mode preserve caller's id order) · zod edit schema + hash · DB-side edit application (fans out context change to set siblings sharing set_id; **stamps `last_edited_by` + `last_edited_at` when called with `editorUserId`** and **rejects `payload.visibility !== existing.visibility` with `kind: "forbidden_field"` when `callerRole === "TEACHER"`** — both added 2026-05-26 for migration 0025) · admin delete (cascade + storage cleanup, discriminated-union return) · pure-function form-state diff for edit page · `applyPartial(base, partial)` is the shared cascade (exam → subject → chapter → subtopic) used by both live and staged FilterBar modes · `selectRecentYears(years, n)` powers PYQ presets · `buildShareUrl(filters, origin)` for the Copy-link chip · `mergeAndSortFacets(options, facets)` merges per-id q counts onto chapter/subtopic options, hides zero-count, sorts by count desc with case-insensitive name tiebreak — powers the "Chapter (N)" / "Subtopic (N)" rendering · `formatProvenance({examName, questionNumber, pyqYear, pyqMonth, pyqNote}) → string | null` composes the QuestionCard footer bracket — exam-aware disambiguator (NDA uses month: Apr⇒NDA 1, Sep⇒NDA 2 per the canonical convention; all other exams use pyq_note which carries day+shift). `QuestionRow` extended 2026-05-26 with `questionNumber + pyqYear + pyqMonth + pyqNote` (all nullable) threaded through both `queryQuestions` + `queryQuestionsByIds`. Also composes the PYQ citation on `/notes` + `/guide` `WorkedExampleCard` (via `loadWorkedExamples`' `provenance` field, 2026-05-29) — same convention, two surfaces.
│   ├── cart/{storage,CartProvider}.tsx     pure cart helpers (parse/serialize/add/remove/isInCart, 200-cap mirroring export ceiling) + React Context provider that hydrates from localStorage (key qb:cart:v1) on mount and persists on every change. useCart() consumed by QuestionCard / CartPill / DownloadDialog.
│   ├── sync/{payload,mergeAttemptStats,applyMockSync}.ts  zod payload validator · pure stats merge · orchestrator that resolves taxonomy + dedups by content_hash + merges attempt_stats for the sync receiver
│   ├── rate-limit.ts                      checkAndIncrement(client, bucket, {limit, windowMs}) backed by public.rate_limits + public.rate_limit_increment SQL function (service-role only)
│   ├── members/admin.ts                   admin member-management helpers (2026-05-26). Pure validators (`isValidEmail`, `isValidPassword`, `isValidRole`, `MIN_PASSWORD_LENGTH = 8`) plus DB-side `listMembers / createMember / resetMemberPassword / removeMember / updateMemberRole`. All five DB helpers take the caller's `orgId` explicitly and use `createSupabaseAdminClient` (service-role) — `auth.admin.*` needs it; `org_members` reads gate on admin RLS anyway. `createMember` reconciles against existing auth users (email-taken-other-org / already-member / new-user paths) and stamps `user_metadata.name`. `removeMember` + `updateMemberRole` enforce last-admin protection AND self-protection (caller can't remove or demote themselves). All helpers return discriminated-union results consumed by `/api/admin/members`.
│   ├── dashboard/{stats,activity,countUp}.ts      getDashboardStats (totalQuestions/exams/chapters/daysSinceLastUpload/byExam) · getRecentUploads (cap 5; rows are <Link>'d to /uploads/[id]) · useCountUp hook (eased 0→target animation for stat numbers; respects prefers-reduced-motion). Pure interpolateCount + easeOutCubic exported for testability.
│   ├── upload/{parser,validate,hash,taxonomy,commit,subjectAliases}.ts   upload pipeline (pure → DB). parser reads optional Excel columns Q (question_number), Course, Set; commit stamps upload_job_id + pyq_year/pyq_month/pyq_note + question_number + set_id (`<uploadJobId>:<setLabel>`, scoped per upload) onto every inserted row. Taxonomy auto-create runs in two parallel pre-resolve passes (unique chapter keys, then unique subtopic keys) before the row loop; resolveChapter/resolveSubtopic catch 23505 and re-find the winner's id so concurrent commits adopt rather than fail. `taxonomy.findSubject` prefetches all subjects for the examId into a normalized-name map and walks `subjectAliases.subjectMatchKeys` to resolve common variants (Maths ↔ Mathematics ↔ Math, Physics ↔ Phy, Chemistry ↔ Chem, Biology ↔ Bio) without forcing teachers to match the canonical spelling exactly.
│   ├── upload/{detectCourse,resolveExam,propagateSetContext}.ts   pure helpers run by /api/upload/preview before validateRow. detectCourse → none|uniform|mixed across rows' "Course" column. resolveExam combines the detection with the form's optional examId and the live `exams` list to either resolve a single examId or return a row-level error. propagateSetContext walks rows in order, fills blank Question Context on set siblings from the first non-empty row of the set, and reports row-level errors on drift (different non-empty contexts within a set) or empty (no row in the set has Question Context).
│   ├── upload/{uploadDetail,deleteUploadJob,setUploadPyqMetadata}.ts   /uploads/[id] read aggregate (per-question rows + pyq aggregation: value | null | "mixed") · whole-upload delete (cascades + storage cleanup) · partial PATCH of pyq fields (explicit null clears).
│   ├── uploads/listUploads.ts             paginated upload-jobs query for /uploads index. Uses `count: "exact"` to return `{ items, total }` in one round-trip; recovers from PostgREST 416 (over-the-end range) via a HEAD-count fallback. DEFAULT_PAGE_SIZE = 20.
│   ├── guide/{buildBrowseUrl,principleSlug,resolveTaxonomy,loadWorkedExamples}.ts   server-side helpers for the /guide tree (also reused by /notes). `buildBrowseUrl(partial)` wraps `buildSearchParams` for every "Drill the N questions →" CTA (accepts `extraIds` so principle drills + per-concept notes drills can union subtopic match + curated UUIDs). `principleSlug(name)` → URL-safe slug with Greek-letter transliteration (π → pi, ω → omega) for /principles/[slug]. `resolveTaxonomy(client, examName, subjectName)` returns `{ examId, subjectId, chapters: Map<name, { id, subtopics: Map<name, id> }> }` so editorial content can stay env-agnostic. `loadWorkedExamples(client, ids)` fetches a curated UUID list into WorkedExample rows (text/**context**/difficulty/solution/chapter/subtopic/options + a **`provenance`** string composed via `formatProvenance` from question_number/pyq_*/exam — both added 2026-05-29 so set-bound PYQs rendered inline keep their passage and every card shows a `[Q# · month · year]` citation); RLS-respecting and drops unresolved IDs silently.
│   ├── notes/{chapters,splitNoteIntoSlides,pickInterleavedCheckpoint,deriveSummary,taxonomyCache,subtopicSlugRegistry,loadResolvedDrills}.ts   server-side helpers for the /notes tree:
│   │       · `chapters` — the `NOTES_CHAPTERS` registry (single source of truth for shipped chapters) + `getNotesChapterBySlug` / `getNotesChaptersForSubject`. Each `NotesChapterRegistration` carries examName, subjectName, subjectRoute, **subjectDisplay**, chapterSlug, chipLabel, chapter (ChapterNote), notes, slugs — enough for the shared page renderers to build everything.
│   │       · `splitNoteIntoSlides(note, drillsByConcept)` — pure Present-mode slide derivation (TDD in `tests/notes-slides.test.ts`): title → why → per concept (intro → visualization? → authored → self-check? → practice-set? → pyq? → trap* → concept-drill?) → drill. Second arg is the runtime overlay Map<conceptSlug, questionIds[]>. Minimum 2-slide deck.
│   │       · `pickInterleavedCheckpoint(drillsByConcept, conceptOrder, count=5)` — pure, deterministic round-robin pick across concepts for the per-subtopic mastery checkpoint (TDD in `tests/notes-pick-interleaved-checkpoint.test.ts`).
│   │       · `deriveSummary(note)` — pure; pulls every concept's formula + trap titles into `{formulas, traps}` for both `SubtopicSummary` and `ChapterRevisionSheet` (TDD in `tests/notes-derive-summary.test.ts`).
│   │       · `taxonomyCache` — exposes `getNotesTaxonomy(client, examName, subjectName)`, a process-level memoised wrapper around `resolveTaxonomy` keyed by `(exam, subject)`. Survives between requests in the Node server; bypassed in SSG (each path renders once at build). Restart `npm run dev` after a taxonomy rename to bust.
│   │       · `subtopicSlugRegistry` — aggregates `{subtopicName → subtopicSlug + concept list}` across all chapter notes data, **auto-derived from `NOTES_CHAPTERS`** (today: Statistics + Vectors + Probability; adding a registry entry is enough). Exposes `getSubtopicNotesEntry(name)` for the edit page UI and `validateConceptTag(name, slug, conceptSlug)` for server-side tag validation in applyEdit.
│   │       · `loadResolvedDrills(client, subtopicSlug, concepts)` — Phase 2 reader: calls `loadConceptDrills`, filters each concept's `pyqExampleId` out of its drill list, sorts the remaining UUIDs ascending for stability, returns Map<conceptSlug, string[]> (omits concepts with zero remaining tagged questions).
│   ├── tags/conceptTags.ts                helper API for `question_concept_tags` (migration 0021). `getTagsForQuestion`, `setTagsForQuestion` (diffs added/removed/kept; idempotent on no-op), `getQuestionIdsForConcept`, `loadConceptDrills` (subtopic-scoped, conceptSlug → questionIds[]; the base query under `loadResolvedDrills`). All RLS-respecting.
│   ├── tags/principleTags.ts              helper API for `question_principle_tags` (migration 0023, parallel to conceptTags). `getTagsForQuestion`, `setTagsForQuestion`, `getQuestionIdsForPrinciple`, `loadPrincipleQuestionIds` (batch slug → questionIds map; powers TOP_11 live counts on /guide and the `?principle=<slug>` /browse filter). All RLS-respecting; same horizontal-vs-vertical wall as conceptTags (see Design axes).
│   ├── exam/                              Tier 1 IA helpers (2026-05-18). `examContext.ts` exports the `EXAM_REGISTRY` (slug + display name + per-exam route map) + pure resolvers (`resolveBankHref`, `resolveGuidesHref`, `resolveNotesHref`, `getActiveTab`, `isExamSlug`, `getExamBySlug`). `loadActiveExamContext.ts` is the server-only orchestrator: reads `qb_exam` cookie (RFC-safe underscore name) → validates against registry → fetches exam UUID via cookie-free anon client → returns `{slug, examId, bankHref, guidesHref, notesHref}`. `examHomeStats.ts` is the per-exam head-count helper for `/nda` (avoids the PostgREST 1000-row trap; returns `{examId, totalPublicQuestions}`).
│   ├── links/                             Backlink resolution shared between /browse and /guide. `notesIndex.ts` is the SOURCE-OF-TRUTH `getNotesChapterEntry` (derived from `NOTES_CHAPTERS` registry) used by both `getQuestionResources` and the CHAPTER_TABLE notes chip on `/guide/nda-maths`. `tagNames.ts` exposes pure `getPrincipleName(slug)` (from TOP_11) + `getConceptName(subtopicSlug, conceptSlug)` (from NOTES_CHAPTERS). `getResourceTagsForQuestions.ts` batches per-page principle + concept tag fetches (2 parallel SELECTs against the indexed tag tables, dedups input ids + output tags). `questionResources.ts` is the pure mapping `(examName, subjectName, chapterName, subtopicName, tags?) → {guide, notes}` consumed by QuestionList — chapter→playbook + subtopic→notes by default; tag-specific overrides ("Lever: <principle>" / "Concept: <concept>") when DB tags resolve.
│   ├── notes/chapters.ts                  `NOTES_CHAPTERS` registry — single source of truth for shipped notes chapters (today: NDA Maths Statistics + Vectors + Probability). 6 consumers derive from it: `subtopicSlugRegistry`, `links/notesIndex`, `links/tagNames`, `app/sitemap`, `/notes/nda-maths/page.tsx` chapter cards, `scripts/notes-lint`. Adding a new chapter = write `_data/` + page templates + append ONE entry. Refactored 2026-05-18; was previously a 5-file ritual.
│   ├── reports/                           Question-reports feature (migration 0024). `types.ts` exports the `ReportCategory` + `ReportStatus` enums, display labels, validators, and length caps. `createReport.ts` (client-bound, RLS-scoped) — validates input, resolves question.org_id (RLS-bound lookup → 404 if can't see), INSERTs with denormalized org_id, returns discriminated union (ok / duplicate_open_report on 23505 / question_not_found / invalid_category / invalid_details / error). `listReports.ts` — admin triage list with joined question metadata (PostgREST nested arrays flattened); RLS scopes via `(reporter sees own OR admin sees own-org)`. `updateReport.ts` — admin status transitions; auto-stamps `resolved_at` + `resolved_by` on terminal statuses, clears on revert.
│   ├── storage/
│   │   ├── images.ts                      uploadImage / deleteImage / downloadImage / validateImageUpload (server, uses node:crypto)
│   │   └── imageUrl.ts                    pure-function publicImageUrl — safe to import from client components
│   ├── text/{normalizeNewlines,safeSnippet}.ts   pure helpers. `normalizeNewlines` masks math zones (`\(…\)`, `\[…\]`, `$…$`, `$$…$$`) to opaque placeholders, replaces literal `\n` outside math with real newlines, restores math zones verbatim. Idempotent. Called from upload parser + edit validator so the DB never carries literal `\n` past the boundary; LaTeX commands like `\neq`, `\nu`, `\nabla` inside math are untouched. `safeSnippet(text, maxLen)` truncates with `…` but retreats if the cut would land inside a math delimiter — used by CartItem so cart preview snippets render via KatexRenderer without orphaning a half-open `\(`.
│   └── export/{ommlBuilder,docxBuilder,imageDimensions,groupBySet,stripPassageCount}.ts   LaTeX → OMML, .docx assembly. **Layout (2026-05-25 onwards):** numbering is **flush-left** (`{left: 0, hanging: 0}` + `LevelSuffix.SPACE`) so the question number, wrap lines, options, and Context all sit at column 0 — no hanging indent. Option labels render **lowercase** `(a)/(b)/(c)/(d)` in both question paper and answer key; bank still stores uppercase `A/B/C/D`, the lowercasing happens at render only. A **blank paragraph** sits between every question (singles + set siblings) so the student's eye has a visible boundary; the solution answer-key adds the same blank between each (answer + solution) block (plain answer key stays compact). ommlBuilder.sanitizeOmmlForXml escape-pass over `<m:t>` text content (mml2omml emits raw `<` for LaTeX comparators like `0 < α < 90`, which Word rejects). docxBuilder iterates groupBySet with **student-first set-banner rendering** (2026-05-25): set-of-1 in the export drops the `Set:` framing entirely and renders the passage as inline `Context:` for the lone sibling; set-of-2+ emits `Common context for questions X-Y:` where X-Y is the 1-indexed position of the first + last sibling in the export (computed from a running position counter, so the banner names the actual question numbers wherever the set lands), and the passage is run through `stripPassageCountPhrase` to remove the verbatim "for the three (03) items that follow" claim so the banner doesn't contradict the actual count when the user has selected a subset. Bank text stays verbatim in DB; only the render is normalised. After `Packer.toBuffer`, `patchZip` post-processes both `word/document.xml` (marker-run → OMML) and `word/settings.xml` (injects `<m:mathPr>` with `defJc=left` + `wrapIndent=0` + zero margins so fraction-containing options don't get shifted right by Word's built-in math defaults). imageDimensions reads natural width/height from PNG IHDR or JPEG SOF and scale-to-fits within column-width caps (320×360 for question diagrams, 200×200 for option images), preserving aspect ratio. Falls back to the legacy fixed rectangle if a header can't be parsed. **`stripPassageCount.ts`** — pure helper, regex strip of `for (the next |the )?(two|three|…|\d+)(\s*\(\d+\))?\s*items? that follows?` → `for the items that follow` (or `…that follows` for singular). 10 unit tests cover the common phrasings + idempotency + LaTeX-zone preservation.
├── components/
│   ├── AppHeader.tsx + UserMenu.tsx + ThemeToggle.tsx + PrimaryNav.tsx + ExamPill.tsx   sticky 56px header (Tier 1 IA, 2026-05-18). AppHeader is a Server Component that calls `getSessionMember` + `loadActiveExamContext` in parallel, then renders: brand link (→ /browse non-admin, /dashboard ADMIN; wordmark hides < sm), `PrimaryNav` (client island, 3 tabs Bank · Guides · Notes, active state via `usePathname` + `getActiveTab`, hrefs prebuilt from `examContext`), `ExamPill` (client island Radix Popover, switches the `qb_exam` cookie + `router.refresh()` so server hrefs recompute), `ThemeToggle` (visible to anon + authed), `UserMenu` (org chip hidden < md + avatar trigger) or Sign-in button. UserMenu items are sign-out only. `ThemeToggle` reads/writes `localStorage["theme"]` and toggles `.dark` on `<html>`.
│   ├── OfflineBanner.tsx                  client island listening to `online`/`offline` events; renders a sticky-bottom destructive strip with the WifiOff icon when offline. Mounted globally in layout.tsx so every route gets it. Respects `env(safe-area-inset-bottom)` for iOS.
│   ├── Footer.tsx                         site-wide footer (LWS Pune attribution, all 10 NDA Guide links — Maths, English, Physics, Chemistry, Biology, Geography, History, Polity, Economics, Current Affairs — each with its lucide icon, Statistics Notes link (Vectors + Probability notes not yet linked here), Report-a-question mailto, GitHub link)
│   ├── math/{parseLatex,KatexRenderer,RichText}.tsx   shared LaTeX/text renderers. `parseLatex` (math segmenter — also used by the Word-export `ommlBuilder`, so kept unchanged) plus two pure exports added 2026-05-29: `splitBold` (`**bold**` → runs) + `parseRichText` (blocks: paragraphs + `- ` bullet lists, math-masked; tested). `KatexRenderer` renders inline `**bold**` everywhere it's used (browse/guide/notes); `RichText` is the block renderer (paragraphs + bullet lists + bold + math) used for notes `definition` fields.
│   └── ui/{button,input,label,card,badge,select,dialog,sheet,skeleton,stepper,dropzone}.tsx   shadcn primitives
└── middleware.ts                          Supabase session refresh + /dashboard guard

supabase/
├── migrations/0001..0024_*.sql            apply in order via Supabase MCP.
│       · 0009 — visibility enum + public-read policies (default PRIVATE at the time; flipped to PUBLIC in 0022)
│       · 0010 — sync metadata columns (pyq_year, marks, neg_marks, attempt_stats, source_mock_id, source_app)
│       · 0011 — rate_limits table + atomic increment function
│       · 0012 — partial index on (visibility, exam_id, subject_id, created_at desc) WHERE visibility='PUBLIC' (public hot path)
│       · 0013 — questions.upload_job_id FK ON DELETE SET NULL + partial index
│       · 0014 — data backfill of upload_job_id by (org_id, source_file) when ownership is unambiguous
│       · 0015 — pyq_month + pyq_note text columns on questions
│       · 0016 — question_number text column (original Q-number from Excel "Q" cell)
│       · 0017 — set_id text column + partial index for grouping passage-set siblings (format `<upload_job_id>:<excel-set-label>`, scoped per upload)
│       · 0018 — `get_dashboard_stats(p_org_id)` RPC returning `{total_questions, chapters_covered, by_exam}` as jsonb — bypasses PostgREST's implicit 1000-row cap
│       · 0019 — `get_pyq_years()` SQL aggregate function for the PYQ-year filter chip list
│       · 0020 — `get_chapter_facets()` + `get_subtopic_facets()` RPCs powering "Chapter (N)" / "Subtopic (N)" rendering in the /browse sidebar (context-aware: counts reflect all OTHER active filters)
│       · 0021 — `question_concept_tags` table (composite PK on (question_id, subtopic_slug, concept_slug)) linking bank questions to /notes concept slugs; RLS inherits question visibility (anon reads tags only on PUBLIC questions; ADMIN writes scoped to own-org questions)
│       · 0022 — `questions.visibility` default flipped PRIVATE → PUBLIC + UPDATE flipped remaining 120 PRIVATE rows
│       · 0023 — `question_principle_tags` table (composite PK on (question_id, principle_slug)) linking bank questions to /guide TOP_11 principle slugs; parallel RLS to 0021 — drives live DB-backed counts on `/guide/nda-maths/principles` and the `?principle=<slug>` /browse filter
│       · 0024 — `question_reports` table + `report_category` (8 values) + `report_status` (5 values) enums for in-app reporting. Denormalized `org_id` from question.org_id at INSERT (helper-set, not trigger) so admin triage queries use one indexed lookup. Partial unique index on `(reported_by, question_id) WHERE status='open'` blocks duplicate open reports. 4 RLS policies: insert authenticated-as-self · select-own-reports · select-admin-own-org · update-admin-own-org. No DELETE policy — reports kept for audit.
│       · 0025 — Teacher editor role. New helper `private.current_user_can_edit_questions()` (true for `org_members.role IN ('ADMIN','TEACHER')`). Rewrites `questions UPDATE` → editor; splits the old `admin write options` FOR ALL into `admin insert options` + `editor update options` + `admin delete options`. Storage policies on `question-images` bucket opened from admin-only → editor (TEACHER + ADMIN can upload/update/delete in own-org folder). INSERT + DELETE on `questions` stay admin-only; `upload_jobs` stays admin-only; `chapters`/`subtopics` insert stays admin-only (no taxonomy auto-create by teachers). Adds `last_edited_by uuid references auth.users(id)` + `last_edited_at timestamptz` columns on `questions` + a partial index `(last_edited_at desc) where last_edited_at is not null`. Populated by `applyEdit` when the route hands it a user id.
│       · All RPCs are `security invoker` so RLS still applies (anon → PUBLIC counts only; authenticated → PUBLIC + own-org PRIVATE).
└── seed/
    ├── taxonomy.json                      committed snapshot from MHT_CET_2025_PCM.xlsx
    └── seed-first-org.sql                 manual onboarding for first admin

scripts/
├── extract-taxonomy.ts                    one-shot: regenerate taxonomy.json from a reference Excel
├── seed.ts                                idempotent taxonomy seed (service-role)
├── normalize-newlines.ts                  one-shot backfill: applies `normalizeNewlines` to every question + option. Idempotent. `npx tsx scripts/normalize-newlines.ts` for dry-run; add `--apply` to write back. Not wired to an npm script — it's a one-time clean-up, not a recurring chore.
├── notes-lint.ts                          read-only integrity check for /notes editorial modules (Phase 2 — DB-tag aware). Wired to `npm run notes:lint`. Validates: (1) each note's `subtopicName` resolves under live taxonomy; (2) every `pyqExampleId` is PUBLIC AND lives in the parent module's subtopic (per [[pyqexampleid-must-match-subtopic]]); (3) every DB tag's `concept_slug` matches a known TS concept (catches orphan tags after rename); (4) each TS concept with a `pyqExampleId` has ≥1 DB-tagged question (soft warn for un-tagged bank-anchored concepts). **Foundation concepts (no `pyqExampleId`) are exempt from check 4** — they're deliberate teaching primitives, not bank-anchored, so 0 tags is expected (added 2026-05-22). Exits non-zero on error so it can gate CI later.
├── notes-latex-audit.ts                  read-only LaTeX audit of /notes editorial content. Wired to `npm run notes:latex`. Walks every field, classifies plain-text vs KaTeX-rendered, and reports: (1) LaTeX markup leaking into plain-text fields (title/oneLineDefinition/whyItMatters/concept.name/formula.label), (2) unbalanced `\(...\)` / `\[...\]` delimiters (strips `\\` line-breaks first so `\\[4pt]` row-spacing isn't a false positive), (3) a non-ASCII inventory (informational — prose unicode is fine). Exits non-zero on (1) or (2). Added 2026-05-29.
└── audit-underline-patterns.ts            read-only audit of `\underline{...}` payloads across every PUBLIC question's text/context/solution + every option. Wired to `npm run audit:underlines`. Classifies each hit as **covered** (matches `UNDERLINE_BYPASS_RE` in `src/lib/export/ommlBuilder.ts` → renders as native Word `<w:u>`) or **fall-through** (still routes through OMML borderBox, which Word doesn't reliably render). Run after each new English/Biology batch — silent fall-through is the failure mode. Live state 2026-05-26: 639/639 payloads covered.

tests/                                     101 .test.ts files, 825 tests
├── fixtures/{upload,tinyImage}.ts         in-memory .xlsx fixture builder; 67-byte 1x1 PNG buffer
├── global-teardown.ts                     vitest globalSetup: returns a teardown that sweeps test-prefixed taxonomy (PYQB_/QD_/UD_/EditTest…) + non-canonical auto-creates ("Chemical Thermodynamics" under Chemistry, "Chain Rule" under Differentiation) once after the whole run finishes. Safety net for crashed afterAll hooks; complements per-suite cleanup of run-id-scoped subjects.
├── *.test.ts                              pure unit + DB integration (DB tests skip if env missing)
└── setup.ts                               loads .env.local for tests
```

> **`browse-query.test.ts` is now exam-scoped.** The legacy `expect(totalCount).toBe(150)` assumed LWS Pune held exactly the seeded 150 MHT-CET questions; once admins started uploading additional batches (NDA, more MHT-CET) the count grew. Tests now filter by `examId = MHT-CET` and assert `>= 150` for the seeded baseline rather than an exact number.

## Commands

```sh
npm run dev                # next dev (local)
npm run build              # production build
npm run lint
npm run typecheck          # tsc --noEmit (covers test files too — keep this clean)
npm test                   # vitest run
npm run test:watch
npm run db:seed            # idempotent taxonomy seed (service-role)
npm run extract:taxonomy   # regenerate supabase/seed/taxonomy.json from a reference Excel
npm run notes:lint         # validate /notes editorial modules against live taxonomy + DB tags (read-only)
npm run notes:latex        # audit /notes for LaTeX-in-plain-text leaks + delimiter imbalance + unicode (read-only)
npm run tags:audit         # principle-tag audit (read-only)
npm run audit:underlines   # scan PUBLIC questions for \underline{...} segments not covered by the docx-export bypass
npm run db:types           # supabase gen types (requires SUPABASE_PROJECT_REF env)
npm run prepush            # the chained gate: typecheck → lint → build; run by .githooks/pre-push
```

> **Pre-push gate.** `.githooks/pre-push` runs `npm run prepush` (typecheck + lint + build) before any push. Activated automatically by the `prepare` script (`git config core.hooksPath .githooks`) on `npm install`. `.gitattributes` pins LF on `.githooks/*` so the shebang resolves on Linux/WSL contributors. Escape hatch: `git push --no-verify` — don't reach for it unless you know why.

## Conventions specific to this project

- **Spacing scale.** Page wrappers `p-8`, content cards (CardContent default) `p-6`, compact cards (FilterBar etc.) `p-4`, nested option blocks `p-3`. Pick the smallest tier that contains breathing room — don't mix `p-4`/`p-6` in the same visual rhythm.
- **Typography.** UI text uses `font-sans` (Inter, default). Question/option/solution body text uses `font-serif` (Source Serif 4) — it's how teachers tell content apart from chrome. Both are loaded via `next/font` in `src/app/layout.tsx` and exposed as Tailwind utilities.
- **Icons.** Use `lucide-react` for all icons. Never emoji or text glyphs (▾, ‹, ›) for UI affordances. Standard sizes: `h-3.5 w-3.5` for inline-with-text, `h-4 w-4` for buttons, `h-5 w-5` for the AppHeader logo.
- **Toasts.** `import { toast } from "sonner"`. Fire on every async user-initiated success or failure (save, upload commit, export download, sign-out). Setting an inline `error` state is fine, but never *replace* a toast with it — both should fire so the user can't miss the result while scrolled.
- **AppHeader.** Every page wraps its `<main>` in `<><AppHeader />…</>`. Don't re-add ad-hoc back-links, theme toggles, or sign-out buttons on individual pages — theme lives in the `ThemeToggle` island in the header (visible to anon + authed), sign-out lives in the UserMenu popover (authed only).
- **No Prisma.** Supabase RLS is the security boundary — every read/write goes through `@supabase/supabase-js` so the user's JWT carries through. Reach for `createSupabaseAdminClient()` (service-role) only in server-only contexts (seeds, cross-org admin) — it bypasses RLS by design.
- **Migrations are append-only.** New SQL goes in a new `000N_<name>.sql`. Apply via Supabase MCP `apply_migration`. Never edit a previously-applied file.
- **`.mcp.json` is gitignored.** Holds Supabase + Vercel personal access tokens. Don't commit.
- **Idempotent seeds + uploads.** `scripts/seed.ts` and the upload commit both check existence before insert; re-running is safe.
- **Content hashing for upload dedup.** `content_hash = sha256(normalised question text + sorted options + answer)` plus a unique index on `(org_id, content_hash)`. Re-uploading the same file inserts 0 rows.
- **Text normalization at write boundaries.** Long-form user-authored text (question, context, solution, options) goes through `normalizeNewlines` in the upload parser and the edit validator. The DB never carries literal `\n` (backslash+n) past the boundary; math zones are preserved verbatim. Any new write path that accepts long-form text must call the helper before insert/update — otherwise the render layer can't distinguish "real line break" from "literal text".
- **OMML + math defaults via post-processing.** Two `docx`-library limitations land on the same workaround. (1) `ImportedXmlComponent` wraps imported XML in an `<undefined>` element Word rejects, so we emit placeholder runs and swap them for OMML after `Packer.toBuffer`. (2) The library doesn't expose `<m:mathPr>` for settings.xml, and Word's built-in math defaults (`defJc=centerGroup`, `wrapIndent=1440`, non-zero margins) shift any paragraph containing a 2-D math element (`<m:f>` etc.) ~1″ right of plain-text paragraphs — option `(A) -3/4` ends up out of column with option `(A) -1`. `patchZip` in `src/lib/export/docxBuilder.ts` handles both: marker → OMML in `word/document.xml`, and `<m:mathPr defJc=left wrapIndent=0 lMargin=0 rMargin=0>` injected before `</w:settings>` in `word/settings.xml`.
- **Restart `npm run dev` after adding new routes.** Tailwind JIT can miss files created mid-session. Stop dev, `rm -rf .next`, restart before UI smoke tests.
- **`/notes` LaTeX in body prose only.** Four `SubtopicNote` / `ConceptUnit` fields render as plain text (no KaTeX) AND feed SEO `metadata.description` / JSON-LD where KaTeX would be invalid: `note.title`, `note.oneLineDefinition`, `note.whyItMatters`, `concept.name`. LaTeX in any of these leaks through as raw `\(\hat{i}\)` markup on the page AND in page metadata. **Keep math only in body fields** (`intuition`, `definition`, `formula.latex`, `authoredExample.{prompt,steps,answer}`, `trap.{title,body}`) — those go through `KatexRenderer` correctly. Use Unicode (`î`, `ĵ`, `k̂`, `Σ`, `°`) or plain prose for the four plain-text fields. **Body fields also support `**bold**`** (renders via KatexRenderer / RichText — use it for key terms), and the **`definition` field additionally supports `- ` bullet lists** (rendered by `RichText`): bullet *list-shaped* definitions (term/type/property enumerations), keep narrative ones as prose. The plain-text fields render `**`/`- ` literally — never use them there.
- **Word export: lowercase option labels at render only.** Bank stores `A/B/C/D` uppercase (Excel + DB convention); the `.docx` export renders them as `(a)/(b)/(c)/(d)` in both question paper and answer key (`opt.label.toLowerCase()` in docxBuilder). Browse cards + edit form still show uppercase — only the rendered Word doc is lowercased. If a future surface needs to render bank-style labels, follow the same boundary: lowercase at render, never in the DB.
- **Word export: underline bypasses the math pipeline.** DB convention is `\(\underline{\text{word}}\)` (or `\(\underline{\textit{word}}\)` for italic+underline — biology taxonomy questions). KaTeX renders these in the browser; the docx export detects them via `UNDERLINE_BYPASS_RE` in [src/lib/export/ommlBuilder.ts](src/lib/export/ommlBuilder.ts) and emits a native Word run (`TextRun({ text, underline: { type: "single" }, italics? })`) instead of routing through `temml → mml2omml → <m:borderBox>` — Word doesn't reliably draw the bottom border on inline-math borderBox under our injected math defaults. Trailing punctuation INSIDE the delimiters (`\(\underline{\text{x}}.\)`) is split off into a separate text segment. Any pattern outside the regex (bare `\underline{word}`, chained `\text{}\text{}`, `\textbf` variants, block math) falls through to the OMML pipeline and renders without an underline in Word — run `npm run audit:underlines` after each new English / Biology batch to catch fall-throughs.

## Recurring pitfalls

Patterns that have bitten this codebase more than once. Each one earned its place by causing a real production regression — read before writing new code that resembles the shape.

- **PostgREST 1000-row cap on raw `.select()`.** Any `client.from(...).select(...)` that derives a count, a distinct set, a sort, or any aggregate from the *row payload* (rather than response headers via `count: "exact"` or a SQL aggregate via RPC) is suspect once the table can exceed 1000 rows. PostgREST silently truncates at row 1000 — no error, just wrong answers. **Already bit us three times:** dashboard totalQuestions (migration `0018` get_dashboard_stats), PYQ-year filter dropping older years (migration `0019` get_pyq_years), and upload commit dedup races (fixed by switching to `.upsert(..., { onConflict, ignoreDuplicates: true })` — let the DB unique index enforce dedup, not an in-memory `Set`). **Audit checklist before writing any new `.select()`:** (1) Is the row payload itself the answer, or are you computing something from it? (2) If computing, can the underlying table exceed 1000 rows ever (today or as the bank grows)? (3) If yes, use `count: "exact", head: true` for counts, or write a `security invoker` RPC that does the aggregate in Postgres. Never trust a row-derived count from `.select()` past the first 1000 rows.
- **`next dev` is more forgiving than `next start` for server↔client prop serialization.** Functions and other non-serializable values passed from a Server Component to a Client Component compile cleanly under `next dev` but throw at SSR time under `next start` ("Functions cannot be passed directly to Client Components"). This bit us on the 2026-05-13 Pagination revert. **Mitigation:** the pre-push hook in `.githooks/pre-push` runs `next build` before allowing a push, so this class of bug is caught locally rather than after a Vercel deploy. Don't `git push --no-verify` unless you know exactly why.
- **Source-source subtopic name collisions during bulk chapter merges.** When canonicalizing N source chapters into one target chapter, a bulk `UPDATE subtopics SET chapter_id = '<tgt>' WHERE chapter_id IN ('<src1>', '<src2>', ...)` can fail with `23505` on `subtopics_chapter_id_name_key` even if a pre-flight source-vs-target collision check came back clean — because two sources may share a same-named subtopic that collides only once both are reparented. Bit us on the NDA Physics E&M cluster (5 sources → "Magnetic Field Inside a Solenoid" appeared in two of them). **Mitigation:** process sources sequentially via a `DO $$ FOR mapping IN (VALUES (...)) LOOP ... END $$` block; each iteration redirects colliding questions, deletes colliding source subtopics, reparents the rest, updates remaining question chapter_ids, and deletes the source chapter — so by the time source 2 merges, source 1's content is already in target and the standard collision-handling kicks in. Pattern + full template in `[[reclassification-sql-pattern]]`.
- **Catch-all chapters and subtopics drift until you reclassify per-question.** Buckets named "Mechanics", "Physics", "Error Identification in Sentences" collect every question that doesn't cleanly fit a more-specific name. They look harmless during a chapter-level merge but rot the bank's usefulness for technique drills. **Mitigation:** when a chapter or subtopic holds ≤~50 mixed-topic questions, read each stem and produce a `(question_id, target_canonical_id)` mapping; execute as a `DO $$ FOR m IN (VALUES (qid, tgt)) LOOP ... END $$` that (a) looks up the question's source subtopic, (b) finds same-named subtopic in target chapter, (c) redirects+deletes on collision, otherwise reparents+moves chapter_id. Precedents: Spotting Errors 29 q (2026-05-15 NDA English); Mechanics 19 q + Physics 26 q catch-all chapters (2026-05-15 NDA Physics). Beyond ~50 q the user prefers chapter-level merge + subtopic-cleanup as a follow-up pass.

## Design axes

Load-bearing design distinctions for the project. Each one earns its place by being structural — getting the axis wrong has cost real work, and the answer to "which axis does this live on?" is non-obvious for the named feature areas.

### Tag taxonomies — principles (horizontal) vs concepts (vertical)

**`/guide` principles** and **`/notes` concepts** are different kinds of tag and must NEVER share a schema or be merged into one taxonomy.

| | Principles | Concepts |
|---|---|---|
| Axis | Horizontal — cross-subtopic | Vertical — within-subtopic |
| Cardinality | ~20 in NDA Maths | ~5–10 per subtopic × hundreds = thousands at scale |
| Storage | TS modules (`/guide/.../principles.ts`) + curated `extraQuestionIds` | DB table `question_concept_tags` (migration 0021) |
| Authoring | Editorial — hand-curated, slow update cadence | Per-upload — admin tagging UI or chat session, fast cadence |
| Value comes from | **Selectivity** (only the top-20, hand-picked) | **Coverage** (every relevant question tagged) |
| Identity question | "What unifying lens does this question demand?" | "What specific technique within this chapter?" |

**The wall — non-negotiable:**
- DON'T migrate `/guide` principles to a DB table. Hand-curation is the signal-to-noise mechanism; "scaling" them dilutes their value.
- DON'T bundle principles into `question_concept_tags` via a `tag_kind` discriminator. Flattens the strategic distinction.
- DO share infrastructure at the seam: one admin tagging UI on `/questions/[id]/edit`, one notes-lint validator, future LLM-suggest can suggest both kinds. Sharing UI ≠ sharing schema.

**Why this earned a section:** The initial concept-tags proposal (Phase 1, 2026-05-17) was a unified `question_tags` table. User pushed back; I conceded. Both axes shipped separately (principles still in TS, concepts in migration 0021) and the wall has held. This section exists so future tag-related features (admin UI, LLM-suggest, AI tutor grounding, MHT_CET_AI integration) don't re-litigate it from scratch.

### Cross-link backlinks — chapter-level (automatic name lookup) vs tag-level (manual DB-tagged override)

The `/browse` QuestionCard chip row (Tier 1 + Tier 1.5, 2026-05-18) renders **two distinct classes of backlink** and the wall between them matters. Same dropdown of chips, two completely different authoring loops and data sources.

| | Chapter-level chips | Tag-level chips |
|---|---|---|
| Examples | "Playbook: Sound" · "Concept notes" | "Lever: Vieta" · "Concept: Weighted Mean" |
| Source | Name-based lookup at render time | DB row in `question_principle_tags` / `question_concept_tags` |
| Authoring cost | **Zero per question** — purely a function of `(exam, subject, chapter, subtopic)` | **Manual per question** — chat tagging session, INSERT with `ON CONFLICT DO NOTHING` |
| Coverage | 100% of questions whose chapter has a guide / subtopic has notes | Only questions explicitly tagged (today: 488 principle tags · 262 concept tags) |
| Failure mode | Silent break when DB chapter renamed (no lint catch yet — see ROADMAP "notes-lint guide-side rename validation") | Silent fallback to generic chip when slug doesn't resolve via `getPrincipleName` / `getConceptName` |
| Override behaviour | The default; renders for all qualifying questions | When present, **replaces** the chapter-level chip for the same axis (more specific wins) |
| Resolver | `getQuestionResources({exam, subject, chapter, subtopic})` in `src/lib/links/questionResources.ts` | `getQuestionResources({...}, tags)` — same function, second arg from `getResourceTagsForQuestions` batch |

**The wall — design intent:**
- DON'T expand the chapter-level lookup to do "fuzzy" matching when a chapter rename happens. The failure mode (chip disappears) is the correct signal — fix the rename, don't paper over the drift.
- DON'T attempt to back-fill tag-level chips from the chapter-level data (e.g. auto-create a `Lever: <chapter-name>` chip when no principle tag exists). That destroys the curated-vs-automatic distinction and floods low-leverage questions with meaningless tag chips.
- DO let tag-level chips quietly fall through to chapter-level chips when the tag's slug doesn't resolve. Better a generic "NDA Maths strategy" chip than a broken `/principles/<unknown-slug>` link.
- DO share infrastructure at the seam: both axes flow through the same `getQuestionResources(input, tags?)` function, both render via the same `<ResourceChip>` component in QuestionCard. The two-tier system is a function-signature distinction (optional second arg) not two separate code paths.

**Why this earned a section:** The 2026-05-18 Tier 1.5 ship added tag-level overrides AFTER the Tier 1 chapter-level chips were in production. The temptation in the moment was to refactor into "one unified backlink table" or to have tag-level chips deduplicate against chapter-level chips. Both ideas are wrong — they conflate two different authoring loops (curated tagging session vs no-touch render-time mapping) and two different coverage shapes (long-tail per-question vs blanket-per-chapter). The split shipped cleanly because the function signature preserved the asymmetry: `tags?` is optional, the resolver short-circuits chapter-level chips when a more-specific tag-chip exists, and untagged questions still get the generic chip — graceful fallback throughout. Sister wall to the tag-taxonomies axis above.

### Guide strand axis — execution-mode (chapter-partitioning) vs tier-style (chapter-tiering)

Within Template B variants, the **strand split** is the strategic axis the strategy page renders against. There are two fundamentally different ways to construct it, and they are NOT interchangeable — picking the wrong one produces a strategy axis that sorts questions by nothing meaningful.

| | Execution-mode strands | Tier-style strands |
|---|---|---|
| Subjects | NDA English (R/Rule/Reason), NDA Chemistry (R/Rule/Calculate), NDA Biology (R/Apply/Verify), NDA Geography (R/Apply/Verify) | NDA History (Cornerstone/Foundation Recall/Quick-Win) |
| Strand semantics | Each strand is a SKILL bucket (recall a fact / apply a rule / trace a mechanism / evaluate a statement). Chapters get grouped into a strand by their dominant execution mode. | Each strand is a STRATEGIC TIER (cornerstone weight / foundation recall / quick-win). Chapters get grouped into a strand by their bank share + %HARD profile. |
| Recall slot | Stable — there's always a Recall strand (named-fact recall is universal in Indian entrance exams). | Stable in spirit (Foundation Recall) but the slot SEMANTICS bent — it now groups chapters by tier, not by execution mode. |
| When to pick | Execution modes PARTITION chapters: one cluster of chapters is 60%+ dominated by one mode, others are 20%- on that mode. | Execution modes are UNIFORM across all chapters (25–45% everywhere) — can't partition. AND/OR one chapter has dominant bank share (40%+) that warrants its own Cornerstone tier. |
| Decision probe | For each candidate mode (recall/rule/apply/verify/calculate/reason/multi-statement/date-anchored), count `q × chapter_spread` AND per-chapter %. Top mode at 60%+ in one cluster = execution-mode strands work. | Top mode is 25–45% of every chapter = uniform = falls back to tier-style. Use bank-share + %HARD per chapter as the partition signal instead. |
| HARD calibration | Strand-level (densest-HARD strand called out — Geography's Apply at 23% avg HARD). | Implicit in the tier-structure — Cornerstone IS the densest-HARD tier by construction (History's Cornerstone = Modern India 34% HARD). |

**The wall — design intent:**
- DON'T force execution-mode strands when modes don't partition. History's multi-statement (70 q × 4 ch · 43% HARD) and date-anchored (61 q × 4 ch · 43% HARD) cut UNIFORMLY across chapters — a Bio/Geo-style R/Apply/Verify split would force lopsided cuts (Recall 44 q : Date 163 q : Verify 53 q if grouped by chapter's dominant mode). The strategy axis would sort by nothing meaningful.
- DON'T force tier-style strands when execution modes DO partition cleanly. English's Vocab/Idioms (Recall) and RC/Cloze (Reason) chapters are genuinely different SKILL types, not just different priorities — a Cornerstone/Foundation/Quick-Win split would lose that information.
- DO let the decision probe pick. Test execution-mode first (Template B default); fall back to tier-style only when modes are uniform.
- DO share infrastructure at the seam: both axes use the same `STRATEGY_STRANDS` data shape (each strand has `id`, `label`, `pitch`, `approach`, `chapters: StrandChapter[]`) and the same strategy-page renderer. The two-axis distinction is in the BUCKET MEANINGS (`PlaybookBucket = "recall" | "apply" | "verify"` for Bio/Geo vs `"cornerstone" | "foundation" | "quickwin"` for History) — same code path, different semantics.

**Why this earned a section:** The 2026-05-19 NDA History ship was the first time Template B's strand axis itself bent. Before History, every Template B variant had bent the second + third SLOTS within an execution-mode axis (Rule→Apply for Bio, Calculate→Verify for Chem) — but the axis itself was always execution-mode. History exposed the next level: when execution modes don't partition chapters at all, the axis itself has to change. The temptation in the moment was to force one of the existing slot-bend patterns onto History (most naturally R/Apply/Verify like Bio + Geo) — that would have shipped a strategy page where students see "Apply chapters" and "Verify chapters" labels but no chapter actually leans either way, sorting by nothing. The tier-style fallback shipped cleanly because the strand data shape was already general (chapters grouped under labelled strands with a pitch + approach) — only the bucket vocabulary changed. Sister wall to the tag-taxonomies + cross-link-backlinks axes above: in all three, the right call is to preserve an asymmetric distinction at the shared seam rather than to flatten for elegance.

## Guide structure templates

Three guide structures have shipped. They serve different bank shapes; a fourth subject's guide should pick from these (or pivot again) by analysing the bank first, not by copying precedent.

### Template A — principles-first (NDA Maths)

Use when the bank has **cross-chapter levers** (one principle spans ≥4 chapters) and **non-flat %HARD** (multiple chapters > 20% HARD).

- **Routes:** landing + `/strategy` (Tier A/B/Skip) + `/principles` (catalog) + `/principles/[slug]` (deep dives) + `/compound-tricks` + `/trends` + `/traps`.
- **Tag infra:** DB-backed `question_<X>_tags` table (today: `question_principle_tags`, migration 0023). Hand-curated TOP_N; counts + %HARD + chapter spread computed live from the tag table at request time.
- **Strategy axis:** %HARD-driven tiering. Chapters with high q-count AND low %HARD = Tier A; high q-count + high %HARD with one drillable subtopic = Tier B cherry-pick; everything else = skip.
- **Editorial data shape:** `OVERVIEW + CHAPTER_TABLE + TIER_A/B + SKIP_LIST + TOP_11 principles + DOMAINS long-tail + COMPOUNDS + DRIFT_ROWS + TRAPS + per-principle DETAILS`.

### Template B — playbooks-first (NDA English, NDA Chemistry, NDA Biology, NDA Geography, NDA History)

Use when the bank has **sealed chapters** (largest cross-chapter lever < ~30 q). Pure Template B also requires **flat %HARD** (≤2 chapters > 15%); the Geography variant relaxes that gate (see "B + non-flat %HARD" below).

- **Routes:** landing + `/strategy` (skill buckets, not tiers) + `/playbooks` (catalog) + `/playbooks/[slug]` (deep dives — one per major subtopic OR one per chapter — see variants below) + `/<subject-specific-page>` (e.g. `/vocab-families` for English, `/common-compounds` for Chemistry) + `/trends` + `/traps`.
- **Tag infra:** NONE. Cross-chapter horizontal axis is too thin to justify a DB table. Drill CTAs use `subtopicIds` or `chapterIds` from `resolveTaxonomy` at request time — same machinery as the maths long-tail DOMAINS principles.
- **Strategy axis:** Skill-type buckets, not %HARD tiers. The middle bucket is the variable slot — pick it from the subject's natural seams:
  - **English** uses Recall (Vocab + Idioms — memorisation) / **Rule** (Errors + Grammar — rule application) / **Reason** (RC + Cloze + Rearrangement + FIB — context-driven).
  - **Chemistry** uses Recall (Carbon + Matter + Industrial + Metals + Hydrogen + Everyday Life + Practical — fact recall) / **Rule** (Atomic Structure + Acids/Bases + Reactions + Bonding — rule application) / **Calculate** (Mole Concept — small numeric strand). Chemistry has no chain-reasoning lever (no analogue of RC author-stance) but does have a small but distinct numeric strand worth surfacing as its own bucket.
  - **Biology** uses Recall (Human Physiology + Cell Biology + Microbiology + Biodiversity + Genetics — named-fact recall) / **Apply** (Plant Biology + Reproduction — mechanism-tracing: photosynthesis flow, osmosis direction, inheritance ratios, pollination genetics) / **Verify** (Ecology + Biochemistry — multi-statement true/false evaluation, the bank's second-largest question shape at ~24 q). Biology has NO numeric strand (no analogue of Mole), no Rule-application lever beyond pattern-application (which collapses into Recall in practice), and only 4 chain-reasoning HARDs across 190 q — too thin for a Reason strand. The **third slot bent to Verify** to capture the dominant non-recall execution mode; the **second slot bent to Apply** to surface the mechanism-tracing chapters that carry 4 of the bank's 5 HARDs. So Biology's Recall + Apply + Verify split is unique even within Template B — the Rule slot itself bent.
  - **Geography** uses Recall (Indian Geography Economy + Indian Geography Physical + World/Human — named-fact recall: rivers/peaks/states/crops/minerals/ports) / **Apply** (Climatology + Earth's Structure — mechanism-tracing: cyclogenesis, plate tectonics, weathering, rock-cycle classification) / **Verify** (Earth in Space + Oceanography — multi-statement evaluation: "consider the following statements about terrestrial planets / cold ocean currents"). Same Recall/Apply/Verify split shape as Biology; the unique twist is that Geography has **non-flat %HARD** (4 of 7 chapters >15% HARD) — see "B + non-flat %HARD" below for the variant pattern. Initial guess was a Map/Concept/Current-Events split (in the deprecated "other subjects may need different splits" prose); SQL analysis showed Recall/Apply/Verify fits better because Geography's multi-statement shape (92 q) dominates the non-recall execution mode.
  - Current Affairs (not yet a guide) might still need a different split — one-bucket-many-themes. The Recall slot is universally stable; the OTHER TWO slots bend to the subject's natural seams.
- **Playbook granularity:** English uses per-subtopic playbooks (chapters are skill buckets, each subtopic is a distinct question type — 16 playbooks across 8 chapters). Chemistry uses per-chapter playbooks (subtopics well-cleaned but thin at 2–5 q each — 12 playbooks 1:1 with chapters, same rationale as physics). Biology uses per-chapter playbooks too (9 playbooks 1:1 with chapters; subtopics thin, max 13 q on HP.Circulatory + MD.Pathogens). Geography uses per-chapter playbooks too (7 playbooks 1:1 with chapters; subtopics avg ~5 q, max 36 q on Agriculture). Pick based on whether subtopics are large enough to anchor a standalone deep-dive.
- **Subject-specific page:** Required for Template B in practice. English's `/vocab-families` (13 themed word clusters); Chemistry's `/common-compounds` (6 themed name↔formula↔use clusters, ~50 compounds, rendered via the new `CommonCompoundsTable` component); Biology's `/reference-tables` (4 themed clusters, ~52 named-fact pairs — rendered via `BiologyReferenceTables`, **multi-domain**: each cluster carries its own column headers); Geography's `/reference-tables` (4 themed clusters, 62 named-fact pairs — Indian Rivers / Mountain Peaks / Mineral & Crop Producer States / Local Winds + Climate Zones — rendered via the new parallel `GeographyReferenceTables` component, same multi-domain shape as Biology since Geography's named facts also span 4 different domains). All four follow the same recall-anchor pattern — a flat-list reference the recall strand drills against.
- **Editorial data shape:** `OVERVIEW + CHAPTER_TABLE + STRATEGY_BUCKETS + PLAYBOOKS + per-playbook DETAILS + <subject-specific clusters/families> + DRIFT_ROWS + TRAP_SHAPES`.

#### Template B variants — pure-B strand axis (English / Chemistry / Biology)

Three subjects fit pure Template B (sealed chapters + flat %HARD) but differ on the strand split and on subject-specific page shape. Biology in particular shows that even the SECOND slot can bend (Rule → Apply) when the bank has no rule-application lever. Geography is NOT in this table — its strand split is identical to Biology's; what makes Geography different is its non-flat %HARD, covered in the second table below. Use this table as a quick decision aid for the next pure-Template-B subject — match each axis to the new subject's seam, don't reflexively copy any column.

| Axis                       | English (the original B)                                                                   | Chemistry (variant 1)                                                                                | Biology (variant 2)                                                                                  | What decided                                                                                         |
|----------------------------|--------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------|
| **Bank size**              | 900 q · 8 ch · 29 sub                                                                      | 262 q · 12 ch · 47 sub                                                                               | 190 q · 9 ch · 39 sub                                                                                | Bank shape, not template choice.                                                                     |
| **Strand split**           | Recall / **Rule** / **Reason** — Vocab+Idioms · Errors+Grammar · RC+Cloze+Rearrangement+FIB | Recall / **Rule** / **Calculate** — Carbon+Matter+Industrial+Metals+Hydrogen+Everyday+Practical · Atomic+Acids+Reactions+Bonding · Mole | Recall / **Apply** / **Verify** — HP+Cell+Micro+Biodiversity+Genetics · Plant+Reproduction · Ecology+Biochemistry | Biology has no rule-application lever (the Rule slot itself bent → Apply) and no numeric strand (Calculate doesn't apply). |
| **Bucket shapes**          | Recall (memorise) / Rule (apply principle) / Reason (chain-context inference) | Recall (memorise) / Rule (apply principle) / Calculate (numeric plug-in) | Recall (memorise) / Apply (mechanism-trace) / Verify (multi-statement T/F evaluation) | What execution modes the bank's questions ACTUALLY demand.                                          |
| **% of bank**              | Recall 45% / Rule 25% / Reason 30%                                                          | Recall 55% / Rule 42% / Calculate 3%                                                                  | Recall 69% / Apply 22% / Verify 8% (by chapter grouping; by question shape Recall is 82%)             | Calculate is small (3%) because Mole is one chapter. Verify is small (8%) because Ecology + Biochemistry are the only chapters where the shape dominates. |
| **Playbook granularity**   | Per-subtopic (16 playbooks across 8 chapters) — chapters are skill buckets, subtopics are distinct question types large enough to anchor a deep-dive | Per-chapter (12 playbooks 1:1 with chapters) — subtopics well-cleaned but thin at 2–5 q each, would proliferate without adding clarity | Per-chapter (9 playbooks 1:1 with chapters) — subtopics thin (max 13 q), per-subtopic split would proliferate | Largest subtopic size. English's biggest is Synonyms 150 q; Chemistry's Allotropes 15 q; Biology's Circulatory 13 q. |
| **Subject-specific page**  | `/vocab-families` — 13 themed word clusters mined from PYQ underlines                       | `/common-compounds` — 6 themed name↔formula↔use clusters, ~50 compounds                              | `/reference-tables` — 4 themed clusters (diseases↔pathogens, vitamins↔deficiencies, hormones↔glands, scientists↔discoveries), ~52 named-fact pairs | The recall-strand's highest-leverage flat-list artefact. All three are 4-pass active-recall references.   |
| **Subject-specific renderer** | Inline JSX in the page (no dedicated component — clusters are simple `<ul>` lists)      | New `CommonCompoundsTable` component (single-domain — every cluster is name↔formula↔use)             | New `BiologyReferenceTables` component (MULTI-domain — each cluster has its OWN column headers since the 4 fact domains are different shapes) | Compound table needs richer per-row affordances than word lists; reference-tables need per-cluster column headers since the domains differ. |
| **Trends headline framing**| "Grammar exploded post-2024" — chapter-mix shifts are the story                            | "Paper has NOT hardened" — explicit negation of Physics's "hardened 22×" framing                     | "Paper has NOT hardened — only 4 HARDs across 190 q over 10 yrs" — even more explicit absence-of-drift framing (Biology is flatter than Chemistry) | Whether the bank's drift has a sharp story or a stable one. Frame the absence of drift explicitly when a reader might import the wrong calibration from a sibling guide. |
| **Trap-page bucketing**    | 9 trap shapes by Recall/Rule/Reason — meaning-based (near-synonym, near-idiom, near-passage-inference) | 12 trap shapes by Recall/Rule/Calculate — identity-confusion (acid-source swap, diamond-graphite property flip, scientist-discovery swap) | 13 trap shapes by Recall/Apply/Verify — paired-fact-swap (disease↔pathogen, vitamin↔deficiency, hormone↔gland, RNA-vs-DNA virus, scientist↔discovery) + apply-flips + multi-statement traps | Distractor genre. English: semantic. Chemistry: identity. Biology: paired-fact. Physics: formula misapplication.            |
| **DriftTable `rowLabel`**  | `"Chapter"`                                                                                 | `"Chapter"`                                                                                          | `"Chapter"`                                                                                          | All three flow chapter-rows (vs Maths' principle-rows). Shared.                                          |
| **`RelatedX` component**   | `RelatedPlaybooks` (bound to English PLAYBOOKS)                                            | `RelatedChemistryPlaybooks` (bound to Chemistry PLAYBOOKS)                                           | `RelatedBiologyPlaybooks` (bound to Biology PLAYBOOKS)                                              | Link path is hard-coded per guide. Don't parameterise until a seventh guide forces the abstraction (6 parallels shipped through Geography without forcing it). |

**The strand-axis lesson (updated by Biology + Geography + History):** ONLY the Recall slot is universally stable across Template B subjects — and even that can manifest as one strand (Recall) or part of a larger Foundation Recall tier. The SECOND slot is usually Rule but can bend to Apply (Biology + Geography mechanism-tracing) or — when even execution-mode is uniform across chapters — to TIER (Cornerstone for History's dominant Modern India). The THIRD slot is the most variable — Reason (English chain-context), Calculate (Chemistry numeric Mole strand), Verify (Biology + Geography multi-statement evaluation), Quick-Win (History's low-%HARD World History). Picking the strand axis itself (execution mode vs strategic tier) is the highest-leverage decision in a new Template B guide. **History was the first subject where execution-mode strands failed** — multi-statement and date-anchored modes are uniform across all 4 chapters, so they couldn't partition. Tier-style strands (Cornerstone / Foundation Recall / Quick-Win) were the fallback.

#### Template B variants — strand-axis fork (execution-mode vs tier-style)

| Strand axis | Subjects | When to pick | Examples |
|---|---|---|---|
| **Execution-mode strands** | English, Chemistry, Biology, Geography | When execution modes (Recall/Rule/Reason etc.) partition chapters into recognisable clusters — each chapter has a DOMINANT execution mode that distinguishes it from others. | English: Vocab/Idioms = Recall vs RC/Cloze = Reason. Bio: HP/Cell = Recall vs Plant/Reproduction mechanism-tracing = Apply. |
| **Tier-style strands** | History, Polity | When execution modes are bank-wide-UNIFORM (cut across all chapters at similar %), OR the bank is too small + chapter count too low for skill-bucket labels to be more actionable than strategic-priority labels. | History: multi-statement 70 q × 4 ch + date-anchored 61 q × 4 ch are uniform — but Modern India is 47% of bank (vs next chapter 20%), creating a Cornerstone/Foundation/Quick-Win priority axis. Polity: only 90 q · 4 ch · avg 5 q/paper (smallest GAT section); multi-statement partition is partial (4.5–50% across 4 chapters); strategic-priority labels name-drop each chapter directly. |

**Decision probe:** For each candidate execution mode, count `q × chapter_spread` AND look at the per-chapter distribution. If the top mode is 60%+ of one chapter and 20%- of others, execution-mode strands work. If it's 25–45% of every chapter, modes are uniform → fall back to tier-style. **Also drop to tier-style** when the bank is small enough (≤100 q · ≤4 ch) that strategic-priority labels are more actionable than execution-mode labels at this scale — Polity's call.

**Tier-style third-tier inversion** (History vs Polity): tier-style strands have a stable Cornerstone (dominant chapter) + Foundation Recall (recall-heavy mid-chapters) — but the THIRD tier's semantics depend on the smallest chapter's profile. History's World History is small AND easiest (20% HARD, lowest in subject) → **Quick-Win** label. Polity's World Polity is small BUT HARDEST (42% HARD, highest in subject, 50% multi-statement) → **Specialist Wildcard** label (small + high-reward, opposite framing). Don't reach reflexively for "Quick-Win" — verify the third tier's %HARD profile against the rest of the subject first, and relabel when the smallest chapter is the HARDEST.

#### Template B variants — %HARD-handling axis (all 6 B subjects)

A separate axis from strand split: how does each Template B variant deal with its %HARD shape? Three of the four early B subjects were flat (no special handling needed); Geography forced the discovery of a non-flat-%HARD fork; History sharpened the case for the tier-style strand-axis fork (above); Polity added a second tier-style subject with INVERTED third-tier semantics.

| Axis                                  | English                                          | Chemistry                                            | Biology                                             | Geography                                                                                                                                                       | History                                                                                                                                                          | Polity                                                                                                                                              | What decided                                                                                                                                                       |
|---------------------------------------|--------------------------------------------------|------------------------------------------------------|-----------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **%HARD spread**                      | flat (1 ch >15%: Sentence Rearrangement 22%)     | flat (0 ch >12%; max 11.1% Mole)                     | flat (max 7.7% on 13-q chapter; 4 ch at 0% HARD)   | **non-flat** (4 of 7 ch >15%: Climatology 28% / IG Economy 24% / Earth's Structure 20% / Earth in Space 18%)                                                     | **non-flat** (ALL 4 ch >15%: Modern 34% / Medieval 28% / Ancient 27% / World 20%)                                                                              | **borderline non-flat** (only 1 of 4 ch clearly >15%: World 42% outlier; other 3 cluster 13.6 / 15.0 / 16.7%)                                       | The pure-B vs B-variant fork. Geography + History fail the "≤2 chapters >15% HARD" gate; Polity is borderline but the outlier is small + immovable.                  |
| **HARD concentration within chapters**| n/a (flat)                                       | n/a (flat)                                           | n/a (flat)                                          | **diffuse** (Climatology HARD 4-3-3-3-2-1 across 6 subs; Earth's Structure 5-4-3-2-1-0-0 — no chapter has 1–2 subtopics carrying all the HARD)                  | **mixed-immovable** (Modern's top-2 share 63% LOOKS concentrated, but the top sub is Freedom Movement 56 q · 46% of chapter — the chapter's PRIMARY content, cannot be skipped) | **small-and-immovable** (World Polity's HARDs concentrate in UN 60% + Democracy 40% — both 5-q subs that ARE the chapter)                          | The B-variant vs Template-C fork. Concentrated + skippable → Template C (Physics). Diffuse OR concentrated-but-immovable → Template B + non-flat variant.            |
| **%HARD handling on the strategy page**| none needed                                      | none needed                                          | none needed                                         | strand-level calibration — densest-HARD strand (Apply at 23% avg) called out in hero subtitle; no per-chapter posture cards                                      | Cornerstone strand IS the densest-HARD strand (Modern 34% HARD = 1 chapter strand) — strand calibration is implicit in the tier-strand structure                | Specialist Wildcard strand IS the densest-HARD strand (World 42% HARD = 1 chapter strand) — strand calibration is implicit in the tier-strand structure | Strand-level calibration carries the non-flat HARD signal without per-chapter DrillPosture. For History + Polity, the tier-style strand structure already encodes it. |
| **Per-chapter `DrillPosture` overlay?** | no                                              | no                                                   | no                                                  | **no** (despite non-flat %HARD)                                                                                                                                  | **no** (despite non-flat %HARD AND apparent concentration — but the concentrated pool is immovable)                                                              | **no** (only 1 ch clearly non-flat AND the chapter is too small — 12 q — for DrillPosture to add signal)                                            | DrillPosture only earns its place when HARD is BOTH non-flat AND concentrated AND skippable. Geography fails the concentration test; History fails the skippable test; Polity fails the bank-size test. |
| **Strand axis**                       | execution-mode (Recall/Rule/Reason)              | execution-mode (Recall/Rule/Calculate)               | execution-mode (Recall/Apply/Verify)                | execution-mode (Recall/Apply/Verify)                                                                                                                             | **tier-style (Cornerstone/Foundation Recall/Quick-Win)** — execution modes were uniform across chapters, forcing the fallback                                    | **tier-style (Cornerstone/Foundation Recall/Specialist Wildcard)** — execution modes partition only partially (4.5–50%) AND bank is small (90 q · 4 ch); strategic-priority labels more actionable. Third tier INVERTED from History's pattern (smallest chapter is HARDEST not easiest) | When execution modes partition chapters → execution-mode strands. When uniform OR bank is small enough that strategic-priority labels are more actionable → tier-style. |
| **Trends headline framing**           | "Grammar exploded post-2024" — chapter-mix shifts | "Paper has NOT hardened" — explicit negation of Physics's 22× framing | "Paper has NOT hardened — only 4 HARDs across 190 q over 10 yrs" | "Paper has NOT consistently hardened — %HARD bounces 6%→42% with no monotonic trajectory; 2018 + 2025 outlier highs" | "Paper has NOT consistently hardened — %HARD bounces 19%→42%→10%, 2021 peak, 2026-1 lowest" + secondary callout "chapter mix shifted: Modern dominated 2017–20, Ancient surged 2022–24 from ~2 q/yr to 9–10 q/yr" | "Paper has NOT consistently hardened — %HARD bounces 0%→50% with noise inherent to small per-paper samples (avg 5 q/paper)" + secondary callouts "2026 NDA-1 was hardest + most Govt-Structure-heavy 7-of-10; 2021 FR/DPSP outlier 7-of-14"   | Frame the absence/shape of drift explicitly when a reader might import the wrong calibration from a sibling guide. Polity's noise floor is amplified by the small per-paper sample. |

**The %HARD-handling lesson:** Template B's "flat %HARD" gate is the most easily relaxed prerequisite. When %HARD is non-flat but HARD is **diffuse within chapters**, stay in Template B and switch from "no HARD signal" to "strand-level HARD calibration" — don't escalate to Template C. The escalation to C only earns its place when HARD pools are concentrated enough that "drill all EASY subs, skip the HARD ones" is a meaningful piece of strategy.

### Template C — chapter-playbooks + skill-strand + formula compendium (NDA Physics)

Use when **neither A nor B fits cleanly**: cross-chapter lever too thin for principles (max < ~30 q × ≥4 ch — fails A's gate) AND %HARD is mid-spread (3–6 chapters > 15% HARD — too non-flat for B's strategy to capture). NDA Physics: max cross-chapter lever 12 q × 6 ch; 5 of 14 chapters > 15% HARD with HARD concentrated in 1–2 specific subtopics per chapter (Resistance & Circuits 42%, Calorimetry 31%, Thermo Processes 33%, Buoyancy 31%, etc.).

- **Routes:** landing + `/strategy` (skill strands with %HARD-aware DrillPosture overlay) + `/playbooks` (catalog) + `/playbooks/[slug]` (deep dives — **one per chapter**, not per subtopic; physics chapters ARE the natural unit, subtopics too fine at 1–3 q each) + `/formulas` (single-page compendium — NEW page kind) + `/trends` + `/traps`.
- **Tag infra:** NONE (same rationale as B). Per-playbook drills use `chapterIds` + optional `subtopicIds` from `resolveTaxonomy` at request time.
- **Strategy axis:** Skill strand (Recall / Apply / Reason) PLUS per-chapter `DrillPosture` ∈ {drill-all, drill-all-target-hard, cherry-pick-easy-mod, skim}. The posture answers the question B can't: "drill every subtopic of this chapter, or cherry-pick the EASY/MOD subtopics and skip the HARD ones?" — necessary when 5 chapters carry real HARD load.
- **Editorial data shape:** `OVERVIEW + CHAPTER_TABLE + STRATEGY_STRANDS + (per strand) StrandChapter[] with DrillPosture + targetHard + skipSubtopics + PLAYBOOKS (1 per chapter) + per-playbook DETAILS + FORMULA_GROUPS (chapter-grouped) + DRIFT_ROWS + HARD_BY_YEAR + DRIFT_CALLOUTS + TRAP_SHAPES (by strand)`.
- **New components:** `FormulaSheet` (chapter-grouped formula renderer — plain-text + unicode, no KaTeX pipeline → page stays server component). `RelatedPhysicsPlaybooks` (third parallel to RelatedPrinciples + RelatedPlaybooks; link path hard-coded per guide).

### Single-page landing (NDA Economics)

Use when the bank is **too thin for any multi-route template** to produce a non-parody strategy. NDA Economics: 24 q · 1 chapter · 3 subtopics · 1.5 q/paper · ~6 marks/paper max — Template A impossible (1 chapter rules out cross-chapter levers), Template B/C `STRATEGY_HEADLINE` reads as parody ("attempt 1.5 of 1.5, target 4 of 6"). Deliberately thinner terminal node, not a fourth template variant.

- **Routes:** landing only. No `/strategy`, no `/playbooks`, no `/trends`, no `/traps`, no `/principles`, no `/reference-tables`, no per-playbook detail pages.
- **Tag infra:** NONE. Cross-chapter axis impossible at 1 chapter.
- **Strategy stance:** named in prose, not in a `STRATEGY_HEADLINE` constant. Cap the subject explicitly ("~6 marks of 600-mark GAT, ~1%"), tell the reader what to do (drill the bank, internalise the one recall anchor, accept the cap), and link out.
- **Editorial data shape:** `OVERVIEW + SUBTOPIC_TABLE + <single-domain recall anchor>` (e.g. `FIVE_YEAR_PLANS` for Economics) `+ POST_PLAN_NOTE` or analogue. No `CHAPTER_TABLE` — 1 chapter doesn't warrant a table.
- **New components:** NONE. The subject's recall anchor renders as an inline `<table>` on the landing — simpler than `CommonCompoundsTable` because there's no per-cluster sub-grouping.
- **Sitemap priority 0.7** (vs 0.8 for full landings) — flags the thinness for crawlers.
- **`questionResources.ts` maps the subject's chapter to the landing** (Template A pattern — single landing, label `"NDA <subject> strategy"`) so `/browse` cards still get a backlink chip.

The bank-thinness gate is the entry point — see "Choosing the template" step 0.

### Template D — theme-prep checklist for short-half-life subjects (NDA Current Affairs)

Use when the bank's CONTENT goes stale (specific facts rarely repeat across years) BUT the bank's SHAPES are stable (recurring question types persist even when the specific facts in them rotate). NDA Current Affairs: 180 q · 8 ch · 31 sub · ~10 q/paper · ~40 max marks — multi-route templates would teach students 2017–2025 trivia and call it strategy. Template D is the honest alternative: name the half-life, name the recurring shapes, point at this year's news.

- **The half-life test** (empirical entry criterion): SQL-probe the bank for "year mentioned in question vs paper year". If 80%+ of explicit-year mentions are within 12 months of paper, half-life is short. Cross-check by counting recurrence of named entities (specific exercises / schemes / award winners) across paper-years — if each named entity appears in at most one paper, fact-memorisation has near-zero forward-prep value.
- **The stable-themes counter-test:** before defaulting to no-ship, count subtopics that appear in 5+ years of papers. If ≥6 subtopics meet that threshold and carry ≥40% of bank, the recurring SHAPES are stable enough to anchor a guide even when the FACTS aren't.
- **Strand axis = recurrence strength** (NEW — not skill, not strategic priority): Anchor themes (5+ year recurrence) → Recurring themes (3–4 year recurrence) → Occasional themes (1–2 year recurrence). Strand sizes are uneven by design; the prep-investment asymmetry is the point.
- **Theme cards** (the load-bearing artefact) — each anchor theme card carries: (a) **question SHAPE** — one sentence describing the recurring stem pattern, sourced from real bank stems (durable); (b) **prep CHECKLIST** — 3–6 fact-CATEGORIES to harvest from this year's news (durable directives, the student fills in this year's specific facts from an external compendium); (c) optional **durable-anchors** callout — the small subset of structural facts that genuinely don't rotate (G7 nations, UN agency HQs, ISRO launch-vehicle classes, etc.); (d) **drill BrowseLink** — practises the SHAPE against the bank's historical instances, NOT the facts.
- **Routes:** landing ONLY. No /strategy, no /playbooks, no /trends, no /traps, no /reference-tables, no per-playbook detail pages. Every section lives on the single page, ordered: hero + half-life directive + chapter table + anchor theme cards + recurring themes table + occasional themes chips + test-day plan + drill CTAs + closing rationale.
- **Tag infra:** NONE. Cross-chapter axis impossible — each chapter is a different subject domain (Defence ≠ Sports ≠ Awards). No `question_<X>_tags` table.
- **New components:** NONE. Anchor theme cards render as inline JSX `<article>` (same pattern as nda-economics's inline FYP `<table>`).
- **Editorial data shape:** `OVERVIEW + CHAPTER_TABLE + ANCHOR_THEMES (8 entries with shape + checklist + durableAnchors? + drillSubtopics) + RECURRING_THEMES (16 entries with oneLineShape + drillSubtopics) + OCCASIONAL_THEMES (7 entries minimal) + HALF_LIFE_NOTICE + TEST_DAY_PLAN`.
- **STRATEGY_HEADLINE stance:** named in the TEST_DAY_PLAN block, not in a copy-pasted constant. CA per-paper count is ~10 q (range 6–14), so ~40 max marks and target ~24 marks at 80% on 7 attempts.
- **Sitemap priority 0.6** (vs 0.7 for Economics, 0.8 for full landings) — flags the unusual template shape for crawlers.
- **`questionResources.ts` maps the subject's chapters to the landing** (Template A / single-page pattern — single landing, label `"NDA <subject> strategy"`).
- **Honest framing in copy is non-negotiable** — the half-life directive callout at the top of the page MUST explicitly say "drill this bank for SHAPE, not for FACTS" and point at an external compendium. Without it, students mistake the bank for the prep resource and memorise historical answers. The "Why this guide is one page" closing section names the alternative we didn't ship ("a 5-section guide would teach you 2019 facts and call it strategy").

### Shared across all guide treatments

- `GuideShell`, `GuideSideNav`, `GuideHero`, `StatBlock`, `BrowseLink`, `PrevNextNav`, `ChapterCard`, `WorkedExampleCard`, `GuideJsonLd` are all generic. The single-page landing treatment skips `GuideShell` + `GuideSideNav` + `PrevNextNav` (no sub-routes to navigate) but still uses `GuideHero` + `StatBlock` + `BrowseLink` + `GuideJsonLd`. Threading new behaviour through them as props (the 2026-05-18 ship added `landingHref` on Shell/SideNav, `rowLabel` on DriftTable) is the right pattern. Don't fork components per guide.
- `_data/*.ts` modules and the route topology are subject-specific. Diverge there.
- `/sitemap.ts`, Footer link, and `/browse` Hero pill plumbing additions are mandatory per new guide.
- Cross-link banner from the parent subject's landing (e.g. nda-maths landing → nda-english) is mandatory to discover the new guide.
- **Operations — before shipping `STRATEGY_HEADLINE`:** verify per-paper q-count against the live bank. Each PART B section has its own q-count (Physics ~25, Chemistry ~15, Biology ~10–11); copy-pasting between sibling guides has shipped wrong numbers (Chem + Bio caught 2026-05-18). The full SQL probe + 7-step recalibration recipe lives in [[per-paper-q-counts]] memory. `tests/guide-strategy-headline.test.ts` codifies the invariants — `paperQ × marksPerCorrect === totalMarks` (unit) and `paperQ within ±30% of live-bank avg q/paper` (DB integration). Run `npm test -- guide-strategy-headline` before commit.

### Choosing the template — the analysis script

Before proposing a new guide, run this analysis (SQL probes documented in [[english-guide-structure-diverges]] memory):

0. **Bank-thinness gate.** If avg q/paper < 2 OR total bank < ~50 q OR chapters = 1, no Template A/B/C will produce a non-parody `STRATEGY_HEADLINE`. **Ship a single-page landing instead** (see "Single-page landing" section above). NDA Economics (24 q · 1 ch · 1.5 q/paper · ~6 max marks) is the precedent; NDA Polity (90 q · 4 ch · 5 q/paper · 20 max marks) is the smallest *multi-route* guide and clears the floor. Below that floor, multi-route padding hurts more than it helps.
0a. **Content half-life gate** (run AFTER the thinness gate clears). SQL-probe `regexp_matches(text, '(20[12][0-9])', 'g')` against the bank and compute `(year_mentioned - paper_year)`. If 80%+ of explicit-year mentions are within 12 months of paper AND specific named entities (exercises / schemes / awards) almost never recur across paper-years, the content half-life is too short for a multi-route fact-memorise guide. Counter-test: if ≥6 subtopics appear in 5+ years (stable SHAPES even when FACTS rotate), **ship Template D — Theme-Prep Checklist** (see section above). NDA Current Affairs (180 q · 8 ch · 10 q/paper · 90% within-12-months mention rate · ~zero entity recurrence · 8 anchor themes at 5+ year recurrence) is the precedent.
1. **Chapter mix + %HARD per chapter.** Count chapters at each %HARD tier (>15%, >20%, >25%). Helps separate flat (Template B) from non-flat (A or C).
2. **Cross-chapter principle candidates.** Run keyword surveys for plausible horizontal levers. If the top candidate spans ≥4 chapters AND ≥40 q, **principles axis lives — Template A**. If it spans <4 chapters AND <30 q, **principles axis dead — go to step 3**.
3. **Pick B vs C by %HARD spread + concentration.** Three branches:
   - **≤2 chapters > 15% HARD** → flat — **pure Template B** (English / Chemistry / Biology shapes).
   - **3+ chapters > 15% HARD AND HARD is concentrated in 1–2 subtopics per chapter** (cherry-pick pattern lives) → **Template C** (chapter-playbooks + skill-strand + per-chapter DrillPosture overlay + flat-list page like formulas).
   - **3+ chapters > 15% HARD BUT HARD is diffuse within chapters OR concentrated-but-immovable** (no clean cherry-pick) → **Template B + non-flat %HARD variant** (Geography shape — Template B routes/granularity/subject-specific page, but strand-level %HARD calibration in place of DrillPosture).
4. **Subject-specific page.** Most guides have one (vocab-families for English, formulas for Physics, common-compounds for Chemistry, reference-tables for Biology + Geography, timeline-and-pairs for History). If the subject has a flat-list artefact that doesn't fit a playbook (formula sheet, periodic table chunk, named-acts list, name↔formula reference, named-fact pair table, named-place reference, chronology timeline), it's a candidate. Single-domain artefacts → single component (CommonCompoundsTable); multi-domain artefacts → multi-cluster component (BiologyReferenceTables / GeographyReferenceTables / HistoryReferenceTables with per-cluster column headers).
5. **Strand axis — execution-mode vs tier-style.** First test EXECUTION-MODE strands (Template B default, what English/Chem/Bio/Geo do). For each candidate mode (recall / rule / apply / verify / calculate / reason / multi-statement / date-anchored), measure `q × chapter_spread` AND the per-chapter distribution. If a mode dominates ONE chapter cluster (60%+ of one chapter, 20%- of others), execution-mode strands work. If modes are roughly uniform across all chapters (25–45% of every chapter), execution mode CAN'T partition → fall back to **TIER-STYLE strands** (Cornerstone / Foundation Recall / Quick-Win — History's pattern). For execution-mode strands, the Recall slot is universally stable. The SECOND slot is usually Rule but can bend to Apply (Bio + Geo) when no rule-application lever exists. The THIRD slot is most variable — Reason (English), Calculate (Chem Mole), Verify (Bio + Geo multi-statement). For tier-style, the slot semantics are different: Cornerstone = dominant chapter, Foundation Recall = recall-heavy mid-chapters, Quick-Win = low-%HARD / smallest chapter.
6. **Subject's natural seams.** If steps 0–5 don't fit (i.e. bank clears the thinness gate but none of A / B-pure / B-variants / C match), propose a new template (and write a memory documenting it).

See `feedback_english_guide_structure_diverges.md` memory for the full "trust the bank's shape" rationale + the 2026-05-18 ship analyses that produced Template B (English), Template C (Physics), Template B variants (Chemistry with Recall/Rule/Calculate + `/common-compounds`; Biology with Recall/Apply/Verify + `/reference-tables`), Template B + non-flat %HARD variant (Geography with Recall/Apply/Verify + strand-level %HARD calibration + `/reference-tables`), and the 2026-05-19 Template B + tier-style strands variants (History with Cornerstone/Foundation Recall/Quick-Win + `/timeline-and-pairs`; Polity with Cornerstone/Foundation Recall/**Specialist Wildcard** + `/reference-tables` — second use of tier-style with the THIRD tier INVERTED because Polity's smallest chapter is the HARDEST, not the easiest).

## Decisions log

Why behind architectural pivots — saves future-you from "why didn't we just…?".

**Convention:** topical intros (like "Foundations" below) sit at the top. Dated entries are bucketed by month — `### YYYY-MM` per month, with entries inside in date order. When a new month begins, add a new `### YYYY-MM` sub-section above the prior one (newest month closest to top, so the current month is the first thing a reader sees after the foundations). Within a month, entries stay in chronological order top-down. This lets a reader collapse historical months mentally and focus on what's recent.

### Foundations (M1–M3, 2026-05-08)

The load-bearing choices made during the initial scaffold. Every later phase rests on these — re-litigate only with explicit reason.

- **No Prisma.** Supabase RLS is the security boundary; Prisma uses a single privileged DB user and bypasses RLS by default. Forcing per-request `SET LOCAL` JWT injection fights the tide. `@supabase/supabase-js` carries the user's JWT through naturally.
- **RLS helpers in `private` schema.** PostgREST exposes any `public.*` function as a `/rest/v1/rpc/<name>` endpoint. Supabase advisor flagged `current_user_org_id()` / `current_user_is_admin()` as callable by `anon` and `authenticated`. Functionally safe (returns `null`/`false` for anon) but defence-in-depth — moved to a schema PostgREST doesn't expose.
- **OMML via marker-and-post-process.** The `docx` library's `ImportedXmlComponent.fromXmlString` wraps imported XML in an `<undefined>` element that Word refuses to open. We render placeholder text where math should go, then patch `word/document.xml` after `Packer.toBuffer`. Sidesteps the library quirk; works for arbitrary OMML.
- **Word export = ZIP of two files.** First version was a single .docx with a 3-way layout toggle. User asked to split: a Question Paper distributable without leaking answers, and a separate Answer Key. One ZIP keeps the bundle obvious. **Superseded 2026-05-13** — ZIP killed for mobile UX; route now returns one .docx per request via a required `kind` selector.
- **Hard 200/export cap, no auto-split.** Vercel function timeout is the binding constraint. A clear error ("narrow filters") ships faster than a zip-of-zips. Revisit if a teacher genuinely needs 500 questions in one paper.
- **Chapters/subtopics auto-create, subjects don't.** Subjects are top-level taxonomy curated centrally; auto-creating "Phyiscs" from a typo would corrupt the canonical list. Chapters/subtopics are per-upload working data — small typo cost, and a merge tool can clean up later.

### 2026-06

_No entries yet — the next month's decisions will land here. Convention reminder: newest month closest to top (so this block sits above 2026-05 once it has entries); within a month, entries stay in chronological order top-down (newest at bottom of the month's block)._

### 2026-05

- **2026-05-30 (later) — /notes template extension: `ConceptUnit` discriminated union + reference variant; NDA Physics Sound chapter ships (1st reference-variant, 1st non-Maths /notes subject).** Reframed the recall-strand authoring problem. The existing template (Statistics/Vectors/Probability) had one ConceptUnit shape — every concept needs a `formula?` + a required `authoredExample`. That works for technique-driven chapters but strains for Recall-strand chapters where the bank tests memorisation of flat tables (frequency bands, SONAR, ear anatomy, sound-speed-in-medium). Forcing reference content into "intuition → definition → formula → worked example" produced padded ConceptUnits ("Q: A bat emits 40 kHz; is this ultrasonic? A: Yes since 40 > 20"). **Template extension** (Phases 1+2 of the ship): `ConceptUnit` became a **discriminated union** `ConceptUnitFormula | ConceptUnitReference` gated by `kind: "formula" | "reference"`. Formula variant carries `formula?` + required `authoredExample` (existing shape). Reference variant carries required `table: ReferenceTable` with 2–5 columns, ≥1 rows (cell-count parity enforced by notes-lint), optional per-row `noteAmber` (amber-tinted callout) + per-row `pyqExampleId` (renders a `[Q]` chip linking to /browse). Both share intuition/definition/visualizationSlug/selfCheckExample/practiceSet/pyqExampleId/traps slots — only the core teaching slot differs. New `ReferenceTableBlock` component (full + `compact` modes) renders bordered tables with RichText cells. `splitNoteIntoSlides` emits a new `reference-table` slide variant in place of `authored-example` for reference concepts. `deriveSummary` adds a `references: SummaryReference[]` array; both `SubtopicSummary` + `ChapterRevisionSheet` render a "Reference tables (N)" section with per-concept disclosures (the full table inline behind the disclosure — revision IS the table for recall concepts). `notes-lint` validates table shape + walks per-row `pyqExampleId` against the same PUBLIC + in-parent-subtopic gates; foundation-exemption (check 4 silently skips formula concepts with no `pyqExampleId`) is **formula-variant only** — reference concepts MUST have ≥1 tagged question since they're the bucket for the subtopic's flat-fact recall. `notes-latex-audit` adds `table.columns[]` to the plain-text-no-LaTeX checklist; cells, caption, and noteAmber are KaTeX-aware. **Discriminator decision**: explicit-required (`kind` is mandatory) over implicit-default, paid the one-time cost of a mechanical sweep adding `kind: "formula" as const,` to all 83 existing concepts across Stats/Vectors/Probability. Pays back in cleaner narrowing + a lint pathway to enforce. **Sound chapter ship** (Phases 3+4): 5 subtopics × 13 concept units (7 formula + 6 reference) under new `/notes/nda-physics` subject route. Pedagogical split: Sound's bank tests BOTH technique-driven concepts (v=fλ at `wave-equation-and-units`, beats at `beats-formation`, echo distance at `echo-and-minimum-distance`) AND named-fact recall (frequency bands at `frequency-bands-and-scales`, SONAR at `sonar-and-applications`, transducer chain at `sound-transducers`, ear anatomy at `ear-and-instruments`, properties checklist at `sound-properties-checklist`) — each subtopic mixes the two variants. 5 new visualizations (`compression-rarefaction-wave`, `beats-envelope`, `ear-anatomy`, `frequency-spectrum-strip`, `echo-geometry` — all static SVG, server components, bringing the diagram set to 44). 34 PUBLIC questions tagged in `question_concept_tags` (100% coverage). Tagging session unset 2 degenerate `set_id`s (two Statement-I/II questions grouped only by question format, no shared passage) before tagging — co-location rule preserved without dual-tagging. `nda-physics/page.tsx` is the new subject-index landing (CollectionPage with chapter cards, parallel to `nda-maths/page.tsx`). Footer swapped the per-chapter "Statistics Notes" link for two subject-level links (`NDA Maths Notes` + `NDA Physics Notes`); `/nda` exam home Teaching-notes grid now shows 4 cards (Statistics + Vectors + Probability + Sound) with a per-card `subject` field instead of hardcoded "NDA Mathematics". Sitemap auto-derives the 7 new routes from `NOTES_CHAPTERS` — no manual sitemap edit needed. 849/849 vitest green; typecheck + lint + build + notes:lint + notes:latex all clean throughout.

- **2026-05-30 — /notes interactive-diagram program: all-SVG, 3 batches, 39 diagrams.** Grew the per-concept visualization set from 9 → 39 across Statistics/Vectors/Probability and locked the rendering approach. **Renderer decision — all hand-rolled SVG; three.js/react-three-fiber evaluated and REJECTED.** Built a WebGL parallelepiped next to an SVG one to compare; SVG won on bundle size + mobile/low-end-phone performance + visual consistency with the flat set (the one genuine WebGL candidate — the scalar-triple-product solid — ships as a painter's-order SVG box instead). Static figures are server components (no client JS); interactive + rotatable ones are `"use client"`. Rotatable "3-D" uses an orthographic turntable projection that **follows the cursor** (`az -= dx`, `el += dy`; the naive `+dx`/`-dy` feels inverted). Banked gotcha: an SVG `fill` opacity modifier must be a standard step — a non-generated `/8` silently falls back to SVG-default black. Shipped in three commits: a polish pass on the original 9 (off-canvas clipping fixes on vector-add/cross + variance squares, unified indigo wrapper + `max-w-md`, `ceb2995`); a **High-tier batch of 13** (`87049ea`); a **Medium-tier batch of 17** (`ed39916`). Every diagram wires through the `VisualizationSlug` union + `renderVisualization` dispatcher + per-concept `visualizationSlug`. A full per-concept audit of all 83 concept units drove the High/Medium selection — ~43 concepts deliberately stay text-only (algebraic, no spatial setup to draw). Program status + remaining gaps live in [[visualizations-status]] memory. Per-chapter share: Statistics 12, Vectors 17 (carries the rotatable 3-D figures), Probability 10.

- **2026-05-29 — NDA Probability /notes chapter (3rd chapter) + shared-card credibility + bold/bullet rendering.** Four related ships in one session. **(1) Probability chapter, pedagogy-first.** Decided the structure from the teaching arc (*define → count → add → multiply → condition → bound*), NOT the bank's /browse-oriented subtopic split — the user's rule "bank is guide" (captured as step 0 of the Notes editorial workflow + [[notes-structure-pedagogy-first]]). To make the notes coherent, the bank was reshaped to match: a Phase-D merge folded the dice/coins/arrangements/balls subtopics into "Probability via Counting" (162 q preserved, NDA Maths 113 → 109 sub) so one "Classical Probability & Counting" page replaces four repetitive ones. Shipped 5 pages / 24 concept units, all bank-anchored to verified in-subtopic PYQs; 162 q tagged in `question_concept_tags` (100% coverage, set-siblings co-located, the mis-filed "sock" combinations Q cross-tagged to the Classical page). Phase 1 = the 4 backbone pages; Phase 2 = Bounds + 3 bespoke visualizations. **(2) Set-bound PYQ context now renders inline.** `loadWorkedExamples` + `WorkedExample` gained `context`; `WorkedExampleCard` renders the passage above the question (stripping the "for the next N items" count phrase via `stripPassageCountPhrase`) so featured PYQs + mastery-checkpoint questions that are passage-set siblings are solvable. Was a latent gap on `/guide` too (shared card). **(3) PYQ provenance citation.** `loadWorkedExamples` composes a `provenance` string via the existing `formatProvenance`; `WorkedExampleCard` shows `[Q# · month · year]` below the question — same convention as `/browse`, for credibility. **(4) `**bold**` + bullet definitions.** The notes renderer dropped Markdown bold (literal `**` leaked across all chapters). Added pure `splitBold` + `parseRichText` to `parseLatex.ts` (math-masked; `parseLatex` itself untouched for the Word export), inline bold in `KatexRenderer` (fixes `**` everywhere), and a new `RichText` block component (paragraphs + `- ` bullet lists) for the `definition` field. Converted the genuinely list-shaped definitions to bullets (Probability 6, Vectors 2, Statistics 1); narrative definitions stay prose. 3 new visualizations (dice-sum-grid, venn-two-events, probability-tree) bring the set to 9; `VisualizationSlug` union + `renderVisualization` dispatcher extended. 839 vitest green; notes:lint + notes:latex clean; prepush green throughout. Also fixed a stale Phase-1 `notes-index-helper` fixture that used "Probability" as a no-notes chapter (the pre-push gate runs typecheck/lint/build, not vitest, so it had slipped through).
- **2026-05-28/29 — Self-sufficient `/notes` template: built on Statistics, ported to Vectors, then abstracted + documented.** Turned the two existing notes chapters into a complete self-study path and froze it as a reusable template. Per-concept ramp is now worked example → **self-check** (fresh problem, full solution behind one reveal) → **Level 1 practice set** (short mastery reps) → featured PYQ → traps, with a per-subtopic **mastery checkpoint** (5 bank PYQs interleaved across concepts via `pickInterleavedCheckpoint`), a per-subtopic **summary** + collapsible chapter **revision sheet** (both auto-derived via `deriveSummary` from `concept.formula` + `traps` — zero manual sync), and **3 interactive SVG visualizations per chapter** (Statistics: regression-line-fit / variance-squares / histogram-bin-slider; Vectors: vector-addition / projection / cross-product-area). Statistics has 31 concept units (6 foundations + 25 bank, 29 with practice, ~116 reps); Vectors 28 (6 + 22, 27 with practice, 108 reps). Key decisions: **(1) Level 1 practice is AUTHORED per-concept, not bank-derived** — a difficulty audit (`question_concept_tags` × difficulty) showed per-concept EASY bank coverage is too thin (10 concepts have 0 easy PYQs); bank-derived leveling only works at subtopic granularity, and mastery reps want procedurally-near-identical problems PYQs don't give. **(2) The faded-example rung was built then REMOVED** — `FadedExampleCard` always rendered the answer and the "hidden" step's value reappeared in a later visible step, so the reveal gained nothing (a user screenshot proved it); it was also redundant with self-check. Don't re-add. **(3) Density fix: the practice half (self-check + reps) collapses behind one native `<details>` "Practice this concept" disclosure** per concept — stacking the full ramp inline made a ~40-screen scroll wall; teaching half (intuition → formula → worked → traps) stays visible. **(4) LaTeX hygiene: added `npm run notes:latex`** (`scripts/notes-latex-audit.ts`) — catches LaTeX leaking into the 5 plain-text-rendered fields (title, oneLineDefinition, whyItMatters, concept.name, formula.label) + unbalanced delimiters; fixed 2 real leaks and normalized in-math unicode (`°`→`^\circ`, `✓✗`→words, `÷`→`/`, `≠`→`\neq`). **(5) Template abstraction:** the two duplicated ~260-line page files became one `NotesSubtopicPage` + one `NotesChapterLanding` (shared server components driven by a `NotesChapterRegistration`; added `subjectDisplay` to the registry); the 4 route files are now ~15-line wrappers (`getNotesChapterBySlug(...)` + delegate). Adding a 3rd chapter = write `_data` + append one registry entry + clone the two thin wrappers + tag the bank. SSG routes byte-identical; 825/825 vitest, notes-lint + notes:latex clean. Full structure + workflow in the file-layout + "Notes editorial workflow" sections above.
- **2026-05-27 — Combined latex+solution cleanup: NDA · Polity + Current Affairs + Economics (294 PUBLIC q · 13 chapters · 3 subjects in one pass).** Final NDA non-Maths trio audited together since each subject was small enough that running latex-cleanup + solution-cleanup separately per subject was overkill. **16 flagged rows total** across the three subjects. **10 AGREE solution rewrites** (drop REVIEW / ✓✗ / → unicode + 1 stem fix with hash refresh on CA Q100 Modhera "24 × 7" → `\(24 \times 7\)`). **6 key flips**:
  - Current Affairs Q116 2022-Sep `8e570cc0`: A Canada → B Sweden. UN World Environment Day 2022 hosted by Sweden (Stockholm+50 anniversary, theme "Only One Earth"); Canada hosted 2017, not 2022.
  - Current Affairs Q96 2017-Sep `36e94cab`: B Revenue → C Economic Affairs. Interdisciplinary Committee on virtual currencies (Apr 2017) chaired by Special Secretary, Dept of Economic Affairs (Dinesh Sharma).
  - Current Affairs Q91 2025-Sep `820ec0c1`: A "1 only" → D "Neither 1 nor 2". First BRIC Summit was Yekaterinburg, Russia 2009 (NOT Brazil — stmt 1 wrong). New Delhi hosted BRICS only ONCE (2012, 4th summit), not three times (stmt 2 wrong). Both false.
  - Current Affairs Q144 2019-Apr `4df31836`: B "2 and 3 only" → D "3 only". Dec 2018 renaming: Ross→Netaji SCB Dweep, Neil→Shaheed Dweep, Havelock→Swaraj Dweep. Stmt 1 wrong (Ross=Netaji not Shaheed), stmt 2 wrong (Neil=Shaheed not Netaji), only stmt 3 correct.
  - Economics Q56 2020-Apr `8e00ea38`: A "3-1-4-2" → B "3-4-1-2". Plan-objective mapping A-11th(3), B-12th(4), C-1st(1), D-5th(2) = code 3-4-1-2 = option B exactly. Bank's prose derived B but conceded to "closest available" — option B IS the exact derivation.
  - Economics Q81 2017-Apr `4fdc20d2`: A "8% GDP" → B "5% agriculture" (judgment-flag). 12th Plan agriculture growth target was 4%, not 5%; 5% does NOT match — so option B is the "NOT a target" answer. Some sources hedge on whether GDP target was 8% or 8.2%, but agriculture is unambiguously 4%.
  
  **7 hash refreshes** (6 flips + 1 stem-edit Modhera), all match recomputed; single-correct-option invariant holds across all 294 rows. **Cluster pattern**: 6 flips distribute as 3 from-A / 2 from-B / 0 C / 0 D / 1 judgment-A. Mild A-bias. **All Phase 5 probes return 0** including `unicode_residual` (no regression — used `\(\times\)` LaTeX consistently in replacement prose). **NDA non-Maths content audit (probe-flagged surface) CLOSED across all 9 non-Maths subjects**: English (11 flips), Physics (10), Geography (8), History (3), Chemistry (5), Biology (1), Polity + CA + Economics (6) = **44 NDA non-Maths flips total** across 2,700 PUBLIC q (1.6% subject-wide rate). Plus 1 preserved paper defect (Polity Q150 civil aviation — closest match defended in solution prose).
- **2026-05-27 — Solution cleanup: NDA · History (3 chapters touched, full subject probe).** 6 questions resolved end-to-end (2 from /latex-cleanup hand-off + 4 new REVIEW-marker rows surfaced by the wider probes). **3 AGREE solution rewrites**: Medieval Q96 2024-Apr `1e06ed41` (Upari-Pattadar-Mirasidar-Inam mapping, code A-2,B-1,C-4,D-3 = key A correct); Modern Q132 2017-Apr `7c73e78a` (railways NOT-a-feature, key D correct); Ancient Q85 2025-Sep `f1c9602b` (**Senguttuvan paper defect** — was Chera king, but ALL 4 options factually wrong; key B preserved with explicit note as printed-paper-error per [[gdrive-pdf-fetch]] precedent). **3 key flips**:
  - Ancient Q138 2022-Sep `75f3f5ec`: A "3-2-1-4" → C "4-1-2-3". Mahamatta Sanskrit etymology: Anta="frontier"=4, Ithijhakha="women"=1, Dhamma="propagation"=2, Nagalaviyohalaka="city"=3.
  - Modern Q79 2025-Sep `de430711`: B "1 and 3 only" → D "1, 2 and 3". All three Home Rule League facts are correct per Bipan Chandra (Tilak's HRL at Belgaum Apr 1916, Besant's at Madras Sep 1916, partitioned spheres).
  - Modern Q55 2022-Sep `801ffa23`: B "2 only" → C "Both 1 and 2". British EIC chartered 1600, Dutch VOC 1602 — Dutch IS after British, so statement 1 is correct. Vasco da Gama 1498 — statement 2 correct.
  
  **3 hash refreshes**, all match recomputed; single-correct-option invariant holds. **Cluster pattern**: 1 from-A, 2 from-B — mild B-bias on small sample. **All Phase 5 probes return 0** across 260 PUBLIC rows. **Paper defect: Senguttuvan** — 1 row preserved at key B per official-key precedent because no option correctly identifies him as a Chera king (joins Probability Q117, Trig Id Q39 20-Apr, IT Q42 24-Apr, Work Q123 24-Sep as the cumulative-bank's preserved-with-note set). **First non-Maths preserved-defect case** — earlier ones were all NDA Maths.
- **2026-05-27 — LaTeX cleanup: NDA · History (all 4 chapters).** Extremely sparse — 4 flagged rows total across 260 PUBLIC questions (1 chapter clean: World History). Single unicode char found across the entire subject: `→` (3 occurrences) in chronological-order solution prose. **Bucket A — 2 mechanical `→` → ` -> ` replacements** (Ancient Q122 2023-Sep `0e89e781` and Modern Q121 2017-Sep `58881a65`). **2 Bucket B deferrals to /solution-cleanup**: Ancient Q138 2022-Sep `75f3f5ec` (wrong_key, Mahamatta match-list); Medieval Q96 2024-Apr `1e06ed41` (REVIEW marker + `→`, defer wholesale per skill rule). No stem edits, no hash refreshes. All Phase 5 probes return 0 except the 2 deferred (1 unicode_sol + 1 wrong_key).
- **2026-05-27 — Solution cleanup: NDA · Geography (5 chapters touched, full subject probe).** 11 questions resolved end-to-end (1 wrong_key from /latex-cleanup hand-off + 10 new REVIEW-marker rows surfaced by the wider probes). **3 AGREE solution rewrites**: IG Economy Q125 2023-Sep `3c522246` (N-S × E-W corridors meet at Jhansi); IG Economy Q80 2020-Apr `29045844` (Census 2011 — Punjab least among options); World/Human Q92 2023-Apr `5c6783ed` (New Delhi 28.6°N northernmost). **8 key flips**:
  - Earth in Space Q113 2021-Apr `ad33b6da`: D 75°W → A 7.5°E. 5h × 15°/h = 75° west of 82.5°E = 7.5°E.
  - IG Economy Q100 2025-Apr `dd502cca`: A Bhilai → B Bokaro. All three clues (1964 Soviet, Rourkela-region ore, DVC power) match Bokaro; Bhilai was 1959 with Korba power.
  - IG Economy Q128 2025-Sep `094b857c`: A 3-1-4-2 → C 2-1-4-3. Ballary = Iron-ore (unambiguous); Balaghat = Manganese; Bewar (Beawar) = Mica; Bilaspur (CG) = Bauxite.
  - IG Economy Q59 2022-Sep `49d8fa99`: A → B. NER HQ = Gorakhpur, NFR HQ = Maligaon (not swapped as in key A). Correct: A-3, B-1, C-4, D-2.
  - IG Economy Q118 2017-Apr `5d17ab47`: B Chhattisgarh → A Jharkhand. Chhattisgarh **has** SECR HQ at Bilaspur (since 2003); Jharkhand has no zonal HQ.
  - IG Physical Q144 2022-Sep `f52d5836`: C "3 correct" → B "2 correct". Brahmani is formed by South Koel + Sankh (not Kosi + Sankh), so statement 2 is incorrect — only 1 and 3 hold.
  - IG Physical Q142 2017-Sep `48b609b5`: B → C (judgment-flag — option set is imperfect). Canonical descending % forest cover is Kerala > Odisha > AP > Karnataka = 3-2-4-1, which isn't in the options. C (3-2-1-4) is closer to canonical than key B (3-1-2-4) which incorrectly puts Karnataka above Odisha. Solution prose explicitly documents the option-set imperfection.
  - World/Human Q67 2021-Sep `e048bfa6`: A India +5:30 → D Bhutan +6:00. Bhutan's offset is the largest.
  
  **8 hash refreshes**, all match recomputed; single-correct-option invariant holds across all 345 NDA Geography PUBLIC rows. **Cluster pattern**: 4-from-A / 2-from-B / 1-from-C / 1-from-D — mild A-bias. **All Phase 5 probes return 0**. **No unicode regression** in replacement prose (lesson from Chemistry Q140 stuck — used `\(\times\)`, `\(^\circ\text{N}\)` in math zones, plain prose elsewhere).
- **2026-05-27 — LaTeX cleanup: NDA · Geography (all 7 chapters).** ~21 rows touched across 5 chapters (2 entered clean: World/Human Geography, Oceanography). Unicode vocab: 46 `→` (matching arrows / process transitions in climate-cycle prose), 11 `✓` + 4 `✗` (correctness annotations in multi-statement explanations), 5 `≈`, 2 `φ` (latitude angles), 2 `³` (km³/m³ volumes), 1 `Ω`. New `pg_temp.fix_geo_unicode` extends the chemistry/biology helpers with `φ → \(\varphi\)` for latitude notation. **1 Bucket B wrong_key deferred to /solution-cleanup** (IG Economy chapter). All Phase 5 probes return 0 except the deferred wrong_key; 0 nested-math, 0 hash mismatches across 345 PUBLIC rows.
- **2026-05-27 — Solution cleanup: NDA · Biology (1 chapter touched, full subject probe).** Bank-wide probe surfaced only 1 row across all 9 NDA Biology chapters (8 entered clean). **1 key flip**: Cell Biology Q112 2022-Sep `7da584f6`: D "Cell will swell up" → A "Cell will lose water". Osmosis rule: water moves down its concentration gradient; a medium with LOWER water concentration is hypertonic (more solute outside), so water flows OUT of the cell — cell shrinks/loses water. Key D describes the opposite hypotonic scenario. Bank's solution prose correctly derived A but conceded to the stored key D ("Answer should be A. However, NDA key marks D — Marking D per context"). Per [[no-UPSC-authority]] reframe, biology derivation wins. **1 hash refresh**, matches recomputed. All Phase 5 probes return 0 across 190 PUBLIC rows. **NDA Biology audit closed** (probe-flagged surface). Wrong-key rate this run: 1 / 190 = 0.5% — much lower than NDA Maths chapters (~29%) or Physics (4%), reflecting Biology's recall-only content and tighter LLM transcription.
- **2026-05-27 — LaTeX cleanup: NDA · Biology (all 9 chapters).** ~10 rows touched across 7 chapters (2 entered clean: Plant Biology, Genetics & Evolution). Tiny unicode vocab — 20 `→` (process arrows / food chains), 3 `β` (β-glucose / β-carotene context), 2 `⁺` (ion-like notation), 1 `³`. New `pg_temp.fix_bio_unicode` mirrors the Chemistry function with Greek-letter additions (`β → \(\beta\)`, `α → \(\alpha\)`, `γ → \(\gamma\)`). No stem edits (`uni_q` was 0 across all chapters), so no hash refreshes needed at the stem level — but bulk pass still ran the hash recomputation defensively over the ~10 row set (all matched). **0 Bucket B deferrals** from the wrong_key probe; /solution-cleanup will run a wider REVIEW-marker scan next. All Phase 5 probes return 0 across 190 PUBLIC rows.
- **2026-05-27 — Solution cleanup: NDA · Chemistry (5 chapters touched, full subject probe).** 8 questions resolved end-to-end across two probe passes (the 3 from /latex-cleanup hand-off + 5 new REVIEW-marker rows that /solution-cleanup's wider probes surfaced). **3 AGREE solution rewrites** (REVIEW prose stripped, keys unchanged): Carbon Q110 2022-Sep `ad471085` (C₄H₈ = alkenes B; cycloalkanes also share formula but B is the conventional answer); Chemical Reactions Q62 2023-Sep `4ddb4fc4` (match-list 3-2-4-1 = option C); Industrial Q65 2019-Apr `d00b4e6d` (Silicones D as heat-resistant paint binder). **5 key flips**:
  - Atomic Structure Q63 2022-Apr `5202ef1f`: C "Ne<N<Si<Mg" → A "Ne<Mg<N<Si". Standard valencies Ne=0, Mg=2, N=3, Si=4 give ascending order Ne<Mg<N<Si.
  - Atomic Structure Q83 2017-Sep `c94c2f28`: D "S²⁻" → C "Ar⁺". Cl (Z=17) has 17 e⁻; Ar⁺ (Z=18, lost 1) has 17 e⁻; S²⁻ (Z=16, gained 2) has 18 e⁻.
  - Mole Q84 2017-Sep `c5ddc56b`: B "%H=2·%C" → C "%C=6·%H". For C₆H₁₂Oₙ: %C/%H = (6·12)/(12·1) = 6 always. Bank stem C₆H₁₂O₄ creates ambiguity (atoms = 22 makes A also true); solution notes the ambiguity and identifies C as the unique-under-glucose-interpretation answer; key B (%H = 2·%C) was wrong under any reading.
  - Acids/Bases Q85 2021-Apr `f6a97e5d`: B "Tartaric" → C "Oxalic". Per NCERT Class 7 Chapter 5 acid-source table, tomatoes → oxalic acid; tartaric is for tamarind/grapes.
  - Chemical Reactions Q140 2017-Sep `930fa968`: B "1 and 2" → A "1 only". CH₄ + 2O₂ → CO₂ + 2H₂O: strict oxidation-state math gives C: −4→+4 (oxidized, stmt 1 ✓), H: +1→+1 (unchanged, stmt 2 ✗). Key B follows the loose "combine-with-O = oxidized" NCERT-Class-10 framing; the strict definition (NDA candidate level) makes only statement 1 correct. Per [[no-UPSC-authority]] reframe, math derivation wins over stored key.
  
  **5 hash refreshes** (5 flips); all match recomputed; single-correct-option invariant holds across all 262 NDA Chemistry PUBLIC rows. **Cluster pattern**: 5 flips distribute as 0-from-A / 2-from-B / 1-from-C / 1-from-D / 1-from-B (Q84+Q85+Q140 all from B = 3 from-B). Mild B-bias but small sample. **All Phase 5 probes return 0** (`wrong_key`, `review_markers`, `hedge_phrases`, `unicode_residual`, `hash_mismatch`, `multi_or_zero_correct`).
  
  **Self-inflicted regression caught + fixed**: Q140's first-pass rewrite included unicode `×` (for "×4 = +4") and `✓`/`✗` checkmarks — exactly the class of regression the skill warns about in Phase 5. Phase 5 probe caught the unicode_residual immediately; rewrote the solution in pure ASCII/LaTeX prose. Decision-log note: always use `\times` inside `\(...\)` and replace checkmarks with words like "correct"/"incorrect" or just drop, even in derivation prose.
- **2026-05-27 — LaTeX cleanup: NDA · Chemistry (all 12 chapters).** ~19 rows touched across 7 chapters (5 entered clean: Industrial Chem, Metals & Non-Metals, Hydrogen & Water, Chemistry in Everyday Life, Practical Chem). Sparser than Physics — total unicode counts: 16 `→` (chemical equation arrows), 10 `×`, 8 `²`, 8 `≈`, 7 `⁺`, 6 `⁻`, plus small numbers of `³ ¹ ✓ ✗`. New `pg_temp.fix_chem_unicode` helper extends the Physics function with ion-charge handling: compound patterns (`²⁺ → \(\w^{2+}\)`, `²⁻ → \(\w^{2-}\)`, `³⁺ → \(\w^{3+}\)`, `⁻³¹ → \(\w^{-31}\)`, `²³ → \(\w^{23}\)`) and single-char ion markers (`⁺ → \(\w^{+}\)` for cations like Na⁺ / p⁺; `⁻ → \(\w^{-}\)` for anions like Cl⁻ / e⁻). One stem edit (Chemical Reactions Q61 2024-Sep `aa2cda4a` — `2AgCl(s) → 2Ag(s) + Cl2(g)` arrow → ASCII), hash refreshed. **3 Bucket B rows deferred to /solution-cleanup**: Atomic Q63 2022-Apr `5202ef1f` (wrong_key — valency order Ne/Si/N/Mg), Atomic Q83 2017-Sep `c94c2f28` (wrong_key + REVIEW — isoelectronic-with-Cl), Mole Q84 2017-Sep `c5ddc56b` (REVIEW — C₆H₁₂O₄ mass percent). All Phase 5 probes return 0 except the deferred 2 unicode_sol + 2 wrong_key (overlap on Q83); 0 nested-math; 0 hash mismatches across 262 PUBLIC rows.
- **2026-05-27 — Solution cleanup: NDA · Physics (8 chapters touched, full subject probe; first NDA non-Maths Physics audit).** 13 flagged, 11 resolved end-to-end + 2 diagram-pending. **1 AGREE solution rewrite** (Heat & Thermo Q121 2017-Apr `4b587f73` — Kelvin=Fahrenheit gives C=301.25≈301; bank solution had a sign-error mid-derivation but final answer matched key B). **9 key flips** (every derivation lands exactly on a listed option — clean labeling errors, no PDF needed):
  - E&M Q96 2019-Apr `75d565c5`: A `P=2P₁` → B `P=P₁/2`. \(R_B = R_A/2\), so \(P_1 = 2P\).
  - Fluid Q99 2022-Apr `11963628`: C 333.3 → D 666.6 N/m². Contact area 30×10=300 cm²=0.03 m², F=20 N → P=666.6.
  - Fluid Q134 2022-Sep `07afef44`: C "sinks in water, floats in B" → A "floats in both". Packet density 0.8 g/cm³ < both fluid densities (1.0, 1.5).
  - Gravitation Q95 2018-Sep `3696291d`: A `g₁` → C `g₁/2`. \(g = GM/R^{2}\); with \(M,R\) both doubled, \(g\) halves.
  - Kinematics Q140 2021-Sep `29b78b37`: C 16 m/s → D 20 m/s. \(u = \sqrt{2gh} = \sqrt{2 \cdot 10 \cdot 20} = 20\).
  - Laws of Motion Q83 2024-Apr `9ed6fc8c`: B (N₁=0.8, N₂=1.6) → C (N₁=0.6, N₂=1.8). Torque about W₁: N₂·0.4 = mg·0.3.
  - Laws of Motion Q81 2022-Apr `ff9c1d41`: A 2.15 m/s → B 1.89 m/s. Momentum: 52·2 = 55v → v=104/55≈1.89.
  - Oscillations Q84 2017-Apr `ce6adcc9`: B (t=2,t=8; Δt=6) → C (t=3,t=7; Δt=4=T). Only Δt=nT gives same phase; T=4s.
  - Work Q58 2026-Apr `10aa9f07`: B (ΔK larger in S) → C (larger in S'). ΔK_S = mgh; ΔK_S' = mgh + mu√(2gh), strictly greater.
  
  **1 PRINTED-PAPER inconsistency preserved (key unchanged)**: Work Q123 2024-Sep `1678f01c` — bank stem matches PDF (GAT_2024_NDA2.pdf verified via gdrive MCP, second GAT fetch this session) exactly: \(m=2.0\) kg, \(v=10\) m/s, \(d=20\) m → \(f=5\) N, but options are \(\{10,20,40,50\}\) N. Most likely intended numbers were \(d=2\) m or \(m=20\) kg (both give 50 N matching key D). Per [[gdrive-pdf-fetch]] precedent (IT Q42 Apr 2024), preserved at official key D with discrepancy documented in solution prose. Joins the "unrecoverable from PDF" tier — printed-paper transcription error, not bank extraction defect.
  
  **2 diagram-driven fixes resolved via user-pasted screenshots** (gdrive PDF OCR doesn't preserve spatial info in figures; screenshot fallback used per the diagram exception):
  - E&M Q70 2019-Sep `6dc64944`: **stem extraction error + key flip A → B**. The screenshot shows three resistors (2Ω, 4Ω, 8Ω) all in parallel — the bank's stored stem had "2Ω in series with parallel (4Ω, 8Ω)" which was wrong. Corrected stem; \(1/R_\parallel = 1/2+1/4+1/8 = 7/8 \Rightarrow R_\parallel = 8/7\), total \(= 8/7 + 1 = 15/7\,\Omega\) = option B. Key A=15/8 is not derivable from any topology of these 4 resistors.
  - E&M Q132 2020-Apr `0c26bb7f`: **key flip C → D**. The screenshot shows B pointing right and proton moving up; right-hand rule for positive charge gives \(\hat{y}\times\hat{x} = -\hat{z}\) = into the page. The bank's solution prose was internally self-contradictory (correct geometry, wrong conclusion).
  
  **12 hash refreshes** total (11 flips including Q70+Q132 + Q70's stem edit; Q123 + Q121 no — solution-only). All 12 verify against recomputed; single-correct-option invariant holds across all 449 NDA Physics PUBLIC rows. **Final cluster pattern**: 11 flips distribute as 4 from-A / 3 from-B / 3 from-C / 0 from-D — slight A-bias but not significant. **All Phase 5 probes return 0** (`wrong_key`, `review_markers`, `hedge_phrases`). NDA Physics content-correctness audit (probe-flagged surface) **CLOSED**.
  
  **Workflow milestone**: first audit using gdrive workflow on a math-derivation-heavy non-Maths subject. PDF fetched for Q123 (text-only) via the same `mcp__claude_ai_Google_Drive__search_files` + `read_file_content` flow as Maths. Explicit limitation documented: PDF OCR doesn't preserve diagrams, so spatial questions (circuit topology, vector direction) need screenshot fallback — added to the in-skill "When to stop and ask the user" surface.
- **2026-05-27 — LaTeX cleanup: NDA · Physics (all 14 chapters).** ~70 rows touched across 11 chapters (3 entered clean: Modern Physics, Oscillations & Waves, Energy Sources). Bulk replacement chain via temp plpgsql helper covered: standalone Greek (`Ω → \(\Omega\)`, `ρ → \(\rho\)`, `θ λ μ α`), math operators (`× → \(\times\)`, `≈ → \(\approx\)`), arrow (`→` → ` -> ` per NDA English precedent), checkmark dropped, compound superscripts (`¹⁵ ⁻² ⁻¹ ⁻³ ⁻⁶` via `(\w+)<compound>` regex captures), and single-char superscripts (`² ³ ⁵ ⁶ ⁸ ¹` via `(\w+)<sup>`). One row (Kinematics Q98 2024 `92b8e7f0`) hand-crafted because of nested `√(100²+(200√2)²)` derivation. **5 Bucket B wrong-key candidates deferred to /solution-cleanup** (1 each in Electricity & Magnetism, Laws of Motion, Kinematics, Fluid Mechanics, Gravitation). All Phase 5 probes return 0 except the deferred; 0 nested-math artefacts; 0 hash mismatches across all 449 NDA Physics PUBLIC rows.
- **2026-05-27 — Solution cleanup: NDA · English (3 chapters touched, full subject probe).** 18 questions touched (4 of 8 chapters entered clean: Grammar, Idioms and Phrases, Reading Comprehension, Cloze Test). **6 AGREE solution rewrites** — REVIEW/hedge prose stripped, key unchanged: Vocab Q37 2025-Apr `ea418ae5` (Coeval = same age, key A); FIB Q50 2021-Apr `ebe34b3e` ("had known" not in options, B "have known" defended); SR Q47 2017-Apr `5067a638`; SR Q44 2017-Apr `93863b87` (probe false positive — confused prose, key B correct); SR Q45 2017-Apr `467447c0`; SR Q46 2017-Apr `8daf203e`. **11 key flips** (every derivation lands exactly on a listed option — clean labeling errors, no PDF needed):
  - FIB Q47 2017-Sep `14f4c6f3`: A "happening" → D "going". Standard idiom is "what is going on"; "happening on" non-idiomatic.
  - Spotting Errors Q2 2019-Apr `0672de4e`: A → B. Error is "but becoming a great man" (needs perfect participle "having become"); segment A is clean past tense.
  - SR Q26 2022-Sep `2bebc648`: D PQRS → C SPRQ.
  - SR Q30 2021-Sep `5f8fab1f`: A RQPS → C RSQP.
  - SR Q26 2021-Sep `09ac7525`: D QPSR → B QRSP.
  - SR Q25 2021-Sep `f93c9a01`: C SPQR → D QRPS (judgment call, user-approved — "like mine" modifies "tin ear" not "singing").
  - SR Q12 2020-Apr `e99f6346`: D PQRS → A PRQS.
  - SR Q14 2020-Apr `339698ba`: A PQSR → D PRSQ (A places "the visible range" before "glowing" — grammatically broken).
  - SR Q40 2019-Apr `4114d6e2`: D PQRS → B PSRQ.
  - SR Q35 2017-Sep `27f5ea0b`: A PQRS → C SPRQ.
  - SR Q32 2017-Sep `9f927448`: D PRQS → C RPQS.
  
  **1 PDF-driven stem correction** (key D unchanged): SR Q34 2017-Sep `806957a2` — bank's extraction had duplicated fragment P as "that it is an aftermath" (same as Q) and embedded "of sunspot activity" in the lead-in. **First successful GAT PDF fetch via Google Drive MCP** (`GAT_2017_NDA2.pdf`, fileId `1uctRVpNHbgaXUphTU2nphiR71aYoH5X2`); workflow validates that the GAT folder enumerated in `[[gdrive-pdf-fetch]]` is actually usable end-to-end for NDA non-Maths audits, not just Maths. Stem corrected: lead-in "Scientists point out" + P "of sunspot activity" + Q "that it is an aftermath" + R "that has now reached its peak" + S "of the eleven-year cycle". Natural sentence "Scientists point out that it is an aftermath of the eleven-year cycle of sunspot activity that has now reached its peak" = QSPR = option D (which the bank already had).
  
  **12 hash refreshes** (11 flips + 1 stem-text edit); all 12 verify against recomputed hash; single-correct-option invariant holds across all 12 rows. **Cluster pattern**: of the 11 flips, **5 are FROM keyed D** and **4 from keyed A** — mild D-bias visible (45%) but less extreme than Statistics' all-on-C cluster from earlier audits. **All Phase 5 probes return 0** (`matches_option_disagrees_with_key`, `review_markers`, `hedge_phrases`, `unicode_in_solution_residual`).
  
  Set-sibling re-verification check skipped as not load-bearing for Sentence Rearrangement: SR siblings share only a section-instruction header (not propagating context), and SR solutions never cross-reference other siblings' answers. No `context` UPDATE was performed. **Bank-wide content audit advances** from 31 chapters (NDA Mathematics only) to 31 chapters + NDA English wrong-key-surface probe-flagged subset closed.
- **2026-05-27 — LaTeX cleanup: NDA · English (all 8 chapters).** 43 questions touched across 2 chapters; 4 chapters (Grammar, Idioms and Phrases, Reading Comprehension, Cloze Test) entered clean. **Bucket A — 43 mechanical edits applied**: 3 Vocabulary stems (2018-Sep `5cbf032e` / `2ef0b541` / `31b05445`) restored their missing closing `\)` after the period in `\(\underline{\text{word}}.\)` (hash refreshed); 40 Sentence Rearrangement solution-prose `→` arrows (U+2192) bulk-replaced with ASCII `->` (solution-only, no hash change). **Bucket B — 7 deferrals to /solution-cleanup**: 3 clean-flip candidates whose derivations land EXACTLY on a listed option (Spotting Errors Q2 2019-Apr `0672de4e` soln-B vs key-A; Sentence Rearrangement Q26 2022-Sep `2bebc648` soln-C vs key-D; Sentence Rearrangement Q40 2019-Apr `4114d6e2` soln-B vs key-D); 3 hedge/REVIEW-marker solutions needing re-derive (Sentence Rearrangement `09ac7525` Q26 2021-Sep + `e99f6346` Q12 2020-Apr + `93863b87` Q44 2017-Apr); 1 hedge-prose-only cleanup (FIB Q47 2017-Sep `14f4c6f3` solution explicitly notes "Note: official answer is A" discrepancy — unicode `→` also present). All Phase 5 probes return 0 except the deferred (1 `unicode_in_solution` + 6 `matches_option_disagrees_with_key`). Arrow-replacement style chosen: ASCII `->` (preserves "flow" semantic without claiming math).
- **2026-05-27 — No-UPSC-authority audit reframe + 4-case retroactive resolution.** User clarified that UPSC does not publish answer keys for NDA papers. The "stored key" in the bank is one source's claim (LLM extraction from coaching material), NOT authoritative — so "preserved per UPSC official" was the wrong justification for the 5 cases earlier labelled "UPSC misprints". New rule: when a math derivation conflicts with the stored key and lands on a listed option → flip; when it doesn't land on any listed option → try option-text extraction-error repair first (most common: `1/√2` mis-extracted as `1/2`, `32/70` as `33/70`); fall back to "placeholder + flag" only when PDF OCR can't disambiguate. **Re-resolved 4 of the 5 prior "preserved UPSC misprint" cases**: (1) Probability Q117 Apr 2023 `00e8e6ff` — fixed option B text `33/70` → `32/70 (= 16/35)` to match derivation; key B stays. (2) Trig Id Q39 Apr 2020 `92bb246b` (set S, items 37–39 with α=β=15°) — fixed option D text `(1/2)(√3 cos 1°+sin 1°)` → `(1/√2)(√3 cos 1°+sin 1°)` (common LLM-extraction √ drop); derivation `sin 16°+cos 16°=(1/√2)(√3 cos 1°+sin 1°)` matches option D as corrected; key D stays. (3) Trig Id Q39 Apr 2017 `bb13d969` — clean key flip A → D ("None of the above") under strict math: \(450^\circ<A<540^\circ\Rightarrow A/2\in(225^\circ,270^\circ)\) (Q3, cos negative), giving \(\cos(A/2)=-1/\sqrt{10}\) which is not in options A/B/C. (4) Sequence & Series Q22 Apr 2022 `bf15e9f2` — clean key flip A → B; literal stem \(\sum_{r=2}^{9}r\cdot r!+2\) telescopes to \(10!\) via \(r\cdot r!=(r+1)!-r!\), matching option B. (5) Inverse Trig Q42 Apr 2024 `1aff981b` — STAYS at key C as placeholder (derivation `1/(5+cot²(π/4−2cot⁻¹3))=1/54` not in options {1,7,8,16}; PDF OCR garbled; solution reframed to drop UPSC misprint language). **3 hash refreshes** (Q117 option-text change, Q39 2020 option-text change, no flips affect the 2 already-flipped earlier today). All 5 solution prose rewrites drop "preserved per UPSC official" framing. **Memory updates**: `[[content-audit-progress]]` reflects the reframe + new entries; `[[gdrive-pdf-fetch]]` row 6 ("Genuine UPSC misprint") rewritten as "Bank or printed-paper transcription error" with new resolution rule (try option-text repair first, placeholder only as last resort). **Remaining 2 placeholders** (stem/options can't be reconciled with derivation): Functions Q77 Apr 2017 (stem incomplete in printed paper, bank matches PDF) and IT Q42 Apr 2024 (bank stem unrecoverable from PDF OCR — first one to revisit when a clean OCR or screenshot surfaces). All Phase 5 probes still return 0 across NDA Maths 2,160 q post-reframe.
- **2026-05-27 — NDA · Mathematics CONTENT-CORRECTNESS AUDIT COMPLETE: 13-chapter batch closes the subject (31/31, all 2,160 q).** Single-session sweep of the remaining smaller chapters (Binomial Theorem 54 q, Properties of Triangle 49 q, Indefinite Integration 40 q, Conics 38 q, Inverse Trigonometry 34 q, Trigonometric Equations 33 q, Binomial Distribution 30 q, Circles 27 q, Logarithms 27 q, Applications of Integration 25 q, Height & Distance 24 q, Binary Numbers 13 q, Linear Inequalities 5 q — 399 q total). **17 Bucket A edits** (✓/✗/×/→/π/°/√/² unicode cleanup across 9 chapters; one option-text fix none — all solution-only). **22 wrong-key flips** (every derivation lands EXACTLY on a listed option — clean math justification, no PDFs needed beyond IT Q42): Binary Numbers Q9 (A→C) + Q41 (C→A); Binomial Distribution Q109 (D→C); Binomial Theorem Q49 (B→A) + Q17 (B→C); Circles Q87 (C→B); **Conics 6 flips — DENSEST OF ANY CHAPTER AUDITED (15.8 %)**: Q62 S7 (B→A) + Q59 (C→B) + Q55 (A→B) + Q57 (A→B) + Q49 (A→B) + Q54 (B→A); Height & Distance Q35 (C→B) + Q97 (B→A) + Q31 (C→B); Indefinite Integration Q78 (A→B, off-by-2) + Q97 (C→A, a/b swap in arctan); Inverse Trigonometry Q34 (C→B); Linear Inequalities Q55 (C→D); Logarithms Q7 (B→A) + Q72 (B→D); PoT Q29 (C→B) + Q22 S3 (C→A — full derivation verified numerically at \(p=3,q=4,\theta=30^\circ\)); Trigonometric Equations Q42 (A→B). **1 preserve-with-note** (per [[gdrive-pdf-fetch]] precedent): Inverse Trigonometry Q42 2024-Apr `1aff981b` — bank stem matches source PDF (OCR-readable portion confirms structure), derivation yields \(1/54\) (\(\cot^2(\pi/4-2\cot^{-1}3)=49\), sum 54, reciprocal 1/54) which is NOT in options \(\{1,7,8,16\}\); no plausible re-interpretation reaches the UPSC key C=8 (tried tan/cos/csc inverses, swapped angles, alternate denominators); preserved at C with explicit discrepancy note in solution prose, third UPSC misprint to be preserved (joins Probability Q117 Apr 2023 + Trig Id Q39 Apr 2020). **Set-sibling re-verification passed**: Conics S7 (Q61+Q62; Q61 stays at correct key C=7 deriving p=7 from c²=16-p=9), PoT S7 (Q46+Q47; Q46 stays at correct key B=30°), PoT S3 (Q21+Q22; Q21 stays at correct key C since sine-rule statements 1+2 derive independently of Q22's specific AB formula). **22 hash refreshes**; single-correct-option invariant preserved across all 24 touched rows. **Cluster patterns observed**: Conics 6 flips include a 2017-Sep cluster (Q57+Q49+Q54, three in one paper-batch — fourth chapter today with that paper-batch's wrong-keys after Stats / Trig Id / Functions); 2020-Apr cluster appears AGAIN (Conics Q55 + Height & Distance Q31 + Indefinite Integration Q78 + Logarithms Q7 + PoT Q29 + PoT Q22 — six 2020-Apr flips across 6 chapters today). **NDA Mathematics chapter closure: 31 of 31 fully closed (100 %, 2,160 / 2,160 q).** Decisions log + [[content-audit-progress]] memory updated; bank-wide content audit advances from 18 → 31 / 185 chapters (16.8 %), 2,160 / 9,578 q (22.6 %). Next subjects pending audit: NDA non-Maths (English 900 q, Physics 449 q, Geography 345 q, Chemistry 262 q, History 260 q, Biology 190 q, Current Affairs 180 q, Polity 90 q, Economics 24 q) + MHT-CET (4,718 q across 79 chapters).
- **2026-05-27 — Skill workflow shift: `/latex-cleanup` + `/solution-cleanup` now fetch PDFs autonomously via Google Drive MCP.** Both skill files (`.claude/commands/latex-cleanup.md` + `.claude/commands/solution-cleanup.md`) updated to replace the "ask user for screenshot" prompt with autonomous PDF fetch from a shared Google Drive folder. **Folder convention**: `PYQPs/NDA/Maths/NDA_Maths_PYQPs_Edited/Maths_{YEAR}_NDA{1|2}.pdf` — shared from `connect.lwspune@gmail.com` (LWS Pune org email) with `vilas11shinde@gmail.com` (Drive MCP authed account). Filename rule maps `(pyq_year, pyq_month)` → file via Apr⇒NDA 1 / Sep⇒NDA 2. Skills use `mcp__claude_ai_Google_Drive__search_files` to locate, then `read_file_content` to read the full OCR'd text (single call covers a 120-q paper). **Question matching by stem-text** (NOT Q-number — Q# varies by SET A/B/C/D, only the stem is invariant). New section in both skills: **PDF-vs-bank discrepancy taxonomy** with 6 failure modes (option-text extraction error, stem-text extraction error, context extraction error, set-context over-specification, dropped sign/marker in stem, genuine UPSC misprint). First 5 are bank-storage fixes; only the 6th preserves key as-is. Default assumption when math conflicts with options: bank extraction error, not UPSC error. **Workflow validated end-to-end today on NDA Maths · 3D Geometry** — first chapter audit closed with zero user prompts after initial "go" (25 candidates triaged, 2 truly-corrupted PDFs fetched + diagnosed autonomously, both turned out to be option/stem extraction errors not UPSC misprints). **Skill files also updated with set-sibling re-verification rule** (Q75/Q76 + Q34/Q35 surfaced earlier today): when fixing a flagged item with a `set_id`, ALWAYS pull and verify sibling solutions in the same DO block — context UPDATEs propagate via set_id and can silently break unflagged siblings' derivations. Memory captured at [[gdrive-pdf-fetch]].
- **2026-05-27 — Solution cleanup: NDA · Mathematics · 3D Geometry (first fully-autonomous run including PDF triage via Google Drive).** 25 candidates triaged in a single end-to-end loop with **zero user prompts after initial "go" — all PDF fetches done autonomously through the Google Drive MCP**. Per the same-day gdrive setup (PYQPs/NDA/Maths/NDA_Maths_PYQPs_Edited/ folder shared from connect.lwspune@gmail.com with vilas11shinde@gmail.com on which Claude's connector is authed), I now fetch source PDFs by predictable filename `Maths_{year}_NDA{1|2}.pdf` whenever a "truly corrupted" item surfaces. **Breakdown**: 11 clean key flips (derivations land exactly on listed options) + 12 solution-only cleanups + 2 PDF-driven stem/option fixes. **Key flips**: Q56 2026 Apr B→D, Q51 2025 Sep A→B, Q91 2022 Apr C→B, Q94 2022 Apr B→A, Q95 2022 Apr A→B, Q61 2020 Apr B→D, Q43 2017 Sep B→C, Q53 2017 Sep C→B, Q59 2017 Sep B→C, Q64 2017 Apr A→D, Q65 2017 Apr A→C. **PDF-driven fixes**: (a) Q62 2025 Apr `6df1a6a5` — bank stored options C=`6√6` and D=`6√8`, but PDF shows `√66` and `√68` (OCR misread `√66` as `6√6` — same fontmark-position error on D). Options fixed, key C stays correct (derivation = √11·√6 = √66). (b) Q54 2024 Apr `d8d9f4d9` — bank stored stem `497(l²+m²-n²)` (extracted `49·7l²` as `497·l²`), PDF shows `49(7l²+m²-n²)`. Stem fixed, key B stays correct (`49·(28+9-36)/49 = 1`). **13 hash refreshes** (11 key flips + 2 stem/option changes). All Phase 5 probes return 0 across 89 q. **Workflow validation**: this is the first chapter audit where the user did not need to provide any input after "go" — the new gdrive setup let me self-serve both source PDFs in 2 calls (1 per file) and resolve "truly corrupted" autonomously. Both PDF-driven cases turned out to be option/stem extraction errors (not actual UPSC misprints), reinforcing that "derivation lands outside option set" is usually a bank extraction defect, not a paper bug — fixing the stored data is often the right call rather than preserving as misprint. Cross-chapter wrong-key queue: 17 candidates remain (M&D + Stats + Trig Id + Prob — all closed today; pre-existing only — no new). NDA Maths chapter closure status: 8 of 31 fully closed (Functions, Vectors, Lines, M&D, Statistics, Trig Identities, Probability, 3D Geometry).
- **2026-05-27 — Cross-chapter wrong-key + hedge sweep: M&D + Statistics + Trig Identities + Probability (closes May-23 backlog).** Single-pass audit of all 4 NDA Maths chapters that carried unresolved wrong-key / REVIEW-marker candidates from the May-23 pre-skill audits. **Found 59 candidates** (vs the 17 originally estimated — the May-23 narrative had undercounted by lumping in pending REVIEW-prose markers under "wrong-key candidates" verbally without distinguishing). Triage: **37 clean key flips** (derivation lands exactly on a listed option — bank labeling errors with no PDF needed), **14 solution-only cleanups** (REVIEW/hedge prose removed, key already correct), **7 truly-corrupted requiring PDF** (stem-extraction errors, derivations outside option set, or convention ambiguities), **1 already-preserved NDA-paper misprint** (Probability Q117 2023 Apr from May-23 ship). Per-chapter breakdown: M&D 9 flips + 4 cleanups + 0 corrupted; Statistics 13 flips + 6 cleanups + 2 corrupted; Trig Identities 13 flips + 3 cleanups + 4 corrupted; Probability 2 flips + 1 cleanup + 1 corrupted. **37 hash refreshes** (one per key flip). **All clean flips applied autonomously** under today's refined workflow rule "derivation landing exactly on a listed option = bank mislabel, no PDF needed; apply with math justification" — no per-question approval required for the 37 flips + 14 rewrites. **Pattern surfaced:** the May-23 audit had treated EVERY wrong-key candidate as PDF-pending, which created a 17-question backlog that ballooned to 59 hits when the probe was widened to include hedge prose. Under today's rule, 51 of the 59 (86%) needed zero PDF intervention. Truly-corrupted rate is only 7/59 = 12% — and 4 of those 7 are stem-extraction errors (notation ambiguity, missing constraints, dropped bars) rather than UPSC misprints. **Cross-chapter wrong-key queue is now drained AND all 7 truly-corrupted resolved via PDFs in same session.** PDF-triage outcomes: (a) Stats Q104 24-Apr — stem extraction error `tan^3θ` should be `tan 3θ`, key A stays; (b) Stats Q102 23-Sep — stem extraction error `Σ(3xᵢ−45)` should be `Σ(3xᵢ−4)/5`, key C stays; (c) Trig Id Q75 23-Sep — context extraction error `p=1/(3−tan³x·tan x)` should be `p=1/3 − (tan 3x)/(tan x)`, key D stays; (d) Trig Id Q34 19-Apr — set context's no-XY-cross-term constraint fixes θ=π/4 (from Q33), giving A=3, clean flip C→B; (e) Trig Id Q39 20-Apr — derivation (1/√2)(√3 cos 1°+sin 1°) ≈ 1.237 not in options, closest is D=(1/2)(√3 cos 1°+sin 1°) with wrong coefficient — preserved as NDA-paper misprint per Probability Q117 Apr 2023 precedent; (f) Trig Id Q39 17-Apr — strict reading of 450°<A<540° gives cos(A/2)=−1/√10 (option D "None"); reduced-angle reading gives +1/√10 (option A); preserved with explicit convention note per UPSC official key A; (g) Prob Q102 19-Apr — bank's stem had extra bar `P(A̅∪B̅)`, PDF shows `P(A̅∪B)` (bar over A only), gives 0.9, clean flip B→A. **Final tally**: 4 stem-extraction fixes + 2 NDA-paper misprints preserved with notes + 1 convention ambiguity preserved with note + 1 additional clean flip from PDF (Q34 Trig Id 19-Apr). All other content-correctness work across M&D + Stats + Trig Id + Prob is closed; only 3 preserved-with-note items remain in the probe, all deliberately documented. **Workflow lesson:** the original May-23 distinction between "wrong-key flip" and "truly corrupted" wasn't crisp enough; both got bundled into "Bucket B defer to /solution-cleanup". Today's refinement (only defer when derivation lands OUTSIDE option set or context is structurally broken) is what made the sweep feasible in one session.
- **2026-05-27 — MHT-CET Chemistry first cleanup (Phase E + 29-chapter Phase D, no Phase B/C needed) — closes bank-wide cleanup.** 13th and final pending subject. Live bank: 1,553 q · 30 ch · 1,511 sub · avg **1.03 q/sub** (joint worst fragmentation in the bank, matching MHT-CET Physics pre-cleanup). **Phase E** — disposed empty duplicate "Chemical Thermodynamics" chapter at order 29 (canonical lives at order 5 as "Chemical Thermodynamics and Energetics"). Re-packed `order_index` 0–28. **No Phase B/C needed** — unique among first-time cleanups so far: all 30 chapter names were canonical MHT-CET 12th syllabus chapters (the extraction prompt's canonical-list discipline had held for chapter names, unlike MHT-CET Physics's pre-cleanup which needed 6-q Phase B + 3 Phase-C merges + 1 Phase-E orphan disposal). **Phase D (29 chapters)** — per-chapter subtopic consolidation via temp-table-driven canonical upsert (`_map` source→canonical + `_canon_ids` upsert + UPDATE questions + orphan check + DELETE non-canonical, all in one atomic DO block per chapter). Each chapter mapped 20–98 source subs into 2–6 canonicals (4–6 for physical-chem topic-rich chapters, 3 for organic chapters with prep/reaction split, 2–3 for thin chapters). Hardened-template variant of [[reclassification-sql-pattern]] Phase D adds three safety probes: pre-flight `unmapped sanity check` (RAISE EXCEPTION if any source sub not in `_map`), post-flight `orphan check` (every question must land on a canonical), post-flight q-count drift check. Caught zero issues across all 29 transforms. **2 cross-chapter moves from ch05 Ionic Equilibria**: `efeb4e70` ("partial pressures → mole fractions" — gas-laws content) → ch01's "Types of Solutions, Solubility and Henry's Law" (Dalton's Law absorbed there); `4eb08ae1` ("Boiling point of butanol isomers" — Alcohols physical-properties content) → ch07 via placeholder subtopic "Physical Properties — Boiling Point of Isomeric Alcohols" that ch07's Phase D later absorbed. **Borderline routing kept within-chapter**: Charles's Law question (`8b4320e1`, "T₂ from V₁ T₁ V₂ at const P") in ch06 Thermodynamics stayed in ch06 (route to "Thermodynamic Systems, Properties and Processes") rather than cross-chapter to ch14 — MHT-CET 12th has no separate gas-laws chapter, and an isobaric V/T change IS a thermodynamic process; preserves chapter self-containment for a single borderline case. **Final shape**: 29 ch · 1,553 q · 121 sub · avg **12.8 q/sub** (1,511 → 121, **−92%**). All 4 invariants clean (0 null subs · 0 empty subs · 0 chapter/subtopic mismatch · 0 cross-chapter set siblings). **Bank-wide cleanup grand total now**: **185 ch · 8,785 q · 661 sub · avg 13.3 q/sub** — **all 13 of 13 subjects fully cleaned at chapter + subtopic level**, closes the cross-subject taxonomy cleanup pass that began 2026-05-13. **Pattern lesson**: when a never-cleaned subject preserves its chapter names (extraction-prompt discipline held there), the cleanup degenerates to a pure Phase D pass — no Phase B/C needed, no orphan chapters to dispose. MHT-CET Chemistry is the first such case; MHT-CET Physics's same-scale first-cleanup needed all four phases because the extraction prompt had auto-created `Magnetism` and `Scalars and Vectors` orphans. **Forward looking**: the bank-wide cleanup is done, but fresh uploads can re-fragment subtopics → Phase D delta cleanup remains the recurring post-upload step per [[post-upload-ritual]]. Memory updated: [[taxonomy-cleanup-progress]] now reflects 13/13 done.
- **2026-05-26 — Upload-form NDA metadata hardening.** Prevent the upload-metadata bug class that produced the 4 misfiled batches the same-day SQL cleanup just fixed. New pure helpers in [src/lib/upload/ndaMetadata.ts](src/lib/upload/ndaMetadata.ts) — `NDA_MONTHS`, `NDA_MONTH_TO_NOTE`, `getNdaPaperFromMonth(month)`, `isNdaCanonical({month, note})` — with 13 TDD cases covering the two canonical pairs, the 3 historical bug-shapes, partial pairs, and case-sensitivity. **Server-side guard** in `/api/upload/preview/route.ts` rejects non-canonical NDA `(pyq_month, pyq_note)` pairs with 400 before staging. Fires after `resolveExam` so it works for both explicit-exam uploads AND auto-detect-from-Course-column uploads. **UI affordance** in `UploadForm.tsx`: when the form's exam selector is set to NDA, the Month input swaps from free-text `<Input>` to a `<Select>` with `{None, Apr, Sep}`; selecting Apr or Sep auto-fills `pyqNote` with the canonical value and renders the Note field read-only with an explanatory hint. Non-NDA exams (MHT-CET et al.) keep the free-text inputs unchanged — MHT-CET's per-shift notes ("10th May Shift 1") need the freedom. When the form leaves examId blank for auto-detection, free-text stays too; the server-side guard catches NDA non-canonical at preview time. 798/798 vitest green, prepush clean. **The 4 misfiles today would have been impossible with this guard.** Follow-up unaffected: still need the `audit:underlines` script in step 6 of [[post-upload-ritual]] for English/Biology batches; the NDA-metadata guard is orthogonal.
- **2026-05-26 — Provenance bracket on QuestionCard footer (commit 6e57865).** Teachers cross-referencing bank questions against source PYQ PDFs had to filter `/browse` by year + manually disambiguate paper-sittings. Added a `[Q# · disambiguator · year]` chip in the expanded card's footer row (between Edit and Report). New pure helper [src/lib/questions/formatProvenance.ts](src/lib/questions/formatProvenance.ts) with 9 TDD cases — exam-aware disambiguator (NDA uses `pyq_month` since Apr⇒NDA 1, Sep⇒NDA 2 is canonical; every other exam uses `pyq_note` which carries day+shift like "10th May Shift 1"). Format: `[Q95 · Apr · 2025]` for NDA, `[Q95 · 10th May Shift 1 · 2024]` for MHT-CET. `QuestionRow` extended with 4 nullable fields (`questionNumber`, `pyqYear`, `pyqMonth`, `pyqNote`) threaded through `queryQuestions` + `queryQuestionsByIds`. Test-fixture sweep across 7 docx/group-by-set/underline-roundtrip suites adds `: null` for the new fields. Required pre-step: the NDA metadata canonicalization landed earlier in the day (4 SQL UPDATEs fixed 510 misfiled questions) — without that, the exam-aware rule would need ugly conditional logic for the 10.5% anomalies. After the cleanup, the rule is `examName === "NDA" ? pyq_month : pyq_note` with zero edge cases. Browser KaTeX preview unchanged. 785/785 vitest green, prepush clean.
- **2026-05-26 — NDA paper metadata canonicalized (4 SQL UPDATEs, 510 q).** NDA papers follow a strict convention — April = NDA 1, September = NDA 2; UPSC has never run a retest or "Shift 2". The bank had 4 anomaly batches where upload-time metadata was entered wrong: (1) 2025 Apr "Shift 2" / 120 q Maths → corrected to "NDA 1" (Maths section of NDA-1 Apr 2025; the matching 150 q GAT was already "NDA 1"); (2) 2018 Sep "Shift 2" / 120 q Maths → "NDA 2"; (3) 2023 Sep "NDA 1" / 120 q Maths → "NDA 2"; (4) 2017 Apr "NDA 2" / 150 q GAT → `pyq_month` changed to "Sep" (the missing Sep-2017 NDA-2 GAT section that completes that paper from 120 → 270 q). The first three were Maths-section misfiles (a full NDA paper = 150 GAT + 120 Maths = 270; the GAT half was tagged right but the Maths half got "Shift 2" or "NDA 1" instead of matching the GAT). The fourth was a whole-batch month error. User verified Pattern B against PDFs before commit. Post-fix state: every (year, month, note) tuple is canonical — 18 paper-batches × 270 q each, every Apr⇒NDA 1, every Sep⇒NDA 2, no Shift labels. 2020 Sep is genuinely absent (UPSC cancelled it for COVID); 2026 Sep hasn't happened yet. **No `upload_jobs` rows updated** — those reflect audit truth (what was uploaded at the time). **No content_hash impact** — hash is over text+options+answer, not metadata. Bank count unchanged at 4,860 NDA. **The canonicalization unlocked the bracket helper's simple `examName === "NDA"` rule** — without it the helper would have needed a per-(year,month) anomaly table. Captured as a [[nda-paper-canonical]] memory + a doc-the-rule sentence here for future upload audits: when an NDA upload arrives with `pyq_month != pyq_note`'s canonical, treat as a metadata bug and verify against source PDF before commit.
- **2026-05-26 — TEACHER edit link surfacing on /browse (commit a44016a).** Migration 0025 (same day, earlier) gave TEACHER edit access on the backend but the discoverability link on /browse stayed gated to `isAdmin`. Teachers signed in and saw no way to reach `/questions/[id]/edit`. Renamed the `isAdmin` prop on `QuestionList` + `QuestionCard` to `canEdit`; gate widened to `member?.role === "ADMIN" || member?.role === "TEACHER"`. The edit page itself still threads `isAdmin` separately to hide the Delete button + Visibility toggle (admin-only inside the form, server-rejected via `applyEdit`'s `forbidden_field` guard). `isAdmin` prop on QuestionCard had a single consumer (the Edit link conditional), so the rename had zero collateral. Per the [[click-to-reveal-admin-bypass]] rule, `isAdmin` is for chrome that genuinely needs admin gating; `canEdit` is the right primitive for "show the Edit link" since Edit is an action both roles can perform. No test changes (UI gate, no logic) per the [[tdd-calibration]] rule.
- **2026-05-26 — Dropped 10 cross-link banners from `/guide/nda-maths` (commit 8ae6fba).** The landing page carried 9 cross-subject "Also new: NDA [English/Physics/…] strategy guide" banners + 1 "/notes/nda-maths/statistics" banner — ~230 lines of JSX (lines 73–329) pushing the actual NDA Maths content (chapter table + principles + strategy) below the fold. User flagged "such references in guide are not necessary" + "look too cluttered". Removed all 10 + dropped 9 unused lucide icon imports (`Atom`, `FlaskConical`, `Globe`, `Landmark`, `Languages`, `Leaf`, `Newspaper`, `Scale`, `TrendingUp`); kept `NotebookPen` (still used in chapter table chip) + `ArrowRight` (used elsewhere). `nda-maths/page.tsx` collapsed 466 → 208 lines, restoring structural parity with the other 7 multi-route guide landings (181–184 lines). **Same pattern as the 2026-05-21 `/browse` Hero pill row removal** — both shipped when guides were new and discoverability needed scaffolding, both became pure duplication once PrimaryNav (Bank · Guides · Notes) + `/guide/nda` exam-picker + `/nda` exam home + Footer covered the same surface. Lesson worth recording as a feedback memory: [[stale-scaffolding-decay]] — "New:" / "Also new:" prefixes and cross-link banners decay once primary IA covers discoverability; revisit them every few months and prune. **Note**: this contradicts the 2026-05-18 `feedback_no_guide_meta_commentary.md` line "Cross-link banners to sibling guides are fine and useful — they're navigation, not meta-commentary"; the rule's been refined — cross-links are still fine as PRIMARY navigation (Footer, PrimaryNav, `/guide/nda` grid), but stale once a redundant landing-page banner reaches mass.
- **2026-05-26 — Word export underline fix: bypass OMML borderBox for `\underline{\text{...}}` and `\underline{\textit{...}}`.** Teacher reported the underlined keyword in NDA English antonym questions wasn't visible in the exported Word file (e.g. Q5 `01d50cab` "He nodded `absently` throughout the meeting." — rendered with no underline on `absently`). Root cause: the 2026-05-13 convention routed `\(\underline{\text{word}}\)` through `temml → mml2omml`, emitting `<m:borderBox>` with `hideTop/hideLeft/hideRight="on"`. The OMML markup is technically correct (only the bottom border drawn = supposed underline) and `tests/underline-roundtrip.test.ts` confirmed those markers landed in `word/document.xml` — but Word's actual rendering of an inline-math borderBox suppresses the bottom border under the `<m:mathPr defJc="left" wrapIndent="0" lMargin="0" rMargin="0">` defaults we inject for fraction alignment. The convention had been TDD-verified at the XML-markup level but never visually verified in Word. **Fix**: bypass the math pipeline for the documented underline patterns. New `OmmlSegment` variant `{ type: "underlined-text"; content; italic }` in [src/lib/export/ommlBuilder.ts](src/lib/export/ommlBuilder.ts) emitted when the whole inline-math segment matches `UNDERLINE_BYPASS_RE` (`^\s*\\underline\{\s*\\(text|textit)\{([^{}]+)\}\s*\}\s*([.,;:!?]?)\s*$`). `mathRuns` in [docxBuilder.ts](src/lib/export/docxBuilder.ts) renders the new variant as a native Word run — `TextRun({ text, underline: { type: "single" }, italics: italic ? true : undefined })` — producing `<w:r><w:rPr><w:u w:val="single"/></w:rPr><w:t>…</w:t></w:r>` directly, which renders rock-solid in every Word version, Word Online, LibreOffice, Google Docs. Trailing-punctuation capture handles the real-bank pattern where authors put sentence-ending `.,;:!?` INSIDE the math delimiters (`\(\underline{\text{insidious}}.\)`); the punctuation splits into a separate text segment so the rendered sentence keeps its full stop. Italic variant covers the taxonomy-formatting question (`860087ed`) where option D is `\(\underline{\textit{Amoeba}}\)` — without the underline, option D becomes visually identical to option A and the question is unsolvable. **What didn't change**: DB convention (`\(\underline{\text{word}}\)` stays in the bank — no backfill); browser KaTeX preview (KaTeX uses CSS `text-decoration: underline`); the `latexToOmml` borderBox path itself (preserved as fallback for direct callers — `/notes` preview + non-export surfaces — with isolated unit-test coverage so the LaTeX pipeline still has regression protection). **Audit + safety net**: new `scripts/audit-underline-patterns.ts` (`npm run audit:underlines`) scans every PUBLIC question's text/context/solution + every option for `\(\underline{...}\)` segments, classifies each as "covered" (bypassed → native underline) or "fall-through" (still routes through borderBox), prints a ranked list of distinct fall-through patterns. Live-bank run after the fix: **639/639 payloads covered, 0 fall-through**. The first run caught 5 trailing-period cases (`\(\underline{\text{insidious}}.\)`) that drove the regex extension. Fold into the post-upload ritual after each new English / Biology batch to catch silent-fall-through regressions. **Why "extend regex" over "fix the 5 DB rows"**: future uploads will keep producing trailing-punctuation-inside-delimiters patterns (extraction prompt doesn't enforce delimiter placement). Author-defensive in code beats chasing data each batch. Test churn: existing `underline-roundtrip.test.ts` updated — kept the LaTeX-pipeline borderBox assertion, inverted the docx-export assertion from "find `<m:borderBox>`" to "find `<w:u w:val="single"/>` AND don't find `<m:borderBox>` wrapping the same payload"; new fall-through test asserts non-matching shapes (`\(\underline{x}\)`) still route through OMML. New `omml-builder.test.ts` cases (10) cover the bypass + trailing-punctuation + each fall-through shape. Total: 776/776 vitest green, prepush clean. Out of scope (0 occurrences in bank): bold-underline (`\textbf` variant), mixed math+underline payloads, chained `\text{}\text{}`, block-math underline — all fall through to the still-broken OMML path with same behaviour as today (no new regression).
- **2026-05-26 — TEACHER role can edit questions + admin Members management UI (migration 0025, commit 683d9b2).** Reverses the long-standing "TEACHER is read-only" stance set during M2 (migration 0004). Migration `0025_teacher_editor_role.sql` adds `private.current_user_can_edit_questions()` helper (true for `org_members.role IN ('ADMIN','TEACHER')`); rewrites `questions UPDATE` policy → editor; splits the old `admin write options` FOR ALL into `admin insert options` + `editor update options` + `admin delete options`; opens the three `storage.objects` policies on the `question-images` bucket from admin-only → editor (TEACHER + ADMIN can upload/update/delete in own-org folder); adds `last_edited_by uuid references auth.users(id)` + `last_edited_at timestamptz` columns on `questions` + partial index. `src/lib/auth.ts` gains `requireEditor()` (401 if not signed in, 403 if role not in {ADMIN, TEACHER}). `applyEdit` signature gains optional `editorUserId` + `callerRole` params — stamps audit fields when given, AND returns new `kind: "forbidden_field"` result if `callerRole === "TEACHER"` AND `payload.visibility !== existing.visibility` (server-side guard against tampered requests; route maps to HTTP 403). `PUT /api/questions/[id]` + `POST /api/questions/[id]/leave-set` swap from `requireAdmin()` → `requireEditor()`. `/questions/[id]/edit` accepts TEACHER (was admin-only redirect to /dashboard); EditQuestionForm receives `isAdmin` prop and hides Delete button + Visibility section when `false`. `/dashboard` redirects TEACHER → /browse (admin-only territory). New `/dashboard/members` admin UI (page.tsx + MembersClient.tsx) wraps `/api/admin/members` POST endpoint (single action-discriminator route: `list | create | reset | remove | update_role`). Backed by new `src/lib/members/admin.ts` — pure validators + 5 DB helpers using `createSupabaseAdminClient()` (service-role; `auth.admin.*` needs it). Provisioning shape mirrors NDA Tracker's `TeachersTab.jsx` + `/api/teacher-account`: admin types **name + email + password + role** → auth user created with `user_metadata.name` stamped, `org_members` row inserted in one operation. Last-admin protection + self-protection (caller can't remove themselves OR demote themselves) enforced in the helpers. Test coverage: 32 new (8 validators + 5 RLS smoke + 5 audit+visibility-guard + `storage-rls.test.ts` updated for the opened editor write). All 764/764 vitest green; advisor lints unchanged (only the pre-existing `rate_limits no-policy` info + auth leaked-password-protection warning, both intentional). **Why:** user asked to enable teachers to edit questions, taking the NDA Tracker auth-shape as a reference. Borrowed: name + email + password admin form, serverless function with service-role, `user_metadata.role` stamping. Did NOT borrow: NDA Tracker's read-only-by-UI constraint (QB's TEACHER writes; the gating is in RLS + route guard, not just convention). Email serves as username (Supabase auth is email-native); the "non-email username" alternative was offered and the user picked email for simplicity.
- **2026-05-26 — Word export polish: student-first set banner + flush-left layout + lowercase option labels + per-question spacing (commits 92034c4, ac509df, 4d6b851).** Four coordinated render-time fixes to the `.docx` export, all in `src/lib/export/docxBuilder.ts` (+ new pure helper `src/lib/export/stripPassageCount.ts`). **(1) Student-first set banner:** when a user exports a subset of a set's siblings (cart picks just one, filter excludes others, extraction missed some), the verbatim PYQ banner "Set: …for the three (03) items that follow" promised items not on the page. Replaced with `Common context for questions X-Y:` where X-Y is the 1-indexed position of the first + last sibling in the export's question list (computed from a running position counter across the `groupBySet` loop), AND the passage is run through `stripPassageCountPhrase` which regex-strips the count claim (`for (the next |the )?\w+\s*(\(\d+\))?\s*items? that follows?` → `for the items that follow`). Bank text stays verbatim in DB; only render is normalised. Set-of-1 in the export falls through to inline `Context:` (no banner — a one-question "Set:" framing reads worst). **(2) Flush-left numbering:** changed `{left: 360, hanging: 360}` → `{left: 0, hanging: 0}` + `LevelSuffix.SPACE`. Wrap lines come back to column 0 instead of 0.25", aligning with options + Context + Set banner. **(3) Lowercase option labels:** `(A)/(B)/(C)/(D)` → `(a)/(b)/(c)/(d)` in both question paper AND answer key (`opt.label.toLowerCase()` at render; DB still stores uppercase). **(4) Per-question spacing:** one empty paragraph after every question (standalones AND set siblings) in the paper so the student's eye has a visible boundary between Q_n's option D and Q_{n+1}'s stem. The trailing blank after a set group moved to per-sibling instead, no double blank. Answer key: plain mode stays tight (one paragraph per question), solution mode gets the same blank between (answer + solution) blocks. New tests: `tests/docx-set-rendering.test.ts` (3 tests) + `tests/docx-spacing.test.ts` (5 tests) + `tests/strip-passage-count.test.ts` (10 unit tests). Existing `tests/docx-layout.test.ts` + `tests/docx-export.test.ts` updated for new numbering shape + lowercase labels. **Why:** progression across 3 commits — flush-left came from user feedback that wrap lines weren't aligned with the rest of the doc; lowercase was a same-day add; the K-of-N banner replacement was the third iteration after Option A (set-of-1 only) and Approach 2 (K-of-N from DB) — user pushed back that banner overpromise still happened on 2-of-3 cases, and the student-POV reframe ("student doesn't know what 'bank' means") drove Approach 4 which names the actual question numbers. Inter-question spacing was the fourth ask in the same review.
- _For entries dated **2026-05-08 through 2026-05-25** + the **2026-05-27 per-chapter NDA Maths audit detail (26 entries)**, see [`DECISIONS_HISTORY.md`](DECISIONS_HISTORY.md) — 111 entries archived 2026-05-27 (morning) + 26 more archived 2026-05-27 (evening) after the consolidated NDA Mathematics audit milestone covered the same ground in summary form. Foundations (M1–M3, 2026-05-08) remain above; the consolidated milestone + recent infrastructure entries stay here._

## Pending work

The chronological decisions log above captures *why* shipped choices were made; for what's pending, see [`ROADMAP.md`](ROADMAP.md) at the project root. It consolidates every "deferred", "future scope", and "explicitly chose not to build" annotation scattered through this file plus open product questions (e.g., the `principle_tags text[]` column proposal). When an item ships, move its entry into the Decisions log here with a `2026-MM-DD —` prefix and a "Why" sentence so the two files don't drift.

## Cleaned subjects · post-delta sizing

At-a-glance sizing of the cross-subject taxonomy cleanup (chapter + subtopic technique-level canonicals — see Decisions log entries dated 2026-05-13 onward for per-subject narratives). For per-chapter **content-correctness** audit status (orthogonal to taxonomy structure), see [[content-audit-progress]] — **NDA Mathematics fully closed (31 of 31 chapters, all 2,160 q)** as of 2026-05-27, ~22.6 % bank-wide.

**Contract:** numbers below reflect **live counts as of the most recent cleanup pass per subject** (not historical "cleanup-pass-time" snapshots). They match live DB exactly the moment a subject ships, then drift again with the next upload — re-cleanup (Phase D delta) re-syncs them. After any upload batch, treat any subject in this table as stale until SQL-verified or re-cleaned. The live bank-size line at the top of this file is the always-current total. See [[project-docs-staleness]].

**Done — 13 of 13 subjects · 185 ch · 8,785 q · 657 sub** (avg 13.4 q/sub; NDA Maths re-Phase-D'd 2026-05-17; all 9 other NDA subjects on 2026-05-18 after fresh uploads; MHT-CET Maths re-Phase-D'd 2026-05-23; MHT-CET Physics first cleanup 2026-05-23; MHT-CET Chemistry first cleanup 2026-05-27; NDA Maths Probability re-merged 113 → 109 sub on 2026-05-29 for the notes pedagogy split):

| Subject | Chapters | Questions | Subtopics |
|---|---|---|---|
| NDA Mathematics | 31 | 2,160 | 109 |
| MHT-CET Mathematics | 26 | 1,588 | 69 |
| MHT-CET Physics | 24 | 1,577 | 91 |
| MHT-CET Chemistry | 29 | 1,553 | 121 |
| NDA English (GAT) | 8 | 900 | 29 |
| NDA Physics | 14 | 449 | 50 |
| NDA Chemistry | 12 | 262 | 47 |
| NDA Biology | 9 | 190 | 39 |
| NDA Geography | 7 | 345 | 36 |
| NDA History | 4 | 260 | 22 |
| NDA Polity | 4 | 90 | 14 |
| NDA Economics | 1 | 24 | 3 |
| NDA Current Affairs | 8 | 180 | 31 |

**No subjects pending.** Bank-wide subtopic taxonomy is now technique-canonical end-to-end. The post-upload ritual still applies — fresh uploads can re-fragment subtopics, requiring Phase D delta cleanup; see [[post-upload-ritual]].

Workflow: inline chapter-by-chapter in chat (no scripts), descending q-count first, source-source collision-loop for bulk merges, per-question reclassification for catch-all chapters ≤~50 q. Patterns + SQL templates: `[[reclassification-sql-pattern]]` + `[[taxonomy-inline-iteration]]`.

## Notes editorial workflow

Authoring a new chapter of `/notes` content. The loop is locked-in as of the Phase 2 ship (concept drills come from the DB, not TS). Order matters because some steps depend on earlier ones.

### 0. Decide the chapter structure — pedagogy-first, bank is a guide

The chapter's subtopic breakdown and concept order are decided from the **teacher/student point of view**: the content must read as a coherent, logical learning progression (foundations → core method → applications → advanced). **The bank's subtopic taxonomy is a GUIDE, not the organizing principle** — the bank is structured for *drilling/filtering on /browse*, so it tells you *what* is tested and *how much* (coverage + emphasis), NOT how to *sequence the teaching*. It frequently splits one pedagogical idea across several subtopics.

Where the bank's split is finer or differently-cut than the teaching arc, **reshape the bank to match the pedagogy** (Phase D merge/rename per [[reclassification-sql-pattern]]) rather than forcing the notes to mirror an incoherent split. The notes `subtopicName` still maps 1:1 to a DB subtopic (lint + concept-tag constraint), so align the DB subtopics to the pedagogical structure *first*, then author 1:1 against the consolidated set.

Concretely: (1) read the bank — sizes, difficulty mix, year spread, real stems — to learn what to teach + emphasise; (2) design the subtopic list + concept ramp as a teacher would, for coherent progression; (3) if the bank's split doesn't match, merge/rename DB subtopics so the 1:1 mapping lands on coherent teaching units; (4) proceed to step 1. **Precedent (2026-05-29, NDA Probability):** the bank stored dice / coins / balls-and-urns / arrangements as 4 separate subtopics that are pedagogically one idea (classical probability = favourable/total on different props); these were merged into "Probability via Counting" so the notes could teach one coherent "Classical Probability" page instead of four repetitive ones. See [[notes-structure-pedagogy-first]].

### 1. Write the TS module

Under `src/app/notes/<exam>/<chapter>/_data/`:

- `chapter.ts` exports `<CHAPTER>_CHAPTER: ChapterNote` with `chapterName` (must match the canonical DB chapter name exactly), `title`, `intro`, and `subtopicOrder: string[]` of stable subtopic slugs.
- One file per subtopic (`<subtopic-slug>.ts`), exporting a `SubtopicNote` with `subtopicName` (canonical DB name), `title`, `oneLineDefinition`, `whyItMatters`, and `concepts: ConceptUnit[]`. **`ConceptUnit` is a discriminated union** (`kind: "formula" | "reference"` is **required**). Both variants share `slug` (stable identity for DB tags), `name`, `intuition`, `definition`, optional `selfCheckExample` (fresh problem, full solution behind one reveal), optional `practiceSet` (Level 1 mastery reps — ~4 short prompt+answer+method items, authored not bank-derived), optional `visualizationSlug` (a static or interactive SVG diagram — build it under `_components/visualizations/`, add its slug to the `VisualizationSlug` union, and add a `case` in `renderVisualization`; all-SVG, no 3D library — see the `_components/` note above), optional `pyqExampleId` (one featured PYQ), and optional `traps`. Variants differ only on the **core teaching slot**:
  - **`kind: "formula"`** — carries optional `formula?: FormulaSpec` (label + LaTeX + symbol legend) and required `authoredExample` (prompt + step-by-step solution + final answer). Use for technique-driven concepts where the teaching unit is "apply this formula / method to a problem". The pedagogy ramp: intuition → definition → formula? → visualization? → worked example → self-check? → Level-1 practice? → featured PYQ → traps.
  - **`kind: "reference"`** — carries required `table: ReferenceTable` (`columns: string[]` with 2–5 plain-text headers, `rows: ReferenceRow[]` with `cells: string[]` parity-checked against `columns.length`, optional per-row `noteAmber` for amber-tinted callouts, optional per-row `pyqExampleId` for `[Q]` chips, optional table-level `caption`). Use for named-fact recall where the teaching unit is "memorise this table" (frequency bands, transducer chain, ear anatomy, periodic-table-style lookups). The pedagogy ramp: intuition → definition → visualization? → reference table → self-check? → Level-1 practice? → featured PYQ → traps. Self-check + practice on reference variant ARE NOT redundant — they're "spot the wrong row" / "single-row recall" reps.
- **Picking the variant**: read the bank questions for that concept. If the answer hinges on a derivation or formula application → formula. If the answer hinges on memorising a specific named fact ("ultrasonic is > 20 kHz", "cochlea converts pressure to electrical") → reference. A subtopic can (and often should) mix both — Sound's `properties-of-sound-waves` has 2 formula concepts + 1 reference, `sonar-and-ultrasonic` is pure reference, `echo-acoustics-reverberation` is pure formula.
- **Foundations**: prepend a few primitive concepts (no `pyqExampleId`/drill — exempt from notes-lint check 4 **only on formula variant**; reference concepts are never foundations because they are the bucket for the subtopic's flat-fact recall). Give procedural foundations a `practiceSet`; skip it on purely-conceptual ones.
- **Plain-text fields** (`title`, `oneLineDefinition`, `whyItMatters`, `concept.name`, `formula.label`) render WITHOUT KaTeX — use Unicode/prose, no `\(...\)`. `npm run notes:latex` catches leaks.
- **Markup in body fields** (2026-05-29): use `**bold**` for key terms (renders via KatexRenderer / RichText), and in the `definition` field use `- ` line-prefixed **bullets** when the definition is list-shaped (enumerates terms/types/properties) — `RichText` renders them as a `<ul>`. Keep narrative definitions as prose; don't force single statements into bullets. `**`/`- ` are literal in the plain-text fields above — never use them there.
- `index.ts` re-exports the union as `<CHAPTER>_NOTES: Record<slug, SubtopicNote>` and `<CHAPTER>_SLUGS: string[]`.

**No page files to write.** The chapter's `page.tsx` + `[subtopicSlug]/page.tsx` are thin wrappers — clone the Statistics pair, swap the two `getNotesChapterBySlug(...)` args. ALL data-loading + layout lives in the shared `NotesChapterLanding` / `NotesSubtopicPage`.

**The slug is the stable contract** — once a chapter ships, never rename a concept `slug` without a matching UPDATE on `question_concept_tags`. Notes-lint flags orphan slugs as errors but only after they've already broken pages.

### 2. Register in `NOTES_CHAPTERS` (single source of truth)

`src/lib/notes/chapters.ts` is the registry. Append one entry — `{examName, subjectName, subjectRoute, subjectDisplay, chapterSlug, chipLabel, chapter, notes, slugs}` — importing the chapter's `_data` exports. Consumers derive from it automatically: the shared page renderers (`NotesChapterLanding` / `NotesSubtopicPage`), `subtopicSlugRegistry` (concept-tag validation + admin tagging UI), `links/notesIndex` (chip lookups), `links/tagNames` (concept-name lookups), `app/sitemap` (route entries), `/notes/<subjectRoute>/page.tsx` (chapter cards), `scripts/notes-lint` + `scripts/notes-latex-audit` (validation). The only per-chapter files are `_data/*` + the two thin route wrappers.

### 3. Run a chat tagging session

For each chapter, propose a `(question_id, concept_slug)` mapping for every PUBLIC question. Same inline-iteration pattern as the taxonomy cleanup work.

Audit query:
```sql
SELECT q.id, q.difficulty, q.pyq_year, q.set_id, LEFT(q.text, 200) AS text_preview
FROM questions q
WHERE q.chapter_id = '<chapter-uuid>' AND q.visibility = 'PUBLIC'
ORDER BY q.set_id NULLS LAST, q.difficulty, q.pyq_year DESC NULLS LAST;
```

After user approval, INSERT via the standard atomic block:
```sql
INSERT INTO question_concept_tags (question_id, subtopic_slug, concept_slug, tagged_by_llm)
VALUES
  ('<q-uuid>'::uuid, '<subtopic-slug>', '<concept-slug>', true),
  -- ...
ON CONFLICT (question_id, subtopic_slug, concept_slug) DO NOTHING;
```

Mark `tagged_by_llm = true` on LLM-suggested rows so a future admin-review query can find them. The DB row count plus the LLM flag is the audit trail.

### 4. Verify integrity

- `npm run notes:lint` — every `subtopicName` resolves under live taxonomy; every `pyqExampleId` is PUBLIC; every DB tag's `concept_slug` matches a known TS concept (catches typos in either direction); each TS concept has ≥1 DB-tagged question (soft warn for un-tagged concepts). Exits non-zero on error.
- `npm run notes:latex` — no LaTeX markup in plain-text fields, no unbalanced `\(...\)` / `\[...\]` delimiters. Exits non-zero on either.
- `npm run prepush` — typecheck + lint + build clean.

### 5. Ship

ISR-cached `/notes` pages pick up new tags within the 1-hour `revalidate` window. Commit the TS module + the new registry entry; the DB rows are already live from Step 3.

### Invariants to verify before commit

- **Set-sibling co-location**: questions sharing `set_id` (passage groups) MUST be tagged with the same concept(s). Splitting a passage group breaks `/browse` + Word-export grouping. Audit `set_id` groupings in Step 3 before drafting the mapping.
- **`pyqExampleId` stays in TS**: the one editorial "featured" example per concept is hand-picked and lives in the TS module. Drill UUIDs come from the DB — don't reach back to a TS array.
- **Concept slugs are stable IDs**: never edit a `slug` in a published note. If a rename is genuinely necessary, do it as a one-shot `UPDATE question_concept_tags SET concept_slug = '<new>' WHERE subtopic_slug = '<sub>' AND concept_slug = '<old>'` in the same transaction as the TS edit.

See "Design axes" above for why drills live in the DB while pyqExampleIds + concept definitions live in TS — they're on different axes.

## After a new upload

When a new question-batch lands, run the 6-step ritual in order. The upload pipeline is auto (taxonomy auto-creates, `content_hash` dedup, default PUBLIC visibility, live counts in `/browse` + dashboard + `/guide` TOP_11 + `/notes` drills). What's NOT auto and needs this ritual: **tag coverage, taxonomy fragmentation cleanup, and TS-static editorial copy**.

1. **Sanity-check the batch** (always) — confirm `inserted` vs PUBLIC/PRIVATE counts, PYQ metadata stamped, no missing years
2. **Taxonomy delta cleanup** (if ≥5 new subtopic auto-creates or a new chapter name) — Phase D delta inline; don't restart from scratch. Precedent: 2026-05-17 NDA Maths re-Phase-D (~770 new auto-creates closed in one session)
3. **Principle-tag delta survey** (if new questions in TOP_11 chapter footprints) — incremental per-new-chapter, not whole-bank
4. **Concept-tag delta session** (if new questions in `/notes`-covered subtopics) — also revisit dropped-gap ConceptUnits; bank growth can mature what was previously dropped
5. **Editorial copy refresh** (if counts move ≥5–10%) — `whyItMatters`, OVERVIEW constants, CHAPTER_TABLE, strategy %HARD, trends callouts. Mechanical TS edits
6. **Integrity check** (always) — `npm run notes:lint` + `npm run prepush` before commit

Steps 2–5 are conditional and can be deferred / queued across sessions. Full procedure + SQL probes + skip-conditions: [[post-upload-ritual]].

## Adding a new RLS-protected table

Follow the `questions`/`options` pattern (see `0002_rls.sql` + `0004_upload_staging.sql`):
1. Enable RLS.
2. Read policy: `org_id = private.current_user_org_id()`.
3. Write policy: same plus `private.current_user_is_admin()` if admin-curated.
4. Add a test in `tests/upload-rls.test.ts` (or new equivalent) proving a TEACHER cannot write and a member of another org cannot read.
5. Run `mcp__supabase__get_advisors` after applying the migration.

## Live infra

- **Supabase project:** `wunvtnqlzjrkvolslbnm` (https://wunvtnqlzjrkvolslbnm.supabase.co)
- **Production:** https://question-bank-sage.vercel.app — auto-deploys from `main`
- **Repo:** https://github.com/lwspune/question-bank (public)
- **Auth callback URLs allow-listed in Supabase Auth:** production + `localhost:3000` (legacy from the magic-link era; harmless to leave even though we currently use password auth)

## Operations

Day-to-day knobs and where to look when something goes sideways.

### Monitoring

- **Vercel function logs:** dashboard → Project → Logs. The `/api/export`, `/api/sync/mock`, and `/api/upload/*` route handlers all `console.error` on the catch path; surface there.
- **Vercel Analytics:** dashboard → Project → Analytics. Pageviews + visitor counts. Free tier, cookieless. Frontend errors don't surface here yet — if/when noise picks up, consider Sentry.
- **Supabase logs:** dashboard → Logs → API/Postgres. Useful when an export 500s and you want to see the underlying SQL error.
- **Supabase advisor:** run `mcp__supabase__get_advisors` periodically (or after a migration). Two acceptable lints today: `rls_enabled_no_policy` on `public.rate_limits` (intentional, service-role-only access) and `auth_leaked_password_protection` (Supabase auth setting, can be enabled in dashboard).

### When to upgrade tiers

- **Vercel Hobby (current)** is bounded by ~100 GB-h/month of function execution and ~100 GB of bandwidth. The export endpoint is the heavy hitter — when daily traffic crosses ~500 papers, watch the dashboard's bandwidth meter weekly.
- **Supabase Free (current)** is bounded by 500 MB DB + 1 GB storage egress + 5 GB bandwidth. The public bucket of question images is the egress risk; if egress crosses ~80% of the cap mid-month, plan a Pro upgrade.

### Rate limit visibility

The rate-limit table (Phase D) is service-role only and not exposed to the API. To see who's hitting the limit:
```sql
select bucket, count, window_start
from rate_limits
order by count desc
limit 20;
```
Buckets are formatted `export:anon:<ip>` or `export:user:<user_id>`.

### Rotating SYNC_SHARED_SECRET

No code change needed. Set a new value in:
1. Vercel env vars (Question Bank project) → trigger redeploy
2. MHT_CET_AI's env (whatever variable name its publisher uses) → redeploy

The secret is read at request time via `process.env.SYNC_SHARED_SECRET`, so a redeploy on Question Bank's side picks up the new value immediately. Until both sides match, syncs return 401.

### Managing members (admins + teachers)

`/dashboard/members` is the admin UI as of 2026-05-26. Add new admins/teachers with name + email + password + role; reset passwords; change roles; remove members. Last-admin protection + self-protection prevent locking yourself out. The page is admin-only; teachers are redirected to /browse.

For one-off ops outside the UI (cron, scripted bulk reset, etc.), the helpers in `src/lib/members/admin.ts` are importable from any server-only context (they use `createSupabaseAdminClient()`). For purely manual SQL reset of a password without the UI:
```sql
update auth.users
set encrypted_password = crypt('new-password', gen_salt('bf'))
where email = 'admin@example.com';
```
Run in the Supabase SQL editor with the service role.

### Flipping visibility on existing questions

The default for new rows is **PUBLIC** (since migration 0022). To take a batch PRIVATE (e.g., a paid-org-only release):
```sql
update questions
set visibility = 'PRIVATE'
where source_file = 'NDA_2024_paper.xlsx';
```
The reverse (flipping PRIVATE → PUBLIC) is rarely needed now since the default is PUBLIC, but the same shape with `set visibility = 'PUBLIC'` still works.

### Setting PYQ year/month/comment for a whole upload

Preferred path: open `/uploads/<jobId>` as the org admin, click **Edit** on the PYQ row, fill any of Year / Month / Comment, save. The PATCH covers all questions linked via `upload_job_id`. For a one-shot SQL backfill (e.g. tagging a batch that was uploaded before Phase A landed):
```sql
update questions
set pyq_year = 2025, pyq_month = 'May', pyq_note = 'Shift I'
where source_file = 'MHT_CET_2025_PCM.xlsx';
```
Use SQL only when you can't reach the upload via the UI (ambiguous `upload_job_id`) — otherwise the UI is auditable.

### Deleting a whole upload

`/uploads/<jobId>` → **Delete upload** removes every question linked via `upload_job_id` (cascades options + question_images via FK), then deletes the storage objects, then deletes the `upload_jobs` row. Pre-0013 questions have `upload_job_id = NULL` and are unreachable via this UI — delete those by `source_file` from SQL if needed:
```sql
delete from questions
where source_file = 'BAD_BATCH.xlsx' and upload_job_id is null;
```
