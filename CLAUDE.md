# PYQ Vault — project notes

**Brand:** the product is **PYQ Vault**, live at **https://www.pyqvault.com** (rebranded from "Question Bank" 2026-06-04 — the repo/folder/Vercel-project are still named `question-bank`; in code the brand is `SITE_NAME` in `layout.tsx`). Lowercase "question bank" in prose still means the question corpus, not the brand.

Free, public past-year-question paper builder for Indian entrance exams. Anyone can filter the public bank by exam/chapter/subtopic/difficulty/PYQ year, then download a Question Paper + Answer Key as Word files. Admins (org members) upload Excel batches privately, then flip individual questions PUBLIC. Sibling app `MHT_CET_AI` posts finalized mocks to `/api/sync/mock` for cross-app dedup. Currently MHT-CET + NDA + **JEE Mains** (2021 — **718 q PUBLIC**: standard shift Papers 1-10 + 18, plus compilation Paper 11; Paper 12 staged PRIVATE; ingested via the `scripts/jee/` pandoc pipeline — see [[jee-mains-ingestion]] for the per-paper state + resume point) + **CDS English** (new exam; **ALL 19 papers ingested PRIVATE — 2017-I … 2026-I = 2,280 q**; scanned booklets, **no answer key** → LLM-derived + confidence-flagged answers awaiting human spot-check before PUBLIC; `scripts/cds/` pipeline + per-exam dedup migration 0038 — see [[cds-english-ingestion]] + [[section-based-exam-ingestion]]) + **Foundation Course** (NEW exam, 2026-06-19/20 — the LWS "NDA Foundation" Class 9/10 NCERT worksheets; **Chemistry COMPLETE: ~1,014 q PUBLIC across all 8 chapters (+11 flawed PRIVATE), 85 figures, `question_kind='practice'`** — a Foundation Course has no PYQ corpus so the worksheets ARE its bank, and `/browse` defaults to the Practice view for it (`practiceOnly` flag); no printed answer keys → every answer DERIVED + REVIEW-flagged; `scripts/foundation/` pipeline incl. a docx→PDF step + figure-attach; **Physics COMPLETE — 991 q PUBLIC + 43 flawed PRIVATE across 9 chapters (subject `bf9528d4-…`; text-phase 2026-06-21 = 894 q, figure-attach 2026-06-22 = +140 figure-Qs / 103 figures, review-pass 2026-06-22 = all 127 REVIEW-flagged re-derived → 11 key-flips + 43 flawed kept PRIVATE, then flipped PUBLIC); **Biology COMPLETE 2026-06-23 — 847 q PUBLIC + 40 flawed PRIVATE across 7 chapters (subject `7d34c419-…`; 14 docx worksheets; text 758 → +129 figure-Qs/95 figures → review 138 flagged = 10 flips/40 flawed → flipped PUBLIC)** — the Foundation Course is now COMPLETE, all 3 subjects PUBLIC — see [[foundation-course]]); IPMAT, CUET, NEET on the roadmap.

**Live bank size — run `npm run stats` for the live figures; this prose lags by several ingests.** As of **2026-06-26**: **17,123 PUBLIC** (pyq + practice, across 4 exams) — **NDA 8,259** (Mathematics 5,248, English 1,077, Physics 478, Geography 419, Chemistry 294, History 260, Biology 189, Current Affairs 180, Polity 90, Economics 24) + **MHT-CET 5,294** (Maths 1,784, Physics 1,771, Chemistry 1,739) + **JEE Mains 718** (Chemistry 240, Physics 239, Maths 239; 2021 standard shifts + compilation Paper 11; Paper 12 + P3 Q37 PRIVATE; MCQ-only — see [[jee-mains-ingestion]]) + **Foundation Course 2,852** (Chemistry 1,014, Physics 991, Biology 847 — practice-only, no PYQ corpus; see [[foundation-course]]). **MHT-CET grew +588 on 2026-06-26 from the 2025 April PYQ ingest** (4 PCM shifts — 19 Apr S1/S2 + 20 Apr S1/S2 — Physics 197, Maths 197, Chemistry 194 PUBLIC + 4 flawed PRIVATE; `pyq_month='April'`; via the new `scripts/mhtcet/` pandoc pipeline + 5 wrong-AK-key corrections; see the Decisions log + [[mhtcet-april-2025-ingestion]]). **The per-exam NDA/MHT-CET totals now BUNDLE the practice corpus on top of the PYQ corpus** (e.g. NDA Mathematics = 2,160 PYQ + ~3,088 practice; NDA English = 900 PYQ + ~177 practice; MHT-CET Chemistry = 1,525 + 194 April-2025 PYQ + 20 practice). The **PYQ-only formally-audited corpus is 9,546 q** — the scope of the content-audit + guides/notes (the audit-status line above); the +588 April-2025 PYQs are derivation-verified at ingest but are NOT part of that 9,546 formal-audit subset. (MHT-CET Chemistry PYQ dropped 1,553 → 1,529 → **1,525** on 2026-06-03 — first 24 exact-duplicate rows deduped, then during the source-PDF verification pass: 1 OF₂ conflicting-key duplicate deleted + 3 unreconstructable broken-extraction rows set PRIVATE; 6 image-naming rows + 5 conflicting-key pairs remain deferred. MHT-CET Physics dropped 1,577 → 1,574 on 2026-06-02 — 3 cross-shift duplicate rows deduped during its solution-cleanup. MHT-CET Maths dropped 1,588 → 1,587 on 2026-06-02 — one tan⁻¹-in-AP duplicate deduped during the Sequences & Series bank-hygiene fix, which also spun the 3 misfiled roots questions out into a new **Quadratic Equations** chapter; MHT-CET Maths is now **27 chapters**.)

**Content audit status (2026-06-03):** **BANK-WIDE CLOSED** for the original 13 subjects audited at the no-source-needed surface (**9,546 / 9,546 q, ~100%**): NDA bank-wide (4,860) + MHT-CET Maths (1,587) + Physics (1,574) + Chemistry (1,525). (The 600 ingested JEE Mains q — 599 PUBLIC + 1 PRIVATE — are NOT in this: they carry source answer keys verified only against the source images during ingestion + 126 AI-authored solutions; a content-correctness audit of JEE is a separate future pass. Paper 2 already exposed Q1 wrong-keyed by a duplicate-numbered soln block, fixed via override; Paper 3 Q37 was incomplete — its reaction scheme didn't extract — and was set PRIVATE.) MHT-CET Chemistry closed 2026-06-03 across three passes (subject-wide content probes → 8 numeric chapters HARD+MODERATE stealth-key sweep → 21 recall-chapter deep-reads) + a **source-PDF verification pass** off the local MHT-CET DOCX papers — **31 wrong-key flips** (HARD numeric ran ~22%; recall ~0.5–1%, errors only in reaction/IUPAC-naming, never pure fact-recall), 24 exact-dupes deleted, 5 bank-extraction fixes, 12 printed-paper-defect preserve-notes, 3 broken rows → PRIVATE. **~230 wrong-key flips bank-wide** (MHT-CET Physics 22, Chemistry 31, NDA Maths ~114 — including 28 stealth flips found *after* the 2026-05-27 "closure": 3 via the Indefinite Integration notes build + 2 via the Binomial Distribution notes build + **21 via the 2026-06-04 /solution-cleanup sweep of the 28 other "closed" chapters**, which exposed that the 2026-05-27 disagree-probe was case-sensitive and missed lowercase/`REVIEW:`-hedged self-disagreements; + **2 via the 2026-06-09 source-verified review of 5 questions flagged during the 5-chapter notes build** (flips `ec2987e3` Conics directrix + `9e5b991b` Trig-Eqn 4th-quadrant; plus 2 stem-extraction fixes `bbddd3e0`/`88fd7bc8`, keys already right); the rest spread thin). Preserved paper defects: 6 clean (5 NDA + 1 MHT-CET Maths) + ~10 MHT-CET Physics + 12 MHT-CET Chemistry + **2 NDA Geography** (`2604a840` chert/geyserite/shale flawed-question + `8f34b21d` Assam-borders, both source-verified against the local GAT 2025 NDA-2 PDF 2026-06-14 during the Geography notes build — the latter's bank solution falsely claimed Assam borders China, corrected to 2 countries with official key C preserved) honest-note rows. **Still open (all need source PDFs/figures, not derivation):** ~6 MHT-CET Chemistry image-naming rows + 5 user-deferred conflicting-key dup-pairs + 32 same-stem/diff-option Chemistry groups. Key lesson from this frontier: a probe only catches *self-contradicting* solutions — most flips were **stealth wrong-keys** caught by fresh re-derivation, and several were **sibling-confirmed** (a parallel question carried the correct key); see [[audit-probe-symmetry]] + [[audit-effort-by-question-type]]. Per-subject register in [[content-audit-progress]]; per-row narratives in the Decisions log below.

**All 13 subjects fully cleaned** at chapter + subtopic level — bank-wide cleanup pass closed. (The cleanup-pass snapshot numbers below are point-in-time; subjects have grown via new uploads since their original cleanup; NDA Maths was re-Phase-D'd on 2026-05-17; all 9 other NDA subjects (English + Physics + Chemistry + Biology + Geography + History + Polity + Economics + Current Affairs) on 2026-05-18 to fold in fresh auto-creates; MHT-CET Maths re-Phase-D'd on 2026-05-23 after fresh uploads grew it 795 → 1,588 q; MHT-CET Physics fully cleaned for the first time 2026-05-23 at 1,577 q; MHT-CET Chemistry fully cleaned for the first time 2026-05-27 at 1,553 q — closes the bank-wide cleanup.) **10 NDA strategy guides shipped** at /guide/nda-{maths,english,physics,chemistry,biology,geography,history,polity,economics,current-affairs} (economics + current-affairs are thin single-page landings — economics because the bank's that thin at 24 q · 1 ch, current-affairs because the content half-life is too short for a multi-route fact-memorise guide; CA's single page is **Template D — theme-prep-checklist**, the first guide where the strand axis is recurrence-strength rather than skill or strategic priority). **72 chapters of self-sufficient /notes content across 6 subjects** (verify counts with `npm run stats`, NOT this prose — the un-noted-chapter selection MUST come from the `NOTES_CHAPTERS` registry / `npm run stats`, never this list, which lagged 4 chapters behind and caused a 2026-06-09 mis-pick) (NDA Maths Statistics + Vectors + Probability + **3D Geometry** + **Matrices & Determinants** + **Sequence & Series** + **Indefinite Integration** + **Binomial Distribution** + **Functions** + **Differentiation** + **Trigonometric Identities** + **Limits & Continuity** + **Application of Derivatives** + **Lines** + **Permutation & Combination** + **Complex Numbers** + **Sets & Relations** + **Definite Integration** + **Differential Equations** + **Quadratic Equations** + **Binomial Theorem** + **Properties of Triangle** + **Conics** + **Inverse Trigonometry** + **Trigonometric Equations** + **Circles** + **Logarithms** + **Applications of Integration** + **Height & Distance** + **Binary Numbers** + **NDA Physics Sound** + **Electricity & Magnetism** + **Light & Optics** + **Laws of Motion** + **Heat & Thermodynamics** + **Modern Physics** + **Kinematics** + **Fluid Mechanics** + **Work, Energy & Power** + **Gravitation** + **Units, Measurement & Dimensions** + **Oscillations & Waves** (NDA Physics now 12 of 14 chapters — the 7 added 2026-06-10 + the 3 borderline added 2026-06-11: Gravitation 17 q · Units & Measurement 14 q · Oscillations & Waves 13 q = 29 concepts · 44 q · 7 diagrams, all reorder-only, quiz-ready per Step 1b; only Astronomy 4 q + Energy Sources 2 q remain, below the bank-coverage gate) + **MHT-CET Maths Indefinite Integration** + **MHT-CET Maths Differentiation** + **MHT-CET Maths Vectors** + **NDA Biology** (ALL 9 of 9 chapters — Human Physiology · Cell Biology · Microbiology & Disease · Biodiversity & Classification · Plant Biology · Reproduction · Ecology & Environment + the 2 thin chapters **Genetics & Evolution** + **Biochemistry** added 2026-06-12 foundation-led; 6 of the 8 new chapters via a 6-agent parallel batch, the 2 thin ones authored directly · all reorder-only · 94 concepts · 138 q · 17 static SVGs — first FULLY-noted NDA subject) + **NDA Chemistry** (11 of 12 chapters — all but Practical: Carbon · Atomic Structure · Acids, Bases & Salts · Matter & Its States · Chemical Reactions · Industrial Chemistry · Mole Concept · Metals & Non-Metals · Hydrogen & Water · Chemical Bonding · Chemistry in Everyday Life — first nda-chemistry notes subject, shipped 2026-06-10: ~125 concepts · 259 q · recall→reference profile · all reorder-only · quiz-ready per Step 1b · 12 diagrams (allotrope structures · Bohr shells · periodic trends · states of matter · soap micelle · functional-group structures · pH scale strip · distillation apparatus · Tyndall/colloid · ionic-vs-covalent formation · water H-bonding · reaction-type shapes — all static SVGs on spatial-answer concepts)) + **NDA Geography** (ALL 7 of 7 chapters — Earth's Structure · Indian Geography Economy · Indian Geography Physical · Climatology · World & Human Geography · Earth in Space · Oceanography — **2nd FULLY-noted NDA subject**, shipped 2026-06-13/14: Earth's Structure built solo then the other 6 via a **6-agent parallel batch + serial merge**; ~142 concepts · 345 PYQs · all reorder-only · **24 static SVGs** (geomorphology cross-sections, plate boundaries, seismic waves, atmosphere layers, lat/long grid, ocean-floor profile, etc.); recall-heavy → reference-variant the workhorse. The Physical "Forests and Natural Vegetation" 34-q catch-all was then **Phase-D-split** 2026-06-14 — 20 misfiled q carved out (12→Rivers, 3→Soils/Climate, 5→a NEW **Location, Extent and Frontiers of India** subtopic), leaving Forests at its 14 genuine forest q)) — foundations → worked → self-check → Level 1 practice → per-subtopic mastery checkpoint, plus per-chapter interactive visualizations + a collapsible chapter revision sheet; rendered by registry-driven shared page components. **Notes navigation is per-exam (2026-06-08):** `/notes` is a cross-exam index and `/notes/<examSlug>` (nda·mht-cet·jee-mains) is a per-exam hub listing that exam's notes subjects — both derived from the `NOTES_CHAPTERS` registry via `lib/notes/notesNav.ts`, so a new subject's notes surface in nav automatically (a new subject still needs its hand-written `/notes/<subjectRoute>/page.tsx` — see [[notes-self-sufficient-template]] + the 2026-06-08 Decisions log). **Per-chapter ship details are NOT duplicated in this header** — the one-line "what" per chapter (subtopic/concept counts, Phase-D reshape vs reorder-only vs as-is, diagram choices, slug prefix) lives in the file-layout `notes/` list in [ARCHITECTURE.md](ARCHITECTURE.md); the full "why" narrative per chapter is in the Decisions log. Append a new chapter's one-liner to that ARCHITECTURE.md list, not here. As of 2026-05-30 the `ConceptUnit` type is a **discriminated union** (`kind: "formula" | "reference"`) — formula variant carries an authored worked example, reference variant carries a flat `ReferenceTable` in the same slot — letting Recall-strand chapters teach via named-fact lookups (Sound is the first user; 7 of its 13 concepts are formula variants, 6 are reference variants). Definitions render `**bold**` key terms + `- ` bullet lists (via `RichText`, 2026-05-29); featured-PYQ + checkpoint cards now show the set-bound passage **context** and a `[Q# · month · year]` provenance citation. **Tier 1 IA + cross-linking shipped 2026-05-18** — primary nav (Bank · Guides · Notes) + cookie-backed exam pill in AppHeader; per-question backlink chips on /browse (chapter→playbook + subtopic→notes + principle/concept tag overrides); `/nda` exam home; two-way notes ↔ guide cross-links; click-to-reveal answer; in-app **question reports** (migration 0024) with admin triage at /dashboard/reports.

**Premium paywall shipped (2026-06-01):** self-serve student signup (`/signup`, Google + email/password) · per-user `entitlements` (orthogonal to orgs, migration 0026) · comp-access admin UI (`/dashboard/entitlements`) · `/notes` preview-gate (`tier:"paid"` chapters — all currently free, machinery dormant) · Razorpay ₹999/365 one-time checkout (`/pricing`, `/api/billing/*`). **Razorpay not yet activated** — checkout 503s until the 4 env keys are set. See the 2026-06-01 Decisions log + [[project-paywall-build]].

**Public lead-magnet quiz funnel shipped (2026-06-09):** cold traffic takes a public `/quiz/[slug]` (assembled-quiz, ISR + per-quiz OG card) → name+mobile+consent gate at the reward → server-side grade → captured as a `quiz_leads` row (mobile = the lead, never an account; migration 0033/0034) → results show verdicts + `/notes` deep-links + a `billingLive`-gated premium CTA. Admin "Publish to public" on `/dashboard/quizzes` sets `public_slug`; leads roll up by mobile at `/dashboard/leads`; `/privacy` for DPDP consent; a "Test yourself" CTA on `/notes`. **Dormant until an admin Publishes a quiz.** See the 2026-06-09 Decisions log + [[project-public-quiz-funnel]].

## Stack

- **Next.js 14** App Router · TypeScript · Tailwind 3 · shadcn-style UI primitives
- **Supabase** Auth + Postgres + RLS, accessed via `@supabase/supabase-js` (no Prisma, no ORM)
- **Vitest 2** for tests · `tsx` for scripts
- **Word export:** `temml` (LaTeX → MathML) → `mathml2omml` → `docx` + `jszip`
- **Browser math preview:** `katex` + `react-katex`
- **Deploy:** Vercel, git-integrated

## Multi-tenancy + visibility model

Per-school orgs, RLS-enforced. Three tiers of access:
- **Service role** (seed scripts, server-only admin actions, `/api/admin/members`) bypasses RLS by design — never expose to client code.
- **ADMIN** can do everything in own org: insert + update + delete questions, write options, upload Excel batches, auto-create chapters/subtopics, flip visibility, triage reports, manage members.
- **TEACHER** (as of migration 0025 / 2026-05-26) can **edit** question content — text, options, taxonomy moves, difficulty, PYQ metadata, images, concept/principle tags, leave-set — in own org. Cannot upload Excel, delete questions, flip PUBLIC/PRIVATE, auto-create chapters/subtopics, triage reports, or manage members. Server-side guard in `applyEdit` rejects `payload.visibility` changes from TEACHER (`kind: "forbidden_field"` → HTTP 403) so a tampered request can't bypass the hidden UI toggle.
- **anon** (no JWT) can read PUBLIC questions/options + all taxonomy. Cannot write anything.
- **STUDENT** (signed-in, self-serve, **no `org_members` row** — added 2026-06-01) registers at `/signup` (Google OAuth or email/password). By RLS construction they get zero private data; what an account unlocks is in-app question reports today + premium content via the entitlement axis below. Org-less = student (admins always get an `org_members` row atomically), so `/dashboard` redirects them to `/browse`.

**Premium entitlement axis (orthogonal to orgs, migration 0026).** Paid/comped access lives in `public.entitlements`, keyed on `auth.users(id)` — NOT org-scoped, because a student has no org. A grant unlocks a `scope` ('all' = full premium); `source` ∈ `razorpay | comp | manual` (the gate is **source-agnostic** — `userHasAccess` just asks "active entitlement for this scope?"; staff/org-members bypass the paywall). Writes are **service-role only** (no write RLS); reads are own-row only. Consumers: the `/notes` preview-gate (`tier:"paid"` chapters), `/pricing`/`/account`, the Razorpay webhook, and the `/dashboard/entitlements` comp UI. See the 2026-06-01 Decisions log entry + [[project-paywall-build]].

Each question has a `visibility` enum (`PUBLIC | PRIVATE`, default **PUBLIC** as of migration 0022 / 2026-05-17 — was PRIVATE prior). Read RLS is the union of two permissive policies: `(visibility = 'PUBLIC')` for everyone, plus `(org_id = private.current_user_org_id())` for authenticated org members on their own org's PRIVATE rows. Authenticated users see PUBLIC across all orgs + their own PRIVATE; anon sees only PUBLIC. Admins flip per-question visibility from the edit page.

Each question also has a **`question_kind` enum** (`pyq | practice`, default `pyq`, migration 0036) — the axis that separates the past-year corpus from the supplementary **practice** bank (the product is PYQ-first; **3,040 practice q live across 29 NDA Maths chapters as of 2026-06-13 — ALL 5 source folders ingested (Algebra + Trigonometry + Calculus + 2D Geometry + 3D Geometry; the 3D folder fed both the Vectors and 3D Geometry chapters)**, separate from the PYQ counts above). `/browse` exposes it as a **PYQ / Practice / All** toggle (default PYQ; `kind` filter in [filters.ts](src/lib/questions/filters.ts) → `query.ts` + `p_kind` on the facet RPCs). PYQ-only surfaces stay clean two ways: **tag-gated** surfaces (guide principles, notes drills, worked examples, quiz harvest) are PYQ-only *by construction* (practice questions are never principle/concept-tagged — doing so would leak them in), while the four **raw count-by-taxonomy** queries (`examHomeStats` + the 3 notes landings) carry an explicit `question_kind='pyq'` guard. Practice ingestion is its own pipeline + runbook — see [[practice-ingestion]] + `scripts/practice/README.md`.

RLS helpers live in the **`private` schema** (not exposed by PostgREST): `private.current_user_org_id()`, `private.current_user_is_admin()`, `private.current_user_can_edit_questions()`. New org-scoped policies should reference these. Use `current_user_is_admin()` for destructive / privileged ops (DELETE, upload, visibility flip, reports triage, member management); use `current_user_can_edit_questions()` for content edits that TEACHER should be allowed to do.

## File layout

The full file-by-file map — the `src/` tree (routes, components, lib helpers), `supabase/` migrations + seed, `scripts/`, and `tests/` — lives in **[ARCHITECTURE.md](ARCHITECTURE.md)**, extracted to keep this file's always-loaded payload lean. **Append a new file/component/route there, not here**; update the high-level shape below only when the top-level structure changes.

High-level shape:
- `src/app/` — Next.js App Router pages: `/browse` (paper builder), `/guide` (10 NDA strategy guides), `/notes` (cross-exam index → `/notes/<examSlug>` per-exam hubs → 20 teaching-notes chapters), `/dashboard` (admin), `/upload`, `/signup`·`/login`·`/pricing`·`/account`, plus the `/api/*` route handlers.
- `src/lib/` — server/pure helpers by domain: `supabase/`, `auth*`, `questions/`, `guide/`, `notes/`, `links/`, `export/` (Word/OMML), `entitlements/`, `billing/`, `reports/`+`notes-reports/`, `upload/`, `tags/`, `quiz/` (the **Quiz-Factory core**: `atoms`/`daily`/`quizPayload`/`stemLint`/`verdict`/`coverage` pure helpers + `assemble.ts` shared `assembleNextQuiz` (snapshots questions onto the quiz row) + `deleteQuiz.ts` (delete + propagate to nda-tracker) + `admin.ts` dashboard reads — the `scripts/quiz/` CLIs import these; `coverage.ts` powers `quiz:coverage`).
- `src/components/` — `AppHeader`/nav, `math/` renderers (`KatexRenderer`·`RichText`·`BlockText`·`parseTableBlocks`), shadcn `ui/` primitives.
- `supabase/migrations/` — `0001…0040` (append-only; 0040 = **RAG grounding layer** — `pgvector` (extensions schema) + `questions.plain_text`/`solution_json`/`derived_model`/`derived_at` + generalized `embeddings` table (unsized `vector`, no index) + `match_chunks` RPC (JOIN-guarded PUBLIC+pyq); see the 2026-06-19 Decisions entry + [[rag-grounding-layer]]; 0039 = **collaborative paper builder** — `papers` + `paper_questions` junction, org-scoped RLS, finalize-snapshot; 0038 = **per-exam dedup** — swap the `questions` unique key from `(org_id, content_hash)` to `(org_id, exam_id, content_hash)` so CDS↔NDA shared UPSC questions keep both copies; 0029 = `subtopics.order_index` for /notes teaching order; 0030 = `quiz_atoms` Quiz-Factory pool; 0031 = `quizzes` + `quiz_atoms_map`; 0032 = `quiz_atoms.theme`; 0033 = `quizzes.public_slug` + `quiz_leads` (public-quiz funnel); 0034 = `record_quiz_lead` retake-aware upsert RPC; 0035 = `quizzes.questions` jsonb + `theme` (immutable quiz snapshot — display decoupled from the live atom pool); 0036 = `questions.question_kind` enum (`pyq`|`practice`); 0037 = `p_kind` on the facet RPCs for the /browse PYQ/Practice toggle). `scripts/` — seeds, `notes-lint`, `notes-latex-audit`, `notes:order` sync, `jee/` ingestion, `practice/` (NDA practice-question ingestion — `render` → vision-transcribe → `commit` → `preview`; runbook in `scripts/practice/README.md`, pure core in `lib.ts`), `quiz/` (Daily Quiz Factory CLI runners — `quiz:harvest` → `quiz:sync` → `quiz:verify` → `quiz:lint` → `quiz:assemble` → `quiz:delete` → `quiz:push`; pure core lives in `src/lib/quiz/`), `grounding/` (RAG grounding + key-audit pipeline — `backfill-plain-text` → `export-batch` → [blind agents] → `commit` → `apply-fix`; pure core `lib.ts`, runbook `README.md` — see [[rag-grounding-layer]]). `tests/` — Vitest unit + DB-integration. The `/dashboard/quizzes` admin page (read-only: pool stats + filterable list + Publish-to-public; assembly/delete are CLI-only) + `/dashboard/leads` (quiz-lead rollup) + the public `/quiz/[slug]` funnel are the UI over this.

## Commands

```sh
npm run dev                # next dev (local)
npm run build              # production build
npm run lint
npm run typecheck          # tsc --noEmit (covers test files too — keep this clean)
npm test                   # vitest run
npm run test:watch
npm run db:seed            # idempotent taxonomy seed (service-role)
npm run extract:taxonomy   # regenerate supabase/seed/taxonomy.json from a reference Excel
npm run notes:lint         # validate /notes editorial modules against live taxonomy + DB tags (read-only)
npm run notes:latex        # audit /notes for LaTeX-in-plain-text leaks + delimiter imbalance + unicode (read-only)
npm run notes:order        # populate subtopics.order_index from each /notes chapter's subtopicOrder (teaching order on /browse); --dry previews
npm run notes:coverage [subjectRoute] [chapterSlug]  # triage probe: math in a chapter's PUBLIC solutions but NOT in its notes (formula-gap candidates); default nda-maths statistics. Noisy — human-review list, not a gate
npm run tags:audit         # principle-tag audit (read-only)
npm run audit:underlines   # scan PUBLIC questions for \underline{...} segments not covered by the docx-export bypass
# — Daily Quiz Factory (see Decisions log 2026-06-08 + [[daily-quiz-pilot]] memory) —
npm run quiz:harvest [route/chapter ...]   # /notes ConceptUnits → MCQ atoms JSON (scripts/quiz/atoms/*.json); default 3 sample chapters
npm run quiz:sync          # upsert harvested atoms → public.quiz_atoms (staleness-aware: preserves verified, re-flags changed)
npm run quiz:verify [name] # promote needs_review atoms → 'verified' from scripts/quiz/verify/<name>.ts (Claude-authored distractors)
npm run quiz:lint [route] [chapter]   # flag quiz-atom stems that aren't self-contained (back-refs, criterion-less "which is correct?", fragments, generic auto-harvest "formula for <X>?" template) — paginates the full ready pool; triage, run before publishing a quiz public
npm run quiz:coverage [route] [chapter] [--review]   # flag formulas/traps TAUGHT in /notes prose but not harvestable (formula.latex empty / <12 traps) — the theme-coverage guardrail; triage, not a gate
npm run quiz:assemble <route> <chapter> [max] [size] [-- --theme=X]   # ready atoms → quiz → record (quizzes+map) → push nda-tracker draft
npm run quiz:delete <slug>   # delete an assembled quiz here + propagate the delete to nda-tracker (drafts only there)
npm run quiz:push [daily/<slug>]   # push a single DraftQuiz module to nda-tracker (default: the hand-authored classical quiz)
npm run stats              # report live bank size (PUBLIC by exam/subject) + taxonomy + /notes chapter & diagram counts — for verifying the header lines instead of hand-maintaining them
npm run db:types           # supabase gen types (requires SUPABASE_PROJECT_REF env)
npm run prepush            # the chained gate, MIRRORS CI: typecheck → lint → notes:lint → notes:latex → test → build; run by .githooks/pre-push
npm run gate [target]      # wrapper that runs `npm run <target>` (default prepush) and prints a GATE_RESULT: PASS/FAIL sentinel as its LAST line — so the result survives `| tail` even though a pipe masks the exit code
```

> **Pre-push gate.** `.githooks/pre-push` runs `npm run prepush` before any push — which now **mirrors CI exactly**: typecheck → lint → notes:lint → notes:latex → **test** → build (single source of truth: the `prepush` script). So a green pre-push means a green CI. Widened 2026-06-02 from the old build-only chain after a broken test passed pre-push and only failed in CI (it didn't run `npm test`). Cost: ~3–4 min (full suite + build). Activated by the `prepare` script (`git config core.hooksPath .githooks`) on `npm install`. `.gitattributes` pins LF on `.githooks/*` so the shebang resolves on Linux/WSL contributors. Escape hatch for WIP pushes: `git push --no-verify` — don't reach for it unless you know why.

> **CI gate.** `.github/workflows/ci.yml` runs the **full** gate (typecheck → lint → `notes:lint` → `notes:latex` → `npm test` → build) on every push to `main` + every PR. The local pre-push hook now runs this same chain (see Pre-push gate above), so CI is a backstop rather than the first place these checks run — it still matters because it runs on a clean checkout with repo secrets even if someone `--no-verify`s. Requires three repo secrets (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`); without them the build + DB steps fail and the DB tests skip. Runs against the single prod Supabase project (test rows auto-swept by `global-teardown`) — point the secrets at a staging project if write traffic gets noisy. Vercel still owns deploys; CI is the correctness gate.

## Conventions specific to this project

- **Spacing scale.** Page wrappers `p-8`, content cards (CardContent default) `p-6`, compact cards (FilterBar etc.) `p-4`, nested option blocks `p-3`. Pick the smallest tier that contains breathing room — don't mix `p-4`/`p-6` in the same visual rhythm.
- **Typography.** UI text uses `font-sans` (Inter, default). Question/option/solution body text uses `font-serif` (Source Serif 4) — it's how teachers tell content apart from chrome. Both are loaded via `next/font` in `src/app/layout.tsx` and exposed as Tailwind utilities.
- **Color / brand (indigo, 2026-06-01).** The brand accent is **indigo** — chosen to unify with the `/notes` content palette. Three CSS vars in `globals.css`: `--brand` + `--brand-foreground` (fills — indigo-600 + white, AA-safe as a button bg in both themes) and `--brand-accent` (text/icon/border — indigo-600 light / indigo-400 dark, AA-legible as small text in both). `--ring` points at the brand so focus rings are branded. Tailwind exposes `bg-brand` / `text-brand-foreground` / `text-brand-accent` (+ a `brand` Button variant). **Application is SELECTIVE**: brand goes on chrome the eye lands on (primary CTAs, active nav, header mark, hero, stat numbers, table heatmap/peak-highlight, focus ring); near-black `--primary` stays the workhorse for secondary/admin buttons. **Off-limits:** the `/notes` + `/guide` *content* colors (concept-card indigo, amber traps, sky self-check, violet practice, emerald checkpoints) are content semantics — do NOT fold them into the brand token or recolor them. See [[brand-indigo-tokens]] + the 2026-06-01 Decisions-log entry.
- **Icons.** Use `lucide-react` for all icons. Never emoji or text glyphs (▾, ‹, ›) for UI affordances. Standard sizes: `h-3.5 w-3.5` for inline-with-text, `h-4 w-4` for buttons, `h-5 w-5` for the AppHeader logo.
- **Toasts.** `import { toast } from "sonner"`. Fire on every async user-initiated success or failure (save, upload commit, export download, sign-out). Setting an inline `error` state is fine, but never *replace* a toast with it — both should fire so the user can't miss the result while scrolled.
- **AppHeader.** Every page wraps its `<main>` in `<><AppHeader />…</>`. Don't re-add ad-hoc back-links, theme toggles, or sign-out buttons on individual pages — theme lives in the `ThemeToggle` island in the header (visible to anon + authed), sign-out lives in the UserMenu popover (authed only).
- **No Prisma.** Supabase RLS is the security boundary — every read/write goes through `@supabase/supabase-js` so the user's JWT carries through. Reach for `createSupabaseAdminClient()` (service-role) only in server-only contexts (seeds, cross-org admin) — it bypasses RLS by design.
- **Migrations are append-only.** New SQL goes in a new `000N_<name>.sql`. Apply via Supabase MCP `apply_migration`. Never edit a previously-applied file.
- **`.mcp.json` is gitignored.** Holds Supabase + Vercel personal access tokens. Don't commit.
- **Idempotent seeds + uploads.** `scripts/seed.ts` and the upload commit both check existence before insert; re-running is safe.
- **Content hashing for upload dedup.** `content_hash = sha256(normalised question text + sorted options + answer)` plus a unique index on **`(org_id, exam_id, content_hash)`** (migration 0038 — **per-exam**: was org-wide `(org_id, content_hash)` until 2026-06-15). Re-uploading the same file inserts 0 rows; the SAME question may recur across exams (CDS ↔ NDA share UPSC GAT English questions — org-wide dedup silently dropped the CDS copy). `commitStaged` upserts on `org_id,exam_id,content_hash`; `applyMockSync`'s content_hash lookups are exam-scoped to match.
- **Text normalization at write boundaries.** Long-form user-authored text (question, context, solution, options) goes through `normalizeNewlines` in the upload parser and the edit validator. The DB never carries literal `\n` (backslash+n) past the boundary; math zones are preserved verbatim. Any new write path that accepts long-form text must call the helper before insert/update — otherwise the render layer can't distinguish "real line break" from "literal text".
- **OMML + math defaults via post-processing.** Two `docx`-library limitations land on the same workaround. (1) `ImportedXmlComponent` wraps imported XML in an `<undefined>` element Word rejects, so we emit placeholder runs and swap them for OMML after `Packer.toBuffer`. (2) The library doesn't expose `<m:mathPr>` for settings.xml, and Word's built-in math defaults (`defJc=centerGroup`, `wrapIndent=1440`, non-zero margins) shift any paragraph containing a 2-D math element (`<m:f>` etc.) ~1″ right of plain-text paragraphs — option `(A) -3/4` ends up out of column with option `(A) -1`. `patchZip` in `src/lib/export/docxBuilder.ts` handles both: marker → OMML in `word/document.xml`, and `<m:mathPr defJc=left wrapIndent=0 lMargin=0 rMargin=0>` injected before `</w:settings>` in `word/settings.xml`.
- **Restart `npm run dev` after adding new routes.** Tailwind JIT can miss files created mid-session. Stop dev, `rm -rf .next`, restart before UI smoke tests.
- **`/notes` LaTeX in body prose only.** Four `SubtopicNote` / `ConceptUnit` fields render as plain text (no KaTeX) AND feed SEO `metadata.description` / JSON-LD where KaTeX would be invalid: `note.title`, `note.oneLineDefinition`, `note.whyItMatters`, `concept.name`. LaTeX in any of these leaks through as raw `\(\hat{i}\)` markup on the page AND in page metadata. **Keep math only in body fields** (`intuition`, `definition`, `formula.latex`, `authoredExample.{prompt,steps,answer}`, `trap.{title,body}`) — those go through `KatexRenderer` correctly. Use Unicode (`î`, `ĵ`, `k̂`, `Σ`, `°`) or plain prose for the four plain-text fields. **Body fields also support `**bold**`** (renders via KatexRenderer / RichText — use it for key terms), and the **`definition` field additionally supports `- ` bullet lists** (rendered by `RichText`): bullet *list-shaped* definitions (term/type/property enumerations), keep narrative ones as prose. The plain-text fields render `**`/`- ` literally — never use them there.
- **Word export: lowercase option labels at render only.** Bank stores `A/B/C/D` uppercase (Excel + DB convention); the `.docx` export renders them as `(a)/(b)/(c)/(d)` in both question paper and answer key (`opt.label.toLowerCase()` in docxBuilder). Browse cards + edit form still show uppercase — only the rendered Word doc is lowercased. If a future surface needs to render bank-style labels, follow the same boundary: lowercase at render, never in the DB.
- **Word export: underline bypasses the math pipeline.** DB convention is `\(\underline{\text{word}}\)` (or `\(\underline{\textit{word}}\)` for italic+underline — biology taxonomy questions). KaTeX renders these in the browser; the docx export detects them via `UNDERLINE_BYPASS_RE` in [src/lib/export/ommlBuilder.ts](src/lib/export/ommlBuilder.ts) and emits a native Word run (`TextRun({ text, underline: { type: "single" }, italics? })`) instead of routing through `temml → mml2omml → <m:borderBox>` — Word doesn't reliably draw the bottom border on inline-math borderBox under our injected math defaults. Trailing punctuation INSIDE the delimiters (`\(\underline{\text{x}}.\)`) is split off into a separate text segment. Any pattern outside the regex (bare `\underline{word}`, chained `\text{}\text{}`, `\textbf` variants, block math) falls through to the OMML pipeline and renders without an underline in Word — run `npm run audit:underlines` after each new English / Biology batch to catch fall-throughs.
- **Tables in question content use GFM pipe-tables (2026-06-02).** A table inside a question `text` or `context` is authored as a standard Github-flavored markdown pipe-table — a header row **plus a `|---|---|` separator row**, then data rows. The separator row is mandatory and is what distinguishes a real table from inline `|·|` math (determinant `\(|A|\)`, abs value `|x|`, cell notation `Zn|Zn²⁺`): a line of pipes with **no** following separator stays prose. `parseTableBlocks` in [src/components/math/parseTableBlocks.ts](src/components/math/parseTableBlocks.ts) (pure, math-masked via `maskMathZones`, TDD in `tests/parse-table-blocks.test.ts`) splits text into prose + table blocks; cells keep their `\(...\)` math and `**bold**`. Rendered by `BlockText` (`src/components/math/BlockText.tsx`) on `/browse` (expanded card only — collapsed preview stays plain `KatexRenderer` so a table can't blow past `line-clamp-2`) + the editor Preview + the `/notes` mastery-check / `/guide` worked-example cards (`WorkedExampleCard`'s `example.context` + `example.text`, 2026-06-04), and as a native Word `<w:tbl>` in `docxBuilder` (the question NUMBER rides on the first paragraph; a stem opening with a table gets a numbered empty paragraph first). **No DB primitive** — it's all in the existing `text`/`context` columns; `normalizeNewlines` is compatible (it upgrades an Excel-authored literal-`\n` table into real newlines). **Future-only**: the existing 13 Statistics (lossless 2-row linearization) + 37 Match-Lists were NOT backfilled — they still render as linearized text. Options don't support tables (v1). For a genuine ≥3-column data grid that can't be a markdown table, fall back to a question image like a diagram.

## Recurring pitfalls

Patterns that have bitten this codebase more than once. Each one earned its place by causing a real production regression — read before writing new code that resembles the shape.

- **PostgREST 1000-row cap on raw `.select()`.** Any `client.from(...).select(...)` that derives a count, a distinct set, a sort, or any aggregate from the *row payload* (rather than response headers via `count: "exact"` or a SQL aggregate via RPC) is suspect once the table can exceed 1000 rows. PostgREST silently truncates at row 1000 — no error, just wrong answers. **Already bit us three times:** dashboard totalQuestions (migration `0018` get_dashboard_stats), PYQ-year filter dropping older years (migration `0019` get_pyq_years), and upload commit dedup races (fixed by switching to `.upsert(..., { onConflict, ignoreDuplicates: true })` — let the DB unique index enforce dedup, not an in-memory `Set`). **Audit checklist before writing any new `.select()`:** (1) Is the row payload itself the answer, or are you computing something from it? (2) If computing, can the underlying table exceed 1000 rows ever (today or as the bank grows)? (3) If yes, use `count: "exact", head: true` for counts, or write a `security invoker` RPC that does the aggregate in Postgres. Never trust a row-derived count from `.select()` past the first 1000 rows. **Bit us twice more 2026-06-11:** `quiz:lint` used a bare `.limit(2000)` which PostgREST still caps at 1000, so it silently scanned only 1000 of ~1762 ready atoms (fixed by paging in 1000-row `.range()` windows); and **the sibling self-imposed-limit trap** — `/dashboard/quizzes` `listAssembledQuizzes` had a hard `.limit(60)` and a "Quizzes built" stat derived from `list.length`, so once the table crossed 60 quizzes (now 109) the oldest silently vanished from the filtered admin view AND the stat under-reported. Rule: a hard `.limit(N)` on a list hides rows once the table exceeds N, and any stat from `list.length` lies — derive counts from a true `count:"exact"`, and raise/paginate the list.
- **`next dev` is more forgiving than `next start` for server↔client prop serialization.** Functions and other non-serializable values passed from a Server Component to a Client Component compile cleanly under `next dev` but throw at SSR time under `next start` ("Functions cannot be passed directly to Client Components"). This bit us on the 2026-05-13 Pagination revert. **Mitigation:** the pre-push hook in `.githooks/pre-push` runs `next build` before allowing a push, so this class of bug is caught locally rather than after a Vercel deploy. Don't `git push --no-verify` unless you know exactly why.
- **`next build` does NOT exercise a DYNAMIC (`ƒ`) page's render — so the build gate is blind to its render-time throws.** Only static/SSG (`○`/`●`) routes are prerendered at build; a server-rendered-on-demand page (any page that awaits cookies / a per-request query — most `/guide/*` + `/notes/*` pages that call `resolveTaxonomy` via the anon client) is never executed until a real request, so a render exception passes `typecheck` + `lint` + `next build` + the full pre-push gate and only 500s in production. **Bit us 2026-06-14:** the new `/guide/nda-physics/ncert-map` page named a `<NcertChip>` prop `ref` — React **reserves** `ref` (it's never passed to a plain function component, so the value was `undefined` at render → "Refs cannot be used in Server Components"). Typecheck passed because the prop was explicitly typed `{ ref: NcertRef }`; the build passed because the page is `ƒ`. **Two mitigations:** (1) never name a component prop `ref` (or `key`) — they're React-reserved regardless of your prop type; (2) **curl/browser smoke-test every new DYNAMIC route before claiming done** — the build gate's green is necessary but NOT sufficient for `ƒ` pages (it IS sufficient for `○`/`●` pages, which prerender). A data-only test (like the ncert-map taxonomy test) doesn't render the page, so it can't catch this either.
- **Source-source subtopic name collisions during bulk chapter merges.** When canonicalizing N source chapters into one target chapter, a bulk `UPDATE subtopics SET chapter_id = '<tgt>' WHERE chapter_id IN ('<src1>', '<src2>', ...)` can fail with `23505` on `subtopics_chapter_id_name_key` even if a pre-flight source-vs-target collision check came back clean — because two sources may share a same-named subtopic that collides only once both are reparented. Bit us on the NDA Physics E&M cluster (5 sources → "Magnetic Field Inside a Solenoid" appeared in two of them). **Mitigation:** process sources sequentially via a `DO $$ FOR mapping IN (VALUES (...)) LOOP ... END $$` block; each iteration redirects colliding questions, deletes colliding source subtopics, reparents the rest, updates remaining question chapter_ids, and deletes the source chapter — so by the time source 2 merges, source 1's content is already in target and the standard collision-handling kicks in. Pattern + full template in `[[reclassification-sql-pattern]]`.
- **Catch-all chapters and subtopics drift until you reclassify per-question.** Buckets named "Mechanics", "Physics", "Error Identification in Sentences" collect every question that doesn't cleanly fit a more-specific name. They look harmless during a chapter-level merge but rot the bank's usefulness for technique drills. **Mitigation:** when a chapter or subtopic holds ≤~50 mixed-topic questions, read each stem and produce a `(question_id, target_canonical_id)` mapping; execute as a `DO $$ FOR m IN (VALUES (qid, tgt)) LOOP ... END $$` that (a) looks up the question's source subtopic, (b) finds same-named subtopic in target chapter, (c) redirects+deletes on collision, otherwise reparents+moves chapter_id. Precedents: Spotting Errors 29 q (2026-05-15 NDA English); Mechanics 19 q + Physics 26 q catch-all chapters (2026-05-15 NDA Physics). Beyond ~50 q the user prefers chapter-level merge + subtopic-cleanup as a follow-up pass.
- **`/notes` subtopic slugs are GLOBALLY unique, not chapter-scoped.** `question_concept_tags` keys on `(question_id, subtopic_slug, concept_slug)` with **no chapter or subject column**, and `loadConceptDrills` / the mastery-checkpoint query filter by `subtopic_slug` ALONE. So two chapters that both use a slug like `foundations` will cross-contaminate each other's drill lists + checkpoints. Bit us 2026-05-30: MHT-CET Indefinite Integration reused Sound's `foundations` slug — notes-lint caught it as "DB tag has unknown concept_slug" because the other chapter's concepts don't match. **Mitigation:** every notes subtopic slug must be unique across ALL chapters in `NOTES_CHAPTERS` (the URL segment can repeat per subject route, but the slug used as the concept-tag key cannot). MHT-CET's foundation subtopic was renamed `foundations` → `fundamentals`. notes-lint surfaces the collision but only *after* tags are inserted — pick distinctive slugs up front.
- **Renaming a SHIPPED chapter silently rots its `/guide` + `/notes` cross-refs.** The DB taxonomy and the guide/notes editorial TS are joined only by NAME at request time (`resolveTaxonomy`), so a Phase-D rename/merge leaves the TS pointing at a dead subtopic — the "Drill the N →" CTA resolves to an EMPTY `/browse` filter, no error. Bit us twice on 2026-05-30: the Sound notes 5→4 redesign broke the `/guide/nda-physics` Sound playbook + strategy `mustDrill` + CHAPTER_TABLE prose; the 2026-05-29 Probability merge ("Probability with Dice" → "Probability via Counting") broke two `nda-maths` principle drills + the Probability tier. **Mitigation:** after any rename/merge of a shipped chapter, sync `/guide` `_data` (playbooks `subtopics[]`, strategy `mustDrill`/`skipSubtopics`, principles `drill`, compounds filters, prose counts) + `/notes` `_data` + concept tags, then run `npm test` — the 7 playbook guides + CA themes + Maths each have a `describe.skipIf(!HAS_ENV)` live-taxonomy resolution test (`guide-nda-<subject>-playbooks.test.ts`; `guide-nda-maths-taxonomy.test.ts` for Template A) that fails on drill-target drift. Full procedure: [[shipped-chapter-rename-downstream-sync]]. The `/browse` backlink chip (`getQuestionResources` name lookup) + the single-page Economics chapter CTA are the cross-link surfaces still untested.
- **Integration-test fixtures leak into the LIVE bank when `afterAll` is interrupted — and only `org_id` reliably catches them.** Every DB-integration test inserts throwaway rows (org, questions, taxonomy) into the single prod Supabase project and cleans them in `afterAll`; a parallel/killed/throwing run skips that cleanup, stranding PUBLIC fixtures on `/browse` and inflating the bank counts. Bit us 2026-06-11: 18 fixture questions (11 PUBLIC) + 12 orgs leaked. **No content signal is complete** — `source_file`+`upload_job_id` both NULL misses fixtures that set a test `upload_job_id`; text-pattern misses the `goodXlsxBuffer` upload-flow fixtures (realistic stems, **real sha256 `content_hash`, under canonical taxonomy**); `content_hash !~ '^[0-9a-f]{64}$'` (0 real rows) misses those same ones. **The one universal signal is `org_id`:** every fixture belongs to a throwaway org whose name carries an 8-hex run-id (`randomUUID().slice(0,8)`). **Mitigation:** `global-teardown` is now an org-keyed cascade sweep (`isTestOrgName`, [[test-data-leak-org-signal]]) — but it's a SAFETY NET that only runs at the end of a full `vitest run`. A subset/watch run that creates fixtures and is then killed leaves them until the next full suite. After any interrupted DB-test run, sweep manually or run a full `npm test`. To clean: clear `question_reports` (its `org_id` FK is RESTRICT) then delete the test orgs (CASCADEs questions→options/tags/reports + org_members + upload_jobs).

## Design axes

Load-bearing design distinctions for the project. Each one earns its place by being structural — getting the axis wrong has cost real work, and the answer to "which axis does this live on?" is non-obvious for the named feature areas.

### Tag taxonomies — principles (horizontal) vs concepts (vertical)

**`/guide` principles** and **`/notes` concepts** are different kinds of tag and must NEVER share a schema or be merged into one taxonomy.

| | Principles | Concepts |
|---|---|---|
| Axis | Horizontal — cross-subtopic | Vertical — within-subtopic |
| Cardinality | ~20 in NDA Maths | ~5–10 per subtopic × hundreds = thousands at scale |
| Storage | TS modules (`/guide/.../principles.ts`) + curated `extraQuestionIds` | DB table `question_concept_tags` (migration 0021) |
| Authoring | Editorial — hand-curated, slow update cadence | Per-upload — admin tagging UI or chat session, fast cadence |
| Value comes from | **Selectivity** (only the top-20, hand-picked) | **Coverage** (every relevant question tagged) |
| Identity question | "What unifying lens does this question demand?" | "What specific technique within this chapter?" |

**The wall — non-negotiable:**
- DON'T migrate `/guide` principles to a DB table. Hand-curation is the signal-to-noise mechanism; "scaling" them dilutes their value.
- DON'T bundle principles into `question_concept_tags` via a `tag_kind` discriminator. Flattens the strategic distinction.
- DO share infrastructure at the seam: one admin tagging UI on `/questions/[id]/edit`, one notes-lint validator, future LLM-suggest can suggest both kinds. Sharing UI ≠ sharing schema.

**Why this earned a section:** The initial concept-tags proposal (Phase 1, 2026-05-17) was a unified `question_tags` table. User pushed back; I conceded. Both axes shipped separately (principles still in TS, concepts in migration 0021) and the wall has held. This section exists so future tag-related features (admin UI, LLM-suggest, AI tutor grounding, MHT_CET_AI integration) don't re-litigate it from scratch.

### Cross-link backlinks — chapter-level (automatic name lookup) vs tag-level (manual DB-tagged override)

The `/browse` QuestionCard chip row (Tier 1 + Tier 1.5, 2026-05-18) renders **two distinct classes of backlink** and the wall between them matters. Same dropdown of chips, two completely different authoring loops and data sources.

| | Chapter-level chips | Tag-level chips |
|---|---|---|
| Examples | "Playbook: Sound" · "Concept notes" | "Lever: Vieta" · "Concept: Weighted Mean" |
| Source | Name-based lookup at render time | DB row in `question_principle_tags` / `question_concept_tags` |
| Authoring cost | **Zero per question** — purely a function of `(exam, subject, chapter, subtopic)` | **Manual per question** — chat tagging session, INSERT with `ON CONFLICT DO NOTHING` |
| Coverage | 100% of questions whose chapter has a guide / subtopic has notes | Only questions explicitly tagged (today: 488 principle tags · 262 concept tags) |
| Failure mode | Silent break when DB chapter renamed (no lint catch yet — see ROADMAP "notes-lint guide-side rename validation") | Silent fallback to generic chip when slug doesn't resolve via `getPrincipleName` / `getConceptName` |
| Override behaviour | The default; renders for all qualifying questions | When present, **replaces** the chapter-level chip for the same axis (more specific wins) |
| Resolver | `getQuestionResources({exam, subject, chapter, subtopic})` in `src/lib/links/questionResources.ts` | `getQuestionResources({...}, tags)` — same function, second arg from `getResourceTagsForQuestions` batch |

**The wall — design intent:**
- DON'T expand the chapter-level lookup to do "fuzzy" matching when a chapter rename happens. The failure mode (chip disappears) is the correct signal — fix the rename, don't paper over the drift.
- DON'T attempt to back-fill tag-level chips from the chapter-level data (e.g. auto-create a `Lever: <chapter-name>` chip when no principle tag exists). That destroys the curated-vs-automatic distinction and floods low-leverage questions with meaningless tag chips.
- DO let tag-level chips quietly fall through to chapter-level chips when the tag's slug doesn't resolve. Better a generic "NDA Maths strategy" chip than a broken `/principles/<unknown-slug>` link.
- DO share infrastructure at the seam: both axes flow through the same `getQuestionResources(input, tags?)` function, both render via the same `<ResourceChip>` component in QuestionCard. The two-tier system is a function-signature distinction (optional second arg) not two separate code paths.

**Why this earned a section:** The 2026-05-18 Tier 1.5 ship added tag-level overrides AFTER the Tier 1 chapter-level chips were in production. The temptation in the moment was to refactor into "one unified backlink table" or to have tag-level chips deduplicate against chapter-level chips. Both ideas are wrong — they conflate two different authoring loops (curated tagging session vs no-touch render-time mapping) and two different coverage shapes (long-tail per-question vs blanket-per-chapter). The split shipped cleanly because the function signature preserved the asymmetry: `tags?` is optional, the resolver short-circuits chapter-level chips when a more-specific tag-chip exists, and untagged questions still get the generic chip — graceful fallback throughout. Sister wall to the tag-taxonomies axis above.

### Guide strand axis — execution-mode (chapter-partitioning) vs tier-style (chapter-tiering)

Within Template B variants, the **strand split** is the strategic axis the strategy page renders against. There are two fundamentally different ways to construct it, and they are NOT interchangeable — picking the wrong one produces a strategy axis that sorts questions by nothing meaningful.

| | Execution-mode strands | Tier-style strands |
|---|---|---|
| Subjects | NDA English (R/Rule/Reason), NDA Chemistry (R/Rule/Calculate), NDA Biology (R/Apply/Verify), NDA Geography (R/Apply/Verify) | NDA History (Cornerstone/Foundation Recall/Quick-Win) |
| Strand semantics | Each strand is a SKILL bucket (recall a fact / apply a rule / trace a mechanism / evaluate a statement). Chapters get grouped into a strand by their dominant execution mode. | Each strand is a STRATEGIC TIER (cornerstone weight / foundation recall / quick-win). Chapters get grouped into a strand by their bank share + %HARD profile. |
| Recall slot | Stable — there's always a Recall strand (named-fact recall is universal in Indian entrance exams). | Stable in spirit (Foundation Recall) but the slot SEMANTICS bent — it now groups chapters by tier, not by execution mode. |
| When to pick | Execution modes PARTITION chapters: one cluster of chapters is 60%+ dominated by one mode, others are 20%- on that mode. | Execution modes are UNIFORM across all chapters (25–45% everywhere) — can't partition. AND/OR one chapter has dominant bank share (40%+) that warrants its own Cornerstone tier. |
| Decision probe | For each candidate mode (recall/rule/apply/verify/calculate/reason/multi-statement/date-anchored), count `q × chapter_spread` AND per-chapter %. Top mode at 60%+ in one cluster = execution-mode strands work. | Top mode is 25–45% of every chapter = uniform = falls back to tier-style. Use bank-share + %HARD per chapter as the partition signal instead. |
| HARD calibration | Strand-level (densest-HARD strand called out — Geography's Apply at 23% avg HARD). | Implicit in the tier-structure — Cornerstone IS the densest-HARD tier by construction (History's Cornerstone = Modern India 34% HARD). |

**The wall — design intent:**
- DON'T force execution-mode strands when modes don't partition. History's multi-statement (70 q × 4 ch · 43% HARD) and date-anchored (61 q × 4 ch · 43% HARD) cut UNIFORMLY across chapters — a Bio/Geo-style R/Apply/Verify split would force lopsided cuts (Recall 44 q : Date 163 q : Verify 53 q if grouped by chapter's dominant mode). The strategy axis would sort by nothing meaningful.
- DON'T force tier-style strands when execution modes DO partition cleanly. English's Vocab/Idioms (Recall) and RC/Cloze (Reason) chapters are genuinely different SKILL types, not just different priorities — a Cornerstone/Foundation/Quick-Win split would lose that information.
- DO let the decision probe pick. Test execution-mode first (Template B default); fall back to tier-style only when modes are uniform.
- DO share infrastructure at the seam: both axes use the same `STRATEGY_STRANDS` data shape (each strand has `id`, `label`, `pitch`, `approach`, `chapters: StrandChapter[]`) and the same strategy-page renderer. The two-axis distinction is in the BUCKET MEANINGS (`PlaybookBucket = "recall" | "apply" | "verify"` for Bio/Geo vs `"cornerstone" | "foundation" | "quickwin"` for History) — same code path, different semantics.

**Why this earned a section:** The 2026-05-19 NDA History ship was the first time Template B's strand axis itself bent. Before History, every Template B variant had bent the second + third SLOTS within an execution-mode axis (Rule→Apply for Bio, Calculate→Verify for Chem) — but the axis itself was always execution-mode. History exposed the next level: when execution modes don't partition chapters at all, the axis itself has to change. The temptation in the moment was to force one of the existing slot-bend patterns onto History (most naturally R/Apply/Verify like Bio + Geo) — that would have shipped a strategy page where students see "Apply chapters" and "Verify chapters" labels but no chapter actually leans either way, sorting by nothing. The tier-style fallback shipped cleanly because the strand data shape was already general (chapters grouped under labelled strands with a pitch + approach) — only the bucket vocabulary changed. Sister wall to the tag-taxonomies + cross-link-backlinks axes above: in all three, the right call is to preserve an asymmetric distinction at the shared seam rather than to flatten for elegance.


## Guide structure templates

The full template catalogue (A principles-first · B playbooks-first + variants · C chapter-playbooks+formula-sheet · single-page landing · D theme-prep-checklist), the per-subject variant tables, and the **"Choosing the template"** decision script live in **[GUIDE_TEMPLATES.md](GUIDE_TEMPLATES.md)** — **read it before proposing or building a new `/guide`**. Pick the template by analysing the bank first (bank-thinness gate → cross-chapter-lever survey → %HARD spread), never by copying precedent. Shared components (`GuideShell`/`GuideHero`/`StatBlock`/`BrowseLink`/`DriftTable`/`RelatedPlaybooks`/`ReferenceTables`) + the per-new-guide plumbing checklist (sitemap, Footer, /browse pill, parent cross-link, `questionResources.ts` mapping, `guide-strategy-headline` test) are documented there too.

## Decisions log

Why behind architectural pivots — saves future-you from "why didn't we just…?".

**Convention:** topical intros (like "Foundations" below) sit at the top. Dated entries are bucketed by month — `### YYYY-MM` per month. When a new month begins, add a new `### YYYY-MM` sub-section above the prior one. **Within a month, entries are ordered newest-first (reverse chronological top-down): a new entry goes at the TOP of its month's block.** Net result: the most recent entry anywhere in the log is the first dated entry a reader sees after the foundations. This lets a reader collapse historical months mentally and focus on what's recent.

### Foundations (M1–M3, 2026-05-08)

The load-bearing choices made during the initial scaffold. Every later phase rests on these — re-litigate only with explicit reason.

- **No Prisma.** Supabase RLS is the security boundary; Prisma uses a single privileged DB user and bypasses RLS by default. Forcing per-request `SET LOCAL` JWT injection fights the tide. `@supabase/supabase-js` carries the user's JWT through naturally.
- **RLS helpers in `private` schema.** PostgREST exposes any `public.*` function as a `/rest/v1/rpc/<name>` endpoint. Supabase advisor flagged `current_user_org_id()` / `current_user_is_admin()` as callable by `anon` and `authenticated`. Functionally safe (returns `null`/`false` for anon) but defence-in-depth — moved to a schema PostgREST doesn't expose.
- **OMML via marker-and-post-process.** The `docx` library's `ImportedXmlComponent.fromXmlString` wraps imported XML in an `<undefined>` element that Word refuses to open. We render placeholder text where math should go, then patch `word/document.xml` after `Packer.toBuffer`. Sidesteps the library quirk; works for arbitrary OMML.
- **Word export = ZIP of two files.** First version was a single .docx with a 3-way layout toggle. User asked to split: a Question Paper distributable without leaking answers, and a separate Answer Key. One ZIP keeps the bundle obvious. **Superseded 2026-05-13** — ZIP killed for mobile UX; route now returns one .docx per request via a required `kind` selector.
- **Hard 200/export cap, no auto-split.** Vercel function timeout is the binding constraint. A clear error ("narrow filters") ships faster than a zip-of-zips. Revisit if a teacher genuinely needs 500 questions in one paper.
- **Chapters/subtopics auto-create, subjects don't.** Subjects are top-level taxonomy curated centrally; auto-creating "Phyiscs" from a typo would corrupt the canonical list. Chapters/subtopics are per-upload working data — small typo cost, and a merge tool can clean up later.

### 2026-06

- **2026-06-26 — MHT-CET 2025 April PYQ ingestion (NEW `scripts/mhtcet/` pandoc pipeline) — 4 PCM shifts, 588 q PUBLIC, the first real-PYQ MHT-CET ingest since the original seed.** Source: LWS-typed reproductions of the four April 2025 MHT-CET shifts (19 Apr S1/S2 + 20 Apr S1/S2) under `C:\tmp\PYQPs\MHT-CET\2025\` — each a born-digital OMML `.docx` (Physics 1-50, Chemistry 51-100, Maths 101-150, continuously numbered) with a **separate `<paper>_AK.docx`** carrying the answer letter + a worked reference solution per Q. The pre-existing 2025 data was **May-only**; these are April → genuinely new (no month overlap). State + how-to in [[mhtcet-april-2025-ingestion]].
  - **Pipeline (`scripts/mhtcet/`, modeled on `scripts/jee/` — the proven pandoc→commitStaged shape):** `config.ts` (4-shift registry + `ShiftData`) · `lib.ts` (MHT-CET 50/50/50 `subjectForNumber` + section-header segmentation, reusing JEE's `cleanText`/`parseOptionsFromText`; tolerant `**(x)\**` AK parser; `cleanupArtifacts` deglue) · `extract.ts` (pandoc QP→records + AK answer/solution as a **cross-check reference only**) · `merge-shift.ts` (3 per-subject fragments → one shift file; coerces array-form `optionOverrides`) · `commit.ts` (commitStaged PRIVATE, `pyq` 2025 April) · `attach-images.ts` (stem figures + option-figure + option-image modes) · `validate-db.ts` (KaTeX/leak/delimiter/unicode/completeness) · `flip-public.ts` (flips all but `flawed`) · `dump-taxonomy.ts`. The `out/` artifacts are gitignored; `shifts/*.json` (classification + MY derived answer + MY derived solution) is the committed audit trail.
  - **The verification model (user's call): derive independently, use the AK only to cross-check.** Per shift, 3 parallel agents (one per subject, 50 q each) classify into EXISTING chapters/subtopics + INDEPENDENTLY solve + author a clean solution, then compare to the AK letter — flagging disagreements. **5 wrong AK keys caught + corrected across the 4 shifts, every one contradicting the AK's OWN worked solution** (S2 Q75 ΔTb→2.5=C, S2 Q128 switch-circuit A≡C=B, S3 Q5 same-material discs I∝R⁴=1:81=C, S3 Q53 fuel-cell reducing agent=H₂=A, S4 Q61 partial pressure 1/5×20=4 bar=C). **4 flawed** (no correct option among the printed four / AK self-inconsistent: S1 Q69 + Q130, S4 Q148 + Q149) kept PRIVATE. ~20 dropped-digit/garbled stem typos fixed via `stemOverride` (the known MHT-CET coaching-repro genre — e.g. polaroid intensity 56→256, all AK-confirmed). 100% classified into existing taxonomy (one mis-file into the Physics chapter "Semiconductor Devices" auto-created an empty Chemistry chapter → caught by the merge classify-check, reclassified to Solid State, empty chapter deleted).
  - **Pipeline hardening earned per shift (the durable artifacts):** `cleanupArtifacts` (in `lib.ts`, applied at commit before hashing) now strips the recurring pandoc artifacts these papers throw — trailing hard-break `\\`, stray `\`/`\ ` before a math close (`\right.\\)` / `(2-x)\ \)`), `\\[Npt]` row-spacing, glued long-arrows + logic/set/relation macros (`\simp`, `\midZn`, `\landp`, `\inR`, `\inte^x`, `\angleA`, `\bigtriangleupABC`), and `\leqslant`→`\leq slant` splits; plus `normalizeMathFunctions` for bare trig. Figures: `optionFigures` (attach 4 option graphs/structures as images), `optionImages` (describe option structures in text), `figureRefIndex` (pick/skip among multi-image stems). **The re-commit-after-edit hazard** (content_hash changes → orphan+dup) was handled by always DELETE-then-recommit the affected rows. **Sequencing:** Shift I as a validation run (hardened the pipeline) → checkpoint → Shift II → checkpoint → III → IV, auto-flipping each PUBLIC once validation was clean (0 KaTeX-broken/leaks/unicode). Per-exam dedup auto-skipped 8 exact cross-shift recurrences (already in the bank; near-dupes kept as the recurrence signal). Also swept a leaked `__sync_test_aa00cc00__` test chapter (stale globalTeardown cruft) found in MHT-CET Maths. Reconfirms [[mhtcet-extraction-error-genres]] (bank-side-suspect + dropped-digit genre) + [[docx-pandoc-ingestion]] (the OMML-DOCX class) + [[audit-probe-symmetry]] (independent re-derivation catches stealth wrong keys a probe can't).

- **2026-06-23 — LWS GAT Full Mock 3 ingested via `/lws-test-ingest` (150 q, 4 subjects) — Excel + bank-only, then reconciled against the teacher's official answer key.** Second multi-SUBJECT GAT paper (after APJ GAT Mock 5). 150-q NDA GAT mock — English (Q1–50), Geography (Q51–101), Physics (Q102–134), Chemistry (Q135–150); born-digital PDF, no printed key. `createPaper:false` ⇒ tagged Excel + bank ingest, no `/dashboard/papers` paper. **Semantic dedup vs the live NDA bank + heavy internal repetition** (Ring of Fire / Oceanic trenches / Mariana Trench each recur 2–3× within the paper itself): 101 new / 48 dup / 1 flawed (Geography was 40/51 dup — recycled standard GAT items). 101 new committed PUBLIC `question_kind='practice'`; dup + flawed stay out of the bank but ride the OMR Excel for Q-number parity. Built via **4 parallel per-subject agents** (transcribe + dedup + derive, each writing its own records fragment to keep the bank dumps out of context) + a serial merge.
  - **The durable addition — bidirectional official-key reconciliation.** The teacher supplied the official answer key AFTER ingest; diffed all 150 derived keys against it → **11 disagreements**, valuable in BOTH directions: **7 were my wrong derivations** (conceded — e.g. Q102 a lens cut ⊥ to the axis keeps its aperture → image intensity unchanged; Q58 the 105–145° band IS the P+S shadow zone, so statement 1 is true) and **2 were genuine errors in the OFFICIAL key** (Q4/Q5 sentence-rearrangement: the official option letters map to garbled, non-grammatical part-sequences while the coherent order is a *different* option) + 2 debatable recall items. User chose "use official key" → aligned all 11 to it (answer AND the solution rewritten so no row's solution contradicts its key), and flagged the 2 key errors for the teacher to fix the master. **Lesson:** the official key grades the OMR so align the bank + Excel to it, but surface the cases where IT is wrong — don't silently propagate a garbled key. Reconciling BEFORE `flip-public` meant the changed `new` rows were fixed via the cheap delete-source-rows + re-commit path, not per-row content_hash surgery. See [[lws-test-paper-ingest]].
  - Re-derivation *before* the key arrived had already caught 2 judgment items (Q2 rearrangement A→D, Q49 spotting-error → No-error) — the standard pre-PUBLIC re-derivation. Agent difficulty synonyms ("medium"→MODERATE) + answer/status casing were normalized before `validateRecords` (the recurring agent-output quirk). Commit `77cf124` (merge `797520b`); full pre-push gate green.

- **2026-06-23 — Foundation Course BIOLOGY figure-attach + review-pass DONE → flipped PUBLIC: 847 q PUBLIC + 40 flawed PRIVATE. The Foundation Course is now COMPLETE (all 3 subjects PUBLIC).** Finishes the last Foundation subject (same arc as Physics). **Figure-attach:** recovered **129 figure-dependent + match-list questions** excluded from the text pass (13 worksheets; heredity-1 had none) → **95 figures attached**; Biology 758 → 887 q. life-processes-3's whole Q1-28 first half was diagram-based; reproduce-2's match-lists recovered as GFM pipe-tables. **Review-pass:** all **138** REVIEW-flagged answers re-derived sequentially → **10 flips · 88 confirmed · 40 flawed**; flipped 847 PUBLIC, 40 flawed PRIVATE. Full state in [[foundation-course]].
  - **The 40 flawed are overwhelmingly broken match-the-following / multi-statement option sets** — duplicate options (A≡B / A≡C / A≡D printed identically), biologically-wrong forced pairings (e.g. "releases oxygen"→Mitochondria), correct-mapping-not-among-options, figure-ambiguous, or two-valid/no-valid answers. Biology's coaching worksheets lean far more on match-lists than Physics did, so the flawed rate is higher (40/887 ≈ 4.5% vs Physics 43/1034). These need a source/official key — kept PRIVATE.
  - **Transient server-side rate-limit on a 7-agent fan-out (NOT the usage limit):** 5 of 7 figure-agents died with 0 tokens ("Server is temporarily limiting requests · Rate limited") during a busy period. Re-ran the failed ones in smaller groups of 2-3 — all succeeded. Lesson: when a wide parallel fan-out gets server-throttled, retry the failures in smaller batches rather than assuming a real failure; the agents that died wrote nothing (clean to re-run).
  - `apply-review.ts` gained a `--file=` arg so the Biology verdicts (`_review-bio.json`) don't clobber the Physics `_review.json`. Same answer/reason-mismatch catch as Physics ([[diagnose-from-data-not-assumptions]]).

- **2026-06-23 — MHT-CET Maths Vectors /notes shipped (3rd MHT-CET notes chapter) — Phase-D reshape of a catch-all subtopic + 6 pages.** One of CET Maths's heaviest + hardest chapters (173 PYQ · ~58% HARD · 0 practice). 6 pages, pedagogy-first: Magnitude & Unit Vectors → Section Formula → Linear Combinations → Dot Product → Cross Product → Scalar & Vector Triple Product. **Phase-D reshape** (greenfield: 0 tags, 0 sets, no MHT-CET guide ⇒ downstream-safe): the "Magnitude, Components, Projection, and Unit Vectors" subtopic was a catch-all absorbing projection + cross/VTP questions → **14 questions reclassified** out (projection/P-moves → Dot; vector-equations + unit-⊥-two → Cross; orthogonal-and-coplanar VTP → STP; basis-components → Linear) and the subtopic renamed "Magnitude, Components, and Unit Vectors" (rename + question moves only, **no sub-count change**). 59 concepts · **173 PYQs tagged at 100% coverage**; `cetvec-` concept-slug prefix + distinct file slugs (NDA Vectors owns `vec-`/`dot-product-angle`/`cross-product-triple-product` — the mht-cet slug-collision pitfall). **Reused existing spatial viz components** (vector-projection · cross-product-area · triple-product-box · section-formula · direction-cosines · …) → no new SVGs, no `VisualizationSlug`/ConceptUnitCard edits. **Built via 6 parallel authoring agents + serial merge.** A **mid-flight session-limit wipe** hit the 3 big agents (Dot/Cross/STP) — the clean-recovery design held (no partial commits); after reset I re-authored STP and **reconstructed the lost Dot/Cross drill-tag mappings via 2 read-only recovery agents** (the authoring agents had written valid files but their tag handoffs were lost when the limit hit during post-write review). notes-lint #5 caught **3 genuine worked-example == featured-PYQ dups** (parallel-collinear, angle-from-constraints, unit-vector-of-combination — the agents demonstrated the exact PYQ then featured it) → re-authored as distinct problems; + 5 placeholder `related` hrefs + 1 chapter-intro LaTeX leak fixed. Gate: typecheck · notes:lint (0 err) · notes:latex (OK) · notes:order (6 synced, no name-join drift) · build (6 SSG prerendered) · 7 routes HTTP 200. The NDA↔MHT-CET "Vectors" chapter-name backlink collision this surfaced was fixed in a follow-up (commit `2736d42`). Reconfirms [[notes-structure-pedagogy-first]] + [[reclassification-sql-pattern]] + [[notes-self-sufficient-template]] (parallel-batch orchestration + clean recovery from a mid-flight limit) + [[notes-concept-content-alignment]] (#5 caught real dups).

- **2026-06-22 — APJ 11th Chemistry Test ingested via `/lws-test-ingest` (20 q PUBLIC) — first MHT-CET paper through the practice-paper pipeline.** A 25-q Class-11 Periodic Classification test (born-digital docx, **no printed key → all answers DERIVED**), filed under MHT-CET Chemistry → Modern Periodic Table. Semantic dedup vs the 16-q Modern Periodic Table bank found **all 25 NEW** (the bank had zero ionisation-energy / atomic-radius-ordering questions). **20 new committed PRIVATE practice → flipped PUBLIC** (answers independently re-derived before exposure — Q5/Q6/Q14 are slightly soft but the keyed option is the defensible intended one); **5 flawed** printed-paper defects (Q13/15/16/18/25 — multiple-correct or correct-answer-absent) kept out. First non-NDA exam through `scripts/practice-paper/`: `examId` override + `createPaper:false` (Excel + bank-only, no `/dashboard/papers` paper; the Tags Excel still carries all 25 for OMR parity, the 20 new committed). The official LWS answer key was applied in a later pass (commit `fc8781f`). Reconfirms [[lws-test-paper-ingest]] (re-derive DERIVED keys on judgment items before PUBLIC; per-exam dedup; `createPaper:false` bank-only mode).

- **2026-06-22 — Foundation Course BIOLOGY text-phase DONE: 758 q PRIVATE across 7 chapters (subject `7d34c419-…`); figure-attach + review + PUBLIC-flip pending.** The 3rd (and last) Foundation subject. Source: LWS "NDA Foundation" Class 9/10 NCERT Biology worksheets under `C:\tmp\Practice\Foundation\Biology\` — **14 worksheets, all docx-only** → converted via `docx-to-pdf.ts` to `_converted/bio-*.pdf`. 7 chapters (Cell · Tissues · Life Processes · Control & Coordination · How Do Organisms Reproduce · Heredity & Evolution · Our Environment); syllabus ch.3 "Diversity in Living Organisms" has **no worksheet folder** → not ingestible. **ZERO pipeline code change** — only a subject seed (direct INSERT) + the `bio()` helper + 14 config entries; confirms (3rd subject after Chemistry/Physics) the pipeline is fully subject-agnostic. Full state in [[foundation-course]].
  - **Text transcription: 758 q PRIVATE + practice** — Life Processes 157 · Tissues 128 · Cell 116 · Control 103 · Reproduce 98 · Our Environment 96 · Heredity 60. Validation-first: cell-1 solo (68 q, end-to-end flow check) → Wave 1 (7 worksheets) + Wave 2 (6) via **parallel transcription agents** (one per worksheet: render → read column PNGs → transcribe text MCQs, EXCLUDE figure-Qs, derive answers, REVIEW-flag). Per-exam dedup auto-skipped ~7 WS1↔WS2 verbatim overlaps; em-dash subtopics committed clean (UTF-8 discipline held).
  - **PENDING (next):** (1) **figure-attach** — ~128 figure-dependent Qs excluded from the text pass (heaviest: life-processes-3 28 — its whole first half is diagram-based; reproduce-2 28 — figures + match-lists the agent left out of scope; tissues-2 17); same `figure-bbox.ts` + `merge-figures.ts` + `attach-images.ts` flow. (2) **review-pass** — the many `REVIEW`-flagged derived answers (match-the-following + assertion-reason items dominate, several with duplicate/garbled option sets preserved verbatim); same sequential re-derivation as Physics (read reason-vs-answer for agent mismatches). (3) **flip PUBLIC** (keep flawed PRIVATE). NOTE: reproduce-2 match-lists (Q38-41) are transcribable as GFM pipe-tables — recover in the cleanup pass.

- **2026-06-22 — Foundation Course PHYSICS review-pass DONE → flipped PUBLIC: 991 q PUBLIC + 43 flawed PRIVATE.** Completes Foundation Physics (after the same-day figure-attach). All **127** `REVIEW`-flagged Physics answers (no printed key → every answer derived) re-derived **sequentially in the main session — NOT parallel agents (user was near a usage limit)**, worksheet by worksheet; verdicts logged in `scripts/foundation/data/_review.json` + applied by a new `apply-review.ts`: **11 flips · 73 confirmed · 43 flawed**. Then flipped **991 PUBLIC** (73 confirmed + 11 fixed + ~907 never-flagged high-confidence), keeping the **43 flawed PRIVATE** via a single guarded `UPDATE … WHERE NOT EXISTS` against the 43-row flawed set (verified 991/43 live). Full state in [[foundation-course]].
  - **`apply-review.ts` applies flips on the LIVE rows — direct DB `is_correct` move + `content_hash` recompute (collision-guarded), NOT a re-commit** (a re-commit would orphan figure `image_url`s when a flipped row's hash changes — the documented Foundation/practice hazard). It also rewrites every override `reason` (flips→`FIXED`, confirmed→`CONFIRMED`, flawed→`FLAWED (keep PRIVATE)`) so 0 `REVIEW` flags remain and the flawed set is explicit. magnetic-2 Q17→D hit a hash **collision** (identical to magnetic-1 Q16, a legit WS1/WS2 recurrence) → `is_correct` fixed, hash left stale ([[duplicate-question-dedup-collision]] + [[keep-duplicates-recurrence-signal]]).
  - **The reusable finding: the figure/text agents frequently left an answer/reason MISMATCH** — the `reason` had correctly derived the right letter but the stored `answer` was a different (wrong) one. **4 of the 11 flips were exactly this** (gravitation-1 Q25/Q41, work-energy-2 Q58/Q64). When re-verifying agent-derived answers, **read the reason against the answer field — a derivation concluding a different letter than `answer` is a free catch.** ([[diagnose-from-data-not-assumptions]])
  - **The 43 flawed (PRIVATE) split:** no-correct-option among the four (~14), garbled/inconsistent stem (~6), **duplicate options** A≡B / C≡D (~6), **two-equally-valid answers** (~10, e.g. convex-mirror-AND-concave-lens both position-independent; multiple distant sources for "find focal length"), figure-illegible / value-not-among-options (~7). All need the source PDF or an official key — a future source-verified pass could salvage the figure-illegible/garbled ones. (Optional, non-blocking: work-energy-2 Q18/Q19/Q49 are answer-correct but off-topic buoyancy/pressure misfiled under Work-and-Energy.)

- **2026-06-22 — Foundation Course PHYSICS figure-attach DONE: 894 → 1,034 q (140 figure-Qs recovered), 103 figures attached, all PRIVATE; + 2 reusable pipeline helpers.** Recovers the figure-dependent questions EXCLUDED from the 2026-06-21 Physics text pass and attaches their cropped figures across all 15 worksheets. Flow per worksheet: `render.ts` → a **parallel figure-agent** (one per worksheet) classifies each gap number + transcribes/derives → `merge-figures.ts` → `commit.ts` (PRIVATE + practice) → `attach-images.ts`. Validation-first (Motion WS1 solo + user sign-off) then Batch-1 (4) + Wave-A (6) + Wave-B (5) fan-outs. Still PRIVATE per the review-before-flip policy. Full state in [[foundation-course]].
  - **Two NEW reusable helpers (`scripts/foundation/`):** `figure-bbox.ts` — marker-based bbox auto-resolver (each raster image-block → the question-number marker above it in the same column, union + 4pt pad → `figures.json`). **The fix that made it general:** the v1 matched only a standalone `^\d+\.$` word and returned NONE on every worksheet except Motion, because the docx-converted/born-digital PDFs **glue the marker to the next word** ("9.In", "23.Two"); broadened to `^(\d+)\.(?=\D|$)` (still excludes a decimal like "1.5") and it auto-resolves the large majority (≈1 cross-column hand-fix per worksheet). `merge-figures.ts` — merges the `figure-*` recovery files into the main `questions/overrides` so `commit.ts` (which reads only the base files) picks them up.
  - **The gap-triage finding (durable):** a worksheet's missing question-numbers ≈ figure-Qs + dedup-skipped WS1/WS2 verbatim overlaps + a few flawed + occasionally text-Qs the original pass missed. Each gap is triaged into **embedded-figure** (figure in stem → transcribe + crop), **option-graph** (the 4 OPTIONS are the figures, often split across a column/page break → commit TEXT-ONLY describing each option in words, NO image — the Chemistry split-figure precedent), **duplicate** (don't transcribe; commit dedup-skips anyway), **flawed**, or **text-only-missed**. A **long consecutive gap-run is usually a coherent figure BLOCK, not a dedup block** (gravitation-2 8–16 experimental-skills, light-3 23–37 ray diagrams, magnetic-2 19–28 charged-particle/field). Sound worksheets skew option-graph (waveform "which graph" → text-only). 140 committed > 103 figures because option-graph + text-only carry no image.
  - **Flagged for the pending review-pass (keep PRIVATE until resolved):** magnetic-1 **Q41** flawed (correct force = out of page, not among the 4 options; placeholder A + REVIEW; twin Q42 is correct), magnetic-2 **Q19** borderline (correct pole answer not an option), plus the figure pass's REVIEW-flagged derived answers + the text-pass flags (human-eye Q44/Q62/Q11/Q60, sound-1 Q61/Q62, forces-1 Q23, work-energy-2 Q18/Q19/Q49 off-topic buoyancy). Derived figure answers are the highest mis-key risk → review then flip PUBLIC; Biology is the last Foundation subject.
  - **Ops gotcha banked:** running 6+ concurrent `commit`/`attach --apply` against the live Supabase **while a `git push` pre-push gate runs `npm test`** (DB-integration on the same prod project) trips a 1-of-~1679 shared-DB flake → push rejected; the `| tail` masks it (reports exit 0). Don't push while DB-writing; verify via `git rev-parse main == origin/main`, not the piped exit code. ([[shared-db-test-flake]], [[gate-exit-code-masked-by-tail]])

- **2026-06-21 — `/lws-test-ingest` extended to MULTI-SUBJECT; APJ GAT Mock 5 ingested (150 q across 5 subjects) — Excel + bank-only, no paper.** First multi-SUBJECT LWS paper (a full GAT mock — English Q1–50, Geography Q51–100, Physics Q101–120, Chemistry Q121–142, Biology Q143–150), where the prior APJ Maths Mock 5 (commit `6b73936`) was the first multi-CHAPTER one. Source is a born-digital `.docx` (clean text → no vision pass needed for transcription). No printed key → all answers derived. State + how-to in [[lws-test-paper-ingest]].
  - **Pipeline extension (`scripts/practice-paper/config.ts`, all additive/typecheck-clean):** a third filing mode `subjects` (subject → chapter → subtopics) + per-record `subject`, mirroring the existing `chapters` multi-chapter mode; `subjectOf`/`validSubtopicsFor` resolvers; both adapters file per-record subject (the tags sheet already reads `q.subject` per-row, so the multi-subject Excel "just worked"). Added `context` + `setLabel` to `PaperRec` (the comprehension passage Q31–35 rides through to `questions.context` + `set_id`; downstream `commitStaged` + `groupBySet` already supported both). New `createPaper?: boolean`: **`false` ⇒ Excel + bank ingest only, no `/dashboard/papers` paper** — and in that mode `commit-paper` commits ONLY `status:"new"` rows (with no paper to back them, dup/flawed rows have no consumer; the OMR Excel is built from the records file, not the bank).
  - **Content: 150 q → 85 new / 61 dup / 4 flawed.** Dedup-before-derive (5 parallel per-subject agents: transcribe + classify + dedup + derive in one pass) paid off — **Physics (Q101–120) and Biology (Q143–150) are 100% reproduced from an existing bank mock** (all dup, not re-committed); embedded English Q56/Q62 (sitting in the Geography block) filed under English. **84 new rows committed PRIVATE practice then flipped PUBLIC** (English 52, Geography 14, Chemistry 18); Q67 byte-matched an existing row (content_hash dedup auto-skipped what semantic dedup marked new). The 4 flawed (Geo Q79 code-not-printed; Chem Q132 duplicate-correct-option, Q137 count-not-in-options, Q138 multiple-correct) stayed PRIVATE with best-guess OMR keys.
  - **The lesson banked ([[lws-test-paper-ingest]]): re-derive the agent's DERIVED answers on JUDGMENT items before PUBLIC — don't rubber-stamp.** A user nudge ("check those 7 again?") on the low-confidence flags caught **Q46 keyed wrong** (sentence-rearrangement: agent picked PSRQ; the only coherent dialogue order is QPSR — the peasant's "Yes" answers the traveller's "Can you…?"). Fixed on the live PUBLIC row the proper way (flip option `is_correct` + recompute `content_hash` via the real helper, collision-guarded — NOT a re-commit, which would orphan the row). The remaining ambiguous key (Q45 "it is your duty"→"supposed to") is flagged for the official LWS key.

- **2026-06-21 — Foundation Course PHYSICS text-phase ingested: 894 q PRIVATE across 9 chapters, ZERO pipeline code change.** Second subject under the Foundation Course exam (after Chemistry). Source: the LWS "NDA Foundation" Class 9/10 Physics worksheets under `C:\tmp\Practice\Foundation\Physics\` — same shape as Chemistry (2-column "Section A" MCQ worksheets, 4 options, some assertion-reason, **no printed answer key**, lossy text + embedded figures). Full state + per-chapter counts in [[foundation-course]].
  - **Setup:** seeded a `Physics` subject (id `bf9528d4-…`, direct INSERT — subjects don't auto-create) + added a `phys()` path helper and **16 worksheet entries** to `scripts/foundation/config.ts` across **9 chapters** (Motion · Force and Laws of Motion · Gravitation · Work and Energy · Sound · Light - Reflection and Refraction · The Human Eye and the Colourful World · Electricity · Magnetic Effects of Electric Current). Syllabus ch.10 "Sources of Energy" has **no worksheet folder** → not ingestible (the Chemistry "Periodic Classification" situation). 4 docx-only worksheets (Sound WS2, Light WS2/WS3, Magnetic WS2) converted via the existing `docx-to-pdf.ts` (Word COM).
  - **Ingestion:** ran **Motion WS1 as a validation chapter first** (user's sequencing call), then 3 batches via **parallel vision-transcription agents** (one per worksheet, each enumerating its own render dir, transcribing text MCQs, EXCLUDING figure-dependent Qs, deriving answers + REVIEW-flagging low-confidence, writing `data/<id>.{questions,overrides}.json` with `encoding='utf-8'`). Committed PRIVATE + `question_kind='practice'`. **894 q across 9 chapters** (Motion 56 · Force 87 · Gravitation 120 · Work-Energy 135 · Sound 125 · Light 170 · Human Eye 53 · Electricity 68 · Magnetic 80); per-exam dedup auto-skipped ~12 WS1/WS2 verbatim overlaps.
  - **The reusable finding: the Foundation pipeline is fully subject-agnostic — a new subject under an existing exam needs only a subject seed + config entries, ZERO code change** (`render`/`commit`/`attach-images`/`docx-to-pdf`/`preview` + the `practice/lib.ts` helpers are exam-/subject-agnostic; the `practiceOnly` browse flag is exam-level so Physics inherits the Practice-default `/browse` view automatically). Confirms [[practice-pdf-vision-ingestion]] + [[foundation-course]].
  - **PUBLIC-flip policy = stage PRIVATE → review → flip** (user's call, **differs from Chemistry's flip-immediately**) — Physics answers are numeric/derivation-heavy → higher mis-key risk than Chemistry's recall, so review before exposure. **PENDING (next session):** (1) **figure-attach pass** — ~134 excluded figure-dependent Qs (Light 40 + Magnetic 22 heaviest; ray diagrams, circuits, v–t graphs, field lines, eye/prism); (2) **review pass** — ~87 REVIEW-flagged + 3 specific items (forces-1 Q23 agent *edited* an option; human-eye Q44/Q62 agent *reconstructed* missing options → likely flaw/PRIVATE; work-energy-2 buoyancy/pressure Qs misfiled under "Work and Its Measurement" → reclassify); (3) **flip PUBLIC**. Commit `5401e19` (config + 32 data files; pre-push gate green). Biology still pending.

- **2026-06-20 — Foundation Chemistry REVIEW-flag cleanup + last gaps: 71 derived answers re-verified, 10 key-flips, 11 flawed → PRIVATE, gap Q27 recovered. Chemistry 1,024 → 1,014 PUBLIC + 11 PRIVATE.** Completes the Foundation Chemistry quality pass (after the Acids-3 + split-figure coverage work). The Foundation worksheets carry NO printed key, so 71 answers were `REVIEW`-flagged as lower-confidence (the real count, > the ~56 estimate). Re-verified by **5 parallel sub-agents** (each re-derived a ~14-question batch from the committed stem+options → CONFIRM/FLIP/UNRESOLVED + a clean reason), then I **adjudicated every FLIP + UNRESOLVED from the text myself** before touching live content — the agents both over-flip (several is-matter match-the-pairs needed re-checking) and mis-categorize (metals-2 Q45 was a flawed-non-unique, not a flip). Outcome: **41 CONFIRM** (flag cleared, answer unchanged — override `reason` edit only, no DB change), **10 FLIP** (answer corrected: DB `is_correct` moved + `content_hash` recomputed via the real `contentHash(stem, optionTexts, letter)` helper, collision-guarded; JSON override answer synced — e.g. atoms-mol Q20 C→B [Exp2 product C:O = 2:5 violates, Exp4 is mass-impossible], is-matter Q14 D→B [mixture not compound → H₂ not H₂S], 5 match-the-pair fixes), **11 FLAWED → PRIVATE** with a `FLAWED (set PRIVATE …)` note (no correct option among the printed four / current public answer outright wrong — acids-1 Q10 every option includes non-conducting distilled water; chem-3 Q42/Q43 PbO+C with no option isolating the single incorrect statement; chem-1 Q51 duplicate options; structure-1 Q2/Q44; chem-2 Q3; carbon-1 Q19; acids-3 Q55; matter-1 Q13; is-matter Q67), **9 kept REVIEW** (5 figure-dependent + 4 borderline-but-defensible). The **3 ingestion gaps** resolved: **Chem-1 Q27** recoverable (electrolysis of water, V_O/V_H = ½ → B) committed + PUBLIC; **Carbon Q53** + **Chem-3 Q30** source-verified genuinely flawed (correct answer not an option / only 3 printed options) → stay deferred, never committed. **Periodic Classification** confirmed **no source worksheet exists** → cannot ingest. **Key technique: direct-DB application (flip `is_correct`+hash, set `visibility`) is the right tool, NOT a worksheet re-commit** — re-committing orphans figure `image_url`s when a flipped row's `content_hash` changes (the re-commit-after-edit hazard). The cp1252-`open('w')` UTF-8 trap from the split-figure pass was avoided by passing `encoding='utf-8'` on every JSON write. Full state in [[foundation-course]].

- **2026-06-19/20 — NEW exam "Foundation Course" + `scripts/foundation/` worksheet pipeline (incl. docx→PDF) — NDA-Foundation Chemistry COMPLETE (8 chapters, ~1,024 q, 85 figures, all PUBLIC; Acids-3 figure pass + split-figure set DONE 2026-06-20).** First brand-new exam since CDS. Source: the LWS "NDA Foundation" course (Class 9/10 NCERT Science worksheets) under `C:\tmp\Practice\Foundation\` — MCQ worksheets (4 options, single "Section A"), **no printed answer keys** + figure-heavy + lossy text layer. Full state + per-chapter detail in [[foundation-course]].
  - **Setup (Phase 0):** seeded the `Foundation Course` exam (`22d88324-…`) + `Chemistry` subject in the DB (a direct INSERT — exams/subjects don't auto-create) and registered the `foundation-course` slug in `examContext.ts`. Chapters + subtopics auto-create on commit. A Foundation Course has **no PYQ corpus** — the worksheets ARE its bank — so everything is `question_kind='practice'`; the user chose to make `/browse` **default to the Practice view** for it via a new `practiceOnly` flag on `EXAM_REGISTRY` + `isPracticeOnlyExam()` (the browse page resolves the active exam by id and overrides the `kind` default when `kind` isn't explicit; `parseFilters` stays pure). Smoke-tested + test-locked.
  - **Pipeline (`scripts/foundation/`, mirrors `scripts/practice/` + the JEE figure-attach):** `config.ts` (FOUNDATION exam id + a `WORKSHEETS` registry; reuses `scripts/practice/lib.ts` pure helpers) · `render.ts` (per-column hi-DPI PNGs) · vision-transcribe via parallel agents → `data/<id>.{questions,overrides}.json` (no key → every answer DERIVED, low-confidence/flawed flagged `REVIEW:` in `overrides`) · `commit.ts` (commitStaged PRIVATE + stamp practice; reuses the per-exam `content_hash` dedup, which auto-skipped the verbatim WS1/WS2 overlaps) · `attach-images.ts` (manifest-driven figure crop → `uploadImage` → `questions.image_url`, the JEE precedent — figure questions are excluded from the text pass, then recovered + attached) · `docx-to-pdf.ts` (**Word COM** conversion for the docx-only WS2/WS3 + Carbon, the MHT-CET-source precedent) · `preview.ts` (KaTeX review HTML). Same dedup/derive/REVIEW discipline as practice ingestion.
  - **State — Chemistry COMPLETE:** all 8 worksheet chapters (WS1 + the WS2/WS3 supplementary) — Matter (122), Is Matter Pure (70), Atoms & Molecules (58), Structure of the Atom (117), Chemical Reactions (197), Acids/Bases/Salts (223), Metals (123), Carbon (112), Structure of the Atom (119) = **~1,024 q PUBLIC, 85 figures** (→ 1,014 PUBLIC + 11 flawed PRIVATE after the 2026-06-20 REVIEW cleanup — see the top entry). Ingested in batches via parallel transcription agents (large worksheets split 2 ways + merged); each batch committed PRIVATE → figures attached → flipped PUBLIC → pushed (every push green). The **review pass** zoom-verified 20 flagged WS1 answers (6 answer flips + 3 transcription fixes that were masking correct answers). **Acids-3 figure pass DONE (2026-06-20):** the 25 deferred WS3 figure questions (Q3, Q13–Q33, Q35–Q37) transcribed + answered + attached (WS3 50→75; Acids chapter 198→223, +25 figures) — the "color/apparatus, low crop-yield" deferral was overstated (standard apparatus/graph/hazard/table figures crop cleanly in color). Bbox method: union the page image-blocks under each question marker (handles Q3's 4 graph-options); Q25 was a two-column split → Pillow stitched composite (the worked precedent for the remaining split figures); 5 answers `REVIEW`-flagged (Q14 green/blue, Q16/17/23/33 apparatus-detail). **Split-figure set DONE (2026-06-20):** the 4 split questions (Structure Q6/Q9 + Carbon Q58/Q62 — Bohr-model "which is wrong"/"which is Na⁺", Lewis N₂, benzene structure) shipped as **self-contained TEXT** (no figure: the stems describe every diagram in words; Pillow composites kept cropping out the option labels, and the text already carries the diagram→option map). Structure 117→119, Carbon 110→112; Q6 REVIEW-flagged (low-res). Vision delegated to a sub-agent (main session image budget exhausted). **Deferred:** 2 flawed-option figures (Chem Q51, Carbon Q53); Chem-3 Q30 (page-split boundary); ~56 `REVIEW`-flagged answers (cleanup pass, +5 from Acids-3 figures, +1 from Structure Q6); **Periodic Classification** chapter (syllabus ch.8 — no worksheet in source); **Physics + Biology** subjects (parallel folders, same pipeline). Recurring agent gotcha banked: agents sometimes write `"REVIEW: A"` into the `answer` field (must be a bare letter) — `commit` rejects it; sanitize before re-commit. Reconfirms [[practice-pdf-vision-ingestion]] (vision transcription + figure-attach) and the Word-COM docx→PDF render approach from [[mhtcet-source-docx-render]]; full state in [[foundation-course]].

- **2026-06-19 — LWS test-paper ingestion pipeline (`/lws-test-ingest` skill + `scripts/practice-paper/`) + NDA Matrices & Vector tests ingested.** A reusable flow for a teacher-authored LWS test PDF (an existing printed paper, e.g. "Matrices 6M QP", "Vectors_QP B") → THREE deliverables: the nda-tracker tagged-enrichment Excel (all questions, OMR Q-order), PRIVATE `question_kind='practice'` bank rows (dedup-gated), and a `/dashboard/papers` paper (the full printed test, all questions in Q-order, single section, created programmatically via service-role `createPaper`/`addQuestion` — the Atmosphere-test precedent). Generalized the per-paper one-offs (`build-vectors-b`/`build-matrices-test`/`commit-matrices-test`, now DELETED) into one `PAPERS`-registry-driven pipeline: `config.ts` (registry + `recToQuestionRow`/`recToParsedRow` adapters + `validateRecords`) + `build-tags.ts <slug>` + `commit-paper.ts <slug> --apply` + `flip-public.ts <slug> --apply`. Skill + how-to: `.claude/commands/lws-test-ingest.md` (gitignored, local, like the other project skills). Full state in [[lws-test-paper-ingest]].
  - **The load-bearing design — full test everywhere, deduped bank.** A paper references question *rows*, so ALL questions are committed (PRIVATE) + added to the paper + the Excel — **OMR Q-number parity is non-negotiable** (the student bubbles Q1..QN against the printed paper). But the *browsable* bank stays deduped: only `status:"new"` rows flip PUBLIC. `status:"dup"` (semantic duplicate of an existing bank question) + `status:"flawed"` (broken printed options) stay PRIVATE forever — they back the faithful paper but never enter the public corpus. This reconciles "add if not duplicate" (the public bank) with "create the paper as printed" (the full test); `flip-public.ts` is the gate (promotes `status:"new"` only).
  - **Two instances (both PRIVATE pending the PUBLIC flip — NOT in the 3,040 PUBLIC practice count above).** **Matrices** (`Matrices 6M QP 13-6-26.pdf`, 40 q, NO printed key → every answer DERIVED): 26 new + 12 semantic dups + 2 flawed (Q9 prints three identical options; Q25 the true `A⁻¹=CB` matches no printed option) vs the 383-q Matrices & Determinants bank; paper `bed3cfbd-…`. **Vectors** (`Vectors_QP B`, 120 q, VERIFIED key — no derivation): 102 new + 18 dups vs the 271-q Vectors bank, 0 flawed; paper `dc55cf9e-…`. Both papers + tagged Excels (`generated-papers/Tags_NDA_{Matrices_Test,LWS_Vector_Test_B}.xlsx`) are nda-tracker-OMR-ready now (independent of the flip). Pushed in two branch→merge→main commits (`6f44d42`, `629e063`); both passed the full pre-push gate.
  - **Dedup is semantic, via a subagent** (pull the chapter's existing PYQ+practice stems → too big for context → subagent returns DUP/MAYBE/NEW per question with the matching stem quoted; MAYBE = same template but different numbers/vectors/question-asked → kept NEW). No-key papers → derive every answer + flag low-confidence/flawed in a `reviewNote`; verified-key papers skip derivation. **Vision transcription + answer derivation stay the irreducible manual core** — the skill codifies only the mechanical wrapper + the verification checklist. Reconfirms [[practice-pdf-vision-ingestion]] (transcription) + [[cross-app-feature-execution]] (Excel-from-paper) + [[concurrent-selection-junction-table]] (the paper is a junction table).

- **2026-06-19 — RAG grounding + key-audit layer (migration 0040): `plain_text` + `solution_json` + `embeddings` + `match_chunks`; in-progress over NDA Maths.** First half of a RAG data-infrastructure layer that future AI products consume at runtime — first user: a grounded NDA doubt tutor on **nda-tracker** (the delivery app; the tutor chat is NOT built here — PYQ Vault builds the substrate). Pipeline + runbook in `scripts/grounding/` (README.md); pure helpers `scripts/grounding/lib.ts` (TDD `tests/grounding-extract.test.ts`, 31 cases). Full state in [[rag-grounding-layer]].
  - **Migration 0040** (advisor-clean): `pgvector` (installed in the `extensions` schema — so `match_chunks` qualifies the type as `extensions.vector` and the cosine op as `OPERATOR(extensions.<=>)` under `set search_path=''`); 4 additive nullable columns on `questions` — `plain_text` (LaTeX-stripped embed/ground input), `solution_json` (`{approach, steps[], final_answer, option_matched}`), `derived_model`, `derived_at`; a **generalized `public.embeddings` table** (nullable `question_id` FK, **unsized `vector`** — model-agnostic, no index at 2,160 rows; `model`/`source_content_hash`/`chunk_index`; RLS mirrors `0023`); and the **`match_chunks` RPC** (`security invoker`, JOIN-guarded `visibility='PUBLIC'` + `question_kind='pyq'` — the guard is on the JOIN, never the embeddings table, so it holds for service-role callers too). NO vector index (flat scan is sub-ms at this scale; IVFFlat/HNSW deferred to ~100k+ rows).
  - **The two derived fields split by mechanism.** `plain_text` is **deterministic** (`latexToPlainText` — keeps command NAMES like `\int`→`int`, strips only formatting; a first version that *deleted* `\int`/`\log`/`\sin` was caught + fixed before scaling). `solution_json` is the **agent** job — **blind** Sonnet agents structure the canonical solution WITHOUT seeing `is_correct`, so `option_matched` vs the verified key is a real audit. Scripts: `backfill-plain-text` · `export-batch` (omits `is_correct`) · `commit` (agreeing rows write; **disputes are HELD, never written** — the grounding layer must never contradict the verified key) · `apply-fix` (key flip + stem/option fix + `content_hash` recompute, collision-guarded). Agents WRITE their own `data/<batch>.solution.json` to keep output out of the orchestrator's context (the practice-ingestion pattern); a UUID guard skips a truncated id rather than crashing the batch.
  - **The finding that reframes the work: the blind re-derivation IS the "LLM-assisted secondary probe" that [[audit-probe-symmetry]] said could close the stealth-wrong-key gap — and it's ~90% precise.** Of 21 disputes flagged across the first ~365 NDA Maths rows, source verification against the local papers (`C:\tmp\PYQPs\NDA\NDA_Maths_PYQPs\`, 6 parallel render+rederive agents) confirmed **18 were genuine bank errors** (12 wrong keys + 6 corrupt extractions), 2 flawed-but-keep-official, 1 false alarm — an **~8% real error rate in "audit-closed" NDA Maths** (vs the 3.8% the prior probe-based sweeps found). **17 fixes applied** (`apply-fix.ts wave-src`); the `05c32038`/Q100 set deferred (doubly corrupt — both stems mis-extracted). So scaling the grounding is **two bundled deliverables**: the grounding layer AND a high-precision wrong-key audit; the audit is what *makes* the grounding trustworthy. User decision: run it as a **deliberate, batched audit** (ground in waves → source-verify each wave's disputes → `apply-fix` → repeat), not fire-and-forget. **State (2026-06-20, after wave 6): 560/2,160 grounded (~26%); ~40 source-verified bank corrections across waves; see [[rag-grounding-layer]] for the live frontier + resume point.**
  - **Not built yet (the retrieval half + the consumer):** generate embeddings (keyless model TBD — Supabase `gte-small` edge fn vs local `bge-small`, both 384-dim; Voyage a later eval-gated upgrade) over `plain_text`; the cross-app grounding/retrieval **API** on PYQ Vault (Bearer-secret, mirror `src/app/api/sync/mock/route.ts`) that nda-tracker's tutor calls; nda-tracker's chat UI. Tutor output validated via a faithful simulation (grounded vs ungrounded), not the live app.

- **2026-06-17 — Cross-app wrong-answer remediation: a `/go` redirect seam + "Learn this / Practice" links on nda-tracker's exam + quiz reviews + conceptSlug plumbed into the Tags export.** Answers "for every wrong question, give 3 practice questions — is it worth it?". The analysis reframed the ask (the literal "3 immediate clones" is massed practice + ignores error-type + balloons length); shipped the better shape — **teach-link as the universal spine + ONE bounded practice set, not 3-per-wrong**. Built for **exams** (the OMR/Evalbee papers — the real LWS assessment; an early build targeted the daily *quiz* review first, then the user clarified "exams") while keeping the quiz buttons.
  - **The seam (PYQ Vault, reusable): two GET redirects `/go/learn` + `/go/practice`** ([src/app/go/](src/app/go/)) that take whatever a delivery-app review row carries (notes slugs OR DB names) and resolve to the right `/notes` page / `/browse` filter. `subtopic` resolves **slug-first** (quizzes + tagged exam q) **then by DB name** (the exam path — exam questions carry chapter/subtopic NAMES, never slugs); every path **degrades gracefully** (unresolved → `/notes` index or generic `/browse`, never a dead end). Pure resolver [src/lib/notes/goLinks.ts](src/lib/notes/goLinks.ts) (`getSubtopicBySlug`/`getSubtopicByName`/`buildLearnPath`/`canonicalSubjectName` Maths→Mathematics/`corpusForSubject`); `buildBrowseUrl` gained `kind`. **Why a redirect, not nda-tracker building UUID URLs:** taxonomy resolution lives in the app that owns the taxonomy; nda-tracker stays dumb (passes strings it already has) and PYQ Vault can change `/browse` internals freely. TDD `tests/go-links.test.ts` + `tests/go-routes.test.ts` (DB-backed corpus selection verified live).
  - **Corpus by subject (the "practice vs daily-quiz atoms" call): Maths → the practice bank (fresh problems), every other subject → the PYQ bank.** Daily-quiz atoms are deliberately NOT used for exam remediation — they're Level-1 recall fragments (wrong rung for a missed full *problem*), and their one edge (concept-precision) is nullified because an exam miss only resolves to **subtopic** level anyway. "Learn this" → `/notes` is the universal teach-step (covers English + GK where no practice bank exists).
  - **nda-tracker side** (commit `44b443e`, wiring completed in `9b1b3ce`): `examLearnUrl`/`examPracticeUrl` in `src/lib/remediation.js` (prefer slugs, fall back to names) + the buttons rendered in the **shared `QuestionCard`** via a `showRemediation` prop. `parseTagsFile` (`src/lib/excel.js`) reads the new slug columns into the verbatim-stored `exams.questions` jsonb (no migration). **Under-wiring caught by a user screenshot:** the buttons were first added to only 2 of `QuestionCard`'s **6** render sites (`FocusedExamResult` + `ExamInsightsPanel`); the admin Exam-History view (`ExamHistoryTable`) + 3 audit panels (`ChapterAccordion`/`UnattemptedAudit`/`WrongAnswerAudit`) showed nothing until `showRemediation` was passed there too. **Lesson: a feature behind an opt-in prop on a SHARED component must be wired at EVERY call site — `grep` all `<Component` usages, don't assume "student + teacher = two components."**
  - **Step 2 — conceptSlug precision upgrade, near-zero cost.** [tagsSheet.ts](src/lib/export/tagsSheet.ts) `buildTagRows` now emits `SubtopicSlug` + `ConceptSlug` (the question's primary `question_concept_tags` row, via `getResourceTagsForQuestions`). **The coverage probe is the load-bearing fact: concept tags exist for ~100% of PYQ Maths/Physics/Chem/Bio/Geo and 0% for English/History/Polity/CA/Economics — exactly the `/notes` line** (English, the biggest GAT section, will never have them). So conceptSlug is a Maths-and-science *upgrade where it exists*, never the foundation — tagged exam q get a concept anchor + slug-precise resolution, untagged degrade to subtopic. It auto-improves as notes coverage grows; **no English/GK tagging campaign** (the teach-link is the right remediation there). No migration — the slugs ride into the schemaless jsonb verbatim.
  - **Gates:** PYQ Vault full pre-push gate PASS (commit `eb3e1ac`); nda-tracker 1540 tests + build. Not live until both deploy (buttons point at `pyqvault.com/go/*`); existing exams work immediately (name-based), future imports gain concept-precision automatically. **Deferred** (in SUGGESTIONS.md): error-type signal (slip vs concept-gap), a persistent "mistake notebook" + fixed-tracking, the teacher per-student drill-in (StudentQuizHistory rows → QuizReview). Cross-app mechanics in [[cross-app-feature-execution]].

- **2026-06-16 — First non-Maths PRACTICE ingestion (NDA Geography) + practice pipeline extended for clean-text/no-key sources + a /dashboard/papers entry with its tagged-Excel export.** Ingested the LWS "NDA Atmosphere Test P1" PDF (50 atmosphere MCQs) into NDA Geography → **Climatology, Atmosphere and Weather** as `question_kind='practice'`, PRIVATE. Three firsts/extensions: **(1)** the `scripts/practice/` pipeline (built for the Maths book's vision-transcribed, answer-key-from-text PDFs) now also handles **born-digital clean-text PDFs with NO printed answer key** — `Topic.questionPages/answerKey/solutionPages` are now OPTIONAL, `buildRecords` takes `topic.subjectName` (was hardcoded "Mathematics"), `commit.ts` skips the answer-key parse when absent (answers come entirely from `overrides.json`), and `render.ts` guards the now-optional page fields. All additive — existing Maths topics untouched (typecheck clean). **(2)** No key → **all 50 answers DERIVED** (CDS-English style), committed PRIVATE with **6 `REVIEW`-flagged** lower-confidence items (Q2/Q11/Q15/Q17/Q34/Q36/Q50) for spot-check before PUBLIC; preview HTML at `scripts/practice/out/atmosphere-p1.preview.html`. **(3)** The per-exam `content_hash` dedup **skipped 2 of 50** (Q12 "gases 2nd in abundance", Q23 "not a greenhouse gas") because they already existed as PUBLIC NDA PYQs **misfiled under Chemistry (Industrial Chemistry) and Biology (Ecology)** — a genuine cross-subject GAT recurrence. Per the user, both were **re-filed to Geography → Climatology** (subject_id + chapter_id + subtopic_id updated; content_hash unchanged so no rehash) and their now-stale `question_concept_tags` (`ind-gases/applied-gas-reactions`, `eco-environment-biodiversity/eco-greenhouse-pollution`) **deleted** (else they'd be mistags — minor: each was a drill question in the Chemistry/Biology /notes concept, now removed). **(4)** Built a `/dashboard/papers` paper **"NDA Atmosphere Test P1"** (50 Qs, single Geography section, Q-order) via the `createPaper`/`addQuestion` admin helpers (service-role one-off, created_by=admin), then emitted **`generated-papers/Tags_NDA_Atmosphere_Test_P1.xlsx`** via the SAME `buildTagRows`/`tagRowsToAoa` the `/api/export?kind=tags` download uses — byte-identical to a UI download, for nda-tracker's offline-OMR loop. Including all 50 (48 practice + the 2 re-filed PYQs at positions 12/23) is required for Q-number parity with the Evalbee scan. Reusable lessons in [[practice-ingestion]] + [[practice-pdf-vision-ingestion]] (the clean-text/no-key/non-Maths path is the new variant).

- **2026-06-16 — "Trigonometric Simplification Toolkit" foundation concept added to all 4 calculus notes chapters (NDA + CET × Indefinite Integration + Differentiation).** A user-requested shared reference: grounded by scanning all **433 PUBLIC PYQs** across the 7 NDA-Maths calculus chapters (despace+lowercase the combined `text||solution`, `strpos`-count the canonical motifs, then read real solutions to confirm each simplification). **Finding worth keeping: in NDA *calculus* the recurring trig simplifications are the half-angle / power-reduction / perfect-square family — NOT the inverse-trig substitution table** (`2x/(1+x²)→sin2θ`, `3x−4x³→sin3θ`); those barely appear in calculus (1 and 0 hits) — they live in the Inverse Trigonometry chapter. The six recurring families (each in the concept): `1±cos x` half-angle (`2sin²/2cos²(x/2)`, `(1−cos)/(1+cos)=tan²(x/2)`); `1±cos2x` power-reduction; `√(1±sin2x)=|sin x±cos x|` (**modulus is load-bearing — sign by interval**); `sec x±tan x=tan(π/4±x/2)`; `a sin x+b cos x=√(a²+b²)sin(x+α)`; Weierstrass `t=tan(x/2)`. **Implemented as a duplicated `formula`-variant FOUNDATION concept (no `pyqExampleId` → 0 tags needed, and — confirmed — formula-variant foundations are fully EXEMPT from notes-lint's un-tagged-concept warn, unlike reference-variant foundations which soft-warn).** Globally-unique slugs `ii-`/`cetii-`/`diff-`/`cetdiff-trig-simplification-toolkit`; integration chapters got an ∫-flavoured worked example, differentiation chapters a d/dx one; `\log` (CET) / `\ln` (NDA) — moot here (pure trig, no log). Gate green (typecheck 0 · notes:lint 0 err · notes:latex OK); pre-push build prerenders the SSG notes pages so render is covered. Commit `49a7c68`. **Reusable pattern: a cross-chapter shared reference = the same no-PYQ formula-variant foundation concept duplicated into each chapter's foundation subtopic (cheap, tag-free, warn-free) — there is no single-source shared-concept mechanism in the notes model.** See [[notes-self-sufficient-template]].

- **2026-06-16 — MHT-CET Maths Differentiation /notes shipped (2nd MHT-CET notes chapter) — Phase-D reshape of 3 catch-all subtopics → 6 pedagogically-clean ones.** The bank's HARDEST CET Maths chapter (103 PYQ · ~45% HARD · 0 practice). Detailed analysis first surfaced that the 3 DB subtopics were **label-based catch-alls** scattering the same techniques across each other (logarithmic diff, implicit, differentiability, function-wrt-function all split across "Implicit/Log/Parametric/Special Forms" AND "Chain Rule/Product/Composite"); only "Inverse Trigonometric Differentiation" was coherent. **Pedagogy-first ⇒ Phase-D reshape** (greenfield: 0 concept tags, 0 sets, no MHT-CET guide, no code refs ⇒ downstream-safe — confirmed before touching the DB): 3 → 6 subtopics — foundations-chain (15, +differentiability) · logarithmic (19) · implicit-special (25) · inverse-functions (29, biggest) · parametric-higher (10) · derivative-wrt (5); teaching order set via `order_index` in the same transaction (so `notes:order` was already in sync). 32 concepts; **all textbook formula tables included** (Tables 1.1.1/1.1.2/1.2.1–1.2.4 + log-diff + parametric + nth-derivative as a no-PYQ formula-variant **foundation** so "all formulas" is honored without faking bank evidence). `cetdiff-` concept slugs (NDA Differentiation owns `diff-`; globally-unique per the mht-cet slug-collision pitfall); `\log` convention (not `\ln`, matching MHT-CET); **reused 3 existing viz slugs** (`diff-tangent-slope`/`diff-modulus-corner`/`inverse-reflection-line`) since viz slugs are a shared component registry → **no `VisualizationSlug` union or ConceptUnitCard changes, no new SVGs**. **Built via a 6-agent parallel batch + serial merge** (each agent authored ONE subtopic file off a fixed concept skeleton = slug + featured-PYQ + tag-group I pre-designed, so the output was mergeable and the 103 tags were predetermined; I wrote chapter.ts/index.ts/registry/wrappers + ran the tag INSERTs). 103 tags, **100% coverage, mistag-clean** (each subtopic_slug → exactly one DB subtopic). **Quiz-ready per Step 1b** (the daily-quiz discipline baked in at authoring): every recallable formula in `formula.latex`, self-contained shuffled-atom-safe self-checks, ≥12 self-contained traps/file — `quiz:coverage` came back **0 formula gaps, 75 traps** (no future harvest-rework). notes-lint #5 caught 3 worked-examples/self-checks re-using their featured PYQ's numbers (the logarithmic product-chain + sqrt-quotient concepts) → rewrote to genuinely different problems; 2 `whyItMatters` LaTeX leaks → Unicode/prose. Gate: typecheck · lint · notes:lint (0 err) · notes:latex (OK) · test · build → **prepush PASS**; all 7 routes live-verified HTTP 200. NOTE: the disputed-answer-key `a18fbd89` (sin⁻¹x²+cos⁻¹x²) was taught-but-not-featured, flagged for a future `/solution-cleanup` pass. Reconfirms [[notes-structure-pedagogy-first]] + [[reclassification-sql-pattern]] + [[notes-self-sufficient-template]] (parallel-batch orchestration) + [[quiz-formula-coverage-gap]] (Step 1b).

- **2026-06-15 — Paper builder: replaced the per-card `/browse?paper=` add-mode with a cart→paper bridge — fixes the param-drop banner bug by removing the mechanism, not patching it.** The step-2 per-card mode kept its state in the URL (`?paper=<id>`), but every filter/pagination/search navigation rebuilds the URL from `buildSearchParams(filters)` — which has no `paper` field — so the "Adding to …" banner + the whole add-mode vanished on the first filter (the documented param-drop class the `BackToNotes` pill sidesteps via sessionStorage). The fix wasn't a `qb_active_paper` cookie (the patch); it was the user's better reframe: **lean on the cart**, which is localStorage and already survives all navigation + reload. New model: browse + Add to the cart as normal → open the cart panel → **"Add to paper"** (an org-member-only button beside "Download paper") → pick an active draft paper (or "+ New paper") → **bulk-commit**. Each question auto-files by subject; idempotent (re-add reports "already there"). New: pure `planBulkAdd` (`sections.ts`, 6 TDD — dedup + section/position assignment) + `addQuestionsToPaper` batch (admin.ts, +1 integration test) + `listActivePapersAction`/`addCartToPaperAction` + `AddToPaperDialog` + a `CartPill` `isOrgMember` prop. **Removed:** `src/app/browse/ActivePaper.tsx` (provider/banner/hook), the QuestionCard active-paper branching, and the `browse/page.tsx` `?paper=` wiring; the editor's "Add from Browse" deep-link became a plain "Browse the bank" → `/browse`. The cart is now a hub: **Download** (solo Word) *or* **Add to a paper** (collaborative). Net: one fewer state mechanism, the bug gone by deletion, and the familiar cart (which survives filtering) is the collection buffer. Gate green. (Supersedes the step-2 entry below.) See [[collaborative-paper-builder]].

- **2026-06-15 — Collaborative paper builder, follow-ons: within-section reorder + assignee/attribution UI.** (1) **Reorder** — up/down move buttons per question row (user chose this over drag for a11y + touch/tablet + no new dep). Pure `positionForMove(orderedRows, qid, "up"|"down")` (`src/lib/papers/sections.ts`, 7 TDD) computes the fractional target via `positionBetween`; `reorderQuestion`/`reorderQuestionAction` UPDATE `paper_questions.position`; buttons disabled at section edges. (2) **Soft assignment + attribution** — the SectionManager dialog gained per-section assignee toggle chips (→ `setSectionAssignees` → `updateSectionTemplate`); assignees render as chips on each section progress bar; each question row shows "added by &lt;name&gt;". **Org-member names come from the service-role `listMembers(orgId)`** (reused from `src/lib/members/admin.ts`) because `org_members` read RLS is admin-only — a TEACHER's authed client can't read it, so the editor page resolves names server-side via service-role (scoped to the caller's org) and passes a `{id,label}[]` to `PaperEditor`. Gate green; user-verified the authed golden-path in-browser.

- **2026-06-15 — Collaborative paper builder, step 2: add-to-paper from `/browse`.** Wired the full-filter bank into the paper builder. `/browse?paper=<id>` (org member only) mounts an `ActivePaperProvider` that **repurposes the existing QuestionCard "Add" button to target the active paper** instead of the localStorage cart — questions auto-file into the section matching their subject (`subjectToSectionKey`); a top banner shows the live in-paper count + a "Back to paper" link. **Strictly additive + gated:** the provider value is null whenever there's no `?paper=` param (or the viewer is anon / the paper is finalized / the id is foreign), so normal + anon browse traffic is byte-identical — smoke-confirmed unauthed (`/browse`, `/browse?paper=<uuid>`, `/browse?paper=garbage` all 200). The editor links out to `/browse?paper=<id>` ("Add from Browse") so teachers get the full chapter/subtopic/year/principle filters the editor's embedded search panel doesn't expose. Touches: new `src/app/browse/ActivePaper.tsx` (provider + hook + banner) + small edits to `QuestionCard.tsx` (the Add toggle branches on the context) + `browse/page.tsx` (param → provider wrap) + an editor link. Full prepush gate green.

- **2026-06-15 — Collaborative paper builder shipped, step 1 (migration 0039): multiple teachers assemble ONE exam paper together, partitioned by section.** Answers "GAT paper is created by multiple teachers — how can multiple teachers contribute to a single paper?" Today a "paper" is the ephemeral per-browser localStorage cart (`qb:cart:v1`); this promotes it into a persistent, org-scoped, section-partitioned entity. Two new tables: `papers` (org-scoped; `draft`/`finalized`; editable `section_template` jsonb; `finalized_snapshot`) + `paper_questions` junction.
  - **Why a junction table, NOT a `question_ids[]` array on the paper row:** two teachers adding to the same section concurrently must not clobber each other. Each add is one independent INSERT row; `PRIMARY KEY (paper_id, question_id)` + `ON CONFLICT DO NOTHING` makes concurrent/duplicate adds idempotent (no read-modify-write lost-update window). Mirrors `quiz_atoms_map` — membership is a JOIN, not an array.
  - **Lifecycle mirrors the quizzes snapshot (0035):** `draft` is the live mutable junction table; **finalize** freezes the composition onto `papers.finalized_snapshot` (jsonb) so a later bank edit can't silently change a printed paper; **reopen** clears it. The export always feeds the paper's ordered ids into the existing `/api/export` `questionIds` path — **zero new export code**.
  - **The key insight — GAT papers are naturally partitioned by SUBJECT, and teachers are subject specialists.** So collaboration isn't N people editing one list (locking/CRDT) — it's N people each owning a SECTION; conflicts disappear by construction. The per-section progress view (English 48/50 · Physics 12/25 …) IS the coordination mechanism (falls out of the data model, not bolted on). The elegant tie-in: `subjectToSectionKey` derives a question's section from its subject, so adding auto-files (a "Physics" q → the Physics section).
  - **Collaboration model (user decisions, 2026-06-15):** (1) **org-wide access** — any ADMIN/TEACHER in the org opens + contributes to any of the org's papers (the org IS the boundary; no per-paper invite table). (2) **Soft section assignment** — anyone edits any section; assignment is a hint, never a lock. (3) **Multiple teachers per section** (native to the junction model). (4) **Sections fully editable** per paper (add/rename/delete/retarget); `section_template` seeded with a GAT default (English + GK subjects, ~150). (5) **Deleting a section is NON-destructive** — its questions keep their `section_key` and render under "Unassigned" until re-filed (any membership whose key isn't a current template key); no DB reassignment, no lost work.
  - **RLS:** org-scoped like PRIVATE questions — read = any org member; insert/update = org editors (`current_user_can_edit_questions`); **delete a paper = creator-or-admin, enforced in Postgres** (`created_by = auth.uid() OR current_user_is_admin()`). All FKs cascade from `organizations`, so the org-sweep `global-teardown` cleans test fixtures with **no teardown change** (advisor clean — no new lints).
  - **Access lives at `/dashboard/papers`** (list + `[id]` editor). Reachable via a **UserMenu link for org members** — teachers are redirected off the `/dashboard` home but still need Papers, so the entry point is the user menu (+ a dashboard-home card for admins). The editor: section progress bars, questions grouped by section (remove + move-to-section), an embedded search-and-add panel (reuses `queryQuestions`; auto-files by subject), editable sections, finalize/reopen, Download (reuses `/api/export`).
  - **Built ISOLATED — zero changes to `/browse`** (the only existing-code touches were the two additive nav entry points). TDD: 28 pure-helper unit tests (`papers-template`/`papers-sections`) + **9 RLS/data integration tests** (`papers-rls` — org isolation on read AND write, idempotent add, multi-teacher same-section add, finalize-freeze, creator-or-admin-only delete) — all green; full prepush gate green; new `ƒ` routes load (307 auth-redirect, not 500). The `/browse?paper=<id>` add-flow is step 2 (a fast-follow). Pure core in `src/lib/papers/` (`types`/`template`/`sections`/`admin`); UI under `src/app/dashboard/papers/`. See [[collaborative-paper-builder]].

- **2026-06-15 — Dedup is now PER-EXAM (migration 0038): `questions` unique key `(org_id, content_hash)` → `(org_id, exam_id, content_hash)`.** Surfaced during CDS ingestion: CDS-II 2024 Q44 silently vanished because it's an exact duplicate of an NDA English question (`NDA_GAT_2024_1` Q38 — CDS and NDA are both UPSC GAT English and **share questions**), and the org-wide `content_hash` index dropped the CDS copy. The user chose per-exam dedup so each exam's papers are complete and the cross-exam recurrence is visible under both exams (consistent with [[keep-duplicates-recurrence-signal]]). Changed: migration 0038 (0 conflicts pre-flight); `commitStaged` upsert `onConflict` → `org_id,exam_id,content_hash`; **`applyMockSync`'s two `content_hash` lookups scoped by `exam_id`** (else `.maybeSingle()` would throw once a hash exists under 2+ exams). Within-exam dedup (re-upload → 0 rows) + the MHT-CET mock-sync merge are unchanged (mocks are single-exam). The contract tests (`upload-commit-dedup`, `upload-flow`, `sync-mock-flow`) are all within-exam so stay green.
- **2026-06-15 → COMPLETE 2026-06-16 — NEW exam: CDS English ingestion (`scripts/cds/` pipeline) — ALL 19 papers committed PRIVATE (2,280 q); no answer keys → LLM-derived + confidence-flagged answers.** First new exam since JEE. Source: 19 scanned CDS English booklets in `C:\tmp\PYQPs\CDS\02. English\` (2017-I … 2026-I), **image PDFs with no text layer and NO official answer key**. So: vision-transcribe → **DERIVE each answer + flag confidence** (HIGH/MED/LOW) → commit PRIVATE for human spot-check before PUBLIC. New DB exam `CDS` (id `07700c16-…`) + subject English, taxonomy **cloned from NDA English** (8 ch / 29 sub — CDS English ≈ NDA English in style). Registered in `examContext.ts` (`ExamSlug` + registry → exam picker + `/notes/cds` hub auto-derive). State + resume in [[cds-english-ingestion]]; durable how-to in [[section-based-exam-ingestion]].
  - **The pipeline (`scripts/cds/`, mirrors `scripts/practice`/`scripts/jee`):** `config.ts` (paper registry + the **section-type catalog** — each CDS section type → NDA-English taxonomy + `underline`/`inlineStimulus`/`passage`/`perQuestionSubtopic` flags) · `lib.ts` (pure assembly + `validateRows` collision/coverage/delimiter checks + `normalizeQuestions` self-heal; **TDD `tests/cds-lib.test.ts`, 19 cases**) · `render`/`commit`/`preview` + README. Per paper: render → **section-map agent** (`data/<id>.sections.json`) → **transcribe BY SECTION** agents (`data/<id>.questions.json`) → **underline agent** (`data/<id>.underlines.json`) → `commit` dry-run → `preview` → `commit --apply`.
  - **The load-bearing lessons (from the 2026-1 trial → [[section-based-exam-ingestion]]):** (1) **section order/selection varies every year** (2026 opens word-pairs; 2017 ordering-of-words; 2023 word-classes; 2024 antonyms — 13–16 sections) → a per-paper **section-map pre-pass** is mandatory; (2) **Directions are load-bearing, not decoration** — synonyms vs antonyms look identical without them and FLIP the answer; transcribing by raw PAGE range split the antonym section across agents and silently flipped 5 answers (Q56-60, one unflagged HIGH) → **transcribe BY SECTION**; (3) **directions in `context`, one set per section** (mirrors NDA English: 887/900 directions-in-context, 895/900 in sets — renders once per section); (4) **fold the per-question stimulus (match-list code grid / S1-S2 pair) INTO the hash-bearing stem** — `content_hash` excludes `context`, so a generic stem + shared options collide + silently drop (6 dropped on the first trial commit); RC/cloze passages stay shared in the set context; (5) **underlines** via `\(\underline{\text{word}}\)`.
  - **Pipeline hardening as papers revealed shapes:** the section-type catalog grew to **~27 types** as papers exposed new shapes (`word-usage-count`/`word-usage-select`, `voice-transformation`, `word-meaning-statements`, `usage-statements`, `spelling-select`, `sentence-transformation`, `reported-speech`, …). `normalizeQuestions` self-heals three recurring agent quirks: options emitted as an OBJECT instead of array; difficulty synonyms/casing; **and a rearrangement-grid agent returning the ordering STRING (e.g. "QSPR") as `answer` instead of the option label** — mapped back to its label. The biggest per-paper gotcha: **"Ordering of Words" P/Q/R/S grids span both columns → render the FULL PAGE hi-DPI** (`fitz.Matrix(3.0+)`); per-column crops split the grid at the gutter and drop fragments (bit 2024-2 + 2022-2, both recovered full-width). **5-option spotting-errors** (4 parts + "No error") refit to the 4-option bank.
  - **Subagent content-filter gotcha:** a transcription/section-map subagent reproducing **verbatim passage prose** (the 2024-I RC section is Faulkner's Nobel speech with "doom/dying" imagery) can return **"Output blocked by content filtering policy"** (0 tokens). Workaround: have the agent do **structure-only** mapping (no prose, `passage:"TBD"`) and capture the passage separately (OCR/manual) so the trigger text is never generated by the model.
  - **State — COMPLETE.** All **19 papers (2017-I … 2026-I) committed PRIVATE, 2,280 q**, on `main` + pushed; every q has Directions-in-context + 4 options + exactly 1 correct; 253 sets. `Eng_CDS_2024_1.pdf` was Elementary Mathematics until the **user replaced it with the correct English booklet 2026-06-15**. **The ONLY open item before PUBLIC:** human spot-check the LLM-derived answers (no official keys), prioritising MED-flagged items + the oldest dense scans (**2017-1 worst, 60 MED**) + the part-rearrangement grids. Per-paper review HTMLs at `scripts/cds/out/<id>.preview.html`; per-paper detail in [[cds-english-ingestion]].

- **2026-06-14 — Cross-app tagged-sheet export: PYQ Vault emits nda-tracker's enrichment `.xlsx` directly (kills the hand-typed tags file) + passage context carried end-to-end.** The offline-exam loop uploads two files to nda-tracker — the Evalbee OMR *Results* (student responses) + a *Tags* enrichment file (per-question chapter/subtopic/options/answer/solution/difficulty). The Tags file was **hand-built**, leaking wrong-key / mistyped-option / off-by-one / forgotten-GAT-subject errors — root cause: re-typing data that already lives, verified + structured, in the bank. Fix (Phase A): PYQ Vault emits the Tags sheet from the built paper, so there's no human transcription left to get wrong. Shipped both apps, green (PYQ Vault prepush PASS; nda-tracker 1494 tests). See [[cross-app-feature-execution]].
  - **PYQ Vault** ([tagsSheet.ts](src/lib/export/tagsSheet.ts) TDD 19 cases + `kind:"tags"` on [/api/export](src/app/api/export/route.ts) → `.xlsx` via SheetJS (already a dep) + a third DownloadDialog button): `buildTagRows` numbers via the **same `groupBySet` loop the docx uses**, so Q-numbers match the printed paper AND the Evalbee scan **by construction** (the load-bearing alignment). Answer derived from the `isCorrect` option; subjects mapped to nda-tracker keys (`Mathematics→Maths`, `Current Affairs→Others`, GAT subjects pass through so `validateGatSubjects` is satisfied); a **distinct `Context` column** for passage sets.
  - **nda-tracker** (derived copy — **NO DB migration**): `parseTagsFile` reads a `Context`/`Passage` column → a `context` field that flows untouched into the `exams.questions` **jsonb** (Step4Confirm stores the tags array verbatim, no field whitelist — *the schemaless jsonb blob is exactly why context needed no DDL*; typed columns would have forced an `ADD COLUMN`). The passage renders above the stem on all four review surfaces (`QuestionCard`→ExamInsightsPanel + inline `QuizResults` + `quizTaking` take/review), so RC/Cloze are reviewable instead of meaningless stems. `NDA_FREQ_BY_SUBJECT.Maths` synced to PYQ Vault's 31 canonical chapter names (the validation source — `getValidChapters` reads it; the freq *chart* reads each user's persisted `ndaFreqBySubject`, so existing users are untouched; parent `pct` split to keep the total).
  - **The design spine — PYQ Vault is master (user-confirmed).** The Tags Excel is a lossy, schema-coupled bridge; the durable context work (jsonb field + render) lives on the side we keep and is Phase-B-reusable. So context went in as a **distinct field, never inlined into the stem** — zero throwaway modeling, nothing to migrate out later. The one disposable bit is ~5 lines of column-detection in `parseTagsFile`.
  - **Deferred — Phase B (the long-term win, user-confirmed): retire the Excel via a direct API push.** PYQ Vault pushes the built paper's tags over the existing cross-app Bearer-secret bridge (same pattern as the Quiz Factory `quiz-import`), nda-tracker stores it keyed to a paper id, and the upload wizard picks a pre-loaded paper from a dropdown — so only the Evalbee Results is uploaded by hand. Carries context/set-grouping/images losslessly (writes the same jsonb the same way → still no migration). The Evalbee Results upload always stays (it IS the OMR output). → ROADMAP.

- **2026-06-14 — New guide page: `/guide/nda-physics/ncert-map` — an NCERT↔NDA chapter cross-walk with a Class-12 weak-signal detector.** A new *kind* of guide subpage (the first source-syllabus cross-walk; not part of any template). Maps each of the 14 NDA Physics chapters to the NCERT Class 9–12 chapters it absorbs, each row deep-linking to `/browse` via `resolveTaxonomy` → `BrowseLink`. New files: [_data/ncert-map.ts](src/app/guide/nda-physics/_data/ncert-map.ts) (curated TS map — NOT a DB table, same curation-is-signal rule as guide principles), [ncert-map/page.tsx](src/app/guide/nda-physics/ncert-map/page.tsx), [tests/guide-nda-physics-ncert-map.test.ts](tests/guide-nda-physics-ncert-map.test.ts) (11 cases, written test-first). One `ROUTES` entry auto-wired the side-nav + sitemap; Traps' `PrevNextNav` chained forward.
  - **The weak-signal detector (the durable design — [[weak-signal-trend-detection]]).** The user's framing: NDA questions mapping to NCERT *Class 12* in *recent* papers is a leading indicator of syllabus drift — don't ignore the weak signal. The map embodies it: tracked Class-12 topics carry live-bank recency, and a pure `signalStatus()` helper classifies each **live** (≥1 q since `RECENT_FROM=2024`) / **watch** (tested before, now cold) / **dormant** (mapped, never seen). watch/dormant rows render muted with a "last seen" chip, so the page IS the detector — it surfaces a topic the moment it climbs. But the inverse discipline is baked into the copy: **we do NOT pre-write notes/practice for dormant topics** — chasing an absent signal is the same error as ignoring a real one, inverted. Map it, flag it, act when it goes live.
  - **The premise was checked against data before agreeing — and it didn't hold (yet).** Per-paper-normalized probes showed the recent Class-12 footprint is flat-to-*declining* (Modern Physics 1.9→1.3→0.8 q/paper; EMI vanished since 2017; Wave Optics/Communication never seen). So no Class-12 wave is present today. Two corrections fell out: (1) my initial 9/10/11 framing **undercounted** — a large slice of NDA already maps to Class 12 (Ray/Wave Optics, Current Electricity, EMI, AC, all of Modern Physics), so the map is 9–**12**; (2) the "missing rotational-motion chapter" is a **non-gap** — only ~3 incidental q bank-wide, accurately reflecting that NDA barely tests it (surfaced as the `UNMAPPED_NCERT` footer, not manufactured into a chapter). `signal` recency numbers are an authored snapshot (`SIGNAL_SNAPSHOT=2026-06-14`); refresh by re-running the per-topic probe as the bank grows.
  - **Post-ship fix (same day):** the page 500'd in production despite a green build — a `<NcertChip>` prop was named `ref` (React-reserved → `undefined` at render → "Refs cannot be used in Server Components"). The build missed it because the page is dynamic (`ƒ`, never prerendered) and the test was data-only. Renamed `ref`→`item`; curl-verified HTTP 200. Lesson folded into the "Recurring pitfalls" `next build` entry (build-green ≠ runtime-ok for `ƒ` pages; smoke-test dynamic routes).

- **2026-06-14 — NDA Geography /notes COMPLETE (all 7 chapters) — 2nd fully-noted NDA subject; first Geography subject; + a Phase-D split of the Physical "Forests" catch-all.** Built across 2026-06-13/14 on the user's "do one chapter end to end" then "do remaining chapters autonomously" then "full phase-d split". Per-chapter one-liners in [ARCHITECTURE.md](ARCHITECTURE.md); reconfirms [[notes-self-sufficient-template]] + [[reclassification-sql-pattern]] + [[audit-probe-symmetry]] + [[scope-verification-material-first]].
  - **Earth's Structure (solo, 2026-06-13):** the first Geography notes chapter — 7 subtopics · 30 concepts · 74 q · 8 static SVGs (Earth interior, plate boundaries, focus/epicentre, P/S/L waves, shadow zones, volcano types, rock cycle, fluvial landforms). Pedagogy-first endogenic→exogenic arc; reorder-only (3 misfiled q reclassified within-chapter: focus→Earthquakes, NOT-igneous→Rocks, salt-crystal→Weathering). Established the `/notes/nda-geography` subject hub (registry-derived).
  - **Remaining 6 chapters (2026-06-14, 6-agent parallel batch + serial merge):** Indian Geography Economy (81 q · 28 concepts) · Indian Geography Physical (67 q) · Climatology (57 q · 25 concepts · 4 diagrams) · World & Human Geography (25 q) · Earth in Space (22 q · 4 diagrams) · Oceanography (19 q · 3 diagrams). 271 q tagged at 100% coverage, mistag-clean. **All reorder-only** — the shipped `/guide/nda-geography` references every DB subtopic name verbatim (confirmed up front), so display titles + subtopicOrder only. Recall-heavy → reference-variant the workhorse. The Economy agent surfaced **16 catch-all misfiles** in its "Agriculture" subtopic (coal/iron-ore→Minerals, census/schemes→Sectors, ports/railway→their subtopics) — reclassified (subtopic_id only). Same orchestration as the Biology/Chemistry/Physics batches: agents author their own `_data` + SVGs under a unique slug prefix + return a handoff (chapters.ts entry, `VisualizationSlug` union lines, ConceptUnitCard wiring, tag INSERT); the main agent serially merges the 3 shared files + runs the tag INSERTs.
  - **2 source-verified wrong-key/flawed-question findings** (both from local GAT 2025 NDA-2 PDF, [[gdrive-pdf-fetch]] PyMuPDF-render-then-Read): `2604a840` (chert/geyserite/shale "correctly matched" pairs) — extraction faithful but the question is genuinely defective (geologically-correct "1 and 3" isn't an option); key A preserved, solution rewritten honest. `8f34b21d` (Assam shares borders with how many countries) — the bank **solution falsely claimed Assam borders China**; corrected to 2 countries (Bhutan, Bangladesh) + 7 states; official key C preserved with a discrepancy note (couldn't confirm the official key isn't C); notes teach the correct count. The agent-flagged `cafefe16` (Tripura borders only 1 country) independently corroborated Assam=2. Both are **preserves, not flips** — see the content-audit "Preserved paper defects" line.
  - **Phase-D split of the Physical "Forests and Natural Vegetation" catch-all (2026-06-14):** the DB subtopic held 34 q but only 14 were about forests. Carved the other 20 into proper homes (subtopic_id only, no rehash): 12 river/dam/drainage → Rivers (15→27 q, now the chapter's biggest subtopic), 3 climate/monsoon → Soils/Climate (7→10 q), 5 location/extent/frontier → a **NEW "Location, Extent and Frontiers of India" subtopic** (the standard "where is India" topic, with no clean existing home; placed FIRST in teaching order). **Lesson:** a Phase-D split can both reclassify into existing subtopics AND create a new one for content that fits nowhere; and because the agent had already woven the relocated facts into the right notes concepts (the Rivers note already taught Tawi-not-Indus, Vitasta=Jhelum, etc.), 7 of the 12 river questions retagged onto existing concepts at near-zero content cost — only 1 new Rivers concept + the new Location note were needed. A new subtopic is purely additive (no rename), so nothing breaks; downstream sync = notes:order (order_index) + guide prose counts + the playbook `subtopics` array (so the new name resolves in the guide live-taxonomy test). Forests' %HARD rose 12→29% once the easy river/longitude q left, making it (not Mountains) the chapter's densest-HARD pool — guide prose synced across 5 files.

- **2026-06-13 — NDA Maths practice Batches 1–9: ALL 5 source folders ingested → 3,040 practice q across 29 chapters. Practice ingestion COMPLETE.** Resuming the 2026-06-12 pilot, the user asked to "complete NDA Maths practice, three chapters at a time, add to bank, update docs, repeat; some chapters may be split (e.g. Matrices & Determinants) — handle them." First pinned the **full Algebra source→chapter map** (section headers + first-Q markers): the source book's 18 sections collapse onto the existing NDA Maths chapters, several merging multiple sections (Sets+Relation, Permutation+Combination, Matrices+Determinants+System-of-Equations, Probability+Conditional+Baye's+Probability-Distribution). **Batch 1** = Sets & Relations (84) + Permutation & Combination (106) + Binomial Theorem (65) = 255 q. **Batch 2** = Matrices & Determinants (213) + Probability (133) + Binomial Distribution (40) = 386 q. Only **Mathematical Induction** is skipped (no NDA Maths chapter). Each chapter slotted into **existing** subtopics — zero taxonomy created.
  - **Orchestration: parallel transcription agents + orchestrator zoom-verify** ([[practice-pdf-vision-ingestion]]). One sub-agent per chapter (the 214-q Matrices & Determinants split across 2 agents writing temp files I merged), each given its Q-range + rendered PNGs + exact subtopic names + the parsed answer-key letters + the no-unicode-in-solutions rule. Agents transcribe + author solutions + **cross-check every derivation against the key and FLAG mismatches** (the key-cross-check catches option-order transcription errors for free). The orchestrator then **zoom-rendered every proposed override/exclude from the source PDF before shipping** — non-negotiable, because agents mis-read ~1–2 stems per ~100 q exactly where it's costly.
  - **The verify-before-override discipline caught 3 agent misreads this run** (would have shipped wrong answers): P&C Q304 (`=36` read as `=10`, key D was right), Probability Q800 (agent **scrambled the whole option block** A↔C, D↔B — the key D=1/5 was right; fixed the option order, not the key), and several "disputes" that were the agent's own mis-reads of the dense answer-key string. **~22 genuine corrections shipped** via `overrides.json` (wrong-keys + blank-keys across both batches) + **2 single-char stem-typo fixes** where the keyed option only becomes correct after the fix (M&D Q565 `(2,2): a→1`, Q585 `(1,3): cot²135°→cot135°`; same pattern as Quadratic Q212) + **6 genuinely-flawed questions excluded** (Sets Q57/Q82 blank-no-content, P&C Q292/Q315 + M&D Q571 answer-not-an-option). Legitimate multi-key "is/are correct" items kept as-is (M&D 650/656/687, Probability 734/807/819/820, Sets 60).
  - **Two agent output-format quirks to expect + auto-fix** (banked): an agent may write `options` as an **object** `{"A":…}` instead of the array `[{label,text}]`, and may use **difficulty synonyms** ("medium"→MODERATE). Both are mechanical normalizations before commit (a Python pass), not re-transcription. `commitStaged`'s content-hash dedup auto-skipped 1 exact within-section duplicate in Binomial Distribution (40 of 41).
  - **Batch 3 (Trigonometry, 3 of 5 chapters): +Trigonometric Identities (174 q) +Trigonometric Equations (65 q) +Properties of Triangle (51 q) → 14 chapters / 1,266 q.** The Trig answer key is the LAST page of the questions PDF (not a separate file) — a full-PDF parse mis-grabs ~15 keys from question stems starting with an a–d letter, so the key page was extracted to a standalone `_trig_answers.pdf` (banked as a recurring gotcha for the remaining folders). 11 key overrides + 5 single-char stem/option-typo fixes; the verify-before-override pass again caught agent option-mislabels (Q1146 derived π/4 but the agent misread option c as π/6; Q1108 the leading "4" coefficient leaked into the options). No exclusions (flawed-looking items resolved to "None of these" or a single-char fix).
  - **Batch 9 (finishes the campaign — the 3D folder feeds TWO chapters): +Vectors (174 q) +3D Geometry (131 q) → 29 chapters / 3,040 q. Practice ingestion COMPLETE.** The 3D source book's 6 sections span two existing NDA Maths chapters: Vector Algebra + Scalar Product (Q1795–1969) → **Vectors**; Direction Ratios & Cosines + Lines + Planes + Sphere (Q1970–2100) → **3D Geometry** — so the folder was split into two `TOPICS` entries (`vectors`, `geom3d`) each with the chapter's existing subtopics, agents assigning per-question. Answer key = LAST page of the questions PDF → `_3d_answers.pdf` (the Trig/Calculus/2D gotcha). 9 key overrides (Vectors 1865→B, 1884→C, 1904→A, 1907→D, 1929→A, 1940→D, 1960→D; 3D 2034→C, 2049→C) + 5 in-place content fixes (agent component/option misreads: 1869 b→3i−4j+2k [key d then correct, NOT overridden], 1870 P→(2,4,5) [key a correct], 1884/1940 vector components, 2034 stem normal + options) + only 1 exclude (1950 — "four points coplanar" stem lists only 3 PVs, key unverifiable). **The verify-before-override zoom pass was decisive this batch — it caught FOUR agent false-alarm "key errors" that were actually agent option-misreads (1882 d=15/4, 1897 d=−1/2, 1924 b=6(b×c), 2032 d=al+bm+cn=0 all correct as keyed) AND a question-number mis-attribution** (the λ,μ-perpendicular item is Q1908 with key c correct, while the agent-flagged "1907" is the |P−Q|=|Q| item → 45° → None=D); it also rescued 1869 from a wrong exclude (agent misread b, but the source b=3i−4j+2k makes key d=√29 correct). Reconfirms [[practice-pdf-vision-ingestion]] (option ORDER + per-question number attribution are the residual transcription risks; re-render the source before trusting an agent's proposed key change OR exclude).
  - **Batch 8 (finishes 2D Geometry): +Lines (143 q) +Circles (87 q) +Conics (152 q) → 27 chapters / 2,735 q.** The 2D book's TOC **omitted a "Parabola" section** — the Circles agent flagged Q1641–1684 as parabola questions wrongly slotted into Circle subtopics; confirmed a size-12 "Parabola" header at Q1641, corrected the config boundaries (Circles 1554→1640, Conics 1641→1794) and re-subtopiced 1641–1684 into the Conics chapter's "Parabola — Equation, Properties, and Latus Rectum" subtopic. The 2D answer key is the LAST pages of the questions PDF → extracted to `_2d_answers.pdf` (same Trig/Calculus gotcha). 9 key overrides (Lines Q1477→C, Q1534→A; Circles Q1566→A, Q1610→B [blank key], Q1613→C, Q1614→A, Q1638→D; Conics Q1674→C, Q1781→A) + 3 in-place content fixes (Q1610 stem signs; Q1687 + Q1701 option order — all agent misreads of dense option blocks, keys were right) + **only 2 exclusions** (Q1659, Q1743 — no option correct). The verify-before-override zoom pass again paid off: 3 proposed "excludes" (Q1610/Q1687/Q1701) were agent option/stem misreads, kept after the source check. Legitimate multi-key items kept (Q1498 B,D; Q1758 A,B,C; Q1777 B,D). A merge/split surgery script (`out/_b8surgery.py`) did the boundary re-carve + Parabola re-subtopic in one pass — needed a file (not inline `-c`) because a solution string with apostrophes (`g'x`) broke the shell quoting; rewrote with subscript notation.
  - **Batch 7 (finishes Calculus): +Applications of Integration (65 q) +Differential Equations (118 q) → 24 chapters / 2,353 q.** Hit the account session limit mid-fan-out (3 agents died with 0 output, nothing committed to DB — clean recovery, re-ran after reset per [[notes-self-sufficient-template]]'s canary discipline). 6 key overrides; the verify-before-override pass again caught 2 BAD agent proposals — Q3040 the agent dropped the stem's "−1" then called it flawed (restored, key D correct, NOT excluded), and Q3065 the agent swapped the parabola/hyperbola option labels then disputed the (correct) key C. Also Q2910 a^(3/2)→a² expression fix. **Calculus book complete (all 8 chapters).** 3 of 5 source folders done.
  - **Batch 6 (Calculus, to 6 of 8): +Application of Derivatives (180 q) +Indefinite Integration (71 q) +Definite Integration (137 q) → 22 chapters / 2,170 q.** Definite Integration's first 9 q sit on the previous question PDF's last page (rendered in separately). 10 key overrides + 1 stem-typo fix (Q2541 "decreasing"→"increasing") + 0 exclusions. **The verify-before-override zoom pass earned its keep twice this batch:** Q2568 — an agent proposed overriding the (correct) multi-key B,D to A,D, but the curve `y=1/x−2` has slope <0 everywhere so the tangent line needs `a,b` same-sign = B,D (kept); Q2827 — an agent proposed override D=`2√2`, but `√(1−cos²θ)=|sinθ|` integrates to `2` and `2√2` isn't even an option (overrode to C=2 instead). Both bad overrides caught at the source.
  - **Batch 5 (Calculus, 3 of 8 chapters): +Functions (123 q) +Limits & Continuity (158 q) +Differentiation (115 q) → 19 chapters / 1,782 q.** The Calculus book spans 3 question PDFs (Q2101–3068) with the answer key as the last 2 pages of PDF-3 → extracted to `_calc_answers.pdf` (same gotcha as Trig). Transcribed via 6 parallel agents (each chapter split 2-ways + merged). 12 key overrides + 2 single-char stem-typo fixes (Q2112 `f(3)−f(2)`→`f(3)+f(2)`; Q2414 `|x+5|`→`|x−5|`) + 1 exclude (Q2438 — options are expressions in x belonging to a different question). Calculus is the most HARD/derivation-heavy folder so far (periodicity-LCM, L'Hôpital, determinant-limit, inverse-trig-derivative-ratio disputes) — the verify-before-override zoom pass confirmed every override against the source. New gotcha banked: a LaTeX `\\[Npt]` row-spacing inside a `cases` env trips `findLatexImbalance` as a false "unclosed `\[`" — strip the `[Npt]` (the `\\` row break renders fine).
  - **Batch 4 (finishes Trigonometry): +Inverse Trigonometry (104 q) +Height & Distance (16 q) → 16 chapters / 1,386 q.** 4 key overrides (all standard inverse-trig identities, book-solution-backed: Q1315→C, Q1357→C, Q1366→A, Q1367→B) + 2 single-char fixes (Q1394 stem restored to cos⁻¹x+cos⁻¹y; Q1403 option-c sign (√3−3)/2 → (3−√3)/2). No exclusions. The Trigonometry book is now fully ingested (all 5 chapters).
  - **Practice frontier: CLOSED.** All 5 source folders (Algebra · Trigonometry · Calculus · 2D Geometry · 3D Geometry) are ingested — 3,040 practice q across 29 NDA Maths chapters, all PUBLIC. Only Mathematical Induction (Algebra) was skipped (no NDA Maths chapter). The `.rar`/PageMaker `.pmd` files were never ingestable. A future supplementary source would follow the same pipeline (render per-column → vision-transcribe via parallel agents → orchestrator zoom-verify every override/exclude → commit PRIVATE → flip PUBLIC).

- **2026-06-12 — NEW corpus axis: practice questions (`question_kind`) + NDA Maths practice ingestion pipeline + /browse PYQ/Practice/All toggle.** The bank gained a second question corpus: **practice** questions (from the "Mathematics for N.D.A and N.A" practice book), kept separate from the PYQ corpus by a new **`question_kind` enum** (`pyq`|`practice`, default `pyq`, migration 0036). **5 NDA Maths topics live PUBLIC (335 q): Sequence & Series 84 + Complex Numbers 82 + Quadratic Equations 62 + Logarithms 26 + Statistics 81** — each mapped onto an **existing** NDA Maths chapter + existing subtopics (standing rule: never create a new chapter for practice; skip a practice section with no NDA Maths home). State + runbook in [[practice-ingestion]] + `scripts/practice/README.md`; reusable ingestion technique in [[practice-pdf-vision-ingestion]].
  - **Why a new enum, not `pyq_year IS NULL`:** the product is PYQ-first ("PYQ Vault") and every PYQ surface (guides/notes/quiz/paper-builder/counts) assumed the corpus was past-year. `pyq_year IS NULL` is fragile (conflates "practice" with "PYQ of unknown year"). An explicit axis lets practice coexist without polluting PYQ counts. User decisions: separation = new enum; extraction = vision-for-everything; pilot = Sequence & Series within Algebra; include solutions; difficulty = vision-estimated; multi-key = stage PRIVATE + flag.
  - **Ingestion pipeline (`scripts/practice/`, mirrors `scripts/jee/`):** the source PDFs are born-digital but **lossy in the text layer** (set/relational operators drop to blank, superscripts collapse, two-column layout), so transcription is **vision-driven** — `render.ts` rasterises pages **per-column at high DPI**, a human/Claude transcribes the PNGs → `data/<topic>.{questions,solutions}.json`, `commit.ts` merges with the **text-layer-parsed answer key** + solutions → `commitStaged` (PRIVATE) → post-commit UPDATE stamps `question_kind='practice'`. The `.rar` files are legacy PageMaker `.pmd` source — not ingestable. Pure helpers (`parseAnswerKey`/`buildRecords`/`findLatexImbalance`) TDD'd (18 tests).
  - **The verification discipline (what made the pilot correct — [[practice-pdf-vision-ingestion]]):** every question cross-checked three ways (image + answer-key file + worked solution + own re-derivation). **Both the book's answer key AND its printed solution can be wrong together** (Q486 keyed+solved "None" but sinA,B,C are exactly in A.P. → option C) — re-derivation is the arbiter, not either source. **MCQ option ORDER is load-bearing** (the key is by-letter, so a swapped option text silently mis-keys — caught on 414/444/445/450 only by 5× zoom-crops). 6 source answer-key errors/blanks corrected via committed `overrides.json` (with reasons; the commit prints them, the preview badges them). `preview.ts` emits a KaTeX-rendered review HTML.
  - **/browse toggle + PYQ-only guards:** added `kind` to `Filters` (default `pyq`, omitted from the URL when `pyq` so existing links are byte-identical), threaded into `queryQuestions` + the facet RPCs (migration 0037 `p_kind`, dropped+recreated to avoid a Postgres overload); a "Question type" segmented control in `FilterBar`. **The downstream-safety design:** tag-gated surfaces (guide principles, notes drills, worked examples, quiz harvest) are PYQ-only *by construction* (practice is never tagged); only the four **raw count-by-taxonomy** queries (`examHomeStats` + the 3 notes landings) needed an explicit `question_kind='pyq'` guard — added so practice can flip PUBLIC later without inflating PYQ-first surfaces. Verified live: Sequence & Series facet counts pyq=89 / practice=84 / all=173.
  - **The `content_hash` re-commit hazard (banked):** `content_hash = sha256(stem + sorted options + answer)`, so fixing a stem/option/answer in `data/` after committing changes the hash → re-running `commit` INSERTS a new row + orphans the old one. Fix: delete the affected rows first, then re-commit (others dedup-skip). Editing only `solution` text is safe (not in the hash). Same hazard family as JEE's resync.
  - **Batch ingestion (3 chapters at a time): +Sets & Relations (84 q) +Permutation & Combination (106 q) +Binomial Theorem (65 q) → 8 topics / 590 q.** Each merges its source sub-sections into one existing NDA Maths chapter (Sets Q1–35 + Relation Q36–86; Permutation Q233–282 + Combination Q283–340; all subtopics already existed — no taxonomy created). Transcription was distributed across **3 parallel sub-agents** (one per chapter) authoring questions + solutions + a key-vs-derivation dispute report; the orchestrator zoom-verified every dispute against the source PDF before committing. **The verify-before-override discipline paid for itself:** of the agents' proposed key changes, one (P&C Q304) was a *misderivation* (the agent read the stem's `=36` as `=10`; C(n,2)=36 ⇒ n=9, so the original key D was right) — caught only by re-rendering the stem. 13 genuine corrections shipped via `overrides.json` (Sets Q50→D, Q65→C; P&C Q270→D/Q296→D/Q299→B/Q319→C wrong-keys + Q255/276/289/291/326 blank-keys; Binomial Q356→B/Q379→B), and 4 genuinely-flawed questions excluded (Sets Q57/Q82 blank-no-content; P&C Q292/Q315 correct-answer-not-an-option). Sets Q60 is a legitimate multi-key (B,C) "is/are true" item.
  - **Scale-up (earlier same session): +Complex Numbers (82 q) +Quadratic Equations (62 q) → 5 topics / 335 q.** Both slot into existing NDA Maths chapters + subtopics (the Quadratic Equations chapter already held 63 PYQs in the same 3 subtopics — no taxonomy created). Recurring per-topic findings worth budgeting for: a 3rd-party practice book ships **genuinely-flawed MCQs** whose correct answer isn't among the printed options — **exclude those** (don't transcribe them; they show as intentional coverage gaps) rather than ship a wrong key (Quadratic Q202: book solution derives `m=(a−b)/(a+b)`, option c is its reciprocal, key file's `d` also wrong; Q213: blank key + impossible condition). And **single-char source typos** in the stem are common — fix only when the keyed option becomes self-consistent (Q212 `2bx`→`3bx` makes the keyed scaling answer correct; Q180/Q193 stems). Re-derivation against the worked solution + zoom-crops on every key/solution/derivation disagreement is what catches these.

- **2026-06-12 — Daily Quiz Waves 3-7: NDA Maths daily quiz COMPLETE — all 30 noted chapters across all themes (~120 quizzes built).** Operational detail + per-chapter status in [QUIZ_FACTORY.md](QUIZ_FACTORY.md); cadence in [[daily-quiz-pilot]]. Waves 5-7 + the binomial-distribution single-add finished the campaign: Wave 5 = binomial-theorem/properties-of-triangle/indefinite-integration/conics (12 quizzes); Wave 6 = inverse-trigonometry/trigonometric-equations/circles/logarithms (14); Wave 7 (finisher) = applications-of-integration/height-distance/binary-numbers (11). **Across all 7 waves every agent-predicted verify key matched on first run** (~50 verify files) — the parallel-author-with-key-prediction pattern is rock-solid. The per-wave detail below is from Waves 3-4; the structure is identical for 5-7. Wave 3 = limits-continuity, permutation-combination, application-of-derivatives, complex-numbers (16 quizzes / 225 q; all 4 needed formula+trap enrichment from scratch). Wave 4 = sets-relations, definite-integration, quadratic-equations, differential-equations (17 quizzes / 251 q; richer — all already had trap callouts, QE coverage fully clean). Each wave ran the standard cadence: harvest → `quiz:coverage` → **4 parallel agents** author computation distractors (re-derivation = the chapter's correctness check) + triaged formula/trap notes-enrichment + 3 verify files each → main agent re-harvests/syncs/verifies/assembles/pushes serially. **All 24 agent-predicted verify keys matched on first run** (incl. def-int's FTC/King's-property pieces that *re-indexed* when their `formula.latex` was enriched) — confirming the parallel-author-with-key-prediction pattern is reliable. Re-derivation caught + fixed 2 notes errors (CN cube-roots `1+ω+ω⁴`→`1+ω⁴+ω⁸`; diff-eq circles-on-x-axis order garbled answer).
  - **The durable lesson ([[quiz-formula-coverage-gap]], sharpened): "technique-heavy → park the formula/trap theme" can be TOO conservative.** Differential Equations' formula (8) + trap (11) first parked below the 12-floor as "technique-heavy." But several first-order-ODE items the triage dismissed as *methods* are genuine **recall** facts taught in the notes prose — exact differentials (`x dy+y dx=d(xy)`, `(x dy−y dx)/y²=d(x/y)`), the **linear-in-x** integrating factor, and the **Bernoulli** substitution `v=y^{1-n}`. Exposing those 5 formulas + 2 traps cleared both to 13 and shipped a formula + trap quiz. **Trigger rule:** when a formula/trap theme PARKS at 8–11 (just under 12), re-examine the triage before accepting the park — a *named identity/substitution taught in prose* IS recall even if it reads like "a method"; only genuinely formula-less procedures (substitute-the-point, log-differentiate, collinearity test) stay skipped. The "when in doubt SKIP / don't pad" rule still holds for true techniques; the refinement is that exact-differential/Bernoulli/linear-in-x style *named results* are not techniques.
  - **Thin-theme park is still correct where the content genuinely isn't there** — sub-12 themes that can't be honestly topped up (QE computation 7 q — QE is formula/trap-heavy with few practiceSet; Sets fact 8; Def-Int formula-3 leftover; binary-numbers formula 8; conics computation 3) stay parked + carry forward to a future cross-chapter "Formulas/Traps of the day" assembler (the documented deferred mechanism). Don't pad those.
  - **The recurring Wave 5-7 lesson ([[quiz-formula-coverage-gap]], sharpened): the harvest's "MCQ-clean" count CONFLATES formula-piece atoms with computation, so computation is systematically over-counted — most chapters actually had near-zero authored `practiceSet`.** Indefinite Integration had 0 practiceSet, Inverse Trig / Trig Equations / Properties / Binomial Theorem ~2 each, Circles 7 — yet the harvest reported 9-27 "MCQ-clean." So when a computation theme looks like it should clear 12 but the assembler says it parks, **count the ACTUAL `sourceKind:"practiceSet"`+`"selfCheck"` atoms** (not the harvest's MCQ-clean figure), and top up by authoring genuine practice problems into the notes concepts that have a worked example but no practice — which also enriches the student notes (real practice + "common mistakes" sections). This + the trap-callout top-ups were the bulk of the Wave 5-7 work. Genuinely computation-heavy chapters (Binary 39, Circles 17) need no top-up; genuinely formula/trap-heavy ones (Conics) leave computation parked.

- **2026-06-12 — NDA Biology /notes: 6 chapters shipped in one parallel batch (Cell Biology · Microbiology & Disease · Biodiversity & Classification · Plant Biology · Reproduction · Ecology & Environment) — completing NDA Biology to 7 of 9 chapters.** Per-chapter one-liners in [ARCHITECTURE.md](ARCHITECTURE.md). **130 q · 81 concepts · 15 static SVG diagrams**, all reorder-only + 100% concept-tag coverage (130/130). Built via the now-standard **6-agent parallel batch + serial merge**: each agent authored only its own `_data/*` + diagram SVGs + 2 page wrappers under a unique slug prefix (`cell-`/`micro-`/`biodiv-`/`plant-`/`repro-`/`eco-`) and returned a structured handoff; the main agent serially merged the 3 shared files (`chapters.ts` 6 imports+entries, the `VisualizationSlug` union +15, `ConceptUnitCard` +15 imports/cases) + one combined 130-row tag INSERT.
  - **All reorder-only — NO Phase-D, NO guide sync.** The deciding check up front: the shipped `/guide/nda-biology` playbooks reference every DB subtopic name verbatim in `subtopics: [...]`, so any rename would silently break the playbook drill CTAs ([[shipped-chapter-rename-downstream-sync]]); the DB subtopics already formed coherent teaching units, so friendly display titles + `subtopicOrder` sufficed across all 6.
  - **Profiles match the guide strands:** the recall chapters (Cell Biology, Microbiology, Biodiversity — all ~0% HARD) lean `reference`-variant (disease↔pathogen↔type tables, kingdom/phylum tables, organelle facts); the Apply chapters (Plant Biology, Reproduction) mix formula-mechanism + reference (photosynthesis light/dark, double-fertilisation 2n+n→3n, tropisms); the Verify chapter (Ecology) mixes both (food-chain 10% law + symbiosis tables). Diagrams stayed **selective** per [[notes-diagram-decision-rule]] — 15 static SVGs only on genuinely spatial concepts (cell structure / membrane / osmosis / photosynthesis flow / flower structure / food-chain pyramid / five-kingdom tree), none on pure recall.
  - **Gates green:** typecheck + notes:lint (0 errors; 1 documented soft-warn — the `cell-organelle-map` reference foundation has no PYQ) + notes:latex OK + notes:order synced 25 subtopics (no name-join drift, confirming reorder-only intact) + a **mistag detector** (group tags by `subtopic_slug`, assert each maps to exactly ONE DB subtopic by name) clean + full prepush PASS. The combined tag INSERT's FK on `question_id` was the safety net for transcribed UUIDs (no FK violation = all 130 real).
  - **No wrong-key flips.** The agents re-derived every featured/drill key during authoring; unlike the NDA Maths/Physics "audit-closed" chapters (which kept surfacing stealth wrong-keys on notes-build re-derivation), the recall-heavy Biology bank held clean — consistent with [[audit-effort-by-question-type]] (fact-recall ≈ 0% wrong-key). One misfiled-but-correctly-keyed item flagged for awareness: `1f453564` (a human male-reproductive-anatomy question filed under the "Angiosperm Reproduction" DB subtopic) — taught honestly under a dedicated concept, no DB change (a future Phase-D reclassify could tidy it).
  - **Follow-up same day — the 2 thin chapters (Genetics & Evolution, Biochemistry, 4 q each) were then built foundation-led at the user's request, completing NDA Biology to ALL 9/9 — the first fully-noted NDA subject.** Initially deferred below the bank-coverage gate, but the user chose to build them as full foundation-led chapters (the topics — DNA/heredity/evolution, biomolecules/respiration/food-spoilage — are genuinely foundational + teachable, unlike Astronomy trivia). Authored **directly (not via agents)** since each is tiny (4 q, all 8 questions + solutions already in hand) and fact-sensitive: Genetics & Evolution = 2 subtopics · 7 concepts (3 formula foundations + 4 PYQ-anchored) · 1 SVG (DNA base-pairing ladder); Biochemistry = 3 subtopics · 6 concepts (2 foundations + 4 PYQ-anchored) · 1 SVG (4 protein-structure levels). **The foundation-exemption is what makes thin chapters viable** — no-PYQ foundations must be `formula`-variant (lint-exempt → no warn); reference concepts must stay bank-anchored (a reference foundation soft-warns). Both reorder-only, mistag-clean, 100% coverage (8/8). So the bank-coverage gate is a *default*, not a hard rule: a thin chapter is buildable when the topic is foundational enough that foundations carry the teaching and the few PYQs anchor where evidence exists.


- _Entries dated **2026-06-11 and earlier** are archived in [DECISIONS_HISTORY.md](DECISIONS_HISTORY.md) (batch 5: 2026-06-01 → 2026-06-11). The Foundations (M1–M3) sub-section above + everything **2026-06-12 onward** stays here; archive older months to keep this active log to ~the last 2 weeks._

## Pending work

The chronological decisions log above captures *why* shipped choices were made; for what's pending, see [`ROADMAP.md`](ROADMAP.md) at the project root. It consolidates every "deferred", "future scope", and "explicitly chose not to build" annotation scattered through this file plus open product questions (e.g., the `principle_tags text[]` column proposal). When an item ships, move its entry into the Decisions log here with a `2026-MM-DD —` prefix and a "Why" sentence so the two files don't drift.

## Cleaned subjects · post-delta sizing

At-a-glance sizing of the cross-subject taxonomy cleanup (chapter + subtopic technique-level canonicals — see Decisions log entries dated 2026-05-13 onward for per-subject narratives). For per-chapter **content-correctness** audit status (orthogonal to taxonomy structure), see [[content-audit-progress]] — **NDA Mathematics fully closed (31 of 31 chapters, all 2,160 q)** as of 2026-05-27, ~22.6 % bank-wide.

**Contract:** numbers below reflect **live counts as of the most recent cleanup pass per subject** (not historical "cleanup-pass-time" snapshots). They match live DB exactly the moment a subject ships, then drift again with the next upload — re-cleanup (Phase D delta) re-syncs them. After any upload batch, treat any subject in this table as stale until SQL-verified or re-cleaned. The live bank-size line at the top of this file is the always-current total. See [[project-docs-staleness]].

**Done — 13 of 13 subjects · 179 ch · 9,546 q · 672 sub** (this is the **cleaned-13-subjects scope, NOT the live DB total** — `npm run stats` reports the live aggregate, currently **258 ch · 934 sub · 10,264 PUBLIC q**; the difference is JEE Mains' ~79 ch / ~263 sub auto-created across Papers 1–10, tracked separately and not part of this cleanup register) (live-verified 2026-06-03; per-row figures below are the per-subject cleanup snapshots and may sum slightly under the live aggregate — SQL-verify before quoting. MHT-CET Chemistry 1,553 → 1,529 → 1,525 on 2026-06-03 — 24 exact-duplicate rows deduped in its solution-cleanup dedup pass, then −1 OF₂ dedup −3 broken rows set PRIVATE in the source-PDF verification pass; NDA Maths re-Phase-D'd 2026-05-17; all 9 other NDA subjects on 2026-05-18 after fresh uploads; MHT-CET Maths re-Phase-D'd 2026-05-23; MHT-CET Physics first cleanup 2026-05-23; MHT-CET Chemistry first cleanup 2026-05-27; NDA Maths Probability re-merged 113 → 109 sub on 2026-05-29 for the notes pedagogy split; MHT-CET Maths Indefinite Integration re-carved 4 → 6 sub on 2026-05-30 for its notes chapter, +2 net → 71 sub; NDA Maths 3D Geometry "Lines and Planes" split into "The Straight Line in 3D" + "The Plane" on 2026-06-02 for its notes chapter, 109 → 110 sub; NDA Physics "Electricity and Magnetism" re-carved 6 → 9 subtopics on 2026-06-02 for its notes chapter, 49 → 52 sub; NDA Maths Sequence & Series re-carved 4 → 5 sub on 2026-06-02 for its notes chapter — split the lumped GP/HP/AM-GM-HM subtopic into 3 + folded Vieta's, 110 → 111 sub; MHT-CET Maths gained a new **Quadratic Equations** chapter on 2026-06-02 (26 → 27 ch · 71 → 72 sub) holding 3 roots questions reclassified out of "Sequences and Series", and one tan⁻¹-in-AP duplicate was deduped from Sequences (1,588 → 1,587 q); NDA Geography "Indian Geography — Physical Features" Phase-D-split on 2026-06-14 for its notes chapter — the "Forests and Natural Vegetation" 34-q catch-all carved up (20 misfiled q → existing subtopics + a new **Location, Extent and Frontiers of India** subtopic), 36 → 37 sub; MHT-CET Maths Differentiation Phase-D-reshaped on 2026-06-16 for its notes chapter — the 3 label-based catch-all subtopics split into 6 pedagogically-clean ones (Foundations/Chain/Differentiability · Logarithmic · Implicit & Special Forms · Inverse Functions & Inverse-Trig · Parametric/Higher-Order/Relations · Derivative-wrt-Another), 72 → 75 sub; MHT-CET Maths Vectors reshaped 2026-06-23 for its notes chapter — the "Magnitude, Components, Projection, and Unit Vectors" catch-all renamed "Magnitude, Components, and Unit Vectors" + 14 q moved to Dot/Cross/STP/Linear (rename + question moves only, no sub-count change — still 75 sub)):

| Subject | Chapters | Questions | Subtopics |
|---|---|---|---|
| NDA Mathematics | 31 | 2,160 | 111 |
| MHT-CET Mathematics | 27 | 1,784 | 75 |
| MHT-CET Physics | 24 | 1,771 | 91 |
| MHT-CET Chemistry | 29 | 1,739 | 121 |
| NDA English (GAT) | 8 | 900 | 29 |
| NDA Physics | 14 | 449 | 52 |
| NDA Chemistry | 12 | 262 | 47 |
| NDA Biology | 9 | 190 | 39 |
| NDA Geography | 7 | 345 | 37 |
| NDA History | 4 | 260 | 22 |
| NDA Polity | 4 | 90 | 14 |
| NDA Economics | 1 | 24 | 3 |
| NDA Current Affairs | 8 | 180 | 31 |

**No subjects pending.** Bank-wide subtopic taxonomy is now technique-canonical end-to-end. The post-upload ritual still applies — fresh uploads can re-fragment subtopics, requiring Phase D delta cleanup; see [[post-upload-ritual]].

Workflow: inline chapter-by-chapter in chat (no scripts), descending q-count first, source-source collision-loop for bulk merges, per-question reclassification for catch-all chapters ≤~50 q. Patterns + SQL templates: `[[reclassification-sql-pattern]]` + `[[taxonomy-inline-iteration]]`.


## Notes editorial workflow

The full step-by-step workflow for authoring a `/notes` chapter lives in **[NOTES_WORKFLOW.md](NOTES_WORKFLOW.md)** — **read it before building one.** It covers: Step 0 pedagogy-first structure (reshape the bank to the teaching arc; read HARD+MODERATE solutions; MHT-CET-Maths defaults) · Step 1 the `ConceptUnit` formula/reference discriminated union + the diagram decision rule + concept-content alignment · Step 1b author-for-clean-quiz-harvest · Step 2 the `NOTES_CHAPTERS` registry · Step 3 the chat tagging session (set-sibling co-location, 100% coverage) · Step 4 the gate chain (`notes:lint`/`notes:latex`/`notes:coverage`/`quiz:coverage`/`prepush`) · Step 5 ship + `npm run stats` + `notes:order` · the invariants to verify before commit.

## After a new upload

When a new question-batch lands, run the 6-step ritual in order. The upload pipeline is auto (taxonomy auto-creates, `content_hash` dedup, default PUBLIC visibility, live counts in `/browse` + dashboard + `/guide` TOP_11 + `/notes` drills). What's NOT auto and needs this ritual: **tag coverage, taxonomy fragmentation cleanup, and TS-static editorial copy**.

1. **Sanity-check the batch** (always) — confirm `inserted` vs PUBLIC/PRIVATE counts, PYQ metadata stamped, no missing years
2. **Taxonomy delta cleanup** (if ≥5 new subtopic auto-creates or a new chapter name) — Phase D delta inline; don't restart from scratch. Precedent: 2026-05-17 NDA Maths re-Phase-D (~770 new auto-creates closed in one session)
3. **Principle-tag delta survey** (if new questions in TOP_11 chapter footprints) — incremental per-new-chapter, not whole-bank
4. **Concept-tag delta session** (if new questions in `/notes`-covered subtopics) — also revisit dropped-gap ConceptUnits; bank growth can mature what was previously dropped
4b. **Notes-coverage check** (if new questions in a `/notes`-covered chapter) — `npm run notes:coverage <subjectRoute> <chapterSlug>` and triage the macro/technique channel. New uploads are how a *technique gap* drifts in (a fresh question using a method the notes don't teach); this is the routine catch for it. Add a concept for any real gap.
5. **Editorial copy refresh** (if counts move ≥5–10%) — `whyItMatters`, OVERVIEW constants, CHAPTER_TABLE, strategy %HARD, trends callouts. Mechanical TS edits
6. **Integrity check** (always) — `npm run notes:lint` + `npm run prepush` before commit

Steps 2–5 (incl. 4b) are conditional and can be deferred / queued across sessions. Full procedure + SQL probes + skip-conditions: [[post-upload-ritual]].

## Adding a new RLS-protected table

Follow the `questions`/`options` pattern (see `0002_rls.sql` + `0004_upload_staging.sql`):
1. Enable RLS.
2. Read policy: `org_id = private.current_user_org_id()`.
3. Write policy: same plus `private.current_user_is_admin()` if admin-curated.
4. Add a test in `tests/upload-rls.test.ts` (or new equivalent) proving a TEACHER cannot write and a member of another org cannot read.
5. Run `mcp__supabase__get_advisors` after applying the migration.

## Live infra

- **Supabase project:** `wunvtnqlzjrkvolslbnm` (https://wunvtnqlzjrkvolslbnm.supabase.co)
- **Production:** **https://www.pyqvault.com** (custom domain, live 2026-06-04; apex `pyqvault.com` 308-redirects to `www`) — auto-deploys from `main`. The old `question-bank-sage.vercel.app` still resolves (optionally set to redirect → `www.pyqvault.com` in Vercel → Domains). Canonical URL in code is `https://www.pyqvault.com` (the 4 `SITE_URL` consts: layout/sitemap/robots/GuideJsonLd). DNS at GoDaddy: `A @ → 216.198.79.1` + `CNAME www → <unique>.vercel-dns-NNN.com`. **Vercel plan is Hobby** — fine for the free launch, but move to **Pro before Razorpay goes live** (taking payments = commercial use).
- **Repo:** https://github.com/lwspune/question-bank (public; Vercel project is named `question-bank`)
- **Auth callback URLs allow-listed in Supabase Auth (Authentication → URL Configuration):** `https://www.pyqvault.com/api/auth/callback` + `https://pyqvault.com/api/auth/callback` + the legacy `question-bank-sage.vercel.app` + `localhost:3000`. Site URL = `https://www.pyqvault.com`. (Adding the new domain here is mandatory or OAuth/email sign-in breaks on it.)


## Operations

Day-to-day runbook lives in **[OPERATIONS.md](OPERATIONS.md)**: monitoring (Vercel/Supabase logs, advisors) · when to upgrade tiers · rate-limit visibility · rotating `SYNC_SHARED_SECRET` · Razorpay env + go-live · managing members (admins/teachers) · flipping question visibility · setting PYQ year/month/comment for a batch · deleting a whole upload.

