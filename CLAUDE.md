# PYQ Vault — project notes

**Brand:** the product is **PYQ Vault**, live at **https://www.pyqvault.com** (rebranded from "Question Bank" 2026-06-04 — the repo/folder/Vercel-project are still named `question-bank`; in code the brand is `SITE_NAME` in `layout.tsx`). Lowercase "question bank" in prose still means the question corpus, not the brand.

Free, public past-year-question paper builder for Indian entrance exams. Anyone can filter and preview the public bank by exam/chapter/subtopic/difficulty/PYQ year; **downloading the Question Paper + Answer Key as Word files requires a TEACHER account** (org member; the tagged nda-tracker `.xlsx` sheet is likewise staff-only) — a downloadable Word paper is a teacher artifact, so students get the online product (preview, timed mocks, notes) and a non-staff visitor sees a **"request teacher access"** CTA → `/request-access` (lead capture; superadmin triages at `/superadmin`, migration 0060). Anon browse/preview stays fully open so SEO is untouched (server-enforced `resolveExportAccess`; **teacher-gated 2026-07-18** — was "any signed-in account" from 2026-07-11; see the Decisions entries + [[signup-gate-placement]] + [[project-teacher-access-gate]]). Admins (org members) upload Excel batches privately, then flip individual questions PUBLIC. Sibling app `MHT_CET_AI` posts finalized mocks to `/api/sync/mock` for cross-app dedup. Thirteen exams are live, plus one ingested-but-private. **Flags below mirror `EXAM_REGISTRY` in [examContext.ts](src/lib/exam/examContext.ts), which is the source of truth** — `practiceOnly` (this exam has no PYQs and never will), `board` (gets the `/board` reader + Board nav), `mocks` (backs timed `/mock` sittings), `mixed` (carries non-MCQ formats, so `/browse` renders the format filter). **For question counts run `npm run stats`** — any number written in prose, here or in CORPUS.md, is a point-in-time note that lags the bank. Per-exam ingestion narrative, defect classes and source traps: **[CORPUS.md](CORPUS.md)**.

| Exam (slug) | Source | Flags | Pipeline | Detail |
|---|---|---|---|---|
| NDA (`nda`) | UPSC NDA papers — Paper I Mathematics, Paper II GAT (English + 8 GK subjects) | mocks | `scripts/nda-pyq/` · `scripts/nda-gat/` | [[nda-pyq-ingestion]] · [[nda-gat-ingestion]] |
| MHT-CET (`mht-cet`) | MHT-CET PCM papers. **Zero negative marking** — the only such exam here | mocks | `scripts/mhtcet/` | [[mhtcet-april-2025-ingestion]] |
| JEE Mains (`jee-mains`) | All shifts 2021-2026; Maths, Physics and Chemistry all complete. Carries Section-B numeric answers | mocks · mixed | `scripts/jee/` | [[jee-mains-ingestion]] · [[jee-practice-ingestion]] |
| CDS (`cds`) | CDS English, General Knowledge, Elementary Mathematics. **No official key is published** — answers are derived | mocks | `scripts/cds/` · `scripts/cds-gs/` · `scripts/cds-maths/` | [[cds-english-ingestion]] · [[cds-gs-ingestion]] |
| Foundation Course (`foundation-course`) | LWS "NDA Foundation" Class 9/10 NCERT worksheets — the worksheets ARE its bank | practiceOnly | `scripts/foundation/` | [[foundation-course]] |
| NEET (`neet`) | NEET (UG) papers with official keys + worked solutions; two layouts (180 q from 2025, 200 q before) | mocks | `scripts/neet/` | [[neet-ingestion]] |
| Maharashtra HSC Class 12 (`mh-hsc-12`) | Balbharati Std XII textbooks **and** board PYQs, on the same chapters | board · mixed | `scripts/stateboard/` · `scripts/mh-hsc-12-pyq/` | [[state-board-ingestion]] · [[mh-hsc-12-pyq-ingestion]] · [[state-board-physics-ingestion]] |
| CBSE Class 11 (`cbse-11`) | NCERT Class 11 Maths, Physics, Chemistry. Not a board year, so no PYQs ever | practiceOnly · board · mixed | `scripts/ncert/` | [[ncert-class-11-ingestion]] · [[ncert-physics-ingestion]] · [[ncert-chemistry-ingestion]] |
| CBSE Class 12 (`cbse-12`) | NCERT Class 12 textbooks **and** CBSE board PYQs, on the same chapters | board · mixed | `scripts/ncert/` · `scripts/cbse-12-pyq/` | [[ncert-ingestion]] · [[cbse-12-board-pyq-ingestion]] |
| MH State Board Class 9 (`mh-sb-9`) | Balbharati Class 9 — Maths, Geography, History, Political Science | practiceOnly · board · mixed | `scripts/mh-sb-9/` | [[mh-sb-9-ingestion]] |
| MH State Board Class 11 (`mh-sb-11`) | Balbharati Class 11 Mathematics | practiceOnly · board · mixed | `scripts/mh-sb-11/` | [[mh-sb-11-ingestion]] |
| MH State Board Class 10 (`mh-ssc-10`) | SSC board past papers **and** Balbharati Class 10 textbooks. The first real board-PYQ corpus | board · mixed | `scripts/mh-ssc-10/` · `scripts/mh-ssc-10-text/` | [[mh-ssc-10-ingestion]] |
| Worksheets 11+12 (`worksheets-11-12`) | LWS Cadetprep NDA-Maths worksheet course. Its source keys are AI-generated and ~5% wrong, so every chapter is blind-re-derived | practiceOnly | `scripts/worksheets/` | [[worksheets-ingestion]] |

**Deliberately NOT in the registry: UPSC Civil Services (Prelims)** — ingested and **entirely PRIVATE**, so nothing is student-visible. It is the one corpus here with a *published official key*, which makes it the only place blind-derivation accuracy can be measured rather than asserted. `scripts/upsc/` · [[upsc-cse-ingestion]]. IPMAT and CUET are on the roadmap.

A separate `question_kind` axis (`pyq` | `practice`) splits the past-year corpus from the supplementary practice bank *within* an exam — see the Multi-tenancy section below.

**The end-of-book answer-key cross-check is now a mandatory workflow GATE (step 6 in `scripts/stateboard/README.md`)**: every chapter is diffed against the Balbharati ANSWERS section, our authoring errors fixed + book-key errors flagged with `[Textbook…]` brackets → aggregated by `errata.ts` (**151 items across 12 chapters** — the Balbharati key is wrong ~4× as often as our authored answers; Ch.3 Indefinite Integration alone added 28 answer-key errors, the dirtiest single-chapter key yet). **The cross-check's trustworthiness rests on a THIRD independent ground truth** (mpmath/sympy differentiation of the actual stem, testing BOTH our answer and the book's against it — comparing the two to each other lets a shared error hide), and on the rule that **`simplify()` returning non-zero is NOT evidence of inequality** (constraint-curve identities) — extended 2026-07-16: **a sympy `solve`/`solveset` MISS is not evidence either** (on App-of-Derivatives it returned no roots in (0,2π) for a trig row and dropped 5π/4 on another — principal-branch artifacts that would have manufactured 2 bogus OUR-ANSWER-WRONG findings; test the claimed value directly + scan numerically. A CAS returning "nothing" is a question, never a verdict); ~20 apparent disagreements per chapter dissolve on equivalence testing. Subjective answers authored via parallel agents → `apply-solutions.ts` (hash-safe); `scripts/stateboard/` hybrid text+vision pipeline — see [[state-board-ingestion]].

**Live bank size — run `npm run stats` for the live figures; this prose lags by several ingests.** Most recent measurement: **70,079 PUBLIC across all exams (2026-09-09)** across 13 exams — NDA 13,520 · JEE Mains 10,614 · Worksheets 7,376 · MHT-CET 6,634 · CDS 6,556 · MH State Board Class 11 4,216 · MH HSC Class 12 4,193 · CBSE Class 12 3,906 · NEET 3,308 · MH State Board Class 10 3,285 · Foundation 2,852 · CBSE Class 11 2,147 · MH State Board Class 9 1,472 (883 chapters / 6,793 subtopics / 72,740 rows all-visibilities). Note CDS is no longer English + GK alone — it now spans 10 subjects. The per-exam breakdown that follows is a **2026-06-30 snapshot covering only 4 exams and is badly stale — do not quote it**; it is kept because its per-subject splits and the dedup/wrong-key narratives beneath it are still the only record of those events. As of **2026-06-30**: **18,467 PUBLIC** (pyq + practice, across 4 exams) — **NDA 8,259** (Mathematics 5,248, English 1,077, Physics 478, Geography 419, Chemistry 294, History 260, Biology 189, Current Affairs 180, Polity 90, Economics 24) + **MHT-CET 6,638** (Maths 2,231, Physics 2,221, Chemistry 2,186) + **JEE Mains** (that 2026-06-30 snapshot read 771; the corpus has since grown ~10× — as of 2026-08-06 it is **10,614 PUBLIC** = Maths 3,677 · Physics 3,482 · Chemistry 3,455, all three subjects COMPLETE — see [[jee-mains-ingestion]]) + **Foundation Course 2,852** (Chemistry 1,014, Physics 991, Biology 847 — practice-only, no PYQ corpus; see [[foundation-course]]). **MHT-CET grew +588 on 2026-06-26 from the 2025 April PYQ ingest** (4 PCM shifts — 19 Apr S1/S2 + 20 Apr S1/S2 — Physics 197, Maths 197, Chemistry 194 PUBLIC + 4 flawed PRIVATE; `pyq_month='April'`; via the new `scripts/mhtcet/` pandoc pipeline + 5 wrong-AK-key corrections; see the Decisions log + [[mhtcet-april-2025-ingestion]]). **MHT-CET grew a further +598 on 2026-06-28 from the next four 2025 April shifts** (21 Apr S1/S2 + 22 Apr S1/S2 — Physics 200, Maths 200, Chemistry 198 PUBLIC, 0 flawed, 2 exact cross-shift dupes deduped; `pyq_month='April'`; same `scripts/mhtcet/` pipeline + **8 more wrong-AK-key corrections** each contradicting the AK's own worked solution; see the Decisions log + [[mhtcet-april-2025-ingestion]]). **MHT-CET grew a further +748 on 2026-06-30 from the next five 2025 April shifts** (23 Apr S1 + 25/26 Apr S1/S2 — Physics ~250, Maths ~249, Chemistry ~249 PUBLIC, 1 flawed PRIVATE, 1 cross-shift dupe deduped; `pyq_month='April'`; same `scripts/mhtcet/` pipeline + **9 more wrong-AK-key corrections** + a durable `cleanupArtifacts` glued-trig de-glue; see the Decisions log + [[mhtcet-april-2025-ingestion]]). **The per-exam NDA/MHT-CET totals now BUNDLE the practice corpus on top of the PYQ corpus** (e.g. NDA Mathematics = 2,160 PYQ + ~3,088 practice; NDA English = 900 PYQ + ~177 practice; MHT-CET Chemistry = 1,525 + 194 April-2025 PYQ + 20 practice). The **PYQ-only formally-audited corpus is 9,546 q** — the scope of the content-audit + guides/notes (the audit-status line above); the +588 April-2025 PYQs are derivation-verified at ingest but are NOT part of that 9,546 formal-audit subset. (MHT-CET Chemistry PYQ dropped 1,553 → 1,529 → **1,525** on 2026-06-03 — first 24 exact-duplicate rows deduped, then during the source-PDF verification pass: 1 OF₂ conflicting-key duplicate deleted + 3 unreconstructable broken-extraction rows set PRIVATE; 6 image-naming rows + 5 conflicting-key pairs remain deferred. MHT-CET Physics dropped 1,577 → 1,574 on 2026-06-02 — 3 cross-shift duplicate rows deduped during its solution-cleanup. MHT-CET Maths dropped 1,588 → 1,587 on 2026-06-02 — one tan⁻¹-in-AP duplicate deduped during the Sequences & Series bank-hygiene fix, which also spun the 3 misfiled roots questions out into a new **Quadratic Equations** chapter; MHT-CET Maths is now **27 chapters**.)

**Content audit status (2026-06-03):** **BANK-WIDE CLOSED** for the original 13 subjects audited at the no-source-needed surface (**9,546 / 9,546 q, ~100%**): NDA bank-wide (4,860) + MHT-CET Maths (1,587) + Physics (1,574) + Chemistry (1,525). (The ~3,715 ingested JEE Mains Maths q are NOT in this formal-audit scope: safe-key papers carry source keys + AI-authored solutions; every broken- or absent-key sitting was fully BLIND-solved at ingest (derived answer + solution, source key ignored — the blind solvers repeatedly confirmed the source NAT keys were positionally shifted and many MCQ keys wrong). That is a good-faith per-question correctness pass, but not the formal content audit. ~1-4 genuinely corrupted questions per broken paper were skipped rather than shipped with a guessed answer. See [[jee-mains-ingestion]].) MHT-CET Chemistry closed 2026-06-03 across three passes (subject-wide content probes → 8 numeric chapters HARD+MODERATE stealth-key sweep → 21 recall-chapter deep-reads) + a **source-PDF verification pass** off the local MHT-CET DOCX papers — **31 wrong-key flips** (HARD numeric ran ~22%; recall ~0.5–1%, errors only in reaction/IUPAC-naming, never pure fact-recall), 24 exact-dupes deleted, 5 bank-extraction fixes, 12 printed-paper-defect preserve-notes, 3 broken rows → PRIVATE. **~235 wrong-key flips bank-wide** (MHT-CET Physics 22, Chemistry 31, NDA Maths ~119 — incl. +3 via the 2026-06-28 LWS Maths Mock 3 ingest of Vectors `3cbe4d57`/`6741a9d9`/`87f87b05`, and +2 via the 2026-08-05 backfill-ledger pass (`771d677e` det≠0 ⇒ unique-solution B→A, `59e05e11` cos 2x period B→C) — and the 28 stealth flips found *after* the 2026-05-27 "closure": 3 via the Indefinite Integration notes build + 2 via the Binomial Distribution notes build + **21 via the 2026-06-04 /solution-cleanup sweep of the 28 other "closed" chapters**, which exposed that the 2026-05-27 disagree-probe was case-sensitive and missed lowercase/`REVIEW:`-hedged self-disagreements; + **2 via the 2026-06-09 source-verified review of 5 questions flagged during the 5-chapter notes build** (flips `ec2987e3` Conics directrix + `9e5b991b` Trig-Eqn 4th-quadrant; plus 2 stem-extraction fixes `bbddd3e0`/`88fd7bc8`, keys already right); the rest spread thin). Preserved paper defects: 6 clean (5 NDA + 1 MHT-CET Maths) + ~10 MHT-CET Physics + 12 MHT-CET Chemistry + **2 NDA Geography** (`2604a840` chert/geyserite/shale flawed-question + `8f34b21d` Assam-borders, both source-verified against the local GAT 2025 NDA-2 PDF 2026-06-14 during the Geography notes build — the latter's bank solution falsely claimed Assam borders China, corrected to 2 countries with official key C preserved) honest-note rows. **Still open (all need source PDFs/figures, not derivation):** ~6 MHT-CET Chemistry image-naming rows + 5 user-deferred conflicting-key dup-pairs + 32 same-stem/diff-option Chemistry groups. Key lesson from this frontier: a probe only catches *self-contradicting* solutions — most flips were **stealth wrong-keys** caught by fresh re-derivation, and several were **sibling-confirmed** (a parallel question carried the correct key); see [[audit-probe-symmetry]] + [[audit-effort-by-question-type]]. Per-subject register in [[content-audit-progress]]; per-row narratives in the Decisions log below.

**All 13 subjects fully cleaned** at chapter + subtopic level — bank-wide cleanup pass closed. (The cleanup-pass snapshot numbers below are point-in-time; subjects have grown via new uploads since their original cleanup; NDA Maths was re-Phase-D'd on 2026-05-17; all 9 other NDA subjects (English + Physics + Chemistry + Biology + Geography + History + Polity + Economics + Current Affairs) on 2026-05-18 to fold in fresh auto-creates; MHT-CET Maths re-Phase-D'd on 2026-05-23 after fresh uploads grew it 795 → 1,588 q; MHT-CET Physics fully cleaned for the first time 2026-05-23 at 1,577 q; MHT-CET Chemistry fully cleaned for the first time 2026-05-27 at 1,553 q — closes the bank-wide cleanup.) **11 strategy guides shipped — 10 NDA + MHT-CET Mathematics** at /guide/nda-{maths,english,physics,chemistry,biology,geography,history,polity,economics,current-affairs} and /guide/mht-cet-maths (**Template C, tier-style strands, 2026-08-22** — the first non-NDA guide; `/guide` is now a registry-derived exam picker rather than a redirect, and its strategy axis is ORDER-and-TIME because MHT-CET has NO negative marking) (economics + current-affairs are thin single-page landings — economics because the bank's that thin at 24 q · 1 ch, current-affairs because the content half-life is too short for a multi-route fact-memorise guide; CA's single page is **Template D — theme-prep-checklist**, the first guide where the strand axis is recurrence-strength rather than skill or strategic priority). **77 chapters of self-sufficient /notes content across 6 subjects** (verify counts with `npm run stats`, NOT this prose — the un-noted-chapter selection MUST come from the `NOTES_CHAPTERS` registry / `npm run stats`, never this list, which lagged 4 chapters behind and caused a 2026-06-09 mis-pick) (NDA Maths Statistics + Vectors + Probability + **3D Geometry** + **Matrices & Determinants** + **Sequence & Series** + **Indefinite Integration** + **Binomial Distribution** + **Functions** + **Differentiation** + **Trigonometric Identities** + **Limits & Continuity** + **Application of Derivatives** + **Lines** + **Permutation & Combination** + **Complex Numbers** + **Sets & Relations** + **Definite Integration** + **Differential Equations** + **Quadratic Equations** + **Binomial Theorem** + **Properties of Triangle** + **Conics** + **Inverse Trigonometry** + **Trigonometric Equations** + **Circles** + **Logarithms** + **Applications of Integration** + **Height & Distance** + **Binary Numbers** + **NDA Physics Sound** + **Electricity & Magnetism** + **Light & Optics** + **Laws of Motion** + **Heat & Thermodynamics** + **Modern Physics** + **Kinematics** + **Fluid Mechanics** + **Work, Energy & Power** + **Gravitation** + **Units, Measurement & Dimensions** + **Oscillations & Waves** (NDA Physics now 12 of 14 chapters — the 7 added 2026-06-10 + the 3 borderline added 2026-06-11: Gravitation 17 q · Units & Measurement 14 q · Oscillations & Waves 13 q = 29 concepts · 44 q · 7 diagrams, all reorder-only, quiz-ready per Step 1b; only Astronomy 4 q + Energy Sources 2 q remain, below the bank-coverage gate) + **MHT-CET Maths Indefinite Integration** + **MHT-CET Maths Differentiation** + **MHT-CET Maths Vectors** + **MHT-CET Maths Line and Plane** + **MHT-CET Maths Applications of Derivative** + **MHT-CET Maths Differential Equations** + **MHT-CET Maths Probability Distribution** + **MHT-CET Maths Binomial Distribution** + **NDA Biology** (ALL 9 of 9 chapters — Human Physiology · Cell Biology · Microbiology & Disease · Biodiversity & Classification · Plant Biology · Reproduction · Ecology & Environment + the 2 thin chapters **Genetics & Evolution** + **Biochemistry** added 2026-06-12 foundation-led; 6 of the 8 new chapters via a 6-agent parallel batch, the 2 thin ones authored directly · all reorder-only · 94 concepts · 138 q · 17 static SVGs — first FULLY-noted NDA subject) + **NDA Chemistry** (11 of 12 chapters — all but Practical: Carbon · Atomic Structure · Acids, Bases & Salts · Matter & Its States · Chemical Reactions · Industrial Chemistry · Mole Concept · Metals & Non-Metals · Hydrogen & Water · Chemical Bonding · Chemistry in Everyday Life — first nda-chemistry notes subject, shipped 2026-06-10: ~125 concepts · 259 q · recall→reference profile · all reorder-only · quiz-ready per Step 1b · 12 diagrams (allotrope structures · Bohr shells · periodic trends · states of matter · soap micelle · functional-group structures · pH scale strip · distillation apparatus · Tyndall/colloid · ionic-vs-covalent formation · water H-bonding · reaction-type shapes — all static SVGs on spatial-answer concepts)) + **NDA Geography** (ALL 7 of 7 chapters — Earth's Structure · Indian Geography Economy · Indian Geography Physical · Climatology · World & Human Geography · Earth in Space · Oceanography — **2nd FULLY-noted NDA subject**, shipped 2026-06-13/14: Earth's Structure built solo then the other 6 via a **6-agent parallel batch + serial merge**; ~142 concepts · 345 PYQs · all reorder-only · **24 static SVGs** (geomorphology cross-sections, plate boundaries, seismic waves, atmosphere layers, lat/long grid, ocean-floor profile, etc.); recall-heavy → reference-variant the workhorse. The Physical "Forests and Natural Vegetation" 34-q catch-all was then **Phase-D-split** 2026-06-14 — 20 misfiled q carved out (12→Rivers, 3→Soils/Climate, 5→a NEW **Location, Extent and Frontiers of India** subtopic), leaving Forests at its 14 genuine forest q)) — foundations → worked → self-check → Level 1 practice → per-subtopic mastery checkpoint, plus per-chapter interactive visualizations + a collapsible chapter revision sheet; rendered by registry-driven shared page components. **Notes navigation is per-exam (2026-06-08):** `/notes` is a cross-exam index and `/notes/<examSlug>` (nda·mht-cet·jee-mains) is a per-exam hub listing that exam's notes subjects — both derived from the `NOTES_CHAPTERS` registry via `lib/notes/notesNav.ts`, so a new subject's notes surface in nav automatically (a new subject still needs its hand-written `/notes/<subjectRoute>/page.tsx` — see [[notes-self-sufficient-template]] + the 2026-06-08 Decisions log). **Per-chapter ship details are NOT duplicated in this header** — the one-line "what" per chapter (subtopic/concept counts, Phase-D reshape vs reorder-only vs as-is, diagram choices, slug prefix) lives in the file-layout `notes/` list in [ARCHITECTURE.md](ARCHITECTURE.md); the full "why" narrative per chapter is in the Decisions log. Append a new chapter's one-liner to that ARCHITECTURE.md list, not here. As of 2026-05-30 the `ConceptUnit` type is a **discriminated union** (`kind: "formula" | "reference"`) — formula variant carries an authored worked example, reference variant carries a flat `ReferenceTable` in the same slot — letting Recall-strand chapters teach via named-fact lookups (Sound is the first user; 7 of its 13 concepts are formula variants, 6 are reference variants). Definitions render `**bold**` key terms + `- ` bullet lists (via `RichText`, 2026-05-29); featured-PYQ + checkpoint cards now show the set-bound passage **context** and a `[Q# · month · year]` provenance citation. **Tier 1 IA + cross-linking shipped 2026-05-18** — primary nav (Bank · Guides · Notes) + a cookie-backed exam pill in AppHeader (**the pill was REMOVED 2026-08-21** — `qb_exam` survives, written only by /welcome + /account; see the Decisions entry); per-question backlink chips on /browse (chapter→playbook + subtopic→notes + principle/concept tag overrides); `/nda` exam home; two-way notes ↔ guide cross-links; click-to-reveal answer; in-app **question reports** (migration 0024) with admin triage at /dashboard/reports.

**Premium paywall shipped (2026-06-01):** self-serve student signup (`/signup`, Google + email/password) · per-user `entitlements` (orthogonal to orgs, migration 0026) · comp-access admin UI (`/dashboard/entitlements`) · `/notes` preview-gate (`tier:"paid"` chapters — all currently free, machinery dormant) · Razorpay ₹999/365 one-time checkout (`/pricing`, `/api/billing/*`). **Razorpay not yet activated** — checkout 503s until the 4 env keys are set. See the 2026-06-01 Decisions log + [[project-paywall-build]].

**Public lead-magnet quiz funnel shipped (2026-06-09):** cold traffic takes a public `/quiz/[slug]` (assembled-quiz, ISR + per-quiz OG card) → name+mobile+consent gate at the reward → server-side grade → captured as a `quiz_leads` row (mobile = the lead, never an account; migration 0033/0034) → results show verdicts + `/notes` deep-links + a `billingLive`-gated premium CTA. Admin "Publish to public" on `/dashboard/quizzes` sets `public_slug`; leads roll up by mobile at `/dashboard/leads`; `/privacy` for DPDP consent; a "Test yourself" CTA on `/notes`. **Dormant until an admin Publishes a quiz. Known-visitor gate suppression (2026-07-22):** signed-in students **skip the sheet entirely** (grade-only authed branch of the submit POST, no lead written); anon same-number returners get **one-tap sticky consent** — `resolveQuizGate` in `src/lib/quiz/gate.ts`. See the 2026-06-09 + 2026-07-22 Decisions log + [[project-public-quiz-funnel]].

**Board textbook-solutions reader (`/board`) shipped (2026-07-06):** a book-faithful, exercise-by-exercise reader for school-board textbook exams (State Board + CBSE Class 12 today) — each chapter renders in **physical book order** (§ Solved Examples → Exercise → Miscellaneous → future PYQs), a distinct **book-structure axis** (`section_kind` enum `solved_example|exercise|miscellaneous` + `section_group`/`section_label`/`section_seq`, migration 0043) that is ORTHOGONAL to the conceptual `subtopic` used by `/browse`+`/notes` — a single book Exercise is deliberately split across subtopics, so the reader CANNOT group by subtopic without shattering it. Per-chapter **PDF-verified** outlines in `scripts/stateboard/sections.ts` → `assignSections` (pure, TDD) → `backfill-sections.ts` → `npm run board:lint` (gate: section completeness + `section_seq` contiguity per chapter). Routes `/board` → `/board/[examSlug]` → `/board/[examSlug]/[subjectRoute]/[chapterSlug]`; data layer `src/lib/board/query.ts`, client reader `src/app/board/BoardReader.tsx` (collapsible section/block accordion, attempt-first per-question reveal, `breakSentences` sentence-per-line, answers rendered via `BlockText`). Always-visible **Board** nav tab (like Notes; `boardExam` flag on the exam registry). A new chapter appears automatically once its rows carry `section_seq` — the surface is data-driven, no per-chapter code. See the 2026-07-06 Decisions log + [[board-reader]].

**Timed PYQ mock tests (`/mock`) shipped (2026-07-10; NEET added 2026-07-17):** real past papers served whole as **timed, auto-graded, online** tests — NOT blueprint-sampled mixes ("use PYQPs as is"). A mock = the exact PYQs of one sitting, reconstructed from the bank, delivered NATIVE on pyqvault.com. **193 mocks PUBLIC — 188 PYQ + 5 PRACTICE** (the first non-PYQ mocks, 2026-09-07: five 120-question NDA Paper I papers assembled from the bank, `source='practice'`/`scope='full'` per migration 0088 — see that day's (fifth) Decisions entry; JEE Mains added 24 on 2026-09-07; CDS grew to 55 across 3 papers when GK + Elementary Mathematics shipped 2026-09-06; the count below was left at 123 by that ship): 19 NDA Paper I Mathematics (120 q · 300 marks · +2.5/−0.83; NDA II 2026 added 2026-09-14) + 18 NDA Paper II GAT (150 q · 600 marks · +4/−1.33; English 50 + General-Knowledge 100 across 8 subjects), every NDA sitting 2017→2026, 150 min each; **+ 19 CDS English** (2017-I … 2026-I, every sitting; 120 q · **100 marks** · 120 min · +0.8333/−0.2778 — the first FRACTIONAL marking in the bank, which is why `gradeMock`+`totalMarks` now round `maxScore`; ONE section, since CDS English is one bank subject and the paper's own ~13 Directions-based sections vary by year and live in each question's `context`; `pyq_month` is NULL on all 2,280 rows and two sittings share a year, so it uses the NEET-style `source_file` registry with an edition-aware slug `cds-<year>-<i|ii>-english` — derived from `scripts/cds/config.ts` PAPERS rather than hand-written. See the 2026-08-25 Decisions entry); **+ 8 NEET (UG)** (2021·2022·2023·2024·Re-2024·2025·2026·Re-2026 — Physics/Chemistry/Biology(=Botany+Zoology), +4/−1; 180 q/180 min for 2025+, 200 q/200 min pre-2025; see the 2026-07-17 Decisions entry); **+ 64 MHT-CET** (2026-09-01, merged 2026-09-06 — the PCM group's TWO papers per sitting: **Paper I Mathematics** 50 q · 100 marks · +2 and **Paper II Physics & Chemistry** 50+50 q · 100 marks · +1, both 90 min. **ZERO negative marking — the first such exam in the bank**, which is why `src/lib/mocks/marking.ts` exists: the instructions screen hard-coded "(negative marking)" and "skip if unsure", the second of which COSTS MARKS when a blank scores the same as a wrong answer, and contradicted our own MHT-CET guide. Sittings are `source_file`-keyed because `pyq_month` is not merely null but ACTIVELY MISLEADING — 17 of the 45 share (2023, "May"), so the year+month loop would emit one slug for all 17 and silently overwrite 16 real papers; the registry (`scripts/mocks/mhtcetSittings.ts`) is HALF-DERIVED from `scripts/mhtcet/config.ts` (the 13 April-2025 shifts) and half hand-written (the other 32 have no config anywhere in the repo). **64 of a possible 84 ship: 20 papers are HELD** because a mock is the real paper or it is nothing — 10 short because a flawed question is deliberately PRIVATE (irrecoverable), 20 because rows are absent from the bank (closable by a future ingest). A `hold` is an ASSERTION, not a mute: the builder still attempts the paper and reports a hold whose paper now reconstructs, so closing a corpus hole surfaces as a prompt rather than silence. **The bank has 45 source_file labels but only 42 REAL sittings — three papers were uploaded twice** and, the two uploads being typed independently, `content_hash` deduped only the matching rows, so each was SPLIT across two labels with neither reconstructing and the union exactly 150. They are MERGED via `mergeWith` (rows normalised onto the paper's own numbering first, since a `.docx` runs 1-150 and an `.xlsx` 2-151), not re-ingested: nothing was ever missing. See the 2026-09-01 + 2026-09-06 Decisions entries); **+ 24 JEE Mains Paper 1** (2026-09-07 — **the first mocks whose answers are NOT all option labels**. 75 q · 300 marks · 180 min · +4/−1 uniform, three 25-question subject sections Physics → Chemistry → Maths, each 20 MCQ (Section A) + **5 NUMERIC-answer NAT (Section B)** — no options at all, the key in `questions.numeric_answer`. That forced **migration 0087** (`attempt_answers.numeric_response`, because `selected_label` is CHECK-constrained to A-D at the DB level and a value of 17280 was *rejected by Postgres*), a discriminated-union answer axis (`src/lib/mocks/answers.ts`) replacing the `"A"|"B"|"C"|"D"` that ran from storage through grading, palette, runner and review, and a numeric input in `MockRunner`. **Only 2025+ is expressible**: a 2021-2024 shift printed 90 questions of which a candidate attempted 75 — all 60 MCQ plus ANY 5 OF 10 NAT per subject — and `gradeMock` has no "attempt at most N" concept, so those would score out of 360 and let a student answer all ten; from 2025 Section B is 5 compulsory. **A THIRD sitting-discovery rule**, because for JEE not even `source_file` is a sitting: a 2025 file holds a whole DATE, 150 rows = two 75-question shifts back to back, so a sitting is a `source_row` BLOCK within a file (`scripts/mocks/jeeSittings.ts`, derived from `scripts/jee/papers/*.json`). **24 of 37 ship; 13 HELD** short by 1-3 questions absent from the bank. **The 11 shipped 2025 papers carry an INFERRED shift number** — 2026 states its shift twice (filename `_S1` + pyqNote) while 2025 states it nowhere, so "Shift 1 = rows 1-75" is convention, not evidence; recorded as inferred rather than asserted silently. **The NTA 2-decimal tolerance is INERT on this corpus** — all 546 NAT answers across 2025+2026 are non-negative integers at scale 0 — so it is unit-tested synthetically and exists for the 2021-2024 tier, which does carry decimals and negatives. See the 2026-09-07 Decisions entry). Migration 0044 = `mock_tests` (immutable ordered question-REF snapshot + exam-pattern config, deterministic `slugToUuid` id, service-role writes) + `mock_attempts` (per-user own-row RLS, retakes = multiple rows, partial-unique = one in-progress per mock) + `attempt_answers`. Pure core `src/lib/mocks/` (`blueprints` registry · `reconstruct` paper→snapshot · `attempt` grade+timer+palette · `perf` aggregation — all TDD; **generalized for NEET 2026-07-17**: optional per-section counts (soft-count), an edition-aware slug (`neet-2024` vs `neet-2024-re`), a per-question `grace` flag (officially dropped/bonus → full marks to all, no penalty), and per-sitting include-overrides — NDA path byte-identical); `service.ts` = attempt lifecycle; `MockRunner` (refresh-resistant timer → auto-submit · question palette · flag · autosave · resume) + results/review reuse the `/browse` KaTeX/BlockText render path (grace rows show an amber "awarded to all" disclosure). Always-visible **"Mocks"** nav tab — exam-gating REMOVED 2026-08-21 (it hid a working page for 11 of 13 exam states); `hasMocks` still drives the catalogue's own exam rail. Admin **`/dashboard/mocks`** (per-mock performance → drill-down to every attempt) + **`/dashboard/students`** (self-serve signup roster). Built by `scripts/mocks/build.ts` — THREE discovery rules: NDA year+month, a `source_file`-keyed registry (NEET/CDS/MHT-CET), and a `source_file` + `source_row` BLOCK (JEE, where one 2025 file holds two shifts). Self-discovering, idempotent. See the 2026-07-10 + 2026-07-17 Decisions log + [[mock-tests]].

## Stack

- **Next.js 14** App Router · TypeScript · Tailwind 3 · shadcn-style UI primitives
- **Supabase** Auth + Postgres + RLS, accessed via `@supabase/supabase-js` (no Prisma, no ORM)
- **Vitest 2** for tests · `tsx` for scripts
- **Word export:** `temml` (LaTeX → MathML) → `mathml2omml` → `docx` + `jszip`
- **Browser math preview:** `katex` + `react-katex`
- **Deploy:** Vercel, git-integrated

## Multi-tenancy + visibility model

**Platform brand vs. tenant org-name — do NOT conflate (and don't "purge" `"LWS Pune"`).** The public brand is **PYQ Vault** (`SITE_NAME` in `layout.tsx`; Footer, JSON-LD publisher, all anon/student-facing copy) — that's what everyone sees. Separately, **`organizations.name` is tenant data**, and `"LWS Pune"` is the **founding org customer (tenant #1)** that owns the seeded content. An org name is rendered ONLY on staff-gated surfaces (AppHeader, `/dashboard/*`, `/uploads`, `/account`, `/papers`) — always behind a `member` (`org_members` row), so **anon + self-serve students never see it**. When a second school onboards, they get their own org row and *their* staff see *their* name; nobody's org name is ever the public brand. Consequently the `"LWS Pune"` string in `api/sync/mock/route.ts` (`DESTINATION_ORG_NAME`), `tests/global-teardown-helpers.ts` (`REAL_ORG_NAMES`), the DB-integration tests' `.eq("name","LWS Pune")`, and scripts' `sourceFile`/`pyqNote` provenance are all **correct as-is** — legitimate tenant identity + attribution, NOT stale branding. A public-facing `LWS` literal (hardcoded in JSX/copy, not read from the org row) IS a branding bug → use PYQ Vault / "our team" (the 2026-07-12 sweep already fixed the known set). Rule of thumb: reads from `organizations.name` / routes by org identity → keep; hardcoded brand string shown to the public → PYQ Vault.

Per-school orgs, RLS-enforced. **Role model overhauled 2026-07-15 (migrations 0055–0057) — content editing collapsed to superadmin-only; teachers became branch-scoped paper-builders. The old "ADMIN does everything / TEACHER edits content" (migration 0025) is SUPERSEDED — see [[project-member-management]].**
- **Service role** (seed + ingestion scripts, server-only admin actions) bypasses RLS by design — never expose to client code. **The primary content add/edit path** — the whole bank is built here.
- **SUPERADMIN** (platform staff above orgs — `platform_admins` table, migration 0056; seeded = `connect.lwspune@gmail.com`) — the ONLY identity that can add/edit question CONTENT (questions/options/taxonomy/tags/embeddings/uploads), across ALL orgs. Cross-org console at `/superadmin` (list orgs · onboard a new org · provision its admins/teachers). **Also the ONLY identity that sees the platform-wide `/dashboard` surfaces** (students · entitlements · feedback · activity · mocks · quizzes · leads) — those read via the service-role client with no `org_id` filter, so they're cross-tenant by construction and were superadmin-gated 2026-07-20 (see the Decisions log). NOT an `org_members` row; detected via `getSessionSuperadmin()`/`requireSuperadmin()` (service-role read of `platform_admins`).
- **ADMIN** — own-org *management*: create branches + teachers, assign teachers to branches, build papers, triage reports, manage members. **Cannot add/edit content** (superadmin-only since 0056), and **cannot see the platform-wide dashboard surfaces above** (entitlements/students/feedback/activity/mocks/quizzes/leads are superadmin-only — 2026-07-20). The org-scoped dashboard cards an admin DOES get: Members, Branches, Batches, Papers, Question reports, Concept reports.
- **TEACHER** — assigned branch(es) ONLY: read the bank + build papers/batches for their branches (with the per-batch no-repeat warning). Branch-scoped via `branch_members` (0057). **Cannot edit content**, manage members, or manage branches.
- **anon** (no JWT) can read PUBLIC questions/options + all taxonomy. Cannot write anything.
- **STUDENT** (signed-in, self-serve, **no `org_members` row** — added 2026-06-01) registers at `/signup` (Google OAuth or email/password). By RLS construction they get zero private data; what an account unlocks is in-app question reports today + premium content via the entitlement axis below. Org-less = student (admins always get an `org_members` row atomically), so `/dashboard` redirects them to `/browse`.

**Cohorts: Org → Branch → Batch (migrations 0054/0055).** A **branch** (`branches`, admin-managed) is a campus (e.g. "FC Road"); a **batch** (`batches`) is a cohort ("NDA 2026 Morning") filed under a branch; a **paper** optionally targets a batch. The point of batches: the paper builder soft-warns when a question was already used for the SAME batch (per-batch non-repetition, derived from `paper_questions` joined to `papers.batch_id`; scope = per-batch, enforcement = soft-warn). Teachers are scoped to their `branch_members` branches for batches/papers; admins see all org papers; collaboration is branch-mediated. See [[project-member-management]].

**Premium entitlement axis (orthogonal to orgs, migration 0026).** Paid/comped access lives in `public.entitlements`, keyed on `auth.users(id)` — NOT org-scoped, because a student has no org. A grant unlocks a `scope` ('all' = full premium); `source` ∈ `razorpay | comp | manual` (the gate is **source-agnostic** — `userHasAccess` just asks "active entitlement for this scope?"; staff/org-members bypass the paywall). Writes are **service-role only** (no write RLS); reads are own-row only. Consumers: the `/notes` preview-gate (`tier:"paid"` chapters), `/pricing`/`/account`, the Razorpay webhook, and the `/dashboard/entitlements` comp UI. See the 2026-06-01 Decisions log entry + [[project-paywall-build]].

Each question has a `visibility` enum (`PUBLIC | PRIVATE`, default **PUBLIC** as of migration 0022 / 2026-05-17 — was PRIVATE prior). Read RLS is the union of two permissive policies: `(visibility = 'PUBLIC')` for everyone, plus `(org_id = private.current_user_org_id())` for authenticated org members on their own org's PRIVATE rows. Authenticated users see PUBLIC across all orgs + their own PRIVATE; anon sees only PUBLIC. Admins flip per-question visibility from the edit page.

Each question also has a **`question_kind` enum** (`pyq | practice`, default `pyq`, migration 0036) — the axis that separates the past-year corpus from the supplementary **practice** bank (the product is PYQ-first; **3,040 practice q live across 29 NDA Maths chapters as of 2026-06-13 — ALL 5 source folders ingested (Algebra + Trigonometry + Calculus + 2D Geometry + 3D Geometry; the 3D folder fed both the Vectors and 3D Geometry chapters)**, separate from the PYQ counts above). `/browse` exposes it as a **PYQ / Practice / All** toggle (default PYQ; `kind` filter in [filters.ts](src/lib/questions/filters.ts) → `query.ts` + `p_kind` on the facet RPCs). PYQ-only surfaces stay clean two ways: **tag-gated** surfaces (guide principles, notes drills, worked examples, quiz harvest) are PYQ-only *by construction* (practice questions are never principle/concept-tagged — doing so would leak them in), while the four **raw count-by-taxonomy** queries (`examHomeStats` + the 3 notes landings) carry an explicit `question_kind='pyq'` guard. Practice ingestion is its own pipeline + runbook — see [[practice-ingestion]] + `scripts/practice/README.md`.

Each question also has a **`question_format` enum** (`mcq | subjective | numeric`, default `mcq`, migrations 0041 + 0061) — the axis separating standard MCQs (4 options, exactly one `is_correct`) from **subjective/free-response** questions (State Board textbook content) and **numeric (NAT)** questions (JEE Section B — no options, integer/decimal answer in `questions.numeric_answer`, hashed via `numericContentHash`), the latter two carrying **zero option rows** (subjective's model answer lives in `questions.solution`). Subjective support: `subjectiveContentHash(stem, context)` in [hash.ts](src/lib/upload/hash.ts) (namespaced so it can't collide with an MCQ; context-aware so set-based siblings with the same bare sub-item text don't dedup-collide; excludes `solution` so answer-backfill doesn't change the id); `commitStaged` writes `question_format` + emits no options for subjective; `/browse` QuestionCard renders subjective as stem + "Show model answer" (no A–D); the Word answer-key prints the model answer instead of `(?)`. `question_format` is optional on the `QuestionRow` view-model (absent ⇒ MCQ). **A `/browse` FORMAT filter shipped 2026-08-16** (All · MCQ · Written · Numeric — `?format=`, default `all` so every pre-existing URL is byte-identical; facet-aware via `p_format`, migration 0078). It renders only where the exam is genuinely mixed — six exams are, five are 100% MCQ — driven by `EXAM_REGISTRY.mixedFormats` rather than a live count, because the live count is a 49k-row seq scan that exceeds the anon `statement_timeout`. See the Decisions entry + `src/lib/questions/formatMix.ts`. **Still deferred (non-critical — no subjective content reaches them by construction):** subjective in the admin edit *form* (answers are backfilled by script, not the UI), and quiz-harvest / RAG one-correct-option guards. See [[state-board-ingestion]].

RLS helpers live in the **`private` schema** (not exposed by PostgREST): `current_user_org_id()`, `current_user_is_admin()`, `current_user_is_superadmin()` (0056), `current_user_can_edit_content()` (0056), `current_user_can_edit_questions()`, `current_user_branch_ids()` (0057). **The semantic split matters (0056):** `can_edit_content()` = **superadmin-only**, gates all CONTENT writes (questions/options/taxonomy/tags/embeddings/uploads); `can_edit_questions()` is UNCHANGED (admin OR teacher) and now gates **paper/batch building** ONLY (papers/batches/paper_questions), NOT content — do not conflate them (redefining `can_edit_questions` would break the paper builder). Use `current_user_is_admin()` for org-management (branches, members, reports triage, entitlements); `current_user_branch_ids()` scopes a teacher's batches/papers to their assigned branches.

## File layout

The full file-by-file map — the `src/` tree (routes, components, lib helpers), `supabase/` migrations + seed, `scripts/`, and `tests/` — lives in **[ARCHITECTURE.md](ARCHITECTURE.md)**, extracted to keep this file's always-loaded payload lean. **Append a new file/component/route there, not here**; update the high-level shape below only when the top-level structure changes.

High-level shape:
- `src/app/` — Next.js App Router pages: `/browse` (paper builder), `/questions` (**cacheable, indexable per-chapter landing pages** — `/questions/<examSlug>/<subjectSlug>/<chapterSlug>`, 317 of them, derived from the taxonomy; the SEO + caching counterpart to the dynamic `/browse`), `/guide` (**registry-derived exam picker** → `/guide/<examSlug>` hubs → 11 subject guides: 10 NDA + MHT-CET Maths), `/notes` (cross-exam index → `/notes/<examSlug>` per-exam hubs → 20 teaching-notes chapters), `/board` (book-faithful textbook-solutions reader for board exams), `/mock` (timed PYQ mock tests — catalogue · instructions · runner · results), `/dashboard` (admin, incl. `/dashboard/syllabus` the **cross-book syllabus map** + `/dashboard/mocks` performance + `/dashboard/students` roster + `/dashboard/activity` usage-shape + `/dashboard/branches` campus CRUD + `/dashboard/batches` cohort CRUD + `/dashboard/members` incl. teacher↔branch assignment), **`/books`** (superadmin-only assembled PYQ books — review before export), `/superadmin` (superadmin-only cross-org console — list/onboard orgs + provision admins), `/upload`, `/signup`·`/login`·`/pricing`·`/account`·`/saved` (bookmarked questions), plus the `/api/*` route handlers.
- `src/lib/` — server/pure helpers by domain: `supabase/`, `auth*` (+ `isSuperadmin`/`requireSuperadmin`/`getSessionSuperadmin`), `questions/`, `guide/`, `notes/`, `links/`, `export/` (Word/OMML), `branches/` + `batches/` (cohort model: types + pure validate/split + service-role admin), `members/` (+ `setMemberBranches`), `superadmin/` (cross-org `listOrgsWithStats`/`createOrg`), `papers/` (collaborative builder + per-batch `usage` soft-warn), `entitlements/`, `billing/`, `mocks/` (timed mock tests: blueprints · reconstruct · grade/timer/palette · attempt service · perf/adminStats), `syllabus/` (the **cross-book syllabus map** — `summary.ts` pure core: `buildAlignmentRows` (three-book crosswalk, authored-pairings-only) · `dominantSbByChapter`/`sbBookOrder` (order an exam's chapters along a book) · `rollUpChapterStatus`/`tallyByExam` · `SPINE`/`BOOK_OF_EXAM` · `examOfSpine`/`isExamSpine` ("NDA bank taxonomy" → "NDA", anchored to the END so a book spine passes through unchanged); `query.ts` loaders — `loadSyllabusMatrix`/`loadMappingRows`/`loadAlignmentRows`/`loadExamSpineSummaries`/`loadNcertGaps`/`loadOldSyllabusByExam`, each scoped by **BOTH `source` and `subject`** — source alone merges two subjects' Ch.1, subject alone merges three books' Ch.1, and every spine numbers chapters from 1), `relevance/` (the **cross-exam syllabus-fit screen** — `config.ts` holds the audience, the blocking-tool list and the `REVIEWED_CHAPTERS` scope pinned **by chapter UUID** so a rename can't silently rot it; `fit.ts` is the pure core — `parseFit`/`isFitExam`/`fitCoverage`), `dbhealth/` (the **database health tracker** — `types`/`delta` (turns cumulative counters into per-window rates and REFUSES to report one it can't stand behind: stats reset · per-query entry eviction · window too short to extrapolate)/`flags` (thresholds, each tracking something that has already broken this project)/`format`; the CLI is `scripts/dbhealth/run.ts` — see [[db-health-tracker]]), `activity/` (the **engagement spine**: `events` pure core + `service` best-effort writer + `shape`/`classifyUsageShape` + `adminStats` — the A3 usage-shape readout), `students/` (signup roster), `reports/`+`notes-reports/`, `upload/`, `tags/`, **`sync/`** (the cross-app bridge to nda-tracker — `questionPayload.ts` is THE shared shape for all three transports, `paperPush.ts` the pure push builder + sitting helpers, `paperSittings.ts` the `paper_pushes` round-trip, `trackerTarget.ts` the per-org routing; see nda-tracker `CROSS_APP_SYNC.md`), **`itemStats/`** (per-sitting response evidence pooled at READ time — `aggregate`/`leads`/`exposure`/`overview`; never stores a pooled number), `quiz/` (the **Quiz-Factory core**: `atoms`/`daily`/`quizPayload`/`stemLint`/`verdict`/`coverage` pure helpers + `assemble.ts` shared `assembleNextQuiz` (snapshots questions onto the quiz row) + `deleteQuiz.ts` (delete + propagate to nda-tracker) + `admin.ts` dashboard reads — the `scripts/quiz/` CLIs import these; `coverage.ts` powers `quiz:coverage`).
- `src/components/` — `AppHeader`/nav, `math/` renderers (`KatexRenderer`·`RichText`·`BlockText`·`parseTableBlocks`), shadcn `ui/` primitives.
- `supabase/migrations/` — `0001…0097` (append-only. **0078-0097 are documented in the Decisions log rather than enumerated here** — the newest is **0097 = `paper_pushes`**, one row per SITTING of a paper pushed to nda-tracker, so a paper conducted for batch A on Monday and batch B on Thursday is two tracker exams rather than a 409; sitting 1 keeps the unsuffixed `exam_vault_<paperId>` so pre-0097 drafts are untouched, and RLS scopes through the parent paper mirroring 0058. **0077 = the `/browse` list index** — `(question_kind, exam_id, created_at DESC, source_row, id) WHERE visibility='PUBLIC'`. `question_kind` was missing from every PUBLIC index, so answering "NDA PYQs" pulled 9,224 rows from an index and opened the HEAP for each just to test the kind. Measured as anon: kind+exam **1,986 → 5.3 ms** (buffers 6,669 → 432), kind-only **4,454 → 228 ms** (17,403 → 484). `question_kind` MUST stay the leading column — it is what lets the kind-only query use the index too; `exam_id`-first would leave the slower shape unimproved. Built `CONCURRENTLY` in prod, written without it in the file because replay wraps statements in a transaction; **0076 = db-health visibility-map coverage** — adds `heapPages`/`allVisiblePages` per table to the snapshot's `tables` jsonb (no new columns). The ratio decides whether an "Index Only Scan" really is one; `questions` at 67.4% meant 13,092 heap fetches on one `/browse` query. **Dead rows do NOT predict it** — `syllabus_concepts` runs 0% dead rows at 43.2% coverage, because a bulk INSERT creates pages with neither, which is this project's whole ingestion pattern. Absent (not zero) on pre-0076 snapshots; **0075 = `solution_audit` review method** — a read-through of a stored solution, distinct from `blind_rederivation`. Not cosmetic: it decides what a later pass may SKIP, because a stealth wrong key (internally consistent, simply wrong — the 2026-06-03 audit's central finding) survives a read-through by construction. See `src/lib/reviews/coverage.ts`; **0074 = `question_reviews`** — post-ingestion review provenance: one append-only row per "someone checked this question's answer and concluded X" (`method` × `verdict`, both CHECK'd, grouped by `run_label`). Service-role only (RLS on, no policies — the `platform_admins` pattern). **`reviewed_content_hash` is NOT NULL and is the point**: it fingerprints the question AS REVIEWED, so a stem repaired afterwards makes the verdict queryably stale instead of silently trusted — the same move 0040 made for derived fields. Append-only so an OVERTURNED review survives (an UPDATE would erase both the reversal and its reason). `UNIQUE (question_id, run_label, reviewed_content_hash)` makes a re-run of a committing script a no-op. Verdicts split "we were wrong" (`key_fixed`/`stem_fixed`/`solution_rewritten`) from "the source was wrong" (`defect_preserved`) — the claim the Decisions log only ever made in prose. **NO ROW means NOT RECORDED, which is not the same as not reviewed** for anything predating the table. See [[question-review-provenance]]; **0073 = chapter last-added by subject**; **0072 = chapter last-added**; **0071 = db-health statement-store gauges** — adds `statements_tracked` / `statements_max` / `statements_evictions` to `db_health_snapshots` + the collector. The size of `pg_stat_statements` is itself a cost driver: Supabase's `postgres_exporter` reads the WHOLE store every minute, so at 4,868 entries / 3.8 MB of query text against 2.1 MB `work_mem` that read spilled ~5.8 MB to disk twice a minute — 13 GB/day on a 164 MB database, invisible to every query-level probe because the exporter excludes itself from tracking. Nullable ON PURPOSE (pre-0071 rows have no reading, and "not recorded" is not "zero"). See [[db-health-tracker]]; **0070 = db-health collector coverage** — widens `collect_db_health()` from top-25-by-lifetime to three arms (busiest 40 · slowest 20 · **every query that has ever spilled**, 100 of 4,477). Ranking by lifetime hides exactly what the tracker is for: a query that starts misbehaving today has a small lifetime total by definition; **0069 = `db_health_snapshots` + `collect_db_health()`** — one daily operational snapshot (gauges + cumulative counters + per-table and per-query detail) so Postgres' cumulative counters become per-window RATES. Service-role only (RLS on, no policies — the `platform_admins` pattern); the RPC exists because `pg_stat_statements` lives in the `extensions` schema and PostgREST cannot read it. See [[db-health-tracker]]; **0068 = facet RPCs get a one-shot plan** — the optional-parameter (`p_x is null or col = p_x`) anti-pattern makes a `language sql` body plan ONCE, generically, so the planner cannot prove the parameter non-null and demotes every predicate to a filter (`EXPLAIN (GENERIC_PLAN)`: Seq Scan 7,941 vs 541 with literals). Fix is `plpgsql` + `RETURN QUERY EXECUTE … USING`; NOT fixable with `plan_cache_mode` (tested). `public.match_chunks` (0040) is the only other function carrying the pattern — dormant, 2 calls ever; **0067 = questions hot-path indexes** — indexes the `/notes` subtopic counts + rewrites `get_pyq_years` as a loose index scan; **0066 = `syllabus_concept_exams.covered_by`** — WHERE a concept is covered, not just whether: comma-separated target section refs (`"11.4, 11.5"`, `"XI:2.5, XII:1.2"` — the `XI:`/`XII:` prefix marks a cross-YEAR mapping, the common case). Free text, never joined on in SQL; resolved to titles in `src/lib/syllabus/query.ts` against whichever book the row's `exam` names. *Applied via MCP 2026-08-02; the .sql file was missing and was recreated 2026-08-03 — see the Decisions entry.* **0065 = `syllabus_concepts` + `syllabus_concept_exams`** — the syllabus-map spine: one row per section of a BOOK (`source` = 'MH State Board' | 'NCERT') or per subtopic of an EXAM bank taxonomy (`source` = '<exam> bank taxonomy'), plus a per-(concept, exam) `status` (`full`|`partial`|`not`) + `note`. **`source` is load-bearing**: every spine uses subject 'Chemistry' and numbers chapters from 1, so a query filtered by subject alone silently merges three different books. **0064 = public chapter index** (the `/questions` landing pages); **0063 = written_papers**; **0062 = `question_audience_exclusions`** — the cross-exam **syllabus-fit screen**: one row per JEE question a student taught the NDA ∪ CET syllabus CANNOT solve, with `blocking_tool` naming the out-of-syllabus technique. Deliberately an EXCLUSION list, not a verdict-per-question table (~95% of JEE questions pass, so only the failures carry information); the *reviewed scope* — which chapters have been adjudicated at all — lives in `src/lib/relevance/config.ts`, **NOT** the DB, so "passed" and "never looked at" stay distinguishable. Read open (it's a public `/browse` control over already-PUBLIC questions), writes superadmin-only per 0056. See [[syllabus-fit-screen]]; **0061 = numeric (NAT) answer format** — adds `numeric` to the `question_format` enum + `questions.numeric_answer` (nullable) for JEE Section-B numeric-answer questions (no options; `numericContentHash` dedup key); `/browse` QuestionCard + docx answer-key render the numeric answer; **0060 = `teacher_access_requests`** — public "request teacher access" leads (name/institute/email/mobile/city/message/consent/status), RLS-enabled with NO policies → service-role-only (the `platform_admins`/`entitlements` locked pattern); written by `POST /api/teacher-access`, triaged at `/superadmin`; **0059 = email**; **0058 = `paper_questions` branch-scope** — replace `paper_questions_write_editor` with `paper_questions_write_scoped` (the parent paper must be accessible: admin OR own OR the paper's batch's branch ∈ mine), mirroring `papers_update_scoped` — closes the 0057 blind-write gap; **0057 = `branch_members`** — teacher↔branch junction (PK pair, RLS read=org-member/write=org-admin) + `private.current_user_branch_ids()` + rewrite of the batches/papers SELECT/UPDATE (+batches INSERT) policies to branch-scope teachers (is_admin OR own OR branch∈mine); **0056 = superadmin content lockdown** — `platform_admins` (locked RLS-on-no-policy lookup table, seeded `connect.lwspune@gmail.com`) + `current_user_is_superadmin()`/`current_user_can_edit_content()` helpers + repoint EVERY content-write policy (questions/options/chapters/subtopics/upload_jobs/embeddings/*_tags) to superadmin-only cross-org; **0055 = branches** (first-class campus entity, admin-managed) + `batches.branch_id` FK (drops the 0054 free-text `branch` label); **0054 = batches** (cohort under a branch; `papers.batch_id`) for per-batch paper non-repetition — see [[project-member-management]]; 0053 = **`get_activity_shape(p_days)` RPC** — the A3 usage-shape aggregate over `user_activity` (by-kind volume · active-users · per-user distinct-active-DAYS + gap distribution/median · recency · daily-active), computed in SQL so it's cap-safe past 1000 rows (the `get_dashboard_stats` idiom); `SECURITY DEFINER` + `search_path=''` + EXECUTE revoked from PUBLIC/anon/authenticated, granted service_role-only (reads across all own-row-RLS activity → admin-only); consumed by the admin `/dashboard/activity` readout via `src/lib/activity/adminStats.ts` + the pure `classifyUsageShape` (daily vs burst → streak-vs-goal-progress call); 0052 = **user_activity** — the engagement activity spine: one **append-only** fact per signed-in student action (`kind` CHECK'd to a learning-anchored allowlist — mock_submitted · answer_wrong/correct · chapter_mastered · note_checkpoint · question_bookmarked · quiz_taken · drill_completed; NO vanity kinds), own-row **SELECT + INSERT only** (no update/delete = immutable history), `metadata jsonb` for extensible payloads, + a full `UNIQUE(dedupe_key)` making the backfill idempotent (NULLs-distinct so live events aren't deduped). The substrate every later engagement mechanic reads from — see the **Engagement engine** section + [[project-engagement-engine]]; 0051 = **user_feedback** — append-only "voice of user" (NPS `kind='nps'` score 0–10 + feature-request `kind='feature'` message), own-row RLS insert/select + coherence/score CHECKs; powers the engagement-gated NPS + feature form on `/me` and the `/dashboard/feedback` rollup (Phase 4) — see [[project-progressive-profile]]; 0050 = **whatsapp opt-in + `mock_feedback`** — `student_profiles.whatsapp_opt_in`/`whatsapp_prompted_at` (CAPTURE-ONLY, no dispatch) + a `mock_feedback` table (one row per attempt, own-row RLS + parent-attempt EXISTS check, difficulty rating CHECK); Phase 3 of the progressive profile — see [[project-progressive-profile]]; 0049 = **student_profiles detail fields** — `medium`/`academic_stream`/`city`/`goal` (all nullable, enum + length CHECKs) powering the never-gated `/account` profile completion meter (Phase 2); 0048 = **student_profiles onboarding fields** — `target_exams text[]`/`stage`/`onboarded_at` (+ `mobile` dropped NOT NULL) for the `/welcome` post-signup intent capture (Phase 1); 0047 = **question_bookmarks** — per-student saved questions, own-row RLS (select/insert/delete), **real FK** `question_id → questions(id)` cascade (unlike notes' soft slug refs); powers the `/browse` bookmark button + `/saved` page via the app-wide `BookmarksProvider`; see [[project-saved-and-reveal-meter]]; 0046 = **notes_progress** — per-student `/notes` track layer (bookmark · mark-mastered · latest checkpoint self-score · last_viewed) keyed on `auth.users(id)` + the editorial `subtopic_slug` (denormalized `chapter_slug`/`subject_route` — notes are TS modules, no FK), RLS **own-row** all verbs (student writes via JWT), CHECK on checkpoint coherence; see [[project-notes-signin]]; 0045 = **student_profiles** — per-student post-signup contact mobile keyed on `auth.users(id)`, RLS **own-row** SELECT/INSERT/UPDATE (student writes their own row via JWT); powers the mock-result phone-capture gate — see [[project-student-mobile-capture]]; 0044 = **timed mock tests** — `mock_tests` (immutable ordered question-ref snapshot + exam-pattern config; service-role writes, published readable by all) + `mock_attempts` (per-user own-row RLS, retakes, partial-unique one-in-progress-per-mock) + `attempt_answers` (parent-attempt EXISTS RLS); see [[mock-tests]]; 0043 = **book-structure section columns** for the `/board` reader — `section_kind` enum (`solved_example`|`exercise`|`miscellaneous`) + `section_group`/`section_label`/`section_seq` (all nullable; only board/textbook rows carry them); orthogonal to the conceptual `subtopic` axis — see [[board-reader]]; 0042 = **`questions.solution_image_url`** (nullable) — an optional per-question SOLUTION diagram, shown in the `/browse` "model answer" reveal + the docx answer-key solution block only (never the question paper); reuses the existing `image_url` storage/render path; 0041 = **`question_format` enum** (`mcq`|`subjective`) for subjective/free-response questions (State Board textbook content — zero options, answer in `solution`); 0040 = **RAG grounding layer** — `pgvector` (extensions schema) + `questions.plain_text`/`solution_json`/`derived_model`/`derived_at` + generalized `embeddings` table (unsized `vector`, no index) + `match_chunks` RPC (JOIN-guarded PUBLIC+pyq); see the 2026-06-19 Decisions entry + [[rag-grounding-layer]]; 0039 = **collaborative paper builder** — `papers` + `paper_questions` junction, org-scoped RLS, finalize-snapshot; 0038 = **per-exam dedup** — swap the `questions` unique key from `(org_id, content_hash)` to `(org_id, exam_id, content_hash)` so CDS↔NDA shared UPSC questions keep both copies; 0029 = `subtopics.order_index` for /notes teaching order; 0030 = `quiz_atoms` Quiz-Factory pool; 0031 = `quizzes` + `quiz_atoms_map`; 0032 = `quiz_atoms.theme`; 0033 = `quizzes.public_slug` + `quiz_leads` (public-quiz funnel); 0034 = `record_quiz_lead` retake-aware upsert RPC; 0035 = `quizzes.questions` jsonb + `theme` (immutable quiz snapshot — display decoupled from the live atom pool); 0036 = `questions.question_kind` enum (`pyq`|`practice`); 0037 = `p_kind` on the facet RPCs for the /browse PYQ/Practice toggle). `scripts/` — seeds, `notes-lint`, `notes-latex-audit`, `notes:order` sync, `jee/` ingestion (two-lane SAFE/BLIND pipeline + the standing probes `coverage.ts` — reconciles a subject's papers against the DB BOTH ways, the check behind every "COMPLETE" claim — and `triage.ts`, which decides the SAFE-vs-BLIND lane per paper), `practice/` (NDA practice-question ingestion — `render` → vision-transcribe → `commit` → `preview`; runbook in `scripts/practice/README.md`, pure core in `lib.ts`), `quiz/` (Daily Quiz Factory CLI runners — `quiz:harvest` → `quiz:sync` → `quiz:verify` → `quiz:lint` → `quiz:assemble` → `quiz:delete` → `quiz:push`; pure core lives in `src/lib/quiz/`), `syllabus/` (the syllabus-map pipeline + its probes. **Spines:** `ingest-*-spine.ts` load a book/exam spine (exam spines PRUNE: section numbers are positional, so removing a subtopic strands the last as a ghost) — `ingest-bank-spine.ts` deliberately writes concepts with **NO link rows**, so an unruled subtopic stays genuinely unassessed. It is the ONLY exam-spine path: its predecessor `ingest-exam-spine.ts` defaulted an unruled subtopic to `full` and was **retired 2026-08-04** once that default was found to have shipped as a false verdict for 169 subtopics. **Rulings** are authored as DATA in `data/<subject>-<exam>-rulings.json` and written by `commit-bank-rulings.ts` under FOUR guards — section_no exists · the recorded `subtopic` still matches the spine (positional refs shift) · every `coveredBy` ref resolves to a live section of the named book · note ≤500 chars, mirroring the DB CHECK so the dry run catches it rather than the INSERT. **Evidence:** `dump_{chem,physics,maths}_corpus.py` build a PER-CHAPTER book corpus and `search_corpus.py` searches it (prefix-anchored by default; `--corpus=<name>` picks the subject — see [[corpus-search-for-coverage-rulings]]); `dump-sections-handout.ts` emits the citable-section list from the LIVE spine so it cannot drift; `RULING_BRIEF.md` is the contract agents author to. **Probes:** `audit-alignment.ts` re-measures the inferred-vs-authored pairing split and FAILS if any paired row loses its backing, `audit-spine.ts` finds sub-section holes + prose-swallowed titles, `audit-directions.ts` asserts no `status='not'` row is pointed at by a live exam subtopic (SUBJECT-scoped keys + a per-subject retired-chapter list since 2026-08-07 — unscoped it manufactured 129 cross-subject contradictions the day Maths landed), `smoke-page-data.ts` drives the shipped page's own loaders (the page is auth-gated `ƒ`, so a green build proves only that it compiles). **Exports:** `export-alignment.ts`/`export-chapter-map-{docx,xlsx}.ts` render the maps for teachers; `export-html.ts` is a prototype whose `rendercheck.js` executes the page's OWN emitted JS — it caught an empty table, a lost sort and a regex mangled by the template literal), `grounding/` (RAG grounding + key-audit pipeline — `backfill-plain-text` → `export-batch` → [blind agents] → `commit` → `apply-fix`; pure core `lib.ts`, runbook `README.md` — see [[rag-grounding-layer]]). `tests/` — Vitest unit + DB-integration. The `/dashboard/quizzes` admin page (read-only: pool stats + filterable list + Publish-to-public; assembly/delete are CLI-only) + `/dashboard/leads` (quiz-lead rollup) + the public `/quiz/[slug]` funnel are the UI over this.

## Commands

```sh
npm run dev                # next dev (local)
npm run build              # production build
npm run lint
npm run typecheck          # tsc --noEmit (covers test files too — keep this clean)
npm test                   # vitest run — against the DEDICATED TEST Supabase project (.env.test.local; guard refuses prod — see scripts/testdb/README.md)
npm run test:watch
npm run test:prod-contract # read-only suites asserting editorial ↔ LIVE prod content (guide taxonomy, cross-links, live counts) — tests/prodContractFiles.ts — since 2026-08-31 these run on a DAILY GitHub schedule (.github/workflows/prod-contract.yml, 08:30 IST; GitHub emails on failure), NOT in the push gate: their drift arrives from ingestion scripts as often as from pushes, and prod-host memory stalls were failing pushes on them. The two historically flaky suites retry 57014 with backoff (tests/helpers/retryTimeout.ts)
npm run testdb:migrate     # replay supabase/migrations into the test project (idempotent; catches prod-applied-but-uncommitted migrations)
npm run testdb:seed        # taxonomy + LWS Pune org + seed questions/uploads into the test project (idempotent)
npm run testdb:reset       # truncate + delete auth users + re-migrate + re-seed the test project (when killed runs leave crumbs)
npm run db:seed            # idempotent taxonomy seed (service-role)
npm run extract:taxonomy   # regenerate supabase/seed/taxonomy.json from a reference Excel
npm run notes:lint         # validate /notes editorial modules against live taxonomy + DB tags (read-only)
npm run notes:latex        # audit /notes for LaTeX-in-plain-text leaks + delimiter imbalance + unicode (read-only)
npm run notes:order        # populate subtopics.order_index from each /notes chapter's subtopicOrder (teaching order on /browse); --dry previews
npm run notes:coverage [subjectRoute] [chapterSlug]  # triage probe: math in a chapter's PUBLIC solutions but NOT in its notes (formula-gap candidates); default nda-maths statistics. Noisy — human-review list, not a gate
npm run tags:audit         # principle-tag audit (read-only)
npm run audit:underlines   # scan PUBLIC questions for \underline{...} segments not covered by the docx-export bypass
npm run audit:text [srcSubstr]  # standing probe for long-form text defects every other gate is blind to. FOUR classes: LITERAL_NEWLINE (a 2-char `\n` where a line break belongs — kills GFM pipe-tables on web AND in Word) + TABLE_NO_SEPARATOR (pipe rows with no `|---|`, so GFM refuses to build a table) + DROPPED_SYMBOL (a math zone opening with a bare `=` because the object it defines was lost upstream — `Let \(= I + adj(A)…\)`; anchored on the preceding FUNCTION word so `least count \(= 0.001\)` doesn't fire) + OPTION_LEAK (the option block never split off the stem — needs BOTH a label run AND ≥3 of the row's own option values echoed, else every "spot the error" GAT stem fires). Uses the REAL helpers (`normalizeNewlines`, `parseTableBlocks`) + pure probes in `scripts/lib/textProbes.ts` so a hit is a genuine disagreement, not a probe artefact. Triage, exits 0. Run after every ingest
npm run audit:omml [srcSubstr]   # scan for math zones the docx exporter can't convert to OMML (→ raw-LaTeX fallback); report generated-papers/omml-sweep.md. PASS THE FILTER after an ingest — unfiltered reads all ~37.6k rows (~30 MB, a full build's worth); `ilike`, warns + exits 1 + leaves the report untouched if it matches nothing, and stamps "SCOPED" on the heading. Run after a new ingest — see [[docx-omml-export-pipeline]]
npm run audit:keys [filter]   # zero-LLM structural probe over PRACTICE MCQs: flags solution-concludes-a-different-letter-than-key + duplicate options + not-exactly-one-correct. Optional source_file substring filter. Triage (source-verify before flipping) — catches the vision-ingest option-scramble/dup class; see [[practice-pdf-vision-ingestion]]. JEE `pyq` variant: `npx tsx scripts/jee/audit-keys.ts [Subject] [srcSubstr]` (reuses the same `auditRow` core; ran clean 2026-07-25 — 0 wrong keys across 2,642 MCQs)
# — Daily Quiz Factory (see Decisions log 2026-06-08 + [[daily-quiz-pilot]] memory) —
npm run quiz:harvest [route/chapter ...]   # /notes ConceptUnits → MCQ atoms JSON (scripts/quiz/atoms/*.json); default 3 sample chapters
npm run quiz:sync          # upsert harvested atoms → public.quiz_atoms (staleness-aware: preserves verified, re-flags changed)
npm run quiz:verify [name] # promote needs_review atoms → 'verified' from scripts/quiz/verify/<name>.ts (Claude-authored distractors)
npm run quiz:lint [route] [chapter]   # flag quiz-atom stems that aren't self-contained (back-refs, criterion-less "which is correct?", fragments, generic auto-harvest "formula for <X>?" template) — paginates the full ready pool; triage, run before publishing a quiz public
npm run quiz:coverage [route] [chapter] [--review]   # flag formulas/traps TAUGHT in /notes prose but not harvestable (formula.latex empty / <12 traps) — the theme-coverage guardrail; triage, not a gate
npm run quiz:assemble <route> <chapter> [max] [size] [-- --theme=X]   # ready atoms → quiz → record (quizzes+map) → push nda-tracker draft
npm run quiz:delete <slug>   # delete an assembled quiz here + propagate the delete to nda-tracker (drafts only there)
npm run quiz:push [daily/<slug>]   # push a single DraftQuiz module to nda-tracker (default: the hand-authored classical quiz)
npm run stats              # report live bank size (PUBLIC by exam/subject) + taxonomy + /notes chapter & diagram counts — for verifying the header lines instead of hand-maintaining them
npm run seo:dates          # regenerate src/lib/seo/contentDates.generated.ts — the sitemap's per-page <lastmod> for /notes + /guide, read from each chapter's `_data` git commit date. RUN AFTER SHIPPING A NOTES CHAPTER OR GUIDE and commit the result; `-- --check` exits 1 if stale. Generated-and-committed (not computed at build) because Vercel's shallow clone collapses `git log` to one commit, which would silently regress every date back to build time. Staleness is SAFE — it under-claims a change, never over-claims
# — Outbound email (Resend; migration 0059). Runbook in OPERATIONS.md —
npm run email:preview -- --html   # render both templates (no DB, no key, no send) → generated-papers/email-preview.html
npm run email:send                # DRY RUN by default — prints who'd get what, writes NOTHING. Add `-- --apply` to send (npm needs the `--`), `-- --only=<email>`, `-- --limit=N`. Idempotent: a UNIQUE dedupe_key means a re-run can't double-send; re-run it after the 7-day cooldown and each next_mock student gets the next unattempted paper.
npm run syllabus:audit     # ALL syllabus-map probes: audit-directions --ci (gate) + per-subject audit-alignment (gate) + audit-spine (triage). Run after ANY syllabus commit — live-DB scans, deliberately NOT in prepush
npm run db:health          # take a database health snapshot, store it, print a report vs the previous one (`-- --dry` = report only, stores nothing). Daily via .github/workflows/db-health.yml. REPORT-ONLY — never alerts, never fails a build. Runbook: scripts/dbhealth/README.md
npm run db:backup          # local pg_dump of ALL non-internal schemas (incl. auth) → backups/ (gitignored), **~36 MB / 30-75 s** (the long-standing "~25 MB / ~16 s" was measured at build time and has never matched a real run; derive the truth from a run's stamp vs its dump mtime). If it is running MINUTES rather than seconds the cause is almost never Postgres — check `pg_stat_activity.wait_event`, and `ClientWrite` means the server has the rows ready and is blocked pushing bytes to THIS machine, i.e. the network link (measured 2026-09-02: 8-16 KB/s against a ~900 KB/s norm, with cdnjs and github equally slow, so it was the local connection). Weekly via Task Scheduler, and BY HAND before any bulk-write script — the threat model is our own scripts, not Supabase. Verifies the dump before keeping it (pg_dump exit + size floor + every live table present in the pg_restore TOC) and DELETES a dump that fails, pruning nothing. Keeps the newest 5 runs; never deletes a filename it can't parse. `-- --dry` plans only. Needs PostgreSQL 17+ CLIENT tools + SUPABASE_DB_URL in .env.local. Runbook: scripts/backup/README.md
npm run itemstats:rollup    # roll vault /mock responses into question_item_stats (one row per question+sitting). DRY RUN by default; `-- --apply` writes. Idempotent. Intake filters are the point: first attempt only (24% are retakes), an engagement floor (28 submitted attempts answered ZERO questions), grace questions excluded, correctness via the app's own verdictFor. Weekly, in the Monday db:backup slot
npm run itemstats:ingest    # ingest an nda-tracker export (`node item_stats.js --out=...` there) into question_item_stats. DRY RUN by default; `-- --file=... --apply` writes. REFUSES a payload not declaring `grain: "question-per-exam-record"` -- a pooled export cannot be un-pooled. `measured_content_hash` is read from the bank at ingest time; an id resolving to no row is REPORTED, never dropped
npm run itemstats:report    # read question_item_stats back THROUGH the pure core: coverage + the wrong-key LEADS queue (`-- --ratio=3`, `-- --all`). Read-only. A LEAD IS NOT A VERDICT -- a distractor outpulling the key can mean a wrong key, a well-built trap, or a shared misconception
npm run itemstats:smoke     # drive /dashboard/item-stats' own loader + the /browse card read path against live data. That page is auth-gated `f` so the build never renders it; this proves the loaders, NOT the layout
npm run itemstats:smoke-conducted   # drive the PAPER BUILDER's exposure lookup ("has this batch already sat it?") against live paper questions. Also asserts the ORG FILTER, which is the security property: the RLS policy on question_item_stats is deliberately GLOBAL because the STATISTIC pools across institutes, so another institute's batch names are excluded by the QUERY alone
npm run board:lint         # /board section-structure gate: fails if a PUBLIC board (question_kind='practice') row lacks section_* fields or a chapter's section_seq isn't contiguous 1..N (scripts/stateboard/lint-sections.ts)
npm run db:types           # supabase gen types (requires SUPABASE_PROJECT_REF env)
npm run prepush            # the chained gate, MIRRORS CI: typecheck → lint → notes:lint → notes:latex → test → build
npm run prepush:quick      # the same chain WITHOUT the build — what .githooks/pre-push runs when no build-relevant file changed
npm run needs-build <base> <head>   # exit 0 = the changeset needs `next build`, 1 = it can be skipped. Shared by the hook + CI (pure rule: scripts/lib/needsBuild.ts)
npm run gate [target]      # wrapper that runs `npm run <target>` (default prepush) and prints a GATE_RESULT: PASS/FAIL sentinel as its LAST line — so the result survives `| tail` even though a pipe masks the exit code
```

> **Pre-push gate.** `.githooks/pre-push` runs `npm run prepush` before any push — which now **mirrors CI exactly**: typecheck → lint → notes:lint → notes:latex → **test** (vs the dedicated TEST project) → build (single source of truth: the `prepush` script). So a green pre-push means a green CI. **test:prod-contract left this chain 2026-08-31** — it runs on a daily schedule instead (see the Commands note above). Widened 2026-06-02 from the old build-only chain after a broken test passed pre-push and only failed in CI (it didn't run `npm test`). **The BUILD step is conditional since 2026-08-05** — the hook diffs the pushed range and runs `prepush:quick` (no build) when nothing `next build` reads changed, `prepush` otherwise. 78 of 145 pushes in the preceding 30 days were data/docs-only, and each was building 689 pages twice (here + CI) for ~25 MB of egress apiece. The rule is an **allowlist of SKIPS** (`*.md`, `tests/`, `scripts/`, `supabase/`, `generated-papers/`) so an unrecognised path BUILDS — it can only ever be too cautious. Pure core `scripts/lib/needsBuild.ts` (spec `tests/needs-build.test.ts`), CLI `scripts/needs-build.ts`, shared with CI so the two gates cannot drift. **Cost — MEASURED 2026-08-09, per stage, two runs (the old "~3–4 min with a build, ~1–2 min without" was wrong by 3–5× and is corrected here): ~12–17 min with a build, ~8 min without.** typecheck 20-24s · lint 9-11s · **notes:lint 69-76s** (named like a static check, but it validates the editorial modules against the LIVE DB) · notes:latex 7-8s · **test 5m32s-5m47s** · test:prod-contract 36s · **build 3m52s-8m49s**. Two things the measurement settled: the test stage is NOT a cold free-tier project waking up (it is the same to within 15 s on a warm second run) — **it is `maxForks: 2` applied globally to all 244 test files when only 82 touch a database**; and the BUILD is the volatile stage, swinging 2.3× on identical code because it prerenders ~317 pages against production and inherits whatever that database is doing. Activated by the `prepare` script (`git config core.hooksPath .githooks`) on `npm install`. `.gitattributes` pins LF on `.githooks/*` so the shebang resolves on Linux/WSL contributors. Escape hatch for WIP pushes: `git push --no-verify` — don't reach for it unless you know why.

> **CI gate.** `.github/workflows/ci.yml` runs the **full** gate (typecheck → lint → `notes:lint` → `notes:latex` → `npm test` → build) on every push to `main` + every PR. **Its Build step is conditional on the same shared rule as the pre-push hook** (a `Detect build-relevant changes` step calls `scripts/needs-build.ts`); checkout therefore uses **`fetch-depth: 0`** — without the base commit the rule fails safe to "build" and the saving silently evaporates. The local pre-push hook now runs this same chain (see Pre-push gate above), so CI is a backstop rather than the first place these checks run — it still matters because it runs on a clean checkout with repo secrets even if someone `--no-verify`s. Requires SIX repo secrets: the prod trio (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` — build prerender + notes:lint + the prod-contract step) **plus the test-project trio (`TEST_SUPABASE_URL`, `TEST_SUPABASE_ANON_KEY`, `TEST_SUPABASE_SERVICE_ROLE_KEY`) — since 2026-08-06 `npm test` refuses to write fixtures against prod, so CI's test step FAILS until these exist.** Vercel still owns deploys; CI is the correctness gate.

## Conventions specific to this project

- **Spacing scale.** Page wrappers `p-8`, content cards (CardContent default) `p-6`, compact cards (FilterBar etc.) `p-4`, nested option blocks `p-3`. Pick the smallest tier that contains breathing room — don't mix `p-4`/`p-6` in the same visual rhythm.
- **Typography.** UI text uses `font-sans` (Inter, default). Question/option/solution body text uses `font-serif` (Source Serif 4) — it's how teachers tell content apart from chrome. Both are loaded via `next/font` in `src/app/layout.tsx` and exposed as Tailwind utilities.
- **Color / brand (indigo, 2026-06-01).** The brand accent is **indigo** — chosen to unify with the `/notes` content palette. Three CSS vars in `globals.css`: `--brand` + `--brand-foreground` (fills — indigo-600 + white, AA-safe as a button bg in both themes) and `--brand-accent` (text/icon/border — indigo-600 light / indigo-400 dark, AA-legible as small text in both). `--ring` points at the brand so focus rings are branded. Tailwind exposes `bg-brand` / `text-brand-foreground` / `text-brand-accent` (+ a `brand` Button variant). **Application is SELECTIVE**: brand goes on chrome the eye lands on (primary CTAs, active nav, header mark, hero, stat numbers, table heatmap/peak-highlight, focus ring); near-black `--primary` stays the workhorse for secondary/admin buttons. **Off-limits:** the `/notes` + `/guide` *content* colors (concept-card indigo, amber traps, sky self-check, violet practice, emerald checkpoints) are content semantics — do NOT fold them into the brand token or recolor them. See [[brand-indigo-tokens]] + the 2026-06-01 Decisions-log entry.
- **Icons.** Use `lucide-react` for all icons. Never emoji or text glyphs (▾, ‹, ›) for UI affordances. Standard sizes: `h-3.5 w-3.5` for inline-with-text, `h-4 w-4` for buttons, `h-5 w-5` for the AppHeader logo.
- **Toasts.** `import { toast } from "sonner"`. Fire on every async user-initiated success or failure (save, upload commit, export download, sign-out). Setting an inline `error` state is fine, but never *replace* a toast with it — both should fire so the user can't miss the result while scrolled.
- **AppHeader.** Every page wraps its `<main>` in `<><AppHeader />…</>`. Don't re-add ad-hoc back-links, theme toggles, or sign-out buttons on individual pages — theme lives in the `ThemeToggle` island in the header (visible to anon + authed), sign-out lives in the UserMenu popover (authed only).
- **No Prisma.** Supabase RLS is the security boundary — every read/write goes through `@supabase/supabase-js` so the user's JWT carries through. Reach for `createSupabaseAdminClient()` (service-role) only in server-only contexts (seeds, cross-org admin) — it bypasses RLS by design.
- **Migrations are append-only.** New SQL goes in a new `000N_<name>.sql`. Apply via Supabase MCP `apply_migration`. Never edit a previously-applied file.
- **`.mcp.json` is gitignored.** Holds Supabase + Vercel personal access tokens. Don't commit.
- **Idempotent seeds + uploads.** `scripts/seed.ts` and the upload commit both check existence before insert; re-running is safe.
- **Content hashing for upload dedup.** `content_hash = sha256(normalised question text + sorted options + answer)` plus a unique index on **`(org_id, exam_id, content_hash)`** (migration 0038 — **per-exam**: was org-wide `(org_id, content_hash)` until 2026-06-15). Re-uploading the same file inserts 0 rows; the SAME question may recur across exams (CDS ↔ NDA share UPSC GAT English questions — org-wide dedup silently dropped the CDS copy). `commitStaged` upserts on `org_id,exam_id,content_hash`; `applyMockSync`'s content_hash lookups are exam-scoped to match.
- **Text normalization at write boundaries.** Long-form user-authored text (question, context, solution, options) goes through `normalizeNewlines` in the upload parser and the edit validator. The DB never carries literal `\n` (backslash+n) past the boundary; math zones are preserved verbatim. Any new write path that accepts long-form text must call the helper before insert/update — otherwise the render layer can't distinguish "real line break" from "literal text". **Enforced at the choke point since 2026-07-27:** `commitStaged` now **REJECTS** a row whose text/context/solution carries a literal `\n` (`literalNewlineFields` in [textGuard.ts](src/lib/upload/textGuard.ts)) — it does NOT silently repair it, because `content_hash` is computed by the CALLER from the pre-normalisation text, so rewriting at insert would leave stored text ≠ the hash's preimage and the next re-ingest from a corrected source would hash differently and duplicate the row. Fix the SOURCE. Detection is `normalizeNewlines(v) !== v` (not a second regex), so the guard can never disagree with the normaliser. See [[reject-dont-normalise-when-hash-upstream]].
- **OMML + math defaults via post-processing.** Two `docx`-library limitations land on the same workaround. (1) `ImportedXmlComponent` wraps imported XML in an `<undefined>` element Word rejects, so we emit placeholder runs and swap them for OMML after `Packer.toBuffer`. (2) The library doesn't expose `<m:mathPr>` for settings.xml, and Word's built-in math defaults (`defJc=centerGroup`, `wrapIndent=1440`, non-zero margins) shift any paragraph containing a 2-D math element (`<m:f>` etc.) ~1″ right of plain-text paragraphs — option `(A) -3/4` ends up out of column with option `(A) -1`. `patchZip` in `src/lib/export/docxBuilder.ts` handles both: marker → OMML in `word/document.xml`, and `<m:mathPr defJc=left wrapIndent=0 lMargin=0 rMargin=0>` injected before `</w:settings>` in `word/settings.xml`.
- **Restart `npm run dev` after adding new routes.** Tailwind JIT can miss files created mid-session. Stop dev, `rm -rf .next`, restart before UI smoke tests.
- **`/notes` LaTeX in body prose only.** Four `SubtopicNote` / `ConceptUnit` fields render as plain text (no KaTeX) AND feed SEO `metadata.description` / JSON-LD where KaTeX would be invalid: `note.title`, `note.oneLineDefinition`, `note.whyItMatters`, `concept.name`. LaTeX in any of these leaks through as raw `\(\hat{i}\)` markup on the page AND in page metadata. **Keep math only in body fields** (`intuition`, `definition`, `formula.latex`, `authoredExample.{prompt,steps,answer}`, `trap.{title,body}`) — those go through `KatexRenderer` correctly. Use Unicode (`î`, `ĵ`, `k̂`, `Σ`, `°`) or plain prose for the four plain-text fields. **Body fields also support `**bold**`** (renders via KatexRenderer / RichText — use it for key terms), and the **`definition` field additionally supports `- ` bullet lists** (rendered by `RichText`): bullet *list-shaped* definitions (term/type/property enumerations), keep narrative ones as prose. The plain-text fields render `**`/`- ` literally — never use them there.
- **Word export: lowercase option labels at render only.** Bank stores `A/B/C/D` uppercase (Excel + DB convention); the `.docx` export renders them as `(a)/(b)/(c)/(d)` in both question paper and answer key (`opt.label.toLowerCase()` in docxBuilder). Browse cards + edit form still show uppercase — only the rendered Word doc is lowercased. If a future surface needs to render bank-style labels, follow the same boundary: lowercase at render, never in the DB.
- **Word export: underline bypasses the math pipeline.** DB convention is `\(\underline{\text{word}}\)` (or `\(\underline{\textit{word}}\)` for italic+underline — biology taxonomy questions). KaTeX renders these in the browser; the docx export detects them via `UNDERLINE_BYPASS_RE` in [src/lib/export/ommlBuilder.ts](src/lib/export/ommlBuilder.ts) and emits a native Word run (`TextRun({ text, underline: { type: "single" }, italics? })`) instead of routing through `temml → mml2omml → <m:borderBox>` — Word doesn't reliably draw the bottom border on inline-math borderBox under our injected math defaults. Trailing punctuation INSIDE the delimiters (`\(\underline{\text{x}}.\)`) is split off into a separate text segment. Any pattern outside the regex (bare `\underline{word}`, chained `\text{}\text{}`, `\textbf` variants, block math) falls through to the OMML pipeline and renders without an underline in Word — run `npm run audit:underlines` after each new English / Biology batch to catch fall-throughs.
- **Tables in question content use GFM pipe-tables (2026-06-02).** A table inside a question `text` or `context` is authored as a standard Github-flavored markdown pipe-table — a header row **plus a `|---|---|` separator row**, then data rows. The separator row is mandatory and is what distinguishes a real table from inline `|·|` math (determinant `\(|A|\)`, abs value `|x|`, cell notation `Zn|Zn²⁺`): a line of pipes with **no** following separator stays prose. `parseTableBlocks` in [src/components/math/parseTableBlocks.ts](src/components/math/parseTableBlocks.ts) (pure, math-masked via `maskMathZones`, TDD in `tests/parse-table-blocks.test.ts`) splits text into prose + table blocks; cells keep their `\(...\)` math and `**bold**`. Rendered by `BlockText` (`src/components/math/BlockText.tsx`) on `/browse` (expanded card only — collapsed preview stays plain `KatexRenderer` so a table can't blow past `line-clamp-2`) + the editor Preview + the `/notes` mastery-check / `/guide` worked-example cards (`WorkedExampleCard`'s `example.context` + `example.text`, 2026-06-04), and as a native Word `<w:tbl>` in `docxBuilder` (the question NUMBER rides on the first paragraph; a stem opening with a table gets a numbered empty paragraph first). **No DB primitive** — it's all in the existing `text`/`context` columns; `normalizeNewlines` is compatible (it upgrades an Excel-authored literal-`\n` table into real newlines). **Future-only**: the existing 13 Statistics (lossless 2-row linearization) + 37 Match-Lists were NOT backfilled — they still render as linearized text. Options don't support tables (v1). For a genuine ≥3-column data grid that can't be a markdown table, fall back to a question image like a diagram. **Any surface rendering a long-form field that MAY contain a pipe-table (a solution/model-answer, not just stem/context) must render via `BlockText`, not `KatexRenderer`** — `KatexRenderer` prints a table as raw `| p | q |` text. The `/board` + `/browse` solution reveals both shipped on `KatexRenderer` and rendered truth-table answers as raw pipes (fixed 2026-07-06); `BlockText` fast-paths to `KatexRenderer` when there's no table, so it's a safe drop-in. **The same contract binds the WORD EXPORT, and it drifted for a year:** `parseTableBlocks` was wired into docx's stem + context paths but never the solution path, so a table in any solution printed as raw pipes in every downloaded answer key — 123 PUBLIC rows (fixed 2026-07-27 via `solutionBlocks` in [docxBuilder.ts](src/lib/export/docxBuilder.ts)). **The rule that prevents the next drift: the long-form field list (`text`, `context`, `solution`) × every renderer is a CONTRACT, and it is asserted by a golden test** ([docx-solution-table.test.ts](tests/docx-solution-table.test.ts)) that checks the same fixture through both surfaces. Fixing one renderer and not the other is the failure mode — it happened twice.

## Recurring pitfalls

Patterns that have bitten this codebase more than once. Each one earned its place by causing a real production regression — read before writing new code that resembles the shape.

- **PostgREST 1000-row cap on raw `.select()`.** Any `client.from(...).select(...)` that derives a count, a distinct set, a sort, or any aggregate from the *row payload* (rather than response headers via `count: "exact"` or a SQL aggregate via RPC) is suspect once the table can exceed 1000 rows. PostgREST silently truncates at row 1000 — no error, just wrong answers. **Already bit us three times:** dashboard totalQuestions (migration `0018` get_dashboard_stats), PYQ-year filter dropping older years (migration `0019` get_pyq_years), and upload commit dedup races (fixed by switching to `.upsert(..., { onConflict, ignoreDuplicates: true })` — let the DB unique index enforce dedup, not an in-memory `Set`). **Audit checklist before writing any new `.select()`:** (1) Is the row payload itself the answer, or are you computing something from it? (2) If computing, can the underlying table exceed 1000 rows ever (today or as the bank grows)? (3) If yes, use `count: "exact", head: true` for counts, or write a `security invoker` RPC that does the aggregate in Postgres. Never trust a row-derived count from `.select()` past the first 1000 rows. **Bit us twice more 2026-06-11:** `quiz:lint` used a bare `.limit(2000)` which PostgREST still caps at 1000, so it silently scanned only 1000 of ~1762 ready atoms (fixed by paging in 1000-row `.range()` windows); and **the sibling self-imposed-limit trap** — `/dashboard/quizzes` `listAssembledQuizzes` had a hard `.limit(60)` and a "Quizzes built" stat derived from `list.length`, so once the table crossed 60 quizzes (now 109) the oldest silently vanished from the filtered admin view AND the stat under-reported. Rule: a hard `.limit(N)` on a list hides rows once the table exceeds N, and any stat from `list.length` lies — derive counts from a true `count:"exact"`, and raise/paginate the list. **A SECOND, INDEPENDENT LIMIT lives on the other side of the same query and is easy to confuse with this one: `.in("col", [...])` puts the list in the URL**, so a few hundred uuids exceeds the request-line limit and PostgREST answers a bare `Bad Request` (bit `reviews:report` 2026-08-11 at 833 ids — its coverage section had only ever run against an empty table). **Paging a RESULT (1000 rows) and chunking a FILTER (~200 ids) are different limits and only one of them is 1000**; a helper that pages results correctly can still blow up on its filter. Chunk `.in()` at ~200 regardless of how the result is paged.
- **A SORT's payload is every SELECTED column — so a wide `.select()` on a paged, ordered query sorts the whole result set to keep one page.** `queryQuestions` asked for `text`, `context`, `solution`, four lateral joins and the options aggregate in the same query that carried the `ORDER BY`. PostgREST materialises that as one `pgrst_source` CTE, so Postgres sorted **~22,000 rows at ~922 bytes each to return 25** — an `external merge` spilling **13.9 MB to disk on every call**, which was **99.4% of the temp writes `pg_stat_statements` TRACKS** (1,097 GB lifetime, on a 144 MB database) and a major drain on the project's Disk IO Budget. **CORRECTED 2026-08-09 — this was originally written as "99.4% of the entire database's temp writes", which is wrong by an order of magnitude**: `pg_stat_statements` totals 114 GB against `pg_stat_database`'s 1,145 GB, so ~90% of this database's spill has ALWAYS been invisible to statement-level tracking. A percentage computed inside the tracked subset is not a percentage of the database — see the 2026-08-09 entry, where the untracked remainder turned out to be Supabase's own metrics exporter. A throttled disk then produced the `/questions` prerender statement timeouts AND the build-time ECONNRESETs, so one query shape was behind three separate alarms. **Fix pattern: two-phase fetch** — an id-only query carrying the filters + ORDER BY + range, then a wide fetch of just that page's ids (`queryQuestionsByIds`), reordered in JS. Sort payload ~922 → ~30 bytes; verified 13.9 MB → 0. **Audit checklist for any new paged+ordered query:** does the `.select()` carry long-form text through a sort? Is the ORDER BY on a joined/embedded column (no index can satisfy it, so a full sort is forced)? Two traps found while fixing it: `count: "exact"` is NOT the culprit — PostgREST already compiles it to its own narrow CTE (10 ms index-only scan) — and **`EXPLAIN ANALYZE` overstates wall time ~50× on this host** (3,219 ms vs 178.8 ms for the identical statement), so use `TIMING OFF` and treat `Sort Method` / `temp written` as the real acceptance criteria since they are plan properties rather than load-dependent timings. See the 2026-08-05 Decisions entry.
- **`next dev` is more forgiving than `next start` for server↔client prop serialization.** Functions and other non-serializable values passed from a Server Component to a Client Component compile cleanly under `next dev` but throw at SSR time under `next start` ("Functions cannot be passed directly to Client Components"). This bit us on the 2026-05-13 Pagination revert. **Mitigation:** the pre-push hook in `.githooks/pre-push` runs `next build` before allowing a push, so this class of bug is caught locally rather than after a Vercel deploy. Don't `git push --no-verify` unless you know exactly why.
- **`next build` does NOT exercise a DYNAMIC (`ƒ`) page's render — so the build gate is blind to its render-time throws.** Only static/SSG (`○`/`●`) routes are prerendered at build; a server-rendered-on-demand page (any page that awaits cookies / a per-request query — most `/guide/*` + `/notes/*` pages that call `resolveTaxonomy` via the anon client) is never executed until a real request, so a render exception passes `typecheck` + `lint` + `next build` + the full pre-push gate and only 500s in production. **Bit us 2026-06-14:** the new `/guide/nda-physics/ncert-map` page named a `<NcertChip>` prop `ref` — React **reserves** `ref` (it's never passed to a plain function component, so the value was `undefined` at render → "Refs cannot be used in Server Components"). Typecheck passed because the prop was explicitly typed `{ ref: NcertRef }`; the build passed because the page is `ƒ`. **Three mitigations:** (1) never name a component prop `ref` (or `key`) — they're React-reserved regardless of your prop type; (2) **curl/browser smoke-test every new DYNAMIC route before claiming done** — the build gate's green is necessary but NOT sufficient for `ƒ` pages (it IS sufficient for `○`/`●` pages, which prerender). A data-only test (like the ncert-map taxonomy test) doesn't render the page, so it can't catch this either. (3) **Confirm the smoke test actually REACHED the page** — an anon curl to any route under the `src/middleware.ts` matcher (`/dashboard/*`, `/account/*`, `/upload*`, `/uploads*`) is bounced by **middleware** before the page compiles, so the 307 tells you nothing about your code. Check the dev log for a `○ Compiling /your/route` line; no line = the request never got there. Bit us 2026-07-15 (the paper-editor rebuild — a "clean" 307 was pure middleware). For an auth-gated `ƒ` page there is **no headless proof of render** (the repo has no jsdom/testing-library/Playwright): say so plainly and hand the browser check to the user rather than implying the route was verified. See [[probe-must-reach-the-code]]. (4) **The same blindness covers any UI behind an INTERACTION, even on a public `○` page that curls clean.** Bit us 2026-07-28: a checkbox added to `/browse`'s `DownloadDialog` rendered *below* the footer and swallowed a sibling control, through a full green gate — the dialog is a Radix portal that doesn't exist until a click, so `/browse` returning 200 with correct HTML says nothing about it. Modals, popovers, sheets, accordions and anything else gated on a click are **as unverifiable here as an auth-gated `ƒ` page**; the build proves they compile, never that they lay out. Say "compile + logic proven, render not" and hand the click to the user.
- **One `cookies()` read in a SHARED SHELL component silently de-caches the ENTIRE site — and the build's route table will not tell you.** `AppHeader` is on every page. It resolved the session and read the `qb_exam` cookie during server render, so Next marked every route dynamic: **there were literally ZERO prerendered HTML files on disk**, and the `revalidate` settings on 77 `/notes` chapters, 10 `/guide` subtrees, the homepage, the `/mock` catalogue and the `/questions` landing pages had **never once taken effect** — for months. Every page served `Cache-Control: private, no-cache, no-store`. **The route table is NOT the check:** a route with `generateStaticParams` still prints `●` (SSG) even when every prerender bailed, so the build output looked correct throughout. **The only reliable probe is the filesystem** — `find .next/server/app -name '*.html' | wc -l`. If that number is 0, nothing is cached, whatever the legend says. Corollaries: (a) audit the shared shell (`layout.tsx`, `AppHeader`, any always-rendered component) for `cookies()`/`headers()`/`auth.getUser()` **before** optimising any individual page — a page-level fix is worthless while the shell de-caches everything; (b) `createSupabaseServerClient()` reads cookies internally, so calling it counts; (c) once fixed, prove cache SAFETY from the artifacts, not from reasoning — grep every `.html` **and `.rsc`** for emails / org names / authed-only controls, since a cached page is one copy served to everyone (2026-07-29: 622 pages, 0 leaks). See [[shell-component-decaches-site]].
- **Making a page cacheable BREAKS anything that relied on a server re-render, and the failures are silent.** Two fired on the same day: (1) `ExamPill` (since deleted, 2026-08-21) wrote the exam cookie then called `router.refresh()` so the SERVER would recompute the nav hrefs — on a cached page a refresh just re-serves the same copy, so the switcher would have appeared to do nothing (fixed by moving the active slug into client state). (2) `useSearchParams()` anywhere in the tree **bails a static prerender out to client rendering** — `QuestionList`'s bookmark button did this, silently costing the landing pages the caching they exist for; the fix is a `<Suspense>` boundary, and the symptom is only visible as a missing `.html` file. Grep for `router.refresh()` and `useSearchParams()` before making a route static. See [[next-caching-traps]].
- **Source-source subtopic name collisions during bulk chapter merges.** When canonicalizing N source chapters into one target chapter, a bulk `UPDATE subtopics SET chapter_id = '<tgt>' WHERE chapter_id IN ('<src1>', '<src2>', ...)` can fail with `23505` on `subtopics_chapter_id_name_key` even if a pre-flight source-vs-target collision check came back clean — because two sources may share a same-named subtopic that collides only once both are reparented. Bit us on the NDA Physics E&M cluster (5 sources → "Magnetic Field Inside a Solenoid" appeared in two of them). **Mitigation:** process sources sequentially via a `DO $$ FOR mapping IN (VALUES (...)) LOOP ... END $$` block; each iteration redirects colliding questions, deletes colliding source subtopics, reparents the rest, updates remaining question chapter_ids, and deletes the source chapter — so by the time source 2 merges, source 1's content is already in target and the standard collision-handling kicks in. Pattern + full template in `[[reclassification-sql-pattern]]`.
- **Catch-all chapters and subtopics drift until you reclassify per-question.** Buckets named "Mechanics", "Physics", "Error Identification in Sentences" collect every question that doesn't cleanly fit a more-specific name. They look harmless during a chapter-level merge but rot the bank's usefulness for technique drills. **Mitigation:** when a chapter or subtopic holds ≤~50 mixed-topic questions, read each stem and produce a `(question_id, target_canonical_id)` mapping; execute as a `DO $$ FOR m IN (VALUES (qid, tgt)) LOOP ... END $$` that (a) looks up the question's source subtopic, (b) finds same-named subtopic in target chapter, (c) redirects+deletes on collision, otherwise reparents+moves chapter_id. Precedents: Spotting Errors 29 q (2026-05-15 NDA English); Mechanics 19 q + Physics 26 q catch-all chapters (2026-05-15 NDA Physics). Beyond ~50 q the user prefers chapter-level merge + subtopic-cleanup as a follow-up pass.
- **`/notes` subtopic slugs are GLOBALLY unique, not chapter-scoped.** `question_concept_tags` keys on `(question_id, subtopic_slug, concept_slug)` with **no chapter or subject column**, and `loadConceptDrills` / the mastery-checkpoint query filter by `subtopic_slug` ALONE. So two chapters that both use a slug like `foundations` will cross-contaminate each other's drill lists + checkpoints. Bit us 2026-05-30: MHT-CET Indefinite Integration reused Sound's `foundations` slug — notes-lint caught it as "DB tag has unknown concept_slug" because the other chapter's concepts don't match. **Mitigation:** every notes subtopic slug must be unique across ALL chapters in `NOTES_CHAPTERS` (the URL segment can repeat per subject route, but the slug used as the concept-tag key cannot). MHT-CET's foundation subtopic was renamed `foundations` → `fundamentals`. notes-lint surfaces the collision but only *after* tags are inserted — pick distinctive slugs up front.
- **Renaming a SHIPPED chapter silently rots its `/guide` + `/notes` cross-refs.** The DB taxonomy and the guide/notes editorial TS are joined only by NAME at request time (`resolveTaxonomy`), so a Phase-D rename/merge leaves the TS pointing at a dead subtopic — the "Drill the N →" CTA resolves to an EMPTY `/browse` filter, no error. Bit us twice on 2026-05-30: the Sound notes 5→4 redesign broke the `/guide/nda-physics` Sound playbook + strategy `mustDrill` + CHAPTER_TABLE prose; the 2026-05-29 Probability merge ("Probability with Dice" → "Probability via Counting") broke two `nda-maths` principle drills + the Probability tier. **Mitigation:** after any rename/merge of a shipped chapter, sync `/guide` `_data` (playbooks `subtopics[]`, strategy `mustDrill`/`skipSubtopics`, principles `drill`, compounds filters, prose counts) + `/notes` `_data` + concept tags, then run `npm test` — the 7 playbook guides + CA themes + Maths each have a `describe.skipIf(!HAS_ENV)` live-taxonomy resolution test (`guide-nda-<subject>-playbooks.test.ts`; `guide-nda-maths-taxonomy.test.ts` for Template A) that fails on drill-target drift. Full procedure: [[shipped-chapter-rename-downstream-sync]]. The `/browse` backlink chip (`getQuestionResources` name lookup) + the single-page Economics chapter CTA are the cross-link surfaces still untested.
- **Integration-test fixtures leak into the LIVE bank when `afterAll` is interrupted — SOLVED STRUCTURALLY 2026-08-06: `npm test` now targets a DEDICATED TEST Supabase project, never prod.** The old world: every DB-integration test wrote throwaway rows into the single prod project, cleaned by `afterAll` + an org-keyed teardown sweep. The 2026-08-06 incident proved that net had **three structural blind spots** — org-less platform tables (`mock_tests`, `quizzes`), fixtures written INTO the real org by the `/api/sync/mock` route, and `@test.invalid` emails vs the `@test.local` sweep — and the "one universal signal is `org_id`" claim false. Leaked fixtures were publicly visible on `/mock`, `/browse` and `/quiz`; two real students sat a 1-question fake mock. **The fix:** test project `rjwuwmrzkyergflmmfxq` (separate free Supabase account → own quotas); `tests/setup.ts` + `tests/helpers/testdb.ts` resolve `.env.test.local`/`TEST_SUPABASE_*` and **hard-refuse** to run fixture-writing tests against any project ref not on the allow-list (fails closed on unknown refs). Read-only **prod-contract** suites (editorial ↔ live-content checks, listed in `tests/prodContractFiles.ts`) still run against prod via `npm run test:prod-contract`. Runbook + gotchas (idle pause, seed shape, missing-migration detection): `scripts/testdb/README.md`; `npm run testdb:reset` re-baselines the test DB. The replay also exposed that **a prod-applied migration whose `.sql` was never committed (0021, recreated) makes prod unrebuildable — `testdb:migrate` is now the standing check for that class.**

## Design axes

Load-bearing design distinctions for the project. Each one earns its place by being structural — getting the axis wrong has cost real work, and the answer to "which axis does this live on?" is non-obvious for the named feature areas.

### Tag taxonomies — principles (horizontal) vs concepts (vertical)

**`/guide` principles** and **`/notes` concepts** are different kinds of tag and must NEVER share a schema or be merged into one taxonomy.

| | Principles | Concepts |
|---|---|---|
| Axis | Horizontal — cross-subtopic | Vertical — within-subtopic |
| Cardinality | ~20 in NDA Maths | ~5–10 per subtopic × hundreds = thousands at scale |
| Storage | TS modules (`/guide/.../principles.ts`) + curated `extraQuestionIds` | DB table `question_concept_tags` (migration 0021) |
| Authoring | Editorial — hand-curated, slow update cadence | Per-upload — admin tagging UI or chat session, fast cadence |
| Value comes from | **Selectivity** (only the top-20, hand-picked) | **Coverage** (every relevant question tagged) |
| Identity question | "What unifying lens does this question demand?" | "What specific technique within this chapter?" |

**The wall — non-negotiable:**
- DON'T migrate `/guide` principles to a DB table. Hand-curation is the signal-to-noise mechanism; "scaling" them dilutes their value.
- DON'T bundle principles into `question_concept_tags` via a `tag_kind` discriminator. Flattens the strategic distinction.
- DO share infrastructure at the seam: one admin tagging UI on `/dashboard/questions/[id]/edit`, one notes-lint validator, future LLM-suggest can suggest both kinds. Sharing UI ≠ sharing schema.

**Why this earned a section:** The initial concept-tags proposal (Phase 1, 2026-05-17) was a unified `question_tags` table. User pushed back; I conceded. Both axes shipped separately (principles still in TS, concepts in migration 0021) and the wall has held. This section exists so future tag-related features (admin UI, LLM-suggest, AI tutor grounding, MHT_CET_AI integration) don't re-litigate it from scratch.

### Cross-link backlinks — chapter-level (automatic name lookup) vs tag-level (manual DB-tagged override)

The `/browse` QuestionCard chip row (Tier 1 + Tier 1.5, 2026-05-18) renders **two distinct classes of backlink** and the wall between them matters. Same dropdown of chips, two completely different authoring loops and data sources.

| | Chapter-level chips | Tag-level chips |
|---|---|---|
| Examples | "Playbook: Sound" · "Concept notes" | "Lever: Vieta" · "Concept: Weighted Mean" |
| Source | Name-based lookup at render time | DB row in `question_principle_tags` / `question_concept_tags` |
| Authoring cost | **Zero per question** — purely a function of `(exam, subject, chapter, subtopic)` | **Manual per question** — chat tagging session, INSERT with `ON CONFLICT DO NOTHING` |
| Coverage | 100% of questions whose chapter has a guide / subtopic has notes | Only questions explicitly tagged (today: 488 principle tags · 262 concept tags) |
| Failure mode | Silent break when DB chapter renamed (no lint catch yet — see ROADMAP "notes-lint guide-side rename validation") | Silent fallback to generic chip when slug doesn't resolve via `getPrincipleName` / `getConceptName` |
| Override behaviour | The default; renders for all qualifying questions | When present, **replaces** the chapter-level chip for the same axis (more specific wins) |
| Resolver | `getQuestionResources({exam, subject, chapter, subtopic})` in `src/lib/links/questionResources.ts` | `getQuestionResources({...}, tags)` — same function, second arg from `getResourceTagsForQuestions` batch |

**The wall — design intent:**
- DON'T expand the chapter-level lookup to do "fuzzy" matching when a chapter rename happens. The failure mode (chip disappears) is the correct signal — fix the rename, don't paper over the drift.
- DON'T attempt to back-fill tag-level chips from the chapter-level data (e.g. auto-create a `Lever: <chapter-name>` chip when no principle tag exists). That destroys the curated-vs-automatic distinction and floods low-leverage questions with meaningless tag chips.
- DO let tag-level chips quietly fall through to chapter-level chips when the tag's slug doesn't resolve. Better a generic "NDA Maths strategy" chip than a broken `/principles/<unknown-slug>` link.
- DO share infrastructure at the seam: both axes flow through the same `getQuestionResources(input, tags?)` function, both render via the same `<ResourceChip>` component in QuestionCard. The two-tier system is a function-signature distinction (optional second arg) not two separate code paths.

**Why this earned a section:** The 2026-05-18 Tier 1.5 ship added tag-level overrides AFTER the Tier 1 chapter-level chips were in production. The temptation in the moment was to refactor into "one unified backlink table" or to have tag-level chips deduplicate against chapter-level chips. Both ideas are wrong — they conflate two different authoring loops (curated tagging session vs no-touch render-time mapping) and two different coverage shapes (long-tail per-question vs blanket-per-chapter). The split shipped cleanly because the function signature preserved the asymmetry: `tags?` is optional, the resolver short-circuits chapter-level chips when a more-specific tag-chip exists, and untagged questions still get the generic chip — graceful fallback throughout. Sister wall to the tag-taxonomies axis above.

### Guide strand axis — execution-mode (chapter-partitioning) vs tier-style (chapter-tiering)

Within Template B variants, the **strand split** is the strategic axis the strategy page renders against. There are two fundamentally different ways to construct it, and they are NOT interchangeable — picking the wrong one produces a strategy axis that sorts questions by nothing meaningful.

| | Execution-mode strands | Tier-style strands |
|---|---|---|
| Subjects | NDA English (R/Rule/Reason), NDA Chemistry (R/Rule/Calculate), NDA Biology (R/Apply/Verify), NDA Geography (R/Apply/Verify) | NDA History (Cornerstone/Foundation Recall/Quick-Win) |
| Strand semantics | Each strand is a SKILL bucket (recall a fact / apply a rule / trace a mechanism / evaluate a statement). Chapters get grouped into a strand by their dominant execution mode. | Each strand is a STRATEGIC TIER (cornerstone weight / foundation recall / quick-win). Chapters get grouped into a strand by their bank share + %HARD profile. |
| Recall slot | Stable — there's always a Recall strand (named-fact recall is universal in Indian entrance exams). | Stable in spirit (Foundation Recall) but the slot SEMANTICS bent — it now groups chapters by tier, not by execution mode. |
| When to pick | Execution modes PARTITION chapters: one cluster of chapters is 60%+ dominated by one mode, others are 20%- on that mode. | Execution modes are UNIFORM across all chapters (25–45% everywhere) — can't partition. AND/OR one chapter has dominant bank share (40%+) that warrants its own Cornerstone tier. |
| Decision probe | For each candidate mode (recall/rule/apply/verify/calculate/reason/multi-statement/date-anchored), count `q × chapter_spread` AND per-chapter %. Top mode at 60%+ in one cluster = execution-mode strands work. | Top mode is 25–45% of every chapter = uniform = falls back to tier-style. Use bank-share + %HARD per chapter as the partition signal instead. |
| HARD calibration | Strand-level (densest-HARD strand called out — Geography's Apply at 23% avg HARD). | Implicit in the tier-structure — Cornerstone IS the densest-HARD tier by construction (History's Cornerstone = Modern India 34% HARD). |

**The wall — design intent:**
- DON'T force execution-mode strands when modes don't partition. History's multi-statement (70 q × 4 ch · 43% HARD) and date-anchored (61 q × 4 ch · 43% HARD) cut UNIFORMLY across chapters — a Bio/Geo-style R/Apply/Verify split would force lopsided cuts (Recall 44 q : Date 163 q : Verify 53 q if grouped by chapter's dominant mode). The strategy axis would sort by nothing meaningful.
- DON'T force tier-style strands when execution modes DO partition cleanly. English's Vocab/Idioms (Recall) and RC/Cloze (Reason) chapters are genuinely different SKILL types, not just different priorities — a Cornerstone/Foundation/Quick-Win split would lose that information.
- DO let the decision probe pick. Test execution-mode first (Template B default); fall back to tier-style only when modes are uniform.
- DO share infrastructure at the seam: both axes use the same `STRATEGY_STRANDS` data shape (each strand has `id`, `label`, `pitch`, `approach`, `chapters: StrandChapter[]`) and the same strategy-page renderer. The two-axis distinction is in the BUCKET MEANINGS (`PlaybookBucket = "recall" | "apply" | "verify"` for Bio/Geo vs `"cornerstone" | "foundation" | "quickwin"` for History) — same code path, different semantics.

**Why this earned a section:** The 2026-05-19 NDA History ship was the first time Template B's strand axis itself bent. Before History, every Template B variant had bent the second + third SLOTS within an execution-mode axis (Rule→Apply for Bio, Calculate→Verify for Chem) — but the axis itself was always execution-mode. History exposed the next level: when execution modes don't partition chapters at all, the axis itself has to change. The temptation in the moment was to force one of the existing slot-bend patterns onto History (most naturally R/Apply/Verify like Bio + Geo) — that would have shipped a strategy page where students see "Apply chapters" and "Verify chapters" labels but no chapter actually leans either way, sorting by nothing. The tier-style fallback shipped cleanly because the strand data shape was already general (chapters grouped under labelled strands with a pitch + approach) — only the bucket vocabulary changed. Sister wall to the tag-taxonomies + cross-link-backlinks axes above: in all three, the right call is to preserve an asymmetric distinction at the shared seam rather than to flatten for elegance.


### Content-organisation axis — book-structure (`section_*`, /board) vs conceptual (`subtopic`, /browse + /notes)

A board-textbook chapter is stored ONCE but organised on **two orthogonal axes**, and they must not be conflated:
- **Conceptual — `subtopic`** (`chapters`→`subtopics`, `order_index`): technique/topic grouping ("Combined Equation", "Angle Between Lines"). Drives `/browse` filters + `/notes` teaching order. A single book Exercise's questions are deliberately spread across several subtopics by concept.
- **Book-structure — `section_kind`/`section_group`/`section_label`/`section_seq`** (migration 0043): the book's physical layout (§2.1 Solved Examples → Exercise 2.1 → … → Miscellaneous), in reading order. Drives `/board` ONLY.

**The wall:** the `/board` reader groups by `section_group` (book section), NOT by `subtopic` — grouping by subtopic would shatter a single contiguous book Exercise into pieces under different headers. Conversely `/browse`+`/notes` never use `section_*`. The two axes are joined only by living on the same `questions` row. **Why it earned a spot:** the reader was first designed to group by `subtopic.order_index`; Matrices exposed that Exercise 2.2 (Q.1–6) splits across three subtopics, so the book axis had to be a separate, PDF-verified `section_*` outline (`scripts/stateboard/sections.ts`). Book order is NOT derivable from `subtopic` NOR reliably from the messy `question_number` string — it's authored per chapter and gated by `board:lint`. See [[board-reader]].

## Guide structure templates

The full template catalogue (A principles-first · B playbooks-first + variants · C chapter-playbooks+formula-sheet · single-page landing · D theme-prep-checklist), the per-subject variant tables, and the **"Choosing the template"** decision script live in **[GUIDE_TEMPLATES.md](GUIDE_TEMPLATES.md)** — **read it before proposing or building a new `/guide`**. Pick the template by analysing the bank first (bank-thinness gate → cross-chapter-lever survey → %HARD spread), never by copying precedent. Shared components (`GuideShell`/`GuideHero`/`StatBlock`/`BrowseLink`/`DriftTable`/`RelatedPlaybooks`/`ReferenceTables`) + the per-new-guide plumbing checklist (sitemap, Footer, /browse pill, parent cross-link, `questionResources.ts` mapping, `guide-strategy-headline` test) are documented there too.

## Engagement engine — principles gate (always-on)

The engagement engine ties PYQ Vault's scattered behaviour signals into a loop that improves learning + retention. **This gate is non-negotiable — like the "no slop" rule, every engagement mechanic must clear it before it ships.** Imported + adapted from the sibling English AI Tutor's hard-won engine (`Downloads/English_AI_Tutor`), whose findings we inherit rather than re-learn.

**Every mechanic must serve ≥1 learning principle** — else it's a dark pattern, not a feature:
- **Retrieval practice** — test recall, don't re-expose. **Spaced repetition** — resurface what the student got wrong until mastered (80%). **Immediate feedback** — results on the same/next screen, never a dashboard they must go find. **Deliberate practice** — drill the *specific* thing they missed, not "keep practising". **Mastery before progression** — gate on demonstrated 80%, don't reward accumulated low scores. **Metacognition** — every metric we track is shown to the student in plain language (trends, weak chapters, days-to-exam). **Growth-framed feedback** — name what went well + what's next, never a bare score.

**Forbidden (dark patterns — this is a stressed-teen exam-prep cohort):** leaderboards (demotivate the bottom half in a known-peer setting), hearts/lives (anxiety), variable rewards/loot boxes (exploitative), streak freezes/shields (complexity, no learning value), and **vanity milestones** ("logged in 3 days", "earned 100 XP" — awards must be genuine learning achievements).

**Two structural rules the AI Tutor learned the expensive way:**
1. **Measure the usage shape BEFORE building a mechanic.** They shipped 6 milestone kinds; 3 fired *zero* times because students study in **bursts, not daily** — daily streaks are silent demotivation for this cohort. For an exam app, **prefer deadline/goal-progress over daily streaks.** Read `user_activity` first; pick mechanics that fit the real data.
2. **Awards are server-side + idempotent** (dedupe_key), and **celebrations fire at the trigger surface** (the mock-result page), never a buried dashboard.

**The spine (built 2026-07-12):** `user_activity` (migration 0052) is the append-only event log every mechanic reads from — pure core `src/lib/activity/events.ts`, best-effort writer `src/lib/activity/service.ts` (never blocks the primary action), emitters on mock-submit / notes-mastery+checkpoint / bookmark, backfill `scripts/activity/backfill.ts`. **Signed-in only** for now (anon top-of-funnel stays SEO-clean). See [[project-engagement-engine]] + the Decisions entry below.

## Decisions log

Why behind architectural pivots — saves future-you from "why didn't we just…?".

**Convention:** topical intros (like "Foundations" below) sit at the top. Dated entries are bucketed by month — `### YYYY-MM` per month. When a new month begins, add a new `### YYYY-MM` sub-section above the prior one. **Within a month, entries are ordered newest-first (reverse chronological top-down): a new entry goes at the TOP of its month's block.** Net result: the most recent entry anywhere in the log is the first dated entry a reader sees after the foundations. This lets a reader collapse historical months mentally and focus on what's recent.

### Foundations (M1–M3, 2026-05-08)

The load-bearing choices made during the initial scaffold. Every later phase rests on these — re-litigate only with explicit reason.

- **No Prisma.** Supabase RLS is the security boundary; Prisma uses a single privileged DB user and bypasses RLS by default. Forcing per-request `SET LOCAL` JWT injection fights the tide. `@supabase/supabase-js` carries the user's JWT through naturally.
- **RLS helpers in `private` schema.** PostgREST exposes any `public.*` function as a `/rest/v1/rpc/<name>` endpoint. Supabase advisor flagged `current_user_org_id()` / `current_user_is_admin()` as callable by `anon` and `authenticated`. Functionally safe (returns `null`/`false` for anon) but defence-in-depth — moved to a schema PostgREST doesn't expose.
- **OMML via marker-and-post-process.** The `docx` library's `ImportedXmlComponent.fromXmlString` wraps imported XML in an `<undefined>` element that Word refuses to open. We render placeholder text where math should go, then patch `word/document.xml` after `Packer.toBuffer`. Sidesteps the library quirk; works for arbitrary OMML.
- **Word export = ZIP of two files.** First version was a single .docx with a 3-way layout toggle. User asked to split: a Question Paper distributable without leaking answers, and a separate Answer Key. One ZIP keeps the bundle obvious. **Superseded 2026-05-13** — ZIP killed for mobile UX; route now returns one .docx per request via a required `kind` selector.
- **Hard 200/export cap, no auto-split.** Vercel function timeout is the binding constraint. A clear error ("narrow filters") ships faster than a zip-of-zips. Revisit if a teacher genuinely needs 500 questions in one paper.
- **Chapters/subtopics auto-create, subjects don't.** Subjects are top-level taxonomy curated centrally; auto-creating "Phyiscs" from a typo would corrupt the canonical list. Chapters/subtopics are per-upload working data — small typo cost, and a merge tool can clean up later.

### 2026-09

- **2026-09-14 (third) — NDA II 2026 PAPER II (GAT) ingested: 150 q committed PRIVATE via a new `scripts/nda-gat/`, and Set D transcribed a SECOND time as a fidelity check that came back 134/134 with ZERO option permutations. The bank's other 18 GAT sittings all came from untracked `.xlsx`; this is the first with a source of record. Nothing is PUBLIC and no mock exists — the key is still to come.**
  - **NOT A FORK OF `scripts/nda-pyq`, and the criterion is the one cds-maths itself used.** That pipeline is Mathematics at MODULE level — one `SUBJECT`, one `SUBJECT_ID`, `QUESTIONS_PER_PAPER = 120`. A GAT paper carries NINE subjects and decides `subject` **per question**, which makes the catalog THREE levels — a TYPE change, so a re-export would not type-check. But the fork is small and that was measured rather than assumed: of the nine functions in the `cds-maths` pure core, **SEVEN re-export verbatim** (including `validateRows`, which already carries the duplicate-option, `content_hash`-collision, LaTeX-balance and pipe-table-separator gates, and `validateSets`), and only `validateCatalog` and `buildRecords` hard-code one subject — about 90 lines. **No shipped file was modified**, and the proof is that `cds-maths` and `cds-gs` keep all 104 of their tests green. `fidelity.ts` likewise IMPORTS `normText`/`scorePair`/`labelMap` from `nda-pyq/match-variant` (which guards its own `main`) rather than restating rules each earned from a measured failure.
  - **THE BILINGUAL RULE IS NOT A PARITY, and this is the fact that would have silently fed an agent Devanagari.** Part A (English, Q1-50) is printed in **ENGLISH ONLY** — it is an English test, so UPSC prints no Hindi counterpart — over six CONSECUTIVE pages 2-7. Only then does Part B alternate even=Hindi / odd=English. So the English pages are `2,3,4,5,6,7` and every ODD page `9…47`, 26 of the booklet's 46. The sibling Maths booklet is pure alternation, and carrying that assumption across would be wrong for the first six pages. Read off the footers page by page, never derived.
  - **`validateSections` is the one genuinely new gate, and it is not cosmetic.** `NDA_GAT_PAPER` declares two sections with HARD counts (english 50, gk 100) reconstructed by SUBJECT membership, so an English question mis-filed under Physics makes the sections 49/101 and the mock **fails to build with an error naming neither the question nor the cause**. The boundary is PRINTED on the page (`PART - A` p2, `PART - B` p9), so it is checkable against the source rather than inferred. It runs per-band in `check-bands` — where an agent can still fix its own band in context — as well as at merge and commit.
  - **THE FIDELITY PASS IS THE HEADLINE: 134 of 134 MATCH, 0 PERMUTED, 0 OPTION_MISMATCH.** Series D was transcribed a second time — terse stems, but **options read label by label** — and matched onto Series A by option TEXT. Two independent readings of two different photographs of two different booklets agree on option SET *and* label ORDER for every matched question, so **UPSC does not permute options between GAT series on this paper**. The defect it exists to catch is the one nothing downstream can see: the correct option's text landing in the WRONG LETTER's slot, which leaves a question that reads perfectly, passes every structural gate, and makes a blind derivation **confirm the wrong letter** — the mechanism that gave the sibling CDS English corpus 19 wrong keys while a full blind re-derivation returned `confirmed` on all 89 rows it saw.
  - **A PERFECT SCORE FROM A GREEDY MATCHER IS WORTH NOTHING UNCHECKED**, so the map was validated against two observations made BEFORE the tooling existed: an eye-check of Set A p4 against Set D p2 (done while still establishing the page layout), and the b4 transcription agent's independent block report (`A Q51-60 = D Q91-100`, `A Q61-66 = D Q76-81`). Both pass, the map is bijective, and every label map is the identity. **The map is a COMMITTED artifact keeping Series D's own numbering**, because a four-series solution document is wanted — the `nda-pyq` shape.
  - **THE MATCHER'S ONE REFUSAL WAS A NORMALISATION GAP, AND THE FIX BELONGS IN THE FOLD, NEVER THE THRESHOLD.** D-Q76 against base Q61 have byte-identical stems and options reading `\(45^\circ\)` against `45 degrees`; the imported fold reduces those to `45circ` and `45degrees`, so the option score was 0 and the pair fell under the floor. Lowering a floor that is protecting 133 other questions in order to admit one is how a wrong pairing gets in — teaching the normaliser a notation it genuinely did not know is not. A test pins that the four angles of that very question stay four distinct strings after the fold.
  - **SET D'S GAP IS EXACTLY Q21-36, and it is permanent.** Printed pages 4, 5, 34 and 36 are absent from that scan; 34 and 36 are Hindi versos and cost nothing, but 4 and 5 are Part A, which has no Hindi facing page to recover from. The 16 lost questions were pinned from the numbering jump (printed 3 ends at Q20, printed 6 opens at Q37) and corroborated by the `Directions` blocks either side. They cannot be cross-checked and **they will be a hole in any Series D solution document** until a more complete Set D scan turns up.
  - **SET A IS THE WEAKER IMAGE and the escape hatch was used exactly as designed.** Set A carries ~885x1184 real pixels per printed page against Set D's ~1150x1570, and two option blocks on printed p11 are backed by ~109x73 and ~85x24 real pixels — **measured, so zoom adds nothing**. They were closed from Set D under the rule that the MATCH must be established on independently legible content first, and the two values that came from Set D alone are flagged in the row. A later blind deriver then corroborated both on internal-coherence grounds it could not have faked: Q58's distractors are precisely the two physical readings of the stem plus the `mu = 2` arithmetic slip.
  - **THE RECONCILIATION WILL BE THE INVERSE OF THE MATHS ONE, and that is written into the config, the brief and the README before the key arrives.** On NDA Mathematics 2026-II all three key disagreements resolved AGAINST the key, because a maths answer is derivable from the printed page. **A GK fact is not** — "which committee recommended constitutional recognition for local bodies" has no derivation, only recall, where a single blind pass measures ~94% in this repo. So on the 100 GK questions the KEY is the stronger source and the default is to defer to it unless it is self-refuting; Part A (antonyms, PQRS, grammar) is genuinely derivable and gets the Maths treatment. Hence `merge-answers` prints the confidence split per SECTION rather than blended.
  - **THE CONFIDENCE SPLIT IS THE USEFUL PRODUCT, NOT THE ANSWERS: 127 HIGH / 19 MED / 4 LOW, 0 nulls — and all four LOW plus three MED sit in Q141-150.** That is not scatter. The paper's difficulty cliff lands exactly there: Q121-140 is near-verbatim textbook recall (19 of 20 HIGH) while Q141-150 is 2025-26 current affairs — a uranium supply agreement, an IBCA membership list, a Khelo India mascot. **When the key arrives the last ten questions are where the review effort belongs**, then the 19 MED rows, each of which names its runner-up and what would have to be true for it to win. The current-affairs deriver did what the brief asked rather than what looks confident, naming for each LOW exactly what it does not hold — which matters because a mislabelled guess does not cost one answer, it **corrupts the instrument**, since a HIGH disagreement with the key is later read as evidence the KEY is wrong.
  - **ROWS COMMIT PRIVATE AND THE GATE IS STRUCTURAL, PROVEN NOT ASSUMED.** `discoverSittings` filters on `visibility='PUBLIC'`, so the mock builder lists all 18 existing GAT sittings and does **not** list `nda-2026-sep-gat` — it cannot be published early by accident. That matters because `/mock` grades real students against a FROZEN score, so a later key fix would need every prior attempt re-graded by hand.
  - **TEN INDEPENDENT AGENTS RENDERED ONE PRINTED FEATURE THREE DIFFERENT WAYS, and nothing in the pipeline catches it** — because each band is internally consistent, so no gate fires. Eight stems print `not` in bold italic, which INVERTS the question; they came back as `**not**` (five), `\(\textit{not}\)` (two) and plain (one). Normalised to `**not**` on evidence rather than majority: 381 CDS rows use `**`, and it renders on BOTH surfaces (`KatexRenderer` parses rich segments, and `docxBuilder.mathRuns` turns it into a native Word bold run). **A cross-band convention split is a class of defect this repo has no automated detector for; the only signal is reading the flags every band reports.**
  - **A BARE `$` IS A MATH DELIMITER, and the blanket ban inherited from a maths pipeline is too blunt for a paper that prints CURRENCY.** `nda-gat/check-bands` suppresses that error only when EVERY dollar in the question is escaped, and one bare `$` keeps the error for the whole question — proven in both directions by injecting one. The hazard is real (a bare `$` pairs with another and swallows the prose between them, which reached shipped content once), and backslash-escaping the bare character **does not work** because there is no escape handling outside a math zone; `\(\$\)` is the measured-to-survive form, now pinned in `render-check`'s self-test.
  - **DEDUP WAS OVER-REPORTING BY DESIGN AND HAD TO BE MADE DECISIVE.** A stem-only scan flagged 26 matches — but `content_hash` folds the OPTIONS, and this paper legitimately carries five "Which is the correct sentence ?" items whose entire content is in their options, as does the previous sitting. Adding the option-set comparison gives the true answer: **0 collisions across 14,695 NDA rows**, with a positive control proving the scan reached real data. The commit then landed `inserted=150 skipped=0 failed=0`, exactly as predicted.
  - **A DEFECT IN THE PREVIOUS SITTING, found in passing, measured properly, and logged rather than fixed.** A band reported NDA I 2026's English pair-tables as missing their GFM separator row. Its count was low, and measuring it made the finding SHARPER rather than smaller: `GAT_NDA1_2026_QuestionBank.xlsx` carries **12 pipe-bearing rows across SIX subjects and ZERO separators**, while every other GAT sitting that carries a table has one. So it is not an English problem — that one sitting's ingest dropped the separator wholesale, and those rows render as literal pipes today. In the SUGGESTIONS backfill ledger: it is shipped content in another sitting, and `text` is hashed, so it is a delete-and-re-commit rather than an UPDATE.
  - **`scripts/*/_*` IS GITIGNORED, AND IT SILENTLY SWALLOWED TWO DATA-REPAIR SCRIPTS ACROSS TWO COMMITS.** The rule exists for a good reason — it keeps agents' in-flight probes out of commits — but `fix-glyphs.py` and `fix-negation.py` are NOT scratch: they are the auditable record of edits made to already-committed data, each asserting its before-state and refusing a re-run. The tell is that nothing tells you: `git add` reported success for every other file and the absence does not stand out in a 30-file `A` list. **Diff the file list you intended against `git ls-files` before believing a commit is complete**, and rename out of the rule rather than reaching for `git add -f`.
  - **THE PROBE-IS-WRONG RULE FIRED ON ME TWICE, in the same session in which I wrote it into three briefs.** A shell-escaped hygiene probe reported "82 double-escaped fields" against ten bands that had each verified their own file clean; re-authored as a FILE with four controls that must go red, the real answer is **0 across 790 fields**. And a leak scan on the blind packet reported `answer` and `flags` present — both were prose (`"mark your response on the Answer Sheet"`, and a vexillology option), while the key-level check showed zero unexpected fields. Five transcription agents independently hit the identical trap on their own probes.
  - Verified FROM THE DATABASE rather than from the scripts' own output: 150 rows, all PRIVATE, all `pyq`, all stamped 2026/Sep/NDA 2; `source_row` 2..151 with 150 distinct, matching the 18 existing sittings; English 50 entirely inside block 2-51 and GK 100 entirely inside 52-151; 0 rows without a solution, chapter or subtopic; every question exactly 4 options, exactly 1 correct, 4 distinct texts. `audit:text` clean on all six classes; `audit:omml` **0 failing zones across 150**; all 20 underline stems in the bypass-safe `\underline{\text{...}}` form with 0 fall-through; `audit-solutions` 0 rows without a solution and its single hit a false positive it warns about. All 150 carry an AUTHORED student-facing solution, so the 161-row reviewer-jargon leak the CDS Maths corpus shipped **cannot occur here**. `GATE_RESULT: PASS` (304 files / 4,719 tests), `db:backup` taken before the write. **Owed and stated rather than implied: nobody has seen these rows in a browser** — they are PRIVATE by design, so `/browse` renders them only to org staff, and the answer reveal and the 10 GFM pipe-tables are unproven at the render layer. See [[nda-gat-ingestion]].

- **2026-09-14 (second) — the OTHER THREE SERIES of NDA II 2026 Maths matched onto the base: Sets B, C and D at 120/120 each, all four answer keys derived, and a four-series answer-key + solutions `.docx` built (480 questions, 6,564 math zones). `scripts/nda-pyq/{render,match}-variant.ts` + `build-solution-book.ts` + `buildSolutionBook` in `docxBuilder.ts`. NOTHING was committed to the bank — the sibling series are the SAME 120 questions and `content_hash` would dedup every row.**
  - **THE USER'S QUESTION IS NOW ANSWERED WITH EVIDENCE RATHER THAN A PRIOR: across 358 matched questions, `0 PERMUTED` — no series reorders its options.** That was the whole reason they overrode a cheaper "fingerprint only" plan with *"option A+options of all 120 for each set. you never know if options are reshuffled"*, and they were right to: the earlier reshuffle check covered 3 questions from one block. It is now measured on every question of every set. **It remains a property of THIS paper, not a law** — the matcher derives each answer from option TEXT, so a future paper that does reshuffle produces the right letter automatically rather than silently inheriting ours.
  - **THE SHARPEST FINDING IS A CONVERGENCE, and it is what made the diagnosis cheap.** B and C are independently transcribed booklets and both initially failed on **exactly the same nine base questions**. Two independent transcriptions cannot agree on which nine to get wrong, so the common factor had to be the base-side normaliser — and it was: all nine were perfect transcriptions, failing on LaTeX-vs-terse synonyms (`\infty` vs `infinity`, `\int` vs `integral`, `\overline{X}` vs `Xbar`, `\ldots` vs `...`, and a `\frac` expansion that cannot nest, so `\frac{x^{3/2}}{3}` left a literal `frac` in the key). Fixed test-first; B and C went 111 → 120. **When two independent passes fail identically, stop suspecting either pass.**
  - **A GENUINE SILENT-MIS-KEYING DEFECT, found by the agent transcribing Set D and not by me: `normText` stripped every non-alphanumeric, so `+` and `-` vanished.** `-1` and `1` folded to one string (D-Q102), as did `… − 2^n + 1` and `… − 2^n − 1` (D-Q104). `labelMap` pairs greedily, so it could have **inverted two option labels with no error raised anywhere** — the one failure mode nothing downstream sees, because a refused match is REPORTED and a wrong map is not. Sign now survives, with typographic dashes folded onto the ASCII minus so keeping it cannot create a new mismatch class. **The rule the fix encodes: a character kept too eagerly costs a match the tool reports; a character stripped too eagerly costs a wrong answer letter. Prefer keeping.**
  - **THE AGENT OVERRODE MY PROMPT ON MEASURED EVIDENCE AND WAS RIGHT.** Set D's printed p39 (English, Q101-107) is absent from the source, so that band was read off the **Hindi** facing page. I instructed it not to render options into English. It ran the REAL `normText` as a control and showed that as-printed Devanagari options **collide**: Q101/Q103 (c) and (d) both fold to `iii`, Q106 (c) and (d) both to `x12` (min/max lives only in the Devanagari), Q102 (d) to the empty string. So it rendered them into English — every phrase corroborated from **this booklet's own parallel English pages** (printed 40 Hindi vs 41 English carry the same Q108-114 and agree label-for-label) — and reported the contradiction rather than resolving it silently.
  - **ONE QUESTION IS ADJUDICATED BY HAND, deliberately, rather than by loosening a threshold.** D-Q106 reads "Minimum value is at x = -2" against the base's "minimum occurs at \(x = -2\)" — two transcriptions of one question, worded differently. The correct pairing is the maximum of every row AND column of the similarity matrix, but at **0.38 against a 0.32 runner-up**, nowhere near the 0.6 threshold protecting the other 119. Loosening a global rule to pass one hard case is how a wrong key gets in. New `data/<id>-<S>.labelmap.json` + `adjudicatedLabelMap`, which **asserts the option texts it was written against on BOTH sides** and refuses otherwise; an adjudication that never fires is an error, and one the matcher later resolves ITSELF and disagrees with throws. Both guards fault-injected and proven to fire.
  - **A QUADRATIC DEFECT IN SHIPPED CODE, exposed by this workload and fixed: `patchZip` rescanned the WHOLE of `document.xml` once PER math placeholder.** Invisible at the export route's 200-question cap; fatal at **10,268 zones** across a multi-megabyte document — ~100 GB of scanning plus one multi-MB allocation per marker. And being SYNCHRONOUS it blocks the event loop, so **the test could not even be timed out**: it ran past 420 s without vitest's own 120 s timeout ever firing, which is itself the diagnosis. One pass with a callback instead: **>420 s (never finishing) → 1.2 s.** Two safety improvements come free — a replacement is inserted verbatim, where the old string form gave `$&`/`$1`/`$'` inside the OMML their special meaning, and a replacement is never rescanned. **This is a change to the live Word-export path**; it is gated by all 12 docx/OMML suites (144 tests) passing untouched, plus a new scale test that stops COMPLETING rather than going red if the quadratic form returns.
  - **A PROBE BUG worth recognising by sight: `<w:t[^>]*>` also matches `<w:tbl>`, `<w:tc>` and `<w:tr>`**, because `<w:t` is a prefix of every table tag. The lazy body then swallows each table's preamble, so the verifier reported all four answer-key grids as EMPTY while the document was perfectly correct. `<w:t(?:\s[^>]*)?>` is the fix. Same shape as [[probe-must-reach-the-code]] — the probe contradicted much stronger evidence sitting beside it (a 3.6 MB file whose headings it had already found).
  - **The document prints series A's text for every series, and takes from each map only the two genuinely per-series facts: the question NUMBER and the option LABELLING.** B/C/D were transcribed only well enough to be IDENTIFIED (`VARIANT_BRIEF.md` says so in its own opening), so their terse text must never reach a printed page. Numbering is printed as TEXT rather than Word auto-numbering, which rides one shared counter and would run 1-480 straight through four 120-question sections.
  - **Verified from the FILE, not from the builder's own report** (`verify-solution-book.ts`): all four key grids read back out of the table cells and **agree with the committed maps question by question**, all four solution sections number 1..120 and carry 120 answer lines, 6,564 math zones converted, **0 unconverted placeholders** (the silent failure, where Word prints `OMML_412` in place of a formula). The comparison arm was fault-injected — corrupting two rows of the C map made it name exactly questions 7 and 41. Independently: all three maps are bijective onto 1..120 and every derived answer equals the base answer at its mapped label, with the key distribution identical across all four series (A 26 · B 36 · C 38 · D 20), as it must be when nothing is permuted. **Owed, and stated rather than implied: nobody has opened the .docx in Word.** The gate proves it builds and the verifier proves what is in it; neither proves it lays out.

- **2026-09-14 — NDA II 2026 Paper I (Mathematics) ingested AND PUBLISHED: 120 q PUBLIC, `question_kind='pyq'`, plus the 19th NDA Maths mock (`nda-2026-sep-maths`). New `scripts/nda-pyq/` — the FIRST NDA sitting in this bank with a source of record.** NDA Mathematics PYQ 2,160 → **2,280 across 19 sittings**; mocks 192 → 193.
  - **THE EVIDENCE STANDARD, and it is NOT the two blind passes the sibling CDS pipeline runs.** One blind derivation, written BEFORE any key existed so it could not be contaminated by one, then diffed against a coaching institute's **independently produced** key (Centurion, series A). **117 of 120 agreed; all 3 disagreements resolved AGAINST the key**, each read off the original printed booklet. **There is no official UPSC key for this sitting and none is expected** (the user's call — the key is an independent source, not a draft of something official), so this is final rather than interim. Stronger than two blind passes on one point — the second opinion is a separate SOURCE that cannot share our failure modes, where two runs of one model can be wrong the same way — and weaker on another: a row where both we and the key are wrong has no third voice. `DERIVED_MODEL` says exactly this rather than inheriting the CDS wording.
  - **THE THREE DISAGREEMENTS, all the key's, two of them arithmetic and one a reading.** **Q4** (key B, ours A): `sin18 cos36 = 1/4` exactly at 40 dps, and the key's 1/2 is precisely what `2 sinX cosY = sin(X+Y) + sin(X−Y)` gives WITHOUT the halving — a dropped factor of 2. **Q11** (key A, ours C): `6 sin t − 8 sin³t = 2 sin 3t = 1` exactly, and the key's 1/4 is HALF of `sin 3t` where it should be TWICE — and Q4 and Q11 carry an IDENTICAL option set, so the key errs on both by a power-of-two factor on a standard identity (noted as an observation, not asserted as one careless pass). **Q62** (key A, ours C): `β = 90−α` forces `cos γ = 0`, so both statements concern `cos α + sin α` on [0,90] with range [1, √2] — max √2 and min 1, the latter attained at the x- and y-axes, ordinary lines with direction cosines (1,0,0) and (0,1,0). The key's "I only" needs those excluded and nothing excludes them. **This is the one resting on a READING rather than arithmetic, and the blind pass had NAMED "I only" as its runner-up in advance on exactly this ground** — the documented runner-up tendency firing again.
  - **THE SERIES COLUMN IS THE WHOLE RISK ON A MULTI-SET KEY, and `parse-key.ts` is built around it.** The key prints SET-A..SET-D side by side and **the four series are independently scrambled**, so reading the wrong column does not fail — it yields 120 confident WRONG entries and a mismatch list that reads as a transcription catastrophe. The script REFUSES a series that disagrees with the booklet cover, and ASSERTS the three-block row arithmetic (`n`, `n+40`, `n+80` with twelve A-D letters) rather than assuming it. The extraction was then verified **twice by independent methods** — reading-order and spatial-by-x-coordinate against the SET-A header position — with **0 disagreements on all 120**. A test pins that a different `seriesIndex` yields different letters, and was proven to go red.
  - **THE KEY'S OWN DISCLAIMER CLAIMS STAR MARKS ON DOUBTFUL ANSWERS AND THERE ARE NONE** — checked on the rendered page, because the text layer carries no asterisk and a star drawn as vector art would have been invisible to a text parse. The yellow bands that look like highlighting run at the SAME height across all three blocks, i.e. they are the watermark. Recorded so a later reader does not hunt for a confidence flag this key does not carry.
  - **CALIBRATION HELD IN THE DIRECTION THAT MATTERS: the blind pass's two MED rows and its one LOW row ALL agree with the key, and every disagreement is on a HIGH row.** That includes the two the deriver had flagged as source defects — **Q106**, whose three givens are mutually inconsistent, and **Q112**, which prints both inequalities as the exact reverse of the standard results. Both AGREE with the key, which means the paper intended them as printed and vindicates the solve-as-printed rule twice over.
  - **All 120 recorded in `question_reviews`** under `source_key_crosscheck`, **0 stale hashes**. Every verdict is `confirmed`, including the three disagreements, and the reason is that the vocabulary does not fit otherwise: nothing of ours changed (so not `key_fixed`/`stem_fixed`), and the defective artifact is a third-party key that is not part of this corpus and cannot be `defect_preserved` in it. The agreement-vs-adjudicated distinction lives in the per-row note.
  - **Verified after publishing:** 120/120 PUBLIC, all carrying `derived_model` + `derived_at`; the mock is `published / pyq / full`, 120 distinct refs, positions contiguous 1..120, every ref resolving to a live PUBLIC row of THIS paper with exactly one correct option, and **snapshot order == `source_row` order** (the paper's own sequence preserved); the other **18 NDA Maths mocks untouched** (exactly 1 row updated). `flip-public`'s five gates — provenance, figures, solutions, option integrity, coverage — all passed, and it reverts in one command. The 18 sittings already there (2017-I … 2026-I) all arrived as `.xlsx` through the generic `/upload` path and **not one `.xlsx` is tracked in this repo**, so none of them is reconstructable, re-checkable or re-committable, and there was no NDA-PYQ pipeline to extend.
  - **NOT A FORK. `lib.ts` re-exports the `scripts/cds-maths` pure core VERBATIM** (the `mh-sb-9` pattern) — same animal: scanned maths booklet, zero text layer, no key, shared-stimulus sets, per-question chapter. The two divergences ride as **default-off options on those shared functions**, so CDS is byte-identical and its four suites staying green is the proof: **`strictSubtopics`** (NDA's taxonomy is CLOSED after a bank-wide cleanup, so an unlisted subtopic is a near-miss that `commitStaged` AUTO-CREATES and which splits a chapter's corpus in two — where cds-maths is still extending its catalog and correctly treats it as a work item) and **`sourceRowOffset`** (all 18 siblings run `source_row` 2..121 from their `.xlsx` origin). Fault-injection proved exactly the two new tests go red when the options are removed, and nothing else.
  - **THE PAGE-SELECTION PRE-PASS THAT `cds-maths` LISTS AS "STILL TO BUILD".** This is a RAW bilingual booklet — Hindi and English alternate page by page — and with **zero text layer** (0 extractable characters across all 44 pages) `scripts/upsc/classify-pages.py` has nothing to count. **English is the EVEN 0-based indices 2…42, established by reading a contact sheet of all 21 candidates individually, not inferred from parity.** The obvious heuristic does NOT separate this booklet and `config.ts` records why: shirorekha long-run counts are 42-161 on the English pages against 136-390 on the Hindi ones and **those ranges OVERLAP** (p08=157 and p40=161 are English; p29=136 and p39=158 are Hindi), because handwriting and the scan's dark edges push an English page up.
  - **FOUR MEASURED PAGE HAZARDS, each written into the transcription brief before dispatch, and the second one paid for the whole exercise.** A large yellow watermark crosses mid-page text; **show-through from the reverse face looks exactly like option labels** (the foot of printed p31 carries ghost `(b)`, `(a)`, `0 (d)` belonging to no question there); student pen-working fills many margins; and a Devanagari page would mean `englishPages` is wrong and is a finding, not a page to transcribe.
  - **THE SHARPEST CATCH: the shared premise of the Q115-117 set APPEARS to end `= k`, and that `= k` is STUDENT PEN.** Found by the transcribing agent matching the glyph and ink weight against the same candidate's margin working beside Q115, and verified independently at 8x — the three printed `=` are thin and level, the fourth is a thick slanted double-stroke followed by a cursive `k`, in the same ink as a `15` scribbled above. Taken at face value, **three questions would have shipped with an invented symbol in their shared context**. The printed premise is `P(A)/6 = P(B)/3 = P(C)/4 = P(D)/2`, which is self-consistent and needs no constant (6+3+4+2 = 15, and mutually-exclusive-and-exhaustive fixes every probability).
  - **A TRANSCRIPTION FLAG OVERTURNED BY A DERIVATION AGENT, with decisive evidence.** Q72's denominator prints `(ax + bx + c)^2` with no superscript, flagged as "almost certainly a misprint for `(ax^2 + bx + c)^2`". It is not: under the "intended" quadratic reading the integral evaluates to 0.06008, **which matches NONE of the four printed options**, while the printed linear form collapses to `1/(c(a+b+c))` and matches one EXACTLY. **The printed form is the only reading under which the question is answerable** — the "solve as printed, let the gate adjudicate" rule vindicating itself, since a helpfully-corrected derivation would have produced an answer matching no option.
  - **SINGLE blind pass, deliberately, with the key arriving afterwards — and that ORDER is the point.** `cds-maths` runs two passes and crosstabs; this runs one, written **before any key exists**, so it cannot be contaminated by one. That is what makes a later disagreement mean something: on `cds-maths`, of five rows where a blind pass disagreed with a prep-house key, **FOUR were the KEY's error and ZERO were ours**. `reconcile-key.ts` diffs and **applies NOTHING**, ordering disagreements HIGH-confidence-first because the HIGH band ran 1,337/1,358 on UPSC papers here, so a HIGH disagreement is where the KEY is likeliest to be the defective one.
  - **ROWS WERE HELD PRIVATE UNTIL THE KEY LANDED, AND THE GATING IS STRUCTURAL RATHER THAN A CONVENTION.** A single pass measures ~95% on UPSC papers here — about 6 wrong in 120 — and `/mock` grades real students against a FROZEN score, so a later key fix needs every prior attempt re-graded by hand. Verified at the time rather than asserted: **`discoverSittings` filters on `visibility='PUBLIC'`**, so while the rows were PRIVATE the builder reported 18 sittings, not 19, and the mock could not be published early by accident. Both were released together once the cross-check was done.
  - **TWO ROWS THE KEY WAS MOST INFORMATIVE ABOUT — and it AGREED with us on both.** Flagged by the deriver rather than smoothed over: **Q106 is genuinely defective** — its three givens are mutually inconsistent (`P(A-bar ∩ B-bar)=1/2` forces `P(A)=P(B)=1/2`, contradicting `2P(A)=P(B)`); the ∩ glyphs were verified at 3.2x so the transcription is faithful, and the runner-up 1/3 is the only self-consistent reading, reached by discarding a stated datum. **Q112 prints BOTH inequalities as the exact reverse of the standard results**, solved as printed to "Neither I nor II" at MED with "Both I and II" named, because a simultaneous reversal of two textbook inequalities is likelier a source misprint than a real question.
  - **MY OWN WORST BUG, and it is the one this file's own header warns about: `merge-answers.ts` globbed `<id>.d[A-Za-z0-9]+.json`, which matches `<id>.derive.json`** — "d" plus "erive" — so the blind PACKET was read back as a set of derivations and every question appeared twice. Caught on the first real run only because it surfaced as 30 duplicate-number errors; a quieter version of the same glob has silently ingested non-question files AS QUESTIONS in two sibling pipelines here. Fixed to `d\d+` **and** given a fail-CLOSED shape guard, because a name filter goes stale the moment someone adds an artifact and the failure is silent — the packet parsed perfectly and simply had no answers in it.
  - **A PROBE I HAD TO ADMIT WAS UNFIT RATHER THAN KEEP TUNING.** `dedup-check.ts` reports two things and **only one is trustworthy**. The **EXACT normalised-stem match is decisive** (it over-approximates `content_hash`, so it cannot miss a collision): **0 matches across 7,314 NDA Mathematics rows**, with a positive control of 49 "eccentricity" rows proving the probe reached the data — and the commit then confirmed it at `inserted=120 skipped=0`. The **fuzzy similarity is a weak reading list**, and that is a property of NDA Maths rather than an unfixed bug: the stems are short, heavily templated ("Consider the following statements … Which of the statements given above is/are correct?") and their real content is mathematical structure that tokenises poorly. **Four successive fixes each improved it and none made it decisive** — plain Jaccard scored **0.94 between a `d/dx ln|x|` question and a vector-triple-product one**, and even with numbers kept, options included, IDF weighting and LaTeX command names retained as tokens, `lim sin^2 x/(x|x|)` still scores **0.99** against `lim f(x)/g(x)`. Recorded in the script as "similar SHAPE, never probable duplicate", with an explicit instruction not to tighten the threshold to make the list look clean.
  - **Verified from the DATABASE, not from any script's output:** 120 rows, 0 PUBLIC, `2026 / Sep / NDA 2`, `source_row` 2..121 with 120 distinct, 0 rows lacking a solution / chapter / subtopic, 16 rows across 7 sets with identical contexts, exactly one correct option and four distinct options on every row, and **the taxonomy unchanged at 31 chapters / 111 subtopics** — the `strictSubtopics` gate's whole purpose. 1,673 math zones through the app's own KaTeX; `audit:text` clean on all five classes; **`audit:omml` 0 failing zones**, so a teacher's Word download renders every zone; `audit-solutions` 0 flagged. `db:backup` taken before the write. Gate PASS (300 files / 4,651 tests).
  - **Owed, and stated rather than implied: nobody has sat this mock in a browser.** The runner is client-interactive behind auth, so the gates prove the snapshot is faithful, deliverable and gradeable and NOT that it lays out — worth sitting one end to end. Also logged and NOT acted on: **nine FORCED SUBTOPIC FITS** (Q25 divisor-counting, Q63 area-from-3-D-vertices, Q46, Q72, Q93, Q94, Q100, Q116, Q117), each flagged rather than invented. Whether any is a genuine gap in the closed NDA taxonomy is a deliberate decision about the bank, not something a transcription agent should make. See [[nda-pyq-ingestion]].

- **2026-09-12 (second) — THE PAPER PUSH LEARNS SITTINGS: one paper conducted for batch A on Monday and batch B on Thursday is now two tracker exams, not a 409. Migration 0097 `paper_pushes` + `sittingNo` on the push contract. Sitting 1 is byte-identical to before, so the nine live drafts are untouched.**
  - **THE DEFECT: `exam_vault_${paperId}` derived the tracker exam id from the PAPER, so one paper could only ever be one exam.** Push → conduct for batch A → upload results → re-push for batch B → GUARD 1 refuses (`409`, "already has N result rows"). The second conduct was unreachable, and both workarounds are bad: pooling both batches into one exam collapses them into a single `question_item_stats` row that can never be separated again, and cloning the paper vault-side splits `paper_questions` so `getQuestionUsage`'s per-batch no-repeat warning goes blind.
  - **THE TWO HALVES OF THE SYSTEM ALREADY DISAGREED, and only the push key was wrong.** `question_item_stats` (0095) is grained per (question, source, `source_ref` = the tracker exam id) precisely so two sittings stay separable, and `cohort_label` is comma-separated because one sitting routinely runs for several batches. Storage assumed one paper -> many sittings; the push assumed one -> one.
  - **AND POOLING IS NOT ACADEMIC — it would have hidden a real defect.** The `∫₀⁴|x−1|dx` row reads *APJ batches 10 attempted / 0 correct / 10 verdict mismatches* against *LWS 2Y 20 / 2 / 0*; that gap is what showed one sitting had been graded against a different answer. Under one exam id it reads "30 attempted, 10 mismatches" and the sitting is unidentifiable.
  - **I ASSERTED THE PUSH WAS UNBUILT, FROM A STATUS TABLE, AND IT WAS SHIPPED END TO END.** `HANDOFF_TRACKER_SYNC.md` said *"Paper push — not built. Specced only."*; `handlePaperPush`, `paperPush.ts`, the server action and the button were all live. **This is the 2026-09-11 lesson repeating within a day** — a doc's status line is a claim, not evidence. The user challenged it rather than accepting it. That table now carries a warning at the top telling the next reader to `ls` what it describes.
  - **THE USER'S PROPOSED TRIGGER ("pop the dialog once the app detects results") HAD TO BE WIDENED, and their OWN commit that morning is why.** `d2376d4` made Update Results the surface that sets date, marking, subject, batch and branch — which means **batch is only filled AT results upload**, and `date` defaults to the PUSH date (all nine live drafts read `2026-09-12`). So of the three signals I proposed, "date in past" fires on every draft older than a day, "batch filled" is simultaneous with results, and results LAG THE CONDUCT by days. **The tracker cannot know a conduct happened until the Evalbee sheet arrives** — measured, not assumed — so the trigger is **"a sitting exists"**, never "it has results". Cost: one confirmation on the re-push of a draft nobody has sat. Benefit: no silent overwrite in the conduct→upload window.
  - **A CONSTRAINT NOBODY HAD NOTICED: the exam NAME is vault-owned forever.** `ReuploadResultsModal` edits date, marking, subject, batch and branch — **not `name`** — and every push overwrites `name` from the vault. Two sittings would therefore be two rows with the same name, the same date and both `batch` null, indistinguishable at exactly the moment faculty must pick one to file results into. So `pushTitle` is load-bearing rather than cosmetic: a teacher-typed label (`NDA Mock 7 — Batch B`) or the fallback `(sitting 2)`. The label is captured at push time because that is **the only moment a human knows what the sitting is for**, and nothing downstream can add it later.
  - **`d2376d4` is also the prerequisite that made this buildable at all** — before it, a second pushed exam could never have been completed, because no surface set its date or batch.
  - **NO BACKFILL, AND NONE IS NEEDED — the 409 IS the backfill.** The vault kept no record of which papers were pushed and the tracker exposes no list endpoint (12/12 Hobby functions). Both outcomes of a first push under the new code are already correct: a pushed DRAFT (all nine, zero results) goes through as sitting 1 on the unchanged id, and a pushed exam WITH results returns `409 code:'has_results'` — **which is itself the evidence the sitting exists**, so it is recorded from the refusal and sitting 2 is offered. The teacher reaches the outcome they wanted in one extra click.
  - **THE CLIENT PICKS AN INTENT, NEVER A NUMBER — changed mid-build after spotting the flaw.** The first cut had the UI send `sittingNo`, so a tab open since before someone else pushed could name a sitting already taken; it needed a guard and an error message. Sending `{intent: "new" | "update"}` and allocating the number server-side from freshly-read rows deletes the whole class, the guard and the message.
  - **Numbers are ALLOCATED, never recycled** (`nextSittingNo = highest + 1`): a sitting deleted tracker-side must not free its id, because the exam may still be there and reusing it would silently overwrite a conducted paper. `paper_pushes_unique_exam` is the backstop — two papers can never claim one tracker exam.
  - **Verified rather than asserted:** all four CHECK/unique constraints **proven to fire** in a self-aborting `DO` block that also accepted a valid sitting 1 AND a legitimate sitting 2 (a block where nothing is accepted proves nothing), then rolled back — table confirmed at 0 rows, RLS on, 2 policies. 7 new tracker cases and **the id derivation fault-injected**: reverting it turns exactly the two sitting-id tests red and nothing else. 15 new vault pure cases + 7 RLS integration cases, the cross-org read asserted against a paper that **demonstrably holds two rows** rather than an empty table. Migration replayed into the test project so prod and test cannot diverge.
  - **RLS mirrors 0058's predicate through the PARENT PAPER, deliberately not a bare `org_id` check** — otherwise a teacher could read the push history of a paper the papers SELECT policy already hides from them, re-opening the gap 0058 closed one table over. No UPDATE or DELETE policy: a push is an event that happened, and deleting a row would free a sitting number the tracker still holds.
  - **Owed, and stated rather than implied: the browser click-through.** The chooser is an inline panel inside a Radix Dialog that does not exist in the DOM until the Download button is clicked, on an auth-gated `ƒ` page — so the gate proves it compiles and the tests prove the logic, and **neither proves it lays out**. Worth pushing one paper twice end to end.


- **2026-09-12 — the LWS "11th Question paper" folder ingested: 9 printed tests / 465 questions → 9 `/dashboard/papers` papers + 9 OMR sheets, 317 rows PUBLIC. THREE of the folder's twelve PDFs were already in the bank and were NOT re-ingested. The dedup-first rule paid for itself twice over, and the most useful findings are three claims of mine that agents overturned with evidence.**
  - **DEDUP BEFORE DERIVING, and the spread is the argument.** The runbook's rule-one ran on all 465 stems before a single answer was derived: **132 already banked, 5 uncertain, 328 new.** It is wildly uneven — **AP-GP is 19 of 20 already in the bank** (a mechanical pre-scan had predicted 6, so the semantic pass was worth 13 questions of derivation on one 20-question paper) while **the two Chemistry papers are 77 of 80 new**. Three PDFs were re-prints of already-ingested `.docx` papers (`lws-pqrs-test-1` 30/30 stems, `lws-pressure-belt-pos` 61/61 records, `interior-earth-pop-50` 46/50 positional — the 4 "differences" being a page header injected mid-stem and a Directions line folded into the spec's stem). Identified by stem matching, never by filename.
  - **THE DEDUP PACKETS WERE VALIDATED BEFORE USE, WHICH IS WHY THEIR VERDICTS ARE WORTH ANYTHING.** Candidate pools were built by lexical retrieval and scored against THREE known-duplicate papers: **108 known duplicates, 108 in the top-6 pool, 100% recall.** A pool that silently missed duplicates would have produced a confident, unfalsifiable "all new".
  - **THE BINDING CONSTRAINT WAS THE UNDERLINE, AND IT DID NOT NEED VISION.** All three English agents independently reported the same wall: the text layer drops underline markup entirely, and in a parts-of-speech question the underline IS the question ("The car that he bought is very expensive" is unanswerable until you know whether `that` or `very` is underlined). That implied a vision pass over ~180 questions. It did not: the underlines are **drawing primitives** in the PDF (no span flag carries them), recoverable geometrically, and scored **53/53 against independent ground truth** — bank rows whose stems already carry `\underline` markup from a different process months earlier. **My first attempt returned ZERO** because the tolerance window was off by 0.4pt (the rule sits marginally ABOVE the glyph box's descender line, gap −1.9 against a floor of −1.5); the fix was measuring the geometry, not guessing at it. **And my "53/53" overstated what the check could see** — it compared after stripping punctuation, so it was blind to whether the rule covers a trailing stop. Measured properly, **29 of 194 (14%)** wrongly included one; the agents fixed those against the page at 9×.
  - **PRINTED OPTION ORDER WINS, ALWAYS — and this is a correctness rule, not a preference.** The runbook says to fill a `dup` row from its matched bank row. Taken literally that mis-grades every student who sat the printed sheet, because the bank's option ORDER need not match the paper's. Live instances: **Cloud Test Q2 and Q25 are the same fact with Nimbostratus printed at C and at D** (the bank row keys C); **Current Electricity Q14's bank twin keys C for 3 ohm where this paper prints 3 ohm at (b)**; Parts of Speech Q119 is D here and B in the bank. Across Cloud Test's 8 self-repeating pairs, **5 of 8 resolve to a different letter from their twin**. So `dup` controls VISIBILITY ONLY; the key always comes from this paper's own printed order.
  - **DUAL-BLIND ON THE NUMERICALS: 149 AGREE / 1 CONFLICT over 150 rows**, pass 2 hard-isolated (blind files only, no bank access, no sight of pass 1) and required to return the computed **value**, not just a letter — which is what distinguishes a wrong key from a "twin" (the same correct value printed at two labels, where the repair is the option text and never the key). The single conflict (chem-50 Q3) was hand-adjudicated from the data: with the intended 2.4 g, sample 1 gives an O:Fe mole ratio of **exactly 1.5000** (Fe2O3) against sample 2's 1.3565 (~Fe3O4), so two different oxides of the same elements = multiple proportions; constant proportions is excluded because it would require the two iron percentages to be EQUAL and they are 70.0% and 72.07%. **Calibration stated rather than implied: agreement bounds DISAGREEMENT risk, not correlated error** — this project has measured agreeing passes at 94.4% correct on UPSC — so 149/150 is not 99.3% accuracy.
  - **THREE CLAIMS OF MINE THAT AGENTS OVERTURNED WITH EVIDENCE, which is the most transferable part.** (1) I told the CET agent that reaction stoichiometry files under `Laws of Chemical Combination and Percentage Composition`; it checked the live rows and pushed back — **every CET row under `Stoichiometry and Concentration` IS reaction stoichiometry and none is a concentration question, while every row under `Laws…` is a law or percentage composition and none is reaction stoichiometry.** I had read the subtopic from its NAME rather than its CONTENTS. (2) I put "the correct value is a different option so the key is unaffected" for chem-50 Q28 into a derivation brief, taken from a single aside in a dedup report; **both blind passes contradicted it and they are right** — 88 g CO2 = 2 mol = 4 mol O atoms = 24.088e23, which is exactly what is printed at BOTH (a) and (b), so the duplicate IS the answer and a correct student is marked wrong. (3) I told the user the two Chemistry papers "carry no batch label"; they are headed **"NDA Practice Test (Objective) / Batch:- 11th"** and I had read a truncated text dump whose first 260 characters put the footer before the header. **Verify a claim about the source before putting it in another agent's prompt.**
  - **CHEMISTRY WENT TO MHT-CET, NOT NDA (user's call), and that made the taxonomy CHEAPER rather than harder.** Under NDA it needed three new subtopics; under CET it needed **one** — `Empirical and Molecular Formula`, a name already live under JEE Mains and NEET — because CET's `Some Basic Concepts of Chemistry` already carries the other four. Four questions (three redox molar-ratio, one iodometry) belong in CET's existing **Redox Reactions** chapter, so both papers span two chapters. **Moving the target exam invalidated the dedup**: `content_hash` is per-(org, exam), so the NDA verdicts would have deduped nothing, and the gate was re-run against the 2,190-row CET Chemistry corpus (77 of 80 new).
  - **14 QUESTIONS ARE `flawed` AND STAY PRIVATE, AND TWO OF THEM MIS-GRADE ON THE OMR IN A WAY THIS PIPELINE STRUCTURALLY CANNOT FIX.** The tagged Excel has 16 fixed columns with ONE `Answer` cell and **does not grade**: per nda-tracker's own grading invariant, `exam_results.responses` is Evalbee's verdict and `questions[].answer` drives only the displayed answer, solution and per-question analytics. So "accept both letters" and "non-scoring" are **Evalbee-side settings**, and the action list is: **chem-50 Q28** (correct value printed at two options — a correct student is marked wrong) and **atmosphere Q54** (printed labels read A/B/B/D, so the wrong third option is credited), plus 12 questions to mark non-scoring. What the pipeline CAN do, and did: prefix each defective row's `solution` with a `[PRINTED DEFECT]` warning naming option VALUES (never letters, which would trip `audit:keys`), since `Solution` is the only free-text column that reaches the tracker.
  - **A live BANK wrong key found by two agents on two different papers, verified, logged, not fixed.** Row `cf616452` keys *Otherwise* as a **Conjunction** and its own solution says "it connects the two clauses" — but the stem carries a FULL STOP, so those are two SENTENCES; the stated reason is refuted by the stem's own punctuation, and the row is unreviewed prose with no `Matches option X.` line. Its sibling `e681c7b2` puts *Besides,* in the identical position, keys **Adverb**, and carries a reviewed solution saying "it cannot be a conjunction because there is a full stop before it". Adverb is correct. In the SUGGESTIONS backfill ledger with a note that this is likely a CLASS (however/therefore/moreover keyed as conjunctions), not one row. **Eight duplicate PAIRS** were separately verified and logged the same way — 2 of the 9 originally claimed did NOT survive checking, which is the point of checking.
  - **Two required schema fields were missing from my transcription brief — `difficulty` and `subtopic` — and both matter because `check-taxonomy` HARD-FAILS on an unknown name while a NEAR-miss silently auto-creates a duplicate subtopic and splits the chapter.** Backfilled by agents; the subtopic pass anchored on the **123 rows already carrying a `bankId` and agreed with their live filing 123/123**, and the bank corrected it twice (two series questions it had under *Special Series* are filed by the bank as Geometric/Arithmetic Progressions).
  - **Figure extraction gained one durable lesson:** `extract_image` returns a soft-masked image WITHOUT its mask, so the Current Electricity ladder diagram came out **solid black**; the artwork is in the SMask (xref 9) and must be pulled and inverted. All three circuits then extracted as native PNGs (3-9% ink), better than any page crop. Q10's ladder is **infinite**, which the stem never says — only the figure's dotted continuation does.
  - **`verify-commit` FAILS on Parts of Speech and it is a FALSE ALARM of a known class** (the 2026-09-07 scoping blind spot, still unfixed for the SET checks): its set checks are scoped by `source_file` and cannot see the **14 questions `content_hash` correctly deduped onto pre-existing bank rows from six other papers**, which keep their own `set_id`/`context` as they must. The load-bearing checks passed — 125 links, no double-links, OMR parity intact. Left unfixed pending permission, since it is shipped code.
  - Verified after the flip, from the DB rather than any script's output: **397 new rows, 317 PUBLIC / 80 PRIVATE, and ZERO public rows with a bad key, a missing solution, a missing subtopic or duplicate option text**; `check-taxonomy` 9/9; `audit:text` clean on all six classes; `audit:omml` 0 failing zones; `audit:keys` flags exactly the 3 known duplicate-option rows and **all three are PRIVATE**. Backup taken first (79 tables, 440,127 rows). Parts of Speech ships its Directions as **13 sets carrying 7 distinct texts** — byte-identical in shape to the bank's 125 CDS Parts-of-Speech rows (125 → 13 → 7) — with the bare sentence in `stem` and the instruction in `context`, because 272 of the bank's 307 such rows store it that way and a stem prefix would break `content_hash` dedup against the matched rows.
  - **Owed, and stated rather than implied: nobody has clicked through `/dashboard/papers` or opened an OMR sheet in Excel.** The gates prove the rows are well-formed and the papers hold every question in printed order; they do not prove either surface lays out.

- **2026-09-11 (fifth) — THE TRACKER HALF OF ITEM STATISTICS: an export in `nda-tracker` + `npm run itemstats:ingest` here + `verdict_mismatch` (migration 0096). Both sources live — 8,984 sitting rows over 8,245 questions; usable coverage 424 → 1,462 at n>=10 and 50 → 625 at n>=20; the leads queue 57 → 319. The most valuable thing in this entry is a mistake.**
  - **I OVERWROTE A SHIPPED FEATURE BECAUSE I TRUSTED A STATUS LINE.** `nda-tracker/ITEM_STATS.md` said *"Status: spec, nothing built"*. It was stale: a Question Stats page (`src/pages/ItemStats/`), a sidebar entry, two routes, a 187-line pure core and 19 tests had shipped across four commits since that line was written. I wrote `src/lib/itemStats.js` and its test file from scratch and **clobbered both**, leaving their live page broken for part of the session. The Write tool said *"has been updated"* rather than *"created"* for **both** files and I did not register it; what finally caught it was `git status` showing **`M` rather than `??`**. Restored from git; their 19 tests and the full 2,782-test suite pass. **Two rules: a doc's status line is a claim, not evidence — `ls` the files it describes; and "updated" from a write tool you expected to create something is a STOP, not a nicety.**
  - **AND THEY WERE RIGHT WHERE I WAS WRONG, on the substance.** Their core scores correctness **against the KEY**, not Evalbee's verdict. I had built the opposite and written a test asserting *"NEVER re-derives correctness from choices and the key"*, citing the grading invariant. Re-reading it: *"`questions[].answer` drives only the displayed correct answer + solution + **per-question analytics**, never student marks"* — so per-question analytics ARE key-driven, and the invariant protects **marks**. I over-applied a rule to a place it does not reach. Their reasoning is the sharper one: difficulty asks *did they pick the right option*, the verdict answers *what mark did they get*, and the two diverge **exactly when a paper was mis-keyed or a question dropped** — which is when the number matters most. The export now **calls their `computeItemStats` once per exam record** instead of reimplementing it, so there is no second definition to drift.
  - **THE PER-RECORD DECISION WAS VINDICATED ON REAL DATA, not by argument.** `verdict_mismatch` flagged `∫₀⁴|x−1|dx`, where the bank's key **B = 5 is correct** (1/2 + 9/2) — and the same question in two sittings reads **APJ batches: 10 attempted, 0 correct, nobody chose B, 10 mismatches** against **LWS 2Y: 20 attempted, 2 correct, 2 chose B, 0 mismatches**. One sitting was graded against a different answer than the paper records; the other is clean. **A pooled export would have read "30 attempted, 10 mismatches" and made it impossible to say which sitting or which batches.** That is the whole case for the grain, demonstrated. It is also institute-side grading rather than a bank defect, and 10 students' marks are affected.
  - **`verdict_mismatch` IS THE ONLY UNAMBIGUOUS SIGNAL THIS FEATURE PRODUCES, and it needed the leads rule widened to exist at all.** Every other signal is a lead needing a human to separate three causes — all three of the sharpest leads found the previous day had **correct keys**. So `computeLead` now raises a lead when a mismatch exists **even if the key outpulls every distractor**, and is **not gated on the attempt threshold**: that threshold exists because a RATIO needs volume, while a mark disagreeing with the key is one mis-marked student at any sample size. Without the widening the row never surfaces — its distribution looks healthy and only the mark/key disagreement says otherwise. `Lead.reason` became explicit rather than inferred from which fields are null, and earned it immediately: a mismatch row and a key-never-chosen row **both** have a null ratio, and the smoke script duly mislabelled one as the other until it read `reason`.
  - **NULL IS NOT ZERO, and the CHECK enforces it.** Only a source storing a mark SEPARATELY from the response can compute a mismatch: the tracker can (`responses` is Evalbee's verdict, `choices` the letter), the vault **structurally cannot** — it stores responses and derives correctness from the key, so there is nothing independent to disagree with. A `0` on a vault row would assert *"compared, none found"*. Both 0096 constraints were proven to fire before use, with a valid row and a legitimate tracker `0` accepted in the same rolled-back block.
  - **A REAL INCONSISTENCY BETWEEN THE TWO SOURCES, caught before it pooled: discrimination.** The vault rollup counted over ATTEMPTED, the tracker over SEEN. They pool into ONE number, so a definition differing by source is a silent bias, not a rounding difference. Seen is the standard and the one kept — a skip is not a correct answer, and an attempted-only denominator inflates the index for exactly the items the weaker half avoids. Rollup re-run (idempotent). Their core gained the raw `upperSeen/upperCorrect/lowerSeen/lowerCorrect` alongside the derived `discrimination`, purely additively, because **a consumer that pools must pool COUNTS** — averaging the per-sitting ratio lets a 10-student sitting outweigh a 100-student one.
  - **THE DATABASE CAUGHT MY OWN DEFECT ON THE FIRST APPLY.** The export emitted the top split without the bottom whenever a sitting's bottom group never saw a question, and `question_item_stats_disc_coherent` rejected the batch outright — the guard proven to fire the day before, firing on live data. Fixed at source, gated on BOTH halves exactly as their core gates `discrimination`. 1,498 of 1,884 rows carry a coherent split; the 386 without genuinely have no bottom-group seer.
  - **THE EXPORT'S NUMBERS RESOLVED AN INCONSISTENCY IN THEIR OWN SPEC.** It reports `1,259 questions at n>=20` and, separately, `122 of 553` for the distractor check. Measured: pooling by **SEEN** gives exactly **1,259** (and 1,435 at n>=10, which is also its "distinct questions with response data" figure), while pooling by **ATTEMPTED** gives **555**. The two figures are on different bases and neither said so. **Attempted is the one that matters**, for the same reason `pValue` is over attempted. `444 questions in more than one record` reproduces exactly; key conflicts are **0** today, not 3. The export now prints both bases with the usable one labelled.
  - **The ingest REFUSES a payload not declaring `grain: "question-per-exam-record"`** — proven against a doctored fixture. Accepting a pooled export silently would destroy the one property the table exists for. `measured_content_hash` is read from the bank AT INGEST TIME (the tracker cannot know it), which honestly means *the question as it was when we recorded this*; an id resolving to no row is REPORTED, never dropped. **First ingest: 1,884 rows, 1,435 questions, ZERO dangling ids** — every `questionId` that repo has stamped resolves to a live bank row, which is the best available evidence its Phase-0 backfill was accurate. **283 questions now carry data from BOTH sources**, closing the spec's open question on true overlap; the proxy recorded earlier (1,736) was far too high.
  - **A SILENT PARTIAL COMMIT, worth recording because the message described work the commit did not contain.** A `git stash`/`git stash pop` used to run typecheck against a clean tree **unstaged three of four files**, and `git commit` without `-a` then captured only the untracked script — including omitting the discrimination alignment the message described at length. Caught only because `git status` was not clean after the merge. **Check `git status` after a merge, and never assume a stash round-trip preserves the index.**
  - Verified: 27 TDD cases here (7 new, written first and confirmed failing), 19 + 2,782 there; migration replayed into the test project; gate PASS; 762 prerendered `.html`, unchanged. **Owed, and stated rather than implied: the browser click-through.** Both surfaces are auth-gated `ƒ`, so the gate proves they compile and `itemstats:smoke` proves the loaders — neither proves layout, and the dashboard list is now 319 rows rather than 57.
  - **NOT built:** exposure (now UNBLOCKED — every tracker row carries a `cohort_label`, 4 cohorts live) and the weekly cadence, which is honestly a TWO-REPO step (`node item_stats.js --out=…` there, then `itemstats:ingest` + `itemstats:rollup` here).

- **2026-09-11 (fourth) — ITEM STATISTICS: `question_item_stats` (migration 0095) + a pure pooling core + the vault-native rollup + a staff-only chip on every `/browse` card + the wrong-key leads queue at `/dashboard/item-stats`. 7,100 sitting rows live. Spec: [ITEM_STATS.md](ITEM_STATS.md), the counterpart to `nda-tracker/ITEM_STATS.md`.** Opened by "are we pulling any q stat data from tracker?" — the answer was no, and the audit found a dormant shell pointed at the wrong app (`questions.attempt_stats`, built for MHT_CET_AI, 0 of 75,915 rows, read nowhere).
  - **THE DIVISION OF LABOUR IS THE PREMISE EVERYTHING ELSE FOLLOWS FROM: the tracker owns COHORT analytics, the bank owns ITEM properties.** In the tracker the user is a faculty member asking *how did my batch do*, and their spec rightly forbids cross-institute pooling. Here the user asks *is this question any good*, and for that a broader, more diverse sample is **less** biased, not more — a p-value measured on one institute's one batch is the narrow number, not the true one. **I argued the opposite first** and proposed a precedence rule (prefer the institute's number, fall back to the vault's); the user's push-back — *"batch-level analytics already happen in tracker, bank is more of a global app"* — was right, and I had over-transferred a rule that is about their app.
  - **THE MEASUREMENT THAT SETTLED IT INVERTED THE INTUITION, and corrected a number of my own.** Vault `/mock` carries **40x the raw response volume of the tracker and one-twenty-fifth the usable items**: 48,985 answer rows over 7,284 questions, but after the intake filters only **50 clear n>=20** (mean n **3.8**) against the tracker's **1,259**. A classroom sitting concentrates 30-60 students on one 120-question paper; the vault spreads 168 users across 192 mocks. **I had earlier reported 172 at n>=20 — that counted `seen` rows including blanks, not `attempted`.** So vault data is not worth publishing alone, and pooling is the only thing that makes collecting it worthwhile. **Pooling is also self-correcting**: where both sources have data the tracker outweighs the vault ~20:1, so precedence would have produced nearly the same headline number while discarding the extra n.
  - **THE BASE RATE CORROBORATES ACROSS POPULATIONS — 22% against 24%.** The tracker reports a distractor outpulling the key on **122 of 553** measured items; the vault, independently, on **12 of 50** at n>=20. Two cohorts — one proctored, one self-serve online — agreeing that roughly a fifth of measured items are in that state, which suggests it is a property of the **items** (real traps plus real key errors) rather than an artifact of either channel. It is the strongest single argument for building the queue at all. *(Worth asking their side about the 553: they separately report 1,259 items at n>=20 and the denominators disagree.)*
  - **`skipRate` IS THE ONE THING THAT DOES NOT POOL, and it is not the headline number.** MHT-CET mocks carry **zero negative marking** so skipping there is irrational, while NDA costs -0.83; online, 42% of answer rows are blank at 57.4% mean engagement. Pooling a deliberate skip under penalty with an abandoned browser tab gives a number that means nothing. `pValue` and `choiceCounts` pool cleanly — a distractor pulling in two independent populations is *stronger* evidence. Discrimination pools **as counts**, computed within a sitting; a test pins this and fails on the obvious mean-of-rates implementation, which would let a 10-student sitting outweigh a 100-student one.
  - **STORAGE IS PER-SITTING AND AGGREGATION HAPPENS AT READ TIME. This deliberately does NOT extend `questions.attempt_stats`** (0010) — empty, unread, and its merge helper computes an **irreversible** weighted average. The grain is what makes a bad sitting a `DELETE`, an institute that leaves removable, and "did the key fix work" answerable. **The long-term case decided it, not convenience:** pooled is derivable from per-record and never the reverse, and the fallback ("re-export from their raw results later") assumes `exam_results` still exists unpruned years from now — if they archive and we kept only balances, the evidence is gone from both sides at once. Also: `measured_at` per sitting is what would ever answer *is this item getting easier (leak? taught to?)*, and per-record is what a shared-DB tracker rebuild would give anyway.
  - **`measured_content_hash` is load-bearing — the `question_reviews` lesson (0074) applied.** This bank flips keys in place (`apply-key-fixes.ts` re-stamps the hash) and reshuffles option order in the Worksheets pipeline; after either, a stored `{"B": 31}` describes a different question. `aggregateItemStats` takes the live hash as a **required** argument rather than an optional one, so the staleness rule cannot be forgotten at a call site, and a stale row renders **nothing** rather than a wrong number.
  - **ALL SIX CHECK CONSTRAINTS WERE PROVEN TO FIRE before use** (self-aborting `DO` block: sum coherence, correct<=attempted, tracker-implies-org, vault-implies-no-org, half-populated discrimination, duplicate sitting) with a valid row accepted in the same block, then rolled back. **The RLS lock was tested against the NON-EMPTY table** — `service_role=7100, anon=0, authenticated-without-org=0` — because an empty table proves nothing (the batch-roster false green). **My first RLS probe was itself wrong**: it labelled a row 'anon' without ever issuing `set local role`, so both arms ran as service_role and reported 7100/7100.
  - **THE LEADS SURFACE WAS REVISED FROM THE SPEC, and the revision is recorded rather than slipped in.** The spec said a `/browse` filter over a bounded id set. Building it showed the id set cannot be obtained cheaply — narrowing to leads needs the aggregate *before* the question query, i.e. either a new RPC or a 7,100-row fetch **on the most-visited page in the app**, which has a documented history of payload and caching regressions. A ranked ~57-item work list is a dashboard object, not a corpus filter. So `/dashboard/item-stats`, **superadmin-only** — the numbers pool across every org (the platform-wide precedent) and, the stronger reason, acting on a lead means editing content, which is superadmin-only since 0056. The `/browse` filter stays viable once a maintained aggregate exists, which decision 6 already anticipates.
  - **THE HONEST CALIBRATION MATTERS MORE THAN THE COUNTS: all three sharpest leads have CORRECT keys.** *"Which metal floats in cold water?"* — 7 of 10 chose Calcium against the keyed Potassium, which genuinely floats at 0.86 g/cm3: a real misconception. *"A floating body is in stable state"* — 13 of 17 chose "centre of gravity coincides with centre of buoyancy" against the keyed "below the **metacentre**", which is the actual stability condition. And a cloze item **corrected my own reading**: from the stem alone (`________ plants and animals`) I called it a truncation defect, and pulling its `context` showed it is a set member carrying the full passage — a legitimately hard item where "both" reads more naturally than the keyed "the". **0 of 3 are wrong keys**, which is what "a lead is not a verdict" means in practice, and it calibrates expectations: the vault's low-n unproctored sample will mostly surface traps and misconceptions, while the tracker's proctored n>=20 data is where genuine key errors are likelier.
  - **The card follows the `getResourceTagsForQuestions` pattern exactly** — a batched lookup over the 25 ids already on the page, NOT joined into `queryQuestions`, whose ORDER BY payload cost this project 13.9 MB of disk spill per call once already. Skipped outright for anon and students, so the hot path and the cached landing panel are untouched; that is a **performance** gate, while the security boundary is the RLS policy, which refuses them independently. `getPageIdentity()` already resolves `isStaff` before the main `Promise.all`, so the gate adds no session read and no caching risk.
  - **`server-only` forced a split that improved the design.** A probe cannot import a module carrying that guard (it does not resolve outside Next's bundler). Rather than delete the guard to make a probe work — trading a real safety property for a test — the logic moved to `overview.ts` (takes a client, injectable, drivable) and `admin.ts` became a five-line guarded wrapper that supplies the service-role client. `npm run itemstats:smoke` then drives the page's own loader AND the card's read path against live data, since the page is auth-gated `f` and `next build` never executes it.
  - Verified: 20 TDD cases written first and confirmed failing for the right reason; migration replayed into the test project; rollup **idempotent** (7,100 rows on re-run, 0 duplicates, 0 stale-on-arrival, 0 bad sums); the pure core reproduces an independent raw-SQL measurement **exactly** (57 leads / 24 at ratio>=2 / 12 at >=3 / 3 key-never-chosen / 424 at n>=10 / 50 at n>=20); card path 25 ids to 25 aggregates to 25 chips, and the empty-input case returns 0 rather than a PostgREST 400. **Owed, and stated rather than implied: the browser click-through.** Both surfaces are auth-gated `f`, so the gate proves they compile and the smoke proves the loaders — neither proves layout, and that includes the card's expanded distribution bars.
  - **NOT built:** the tracker ingest CLI (their `item_stats.js` is still spec-only on their side, so 1,259 items and 122 leads are unavailable), exposure (needs `cohort_label`, which only tracker rows carry), and the weekly cadence wiring. **The one thing to settle with the tracker repo is the per-record export** — their pure core currently pools by `questionId`, and the bank needs the transactions rather than the balance.

- **2026-09-11 (third) — the phone nav moved to a LABELLED BOTTOM BAR: five tabs, identical for every visitor, below `sm` only. `PrimaryNav` becomes `hidden sm:flex`, so exactly one nav is displayed at any width and tablet/desktop are byte-identical. New `MobileTabBar` + the pure `lib/nav/mobileTabs.ts`.**
  - **THE DEFECT WAS LABELS, NOT PLACEMENT, and it hit every mobile visitor rather than only staff.** Labels are `hidden sm:inline` — 640px, which is every phone in portrait — so a phone got a row of bare icons: **Bank as a `Compass`**, and **three book-ish glyphs in a row** (Guides `BookOpen`, Board `Library`, Books `BookMarked`), with the brand mark using the **SAME `BookOpen` glyph as the Guides tab**, differing only in colour. Even a student saw five undecodable icons. The crowding on top of that was worst for a superadmin who is also org staff — **10 tap targets in 360px** — which is precisely the case `PrimaryNav`'s own comment had named as "the one to check first if this row ever needs to lose something". It now has.
  - **THE QUESTION ASKED WAS WHETHER TO MOVE THE WHOLE NAV TO A LEFT SIDEBAR like the sibling nda-tracker, and the answer was no — for reasons that are measured, not stylistic.** nda-tracker is a **single-audience staff tool with ~13 sibling destinations**; this is a public content site whose primary nav has **five**, and whose signed-in population is **300 student profiles against 10 staff** (3 admins, 7 teachers, 1 superadmin). Decisive: **three surfaces already own the left rail** — `/browse` at `lg:grid-cols-[18rem_1fr]` (filters), `/guide` at `16rem` (`GuideShell`), and `/mock`'s exam rail — so a global left nav means **two stacked rails on the most-used page**, cutting the question-card column from ~960px to ~700px at 1440px, on the one column that renders LaTeX. And below `lg` a rail becomes a hamburger, which would put **Mocks two taps away** — the dominant signed-in activity at **463 submissions by 115 users in 30 days**, against 76 notes checkpoints by 16.
  - **THE REAL SIDEBAR CASE IS `/dashboard`, AND IT IS UNBUILT.** That surface has ~16 sub-pages and **13 of them have no link back to `/dashboard` at all** (only feedback, reports and notes-reports do) — navigation is the browser back button. Meanwhile the top nav on those pages offers Bank/Guides/Notes/Mocks/Board/Papers, **none of which is a dashboard destination**. Many siblings, one audience of 10, dense tables, desktop-only, not indexed: that is where a left rail belongs. **Deliberately NOT bundled here** — separate change, separate approval.
  - **THE BAR IS ROLE-INDEPENDENT BY CONSTRUCTION, which is the load-bearing decision.** Papers (org staff) and Books (superadmin) are absent from it and live in the **account menu below `sm`** instead — `UserMenu` is already the role-conditional list (Superadmin, Dashboard, Saved, Account). So the bar is a **fixed five for anon, student, teacher and superadmin alike**, and the ten-icon crush becomes *structurally impossible* rather than currently-avoided. `resolveMobileTabs` takes **no session argument** and a test **pins its arity**, so a future "just add Papers for staff" fails loudly with the reasoning attached. Five is also the iOS/Material cap: at 360px that is 72px a tab, and the longest label ("Guides") sets to ~45px.
  - **BOTH NAVS DERIVE THEIR HREFS FROM ONE `resolveExamNav`**, so they cannot drift about where a tab goes; `MOCK_TAB_HREF` is exported for the one tab that never personalises. `HeaderBar` now owns the `<header>` element and renders both as siblings from a single state source — **mounting the bar separately would repeat its `/api/me/header` fetch on every page load**. Being a child of `AppHeader` also means the bar is already absent exactly where it must be: **the mock runner, print pages, `/login`, `/signup` and `/welcome` render no `AppHeader` at all**, so none needed a special case. (An earlier collision worry about the runner was simply wrong.)
  - **CLEARANCE POLARITY IS THE SUBTLE PART.** `body` padding in `globals.css` covers all 56 `AppHeader` call sites and the Footer in one rule, **DEFAULT ON with a `[data-no-tabbar-pad]` opt-out** — the mock runner takes it, being `min-h-screen` where 56px would add real scroll mid-exam. The tempting inverse (pad only when `:has()` finds the bar) **fails UNSAFE** on a browser without `:has()` — content hidden under the bar — where this way the fallback is 56px of slack on one page. The `/browse` cart pill is the only genuine collision (`fixed z-40` with its own safe-area inset) and lifts via a **named class, not an inline style**: the `sm+` value differs, and an inline style can carry no media query nor be corrected by one. **Radix overlays needed no change** — Sheet and Dialog are already `z-50` against the bar's `z-40`, so the filter sheet and download dialog cover it correctly.
  - **Verified:** 13 new tests with **both load-bearing guards PROVEN to go red** (injecting a role parameter fails the arity guard; forcing a Papers tab fails five shape guards) rather than trusted for passing; **all 6 pre-existing nav suites pass untouched**, which is the evidence desktop did not move; `GATE_RESULT: PASS`; **762 prerendered `.html`**, so nothing de-cached. **NOT verified, and stated rather than implied: the layout.** This repo has **no DOM test harness** — the only test references to these components are comments — so the gate proves compilation and logic and nothing about how the bar lays out. The device pass is owed, particularly the cart pill at 360px and the iOS safe-area inset.



- **2026-09-11 (second) — the /browse cart's "Download paper" button became "Create paper": staff get a title prompt and land in the new paper; everyone else gets the same button pointed at /request-access. New `CreatePaperDialog` + a shared `createPaperWithQuestions`; `CartPill` props narrowed to `{ isOrgMember }`.**
  - **THE CART PANEL NO LONGER OFFERS A DOWNLOAD, and that is deliberate — do not "restore" it as a regression.** Downloads are teacher-gated (`resolveExportAccess`), so for an anon visitor or a signed-in student that button was *only ever* the "Papers are for teachers" → /request-access funnel; pointing "Create paper" at the same place preserves the lead capture and shortens the path. **Staff lose nothing they can't reach in one more click**, and that was measured rather than assumed: `DownloadDialog` is rendered a SECOND time at the top of /browse and already exposes a **"Selected · N"** mode whenever the cart is non-empty (`cartAvailable`), and the paper page carries the full 4-artifact `PaperDownload`.
  - **A NON-MEMBER CANNOT OWN A PAPER AT ALL, so the fallback is structural rather than a policy choice.** `papers.org_id` is NOT NULL and every RLS policy on `papers`/`paper_questions` is `org_id = private.current_user_org_id()` (0039), which is NULL for a student. "Let students create papers" is a schema change, not a flag. Checked the one thing that could have made the UI lie: `isStaff` is derived as org-membership ONLY, not superadmin (`auth-identity.ts`), so everyone shown the create path passes `requireMember()`.
  - **`AddToPaperDialog`'s own create mode was KEPT, and the risk is drift rather than duplication.** It auto-flips on when an org has no drafts yet, so deleting it would leave that case a dead end. The two doors now share one `createPaperWithQuestions`, so a future batch id or section template lands in one place. They differ ONLY in the aftermath — the button navigates into the paper, the dialog toasts and keeps you on /browse — which is intended (button = take me there, dialog = I'm still collecting).
  - **A mid-sequence failure leaves a REAL, EMPTY paper, and the toast says so.** Reporting a bare error there would send the user hunting for a paper they'd been told was never made; the cart is deliberately NOT cleared and we do NOT navigate, so "Add to paper" can retry into it. The cart clears and the Sheet closes only on success — the Sheet because leaving one mounted through a route change is how a body scroll-lock gets stranded on the page you land on.
  - **What is NOT verified, stated rather than implied: none of this is provable headlessly.** The repo has no jsdom/Playwright, and this is a Radix Dialog inside a Sheet that does not exist in the DOM until a click — so the green gate proves it COMPILES and nothing about whether it lays out. No unit test was added because nothing pure came out of the change (title validation already lives in `createPaperAction`). Owed: the three browser click-throughs (staff create end-to-end, non-staff → /request-access, a forced failure leaving the cart intact).
  - Sort remains display-only and always was: `planBulkAdd` files by SUBJECT section in cart insertion order, so the panel's "By chapter" toggle does not carry into the paper. Unchanged, recorded so it is not read as a new defect.

- **2026-09-11 — the nda-tracker READ BRIDGE went live on the vault side: `tracker_sync_targets` applied (ledger `20260911102415`), LWS Pune provisioned, and the security boundary promoted from a manual curl checklist into a fault-proven test.** The route, payload and migration file were written by an nda-tracker session the same day (`b31895cc`) and left inert; this closed it. Only the tracker's own Vercel env remains, which needs the account owner.
  - **THE ROUTE IS AUTHENTICATED AND ORG-SCOPED, and that inverts the spec's first draft.** `GET /api/questions/by-ids` exists so a tracker deployment can pull the full content of the bank questions an exam was built from — above all the DIAGRAMS, which the text-only Tags sheet drops entirely. Reading it anonymously would see only PUBLIC rows, and much of this bank is PRIVATE, so an anonymous endpoint returns **nothing for exactly the papers that matter, with no error** — and cannot serve a second institute at all. The presented secret resolves to an org through a unique index and the query is then scoped to that org's rows plus PUBLIC, mirroring the two-phase shape of `queryQuestions` (phase A owns scoping, phase B only widens the columns).
  - **ROUTING IS PER-ORG CONFIG, NEVER A PAYLOAD FIELD, and that decides the Push button's gate too.** Every institute gets its own tracker deployment, so a caller-chosen destination means one wrong value delivers institute B's paper into institute A's tracker. The consequence worth writing down: the future Push button is enabled **iff that org has a `tracker_sync_targets` row** — not an allow-list of names, because this repo already carries a hardcoded `DESTINATION_ORG_NAME = "LWS Pune"` in `api/sync/mock` and that is the wart being avoided. Four of the five orgs correctly have no row.
  - **THE LOCK TEST RAN AGAINST A NON-EMPTY TABLE, which is the only way it means anything** — `service_role=1, anon=0, authenticated=0`. RLS on with NO policies (the `platform_admins` pattern); the secret is plaintext BY NECESSITY, since the push direction has to send it, which is precisely why the table is service-role only. Run a minute earlier against the empty table, the identical query returns 0 for every role and proves nothing — the batch-roster false-green, avoided by ordering rather than by luck.
  - **THE SECURITY BOUNDARY HAD NO TEST, ONLY A CHECKLIST — and that is the gap this session actually closed.** The two committed suites were both PURE (parser rules, payload field names); the assertions that justify the route existing — a PRIVATE row resolving for its own org, another org's PRIVATE row NOT resolving, a wrong secret being indistinguishable from none — lived as a curl list in a handoff doc. `tests/sync-by-ids-route.test.ts` builds **two** throwaway orgs so "mine" and "theirs" are genuinely different rows, and **both halves were fault-proven rather than trusted for passing**: deleting the route's org-scoping turns exactly the 2 cross-org cases red and nothing else; making the unknown-secret branch return `403 {"error":"unknown secret"}` turns exactly the 401 case red.
  - **`missing[]` CARRIES TWO CAUSES AND THE CONTRACT DOCUMENTED ONE.** It was written up purely as repair signal — a stem repair here is a delete-and-re-commit (`content_hash` covers the stem) which mints a NEW uuid, so a tracker exam can hold a dead id through nobody's error. As implemented it is ALSO the denial channel for another institute's PRIVATE rows, deliberately indistinguishable for the same reason an unknown secret returns the same 401 as no secret. Inert while one org owns the bank; live at institute #2, when an operator would otherwise hunt for a repair that never happened. Now named in the route's own comment rather than left to be rediscovered.
  - **A stale handoff claim, corrected: "one commit ahead of origin, held because six practice-paper files are in flight".** Four commits ahead by the time it was read, tree clean, blocker evaporated. **And its ledger warning had the right fact with the wrong consequence** — prod records migrations as timestamp versions, but nothing re-applies them on a push (`prepush` and CI never touch prod schema, and `testdb:migrate` tracks the TEST project by filename in its own `public.testdb_migrations`). The real cost of applying by hand is quieter: the ledger stops being a truthful record of prod's schema, the inverse of the 0021 defect. Applied through MCP so DDL and ledger land together.
  - **Accepted gaps, stated rather than buried:** no rate limit and **no per-call logging**, where every other guarded route here uses `checkAndIncrement` — defensible for a bearer-gated server-to-server route, but the secret sits in another app's env indefinitely with no signal of which org called or how often, and rotation today is an `update` plus a redeploy there. Response order is arbitrary (`queryQuestionsByIds` does not reorder; `/browse` does it at the call site), harmless only because the tracker merges by `questionId`. **The paper push is NOT built** — specced in nda-tracker's `CROSS_APP_SYNC.md` with two hard guards: a re-push refuses an exam that already has results, and a paper deleted here must not delete a tracker exam that has results. See `HANDOFF_TRACKER_SYNC.md`.

- **2026-09-09 (second) - NCERT CHEMISTRY CLASS 11 IS COMPLETE IN ONE DAY: all 9 chapters, 530 q PUBLIC, 0 PRIVATE, taking `cbse-11` to 2,147 across three subjects. With Class 12 finished the same morning, NCERT Chemistry is COMPLETE across both years (980 q). Two waves of parallel whole-chapter agents, and the durable findings are about how thin the evidence actually is on this source.**

  - **THE KEY IS PARTIAL PER QUESTION AND THE PAGE SPAN BADLY OVERSTATES IT.** An `answerPages: [0,1,2]` reads as full coverage and is not. Measured per chapter: **ch1 41 of 66 exercise rows keyed / ch2 65 of 66 / ch3 ZERO / ch4 ZERO / ch5 22 of 22 / ch6 60 diffable / ch7 ONE of 30 / ch8 FOUR of 40 / ch9 24 of 25.** So the step-6 gate ran on 217 of 464 exercise rows, and **on four chapters it is effectively inoperative.** Every chapter reports its own denominator, because "0 wrong across 22 keyed" is a different claim from "across 73" and only one of them is true.

  - **ZERO OUR-ANSWER-WRONG across every keyed row in the book**, against **19 errata brackets**. Two worked examples were CORRECTED rather than preserved, because a printed worked solution is a method a student copies: `Eg 5.9` reverses an equation and keeps its enthalpy negative, which makes its own next line arithmetically impossible (its printed (v)+(vi) give -6485.49, not the -48.51 beside them, while -3218.49 + 3267.0 = +48.51 exactly), corroborated by the book's own Table 5.2 at +49.0; and `Eg 8.15` asks which **C-C** bond has the least inductive effect and answers with a **C-H** bond, so as printed it does not answer its own question.

  - **`Eg 5.14` WAS SPLIT DELIBERATELY, and the split is the point.** Its unit is corrected and its value is not. R is in J K-1 mol-1, so the printed kJ overstates the free-energy change a thousandfold - unambiguous, so fixed. But the same printed factors evaluate to 790.0 rather than the 763.8 beside them, and no self-consistent reading of Kp, T or log Kp reproduces 763.8. **Inventing a correction is worse than naming a defect**, so the value stands with the discrepancy recorded.

  - **THE SHARPEST FINDING IS NOT A WRONG ANSWER BUT A WRONG PREMISE.** `Ex 7.3` instructs "justify that the following reactions ARE redox reactions", and in part (c) NO element changes oxidation number: boron +3 and hydrogen -1 on both sides, lithium, chlorine and aluminium unchanged. It is a metathesis. The bracket sets out both readings rather than picking one, because **the chapter contradicts itself**: the PRINCIPLE its rules derive from puts H at -1 (hydrogen is more electronegative than both boron and aluminium), giving "not redox", while a LITERAL reading of its rule 4 - hydrogen is +1 except in metal hydrides, and boron is a metalloid - gives B = -3 and "redox", which is what most answer guides print. The exercise is unkeyed, so nothing external settles it.

  - **183 DERIVED ANSWERS ARE STAMPED, and nearly missing that step is the process lesson.** `flip-public` does NOT gate on provenance in this lane, so the stamp was run by hand; had it been forgotten, 183 answers the book never confirms would have shipped reading as an official key - the CDS General Knowledge defect exactly. `stamp-provenance.ts` is evidence-driven, stamping precisely the rows each chapter's own cross-check recorded as NO-KEY-ENTRY, so Thermodynamics correctly stamps **none** (22 of 22 confirmed) while Bonding and Periodicity stamp **40 each**. **A tooling limitation found the hard way: its dry run does NOT filter already-stamped rows**, so it reports the same count forever and cannot tell you whether a chapter has been stamped - query `derived_model` instead.

  - **SIX FIGURES WERE MISSING AND FIVE ROWS WERE UNANSWERABLE BECAUSE OF IT** - stems reading "shown in the figure" with nothing attached, which no gate here can see. The transcribing agents flagged them rather than papering over them, which is the only reason they were caught. One was a case I had reasoned my way out of: I told the figure agent `Eg 9.7` probably needed nothing, since a worked example's drawing lives inside the book's own solution. Wrong - that page is set in TWO COLUMNS, statement bottom-left and solution top-right, and its sub-item (ii) is printed ONLY as a drawing. **Two crop traps worth keeping:** on `Ex 9.2` the figure is one sub-item of a seven-part question, so its ceiling was set at the top of sub-item (e)'s vector `CH3` five thousandths of a page below; and on Redox's Table 7.1 the **header row's ink STRADDLES the table's own box border**, so cropping to the border would have sliced the column headings in half. Every crop was placed off measured geometry rather than by eye and verified by sha256 against the stored object, with a deliberate wrong-page control returning MISMATCH.

  - **A NUMBER OF MINE WAS WRONG IN BOTH DIRECTIONS, and correcting it changed a rule.** Config recorded ch9's key as covering "22 of 25"; an agent said 25; my text-layer probe said 23. The **page** says 25 - that key draws several answers as STRUCTURES and `get_text()` drops those entry numbers entirely (9.9 and 9.17 are absent from the text layer and plainly on the page), while the same scan reports **68 entries in a 67-question unit**, so it errs in both directions. Since these counts decide which chapters are owed the compensating regime, ch7 and ch8 were re-measured off the render before wave 2 was dispatched: they really are 1 of 30 and 4 of 40.

  - **A CHECK THAT COULD NOT FAIL, found by an agent declaring a weakness in its own work.** A whole-chapter agent derives each MCQ key while transcribing, and that derivation becomes the committed key - so when the same agent runs the "blind" verify pass, `mark-mcq-verify` compares its answer to its own and agreement is guaranteed, while writing a `blind_rederivation` row that overstates the evidence. `dump-mcq` withholds `is_correct`, so the verifier is blind to the KEY; it is not independent of the FIRST DERIVATION, and only the second makes a second pass mean anything. The fix - now the brief's rule, and the only sub-agent it permits - is one focused verifier reading only the rendered pages. **Two chapters correctly reported having no MCQs at all rather than a pass they never ran**, and Redox, which has none, replaced the anchor instead of skipping it: 17 determinate-answer exercises independently re-derived, 17/17 agreeing.

  - **A CONTAMINATION THAT WAS MINE:** I put the key's "15 g" into the Redox agent's brief, so its own pass on the chapter's single keyed row was not blind. It declared this, and its blind agent - which never saw the brief - re-derived 15.00 g independently and named the trap giving 17.65 g. The standing rule was that a claim about the SOURCE must be verified before entering another agent's prompt; this was a claim about the ANSWER and should not have gone in at all.

  - **TWO DEDUP GAPS RECORDED RATHER THAN REPAIRED**, and the precedent an agent cited does not apply. The book reprints its own worked Problems verbatim as exercises - 2.13 as 2.21, and 6.13/6.16 as 6.38/6.41 - so `content_hash` keeps the copy carrying the printed solution and the Exercises blocks carry silent gaps. Re-admitting them was proposed citing Class-12 `Eg 6.3.21`; there the two items posed the same function to teach two DIFFERENT tests, so dropping one lost the lesson. Here they teach the same thing. Recorded in `config.ts`, which is where this repo keeps structural per-chapter facts. Equilibrium also gives the sharpest statement of the denominator problem: **62 key entries are printed but two point at deduped rows, so 62 printed is not 62 diffable and 60 is the honest number.**

  - **A CORRUPTION VECTOR THE BRIEFS DID NOT COVER: the Write tool can emit a RAW control byte where an escape sequence was typed.** Two independent sightings in one day - an agent found a `U+0001` in a file it wrote as plain text, and `apply-solved-fixes.ts` (ported here from `scripts/mh-sb-11`, since this pipeline had no route to a solved row's body at all) shipped with literal control bytes **inside the very regex meant to catch them**, which made `grep` report the file as BINARY and therefore invisible to every text probe in the repo. The rule is no longer "avoid heredocs" but "check the bytes of anything you write containing a backslash escape, whatever wrote it".

  - Verified at close: 530/530 PUBLIC - 0 without a solution, a `section_seq` or a taxonomy assignment - `audit:text` clean across six classes - `audit:omml` **530 scanned, 0 failing zones** - `audit:keys` 21 MCQs, 0 flagged - `board:lint` green over 15,622 rows across 6 board exams - stored errata brackets reconciling against their files in every chapter. **Owed, and stated rather than implied: a browser click-through of `/board`** - answers reveal on click, so nothing headless proves the reveal lays out, and that now includes six attached figures and Redox's split-zone underlines.

  - **A SHIPPED DEFECT FOUND EN ROUTE AND DELIBERATELY NOT TOUCHED: 404 PUBLIC rows across 28 earlier chapters are answers the book's key never confirms, carrying no `derived_model`** - so they are indistinguishable from verified ones. It spans NCERT Class-11 Maths (Trig 31, Sets 20, Sequences 13, Straight Lines 12 and more), Class-12 Vector Algebra 12, Physics Nuclei 1, and most of Class-12 Chemistry (Biomolecules 33, d-Block 27, Alcohols 24, Coordination 22, Haloalkanes 22, Amines 20, Electrochemistry 19, Solutions 14, Kinetics 9, Aldehydes 9). Data-only and reversible, but it is shipped content across three subjects and needs a 360 and explicit permission, not a sweep.



- **2026-09-09 - NCERT CHEMISTRY CLASS 12 IS COMPLETE: 10 of 10 chapters, 450 q PUBLIC, 0 PRIVATE, taking `cbse-12` to 3,906 across three subjects. Two waves of parallel whole-chapter agents after the 2026-09-08 pilot. The step-6 gate held in BOTH directions for the first time in this lane, and the two findings against US are the point of the entry.**
  - **THE GATE FINALLY CUT BOTH WAYS.** Wave 2 diffed 66 rows against a printed key: **5 BOOK-KEY-WRONG and, for the first time here, 2 OUR-ANSWER-WRONG**. Every previous chapter of this lane had reported 0 ours-wrong, which reads as reassurance and is actually the state in which a gate has told you nothing. **Both were repaired before anything shipped, each verified independently by the maintainer first**, and both are instructive in the same way - the error is in the STATED ANSWER while the reasoning beside it is sound. `Ex 7.1(i)`: we numbered the pentane from the wrong end; the `-ol` locant is 3 either way, so substituent locants break the tie and {2,2,4} beats {2,4,4} at the second point of difference. `Intext 7.7(ii)`: we gave but-1-ene by E2, but butan-1-ol cannot form a free primary carbocation - water leaves with a concerted 1,2-hydride shift to the sec-butyl cation, so but-2-ene is the major product. **The agent's OWN authored `_note` had already named but-2-ene and that exact mechanism as the alternative.** So the chemistry was right and the answer was wrong, which is precisely the case an external key catches and no amount of self-review does.
  - **A DEFECT CLASS CAUGHT BEFORE IT SHIPPED, and it is invisible to every gate this repo owns.** `apply-solutions` fills **SUBJECTIVE rows only** - its own output says so - so an `exercise-mcq` row can end up with a correct key and NO WORKING. Nothing catches it: `board:lint` checks structure, `audit:keys` skips a row with no solution, and a NULL solution is legal. **This repo shipped exactly that once** (NCERT Integrals, 22 rows, found months later). ch5 had 2 such rows. They were scanned bank-wide BEFORE fixing - exactly 2, ch5 only, 0 public - then put through `dump-mcq` -> blind derivation -> `mark-mcq-verify` (2/2 agreement, and the deriving agent noted honestly that both answers being C makes that agreement weaker evidence than usual). The check is now a step in the brief, because a count is the only signal it produces.
  - **CONTAMINATION IS STRUCTURAL ON THIS SOURCE AND HAS TWO ROUTES, only one of which is obvious.** Three of four wave-2 chapters print "Answers to Some Intext Questions" on the same page as the tail of the Exercises, so transcribing those questions **in page order necessarily** puts the key on screen before anything is authored. ch7 avoided that entirely and contaminated itself anyway - with a whole-PDF text-layer heading scan run while *planning bands*, which printed two intext answers into its context, the same route ch3 had hit in wave 1. **Every agent declared it unprompted and scoped it precisely**, which is the wanted behaviour: an honest contamination note beats a clean-looking claim, because a reader who does not know those AGREEs are weak evidence will over-trust them. Both routes are now in the brief with their fixes - crop the page from the heading's own y-coordinate, and scope every structural probe to the pages you are about to read.
  - **18 errata brackets across the ten chapters, and the strongest are self-refuting on the book's own words.** `Ex 4.18` contradicts itself in one sentence - *"Except Sc3+, all others will be coloured because of incompletely filled 3d-orbitals"* - where the reason given is exactly what excludes the Cu+ (3d10) that "all others" includes; corroborated twice more by the chapter (§4.3.10 defines colour as promotion INTO a higher d orbital, and Table 4.8 lists 3d10 Zn2+ as colourless). `Ex 7.3`'s key lists **EIGHT** isomers of C5H12O covering only **SEVEN** compounds - entry (e) prints an OH inserted into the chain, impossible as drawn, and duplicates (d) - and because the set is a fixed eight, exactly one is missing and enumeration identifies it as pentan-2-ol. That closes rather than merely suggests, which is the bar a bracket has to clear.
  - **ONE FINDING WAS RECORDED AND DELIBERATELY NOT BRACKETED AS AN ERROR.** ch5's printed key numbering runs past the chapter's own last exercise: its "5.31" is a bare option letter left over from a removed MCQ, and its 5.32 answers our `Ex 5.31`. So that row is matched to the key **BY CONTENT**, and the bracket is a navigation note telling a reader not to compare 5.31 against 5.31 - not a claim that the book got an answer wrong.
  - **`Ex 5.14` is the case where naming BOTH answers was the right call.** The key writes tetracyanidocuprate(**II**); cyanide reduces Cu(II) to Cu(I), so the isolable species is the (I) complex. But the key's CONCLUSION is unaffected and correct, and an examiner following the book expects the 2- charge - so the solution gives the correct species and says what the book prints, rather than silently picking a side.
  - Verified rather than asserted: 450/450 PUBLIC, **0 rows missing a solution, a `section_seq` or a taxonomy assignment**, stored errata counts reconciling against the errata files (counted in the DB, not read off the apply log); `audit:text` clean across 6 classes; `audit:omml` **450 scanned / 0 failing zones**; `audit:keys` 0 flagged; `board:lint` green over **15,092 rows across 6 board exams**.
  - **A cost accepted knowingly, and recorded so it is not rediscovered as a bug:** seeding the `Chemistry` subject under `cbse-11` before dispatching the Class-11 wave leaves a subject row with zero PUBLIC questions for as long as the wave runs, and `listSubjects` applies no question-count filter - so it renders a live `/browse` filter returning nothing until the first chapter flips. The alternative is nine agents racing to create the same row, which is worse. The rule stays "seed immediately before the first commit, never earlier"; this is the one shape where "immediately before" is still hours.

- **2026-09-08 (third) — the NCERT CHEMISTRY lane opens: pilot chapter Cl-12 Ch.1 Solutions, 66 q PUBLIC, and Chemistry becomes CBSE Class 12's third subject. The pilot was chosen to be the HARDEST shape in the subject, and the two most useful findings are both mistakes — one mine, one an agent's report about another agent.**
  - **THE PILOT WAS PICKED TO FAIL FASTEST, not to succeed easiest.** Ch.1 Solutions carries all three question streams, is where the Intext/Exercise numbering collision bites, is 5x-painted, AND is keyed — so the mandatory step-6 gate ran on the first chapter instead of being deferred to some later one. The alternative candidates (Cl-11 ch1, or the 22-question Cl-11 Thermodynamics) are cheaper and test almost nothing. Same reasoning that took mh-sb-11's Sets and Relations third rather than by weightage.
  - **A CORRECTION FROM THE USER THAT CHANGED THE DESIGN, and the repo had already written the answer down.** I opened by asking whether to ship Intext Questions with a derived-answer disclosure and how to treat the unkeyed chapters — i.e. proposing to re-litigate two things the Physics lane had settled. The reply was that the Physics method is established, that nothing "stupid" belongs in a student-facing solution, and that 70k+ solutions exist to learn from. Checking rather than arguing settled it in one query: across all 734 NCERT Physics rows, **`pyq_note` carries a plain source line and ZERO rows disclose anything**, and the only "derived" strings in solutions are ordinary physics prose. The 2026-09-02 State-Board entry had already reasoned this out — *"wrong moment, wrong field, wrong premise: the CDS rule exists because those are EXAM PAPERS where an official key is a real artifact; this is a TEXTBOOK"* — so the disclosure would have been a re-introduction of a defect this project had already diagnosed and removed. **A settled decision reached by reading what the code actually stores beats one reached by re-deriving the principle.**
  - **MY OWN WORST ERROR, and the probe had already told me: I asserted in `config.ts` that "INTEXT QUESTIONS HAVE NO PRINTED ANSWER ANYWHERE IN THE BOOK". False.** Every Class-12 chapter but one ends with "Answers to Some Intext Questions". My structural probe HAD flagged that page and reported **exactly the item numbers the key covers** (1.1-1.5, 1.9-1.12); I read the first 1,400 characters, found the tail of the Exercises, and called the rest parsing noise. The signal was precise and I dismissed it on a partial read. Found by the band-D agent, which owned that page. Scoped properly afterwards: **9 of 10 Cl-12 chapters carry the section, Class 11 carries none** — and it refines the unkeyed count, since Cl-12 ch6 Haloalkanes has no Exercise key but ~7 Intext answers, so only THREE chapters are unkeyed outright. **Read a page to its END before calling a signal noise.**
  - **THE REF COLLISION IS NOT THEORETICAL — IT BIT TWO OF MY OWN SCOPING PROBES.** Intext Questions and Exercises are both numbered `1.1, 1.2, …`, so a `^1\.\d` scan for section HEADINGS returned intext QUESTIONS instead, and a first attempt at counting exercises returned "65076373" by matching decimals. Refs therefore carry the stream, and the 3-block outline routed **13/12/41 with 0 orphans** — the collision defused in practice rather than argued about. The same fact forced a separate `CHEMISTRY_TRANSCRIPTION_BRIEF.md`: the Physics brief's prominent **"THE CHAPTER NUMBER IS NOT THE FILE NUMBER"** section is the INVERSE of Chemistry, so an agent following it would apply a `+7/+8` offset and mis-ref a whole chapter. A brief that merely omitted the contradiction would not have been enough; it names it.
  - **THE PAIRING GATE CAUGHT A REAL DEFECT IN THE WILD.** An authoring agent had left a placeholder where `Ex 1.22` belonged, carrying a fabricated id — and the count matched **14 vs 14**, so a count check or a naive id-set check passes it clean. That is the exact permutation failure the gate was built for, found rather than hypothesised, and by the agent's own probe. Two agents also caught **false greens in their own checkers** (one filtering on `type === "math"` / `.value` when the real KaTeX shape is `"inline"|"block"` / `.content`, so it silently verified nothing and reported "0 math zones"), which is why the house expectation is to prove a probe can go red before trusting it green.
  - **A CROSS-BAND REPORT IS A SNAPSHOT OF IN-FLIGHT WORK, NOT A FINDING ABOUT THE FINAL FILE — a new direction for a rule this file already carries.** Band D reported two defects in band C's file (a bare `1.6` ref, an off-list `Molarity` subtopic) — both **false by the time I checked**, because band C had written early as instructed and then rewritten from source. Verified against the file and its mtime before acting; "fixing" them would have overwritten a correct subtopic with a wrong one. Band D was right not to edit another agent's file, and right that its cross-file check found things eye-checking would not — the reporting rule stays, with the verification step made explicit.
  - **CROSS-CHECK: 39 rows diffed — 30 exercise + 9 intext — 0 OUR-ANSWER-WRONG and 0 BOOK-KEY-WRONG**, 14 NO-KEY-ENTRY. Reported as 39 and never 66: the 13 worked examples print their own solutions and are outside the gate by construction. **Two keys had to be read, one per stream**, and the brief's central warning is that both contain the same-looking labels for different questions, so matching a row to a key entry by number alone manufactures findings that look real.
  - **THE ONE ADJUDICATION THAT CHANGED A PUBLISHED ANSWER.** `Ex 1.26` asks for the molarity of \(\text{Na}^+\) in lake water "per kg of water". Re-derived both readings myself: SOLVENT gives \(4.579\,\text{M}\) (molality \(4.00\,\text{m}\)); the LAKE WATER, i.e. the solution, gives \(5.000\) **exactly** (molality \(4.41\,\text{m}\)). Only the second route reaches the book's printed value, and within that one sentence "lake water" and "water" denote the same thing — so the solution was rewritten to that reading, **and names the other one and its 4.58**, because molality-vs-molarity is precisely what the question tests. The key's UNIT is separately wrong on any reading: it prints `m` for a molarity, and 5 is not a molality either way. The authoring agent had flagged the ambiguity in advance and **explicitly refused to pick the round number**, which is what made the adjudication possible — an answer bent toward the book cannot disagree with it. Second bracket: `Ex 1.20`, whose data imply \(K_f = 13.98\) for water against the real \(1.86\) (that solution would freeze at 272.86 K, not the printed 271 K), though it is keyed as a ratio in which \(K_f\) cancels, so no answer is affected.
  - **Verified rather than asserted:** 66 rows, 0 missing a solution / `section_seq` / taxonomy, all 7 subtopics populated so no empty `/browse` filter ships; `audit:text` 0 across six classes, `audit:omml` 0 failing zones, `audit:keys` correctly NO MCQs TO SCAN (wholly free-response); `board:lint` green with the rows enforced post-flip; stored brackets (2) reconcile against errata entries (2), mirrored to source. No SEO movement — `cbse-12` is not `practiceOnly` so landings derive `kind='pyq'` while every Chemistry row is `practice`, i.e. 0 landing pages gained or lost. See [[ncert-chemistry-ingestion]].

- **2026-09-08 (second) — NCERT PHYSICS IS COMPLETE ACROSS BOTH YEARS: Class 11 458 q / 14 of 14 chapters + Class 12 276 q / 14 of 14 = 734 q PUBLIC, 0 PRIVATE. The last chapter had been stalled by an OUTPUT CONTENT FILTER, and measuring the stall before working around it is the whole entry.**
  - **THE STALL COST EXACTLY ONE QUESTION, and nobody knew that because nobody had measured it.** Ch.13 Nuclei sat at 0 rows because an agent transcribing its middle band (0-based p6-14 — 13.5 Nuclear Force / 13.6 Radioactivity / 13.7 Nuclear Energy incl. Fission, Fusion, Controlled thermonuclear fusion) was stopped by an output content-filter, and the session could not conclude. The natural next move — the one that was proposed — is to ship the questions and skip the solutions. Reconciling every committed chapter against its source PDF first showed that is not the trade on offer: **all 13 other Class-12 chapters already matched or exceeded their printed exercise counts** (excess = `(i)/(ii)` splits), the two surviving Nuclei bands already held Examples 13.1-13.3 and all 10 exercises, and the chapter prints Examples 13.1-**13.4** — so the entire blockage was **Eg 13.4**, alone on those nine pages.
  - **AND SKIPPING ITS SOLUTION WOULD HAVE BEEN THE WORST AVAILABLE CHOICE, because of what KIND of row it is.** Eg 13.4 is a `solved` example, so its solution is the BOOK's own printed one, committed inline from the transcription band — not authored later by `apply-solutions`. Dropping it ships a worked example with no working: the one row type where the solution IS the content, rendering on `/board` as a question that reveals nothing. It would also have recreated the NCERT-Integrals defect class this file already records — rows with a correct answer and no working, invisible to `board:lint` (which checks structure), to `audit:keys` (which skips rows with no solution) and to the schema (a NULL solution is legal), surfacing only as a count months later.
  - **The fix was SCOPE, not persuasion — the UPSC 2018-p2 recipe, applied one level finer.** The question is benign conceptual mass-energy content; only the surrounding prose was ever at issue. So it was transcribed BY HAND from the rendered pages and no agent processed the fission sections at all. Worth keeping: it spans a **mid-block page break** (stem + solution (a),(b) on p11, tail of (b) and all of (c) on p12), so a page-scoped read truncates it silently — the same hazard, in the one chapter where re-reading the page was already expensive.
  - **BOTH ANSWER-KEY DEFECTS FOUND THIS PASS ARE REFUTED BY THE KEY'S OWN MATERIAL, which is what made them safe to bracket rather than argue.** `Ex 11.10(c)` (Dual Nature) prints \(3.0 \times 10^{-23}\) m where the de Broglie wavelength is \(3.0 \times 10^{-25}\) — and the key's OWN part (b) fixes \(h = 6.6 \times 10^{-34}\), which at the stated momentum returns exactly \(3.0 \times 10^{-25}\), reproducing its own mantissa with only the exponent differing. `Ex 13.6` (Nuclei) prints `= 26.90 MeV` with the MINUS DROPPED: its own printed expression, evaluated with the stem's own masses, gives \(-0.028881\) u \(= -26.90\) MeV, **and its own verdict "not possible" REQUIRES \(Q<0\)**, since \(+26.90\) MeV would be exothermic and therefore possible. Both were read at 520 and 900 dpi to rule out a misread — on the 11.10 line the same row sets \(10^{-35}\) and \(10^{-32}\), so the typesetting demonstrably distinguishes the digits.
  - **TWO ROWS THAT LOOKED LIKE FINDINGS AND ARE NOT, recorded WITH their mechanism so they are not re-opened.** `Ex 13.2`: Bi-209 is exactly 7.8481 MeV/nucleon, which ROUNDS to our 7.85 and TRUNCATES to the key's 7.84 — a 0.1% last-digit convention the brief explicitly says not to report. (Separately worth knowing, and a defect in neither side's arithmetic: **that key prints PER-NUCLEON values while the stem asks for the totals** "in units of MeV", so it answers a different quantity than the question asks; our solution gives both.) `Ex 13.8`: the 2% gap is entirely the seconds-per-year constant — our elapsed time gives \(4.996 \times 10^{4}\) y at 1 y \(= 3.154 \times 10^{7}\) s, the value the book's OWN constants table prints, while \(3.2 \times 10^{7}\) reproduces the printed \(4.9 \times 10^{4}\) exactly, and the key hedges with "About". **A disagreement whose mechanism you can name is usually not a defect; one you cannot is usually the reverse.**
  - **A FLAG THAT WAS WRONG IN A WAY THAT LOOKED PERMANENT.** `EXAM_REGISTRY` cbse-11 read *"NO mixedFormats — and unlike the other board corpora that is a PERMANENT property, not a 'not yet'"*. The measurement behind it was real (the Class 11 **Maths** book contains zero MCQs) and the conclusion was still wrong, because **the claim was measured on ONE SUBJECT while the flag is EXAM-scoped**. Physics landing on the same exam ended the "permanent" property immediately. Now `mixedFormats: true`. Two near-misses that look like MCQs and are not, both live in this book: "Choose the correct alternative" heading a SUB-PART list where each (a)-(d) carries its own inline either/or, and a "(i) sphere, (ii) cylinder…" list inside one subjective stem — **key on whether the four alternatives are RIVAL ANSWERS, never on the instruction line or the label shape.**
  - **A SCOPE CLAIM OF MINE THAT WAS HALF WRONG, corrected because it changes how a run is read: `board:lint` FETCHES every `question_kind='practice'` row regardless of visibility and only ENFORCES on PUBLIC ones.** I had said it could not cover the pre-flip rows at all. That is why its count moved 14,627 -> 14,642 across this work — **+15, Nuclei's newly-committed rows, not +111** — while the 96 wave-4 rows had been counted the day before as fetched-but-exempt. Running it after the flip was still the right call: those 111 are only now actually enforced.
  - **Verified rather than asserted**, and independently of the authoring agents' own reports: 11/11 dump rows covered with **(id, ref) PAIRS** diffed rather than counts or the id set (the permutation failure mode where every solution lands on the wrong question and every count still matches); 0 control chars, 0 double-escapes, 0 U+FFFD; **144 math zones rendered through the project's own KaTeX, 0 failing**; `audit:text` clean across all six classes; `audit:omml` 0 failing zones over 261 Class-12 rows; `audit:keys` correctly reporting NO MCQs TO SCAN for the wholly-subjective chapters (a real result, not a false green); `board:lint` green; stored errata brackets reconciling against errata-file entries in both chapters. Flip dry-run counts (47+15+14+13+12+6 subjective, +4 MCQ) reconciled exactly against the applied counts, and every flip is `source_file`-scoped so it cannot reach another chapter — the `scan-flip` lesson.
  - **No SEO movement, checked rather than assumed:** `cbse-12` is not `practiceOnly`, so `listChapterLandings` derives `kind='pyq'` while every Physics row is `question_kind='practice'` — 0 `/questions` landing pages gained or lost. The rows are live on `/browse` (Practice toggle) and in `/board`.
  - **Owed, and stated rather than implied: the browser click-through.** `/board` reveals answers on click, so nothing headless proves the reveal lays out — and that now includes two errata brackets carrying inline math. See [[ncert-physics-ingestion]].

- **2026-09-08 — THE CADET VOCABULARY BOOK SHIPS COMPLETE: 4,100 entries / 199 pages / 4 parts, with Part 1 restructured from four letter bands into an EIGHT-RUNG CLASS LADDER (migrations 0092 + 0093). The A-Z index was dropped, a contents page replaces it, and 398 words were authored to fill the rungs the source book leaves thin.**
  - **THE INDEX WENT ON THE USER'S CALL, AND THE LADDER IS WHAT REPLACED IT.** The A-Z index was 21 of 176 pages and its stated purpose was telling a reader which PART holds a word — a fact they cannot know before looking it up. That earns less in a book used as a WORKLIST than as a reference. Removing it also removed the reason Part 1 was cut into letter bands: **a band answers "where do I look this word up", a rung answers "what should I know by now"**, and only the second is a question Part 1 exists to answer. All four index surfaces went together (route, print component, two scripts, the index lib) plus `indexTagFor` and the now-dead `indexTag` fields, so nothing is left half-wired.
  - **THE SPLIT IS ONLY POSSIBLE BECAUSE OF *WHICH* CLASS A WORD IS FILED AT, and measuring that inverted my own recommendation.** I first advised AGAINST per-class chapters, and the data supported it: the source reprints a word in every list that revises it, so **676 words occupy 1,125 class slots, 188 (27.8%) appear in two or more, one in seven**, and Class 7 and 9 share 86% of the smaller list while 11 and 12 share 75%. A naive split prints **449 duplicate entries**. What dissolved the objection was the user's framing — *a student in class N should know these words* — under which a word revised in Class 8 was already learnt in Class 5 and belongs there, printed ONCE. `extract-docx.ts` warns the labels are not a difficulty axis; that warning still stands against ORDERING the book by them and does not touch the question the ladder actually asks.
  - **THE THIN RUNGS ARE THE POINT, NOT A DEFECT.** Filing at first-introduction leaves Class 9 with 21 of the source's words and Class 12 with 30, precisely because those printed lists are re-runs. Those are the rungs to author into. Target 130 a rung (the user's pick from three costed options); **Class 5 keeps its 164 rather than have real CBSE words dropped to hit a round number.**
  - **`school_source` IS THE COLUMN THAT MATTERS (0092), and it is structured data rather than prose for a reason this project has learned twice.** A heading that says "Class 9" is a claim about a word's level, and most of the upper rungs are OURS: `'cbse'` where a publisher graded it, `'authored'` where the level is our judgement. Prose in a blurb cannot be queried, and this project has twice shipped derived content that did not announce itself. **Not printed per entry** — the book's rule is that presence carries a claim (`sentence_source` present = a real paper asked it), and an "authored" label beside a word would read as a warning about the WORD rather than a statement about its grading. The part blurb says it once.
  - **0093 IS DEFERRED FROM 0092 ON PURPOSE, and the reason generalises.** "A school row must carry both columns" cannot hold the instant the columns are added — the 676 existing rows are NULL until the re-filing runs. Bundling it would either fail outright against real data or, **worse, pass against the EMPTY test project and fail only on a later replay**. All five CHECK behaviours were PROVEN by self-aborting `DO` blocks rather than assumed: the range refuses Class 4, the source refuses `'guessed'`, a valid pair is accepted, a school row cannot lose its rung, a non-school row cannot acquire one.
  - **PARALLEL AUTHORING COLLIDES STRUCTURALLY, AND 79 OF 398 DID.** Seven rungs were authored by seven agents against a SNAPSHOT of the taken list, so every author checked correctly against the book and none could see the others — and words on the boundary between two rungs are exactly the ones two authors both reach for. The Class 10 author PREDICTED this in its own report before the run finished. Contested words go to the LOWER rung (the ladder's own rule, and how the 676 CBSE words were filed); the 79 replacements were written by ONE agent covering all four short rungs precisely so it could not recur. Serialising seven agents to avoid a collision this cheap to resolve is the more expensive half.
  - **THE AGENTS FOUND A DEFECT IN MY OWN GATE, THREE TIMES OVER.** Exact-match grepping is insufficient — `evade` when *evasive* is taken, `zeal` when *zealous* is — and one author measured that exact matching alone would have shipped **~25% of its list** as duplicates of words already in the book. The Class 9 author then found that **a fixed 5-character stem check is BLIND to short words** (`gnaw`/`gnawing`, `urge`/`urged`, `zeal`/`zealous`), a bug its own first scan had. The check now tests PREFIX CONTAINMENT. It is a **warning, not a refusal**, and the asymmetry is deliberate: `vacant` when `vacation` is taken is two unrelated headwords sharing letters, and no rule separates that from a real inflection — so it reports and a human reads. All six survivors were read; all six are coincidental (`dire`/`direct`, `knack`/`knackered`, `flippant`/`flip`).
  - **THE HONEST BAD NEWS, which is worth more than the counts: the school ladder is mining what Parts 2-4 have left.** At Class 11 the A-E band survived screening at **7.5%**, not the 58% first estimated, because the exam parts already hold that vocabulary. Neither top rung is padded — both authors DECLINED the exotic survivors (*obsequious, truculent, querulous, cynosure, turpitude, adumbrate*) rather than reach into the archaic tail to hit a number, and said so. **A ninth rung at that difficulty is not fillable honestly.**
  - **VERIFICATION DISCIPLINE, in both directions.** One agent's collision check used `grep -oP` on a build with no PCRE support, extracted ZERO words and dutifully reported "0 collisions"; it caught the false green itself and re-ran with a control that had to go red. The top-up author injected four faults and confirmed its checker went red on each before trusting a clean run. **Against that, my own worst error: I "added" the near-collision check, the script printed `added`, typecheck passed — and the block was never written**, because Python's `replace()` silently no-ops when the needle misses and I had backslash-escaped backticks Python does not unescape. Only grepping for the code found it absent. **A print statement is not a verification.**
  - **[[heredoc-backslash-eating]] FIRED THREE TIMES IN ONE SESSION** — a `\n` becoming a real line break inside a TS string literal, the silent no-op above, and a third mangling that forced a file to be rewritten wholesale through the editor. That is now the standing rule for this pipeline.
  - **A PRE-EXISTING DEFECT FOUND BY THE RESULTING SWEEP: `check-authored.ts` held LITERAL control bytes** where its `CTRL` regex meant escape sequences — the same corruption already recorded for `scripts/bank-paper/english.ts`. **The behaviour was never wrong** (proven, not assumed: both forms compared across all 256 codepoints, ZERO disagreements), but the file read as BINARY, so `grep` printed "Binary file matches" and **every text probe in the repo had been silently skipping it — including the probes that catch this very class of corruption elsewhere.**
  - **A TOOL BUILT FOR A PROBLEM THAT WAS ALREADY SOLVED, and how it surfaced.** `renumber-rungs.ts` was written because `commit-entries` assigns `position` from the authored FILE's order, which must collide when a chapter is topped up. Its dry run reported **0 rows to move** — contradicting my prediction — and reading the code rather than banking the green showed `commit-entries` already renumbers each touched chapter alphabetically, from a defect a previous session had found and fixed at the source. Deleted rather than left as dead code.
  - **Other shape decisions on the printed page, all reversed or set on the RENDER rather than on reasoning.** Chapters were made continuous and then **REVERSED**: a continuous section break lands the next chapter's heading part-way down the LEFT column with the previous chapter's entries still filling the right one, so it reads as a heading for the text beside it. The chapter name is CENTRED (left-set and full-width it was hard to tell from a long headword). Entries are numbered **within the chapter**, restarting at 1, so work is set precisely ("Part 2, NDA A-B, 1-40") and a corpus addition's churn is confined to the one chapter that changed. 6pt between word groups, 13pt where the initial letter changes — the old 2pt packed entries so tightly that a synonym line read as belonging to the entry below. The contents page carries **no page numbers**: nothing here knows where Word breaks a column, and a contents whose numbers are wrong is worse than one that has none.
  - Verified rather than asserted, from the database and from Word: **all 8 rungs at target (164/130 x 7), every row carrying a class and a source, 0 rows whose class disagrees with their chapter, 0 non-school rows carrying a class, every chapter in the book alphabetical and contiguous, 0 flags**; the export opens in Word at **199 pages / 62 sections / 4,100 entries**; `GATE_RESULT: PASS` with the merge tree byte-identical to the gated commit; 761 prerendered `.html`. `page-estimate.ts` is now calibrated three times and runs **13-17% high** — the bias is deliberate and kept, but quote the measured number. **Owed, and stated rather than implied: the browser click-through of `/books/vocab`** — every route there is superadmin-gated and `force-dynamic`, so the gate proves they compile and never that they lay out. See [[cadet-vocab-book]] + [[vocab-book-pipeline]].

- **2026-09-07 (fifth) — the FIRST practice mocks ship: five 120-question NDA Paper I papers from the LWS NDA-II 2026 test series (mocks 187 -> 192). The corpus turned out to be 97.5% ALREADY IN THE BANK, and 10 of the 11 questions that were NOT are broken — which is the finding, not the mocks.**
  - **THE DEDUP GATE PAID FOR ITSELF AT ITS FULL DESIGNED VALUE, and the runbook's ordering rule is why.** `.claude/commands/lws-test-ingest.md` mandates *dedup FIRST on stems, derive SECOND*, a rule it earned when a 75-question test was fully transcribed and every answer derived before the dedup ran last and found all 75 already PUBLIC. Here five agents matched 600 printed questions against the 7,294-row NDA Mathematics corpus: **585 DUP - 4 MAYBE - 11 NEW**. Test-5 is **120/120 duplicate**. Going straight to transcription — the natural reading of "ingest this content" — would have vision-transcribed and derived **600 questions to add 11**.
  - **MY OWN PRE-GATE ESTIMATE WAS WRONG BY A FACTOR THAT MATTERED: I measured ~55% overlap and the truth is ~98%.** That estimate came from an `ilike` over 32 distinctive prose stems, which is a far weaker instrument than semantic matching over the whole corpus. The gate is not a formality even when you think you already know the answer.
  - **THE HEADLINE: the "NEW" set is ENRICHED FOR DEFECTS, and the asymmetry is stark.** Across ~150 bank rows the agents independently re-derived, they found essentially **zero** wrong keys. Of the **11 questions with no bank match, 10 have no correct printed option**. These questions are not absent because nobody ingested them — **they are absent BECAUSE they are broken**; a previous ingest of this same material filtered them out. The bank is the good half of this series and the dedup residue is the reject pile. Corroborated by the papers' own solution PDFs, sometimes in writing: Mock 1 Q110's solution states *"the required sphere is identical to S, but the options suggest a slight variation in the constant term for different contexts"*; Q62's derives exactly 664/1225 and then keys 0.52; Q72's silently solves a DIFFERENT data set; Mock 3 Q44's derives k = -3 while its own key says -1/2 (both inconsistent); Q100's prints `72 + 107 = 179`, verbatim the overlapping-triangles error an agent had reconstructed before ever seeing it.
  - **A RESOURCE I EXTRACTED AT THE START AND THEN NEVER USED: the SOLUTION PDFs.** All five papers ship one — 18 pages, clean text layer, keyed by question number. An agent found and used them, and they settled Q49 outright and exposed Q92's faulty method. They are a THIRD independent ground truth on every derived key, free, and they were sitting in the same folder the whole time. Consult them first on any source of this shape.
  - **REPAIR vs REPLACE was decided per question by asking whether the true value is RECOVERABLE from what was printed.** Five were repaired with no invented content, every before-state asserted by `build-nda2-2026-records.py`: Q63 needed only a **re-key** (5/6 is neither (a) nor (b) and (c) is not a value, so "None of these" was simply correct); Q49 needed only a **stem fix** (`x^3` -> `x^2`, confirmed as printed at 9x DPI AND from span metrics — 6.48pt raised against an 8.04pt body — but the paper's own solution restates it as `x^2` and reaches 12 = (a), so the STEM is the misprint and the key is right); and Q65 / Q68 / Q92 are single corrupted digits whose true value is recoverable (3/**256** for 3/**715** with the numerator already right; 29.**33** where the author's own working prints 29.53; 64/663 for 59/663 on the SAME denominator). The other five were REPLACED, because repair there means authoring: three have no printed option anywhere near the truth, **Q110 is degenerate by construction** (the plane passes through the sphere's own centre), and **Q44 is structurally unfixable** — only two k values are inconsistent, so with four options at least two must be consistent and no unique answer can exist at ANY option set.
  - **THE REPLACEMENT FILTER THAT MATTERED IS RULE 1: never take a set member.** Matching on chapter + subtopic + difficulty alone surfaced *"What is u+v+w equal to?"*, *"The centre of the sphere lies on the plane"* and *"What is the area of the parallelogram?"* — all unanswerable alone, because they depend on a shared stimulus. Swapping one in would have been WORSE than the question it replaced. `find-replacements.ts` tests `set_id` **and** `context`, since this bank contains rows that share a context with `set_id` NULL. Every replacement key was verified by independent derivation, and each substitution records its own reason in the committed question list rather than happening silently.
  - **A SEPARATE BUILD PATH, and the reason is that a practice paper CANNOT BE DISCOVERED.** `build.ts` finds a paper by asking the corpus a question — which (year, month) sittings exist, which `source_file`s, which row BLOCKS within one. A practice paper has no sitting and its questions are scattered across 40+ unrelated source files, so it exists only as an ordered LIST of question identities, which is the committed artifact (`scripts/mocks/data/nda2-2026-practice.json`). What the two paths share is what matters: `buildMockPaper` and the `mock_tests` column list.
  - **`sourceRow = n`, and getting this wrong would have failed loudly rather than quietly — which is the good outcome.** `buildMockPaper` orders within a section by `sourceRow`, meaningful for a PYQ paper and meaningless here: 40+ files with overlapping row numbers collide, and `validatePaperRows` rejects the paper as ambiguous, correctly. The printed question number IS this paper's ordering key. No change to the pure core.
  - **THE GATE THE PYQ PATH GETS FOR FREE AND THIS ONE DOES NOT: PUBLIC.** A mock stores refs and renders content live through the RLS-bound cookie client, so a ref to a PRIVATE or missing row does NOT error — the student sees a BLANK question and `verdictFor` scores it skipped. `build.ts` is immune because its fetch is hard-filtered to `visibility='PUBLIC'`; a bank-mirrored paper takes its ids from a dedup pass that deliberately saw EVERY visibility, so the builder re-asserts PUBLIC + exactly-one-correct-option itself and REFUSES the paper otherwise. `preflight-practice.ts` runs the same check ahead of time; on first run it confirmed **586/586 ids resolve and all are PUBLIC**.
  - **Q109's figure is load-bearing, not decoration** — its stem is *"The curve given below represent a/an"* and is unanswerable without the plot. The transcribing agent deliberately did NOT describe the y-axis in the stem, because that axis is labelled *"Cumulative frequency"* and that phrase IS the answer. The figure is rendered from the page REGION rather than extracted as the embedded raster, because the rotated axis label sits outside the image's own rect.
  - **Three ledger entries came out of the dedup pass**, all verified rather than taken on an agent's word and all left unfixed pending permission: a PUBLIC row keying an angle that makes its own equations unsolvable (verified algebraically AND by a 400k-point scan — the `pi/6` branch reaches residual 0.000022, the `5pi/6` branch bottoms out at 2.098, and its own PRIVATE twin keys correctly); substantial internal duplication logged as a RANGE (91 strict / 161 aggressive) rather than a number, because the aggressive key collapses `\sin^{-1}` and `\tan^{-1}`; and 6 PUBLIC MCQs shipping an unreadable option.
  - **THAT LAST ONE I MEASURED WRONG TWICE — 4,657 -> 381 -> 6 — in the same session in which I logged the ledger entry warning about exactly this.** A `length <= 1` scan matches every legitimate single-character option (`0`, `1`, `n`, `pi`); restricting to markup fragments still counted **375 JEE image-options**, where empty text is CORRECT because the option is a picture. The honest figure is 6, severity LOW (the key is on a valid option in all six, so nobody was mis-graded). The narrative is in the entry because it is worth more than the finding.
  - Verified from the DB, not the builders' output: all 5 mocks `source=practice` / `scope=full` / **`pyq_year=NULL`** / 120 refs each **all distinct** / 0 non-PUBLIC / 0 dangling / 0 bad keys / positions contiguous 1..120 / one marking scheme. `check-taxonomy` clean on all three record sets (nothing auto-created), `commit-paper` inserted 6 with 0 skipped, `audit:text` clean across all five classes, `audit:keys` 0 flagged, `audit:omml` 0 failing zones, **`GATE_RESULT: PASS`** (284 files / 4,184 tests), 761 prerendered `.html`. `db:backup` taken before the bulk write (76 tables / 390,812 rows). **Owed and stated rather than implied: the browser click-through** — the runner is client-interactive behind auth, so nothing headless proves a practice mock starts, grades and reviews; worth sitting one end to end, including Q109's figure. See [[mock-tests]].

- **2026-09-07 (fourth) — /mock gains a TYPE axis: past papers · practice mocks · sectional tests (migration 0088). SKELETON ONLY — every route, group and label ships with ZERO practice or sectional rows, so nothing has to be un-built when content lands.**
  - **THE LOAD-BEARING DECISION IS THAT IT IS TWO AXES, NOT ONE ENUM — and both combinations already exist as printed papers.** The obvious column is `kind: pyq|practice`. It is wrong because sectional tests are coming and `papers` already holds *"NDA - Matrices & Determinant"* (25 questions, every one a PYQ) beside *"NDA Maths — Circles, Height & Distance and Binary Numbers"* (60 assembled ones). A single enum would grow to four values that are really a product type, or gain a second column later that makes the first one's NAME a lie. So `source` (pyq | practice) and `scope` (full | sectional) are separate, because they answer separate questions a student actually asks: *am I sitting the real thing?* and *is this three hours or thirty minutes?* Navigation follows **scope** first — that is the commitment axis — and `source` renders as a badge only where the two genuinely mix (the sectional list and `/mock/attempts`).
  - **This also de-risks the phase nobody has built yet: `mock_tests` already denormalises `duration_secs`, `marking`, `sections` and `total_marks` per row, so the blueprint is a BUILD-time contract only. A sectional test needs no further schema — it carries its own duration and marking.**
  - **TWO CONCRETE BREAKAGES forced the new level, and the second is the interesting one.** (a) `/mock/exam/[slug]` was a flat year-grouped list — NDA is already 36 cards under 10 year headings and MHT-CET 64; the 27 assembled papers sitting in `papers` would take NDA past 60. That is exactly the wall the 2026-08-29 picker rewrite removed one level up, recurring one level down. (b) **Year is the SECTION HEADING, and an assembled paper has no sitting.** Stamping the build year would print *"2026"* over papers nobody ever sat. So `pyq_year` became NULLABLE, guarded by `mock_tests_full_pyq_has_year`: only a paper claiming to be a whole real sitting must name one — a SECTIONAL test built from PYQs draws on many sittings and honestly has no year, so "pyq implies a year" would have been the wrong constraint.
  - **All three CHECK behaviours were PROVEN, not assumed** — a self-aborting `DO` block confirmed it refuses an undated full past paper, refuses an undeclared `source`, and **allows** an undated practice row (the whole point), then `RAISE`d so nothing committed; prod re-verified at 187 rows / 0 null years afterwards. Both new columns DEFAULT to what every existing row is, so the migration is a no-op and `build.ts` kept working untouched — though it now writes `source`/`scope` EXPLICITLY rather than leaning on a default, so a row can never acquire a type by accident.
  - **THE ROUTE SPLIT IS A CACHING DECISION AS MUCH AS AN IA ONE.** `/mock/exam/[slug]/[type]` is a real route rather than a `?type=` filter because a searchParam here would either bail the static prerender to client rendering (`useSearchParams`) or make the route dynamic (server `searchParams`) — and these are ISR pages the sitemap points at. All **15** (exam × type) combinations prerender, INCLUDING the empty ones: a type page is reachable and shareable, so it must render an honest empty state rather than 404 the day before a build lands. Empty ones are kept OUT of the sitemap — reachable is not the same as worth indexing.
  - **`mockKindNote` states the type on EVERY instructions page, past papers included.** Labelling only the practice mocks would make the ABSENCE of a note mean "this is the real thing" — a claim carried by omission, on the one screen someone reaches from a shared link with no route context. Same [[default-becomes-assertion]] shape the syllabus map's "no single section" and the MHT-CET negative-marking line each cost a session.
  - **THREE PRE-EXISTING DEFECTS THAT THIS CHANGE WOULD HAVE MADE WORSE, each found by asking what a null year does downstream.** (1) **`readPublishedMocks` (the recommendation email) casts `pyq_year as number` unchecked**, and its whole selection logic is `sittingRank = pyqYear * 10 + month` — so a practice mock would rank as year 0 and still be emailed to a student as "the next paper in the sequence". Scoped to `source='pyq' AND scope='full'`: today's behaviour exactly, and it refuses to silently extend an email feature to content it was not designed for. (2) **Postgres orders NULLS FIRST on a DESC sort**, so every undated mock would have floated ABOVE the newest real sitting in both the catalogue and the admin performance table — `nullsFirst: false` in both. (3) **The CDS picker copy was stale**: tagline `"English"` and a blurb naming only the English paper, three weeks after GK and Elementary Mathematics shipped — the exact hand-typed-string rot that file's own header warns about.
  - **A MEASUREMENT ERROR OF MINE, corrected: I reported the `getPublishedMocks` overfetch as 84x and it is 53x.** The first figure divided the discarded `questions` payload (2,446 kB across 187 mocks) by only three columns (29 kB); the honest denominator is the whole used payload, 46 kB. The finding stands and is worth knowing: `MOCK_SELECT` is shared with `getMockBySlug`/`getMockById`, which genuinely need the ordered snapshot, so the LIST fetches it and discards it on its own last line (`const { questions: _q, ...rest }`) — **after** Postgres has read it, PostgREST has serialised it and it has crossed the network. This change takes the pages calling it from 6 to 21, i.e. ~15 MB -> ~51 MB of fetched-and-binned payload per full build. **NOT fixed — left for a decision**, since it is scope beyond the skeleton; the fix is a second column list without `questions`.
  - **Logged, not fixed (pre-existing, out of scope): the homepage is still DYNAMIC.** `/` declares `revalidate = 86400` and then calls `getSessionMember()`/`getSessionUser()` at render to redirect staff, so it emits no `index.html` and that directive has never taken effect. The 2026-08-13 entry fixed its DB reads (`unstable_cache`) but left the cookie read, which is what decides the render mode — the [[shell-component-decaches-site]] shape, one page rather than the whole site. Fixing it means moving the staff redirect to middleware or a client island.
  - Verified: **`GATE_RESULT: PASS`** (typecheck -> lint -> notes:lint -> notes:latex -> test -> build); **761 prerendered `.html` on disk** with all 20 mock exam+type pages static and `/mock`, `/notes`, `/guide`, `/questions` all still present (the filesystem probe, not the route legend); **24 new TDD cases** in `tests/mock-catalogue.test.ts`, every one confirmed failing for the right reason before implementation, plus 2 added to `tests/mocks-nav.test.ts`; migration replayed into the test project (`88 files, 87 applied, 1 pending -> ok`). **Owed and stated rather than implied: the browser click-through.** These are static pages so the build genuinely proves they RENDER — but not that they LAY OUT, and the mobile side-nav is a Radix Sheet that does not exist until tapped.
  - **Deliberately NOT done — Phase 1 is the content.** No practice or sectional mock is published; the `papers` -> `mock_tests` builder (27 eligible full-length papers, measured in the 2026-09-07 analysis) and the student-facing title scheme for them are the next step. See [[mock-tests]].

- **2026-09-07 (third) — an OMR key mismatch on the paper above exposed THREE wrong keys in live PUBLIC NDA GAT PYQs; all three corrected against the ORIGINAL printed booklets, and 8 student mock attempts re-graded upward. A fourth finding is bigger: one 10-question PYQ block is keyed "A" nine times and is wrong eight times.**
  - **THE TRIGGER WAS A THREE-ROW DISAGREEMENT, and every one resolved against us.** Grading the new paper, the teacher's Evalbee RESULTS key and the tags sheet built from the bank disagreed on printed Q10, Q28 and Q29 — and the teacher was right in all three. Reading each sentence settles it: Q10's parts are cut so that **"what you cannot" must be completed by "control, shift your energy"**, which only RSQP does; Q28's "dealing" needs its object and "with them" needs an antecedent, which only QSPR supplies; Q29's Q is the only part carrying a finite main clause, so only QPSR can open the sentence. The keyed alternatives are broken, not merely inferior.
  - **THE BANK'S OWN SOLUTIONS ALREADY SAID SO — and that detector found EXACTLY these three, with no misses and no false positives.** A scan of all 288 rows in the chapter for a solution that derives one sequence and then defers to the key returns three rows: *"…gives QPSR. **The answer key indicates PRSQ.** Matches option B"*, *"…gives QSPR. **On review**, QPRS also assembles a coherent sentence; the standard key is QPRS"*, and one that derives "QPRS" — **a sequence that is not even among the four options** — then picks A. Two entirely independent detectors, the teacher's key and the bank contradicting itself, landing on the same three questions is as settled as this gets. All three solutions were rewritten so they justify the answer instead of deferring to a key.
  - **THE USER'S INSTRUCTION — "review those against original pdf/doc not xlsx" — IS WHAT MADE THE CORRECTION LEGITIMATE, and it changed the verdict's basis rather than confirming it.** The standing convention PRESERVES a printed paper's key even when wrong, because students were marked against it. Reading the booklets removes that objection at the root: `GAT_2022_NDA1.pdf` (SDFR-F-TAG/44A) and `GAT_2020_NDA1.pdf` (KJU-F-GTA) are UPSC **question** papers and **print no answer key at all**, so there is no issued key being overridden — the stored letters came from a prep-house `.xlsx` Answer column, which the 2026-08-26 precedent already treats as a derivation. The booklets also confirmed our parts and all four options are byte-faithful in every row checked, so the defect is isolated to the key. **Both PDFs are pure scans (0 text characters), so this was a vision read, page by page.**
  - **REAL STUDENTS WERE AFFECTED, AND CHECKING THAT WAS NEARLY MISSED.** These are PYQs, so they sit in published mocks — `nda-2022-apr-gat` and `nda-2020-apr-gat`, 4 submitted attempts each. `mock_attempts` stores a FROZEN score, so a key flip alone would leave the review screen calling an answer correct while the result screen still called it wrong. **All 12 answer-events were students picking the correct answer and being marked wrong**; re-graded via the app's own `gradeMock`, +10.66 on four attempts and +5.33 on four more, every move upward. That 12-for-12 agreement is itself corroboration: candidates reading the sentence naturally chose what the bank called wrong.
  - **THE BIGGER FINDING, adjudicated and deliberately NOT fixed: `GAT_NDA1_2022_PYQ.xlsx`'s rearrangement block is keyed A on 9 of 10 rows and is wrong on 8.** Every other source in the chapter sits at 30-60% max share, so 90% is a defect signature rather than chance. All ten were read off the booklet; **only Q27 and Q28 are correct**. Six remain wrong (Q23→C, Q24→C, Q25→B, Q26→D, Q29→C, Q30→B). Blast radius measured rather than estimated: all 6 PUBLIC, all in one published mock, 23 answer-events, and a regrade would **RAISE 16 and LOWER 5** — so unlike the three above this is *not* uniformly in students' favour, which is exactly why it needs its own permission. In the SUGGESTIONS backfill ledger.
  - **AND THE OMR REPORT COULD NEVER HAVE FOUND THEM: the teacher's key agrees with the bank on Q23, and both are wrong.** A disagreement report finds where two sources DIFFER, never where they are wrong in the same direction — so "3 mismatches" is a floor on the defect count, not a measure of it. That is the same correlated-error limit this file records for dual-blind derivation, arriving from a different direction.
  - **A MEASUREMENT ERROR OF MINE, corrected: I reported "5 other wrong keys" and listed six.** The count was wrong, not the list. Also worth recording that **`audit:keys` reports NOTHING SCANNED for both sources** — it is scoped to `question_kind='practice'` and these are `pyq`, so the one probe that could have caught a solution-contradicts-key row has never been able to see them. It says so loudly rather than reporting a false clean, which is the only reason that gap is legible.
  - Verified: three keys flipped in place via `apply-adjudicated-fixes.ts` (asserts the current letter, recomputes `content_hash`, re-stamps IN PLACE so `paper_questions` is never orphaned — each row confirmed "still in 1 paper(s)"), mirrored into the records file with the patched record proven byte-equal to the DB target on stem, solution AND answer; recorded in `question_reviews` as `source_key_crosscheck`/`key_fixed` under run `key-fix:2026-09-07-lws-pqrs-test-1`; the OMR sheet rebuilt (answer distribution moved A11→9, D6→8, exactly the three flips); `verify-commit` still clean on the paper after all three hashes moved; `audit:text` and `audit:omml` clean.

- **2026-09-07 (second) — LWS "PQRS Test 1" becomes a `/dashboard/papers` paper, and the whole test turns out to be ALREADY IN THE BANK: 30 of 30 are live PUBLIC NDA GAT PYQs. Zero questions committed, zero bank drift — and the `verify-commit` gate had to be generalised before it could report the truth about a correct ingest.**
  - **THE DEDUP GATE PAID FOR ITSELF AT ITS FULL DESIGNED VALUE, which is the first time on this pipeline.** The skill's rule is *dedup on STEMS first, derive second*, precisely so a fully-recycled paper is caught before the expensive answer work. It was: all 30 items matched an existing bank row **fragment for fragment WITH THE SAME P/Q/R/S LETTER ASSIGNMENT and the same four printed options**, across five NDA GAT sittings — `GAT_2020_NDA1` Q11-20 → printed Q1-10, `NDA1_2019` Q38-40 → Q11-13, `GAT_NDA2_2019` Q37-40 → Q14-17, `NDA2_2021` Q21-30 → Q18-27, `GAT_NDA1_2022` Q21-23 → Q28-30. The teacher assembled the test out of past papers. **Nothing was derived, nothing was transcribed for the bank, and no answer was authored.**
  - **RECORDS COPIED OUT OF THE BANK, NOT OUT OF THE .docx — and that is what made the commit provably a no-op.** `contentHash` is `sha256(stem + sorted options + answer)` with whitespace collapsed, so mirroring each bank row verbatim makes the hash identical **by construction**; it was then verified against the stored `content_hash` for all 30 **before** applying (0 mismatches). `commitStaged` upserts with `ignoreDuplicates: true`, so `inserted=0 skipped=30`, and commit-paper's `content_hash` fallback resolved all 30 to the pre-existing rows. Confirmed afterwards from the live DB: **0 rows under the new `source_file`, and the Sentence Rearrangement chapter still holds exactly 288 rows — the count it held before.** Re-transcribing from the .docx would have hashed differently on the 17 rows whose bank stem carries a trailing instruction line, and inserted 17 PRIVATE near-duplicates of live PUBLIC PYQs.
  - **`verify-commit.ts` REPORTED 4 FAILURES ON A WHOLLY CORRECT INGEST, and the failure mode is the one the ledger predicted that morning.** Every check was scoped by `source_file`; a bank-mirrored paper owns **zero** rows under its own, so the gate measured an empty set and reported row-count 0-vs-30, a numbering mismatch, foreign paper questions and broken OMR parity. The ledger had identified exactly this on the APJ Biology ingest (where ONE question deduped byte-exactly) and left it unfixed pending permission. **A gate that cries wolf on a legitimate outcome is worse than no gate** — the next person either burns time re-deriving or learns to wave it through — and there was no honest way to report this ingest verified while it was red. Now it resolves each record the way `commit-paper` does (source_file, then content_hash), prints a `note` naming how many questions are bank-mirrored, scopes the practice-kind and dedup-gate checks to rows the ingest actually CREATED, and checks OMR parity on **resolved row identity** rather than `question_number` — which a mirrored row carries from its ORIGINAL paper, so numbering cannot identify a printed Q. **This touched a shipped gate that the ledger says needs explicit permission; it was flagged to the user rather than slipped in, and it is one commit to revert.**
  - **THE CHANGE WAS PROVEN BEHAVIOUR-PRESERVING BY SWEEPING ALL 65 REGISTRY SPECS, OLD BUILD VS NEW: 0 regressions, 27 papers fail → pass, 26 still failing on unrelated pre-existing causes.** Reasoning about the diff would not have been enough — and the sweep is what surfaced a SECOND, unrelated false alarm: a `createPaper:false` paper commits only `status:"new"` rows by design, so its dup/flawed records legitimately have no row and were reading as missing. Fixed by mirroring `commit-paper`'s own `commitRecs` rule at the call site so the two cannot drift.
  - **OMR PARITY WAS CONFIRMED A SECOND, DIFFERENT WAY, with a deliberately-failing control.** A throwaway read `paper_questions` ordered by `position` and compared raw question ids against the 30 resolved by hand: 30/30 in printed order, each link naming its origin paper. The control (two ids swapped) was detected as wrong — a check that has never gone red proves nothing, and the pre-fix run had already shown this one can.
  - **MY OWN VERIFICATION PROBE WAS WRONG BEFORE THE DATA WAS, and it read as a finding.** A first pass reported **17 of 30 "DIFF"** at fragment level while every option set matched — which should have been the tell. The cause: the bank stem for those rows ends with the printed instruction (`The correct sequence should be :`), and my splitter glued it onto fragment **S**, so S could never match. Corrected, all 30 are letter-for-letter identical. Same shape as [[probe-must-reach-the-code]]: the probe contradicted a much stronger signal sitting beside it.
  - **A LIVE KEY CONFLICT FOUND AND DELIBERATELY NOT FIXED.** Printed Q30 exists in the bank **twice with the identical four options and opposite keys** — the PYQ row (`56ee0236`, `GAT_NDA1_2022` Q23) keys **A = SPQR**, an Oswaal mock row (`563bfed0`) keys **C = QSPR**. QSPR is the grammatical reading; SPQR strands "in the arena of sport, perhaps" mid-sentence. They survive `content_hash` because the two typings differ in apostrophe and separators, and `audit:keys` never scans the pyq row because it is scoped to `question_kind='practice'`. Shipped PUBLIC content ⇒ logged to the SUGGESTIONS backfill ledger with the instruction to settle it against the actual UPSC paper first, **not** flipped on a reading.
  - **`flip-public` must never be run for this slug** — all 30 records are `status:"dup"`, so it is a no-op by construction, and the spec comment says so. Verified: paper `cde90a37-…`, 30 links, positions 1-30, one section, status `draft`; `check-taxonomy` clean; `build-tags` emitted the 30-row OMR sheet; typecheck clean; a `db:backup` was taken before the write. The scoped `audit:text`/`audit:keys`/`audit:omml` runs are deliberately skipped — they filter on `source_file`, which owns zero rows here, and the 30 PYQs they would otherwise scan belong to five other corpora.

- **2026-09-07 — JEE Mains gets 24 timed mocks (163 → 187), and they are the first in this bank whose answers are NOT option labels. NEET needed nothing: it was already complete. Migration 0087.**
  - **THE ASK WAS "JEE MAINS AND NEET", AND HALF OF IT WAS ALREADY DONE.** NEET has 8 PYQ papers in the bank and 8 published mocks, counts matching exactly — nothing to build until a new sitting is ingested. Measuring that first is what kept the session from re-deriving a finished pipeline. (Incidentally NEET has since grown a **1,769-question PRACTICE corpus** this file does not record; irrelevant to mocks, which are PYQ-only, but the header is stale.)
  - **A `source_file` IS NOT A SITTING FOR JEE, and that is a THIRD discovery rule.** NDA splits sittings by `pyq_month`; CDS, NEET and MHT-CET by `source_file`. JEE can use neither: `pyq_month` is NULL on all 10,493 rows, and a 2025 file holds a whole DATE — `JEE_2023_Jan25.docx` is 180 rows because rows 1-90 are one shift and 91-180 the next, `JEE_2025_Jan23.docx` is 150 = 2 × 75. So a sitting is a **`source_row` BLOCK within a file**. Read off the data, not assumed: each block runs Physics 1-20 MCQ / 21-25 NAT, Chemistry 26-45 / 46-50, Maths 51-70 / 71-75. The split is deterministic (`block = ceil(source_row / 75)`), so no registry guesswork was needed — only the holds are hand-maintained, and `scripts/mocks/jeeSittings.ts` derives everything else from `scripts/jee/papers/*.json`.
  - **ONLY 2025+ IS EXPRESSIBLE, and the reason is a pattern change rather than a corpus gap.** A 2021-2024 shift printed 90 questions of which a candidate attempted **75** — all 60 MCQ plus **any 5 of 10** NAT per subject. `gradeMock` has no "attempt at most N" concept, so those sittings would score out of **360 instead of 300** and let a student answer all ten; the marking also differed between Section A and B in those years, while `buildMockPaper` stamps one flat `marks`/`negMarks` on every question. From 2025 Section B is 5 compulsory NAT at the same +4/−1, so the paper is simply its 75 and the whole problem disappears. The 2021-2024 corpus is ingested and deliberately NOT built; `deriveJeeSittings` drops it by year rather than failing on it.
  - **THE DATABASE ITSELF REFUSED THE FEATURE, which is what made a migration unavoidable.** `attempt_answers.selected_label` carried `CHECK (selected_label IN ('A','B','C','D'))` from 0044 — the only column holding a student's answer — so a NAT response of `17280` was rejected by Postgres and no application code could route around it. **0087 adds `numeric_response numeric` rather than widening that column**: widening is the smaller diff and the worse change, because it would store a number in a column named *label* (read as a letter by grading, palette and review), **delete the existing guard for all 163 already-shipped mocks** to serve 24 new ones, and make the two answer kinds indistinguishable — a student typing `3` on an MCQ is a different event from choosing option C. A coherence CHECK keeps three states legible (both null = unanswered, exactly one = answered, both = refused), the 0046/0051 pattern. **Proven deployed rather than assumed**: a DO block mutated a real row to the incoherent state, reported `constraint_fired=t`, and aborted itself so nothing committed — 31,232 existing rows untouched, 0 numeric set.
  - **THE ANSWER AXIS IS NOW A DISCRIMINATED UNION, and choosing that over an optional field is the load-bearing decision.** `"A"|"B"|"C"|"D"` ran unbroken from storage through `loadAnswerKey`, `gradeMock`, `paletteState`, `MockRunner` and the review page. An extra optional `numericAnswer` field would have let every one of those keep reading `.label` and silently mis-grade Section B; a union made **the typechecker enumerate all of them — 9 source call sites plus 3 test files**, including `scripts/reviews/regrade-attempt.ts`, which was not on the plan's list at all.
  - **THAT SCRIPT TURNED OUT TO BE THE WORST OF THEM.** It read the key straight from `options` — a SECOND implementation of "what is the answer", and one **blind to numeric questions by construction**, so regrading any JEE attempt would have scored all 15 Section-B questions as skipped and quietly moved real students' marks. It now calls the same `loadAnswerKey` the app grades with (`query.ts` has no `server-only` guard, so a script can), and its A-D validation guard became an ABSENCE guard, which is what a validated union leaves worth refusing.
  - **TWO MORE PLACES WERE COMPUTING RIGHT-VS-WRONG SEPARATELY.** `getAttemptReview` derived its own per-question verdict, and `submitAttempt`'s activity emitter compared `selected !== gq.answer` by hand — so the score, the review list and the wrong-answer drill fuel were three implementations free to disagree on screen, and the latter two could not see a numeric response at all. All three now go through one pure `verdictFor`, and the emitter reads the verdict `gradeMock` already produced. Same two-renderers drift this file records twice for the docx exporter.
  - **A SILENT FALLBACK REMOVED: `answer: key[q.questionId] ?? "A"`** marked every A-picker correct on a question whose key was missing. It cannot fire (`validatePaperRows` refuses to build such a paper) but the union forced the question, and `verdictFor` now scores a null key as **skipped, never wrong** — our defect must not cost a student marks.
  - **THE NTA TOLERANCE IS CORRECT AND INERT, and measuring that is worth more than implementing it.** The 2-decimal rule was chosen deliberately — and **all 546 NAT answers across 2025+2026 are non-negative integers at scale 0**, so no real question in the shipped corpus can exercise it. It is implemented properly (a distance comparison, not round-then-compare: `Math.round(4.005*100)` is 400 because 4.005 is held as 4.00499…, so rounding disagrees with itself at the boundary; ties go to the student) and pinned by **synthetic** tests that say so in their own comment. It earns its place in the 2021-2024 tier, which carries 14 decimal answers and negatives down to −640.
  - **THE 2025 SHIFT LABELS ARE AN INFERENCE AND ARE RECORDED AS ONE.** 2026 states its shift twice — the filename (`JEE_2026_Jan21_S1.docx`) and the config's own pyqNote ("21 Jan 2026 Shift 1"). 2025 states it NOWHERE: its pyqNote is the bare date and the extracted pages carry only `Physics / Section - A`, with no shift, session, morning or afternoon marker anywhere. So "Shift 1 = rows 1-75" is convention. Following the `MHT_CET_2025_PCM` precedent it ships labelled but flagged `shiftInferred`, with the builder printing the inference on every run — a distinctive-stem check against a published copy would settle it. **11 of the 24 shipped papers are affected.**
  - **13 OF 37 SITTINGS ARE HELD**, each short by 1-3 questions the bank never had — a mock is the real paper or it is nothing, and a soft count would relabel a 72-question fragment as "JEE Mains 2025 (3 Apr, Shift 1)". A hold is an ASSERTION: the builder still attempts it and FAILS on one that now reconstructs, so closing a corpus hole surfaces as a prompt. The builder's 24-built/13-held split independently reproduced the figure derived from SQL beforehand.
  - **A LATENT GAP FOUND EN ROUTE: `CDS_GK_PAPER` and `CDS_MATHS_PAPER` were never added to `ALL_BLUEPRINTS`**, so `getBlueprint("cds","gk")` returned null for 35 published mocks. Nothing in `src/` calls it today, so it was inert — but it is exactly the gap that bites the first consumer. Fixed, with a test that resolves every exported blueprint so it cannot recur.
  - **A TEST WAS PASSING FOR THE WRONG REASON, caught by my own fixture.** `deriveJeeSittings`'s orphan-hold guard compared the real HOLDS map against a one-file fixture, so the "throws on a stale hold" test threw because *all thirteen* real holds were orphans — not because of the bogus one it named. Holds are now injectable and the test asserts BOTH directions (a matching hold does not throw; only the bogus one does).
  - **`tests/mocks-nav.test.ts` used `jee-mains` in FOUR places as its "registered but no mocks" exemplar** — moved there from MHT-CET on 2026-09-01, the third instance of this shape. Rather than move it to another exam that might gain mocks later, each invariant now uses **`worksheets-11-12`, which is practice-only and therefore can NEVER back a mock**; jee-mains gains the positive assertion. A structurally permanent exemplar, so this does not have to move a fourth time.
  - **VERIFIED, not asserted**, all after the write and against the live bank: 187 mocks · 24 JEE · 1,800 refs · **0 dangling, 0 non-PUBLIC, 0 non-pyq, 0 MCQs with anything but one correct option, 0 NAT without a numeric answer, 0 NAT carrying options** · 0 wrong-section rows · Physics occupies 1-25, Chemistry 26-50, Maths 51-75 in every paper · positions contiguous 1-75 · exactly 15 NAT per paper · every paper drawn from ONE source file spanning exactly 75 consecutive rows · **0 questions appearing in two mocks** (the check that matters most, since blocks 1 and 2 come from the same file — 2025-jan-23 S1 reads rows 1-75 and S2 reads 76-150) · totals 75/300/180min. A slug/id collision check ran BEFORE the write (the mock id is `slugToUuid(slug)`, so a clash silently overwrites a real mock): 24 distinct slugs, 24 distinct ids, 0 colliding. The other 163 mocks were confirmed untouched by `updated_at`, and CDS (55 + 3 held) and NEET (8) still dry-run identically after the union refactor.
  - **OWED, and stated rather than implied: the browser click-through.** The runner is client-interactive behind auth, so nothing headless proves the numeric input accepts a value, that the palette turns green for an answered NAT question, or that the review page's two-value block lays out. Worth checking a Section-B question end to end (type → resume → submit → review) on one 2026 paper.

- **2026-09-06 (second) — `/books` gains its SECOND book: MHT-CET Mathematics, 2,136 printed of 2,228. It is structurally the opposite of the English book, and building it found SEVEN wrong answers in the public bank — two of them in the copy the book would have printed.**
  - **THE CORPUS INVERTS EVERY ASSUMPTION THE FIRST BOOK WAS BUILT ON, measured before a line was written.** `set_id` is NULL on all 2,228 rows against 3,175-of-3,180 in English, so **sets do not exist**; the Layout-A gate ("no set spans a subtopic") is satisfied VACUOUSLY in all 27 chapters and subtopic grouping is free everywhere, where English has three chapters that can never group. 100% MCQ, 100% carry a solution, 0 missing a subtopic, 11 figures in the whole corpus. And the subject row is **`Maths`, not `Mathematics`** — nine other exams spell it the long way; a near-miss there does not error, it yields an EMPTY BOOK, which is exactly what `tests/books-registry.test.ts` exists to catch (it now covers both books in both directions: 27 chapters + 86 subtopics resolved).
  - **THE SITTING NEEDED A THIRD RULE, because MHT-CET reads by neither.** `pyq_month` collapses 45 source-file labels into 6 buckets (17 share `2023/May`, 12 share `2024/May`) and is ACTIVELY WRONG on one — `MHT_CET_2025_PCM.xlsx` is stamped May and is a second upload of an April paper. The filenames carry the date in at least eight conventions, one of them a typo (`14tthMay2024`), so no regex reads them all. The answer already existed in `scripts/mocks/mhtcetSittings.ts`, so the sitting IDENTITY moved to `src/lib/mocks/` (a page needs it at request time and `src/` cannot import `scripts/`, which reaches for `node:fs`) and the mock builder re-exports it. **Proven behaviour-preserving**: 42 sittings, semantically identical output, `tests/mock-mhtcet-sittings.test.ts` green untouched, and the config-agreement guarantee kept — it was structural (derivation) and is now an explicit throw, **proven to fire** on a renamed booklet and on a duplicated source file.
  - **THE HEADLINE FINDING, and it inverts the obvious reading twice.** 122 of 2,228 rows are a repeated stem (5.5%, against ONE duplicate in the English book's 3,180) and would print adjacent under one subtopic heading. They survive `content_hash` because the hash covers the options and two independent typings spell them differently. Collapsing them into one question carrying "Asked N times: …" turns the defect into the book's best feature — a high-yield marker. **But the count must be in SITTINGS, never in rows: 90 of the 118 groups resolve to ONE sitting** through the three duplicate-upload pairs, so a row-counting badge would print "asked twice" on 90 questions that were asked ONCE. That makes `mergeWith` load-bearing for correctness rather than a convenience. Final split: **90 duplicate-upload (90 rows, no badge) / 28 genuine repeat (32 rows, badge)**.
  - **THE STEM KEY MUST KEEP MATH OPERATORS, and measuring that prevented deleting real questions.** The obvious key (strip all punctuation) finds 126 redundant rows; keeping `+ - * / ^ = < >` finds 121. Reading the five-row difference settles it: **three are genuinely DIFFERENT questions** — `cos 2t` vs `cos^2 t`, two plane pairs differing only in a sign, and a line through `z-m` vs `z+m`. A false merge DELETES a distinct question AND mislabels the pair as an answer conflict; one of those three was sitting in my "18 answer conflicts" and was never a conflict at all. The one true duplicate the strict key splits is separated by an empty `_{}^{}`, a typesetting artifact, so that alone is stripped. Net 122 rows, and the rule is that the key errs towards SPLITTING.
  - **SEVEN WRONG ANSWERS FOUND AND FIXED AGAINST THE PRINTED PAPERS — the mechanism is the point.** A defect invisible in one copy becomes loud when the same paper exists twice: where two independent typings of ONE paper disagree about the answer, one is simply wrong and the source settles it outright. Every fix was adjudicated against the booklet AND re-derived by hand, so a defect in the published key could not be inherited. **THE KEEPER IS NOT ALWAYS THE RIGHT COPY** — four sit in the redundant copy and **two sit in the copy the book PRINTS**, so "trust the primary upload" would have shipped two wrong answers. The sharpest: a triangle question stored as `l(AB) = 2sqrt(3)` where the paper prints `sqrt(23)`, which makes it evaluate to 6.4 and **match no printed option** — unanswerable, in BOTH copies; and a cosine keyed as `17/9`, which exceeds 1 and so cannot be a cosine (the paper prints `sqrt(17)/9`). Also two option-text corruptions where all four options read `log_e x` for the paper's `log(e/x)`. Recorded in `question_reviews` under `source_key_crosscheck`; the fix script asserts BOTH the current and target state, refuses the whole batch on any mismatch, and is idempotent. **`content_hash` is deliberately left untouched** — recomputing it would make a re-upload of the original defective spreadsheet insert a DUPLICATE of the row just repaired (the 2026-09-02 statement-layout call, same reasoning). **Fixing the defects then resolved 5 review groups into agreement**, taking the review list 34 -> 29.
  - **THREE LATENT BUGS THE SECOND BOOK EXPOSED, all silent.** `loadBookOverview` hardcoded `examOfSection = { nda, cds }`, so any other section key counts **0** for every chapter with no error; the reader's move-to-section control was a hardcoded `nda <-> cds` flip that would have offered a section the book does not have; and four scripts hardcoded `NDA_CDS_ENGLISH`, so `smoke.ts` would have reported the English book healthy while the MHT-CET book was the broken one. Two more surfaced on first run: **`smoke.ts` assumed TWO sections** (it threw) and compared a section's raw `questionCount` against the TOC, which counts only what PRINTS — equal only in a book with no curation, so it fired on every collapsed duplicate. All five fixed, with the English book **proven byte-identical throughout** (3,180, every per-exam count unchanged, "every check passed").
  - **A GAP NO TEST COULD HAVE CAUGHT: the print view rendered NO provenance at all for a maths question.** Both its branches are conditional on a shared passage or an authored Directions line, and a maths question has neither — so the book would have printed 2,136 questions with nothing saying which paper any of them came from. Provenance is now per-question, and only where the set did not already print one, so the English book is untouched.
  - **FOUR PROBE FAILURES, every one mine, and three of them nearly became findings.** A grep for `l(AB)` "proved" two 2023 files were different papers — the second writes it as `\mathbf{l}\mathbf{(AB)}` and the merge was fine. `grep -c` counts LINES, so minified HTML reported 185 provenance lines where there are 221. A SQL `LIKE` scan for double backslashes returned 0 because **LIKE treats backslash as its own escape** (the real count is 147 options bank-wide). And [[heredoc-backslash-eating]] fired **four times**, twice corrupting a regex into an unmatchable one. In every case the probe was wrong and the data was fine.
  - **Deliberately NOT done, and logged instead:** 29 review groups (16 answer-conflict, 18 taxonomy-split) are left un-collapsed — refusing costs nothing, since both copies then print and neither claims the other's sitting; **147 options + 46 stems bank-wide carry a triple backslash** (79 options / 25 questions in MHT-CET Maths alone), which is shipped content beyond this scope; and 11 groups span two chapters while 21 span two subtopics, a taxonomy inconsistency the book surfaces. **Owed:** the browser click-through — every `/books` route is superadmin-gated and `force-dynamic`, so the gate proves they compile and `render-print.tsx` covers only the print view. That script's own documented invocation was stale and has never worked (it needs `--tsconfig scripts/books/tsconfig.render.json`); fixed.

- **2026-09-06 — the 20 MHT-CET papers "held for absent rows" were not a corpus gap at all: THREE PAPERS HAD BEEN UPLOADED TWICE. Merging the pairs closed 10 of the 30 holds with ZERO re-ingestion, took 45 source_file labels down to 42 real sittings, and mocks 60 -> 64.**
  - **THE FRAMING IN THE 2026-09-01 ENTRY WAS WRONG, and measuring it inverted the conclusion.** Those 20 papers were recorded as short because "rows are absent from the bank (closable by a future ingest)". Almost nothing was absent. `MHT_CET_2023_Analysis.xlsx`, `MHT_CET_13thMay2024_Shift1_QuestionBank.xlsx` and `MHT_CET_2025_PCM.xlsx` are second uploads of 16-May-2023-S2, 12-May-2024-S2 and 19-Apr-2025-S2. Because the two uploads were typed INDEPENDENTLY, `content_hash` deduped only the rows whose typing happened to match, so each paper's 150 questions were SPLIT across two labels — neither reconstructing alone, **the union exactly 150 in all three cases**.
  - **THE DECISIVE EVIDENCE IS STRUCTURAL, NOT A TEXT PROBE.** For each pair: union = exactly 150 distinct question numbers, ~90-107 numbers present in BOTH labels, and 85/93/103 of those carrying an identical normalised stem. Corroborated independently on disk for the 2024 pair, whose two source `.docx` are **byte-identical — question paper AND answer key**; and for 2023, where the undated `MHT_CET_2023_QP.docx` is the 16-May-Shift-2 paper (same Q7/Q19/Q100). An all-pairs scan over all 45 labels found **exactly these three and nothing else** above the noise floor.
  - **A DISK SCAN WAS THE WRONG INSTRUMENT AND UNDER-REPORTED.** Hashing the extracted text of all 45 papers found only the byte-identical 2024 pair; the 2023 pair differs in whitespace/markup and the 2025 pair has no `.docx` at all (its second upload traces to a PDF). Re-running at the BANK level — comparing normalised stems at matching question numbers — is what found all three. **Compare the artifact you actually ship from, not the one on disk.**
  - **A CORRECTION TO THE SHIPPED REGISTRY: `MHT_CET_2025_PCM.xlsx` is 19 April Shift II, not the "14 May Shift 2" I inferred.** That label came from its own May stamp plus the only May-2025 file on disk. It was recorded as INFERRED rather than verified, and it shipped no mocks, so nothing user-facing was wrong — but it is the reason the inferred/verified distinction is worth writing down at the time.
  - **THE MERGE HAD A TRAP THAT NO COUNT COULD HAVE CAUGHT: the two labels of a pair can NUMBER THE SAME QUESTION DIFFERENTLY.** Every `.xlsx` upload runs `source_row` 2..151 (an Excel header row) while the 2025 `.docx` runs 1..150 — measured against `question_number`, not assumed. Merging on raw `source_row` would pair docx q13 with xlsx q12 AND produce ties between adjacent questions, leaving the paper order arbitrary exactly where the conventions collide. So rows are normalised onto the paper's own numbering BEFORE dedupe (`normalisePaperRows`), and `dedupeMergedRows` then keeps one row per (subject, position), preferring the primary label — deterministically, so PostgREST's row order cannot decide it. Verified after the write: all six merged mocks carry a contiguous 1-100 / 101-150 and **0 ordering mismatches**.
  - **THE FEATURE SHIPPED INERT ON THE FIRST RUN, and the counts are what exposed it.** `mhtCetSittings()` maps the registry into the builder's shape and did not forward `mergeWith`, so the dry run printed no merge line and the three sittings failed their hard count exactly as before — which reads as a corpus gap, not a plumbing bug. Caught because the expected 64 came back as 60. A comment now says so at the call site.
  - **WHAT IS STILL HELD: 20 papers — 8 short because a flawed question is deliberately PRIVATE (permanent), 12 short by 18 questions of ordinary cross-year reuse** (MHT-CET re-asks questions and `content_hash` keeps the first copy, so the question sits under another sitting's label). 13 of those 18 have an exact normalised-stem match elsewhere in the bank and are include-override candidates (the NEET-2021-Q5 pattern); the other 5 are maths, where stems are mostly LaTeX and text matching is unreliable. **Deliberately NOT done** — see the effort note below.
  - **TWO THINGS FOUND AND LOGGED RATHER THAN FIXED.** (1) The double-uploads left **~200 near-duplicate rows live in `/browse`** (~93-107 overlapping numbers per pair, ~178 provably the same question typed twice, surviving only because their option text differs enough to change the hash). They inflate MHT-CET's counts and its weightage analysis; cleaning them is a 360 + permission job on shipped public content, so it is in the SUGGESTIONS backfill ledger. (2) **Bank numbering may not equal printed-paper numbering on the 32 `.xlsx` sittings** — comparing three papers against their source documents put 9%, 31% and 22% of matched questions at a different number, though for 10-May-2023-S1 it is a clean systematic block (31 questions all shifted by exactly -2 across q68-100) rather than scatter. Relative order, which is what a mock's sequence depends on, appears preserved. **Which source is right cannot be settled: the `.xlsx` uploads no longer exist on disk.** Recorded rather than guessed.
  - **The probe lesson, twice over.** A word-phrase probe over stems was wrong in BOTH directions — generic chemistry vocabulary ("Calculate total following reaction") matched hundreds of rows and produced four false "already in the bank" hits, while maths stems are mostly LaTeX so word extraction left almost nothing and produced false absences. A 30-char normalised-stem prefix was the usable instrument. **The three pair findings never depended on the phrase probe** — they rest on the union/overlap measurement — which is why they survived the probe being discredited.
  - Verified: 64 mocks (34 Maths + 30 Phy & Chem) · 4,700 refs · 0 dangling · 0 non-PUBLIC · 0 duplicated within a mock · 0 missing keys · 0 wrong-section · 0 ordering mismatches · every merged paper contiguous and complete · 0 orphan slugs left by the three retired sittings (they had never been built, being held). `db:backup` taken before the write. **Owed, unchanged: the browser click-through** — the runner is client-interactive behind auth.

- **2026-09-02 (third) — Rotational Dynamics ships (Std XII Physics 326 → 366 q PUBLIC across 9 chapters) and the whole Physics lane is finally COMMITTED — 110 files that existed only in the database. The finding is that a MANDATORY GATE HAD BEEN SKIPPED and nothing anywhere could see it.**
  - **THE DEFECT: `Rotational Dynamics` was one command from PUBLIC with the step-6 answer-key cross-check never run, and a skipped step leaves NO error — only a missing file.** It was caught by an artifact diff, not by any gate: all 8 already-shipped Physics chapters carry `data/<id>.book-answers.json` + `data/<id>.errata.json`; this one had neither. Every other signal said ship — 40/40 rows committed and sectioned, all solutions applied, 6/6 MCQ keys blind-verified with 0 mismatches, `board:lint` green. **`flip-public` gates on PROVENANCE, never on whether the chapter was cross-checked**, so the publish path had nothing to refuse. Generalises to all ~15 ingestion pipelines here: **before publishing a unit, diff its artifact SET against a shipped sibling's** — presence of a file is the only trace a completed step leaves.
  - **The gate then earned its place immediately: 9 AGREE · 0 OUR-ANSWER-WRONG · 2 BOOK-KEY-WRONG** over the 11 of 40 rows (28%) that carry a printed `[Ans: ...]` — reported as 11, never 40, per the lane's own rule. **`Ex Q.17` (conical pendulum) prints K.E. = 0.45 J, ten times too large, and is SELF-REFUTING on the key's own numbers**: the `ΔP.E. = 0.04 J` printed beside it uses m = 0.1 kg correctly, while 0.45 J is exactly what ½mv² returns for m = 1 kg — so one printed key uses two different masses. Correct value 0.045 J, confirmed by a second route (`tanθ = v²/rg`) and by the ratio K.E./ΔP.E. = 1.125 rather than 11.25. `Ex Q.20` prints the unit as `g cm⁻²`; the value 24000 is right and a moment of inertia is mass × length², so `g cm²` — dimensionally the printed form is not a moment of inertia at all. Both bracketed and mirrored back to source, so a re-commit cannot revert them.
  - **A THIRD defect was found and deliberately NOT bracketed, which is the more interesting call.** The book prints **" X) "** as Q.1's sixth sub-item, after i) ii) iii) iv) v) — its own typo for `vi)`, verified on the rendered page. Our ref `Ex Q.1 (X)` is therefore FAITHFUL. A student-facing bracket would be noise (the answer is unaffected), but the real risk is a future session "correcting" a faithful transcription — so it went into the `config.ts` chapter comment, which is where this codebase already keeps structural per-chapter facts and is what the next person working the chapter actually reads. **Not every preserved defect belongs in the student-visible solution.**
  - **THE COMMIT WAS THE LARGER RISK, and it was not the one the user asked about.** 103 data JSONs + 7 scripts existed ONLY in the database: the source of record for 366 published questions could not be re-derived, re-synced or re-committed from the repo. The tree also carried **two unrelated in-flight jobs** (a board chapter-order feature touching `src/`, `package.json`, `scripts/board/`; a practice-paper ocean-parity task) plus 9 `_kt_*` scratch files that `.gitignore` does not cover (it has `scripts/*/_tmp_*`, not `_kt_*`). Staged by EXPLICIT PATH per [[git-merge-to-main-flow]], then verified before committing that **every one of the 103 data files matches `-phy`** — `git add scripts/stateboard/data/` would happily have swept a foreign file. Both other jobs left untouched.
  - **A slow `db:backup` was diagnosed to the NETWORK, not the database, and the recipe generalises.** The dump ran 20+ minutes for 9 MB against a ~40 s norm. `pg_stat_activity` gave it away in one query: `wait_event = **ClientWrite**` — Postgres had the `COPY public.questions` rows ready and was blocked writing them to this machine, idle-waiting 13½ minutes. Confirmed as a local link problem rather than a Supabase one by testing unrelated hosts: **cdnjs served a 10 KB file at 3.6 KB/s and github at 4.0 KB/s**, so every host was equally slow. The 100% ICMP loss to the DB host is a red herring (Supabase filters ping). **A DB-side symptom whose wait_event is `Client*` is not a DB problem** — see [[db-perf-diagnosis]].

- **2026-09-02 (second) — MH State Board PHYSICS opens: a `Physics` subject seeded under BOTH `mh-sb-11` and `mh-hsc-12`, and the pilot chapter (Std XII Ch.5 Oscillations) ships 44 q PUBLIC. The source is the first State Board book with NO answers section at all, and its Std-XII text layer fails in a way that is silent and PLAUSIBLE.**
  - **SCOPE, from a source audit rather than a guess.** 30 pre-split chapter PDFs (XI 14, XII 16) whose names match the `syllabus_concepts` MH-State-Board Physics spine EXACTLY, all 30 — the same files that spine was extracted from in August — so chapter names and subtopics are already authored and validated. ~889 exercise + ~273 solved ≈ **1,160 q**. Both target exams had ONLY a Mathematics subject; `Physics` was seeded under each (`subjects.exam_id` is NOT NULL, so they are distinct rows). **CORRECTED — the `mh-sb-11` row was REMOVED the same day and Std XI still has no Physics subject: `listSubjects` applies no question-count filter, so an empty subject renders as a live `/browse` filter returning nothing (the empty-subtopic defect, one level up). Seed it when the first Std XI chapter commits.** Runs on the EXISTING pipelines — parameterised, never forked, per the NCERT Class-11 decision that a fork means applying every future fix twice.
  - **FINDING 1 — NEITHER PHYSICS VOLUME HAS AN ANSWERS SECTION.** Verified across all 644 pages of both books: no standalone `Answers` heading anywhere, and each ends on its last chapter's Exercises. So the mandatory step-6 gate CANNOT run the Maths way, and `config.ts` says so explicitly to stop a later session hunting for a missing `answersPdf`. **What exists instead is a PARTIAL per-question key** — the numericals print their own answer inline as `[Ans: ...]`, **338 across the two books, ~38% of exercise questions** (34% on the pilot). Those are transcribed into a `bookAnswer` field and diffed against our derivation by `dump-book-answers.ts`, which makes the gate REAL on the numerical half and absent on the rest. It reports the KEYED count rather than the chapter total, because "0 wrong across 15 keyed rows" is a different claim from "across 44". **And the blindness is structural, not procedural: `bookAnswer` deliberately never reaches the database (`buildRecords` ignores unknown fields), so the authoring dump is blind to the book's key BY CONSTRUCTION** — which is the only thing that makes the later comparison mean anything.
  - **FINDING 2 — VISION-ONLY, AND Std XII IS THE MOST DANGEROUS TEXT LAYER THIS BANK HAS MET.** Measured, not assumed: the radical sign occurs **ZERO** times in either volume, and superscript two zero times in XI, in physics books full of surds and `m/s^2`. Worse, **Std XII sets Greek in Symbol font and it extracts as Latin letters** — `S` for pi, `q` for the degree sign, `u` for the multiplication sign, `|` for the approx sign, `Z` for omega, `G` for delta, `I` for phi. So `x = 6 sin (100t + S/4)` and `0.1 S2 x2 joule` are real extractions that read as **well-formed equations in a variable `S`**. Unlike the Maths books, which yield a visibly broken `3 2` for root-three-over-two, this corruption is PLAUSIBLE and a text-first pass ships physics that is silently wrong. Std XI differs in mechanism only (real Greek, but 572 private-use U+F0xx glyphs, so a vector `B` extracts as `B ur`).
  - **AND I FELL INTO IT MYSELF WHILE VERIFYING, WHICH IS THE MOST TRANSFERABLE PART OF THE SESSION.** Re-deriving the book's printed answers as a third ground truth, I read `Ex Q.17`'s speed as `b/sqrt(3)` off the TEXT LAYER, got 1.823 s against the book's `pi/3`, and recorded it as a disagreement. The printed page says `b*sqrt(3)` — with which `A = 2b`, travelling a further `b` lands exactly on the extreme, and the time is `pi/2 - pi/6 = pi/3`. **The book was right and I was wrong**, in exactly the way the brief I had just written warns about; two agents reading the image got it right. A verification pass is not exempt from the rule it is verifying.
  - **THE PILOT'S CROSS-CHECK: 13 AGREE - 2 BOOK-KEY-WRONG - 0 OUR-ANSWER-WRONG** over the 15 keyed rows. `Ex Q.12` prints `6.324 cm/s` where `v = sqrt(2E/m) = 6.324 m/s` — magnitude right, unit wrong by a factor of 100 (a speed of 6.324 cm/s implies 0.004 J against the stated 40 J). `Ex Q.15` prints "Decreases by 5.07 mm" where a second's pendulum holds `T = 2 s`, so `L = g/pi^2` is DIRECTLY proportional to g and raising g must lengthen it. Both were reached independently by an authoring agent that could not see the book's answer, and again by me.
  - **A THIRD FLAGGED DEFECT DISSOLVED ON ADJUDICATION, and the agent's honesty is why.** The cross-check reported `Ex Q.14` as BOOK-KEY-WRONG (book `1.48e-2 N`, ours `1.47e-2`) but marked its own confidence as not airtight and NAMED the escape hatch — an unstated `g ~ 9.87`. The hatch holds: `g = pi^2` gives exactly `1.4804e-2 N`, this chapter states `g ~ pi^2 ~ 10` in `Ex Q.23`'s own stem, and the question's `L = 100 cm` is precisely a second's-pendulum length at `g = pi^2`. Not an error — a solution note recording BOTH values, not a `[Textbook...]` bracket. This is the documented "~20 apparent disagreements dissolve on equivalence testing" case, and it is why the maintainer adjudicates rather than the agent.
  - **TWO SOLVED-EXAMPLE DEFECTS, treated DIFFERENTLY on purpose.** `5.1(iii)`'s printed working substitutes `x = 0.04` m (4 cm, part (i)'s displacement) where the stem asks for 1 cm, and prints `-1 m/s^2`. The stem is the correct half — part (i) already gives the force at 4 cm, so reusing it would collapse (iii) to `F/m` and ask nothing — so the WORKING is corrected to `-0.25 m/s^2` and bracketed, per the Limits `7.1.7` precedent that a shipped solved example must not teach an error. `5.12` prints a moment of inertia in `A m^2` (the unit of magnetic moment); the value and final answer are unaffected, so it is PRESERVED and bracketed. Correcting one and preserving the other is the whole distinction between "the book's working contradicts its own stem" and "the book printed a defect".
  - **MCQ KEYS: THREE INDEPENDENT DERIVATIONS, ZERO DISAGREEMENT** (the transcriber, a genuinely blind pass that disclosed every file it opened, and me), stamped 0-mismatch into `question_reviews`. This book prints no MCQ key anywhere, so there was nothing to conform to.
  - **NEW MACHINERY, all OPT-IN so the 15 shipped Maths chapters are byte-identically unaffected.** `Chapter.derivedAnswers` marks a source that prints no key; `stamp-provenance.ts` writes `derived_model`/`derived_at`; `flip-public.ts` REFUSES to publish an authored row carrying no `derived_model`. **Provenance belongs to COMMIT, not publish** — the CDS-GK lesson, where it was caught one step too late. The seam needs no heuristic: `section_kind='solved_example'` rows carry the BOOK's own printed solution and are deliberately left UNSTAMPED, because claiming them would be the opposite error (31 stamped, 13 correctly not). Gate proven to fire (31/31 refused) and then pass.
  - **A BUG I INTRODUCED AND CAUGHT ONLY BY RUNNING THE REGRESSION CHECK.** `flip-public` first imported the shared note constant FROM `stamp-provenance.ts` — whose `main()` runs at module load — so the import EXECUTED the stamper and exited, breaking flip-public for **every shipped Maths chapter**. Reasoning about the diff would not have found it; running `flip-public` on three Maths chapters did, in one line each. Shared constants now live in `config.ts`, which has no top-level side effects.
  - **`merge.ts` HAD NO SHAPE GUARD, AND THE MEASUREMENT WAS WORSE THAN EXPECTED.** Its auto-glob excluded only `.solutions.json`, and this data dir also holds `mcq-blind`/`mcq-verify`/`review`/`topaper`/`xcheck`/`errata`/`diagram-specs`/`anchors` artifacts. Measured across all 15 shipped Maths chapters, a bare `merge.ts <id>` would have ingested non-question artifacts AS QUESTIONS in **every single one** — 21 files for `differentiation-12` (including its own `errata.json`), 25 each for `app-derivatives-12` and `indef-integration-12`. They escaped only because each chapter was merged with EXPLICIT section names, i.e. the glob has always failed OPEN. Now a fail-CLOSED shape guard (every row must carry a string `ref`/`bucket`/`stem`) beside the name list, **proven to reject 0 of the 15 chapters' real fragments and to fire in both directions** on injected cases. The name list alone goes stale by construction; this session added three new artifact types to it.
  - **A subtopic list derived from section headings OVER-generates, and this will recur across the lane.** Sections 5.14 Damped Oscillations and 5.15 Free/Forced/Resonance are genuinely TAUGHT (10 and 5 body mentions) and examined ZERO times — measured across the whole Exercises block, `resonan`/`forced`/`free oscill` occur zero times and the single `damp` hit is the word "**un**damped" inside an angular-S.H.M. torque question. Dropped, per the Class-12 Linear Programming precedent that an empty subtopic ships a `/browse` filter returning nothing. **Diff the committed `by subtopic` tally against config BEFORE `--apply`.**
  - **FIGURE LOAD IS LOW AND THE WHOLE-CHAPTER COUNT SAYS OTHERWISE:** 1,169 `Fig` references across the two books, but scoped to the exercise region only ~15-25 questions across all 30 chapters genuinely depend on one. Most apparent hits are false positives — "significant **figures**", "**Draw** a labelled diagram" (the student draws it). The pilot's one real case, `Ex Q.1 (v)`'s displacement graph, is **vector art** (152 drawings, 0 usable rasters), so it can only be cropped from the rendered page; snapCrop tightened a coarse anchor to the ink and the crop was eyeballed clean — full graph, both axes, every tick, no bleed from option (D) above or the next block below.
  - **Landing pages measured BEFORE the flip and confirmed after:** `mh-hsc-12` is not `practiceOnly`, so `listChapterLandings` derives `kind='pyq'` — a Physics chapter (all `practice`) gets **0** `/questions` landing pages, 0 gained and 0 lost, no indexed URL appearing or disappearing. The board-PYQ lane is what would unlock them.
  - Verified: 44/44 PUBLIC - 0 without a solution - 0 unsectioned - 0 missing taxonomy - 4 errata brackets matching the errata files exactly (counted in the DB, not read off the apply log) - `audit:text` 0 across all five classes - `audit:omml` 0 failing zones - `audit:keys` 0 flagged - `board:lint` green over 9,563 rows across 5 board exams. **Owed and stated rather than implied: a browser click-through of `/board`** (answers reveal on click, so nothing headless proves the reveal lays out), and **`Ex Q.5` asks the student to DRAW three graphs — its answer is genuinely a diagram**, recorded as a `solution_image` candidate rather than silently shipped as prose.
  - **THREE DEFECTS FOUND BY LOOKING AT THE RENDERED CARD, after every gate was green — and the first is a correctness bug I had explicitly reasoned my way into.** `Ex Q.1 (v)` shipped with BOTH the transcriber's prose graph description AND the attached figure. Redundant, but worse than that: the question asks "which statement is correct **from the graph**", and the prose sentence *"falls to its negative extreme at t = 3T/4"* hands over the exact reading step that yields the keyed answer (B, force maximum at 3T/4). I had flagged the decision at the time and chosen to keep the prose because removing it "cost a delete-and-re-commit for no correctness gain" — which was wrong, because the gain is that the stem stops answering itself. Stem stripped 668 -> 151 chars; since `content_hash` covers the stem the row was DELETED and RE-COMMITTED (inserted=1, skipped=43) rather than updated in place, because an in-place edit leaves stored text != the hash's preimage and the next re-commit from the corrected source would insert a duplicate. Dependencies were checked BEFORE deleting (0 papers, 0 attempt answers, 0 concept tags, 0 bookmarks) and the re-commit chain re-ran in full: sections, solution, blind-review provenance, derived-answer stamp, figure — the figure by pointing the new row at the ALREADY-UPLOADED storage object rather than re-cropping, which would have orphaned one. **The re-derivation still agrees (0 mismatches) and all 4 errata brackets survived**, counted in the DB rather than read off the apply log, because `apply-solutions` had to run after `apply-errata` here.
  - **THE DISCLOSURE SHOULD NEVER HAVE BEEN IN `pyq_note` AT ALL, and it took the user asking "is this necessary there?" to see it.** I copied the CDS General Knowledge pattern of appending a derived-answer disclosure to the note, then spent TWO rounds fixing its FORMATTING — unbracketed (ran on as a blob), then bracketed to match CDS (which renders `]]`, because the /browse card draws its own `[{provenance}]` wrapper), then de-bracketed — before asking whether the thing belonged there. Optimising something that should not exist. It should not, three ways: **wrong moment** (a reader looking at the QUESTION has not seen an answer yet, so there is nothing to mistake for an official key), **wrong field** (`pyq_note` has exactly one consumer, the source line, whose only job it crowded out on every row), and **wrong premise** — the CDS rule exists because those are EXAM PAPERS, where "official answer key" is a real artifact students hunt for; this is a TEXTBOOK whose note already names it. I treated a precedent as a requirement without re-testing whether its premise held. `pyq_note` now carries the SOURCE only; the derived-answer fact lives in `derived_model`/`derived_at`, and **the flip-public gate keys on that column rather than a prose match**, so it cannot break silently when wording changes.
  - **The way I checked the precedent was itself the bug, twice over.** I verified the CDS note by reading its STORED TEXT in the database and never by looking at a rendered card — so I matched a shape that is defective at the render layer, and CDS-GK's 2,280 rows carry the same `]]` today (logged as a backfill candidate, not touched). Worse, the user's FIRST screenshot showed exactly that nesting, I diagnosed it correctly as brackets-inside-brackets, and my fix ADDED a bracket. And I had written "found only by looking at the rendered card" into the pipeline memory an hour earlier. **Checking a precedent means checking its OUTPUT, and a render-layer change is accepted against the render, never against the stored string.**
  - **`formatProvenance` prefixed "Q" onto every ref, and 22,368 PUBLIC rows across 8 exams read wrong.** The helper does `Q${questionNumber}`, which is right for a PYQ paper's bare numeral (`42` -> `Q42`) and wrong for every textbook and worksheet corpus, whose refs are DESCRIPTIVE: `QEx Q.2 (ii)`, `QMisc I (iv)`, `QLvl I-CW Q1`, `QA relation between length of an arc...`. **Pre-existing and shipped**, surfaced only because a new chapter put it in front of a reader; it was taken with a 360 and explicit go-ahead rather than folded in silently. The fix is CONDITIONAL, never a blanket removal — `Q42` is correct and wanted for the PYQ corpora — and it is render-only: exactly two consumers (the `/browse` card and `/guide` worked-example citations), no DB write, and the Word export deliberately avoids this helper. **All 8 pre-existing assertions use bare numerals, so the fix ADDS cases rather than changing pinned behaviour**; three new tests pin both directions and were confirmed failing for the right reason first.
  - **A second lane exists and is the agreed next step:** `State_Board/PYQs/` holds 16 chapterwise board-PYQ `.docx` (Std XII ch.1-16, ~25 provenance tags per chapter, real pi surviving in the text layer) plus 9 raw papers 2016-2025 — precisely the `scripts/mh-hsc-12-pyq/` shape, born-digital and pandoc, so far cheaper per question than the vision lane. See [[state-board-physics-ingestion]].

- **2026-09-02 — A 120-q NDA paper over four State Board Std XI chapters, and the bank-wide statement-layout repair it surfaced: 285 stems across 4 exams now print each numbered claim on its own line, and the BLOCKING gate that should have caught it is no longer blind.**
  - **THE PAPER.** `NDA Maths - State Board XI: Angle, Trigonometry-I, Sets & Relations, Functions (120 Q)` — Functions 48 · Sets & Relations 42 · Trigonometry-I 15 · Angle and its Measurement 15. Scope came from the **syllabus map** (`syllabus_concept_exams.covered_by` on the NDA bank spine, authored 2026-08-07), never from chapter names, and that is what made it correct: **95 of the 138 NDA "Trigonometric Identities" PYQs are Trigonometry-II** (Compound Angle 38, Multiple/Half-Angle 30, Product-to-Sum 27 all cite XI:3.x). The two surviving subtopics cite BOTH XI:2.x and XI:3.x, so they straddle and no subtopic filter separates them — all 43 were read individually and 15 solo rows survive strict Ch.2 scoping. **`Angle and its Measurement` has ZERO NDA PYQs** — only 5 of 2,160 mention radian/arc/sector and none is in a trigonometry chapter — so it is filled per blueprint RULE 2 from the Cadetprep Worksheets course, tiers stepped up one per RULE 2a.
  - **Three additive `ChapterPlan` filters**, each defaulting to prior behaviour so every existing spec selects byte-identically: `allowUsed` (23 of the 32 in-scope HARD rows are already in a paper; refusing repeats caps the paper near 7% HARD against an NDA norm of 26.5%), `soloOnly` (RULE 1 the cheap way — `selectByQuota` is set-blind and the docx exporter groups a shared context by ADJACENCY, so picking set members risks both splitting and scattering a set), and `includeIds` — an **allow**-list, because an exclude-list fails OPEN and would admit Trig-II rows added by a later ingest.
  - **THEN THE PAPER PRINTED 22 OF ITS 120 STEMS WITH THE STATEMENTS RUN TOGETHER, and the diagnosis that mattered was that this is DATA, not rendering.** Both renderers already honour a real newline — the Word exporter splits on `\n` into `TextRun({break:1})` (`mathRuns`), the web renderer wraps text in `white-space: pre-wrap` (`KatexRenderer`). Proof: 6 stems in the same paper printed correctly because their text carries newlines. **198 of 286 NDA Maths statement-style rows (69%) were stored flat.**
  - **THE BLOCKING GATE WAS ONE PATTERN AWAY FROM CATCHING IT.** `paper-text.ts`'s `P1-statement-run-on` is in `BLOCKING_RULES` and does exactly this job — but `STATEMENT_LABEL` requires the literal word `Statement`/`Assertion`/`Reason`, while NDA's dominant style is a bare `1.`/`2.` after a lead-in. So the build reported "Paper text: OK" and applied. The narrowness was deliberate (its comment says it exists so the prose word "statements" cannot match), so P1 now ORs in a second scan over `findStatementLabels` rather than loosening the original, which is preserved bit-for-bit. **Proven rather than asserted: P1 fires on 284 of the 285 pre-repair stems and 0 after.**
  - **THE SCOPE DECISION IS THE LESSON. A regex is not a scope.** A bare label pattern matches **899 PUBLIC rows bank-wide and 521 of them are Match Lists** whose labels are already table cells. Reading the survivors by exam showed the defect rate is wildly uneven: in JEE Mains / NEET / MHT-CET roughly **12 of 14 candidates were false positives** — options named in a parenthetical (*"(Options are structures: (1) nitrobenzene…"*), reagent sequences, mid-sentence enumerations (*"as the train (1) approaches … and then (2) recedes"*), and a figure reference (*"Figure 1. electron probability density"*) that produced a line reading only "Figure". Those exams are therefore excluded **by name** (`--exams` allow-list), not by pattern.
  - **FIVE GUARDS, EVERY ONE EARNED FROM A LIVE ROW** (`scripts/lib/statementLayout.ts`, pure + 30 TDD cases): mask math zones first (`\(1\cdot5\)`, `\([.]\)`); skip a real GFM table; skip `Match the following/column` and a bare `Column I / Column II` list; require the run to ASCEND from its first label, scanning each label only AFTER the previous one (so *"…can never be less than 2. Which of the above"* takes the label, not the trailing numeral); and require the FIRST label to be **introduced** by `:`/`?`/`.`/newline, which is what separates a list from an enumeration inside a sentence. **`A.`/`B.` alpha labels were dropped entirely** — measured across every live candidate they drove ZERO rewrites while prose supplies the shape readily.
  - **THE LEAD-IN GUARD VALIDATED ITSELF ACROSS CORPORA.** It was derived from the JEE/NEET false positives, and it independently caught **every one of the 5 corrupting rows** that two review agents later found by reading — including two NDA Chemistry `Column I / Column II` lists whose naive split glued each Column-II entry to the NEXT Column-I entry and thereby **asserted pairings the question exists to test, one of them the correct answer**.
  - **`content_hash` IS DELIBERATELY LEFT UNTOUCHED — do not "fix" the mismatch.** Dedup is `(org_id, exam_id, content_hash)` over the text, so adding newlines changes what the hash would be. Recomputing it would make a re-upload of the original `.xlsx` (which still holds flat text) match nothing and insert a duplicate of every repaired row; leaving it means such a re-upload still dedups and skips. No `.xlsx` is tracked, so there is no upstream source of record to correct instead. The cost is that `question_reviews.reviewed_content_hash` will not mark a review stale after this edit — defensible, since a line break changes no claim, option or answer, but it is a real weakening and is recorded in the script header rather than left to be discovered.
  - **Verified, not asserted:** 285 rows updated and read back; **285/285 whitespace-only** (a character-frequency diff shows the only changes anywhere are `\n` added and spaces consumed); **0 newlines inside any math zone**, 0 unbalanced zones across all 3,537 multiline rows in scope; 0 new blank lines; 0 label-only lines; **re-run finds 0 to repair** (idempotent); the paper's Word export gained **94 line breaks** with 0 raw-LaTeX fallbacks. Snapshot written per run and `--revert=<file>` restores. `db:backup` taken first (76 tables / 353,335 rows).
  - **A probe failure worth recording, because it fired FOUR times in one session.** Authoring anything containing a backslash through a shell heredoc or `python -c` corrupts it: `\b` became a literal **backspace byte (0x08)** inside a regex, making it unmatchable and invisible in review — caught only because a test went red. A later `python -c` check then reported **58 rows with a newline inside a math zone**; the file-authored version of the same check reports **0**. Both the data and the earlier agent reports were right; the one-liner was wrong. Author through the editor. See [[heredoc-backslash-eating]] + [[control-char-latex-corruption]].
  - **Left alone, deliberately:** ~31 rows where the closing directive stays glued to the last item because it follows a `.` inside that item — an under-repair, not damage (the text is intact and no worse than before), and widening the split rule there is exactly what starts cutting mid-sentence. Also logged and NOT fixed: a pre-existing **label-space mismatch** in ~15 Foundation Physics rows whose stems number items `1.`/`(1)` while their options refer to them as `A and C` or `(i) and (ii)`.

- **2026-09-01 — MHT-CET gains 60 timed PYQ mocks (mocks 63 → 123): the PCM group's TWO papers per sitting, across 45 shifts 2021-2025. 30 of the 90 possible papers are deliberately HELD rather than shipped short, and the single most consequential fix in the ship is a UI bug that had nothing to do with MHT-CET until MHT-CET arrived.**
  - **THE CORPUS RECONSTRUCTS BETTER THAN CDS DID, and measuring that first is what made this small.** `source_row` is non-null on all 6,614 PUBLIC PYQ rows with **zero duplicate ordering keys** within any subject, and **zero rows lack an answer key**. Two numbering conventions coexist (the xlsx uploads run Physics 2-51 / Chemistry 52-101 / Maths 102-151, the 2025 April docx run 1-50 / 51-100 / 101-150) and neither needs handling, because `orderPaperRows` only ever sorts WITHIN a section. Verified after the write: **0 ordering mismatches across all 4,350 refs** — position order equals `source_row` order in every section of every mock.
  - **`pyq_month` is not merely useless here, it is ACTIVELY MISLEADING — 17 of the 45 sittings share (2023, "May").** The NDA year+month discovery loop would emit ONE slug for all 17 and, since the mock id is `slugToUuid(slug)`, each upsert would silently overwrite the last, leaving 16 real papers missing with no error. So MHT-CET is `source_file`-keyed (the CDS/NEET shape) with a date-and-shift slug stem, `mht-cet-2023-may-03-s1-maths`.
  - **THE REGISTRY IS HALF-DERIVED AND HALF HAND-WRITTEN, and that split is forced by the corpus rather than chosen.** `scripts/mhtcet/config.ts` is the source of record for the 13 April-2025 shifts, so those are DERIVED exactly as CDS derives from `scripts/cds/config.ts` — a renamed booklet there now fails a test here instead of leaving a mock silently empty. The other **32 shifts came in through the generic `/upload` path years earlier and have no config anywhere in the repo** (grepped repo-wide), so they can only be hand-written. Their labels are deliberately NOT taken from `pyq_note` — see the next point.
  - **A `pyq_note` IS FREE TEXT AND ONE OF THEM IS PROVABLY WRONG.** `MHT_CET_3rdMay2023_S1_QB.xlsx` stores the note "3rd May 2nd Shift", contradicting its own filename, while a *separate* file already legitimately claims that sitting — so on the note's word we would have had two files for 3rd May Shift 2 and none for Shift 1. Settled against the local source booklets rather than by picking the more official-looking field: stems unique to that file ("watch glass", "coloured compound") appear ONLY in `3 may shift 1 (ques).docx`, and the Shift-2 file's stems ("cold or hot water", "equal intercepts") only in `3 may shift 2`. **The FILENAME is right and the note is a typo**; both 3-May shifts exist and neither is missing. Worth noting a single probe would not have settled it — "r.p.s" appears in BOTH files and discriminates nothing. The wrong note is still live on ~150 rows and is a backfill-ledger item, not something to repair in passing.
  - **30 PAPERS ARE HELD, AND THE DECOMPOSITION IS WHAT MADE THE POLICY DECIDABLE.** A soft count (NEET's) was rejected: NEET omits `count` because its layout genuinely varies by sitting, whereas a short MHT-CET paper is questions MISSING, so a soft count ships a fragment labelled as the real paper — `13 May 2024 Shift 1` would have become a 62-question "100-question paper". Splitting the gap by CAUSE gives two different answers: **10 papers are short because a flawed question is deliberately PRIVATE** (irrecoverable — the alternative is un-withdrawing a question we judged bad) and **20 because rows are absent from the bank** (never ingested, or absorbed by cross-shift `content_hash` dedup — closable by a future ingest). Both are held, but only one is a defect. **A `hold` is an ASSERTION, not a mute**: the builder still attempts every paper and reports a hold whose paper NOW reconstructs as a failure telling you to delete the line — otherwise closing a corpus hole would leave the mock un-shipped forever, in silence.
  - **THE SHARPEST FINDING WAS NOT ABOUT MHT-CET AT ALL: the mock instructions screen had a live latent bug that only a zero-penalty exam could expose.** `/mock/[slug]` hard-coded a marking scheme true for the only three exams it had ever served, and for MHT-CET would have rendered *"**0** marks for every wrong answer (negative marking)"* — asserting a rule the exam does not have — and *"**0** marks for un-attempted questions — **skip if unsure**"*, which is **STRICTLY HARMFUL**: with no penalty a guess is never worse than a blank, so that advice costs marks. It would have contradicted our own MHT-CET guide (which sets `targetAttempts` to the full paper for exactly this reason) **on the page a student reads immediately before starting**. Fixed at the cause with a pure `src/lib/mocks/marking.ts` so the zero-penalty branch is provable without rendering the page. The same [[default-becomes-assertion]] shape as the coverage and "no single section" bugs: a default defensible for the cases its author had becomes a false assertion for the first case they did not. The `MockRunner` marks chip was left alone deliberately — `+2 / 0` is accurate, and rewriting it would have changed CDS's shipped display for no correctness gain.
  - **`tests/mocks-nav.test.ts` used MHT-CET as its exemplar of a registered-but-mock-less exam** — the third instance of this shape (after `exam-nav`/`format-mix` on 2026-08-22). Each invariant MOVED to JEE Mains (a live 10,614-question exam with no mocks) and MHT-CET gained the positive assertion, rather than an assertion being deleted.
  - **Verified rather than asserted**, all after the write and against the live bank: 60 mocks (33 Maths + 27 Phy&Chem) · 4,350 refs · **0 dangling, 0 non-PUBLIC, 0 non-pyq, 0 duplicated within a mock, 0 missing keys** · **0 wrong-section** (every `physics` ref really is a Physics row) · Physics occupies positions 1-50 and Chemistry 51-100 in every Paper II · **0 mocks mixing two sittings** · **0 questions appearing in two mocks** · totals exactly 50/100 and 100/100 marks. The builder's own 60-built/30-held/**0-unexplained-failures** split independently reproduced the split derived from SQL beforehand. A `db:backup` was taken before the bulk write per the standing runbook. **Owed and stated rather than implied: the browser click-through** — the runner is client-interactive behind auth, so nothing headless proves a CET mock starts, grades and reviews; worth checking that the new no-negative-marking instruction line reads correctly there.

- _Entries dated **2026-08-31 and earlier** are archived in [DECISIONS_HISTORY.md](DECISIONS_HISTORY.md) (batches 5–9 span 2026-06-01 → 2026-08-31). The Foundations (M1–M3) sub-section above + everything **2026-09-01 onward** stays here; archive older months to keep this active log to ~the last 2 weeks._

## Pending work

The chronological decisions log above captures *why* shipped choices were made; for what's pending, see [`ROADMAP.md`](ROADMAP.md) at the project root. It consolidates every "deferred", "future scope", and "explicitly chose not to build" annotation scattered through this file plus open product questions (e.g., the `principle_tags text[]` column proposal). When an item ships, move its entry into the Decisions log here with a `2026-MM-DD —` prefix and a "Why" sentence so the two files don't drift.

## Cleaned subjects · post-delta sizing

At-a-glance sizing of the cross-subject taxonomy cleanup (chapter + subtopic technique-level canonicals — see Decisions log entries dated 2026-05-13 onward for per-subject narratives). For per-chapter **content-correctness** audit status (orthogonal to taxonomy structure), see [[content-audit-progress]] — **NDA Mathematics fully closed (31 of 31 chapters, all 2,160 q)** as of 2026-05-27, ~22.6 % bank-wide.

**Contract:** numbers below reflect **live counts as of the most recent cleanup pass per subject** (not historical "cleanup-pass-time" snapshots). They match live DB exactly the moment a subject ships, then drift again with the next upload — re-cleanup (Phase D delta) re-syncs them. After any upload batch, treat any subject in this table as stale until SQL-verified or re-cleaned. The live bank-size line at the top of this file is the always-current total. See [[project-docs-staleness]].

**Done — 13 of 13 subjects · 179 ch · 9,546 q · 678 sub** (this is the **cleaned-13-subjects scope, NOT the live DB total** — `npm run stats` reports the live aggregate, currently **258 ch · 934 sub · 10,264 PUBLIC q**; the difference is JEE Mains' ~79 ch / ~263 sub auto-created across Papers 1–10, tracked separately and not part of this cleanup register) (live-verified 2026-06-03; per-row figures below are the per-subject cleanup snapshots and may sum slightly under the live aggregate — SQL-verify before quoting. MHT-CET Chemistry 1,553 → 1,529 → 1,525 on 2026-06-03 — 24 exact-duplicate rows deduped in its solution-cleanup dedup pass, then −1 OF₂ dedup −3 broken rows set PRIVATE in the source-PDF verification pass; NDA Maths re-Phase-D'd 2026-05-17; all 9 other NDA subjects on 2026-05-18 after fresh uploads; MHT-CET Maths re-Phase-D'd 2026-05-23; MHT-CET Physics first cleanup 2026-05-23; MHT-CET Chemistry first cleanup 2026-05-27; NDA Maths Probability re-merged 113 → 109 sub on 2026-05-29 for the notes pedagogy split; MHT-CET Maths Indefinite Integration re-carved 4 → 6 sub on 2026-05-30 for its notes chapter, +2 net → 71 sub; NDA Maths 3D Geometry "Lines and Planes" split into "The Straight Line in 3D" + "The Plane" on 2026-06-02 for its notes chapter, 109 → 110 sub; NDA Physics "Electricity and Magnetism" re-carved 6 → 9 subtopics on 2026-06-02 for its notes chapter, 49 → 52 sub; NDA Maths Sequence & Series re-carved 4 → 5 sub on 2026-06-02 for its notes chapter — split the lumped GP/HP/AM-GM-HM subtopic into 3 + folded Vieta's, 110 → 111 sub; MHT-CET Maths gained a new **Quadratic Equations** chapter on 2026-06-02 (26 → 27 ch · 71 → 72 sub) holding 3 roots questions reclassified out of "Sequences and Series", and one tan⁻¹-in-AP duplicate was deduped from Sequences (1,588 → 1,587 q); NDA Geography "Indian Geography — Physical Features" Phase-D-split on 2026-06-14 for its notes chapter — the "Forests and Natural Vegetation" 34-q catch-all carved up (20 misfiled q → existing subtopics + a new **Location, Extent and Frontiers of India** subtopic), 36 → 37 sub; MHT-CET Maths Differentiation Phase-D-reshaped on 2026-06-16 for its notes chapter — the 3 label-based catch-all subtopics split into 6 pedagogically-clean ones (Foundations/Chain/Differentiability · Logarithmic · Implicit & Special Forms · Inverse Functions & Inverse-Trig · Parametric/Higher-Order/Relations · Derivative-wrt-Another), 72 → 75 sub; MHT-CET Maths Vectors reshaped 2026-06-23 for its notes chapter — the "Magnitude, Components, Projection, and Unit Vectors" catch-all renamed "Magnitude, Components, and Unit Vectors" + 14 q moved to Dot/Cross/STP/Linear (rename + question moves only, no sub-count change — still 75 sub); MHT-CET Maths Line and Plane Phase-D-split on 2026-06-27 for its notes chapter — the 40-q "Distance, Foot of Perpendicular, Projection, and Mirror Image" catch-all split into "Distances in 3-D" + a new "Foot of Perpendicular, Image, and Projection" (+1 sub) + 1 misfiled line question Plane→Line, 75 → 76 sub; MHT-CET Maths Applications of Derivative Phase-D-reshaped on 2026-07-01 for its notes chapter — the two label-based catch-alls ("Monotonicity, Extrema, and Optimisation" 71 q + "Tangent, Normal, Approximation, and Curve from Geometric Condition" 54 q) split into 5 coherent subtopics (Tangents-Normals · Approximations · Angle Between Curves · Increasing/Decreasing · Maxima-Minima-Optimisation), the other 2 kept as-is; 4 → 7 sub · 76 → 79 sub; 1 corrupted dup set PRIVATE so 1,784 → 1,783 q; MHT-CET Maths Differential Equations Phase-D-reshaped on 2026-07-01 for its notes chapter — the 75-q "Solving First-Order ODEs" method-mixing catch-all split by solution method into Variable-Separable + Homogeneous & Reducible + Linear (Integrating Factor), the Applications bucket split into Growth/Decay/Models + Newton's Law of Cooling, and Order/Degree/Formation/Verification kept whole; 3 → 6 sub · 79 → 82 sub; 1 conflicting-key bacteria dup set PRIVATE so 1,783 → 1,782 q)):

| Subject | Chapters | Questions | Subtopics |
|---|---|---|---|
| NDA Mathematics | 31 | 2,160 | 111 |
| MHT-CET Mathematics | 27 | 1,782 | 82 |
| MHT-CET Physics | 24 | 1,771 | 91 |
| MHT-CET Chemistry | 29 | 1,739 | 121 |
| NDA English (GAT) | 8 | 900 | 29 |
| NDA Physics | 14 | 449 | 52 |
| NDA Chemistry | 12 | 262 | 47 |
| NDA Biology | 9 | 190 | 39 |
| NDA Geography | 7 | 345 | 37 |
| NDA History | 4 | 260 | 22 |
| NDA Polity | 4 | 90 | 14 |
| NDA Economics | 1 | 24 | 3 |
| NDA Current Affairs | 8 | 180 | 31 |

**No subjects pending.** Bank-wide subtopic taxonomy is now technique-canonical end-to-end. The post-upload ritual still applies — fresh uploads can re-fragment subtopics, requiring Phase D delta cleanup; see [[post-upload-ritual]].

Workflow: inline chapter-by-chapter in chat (no scripts), descending q-count first, source-source collision-loop for bulk merges, per-question reclassification for catch-all chapters ≤~50 q. Patterns + SQL templates: `[[reclassification-sql-pattern]]` + `[[taxonomy-inline-iteration]]`.


## Notes editorial workflow

The full step-by-step workflow for authoring a `/notes` chapter lives in **[NOTES_WORKFLOW.md](NOTES_WORKFLOW.md)** — **read it before building one.** It covers: Step 0 pedagogy-first structure (reshape the bank to the teaching arc; read HARD+MODERATE solutions; MHT-CET-Maths defaults) · Step 1 the `ConceptUnit` formula/reference discriminated union + the diagram decision rule + concept-content alignment · Step 1b author-for-clean-quiz-harvest · Step 2 the `NOTES_CHAPTERS` registry · Step 3 the chat tagging session (set-sibling co-location, 100% coverage) · Step 4 the gate chain (`notes:lint`/`notes:latex`/`notes:coverage`/`quiz:coverage`/`prepush`) · Step 5 ship + `npm run stats` + `notes:order` · the invariants to verify before commit.

## After a new upload

When a new question-batch lands, run the 6-step ritual in order. The upload pipeline is auto (taxonomy auto-creates, `content_hash` dedup, default PUBLIC visibility, live counts in `/browse` + dashboard + `/guide` TOP_11 + `/notes` drills). What's NOT auto and needs this ritual: **tag coverage, taxonomy fragmentation cleanup, and TS-static editorial copy**.

1. **Sanity-check the batch** (always) — confirm `inserted` vs PUBLIC/PRIVATE counts, PYQ metadata stamped, no missing years
2. **Taxonomy delta cleanup** (if ≥5 new subtopic auto-creates or a new chapter name) — Phase D delta inline; don't restart from scratch. Precedent: 2026-05-17 NDA Maths re-Phase-D (~770 new auto-creates closed in one session)
3. **Principle-tag delta survey** (if new questions in TOP_11 chapter footprints) — incremental per-new-chapter, not whole-bank
4. **Concept-tag delta session** (if new questions in `/notes`-covered subtopics) — also revisit dropped-gap ConceptUnits; bank growth can mature what was previously dropped
4b. **Notes-coverage check** (if new questions in a `/notes`-covered chapter) — `npm run notes:coverage <subjectRoute> <chapterSlug>` and triage the macro/technique channel. New uploads are how a *technique gap* drifts in (a fresh question using a method the notes don't teach); this is the routine catch for it. Add a concept for any real gap.
5. **Editorial copy refresh** (if counts move ≥5–10%) — `whyItMatters`, OVERVIEW constants, CHAPTER_TABLE, strategy %HARD, trends callouts. Mechanical TS edits
6. **Integrity check** (always) — `npm run notes:lint` + `npm run prepush` before commit

Steps 2–5 (incl. 4b) are conditional and can be deferred / queued across sessions. Full procedure + SQL probes + skip-conditions: [[post-upload-ritual]].

## Adding a new RLS-protected table

Follow the `questions`/`options` pattern (see `0002_rls.sql` + `0004_upload_staging.sql`):
1. Enable RLS.
2. Read policy: `org_id = private.current_user_org_id()`.
3. Write policy: same plus `private.current_user_is_admin()` if admin-curated.
4. Add a test in `tests/upload-rls.test.ts` (or new equivalent) proving a TEACHER cannot write and a member of another org cannot read.
5. Run `mcp__supabase__get_advisors` after applying the migration.

## Live infra

- **Supabase project:** `wunvtnqlzjrkvolslbnm` (https://wunvtnqlzjrkvolslbnm.supabase.co) — **plan: PRO.** Egress 250 GB included (2026-08-13: **1.73 GB used, 0 overage**, ~0.7%). Recorded here because a plan upgrade leaves NO trace in the repo — no migration, no config, no commit — so the only way a later session learns it is this line or the provider's usage page. The free-tier "5 GB / 11.48 GB" figures in the 2026-08-05 Decisions entry are history; treating them as live cost an hour on 2026-08-13. See [[project-docs-staleness]]. **Compute: MICRO (1 GB / 2-core) since 2026-08-31** — before that the instance silently ran as a NANO (409 MB) while being billed Micro, and the resulting swap-thrash was the root cause of the 57014 statement-timeouts, the latency spikes and several failed pushes (see the 2026-08-31 (third) Decisions entry). Host truth is readable at `<project>.supabase.co/customer/v1/privileged/metrics` (basic auth `service_role:<key>`); if stalls ever return, check `node_memory_MemTotal_bytes` and swap there BEFORE profiling queries.
- **Production:** **https://www.pyqvault.com** (custom domain, live 2026-06-04; apex `pyqvault.com` 308-redirects to `www`) — auto-deploys from `main`. The old `question-bank-sage.vercel.app` still resolves (optionally set to redirect → `www.pyqvault.com` in Vercel → Domains). Canonical URL in code is `https://www.pyqvault.com` (the 4 `SITE_URL` consts: layout/sitemap/robots/GuideJsonLd). DNS at GoDaddy: `A @ → 216.198.79.1` + `CNAME www → <unique>.vercel-dns-NNN.com`. **Vercel plan is Hobby** — fine for the free launch, but move to **Pro before Razorpay goes live** (taking payments = commercial use).
- **Repo:** https://github.com/lwspune/question-bank (public; Vercel project is named `question-bank`)
- **Auth callback URLs allow-listed in Supabase Auth (Authentication → URL Configuration):** `https://www.pyqvault.com/api/auth/callback` + `https://pyqvault.com/api/auth/callback` + the legacy `question-bank-sage.vercel.app` + `localhost:3000`. Site URL = `https://www.pyqvault.com`. (Adding the new domain here is mandatory or OAuth/email sign-in breaks on it.)


## Operations

Day-to-day runbook lives in **[OPERATIONS.md](OPERATIONS.md)**: monitoring (Vercel/Supabase logs, advisors) · when to upgrade tiers · rate-limit visibility · rotating `SYNC_SHARED_SECRET` · Razorpay env + go-live · managing members (admins/teachers) · flipping question visibility · setting PYQ year/month/comment for a batch · deleting a whole upload.

