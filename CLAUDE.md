# PYQ Vault — project notes

**Brand:** the product is **PYQ Vault**, live at **https://www.pyqvault.com** (rebranded from "Question Bank" 2026-06-04 — the repo/folder/Vercel-project are still named `question-bank`; in code the brand is `SITE_NAME` in `layout.tsx`). Lowercase "question bank" in prose still means the question corpus, not the brand.

Free, public past-year-question paper builder for Indian entrance exams. Anyone can filter the public bank by exam/chapter/subtopic/difficulty/PYQ year, then download a Question Paper + Answer Key as Word files. Admins (org members) upload Excel batches privately, then flip individual questions PUBLIC. Sibling app `MHT_CET_AI` posts finalized mocks to `/api/sync/mock` for cross-app dedup. Currently MHT-CET + NDA + **JEE Mains** (2021 — **718 q PUBLIC**: standard shift Papers 1-10 + 18, plus compilation Paper 11; Paper 12 staged PRIVATE; ingested via the `scripts/jee/` pandoc pipeline — see [[jee-mains-ingestion]] for the per-paper state + resume point); IPMAT, CUET, NEET on the roadmap.

**Live bank size (2026-06-04):** **10,264 PUBLIC** questions across 16 subjects — MHT-CET 4,686 (Maths 1,587, Physics 1,574, Chemistry 1,525) + NDA 4,860 (Mathematics 2,160, English 900, Physics 449, Geography 345, Chemistry 262, History 260, Biology 190, Current Affairs 180, Polity 90, Economics 24) + **JEE Mains 718** (2021 Papers 1-10 + 18 standard shifts + compilation Paper 11 [59/60]; Paper 12 staged PRIVATE — not in this count; MCQ-only, Section B numericals skipped; chapters/subtopics freshly authored per-paper, NOT part of the cleanup/audit tracking below; P3 Q37 set PRIVATE — incomplete, missing reaction figure — see [[jee-mains-ingestion]]). (MHT-CET Chemistry dropped 1,553 → 1,529 → **1,525** on 2026-06-03 — first 24 exact-duplicate rows deduped, then during the source-PDF verification pass: 1 OF₂ conflicting-key duplicate deleted + 3 unreconstructable broken-extraction rows set PRIVATE; 6 image-naming rows + 5 conflicting-key pairs remain deferred. MHT-CET Physics dropped 1,577 → 1,574 on 2026-06-02 — 3 cross-shift duplicate rows deduped during its solution-cleanup. MHT-CET Maths dropped 1,588 → 1,587 on 2026-06-02 — one tan⁻¹-in-AP duplicate deduped during the Sequences & Series bank-hygiene fix, which also spun the 3 misfiled roots questions out into a new **Quadratic Equations** chapter; MHT-CET Maths is now **27 chapters**.)

**Content audit status (2026-06-03):** **BANK-WIDE CLOSED** for the original 13 subjects audited at the no-source-needed surface (**9,546 / 9,546 q, ~100%**): NDA bank-wide (4,860) + MHT-CET Maths (1,587) + Physics (1,574) + Chemistry (1,525). (The 600 ingested JEE Mains q — 599 PUBLIC + 1 PRIVATE — are NOT in this: they carry source answer keys verified only against the source images during ingestion + 126 AI-authored solutions; a content-correctness audit of JEE is a separate future pass. Paper 2 already exposed Q1 wrong-keyed by a duplicate-numbered soln block, fixed via override; Paper 3 Q37 was incomplete — its reaction scheme didn't extract — and was set PRIVATE.) MHT-CET Chemistry closed 2026-06-03 across three passes (subject-wide content probes → 8 numeric chapters HARD+MODERATE stealth-key sweep → 21 recall-chapter deep-reads) + a **source-PDF verification pass** off the local MHT-CET DOCX papers — **31 wrong-key flips** (HARD numeric ran ~22%; recall ~0.5–1%, errors only in reaction/IUPAC-naming, never pure fact-recall), 24 exact-dupes deleted, 5 bank-extraction fixes, 12 printed-paper-defect preserve-notes, 3 broken rows → PRIVATE. **~228 wrong-key flips bank-wide** (MHT-CET Physics 22, Chemistry 31, NDA Maths ~112 — including 26 stealth flips found *after* the 2026-05-27 "closure": 3 via the Indefinite Integration notes build + 2 via the Binomial Distribution notes build + **21 via the 2026-06-04 /solution-cleanup sweep of the 28 other "closed" chapters**, which exposed that the 2026-05-27 disagree-probe was case-sensitive and missed lowercase/`REVIEW:`-hedged self-disagreements; the rest spread thin). Preserved paper defects: 6 clean (5 NDA + 1 MHT-CET Maths) + ~10 MHT-CET Physics + 12 MHT-CET Chemistry honest-note rows. **Still open (all need source PDFs/figures, not derivation):** ~6 MHT-CET Chemistry image-naming rows + 5 user-deferred conflicting-key dup-pairs + 32 same-stem/diff-option Chemistry groups. Key lesson from this frontier: a probe only catches *self-contradicting* solutions — most flips were **stealth wrong-keys** caught by fresh re-derivation, and several were **sibling-confirmed** (a parallel question carried the correct key); see [[audit-probe-symmetry]] + [[audit-effort-by-question-type]]. Per-subject register in [[content-audit-progress]]; per-row narratives in the Decisions log below.

**All 13 subjects fully cleaned** at chapter + subtopic level — bank-wide cleanup pass closed. (The cleanup-pass snapshot numbers below are point-in-time; subjects have grown via new uploads since their original cleanup; NDA Maths was re-Phase-D'd on 2026-05-17; all 9 other NDA subjects (English + Physics + Chemistry + Biology + Geography + History + Polity + Economics + Current Affairs) on 2026-05-18 to fold in fresh auto-creates; MHT-CET Maths re-Phase-D'd on 2026-05-23 after fresh uploads grew it 795 → 1,588 q; MHT-CET Physics fully cleaned for the first time 2026-05-23 at 1,577 q; MHT-CET Chemistry fully cleaned for the first time 2026-05-27 at 1,553 q — closes the bank-wide cleanup.) **10 NDA strategy guides shipped** at /guide/nda-{maths,english,physics,chemistry,biology,geography,history,polity,economics,current-affairs} (economics + current-affairs are thin single-page landings — economics because the bank's that thin at 24 q · 1 ch, current-affairs because the content half-life is too short for a multi-route fact-memorise guide; CA's single page is **Template D — theme-prep-checklist**, the first guide where the strand axis is recurrence-strength rather than skill or strategic priority). **15 chapters of self-sufficient /notes content across 3 subjects** (NDA Maths Statistics + Vectors + Probability + **3D Geometry** + **Matrices & Determinants** + **Sequence & Series** + **Indefinite Integration** + **Binomial Distribution** + **Functions** + **Differentiation** + **Trigonometric Identities** + **Limits & Continuity** + **NDA Physics Sound** + **Electricity & Magnetism** + **MHT-CET Maths Indefinite Integration**) — foundations → worked → self-check → Level 1 practice → per-subtopic mastery checkpoint, plus per-chapter interactive visualizations + a collapsible chapter revision sheet; rendered by registry-driven shared page components. **Per-chapter ship details are NOT duplicated in this header** — the one-line "what" per chapter (subtopic/concept counts, Phase-D reshape vs reorder-only vs as-is, diagram choices, slug prefix) lives in the file-layout `notes/` list in [ARCHITECTURE.md](ARCHITECTURE.md); the full "why" narrative per chapter is in the Decisions log. Append a new chapter's one-liner to that ARCHITECTURE.md list, not here. As of 2026-05-30 the `ConceptUnit` type is a **discriminated union** (`kind: "formula" | "reference"`) — formula variant carries an authored worked example, reference variant carries a flat `ReferenceTable` in the same slot — letting Recall-strand chapters teach via named-fact lookups (Sound is the first user; 7 of its 13 concepts are formula variants, 6 are reference variants). Definitions render `**bold**` key terms + `- ` bullet lists (via `RichText`, 2026-05-29); featured-PYQ + checkpoint cards now show the set-bound passage **context** and a `[Q# · month · year]` provenance citation. **Tier 1 IA + cross-linking shipped 2026-05-18** — primary nav (Bank · Guides · Notes) + cookie-backed exam pill in AppHeader; per-question backlink chips on /browse (chapter→playbook + subtopic→notes + principle/concept tag overrides); `/nda` exam home; two-way notes ↔ guide cross-links; click-to-reveal answer; in-app **question reports** (migration 0024) with admin triage at /dashboard/reports.

**Premium paywall shipped (2026-06-01):** self-serve student signup (`/signup`, Google + email/password) · per-user `entitlements` (orthogonal to orgs, migration 0026) · comp-access admin UI (`/dashboard/entitlements`) · `/notes` preview-gate (`tier:"paid"` chapters — all currently free, machinery dormant) · Razorpay ₹999/365 one-time checkout (`/pricing`, `/api/billing/*`). **Razorpay not yet activated** — checkout 503s until the 4 env keys are set. See the 2026-06-01 Decisions log + [[project-paywall-build]].

## Stack

- **Next.js 14** App Router · TypeScript · Tailwind 3 · shadcn-style UI primitives
- **Supabase** Auth + Postgres + RLS, accessed via `@supabase/supabase-js` (no Prisma, no ORM)
- **Vitest 2** for tests · `tsx` for scripts
- **Word export:** `temml` (LaTeX → MathML) → `mathml2omml` → `docx` + `jszip`
- **Browser math preview:** `katex` + `react-katex`
- **Deploy:** Vercel, git-integrated

## Multi-tenancy + visibility model

Per-school orgs, RLS-enforced. Three tiers of access:
- **Service role** (seed scripts, server-only admin actions, `/api/admin/members`) bypasses RLS by design — never expose to client code.
- **ADMIN** can do everything in own org: insert + update + delete questions, write options, upload Excel batches, auto-create chapters/subtopics, flip visibility, triage reports, manage members.
- **TEACHER** (as of migration 0025 / 2026-05-26) can **edit** question content — text, options, taxonomy moves, difficulty, PYQ metadata, images, concept/principle tags, leave-set — in own org. Cannot upload Excel, delete questions, flip PUBLIC/PRIVATE, auto-create chapters/subtopics, triage reports, or manage members. Server-side guard in `applyEdit` rejects `payload.visibility` changes from TEACHER (`kind: "forbidden_field"` → HTTP 403) so a tampered request can't bypass the hidden UI toggle.
- **anon** (no JWT) can read PUBLIC questions/options + all taxonomy. Cannot write anything.
- **STUDENT** (signed-in, self-serve, **no `org_members` row** — added 2026-06-01) registers at `/signup` (Google OAuth or email/password). By RLS construction they get zero private data; what an account unlocks is in-app question reports today + premium content via the entitlement axis below. Org-less = student (admins always get an `org_members` row atomically), so `/dashboard` redirects them to `/browse`.

**Premium entitlement axis (orthogonal to orgs, migration 0026).** Paid/comped access lives in `public.entitlements`, keyed on `auth.users(id)` — NOT org-scoped, because a student has no org. A grant unlocks a `scope` ('all' = full premium); `source` ∈ `razorpay | comp | manual` (the gate is **source-agnostic** — `userHasAccess` just asks "active entitlement for this scope?"; staff/org-members bypass the paywall). Writes are **service-role only** (no write RLS); reads are own-row only. Consumers: the `/notes` preview-gate (`tier:"paid"` chapters), `/pricing`/`/account`, the Razorpay webhook, and the `/dashboard/entitlements` comp UI. See the 2026-06-01 Decisions log entry + [[project-paywall-build]].

Each question has a `visibility` enum (`PUBLIC | PRIVATE`, default **PUBLIC** as of migration 0022 / 2026-05-17 — was PRIVATE prior). Read RLS is the union of two permissive policies: `(visibility = 'PUBLIC')` for everyone, plus `(org_id = private.current_user_org_id())` for authenticated org members on their own org's PRIVATE rows. Authenticated users see PUBLIC across all orgs + their own PRIVATE; anon sees only PUBLIC. Admins flip per-question visibility from the edit page.

RLS helpers live in the **`private` schema** (not exposed by PostgREST): `private.current_user_org_id()`, `private.current_user_is_admin()`, `private.current_user_can_edit_questions()`. New org-scoped policies should reference these. Use `current_user_is_admin()` for destructive / privileged ops (DELETE, upload, visibility flip, reports triage, member management); use `current_user_can_edit_questions()` for content edits that TEACHER should be allowed to do.

## File layout

The full file-by-file map — the `src/` tree (routes, components, lib helpers), `supabase/` migrations + seed, `scripts/`, and `tests/` — lives in **[ARCHITECTURE.md](ARCHITECTURE.md)**, extracted to keep this file's always-loaded payload lean. **Append a new file/component/route there, not here**; update the high-level shape below only when the top-level structure changes.

High-level shape:
- `src/app/` — Next.js App Router pages: `/browse` (paper builder), `/guide` (10 NDA strategy guides), `/notes` (11 teaching-notes chapters), `/dashboard` (admin), `/upload`, `/signup`·`/login`·`/pricing`·`/account`, plus the `/api/*` route handlers.
- `src/lib/` — server/pure helpers by domain: `supabase/`, `auth*`, `questions/`, `guide/`, `notes/`, `links/`, `export/` (Word/OMML), `entitlements/`, `billing/`, `reports/`+`notes-reports/`, `upload/`, `tags/`.
- `src/components/` — `AppHeader`/nav, `math/` renderers (`KatexRenderer`·`RichText`·`BlockText`·`parseTableBlocks`), shadcn `ui/` primitives.
- `supabase/migrations/` — `0001…0029` (append-only; 0029 = `subtopics.order_index` for /notes teaching order). `scripts/` — seeds, `notes-lint`, `notes-latex-audit`, `notes:order` sync, `jee/` ingestion. `tests/` — Vitest unit + DB-integration.

## Commands

```sh
npm run dev                # next dev (local)
npm run build              # production build
npm run lint
npm run typecheck          # tsc --noEmit (covers test files too — keep this clean)
npm test                   # vitest run
npm run test:watch
npm run db:seed            # idempotent taxonomy seed (service-role)
npm run extract:taxonomy   # regenerate supabase/seed/taxonomy.json from a reference Excel
npm run notes:lint         # validate /notes editorial modules against live taxonomy + DB tags (read-only)
npm run notes:latex        # audit /notes for LaTeX-in-plain-text leaks + delimiter imbalance + unicode (read-only)
npm run notes:order        # populate subtopics.order_index from each /notes chapter's subtopicOrder (teaching order on /browse); --dry previews
npm run notes:coverage [subjectRoute] [chapterSlug]  # triage probe: math in a chapter's PUBLIC solutions but NOT in its notes (formula-gap candidates); default nda-maths statistics. Noisy — human-review list, not a gate
npm run tags:audit         # principle-tag audit (read-only)
npm run audit:underlines   # scan PUBLIC questions for \underline{...} segments not covered by the docx-export bypass
npm run stats              # report live bank size (PUBLIC by exam/subject) + taxonomy + /notes chapter & diagram counts — for verifying the header lines instead of hand-maintaining them
npm run db:types           # supabase gen types (requires SUPABASE_PROJECT_REF env)
npm run prepush            # the chained gate, MIRRORS CI: typecheck → lint → notes:lint → notes:latex → test → build; run by .githooks/pre-push
npm run gate [target]      # wrapper that runs `npm run <target>` (default prepush) and prints a GATE_RESULT: PASS/FAIL sentinel as its LAST line — so the result survives `| tail` even though a pipe masks the exit code
```

> **Pre-push gate.** `.githooks/pre-push` runs `npm run prepush` before any push — which now **mirrors CI exactly**: typecheck → lint → notes:lint → notes:latex → **test** → build (single source of truth: the `prepush` script). So a green pre-push means a green CI. Widened 2026-06-02 from the old build-only chain after a broken test passed pre-push and only failed in CI (it didn't run `npm test`). Cost: ~3–4 min (full suite + build). Activated by the `prepare` script (`git config core.hooksPath .githooks`) on `npm install`. `.gitattributes` pins LF on `.githooks/*` so the shebang resolves on Linux/WSL contributors. Escape hatch for WIP pushes: `git push --no-verify` — don't reach for it unless you know why.

> **CI gate.** `.github/workflows/ci.yml` runs the **full** gate (typecheck → lint → `notes:lint` → `notes:latex` → `npm test` → build) on every push to `main` + every PR. The local pre-push hook now runs this same chain (see Pre-push gate above), so CI is a backstop rather than the first place these checks run — it still matters because it runs on a clean checkout with repo secrets even if someone `--no-verify`s. Requires three repo secrets (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`); without them the build + DB steps fail and the DB tests skip. Runs against the single prod Supabase project (test rows auto-swept by `global-teardown`) — point the secrets at a staging project if write traffic gets noisy. Vercel still owns deploys; CI is the correctness gate.

## Conventions specific to this project

- **Spacing scale.** Page wrappers `p-8`, content cards (CardContent default) `p-6`, compact cards (FilterBar etc.) `p-4`, nested option blocks `p-3`. Pick the smallest tier that contains breathing room — don't mix `p-4`/`p-6` in the same visual rhythm.
- **Typography.** UI text uses `font-sans` (Inter, default). Question/option/solution body text uses `font-serif` (Source Serif 4) — it's how teachers tell content apart from chrome. Both are loaded via `next/font` in `src/app/layout.tsx` and exposed as Tailwind utilities.
- **Color / brand (indigo, 2026-06-01).** The brand accent is **indigo** — chosen to unify with the `/notes` content palette. Three CSS vars in `globals.css`: `--brand` + `--brand-foreground` (fills — indigo-600 + white, AA-safe as a button bg in both themes) and `--brand-accent` (text/icon/border — indigo-600 light / indigo-400 dark, AA-legible as small text in both). `--ring` points at the brand so focus rings are branded. Tailwind exposes `bg-brand` / `text-brand-foreground` / `text-brand-accent` (+ a `brand` Button variant). **Application is SELECTIVE**: brand goes on chrome the eye lands on (primary CTAs, active nav, header mark, hero, stat numbers, table heatmap/peak-highlight, focus ring); near-black `--primary` stays the workhorse for secondary/admin buttons. **Off-limits:** the `/notes` + `/guide` *content* colors (concept-card indigo, amber traps, sky self-check, violet practice, emerald checkpoints) are content semantics — do NOT fold them into the brand token or recolor them. See [[brand-indigo-tokens]] + the 2026-06-01 Decisions-log entry.
- **Icons.** Use `lucide-react` for all icons. Never emoji or text glyphs (▾, ‹, ›) for UI affordances. Standard sizes: `h-3.5 w-3.5` for inline-with-text, `h-4 w-4` for buttons, `h-5 w-5` for the AppHeader logo.
- **Toasts.** `import { toast } from "sonner"`. Fire on every async user-initiated success or failure (save, upload commit, export download, sign-out). Setting an inline `error` state is fine, but never *replace* a toast with it — both should fire so the user can't miss the result while scrolled.
- **AppHeader.** Every page wraps its `<main>` in `<><AppHeader />…</>`. Don't re-add ad-hoc back-links, theme toggles, or sign-out buttons on individual pages — theme lives in the `ThemeToggle` island in the header (visible to anon + authed), sign-out lives in the UserMenu popover (authed only).
- **No Prisma.** Supabase RLS is the security boundary — every read/write goes through `@supabase/supabase-js` so the user's JWT carries through. Reach for `createSupabaseAdminClient()` (service-role) only in server-only contexts (seeds, cross-org admin) — it bypasses RLS by design.
- **Migrations are append-only.** New SQL goes in a new `000N_<name>.sql`. Apply via Supabase MCP `apply_migration`. Never edit a previously-applied file.
- **`.mcp.json` is gitignored.** Holds Supabase + Vercel personal access tokens. Don't commit.
- **Idempotent seeds + uploads.** `scripts/seed.ts` and the upload commit both check existence before insert; re-running is safe.
- **Content hashing for upload dedup.** `content_hash = sha256(normalised question text + sorted options + answer)` plus a unique index on `(org_id, content_hash)`. Re-uploading the same file inserts 0 rows.
- **Text normalization at write boundaries.** Long-form user-authored text (question, context, solution, options) goes through `normalizeNewlines` in the upload parser and the edit validator. The DB never carries literal `\n` (backslash+n) past the boundary; math zones are preserved verbatim. Any new write path that accepts long-form text must call the helper before insert/update — otherwise the render layer can't distinguish "real line break" from "literal text".
- **OMML + math defaults via post-processing.** Two `docx`-library limitations land on the same workaround. (1) `ImportedXmlComponent` wraps imported XML in an `<undefined>` element Word rejects, so we emit placeholder runs and swap them for OMML after `Packer.toBuffer`. (2) The library doesn't expose `<m:mathPr>` for settings.xml, and Word's built-in math defaults (`defJc=centerGroup`, `wrapIndent=1440`, non-zero margins) shift any paragraph containing a 2-D math element (`<m:f>` etc.) ~1″ right of plain-text paragraphs — option `(A) -3/4` ends up out of column with option `(A) -1`. `patchZip` in `src/lib/export/docxBuilder.ts` handles both: marker → OMML in `word/document.xml`, and `<m:mathPr defJc=left wrapIndent=0 lMargin=0 rMargin=0>` injected before `</w:settings>` in `word/settings.xml`.
- **Restart `npm run dev` after adding new routes.** Tailwind JIT can miss files created mid-session. Stop dev, `rm -rf .next`, restart before UI smoke tests.
- **`/notes` LaTeX in body prose only.** Four `SubtopicNote` / `ConceptUnit` fields render as plain text (no KaTeX) AND feed SEO `metadata.description` / JSON-LD where KaTeX would be invalid: `note.title`, `note.oneLineDefinition`, `note.whyItMatters`, `concept.name`. LaTeX in any of these leaks through as raw `\(\hat{i}\)` markup on the page AND in page metadata. **Keep math only in body fields** (`intuition`, `definition`, `formula.latex`, `authoredExample.{prompt,steps,answer}`, `trap.{title,body}`) — those go through `KatexRenderer` correctly. Use Unicode (`î`, `ĵ`, `k̂`, `Σ`, `°`) or plain prose for the four plain-text fields. **Body fields also support `**bold**`** (renders via KatexRenderer / RichText — use it for key terms), and the **`definition` field additionally supports `- ` bullet lists** (rendered by `RichText`): bullet *list-shaped* definitions (term/type/property enumerations), keep narrative ones as prose. The plain-text fields render `**`/`- ` literally — never use them there.
- **Word export: lowercase option labels at render only.** Bank stores `A/B/C/D` uppercase (Excel + DB convention); the `.docx` export renders them as `(a)/(b)/(c)/(d)` in both question paper and answer key (`opt.label.toLowerCase()` in docxBuilder). Browse cards + edit form still show uppercase — only the rendered Word doc is lowercased. If a future surface needs to render bank-style labels, follow the same boundary: lowercase at render, never in the DB.
- **Word export: underline bypasses the math pipeline.** DB convention is `\(\underline{\text{word}}\)` (or `\(\underline{\textit{word}}\)` for italic+underline — biology taxonomy questions). KaTeX renders these in the browser; the docx export detects them via `UNDERLINE_BYPASS_RE` in [src/lib/export/ommlBuilder.ts](src/lib/export/ommlBuilder.ts) and emits a native Word run (`TextRun({ text, underline: { type: "single" }, italics? })`) instead of routing through `temml → mml2omml → <m:borderBox>` — Word doesn't reliably draw the bottom border on inline-math borderBox under our injected math defaults. Trailing punctuation INSIDE the delimiters (`\(\underline{\text{x}}.\)`) is split off into a separate text segment. Any pattern outside the regex (bare `\underline{word}`, chained `\text{}\text{}`, `\textbf` variants, block math) falls through to the OMML pipeline and renders without an underline in Word — run `npm run audit:underlines` after each new English / Biology batch to catch fall-throughs.
- **Tables in question content use GFM pipe-tables (2026-06-02).** A table inside a question `text` or `context` is authored as a standard Github-flavored markdown pipe-table — a header row **plus a `|---|---|` separator row**, then data rows. The separator row is mandatory and is what distinguishes a real table from inline `|·|` math (determinant `\(|A|\)`, abs value `|x|`, cell notation `Zn|Zn²⁺`): a line of pipes with **no** following separator stays prose. `parseTableBlocks` in [src/components/math/parseTableBlocks.ts](src/components/math/parseTableBlocks.ts) (pure, math-masked via `maskMathZones`, TDD in `tests/parse-table-blocks.test.ts`) splits text into prose + table blocks; cells keep their `\(...\)` math and `**bold**`. Rendered by `BlockText` (`src/components/math/BlockText.tsx`) on `/browse` (expanded card only — collapsed preview stays plain `KatexRenderer` so a table can't blow past `line-clamp-2`) + the editor Preview + the `/notes` mastery-check / `/guide` worked-example cards (`WorkedExampleCard`'s `example.context` + `example.text`, 2026-06-04), and as a native Word `<w:tbl>` in `docxBuilder` (the question NUMBER rides on the first paragraph; a stem opening with a table gets a numbered empty paragraph first). **No DB primitive** — it's all in the existing `text`/`context` columns; `normalizeNewlines` is compatible (it upgrades an Excel-authored literal-`\n` table into real newlines). **Future-only**: the existing 13 Statistics (lossless 2-row linearization) + 37 Match-Lists were NOT backfilled — they still render as linearized text. Options don't support tables (v1). For a genuine ≥3-column data grid that can't be a markdown table, fall back to a question image like a diagram.

## Recurring pitfalls

Patterns that have bitten this codebase more than once. Each one earned its place by causing a real production regression — read before writing new code that resembles the shape.

- **PostgREST 1000-row cap on raw `.select()`.** Any `client.from(...).select(...)` that derives a count, a distinct set, a sort, or any aggregate from the *row payload* (rather than response headers via `count: "exact"` or a SQL aggregate via RPC) is suspect once the table can exceed 1000 rows. PostgREST silently truncates at row 1000 — no error, just wrong answers. **Already bit us three times:** dashboard totalQuestions (migration `0018` get_dashboard_stats), PYQ-year filter dropping older years (migration `0019` get_pyq_years), and upload commit dedup races (fixed by switching to `.upsert(..., { onConflict, ignoreDuplicates: true })` — let the DB unique index enforce dedup, not an in-memory `Set`). **Audit checklist before writing any new `.select()`:** (1) Is the row payload itself the answer, or are you computing something from it? (2) If computing, can the underlying table exceed 1000 rows ever (today or as the bank grows)? (3) If yes, use `count: "exact", head: true` for counts, or write a `security invoker` RPC that does the aggregate in Postgres. Never trust a row-derived count from `.select()` past the first 1000 rows.
- **`next dev` is more forgiving than `next start` for server↔client prop serialization.** Functions and other non-serializable values passed from a Server Component to a Client Component compile cleanly under `next dev` but throw at SSR time under `next start` ("Functions cannot be passed directly to Client Components"). This bit us on the 2026-05-13 Pagination revert. **Mitigation:** the pre-push hook in `.githooks/pre-push` runs `next build` before allowing a push, so this class of bug is caught locally rather than after a Vercel deploy. Don't `git push --no-verify` unless you know exactly why.
- **Source-source subtopic name collisions during bulk chapter merges.** When canonicalizing N source chapters into one target chapter, a bulk `UPDATE subtopics SET chapter_id = '<tgt>' WHERE chapter_id IN ('<src1>', '<src2>', ...)` can fail with `23505` on `subtopics_chapter_id_name_key` even if a pre-flight source-vs-target collision check came back clean — because two sources may share a same-named subtopic that collides only once both are reparented. Bit us on the NDA Physics E&M cluster (5 sources → "Magnetic Field Inside a Solenoid" appeared in two of them). **Mitigation:** process sources sequentially via a `DO $$ FOR mapping IN (VALUES (...)) LOOP ... END $$` block; each iteration redirects colliding questions, deletes colliding source subtopics, reparents the rest, updates remaining question chapter_ids, and deletes the source chapter — so by the time source 2 merges, source 1's content is already in target and the standard collision-handling kicks in. Pattern + full template in `[[reclassification-sql-pattern]]`.
- **Catch-all chapters and subtopics drift until you reclassify per-question.** Buckets named "Mechanics", "Physics", "Error Identification in Sentences" collect every question that doesn't cleanly fit a more-specific name. They look harmless during a chapter-level merge but rot the bank's usefulness for technique drills. **Mitigation:** when a chapter or subtopic holds ≤~50 mixed-topic questions, read each stem and produce a `(question_id, target_canonical_id)` mapping; execute as a `DO $$ FOR m IN (VALUES (qid, tgt)) LOOP ... END $$` that (a) looks up the question's source subtopic, (b) finds same-named subtopic in target chapter, (c) redirects+deletes on collision, otherwise reparents+moves chapter_id. Precedents: Spotting Errors 29 q (2026-05-15 NDA English); Mechanics 19 q + Physics 26 q catch-all chapters (2026-05-15 NDA Physics). Beyond ~50 q the user prefers chapter-level merge + subtopic-cleanup as a follow-up pass.
- **`/notes` subtopic slugs are GLOBALLY unique, not chapter-scoped.** `question_concept_tags` keys on `(question_id, subtopic_slug, concept_slug)` with **no chapter or subject column**, and `loadConceptDrills` / the mastery-checkpoint query filter by `subtopic_slug` ALONE. So two chapters that both use a slug like `foundations` will cross-contaminate each other's drill lists + checkpoints. Bit us 2026-05-30: MHT-CET Indefinite Integration reused Sound's `foundations` slug — notes-lint caught it as "DB tag has unknown concept_slug" because the other chapter's concepts don't match. **Mitigation:** every notes subtopic slug must be unique across ALL chapters in `NOTES_CHAPTERS` (the URL segment can repeat per subject route, but the slug used as the concept-tag key cannot). MHT-CET's foundation subtopic was renamed `foundations` → `fundamentals`. notes-lint surfaces the collision but only *after* tags are inserted — pick distinctive slugs up front.
- **Renaming a SHIPPED chapter silently rots its `/guide` + `/notes` cross-refs.** The DB taxonomy and the guide/notes editorial TS are joined only by NAME at request time (`resolveTaxonomy`), so a Phase-D rename/merge leaves the TS pointing at a dead subtopic — the "Drill the N →" CTA resolves to an EMPTY `/browse` filter, no error. Bit us twice on 2026-05-30: the Sound notes 5→4 redesign broke the `/guide/nda-physics` Sound playbook + strategy `mustDrill` + CHAPTER_TABLE prose; the 2026-05-29 Probability merge ("Probability with Dice" → "Probability via Counting") broke two `nda-maths` principle drills + the Probability tier. **Mitigation:** after any rename/merge of a shipped chapter, sync `/guide` `_data` (playbooks `subtopics[]`, strategy `mustDrill`/`skipSubtopics`, principles `drill`, compounds filters, prose counts) + `/notes` `_data` + concept tags, then run `npm test` — the 7 playbook guides + CA themes + Maths each have a `describe.skipIf(!HAS_ENV)` live-taxonomy resolution test (`guide-nda-<subject>-playbooks.test.ts`; `guide-nda-maths-taxonomy.test.ts` for Template A) that fails on drill-target drift. Full procedure: [[shipped-chapter-rename-downstream-sync]]. The `/browse` backlink chip (`getQuestionResources` name lookup) + the single-page Economics chapter CTA are the cross-link surfaces still untested.

## Design axes

Load-bearing design distinctions for the project. Each one earns its place by being structural — getting the axis wrong has cost real work, and the answer to "which axis does this live on?" is non-obvious for the named feature areas.

### Tag taxonomies — principles (horizontal) vs concepts (vertical)

**`/guide` principles** and **`/notes` concepts** are different kinds of tag and must NEVER share a schema or be merged into one taxonomy.

| | Principles | Concepts |
|---|---|---|
| Axis | Horizontal — cross-subtopic | Vertical — within-subtopic |
| Cardinality | ~20 in NDA Maths | ~5–10 per subtopic × hundreds = thousands at scale |
| Storage | TS modules (`/guide/.../principles.ts`) + curated `extraQuestionIds` | DB table `question_concept_tags` (migration 0021) |
| Authoring | Editorial — hand-curated, slow update cadence | Per-upload — admin tagging UI or chat session, fast cadence |
| Value comes from | **Selectivity** (only the top-20, hand-picked) | **Coverage** (every relevant question tagged) |
| Identity question | "What unifying lens does this question demand?" | "What specific technique within this chapter?" |

**The wall — non-negotiable:**
- DON'T migrate `/guide` principles to a DB table. Hand-curation is the signal-to-noise mechanism; "scaling" them dilutes their value.
- DON'T bundle principles into `question_concept_tags` via a `tag_kind` discriminator. Flattens the strategic distinction.
- DO share infrastructure at the seam: one admin tagging UI on `/questions/[id]/edit`, one notes-lint validator, future LLM-suggest can suggest both kinds. Sharing UI ≠ sharing schema.

**Why this earned a section:** The initial concept-tags proposal (Phase 1, 2026-05-17) was a unified `question_tags` table. User pushed back; I conceded. Both axes shipped separately (principles still in TS, concepts in migration 0021) and the wall has held. This section exists so future tag-related features (admin UI, LLM-suggest, AI tutor grounding, MHT_CET_AI integration) don't re-litigate it from scratch.

### Cross-link backlinks — chapter-level (automatic name lookup) vs tag-level (manual DB-tagged override)

The `/browse` QuestionCard chip row (Tier 1 + Tier 1.5, 2026-05-18) renders **two distinct classes of backlink** and the wall between them matters. Same dropdown of chips, two completely different authoring loops and data sources.

| | Chapter-level chips | Tag-level chips |
|---|---|---|
| Examples | "Playbook: Sound" · "Concept notes" | "Lever: Vieta" · "Concept: Weighted Mean" |
| Source | Name-based lookup at render time | DB row in `question_principle_tags` / `question_concept_tags` |
| Authoring cost | **Zero per question** — purely a function of `(exam, subject, chapter, subtopic)` | **Manual per question** — chat tagging session, INSERT with `ON CONFLICT DO NOTHING` |
| Coverage | 100% of questions whose chapter has a guide / subtopic has notes | Only questions explicitly tagged (today: 488 principle tags · 262 concept tags) |
| Failure mode | Silent break when DB chapter renamed (no lint catch yet — see ROADMAP "notes-lint guide-side rename validation") | Silent fallback to generic chip when slug doesn't resolve via `getPrincipleName` / `getConceptName` |
| Override behaviour | The default; renders for all qualifying questions | When present, **replaces** the chapter-level chip for the same axis (more specific wins) |
| Resolver | `getQuestionResources({exam, subject, chapter, subtopic})` in `src/lib/links/questionResources.ts` | `getQuestionResources({...}, tags)` — same function, second arg from `getResourceTagsForQuestions` batch |

**The wall — design intent:**
- DON'T expand the chapter-level lookup to do "fuzzy" matching when a chapter rename happens. The failure mode (chip disappears) is the correct signal — fix the rename, don't paper over the drift.
- DON'T attempt to back-fill tag-level chips from the chapter-level data (e.g. auto-create a `Lever: <chapter-name>` chip when no principle tag exists). That destroys the curated-vs-automatic distinction and floods low-leverage questions with meaningless tag chips.
- DO let tag-level chips quietly fall through to chapter-level chips when the tag's slug doesn't resolve. Better a generic "NDA Maths strategy" chip than a broken `/principles/<unknown-slug>` link.
- DO share infrastructure at the seam: both axes flow through the same `getQuestionResources(input, tags?)` function, both render via the same `<ResourceChip>` component in QuestionCard. The two-tier system is a function-signature distinction (optional second arg) not two separate code paths.

**Why this earned a section:** The 2026-05-18 Tier 1.5 ship added tag-level overrides AFTER the Tier 1 chapter-level chips were in production. The temptation in the moment was to refactor into "one unified backlink table" or to have tag-level chips deduplicate against chapter-level chips. Both ideas are wrong — they conflate two different authoring loops (curated tagging session vs no-touch render-time mapping) and two different coverage shapes (long-tail per-question vs blanket-per-chapter). The split shipped cleanly because the function signature preserved the asymmetry: `tags?` is optional, the resolver short-circuits chapter-level chips when a more-specific tag-chip exists, and untagged questions still get the generic chip — graceful fallback throughout. Sister wall to the tag-taxonomies axis above.

### Guide strand axis — execution-mode (chapter-partitioning) vs tier-style (chapter-tiering)

Within Template B variants, the **strand split** is the strategic axis the strategy page renders against. There are two fundamentally different ways to construct it, and they are NOT interchangeable — picking the wrong one produces a strategy axis that sorts questions by nothing meaningful.

| | Execution-mode strands | Tier-style strands |
|---|---|---|
| Subjects | NDA English (R/Rule/Reason), NDA Chemistry (R/Rule/Calculate), NDA Biology (R/Apply/Verify), NDA Geography (R/Apply/Verify) | NDA History (Cornerstone/Foundation Recall/Quick-Win) |
| Strand semantics | Each strand is a SKILL bucket (recall a fact / apply a rule / trace a mechanism / evaluate a statement). Chapters get grouped into a strand by their dominant execution mode. | Each strand is a STRATEGIC TIER (cornerstone weight / foundation recall / quick-win). Chapters get grouped into a strand by their bank share + %HARD profile. |
| Recall slot | Stable — there's always a Recall strand (named-fact recall is universal in Indian entrance exams). | Stable in spirit (Foundation Recall) but the slot SEMANTICS bent — it now groups chapters by tier, not by execution mode. |
| When to pick | Execution modes PARTITION chapters: one cluster of chapters is 60%+ dominated by one mode, others are 20%- on that mode. | Execution modes are UNIFORM across all chapters (25–45% everywhere) — can't partition. AND/OR one chapter has dominant bank share (40%+) that warrants its own Cornerstone tier. |
| Decision probe | For each candidate mode (recall/rule/apply/verify/calculate/reason/multi-statement/date-anchored), count `q × chapter_spread` AND per-chapter %. Top mode at 60%+ in one cluster = execution-mode strands work. | Top mode is 25–45% of every chapter = uniform = falls back to tier-style. Use bank-share + %HARD per chapter as the partition signal instead. |
| HARD calibration | Strand-level (densest-HARD strand called out — Geography's Apply at 23% avg HARD). | Implicit in the tier-structure — Cornerstone IS the densest-HARD tier by construction (History's Cornerstone = Modern India 34% HARD). |

**The wall — design intent:**
- DON'T force execution-mode strands when modes don't partition. History's multi-statement (70 q × 4 ch · 43% HARD) and date-anchored (61 q × 4 ch · 43% HARD) cut UNIFORMLY across chapters — a Bio/Geo-style R/Apply/Verify split would force lopsided cuts (Recall 44 q : Date 163 q : Verify 53 q if grouped by chapter's dominant mode). The strategy axis would sort by nothing meaningful.
- DON'T force tier-style strands when execution modes DO partition cleanly. English's Vocab/Idioms (Recall) and RC/Cloze (Reason) chapters are genuinely different SKILL types, not just different priorities — a Cornerstone/Foundation/Quick-Win split would lose that information.
- DO let the decision probe pick. Test execution-mode first (Template B default); fall back to tier-style only when modes are uniform.
- DO share infrastructure at the seam: both axes use the same `STRATEGY_STRANDS` data shape (each strand has `id`, `label`, `pitch`, `approach`, `chapters: StrandChapter[]`) and the same strategy-page renderer. The two-axis distinction is in the BUCKET MEANINGS (`PlaybookBucket = "recall" | "apply" | "verify"` for Bio/Geo vs `"cornerstone" | "foundation" | "quickwin"` for History) — same code path, different semantics.

**Why this earned a section:** The 2026-05-19 NDA History ship was the first time Template B's strand axis itself bent. Before History, every Template B variant had bent the second + third SLOTS within an execution-mode axis (Rule→Apply for Bio, Calculate→Verify for Chem) — but the axis itself was always execution-mode. History exposed the next level: when execution modes don't partition chapters at all, the axis itself has to change. The temptation in the moment was to force one of the existing slot-bend patterns onto History (most naturally R/Apply/Verify like Bio + Geo) — that would have shipped a strategy page where students see "Apply chapters" and "Verify chapters" labels but no chapter actually leans either way, sorting by nothing. The tier-style fallback shipped cleanly because the strand data shape was already general (chapters grouped under labelled strands with a pitch + approach) — only the bucket vocabulary changed. Sister wall to the tag-taxonomies + cross-link-backlinks axes above: in all three, the right call is to preserve an asymmetric distinction at the shared seam rather than to flatten for elegance.

## Guide structure templates

Three guide structures have shipped. They serve different bank shapes; a fourth subject's guide should pick from these (or pivot again) by analysing the bank first, not by copying precedent.

### Template A — principles-first (NDA Maths)

Use when the bank has **cross-chapter levers** (one principle spans ≥4 chapters) and **non-flat %HARD** (multiple chapters > 20% HARD).

- **Routes:** landing + `/strategy` (Tier A/B/Skip) + `/principles` (catalog) + `/principles/[slug]` (deep dives) + `/compound-tricks` + `/trends` + `/traps`.
- **Tag infra:** DB-backed `question_<X>_tags` table (today: `question_principle_tags`, migration 0023). Hand-curated TOP_N; counts + %HARD + chapter spread computed live from the tag table at request time.
- **Strategy axis:** %HARD-driven tiering. Chapters with high q-count AND low %HARD = Tier A; high q-count + high %HARD with one drillable subtopic = Tier B cherry-pick; everything else = skip.
- **Editorial data shape:** `OVERVIEW + CHAPTER_TABLE + TIER_A/B + SKIP_LIST + TOP_11 principles + DOMAINS long-tail + COMPOUNDS + DRIFT_ROWS + TRAPS + per-principle DETAILS`.

### Template B — playbooks-first (NDA English, NDA Chemistry, NDA Biology, NDA Geography, NDA History)

Use when the bank has **sealed chapters** (largest cross-chapter lever < ~30 q). Pure Template B also requires **flat %HARD** (≤2 chapters > 15%); the Geography variant relaxes that gate (see "B + non-flat %HARD" below).

- **Routes:** landing + `/strategy` (skill buckets, not tiers) + `/playbooks` (catalog) + `/playbooks/[slug]` (deep dives — one per major subtopic OR one per chapter — see variants below) + `/<subject-specific-page>` (e.g. `/vocab-families` for English, `/common-compounds` for Chemistry) + `/trends` + `/traps`.
- **Tag infra:** NONE. Cross-chapter horizontal axis is too thin to justify a DB table. Drill CTAs use `subtopicIds` or `chapterIds` from `resolveTaxonomy` at request time — same machinery as the maths long-tail DOMAINS principles.
- **Strategy axis:** Skill-type buckets, not %HARD tiers. The middle bucket is the variable slot — pick it from the subject's natural seams:
  - **English** uses Recall (Vocab + Idioms — memorisation) / **Rule** (Errors + Grammar — rule application) / **Reason** (RC + Cloze + Rearrangement + FIB — context-driven).
  - **Chemistry** uses Recall (Carbon + Matter + Industrial + Metals + Hydrogen + Everyday Life + Practical — fact recall) / **Rule** (Atomic Structure + Acids/Bases + Reactions + Bonding — rule application) / **Calculate** (Mole Concept — small numeric strand). Chemistry has no chain-reasoning lever (no analogue of RC author-stance) but does have a small but distinct numeric strand worth surfacing as its own bucket.
  - **Biology** uses Recall (Human Physiology + Cell Biology + Microbiology + Biodiversity + Genetics — named-fact recall) / **Apply** (Plant Biology + Reproduction — mechanism-tracing: photosynthesis flow, osmosis direction, inheritance ratios, pollination genetics) / **Verify** (Ecology + Biochemistry — multi-statement true/false evaluation, the bank's second-largest question shape at ~24 q). Biology has NO numeric strand (no analogue of Mole), no Rule-application lever beyond pattern-application (which collapses into Recall in practice), and only 4 chain-reasoning HARDs across 190 q — too thin for a Reason strand. The **third slot bent to Verify** to capture the dominant non-recall execution mode; the **second slot bent to Apply** to surface the mechanism-tracing chapters that carry 4 of the bank's 5 HARDs. So Biology's Recall + Apply + Verify split is unique even within Template B — the Rule slot itself bent.
  - **Geography** uses Recall (Indian Geography Economy + Indian Geography Physical + World/Human — named-fact recall: rivers/peaks/states/crops/minerals/ports) / **Apply** (Climatology + Earth's Structure — mechanism-tracing: cyclogenesis, plate tectonics, weathering, rock-cycle classification) / **Verify** (Earth in Space + Oceanography — multi-statement evaluation: "consider the following statements about terrestrial planets / cold ocean currents"). Same Recall/Apply/Verify split shape as Biology; the unique twist is that Geography has **non-flat %HARD** (4 of 7 chapters >15% HARD) — see "B + non-flat %HARD" below for the variant pattern. Initial guess was a Map/Concept/Current-Events split (in the deprecated "other subjects may need different splits" prose); SQL analysis showed Recall/Apply/Verify fits better because Geography's multi-statement shape (92 q) dominates the non-recall execution mode.
  - Current Affairs (not yet a guide) might still need a different split — one-bucket-many-themes. The Recall slot is universally stable; the OTHER TWO slots bend to the subject's natural seams.
- **Playbook granularity:** English uses per-subtopic playbooks (chapters are skill buckets, each subtopic is a distinct question type — 16 playbooks across 8 chapters). Chemistry uses per-chapter playbooks (subtopics well-cleaned but thin at 2–5 q each — 12 playbooks 1:1 with chapters, same rationale as physics). Biology uses per-chapter playbooks too (9 playbooks 1:1 with chapters; subtopics thin, max 13 q on HP.Circulatory + MD.Pathogens). Geography uses per-chapter playbooks too (7 playbooks 1:1 with chapters; subtopics avg ~5 q, max 36 q on Agriculture). Pick based on whether subtopics are large enough to anchor a standalone deep-dive.
- **Subject-specific page:** Required for Template B in practice. English's `/vocab-families` (13 themed word clusters); Chemistry's `/common-compounds` (6 themed name↔formula↔use clusters, ~50 compounds, rendered via the new `CommonCompoundsTable` component); Biology's `/reference-tables` (4 themed clusters, ~52 named-fact pairs — rendered via the generic **multi-domain** `ReferenceTables` component: each cluster carries its own column headers); Geography's `/reference-tables` (4 themed clusters, 62 named-fact pairs — Indian Rivers / Mountain Peaks / Mineral & Crop Producer States / Local Winds + Climate Zones — same generic `ReferenceTables` component). All four follow the same recall-anchor pattern — a flat-list reference the recall strand drills against.
- **Editorial data shape:** `OVERVIEW + CHAPTER_TABLE + STRATEGY_BUCKETS + PLAYBOOKS + per-playbook DETAILS + <subject-specific clusters/families> + DRIFT_ROWS + TRAP_SHAPES`.

#### Template B variants — pure-B strand axis (English / Chemistry / Biology)

Three subjects fit pure Template B (sealed chapters + flat %HARD) but differ on the strand split and on subject-specific page shape. Biology in particular shows that even the SECOND slot can bend (Rule → Apply) when the bank has no rule-application lever. Geography is NOT in this table — its strand split is identical to Biology's; what makes Geography different is its non-flat %HARD, covered in the second table below. Use this table as a quick decision aid for the next pure-Template-B subject — match each axis to the new subject's seam, don't reflexively copy any column.

| Axis                       | English (the original B)                                                                   | Chemistry (variant 1)                                                                                | Biology (variant 2)                                                                                  | What decided                                                                                         |
|----------------------------|--------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------|
| **Bank size**              | 900 q · 8 ch · 29 sub                                                                      | 262 q · 12 ch · 47 sub                                                                               | 190 q · 9 ch · 39 sub                                                                                | Bank shape, not template choice.                                                                     |
| **Strand split**           | Recall / **Rule** / **Reason** — Vocab+Idioms · Errors+Grammar · RC+Cloze+Rearrangement+FIB | Recall / **Rule** / **Calculate** — Carbon+Matter+Industrial+Metals+Hydrogen+Everyday+Practical · Atomic+Acids+Reactions+Bonding · Mole | Recall / **Apply** / **Verify** — HP+Cell+Micro+Biodiversity+Genetics · Plant+Reproduction · Ecology+Biochemistry | Biology has no rule-application lever (the Rule slot itself bent → Apply) and no numeric strand (Calculate doesn't apply). |
| **Bucket shapes**          | Recall (memorise) / Rule (apply principle) / Reason (chain-context inference) | Recall (memorise) / Rule (apply principle) / Calculate (numeric plug-in) | Recall (memorise) / Apply (mechanism-trace) / Verify (multi-statement T/F evaluation) | What execution modes the bank's questions ACTUALLY demand.                                          |
| **% of bank**              | Recall 45% / Rule 25% / Reason 30%                                                          | Recall 55% / Rule 42% / Calculate 3%                                                                  | Recall 69% / Apply 22% / Verify 8% (by chapter grouping; by question shape Recall is 82%)             | Calculate is small (3%) because Mole is one chapter. Verify is small (8%) because Ecology + Biochemistry are the only chapters where the shape dominates. |
| **Playbook granularity**   | Per-subtopic (16 playbooks across 8 chapters) — chapters are skill buckets, subtopics are distinct question types large enough to anchor a deep-dive | Per-chapter (12 playbooks 1:1 with chapters) — subtopics well-cleaned but thin at 2–5 q each, would proliferate without adding clarity | Per-chapter (9 playbooks 1:1 with chapters) — subtopics thin (max 13 q), per-subtopic split would proliferate | Largest subtopic size. English's biggest is Synonyms 150 q; Chemistry's Allotropes 15 q; Biology's Circulatory 13 q. |
| **Subject-specific page**  | `/vocab-families` — 13 themed word clusters mined from PYQ underlines                       | `/common-compounds` — 6 themed name↔formula↔use clusters, ~50 compounds                              | `/reference-tables` — 4 themed clusters (diseases↔pathogens, vitamins↔deficiencies, hormones↔glands, scientists↔discoveries), ~52 named-fact pairs | The recall-strand's highest-leverage flat-list artefact. All three are 4-pass active-recall references.   |
| **Subject-specific renderer** | Inline JSX in the page (no dedicated component — clusters are simple `<ul>` lists)      | `CommonCompoundsTable` component (single-domain — every cluster is name↔formula↔use)             | Generic `ReferenceTables` component (MULTI-domain — each cluster carries its OWN `{name,fact,context}` column headers; Bio/Geo/History/Polity all pass their `REFERENCE_CLUSTERS` + `guidePath`) | Compound table needs richer per-row affordances than word lists; reference-tables need per-cluster column headers since the domains differ. |
| **Trends headline framing**| "Grammar exploded post-2024" — chapter-mix shifts are the story                            | "Paper has NOT hardened" — explicit negation of Physics's "hardened 22×" framing                     | "Paper has NOT hardened — only 4 HARDs across 190 q over 10 yrs" — even more explicit absence-of-drift framing (Biology is flatter than Chemistry) | Whether the bank's drift has a sharp story or a stable one. Frame the absence of drift explicitly when a reader might import the wrong calibration from a sibling guide. |
| **Trap-page bucketing**    | 9 trap shapes by Recall/Rule/Reason — meaning-based (near-synonym, near-idiom, near-passage-inference) | 12 trap shapes by Recall/Rule/Calculate — identity-confusion (acid-source swap, diamond-graphite property flip, scientist-discovery swap) | 13 trap shapes by Recall/Apply/Verify — paired-fact-swap (disease↔pathogen, vitamin↔deficiency, hormone↔gland, RNA-vs-DNA virus, scientist↔discovery) + apply-flips + multi-statement traps | Distractor genre. English: semantic. Chemistry: identity. Biology: paired-fact. Physics: formula misapplication.            |
| **DriftTable `rowLabel`**  | `"Chapter"`                                                                                 | `"Chapter"`                                                                                          | `"Chapter"`                                                                                          | All three flow chapter-rows (vs Maths' principle-rows). Shared.                                          |
| **Related-playbooks renderer** | Generic `RelatedPlaybooks` — call site passes `guidePath="nda-english"` + `items={PLAYBOOKS}`                           | same generic, `guidePath="nda-chemistry"`                                          | same generic, `guidePath="nda-biology"`                                             | Consolidated 2026-06-01 — the 8 former per-guide copies differed only by an import + one href, so they collapsed to one parameterised component (the "don't parameterise until forced" call was reversed once the duplication hit 8). |

**The strand-axis lesson (updated by Biology + Geography + History):** ONLY the Recall slot is universally stable across Template B subjects — and even that can manifest as one strand (Recall) or part of a larger Foundation Recall tier. The SECOND slot is usually Rule but can bend to Apply (Biology + Geography mechanism-tracing) or — when even execution-mode is uniform across chapters — to TIER (Cornerstone for History's dominant Modern India). The THIRD slot is the most variable — Reason (English chain-context), Calculate (Chemistry numeric Mole strand), Verify (Biology + Geography multi-statement evaluation), Quick-Win (History's low-%HARD World History). Picking the strand axis itself (execution mode vs strategic tier) is the highest-leverage decision in a new Template B guide. **History was the first subject where execution-mode strands failed** — multi-statement and date-anchored modes are uniform across all 4 chapters, so they couldn't partition. Tier-style strands (Cornerstone / Foundation Recall / Quick-Win) were the fallback.

#### Template B variants — strand-axis fork (execution-mode vs tier-style)

| Strand axis | Subjects | When to pick | Examples |
|---|---|---|---|
| **Execution-mode strands** | English, Chemistry, Biology, Geography | When execution modes (Recall/Rule/Reason etc.) partition chapters into recognisable clusters — each chapter has a DOMINANT execution mode that distinguishes it from others. | English: Vocab/Idioms = Recall vs RC/Cloze = Reason. Bio: HP/Cell = Recall vs Plant/Reproduction mechanism-tracing = Apply. |
| **Tier-style strands** | History, Polity | When execution modes are bank-wide-UNIFORM (cut across all chapters at similar %), OR the bank is too small + chapter count too low for skill-bucket labels to be more actionable than strategic-priority labels. | History: multi-statement 70 q × 4 ch + date-anchored 61 q × 4 ch are uniform — but Modern India is 47% of bank (vs next chapter 20%), creating a Cornerstone/Foundation/Quick-Win priority axis. Polity: only 90 q · 4 ch · avg 5 q/paper (smallest GAT section); multi-statement partition is partial (4.5–50% across 4 chapters); strategic-priority labels name-drop each chapter directly. |

**Decision probe:** For each candidate execution mode, count `q × chapter_spread` AND look at the per-chapter distribution. If the top mode is 60%+ of one chapter and 20%- of others, execution-mode strands work. If it's 25–45% of every chapter, modes are uniform → fall back to tier-style. **Also drop to tier-style** when the bank is small enough (≤100 q · ≤4 ch) that strategic-priority labels are more actionable than execution-mode labels at this scale — Polity's call.

**Tier-style third-tier inversion** (History vs Polity): tier-style strands have a stable Cornerstone (dominant chapter) + Foundation Recall (recall-heavy mid-chapters) — but the THIRD tier's semantics depend on the smallest chapter's profile. History's World History is small AND easiest (20% HARD, lowest in subject) → **Quick-Win** label. Polity's World Polity is small BUT HARDEST (42% HARD, highest in subject, 50% multi-statement) → **Specialist Wildcard** label (small + high-reward, opposite framing). Don't reach reflexively for "Quick-Win" — verify the third tier's %HARD profile against the rest of the subject first, and relabel when the smallest chapter is the HARDEST.

#### Template B variants — %HARD-handling axis (all 6 B subjects)

A separate axis from strand split: how does each Template B variant deal with its %HARD shape? Three of the four early B subjects were flat (no special handling needed); Geography forced the discovery of a non-flat-%HARD fork; History sharpened the case for the tier-style strand-axis fork (above); Polity added a second tier-style subject with INVERTED third-tier semantics.

| Axis                                  | English                                          | Chemistry                                            | Biology                                             | Geography                                                                                                                                                       | History                                                                                                                                                          | Polity                                                                                                                                              | What decided                                                                                                                                                       |
|---------------------------------------|--------------------------------------------------|------------------------------------------------------|-----------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **%HARD spread**                      | flat (1 ch >15%: Sentence Rearrangement 22%)     | flat (0 ch >12%; max 11.1% Mole)                     | flat (max 7.7% on 13-q chapter; 4 ch at 0% HARD)   | **non-flat** (4 of 7 ch >15%: Climatology 28% / IG Economy 24% / Earth's Structure 20% / Earth in Space 18%)                                                     | **non-flat** (ALL 4 ch >15%: Modern 34% / Medieval 28% / Ancient 27% / World 20%)                                                                              | **borderline non-flat** (only 1 of 4 ch clearly >15%: World 42% outlier; other 3 cluster 13.6 / 15.0 / 16.7%)                                       | The pure-B vs B-variant fork. Geography + History fail the "≤2 chapters >15% HARD" gate; Polity is borderline but the outlier is small + immovable.                  |
| **HARD concentration within chapters**| n/a (flat)                                       | n/a (flat)                                           | n/a (flat)                                          | **diffuse** (Climatology HARD 4-3-3-3-2-1 across 6 subs; Earth's Structure 5-4-3-2-1-0-0 — no chapter has 1–2 subtopics carrying all the HARD)                  | **mixed-immovable** (Modern's top-2 share 63% LOOKS concentrated, but the top sub is Freedom Movement 56 q · 46% of chapter — the chapter's PRIMARY content, cannot be skipped) | **small-and-immovable** (World Polity's HARDs concentrate in UN 60% + Democracy 40% — both 5-q subs that ARE the chapter)                          | The B-variant vs Template-C fork. Concentrated + skippable → Template C (Physics). Diffuse OR concentrated-but-immovable → Template B + non-flat variant.            |
| **%HARD handling on the strategy page**| none needed                                      | none needed                                          | none needed                                         | strand-level calibration — densest-HARD strand (Apply at 23% avg) called out in hero subtitle; no per-chapter posture cards                                      | Cornerstone strand IS the densest-HARD strand (Modern 34% HARD = 1 chapter strand) — strand calibration is implicit in the tier-strand structure                | Specialist Wildcard strand IS the densest-HARD strand (World 42% HARD = 1 chapter strand) — strand calibration is implicit in the tier-strand structure | Strand-level calibration carries the non-flat HARD signal without per-chapter DrillPosture. For History + Polity, the tier-style strand structure already encodes it. |
| **Per-chapter `DrillPosture` overlay?** | no                                              | no                                                   | no                                                  | **no** (despite non-flat %HARD)                                                                                                                                  | **no** (despite non-flat %HARD AND apparent concentration — but the concentrated pool is immovable)                                                              | **no** (only 1 ch clearly non-flat AND the chapter is too small — 12 q — for DrillPosture to add signal)                                            | DrillPosture only earns its place when HARD is BOTH non-flat AND concentrated AND skippable. Geography fails the concentration test; History fails the skippable test; Polity fails the bank-size test. |
| **Strand axis**                       | execution-mode (Recall/Rule/Reason)              | execution-mode (Recall/Rule/Calculate)               | execution-mode (Recall/Apply/Verify)                | execution-mode (Recall/Apply/Verify)                                                                                                                             | **tier-style (Cornerstone/Foundation Recall/Quick-Win)** — execution modes were uniform across chapters, forcing the fallback                                    | **tier-style (Cornerstone/Foundation Recall/Specialist Wildcard)** — execution modes partition only partially (4.5–50%) AND bank is small (90 q · 4 ch); strategic-priority labels more actionable. Third tier INVERTED from History's pattern (smallest chapter is HARDEST not easiest) | When execution modes partition chapters → execution-mode strands. When uniform OR bank is small enough that strategic-priority labels are more actionable → tier-style. |
| **Trends headline framing**           | "Grammar exploded post-2024" — chapter-mix shifts | "Paper has NOT hardened" — explicit negation of Physics's 22× framing | "Paper has NOT hardened — only 4 HARDs across 190 q over 10 yrs" | "Paper has NOT consistently hardened — %HARD bounces 6%→42% with no monotonic trajectory; 2018 + 2025 outlier highs" | "Paper has NOT consistently hardened — %HARD bounces 19%→42%→10%, 2021 peak, 2026-1 lowest" + secondary callout "chapter mix shifted: Modern dominated 2017–20, Ancient surged 2022–24 from ~2 q/yr to 9–10 q/yr" | "Paper has NOT consistently hardened — %HARD bounces 0%→50% with noise inherent to small per-paper samples (avg 5 q/paper)" + secondary callouts "2026 NDA-1 was hardest + most Govt-Structure-heavy 7-of-10; 2021 FR/DPSP outlier 7-of-14"   | Frame the absence/shape of drift explicitly when a reader might import the wrong calibration from a sibling guide. Polity's noise floor is amplified by the small per-paper sample. |

**The %HARD-handling lesson:** Template B's "flat %HARD" gate is the most easily relaxed prerequisite. When %HARD is non-flat but HARD is **diffuse within chapters**, stay in Template B and switch from "no HARD signal" to "strand-level HARD calibration" — don't escalate to Template C. The escalation to C only earns its place when HARD pools are concentrated enough that "drill all EASY subs, skip the HARD ones" is a meaningful piece of strategy.

### Template C — chapter-playbooks + skill-strand + formula compendium (NDA Physics)

Use when **neither A nor B fits cleanly**: cross-chapter lever too thin for principles (max < ~30 q × ≥4 ch — fails A's gate) AND %HARD is mid-spread (3–6 chapters > 15% HARD — too non-flat for B's strategy to capture). NDA Physics: max cross-chapter lever 12 q × 6 ch; 5 of 14 chapters > 15% HARD with HARD concentrated in 1–2 specific subtopics per chapter (Resistance & Circuits 42%, Calorimetry 31%, Thermo Processes 33%, Buoyancy 31%, etc.).

- **Routes:** landing + `/strategy` (skill strands with %HARD-aware DrillPosture overlay) + `/playbooks` (catalog) + `/playbooks/[slug]` (deep dives — **one per chapter**, not per subtopic; physics chapters ARE the natural unit, subtopics too fine at 1–3 q each) + `/formulas` (single-page compendium — NEW page kind) + `/trends` + `/traps`.
- **Tag infra:** NONE (same rationale as B). Per-playbook drills use `chapterIds` + optional `subtopicIds` from `resolveTaxonomy` at request time.
- **Strategy axis:** Skill strand (Recall / Apply / Reason) PLUS per-chapter `DrillPosture` ∈ {drill-all, drill-all-target-hard, cherry-pick-easy-mod, skim}. The posture answers the question B can't: "drill every subtopic of this chapter, or cherry-pick the EASY/MOD subtopics and skip the HARD ones?" — necessary when 5 chapters carry real HARD load.
- **Editorial data shape:** `OVERVIEW + CHAPTER_TABLE + STRATEGY_STRANDS + (per strand) StrandChapter[] with DrillPosture + targetHard + skipSubtopics + PLAYBOOKS (1 per chapter) + per-playbook DETAILS + FORMULA_GROUPS (chapter-grouped) + DRIFT_ROWS + HARD_BY_YEAR + DRIFT_CALLOUTS + TRAP_SHAPES (by strand)`.
- **New components:** `FormulaSheet` (chapter-grouped formula renderer — plain-text + unicode, no KaTeX pipeline → page stays server component). Related-playbooks use the generic `RelatedPlaybooks` component (`guidePath="nda-physics"`).

### Single-page landing (NDA Economics)

Use when the bank is **too thin for any multi-route template** to produce a non-parody strategy. NDA Economics: 24 q · 1 chapter · 3 subtopics · 1.5 q/paper · ~6 marks/paper max — Template A impossible (1 chapter rules out cross-chapter levers), Template B/C `STRATEGY_HEADLINE` reads as parody ("attempt 1.5 of 1.5, target 4 of 6"). Deliberately thinner terminal node, not a fourth template variant.

- **Routes:** landing only. No `/strategy`, no `/playbooks`, no `/trends`, no `/traps`, no `/principles`, no `/reference-tables`, no per-playbook detail pages.
- **Tag infra:** NONE. Cross-chapter axis impossible at 1 chapter.
- **Strategy stance:** named in prose, not in a `STRATEGY_HEADLINE` constant. Cap the subject explicitly ("~6 marks of 600-mark GAT, ~1%"), tell the reader what to do (drill the bank, internalise the one recall anchor, accept the cap), and link out.
- **Editorial data shape:** `OVERVIEW + SUBTOPIC_TABLE + <single-domain recall anchor>` (e.g. `FIVE_YEAR_PLANS` for Economics) `+ POST_PLAN_NOTE` or analogue. No `CHAPTER_TABLE` — 1 chapter doesn't warrant a table.
- **New components:** NONE. The subject's recall anchor renders as an inline `<table>` on the landing — simpler than `CommonCompoundsTable` because there's no per-cluster sub-grouping.
- **Sitemap priority 0.7** (vs 0.8 for full landings) — flags the thinness for crawlers.
- **`questionResources.ts` maps the subject's chapter to the landing** (Template A pattern — single landing, label `"NDA <subject> strategy"`) so `/browse` cards still get a backlink chip.

The bank-thinness gate is the entry point — see "Choosing the template" step 0.

### Template D — theme-prep checklist for short-half-life subjects (NDA Current Affairs)

Use when the bank's CONTENT goes stale (specific facts rarely repeat across years) BUT the bank's SHAPES are stable (recurring question types persist even when the specific facts in them rotate). NDA Current Affairs: 180 q · 8 ch · 31 sub · ~10 q/paper · ~40 max marks — multi-route templates would teach students 2017–2025 trivia and call it strategy. Template D is the honest alternative: name the half-life, name the recurring shapes, point at this year's news.

- **The half-life test** (empirical entry criterion): SQL-probe the bank for "year mentioned in question vs paper year". If 80%+ of explicit-year mentions are within 12 months of paper, half-life is short. Cross-check by counting recurrence of named entities (specific exercises / schemes / award winners) across paper-years — if each named entity appears in at most one paper, fact-memorisation has near-zero forward-prep value.
- **The stable-themes counter-test:** before defaulting to no-ship, count subtopics that appear in 5+ years of papers. If ≥6 subtopics meet that threshold and carry ≥40% of bank, the recurring SHAPES are stable enough to anchor a guide even when the FACTS aren't.
- **Strand axis = recurrence strength** (NEW — not skill, not strategic priority): Anchor themes (5+ year recurrence) → Recurring themes (3–4 year recurrence) → Occasional themes (1–2 year recurrence). Strand sizes are uneven by design; the prep-investment asymmetry is the point.
- **Theme cards** (the load-bearing artefact) — each anchor theme card carries: (a) **question SHAPE** — one sentence describing the recurring stem pattern, sourced from real bank stems (durable); (b) **prep CHECKLIST** — 3–6 fact-CATEGORIES to harvest from this year's news (durable directives, the student fills in this year's specific facts from an external compendium); (c) optional **durable-anchors** callout — the small subset of structural facts that genuinely don't rotate (G7 nations, UN agency HQs, ISRO launch-vehicle classes, etc.); (d) **drill BrowseLink** — practises the SHAPE against the bank's historical instances, NOT the facts.
- **Routes:** landing ONLY. No /strategy, no /playbooks, no /trends, no /traps, no /reference-tables, no per-playbook detail pages. Every section lives on the single page, ordered: hero + half-life directive + chapter table + anchor theme cards + recurring themes table + occasional themes chips + test-day plan + drill CTAs + closing rationale.
- **Tag infra:** NONE. Cross-chapter axis impossible — each chapter is a different subject domain (Defence ≠ Sports ≠ Awards). No `question_<X>_tags` table.
- **New components:** NONE. Anchor theme cards render as inline JSX `<article>` (same pattern as nda-economics's inline FYP `<table>`).
- **Editorial data shape:** `OVERVIEW + CHAPTER_TABLE + ANCHOR_THEMES (8 entries with shape + checklist + durableAnchors? + drillSubtopics) + RECURRING_THEMES (16 entries with oneLineShape + drillSubtopics) + OCCASIONAL_THEMES (7 entries minimal) + HALF_LIFE_NOTICE + TEST_DAY_PLAN`.
- **STRATEGY_HEADLINE stance:** named in the TEST_DAY_PLAN block, not in a copy-pasted constant. CA per-paper count is ~10 q (range 6–14), so ~40 max marks and target ~24 marks at 80% on 7 attempts.
- **Sitemap priority 0.6** (vs 0.7 for Economics, 0.8 for full landings) — flags the unusual template shape for crawlers.
- **`questionResources.ts` maps the subject's chapters to the landing** (Template A / single-page pattern — single landing, label `"NDA <subject> strategy"`).
- **Honest framing in copy is non-negotiable** — the half-life directive callout at the top of the page MUST explicitly say "drill this bank for SHAPE, not for FACTS" and point at an external compendium. Without it, students mistake the bank for the prep resource and memorise historical answers. The "Why this guide is one page" closing section names the alternative we didn't ship ("a 5-section guide would teach you 2019 facts and call it strategy").

### Shared across all guide treatments

- `GuideShell`, `GuideSideNav`, `GuideHero`, `StatBlock`, `BrowseLink`, `PrevNextNav`, `ChapterCard`, `WorkedExampleCard`, `GuideJsonLd` are all generic. The single-page landing treatment skips `GuideShell` + `GuideSideNav` + `PrevNextNav` (no sub-routes to navigate) but still uses `GuideHero` + `StatBlock` + `BrowseLink` + `GuideJsonLd`. Threading new behaviour through them as props (the 2026-05-18 ship added `landingHref` on Shell/SideNav, `rowLabel` on DriftTable) is the right pattern. Don't fork components per guide.
- `_data/*.ts` modules and the route topology are subject-specific. Diverge there.
- `/sitemap.ts`, Footer link, and `/browse` Hero pill plumbing additions are mandatory per new guide.
- Cross-link banner from the parent subject's landing (e.g. nda-maths landing → nda-english) is mandatory to discover the new guide.
- **Operations — before shipping `STRATEGY_HEADLINE`:** verify per-paper q-count against the live bank. Each PART B section has its own q-count (Physics ~25, Chemistry ~15, Biology ~10–11); copy-pasting between sibling guides has shipped wrong numbers (Chem + Bio caught 2026-05-18). The full SQL probe + 7-step recalibration recipe lives in [[per-paper-q-counts]] memory. `tests/guide-strategy-headline.test.ts` codifies the invariants — `paperQ × marksPerCorrect === totalMarks` (unit) and `paperQ within ±30% of live-bank avg q/paper` (DB integration). Run `npm test -- guide-strategy-headline` before commit.

### Choosing the template — the analysis script

Before proposing a new guide, run this analysis (SQL probes documented in [[english-guide-structure-diverges]] memory):

0. **Bank-thinness gate.** If avg q/paper < 2 OR total bank < ~50 q OR chapters = 1, no Template A/B/C will produce a non-parody `STRATEGY_HEADLINE`. **Ship a single-page landing instead** (see "Single-page landing" section above). NDA Economics (24 q · 1 ch · 1.5 q/paper · ~6 max marks) is the precedent; NDA Polity (90 q · 4 ch · 5 q/paper · 20 max marks) is the smallest *multi-route* guide and clears the floor. Below that floor, multi-route padding hurts more than it helps.
0a. **Content half-life gate** (run AFTER the thinness gate clears). SQL-probe `regexp_matches(text, '(20[12][0-9])', 'g')` against the bank and compute `(year_mentioned - paper_year)`. If 80%+ of explicit-year mentions are within 12 months of paper AND specific named entities (exercises / schemes / awards) almost never recur across paper-years, the content half-life is too short for a multi-route fact-memorise guide. Counter-test: if ≥6 subtopics appear in 5+ years (stable SHAPES even when FACTS rotate), **ship Template D — Theme-Prep Checklist** (see section above). NDA Current Affairs (180 q · 8 ch · 10 q/paper · 90% within-12-months mention rate · ~zero entity recurrence · 8 anchor themes at 5+ year recurrence) is the precedent.
1. **Chapter mix + %HARD per chapter.** Count chapters at each %HARD tier (>15%, >20%, >25%). Helps separate flat (Template B) from non-flat (A or C).
2. **Cross-chapter principle candidates.** Run keyword surveys for plausible horizontal levers. If the top candidate spans ≥4 chapters AND ≥40 q, **principles axis lives — Template A**. If it spans <4 chapters AND <30 q, **principles axis dead — go to step 3**.
3. **Pick B vs C by %HARD spread + concentration.** Three branches:
   - **≤2 chapters > 15% HARD** → flat — **pure Template B** (English / Chemistry / Biology shapes).
   - **3+ chapters > 15% HARD AND HARD is concentrated in 1–2 subtopics per chapter** (cherry-pick pattern lives) → **Template C** (chapter-playbooks + skill-strand + per-chapter DrillPosture overlay + flat-list page like formulas).
   - **3+ chapters > 15% HARD BUT HARD is diffuse within chapters OR concentrated-but-immovable** (no clean cherry-pick) → **Template B + non-flat %HARD variant** (Geography shape — Template B routes/granularity/subject-specific page, but strand-level %HARD calibration in place of DrillPosture).
4. **Subject-specific page.** Most guides have one (vocab-families for English, formulas for Physics, common-compounds for Chemistry, reference-tables for Biology + Geography, timeline-and-pairs for History). If the subject has a flat-list artefact that doesn't fit a playbook (formula sheet, periodic table chunk, named-acts list, name↔formula reference, named-fact pair table, named-place reference, chronology timeline), it's a candidate. Single-domain artefacts → `CommonCompoundsTable`; multi-domain artefacts → the generic `ReferenceTables` component (pass `clusters` + `guidePath`; per-cluster column headers).
5. **Strand axis — execution-mode vs tier-style.** First test EXECUTION-MODE strands (Template B default, what English/Chem/Bio/Geo do). For each candidate mode (recall / rule / apply / verify / calculate / reason / multi-statement / date-anchored), measure `q × chapter_spread` AND the per-chapter distribution. If a mode dominates ONE chapter cluster (60%+ of one chapter, 20%- of others), execution-mode strands work. If modes are roughly uniform across all chapters (25–45% of every chapter), execution mode CAN'T partition → fall back to **TIER-STYLE strands** (Cornerstone / Foundation Recall / Quick-Win — History's pattern). For execution-mode strands, the Recall slot is universally stable. The SECOND slot is usually Rule but can bend to Apply (Bio + Geo) when no rule-application lever exists. The THIRD slot is most variable — Reason (English), Calculate (Chem Mole), Verify (Bio + Geo multi-statement). For tier-style, the slot semantics are different: Cornerstone = dominant chapter, Foundation Recall = recall-heavy mid-chapters, Quick-Win = low-%HARD / smallest chapter.
6. **Subject's natural seams.** If steps 0–5 don't fit (i.e. bank clears the thinness gate but none of A / B-pure / B-variants / C match), propose a new template (and write a memory documenting it).

See `feedback_english_guide_structure_diverges.md` memory for the full "trust the bank's shape" rationale + the 2026-05-18 ship analyses that produced Template B (English), Template C (Physics), Template B variants (Chemistry with Recall/Rule/Calculate + `/common-compounds`; Biology with Recall/Apply/Verify + `/reference-tables`), Template B + non-flat %HARD variant (Geography with Recall/Apply/Verify + strand-level %HARD calibration + `/reference-tables`), and the 2026-05-19 Template B + tier-style strands variants (History with Cornerstone/Foundation Recall/Quick-Win + `/timeline-and-pairs`; Polity with Cornerstone/Foundation Recall/**Specialist Wildcard** + `/reference-tables` — second use of tier-style with the THIRD tier INVERTED because Polity's smallest chapter is the HARDEST, not the easiest).

## Decisions log

Why behind architectural pivots — saves future-you from "why didn't we just…?".

**Convention:** topical intros (like "Foundations" below) sit at the top. Dated entries are bucketed by month — `### YYYY-MM` per month. When a new month begins, add a new `### YYYY-MM` sub-section above the prior one. **Within a month, entries are ordered newest-first (reverse chronological top-down): a new entry goes at the TOP of its month's block.** Net result: the most recent entry anywhere in the log is the first dated entry a reader sees after the foundations. This lets a reader collapse historical months mentally and focus on what's recent.

### Foundations (M1–M3, 2026-05-08)

The load-bearing choices made during the initial scaffold. Every later phase rests on these — re-litigate only with explicit reason.

- **No Prisma.** Supabase RLS is the security boundary; Prisma uses a single privileged DB user and bypasses RLS by default. Forcing per-request `SET LOCAL` JWT injection fights the tide. `@supabase/supabase-js` carries the user's JWT through naturally.
- **RLS helpers in `private` schema.** PostgREST exposes any `public.*` function as a `/rest/v1/rpc/<name>` endpoint. Supabase advisor flagged `current_user_org_id()` / `current_user_is_admin()` as callable by `anon` and `authenticated`. Functionally safe (returns `null`/`false` for anon) but defence-in-depth — moved to a schema PostgREST doesn't expose.
- **OMML via marker-and-post-process.** The `docx` library's `ImportedXmlComponent.fromXmlString` wraps imported XML in an `<undefined>` element that Word refuses to open. We render placeholder text where math should go, then patch `word/document.xml` after `Packer.toBuffer`. Sidesteps the library quirk; works for arbitrary OMML.
- **Word export = ZIP of two files.** First version was a single .docx with a 3-way layout toggle. User asked to split: a Question Paper distributable without leaking answers, and a separate Answer Key. One ZIP keeps the bundle obvious. **Superseded 2026-05-13** — ZIP killed for mobile UX; route now returns one .docx per request via a required `kind` selector.
- **Hard 200/export cap, no auto-split.** Vercel function timeout is the binding constraint. A clear error ("narrow filters") ships faster than a zip-of-zips. Revisit if a teacher genuinely needs 500 questions in one paper.
- **Chapters/subtopics auto-create, subjects don't.** Subjects are top-level taxonomy curated centrally; auto-creating "Phyiscs" from a typo would corrupt the canonical list. Chapters/subtopics are per-upload working data — small typo cost, and a merge tool can clean up later.

### 2026-06

- **2026-06-07 (latest) — NDA Maths Limits & Continuity /notes chapter (15th notes chapter, 12th NDA Maths) built end-to-end autonomously.** 3 subtopics · 12 concepts (1 foundation + 1 reference) · 81 q · 100% concept-tag coverage. Reorder-only (Template-A guide refs all 3 subtopic names in principles.ts) but a genuine **reorder**: bank lists Continuity first; pedagogy puts limits before continuity, so teaching order = Limit-Evaluation → One-Sided/GIF/Modulus → Continuity. Concepts from reading all 11 HARD + 37 MODERATE solutions: evaluation got 5 (foundations, 0/0-algebraic, standard-limits reference, L'Hôpital, 1^∞), one-sided 3, continuity 4 (definition, find-parameters, discontinuity-types, continuity-vs-differentiability — the last cross-links to the Differentiation chapter's differentiability concepts). `lim-` slugs; 2 static 2D SVGs (3-panel discontinuity-types incl. sin(1/x) oscillation + one-sided-approach jump). notes-lint #5 caught 1 self-check reused from its featured PYQ → rewritten. Gates green: notes:lint 0/0 (71 notes), notes:latex OK, coverage clean (Greek/`\tan`/`[d/dx]` cross-chapter notation only), prepush, notes:order. **Completes the calculus prerequisite base** (Differentiation + Limits + Trig) — the cross-topic prerequisite-links idea (ROADMAP Phase 1) now has real lesson targets.

- **2026-06-07 (latest) — NDA Maths Trigonometric Identities /notes chapter (14th notes chapter, 11th NDA Maths) built end-to-end autonomously.** The bank's **biggest chapter** — 5 subtopics · 21 concepts (1 foundation + 2 reference) · 138 q · 47 HARD (most in the subject) · 100% concept-tag coverage. Reorder-only (the Template-A guide refs all 5 subtopic names in principles.ts/strategy.ts, so no Phase-D rename); pedagogy-first teaching order = Values/Quadrants → Compound → Multiple/Half-Angle → Product-Sum → Max/Min (the bank lists them by q-count; Specific Values is foundational and moved first). Concept design driven by reading **all 47 HARD + all 58 MODERATE solutions** (the HARD+MODERATE discipline) — compound-angle got 5 concepts (sin-cos, tan, applications, identity-manipulation, conditional-roots) because it's the chapter's dominant lever; the others 3–5 each. `trig-` slugs; 2 static 2D SVGs (ASTC quadrant wheel + a·sinx+b·cosx amplitude). 1 PYQ re-filed (cos⁻¹(cos x) was a value question; kept in Values). **Process note:** notes-lint check #5 caught **6 self-checks that reused their featured PYQ's exact problem** (I'd seeded the self-check from the featured question) — all rewritten to distinct problems; reconfirms authoring the worked example/self-check FIRST then attaching a *different* PYQ ([[notes-concept-content-alignment]]). Gates green: notes:lint 0/0 (68 notes), notes:latex OK, coverage clean (only Greek/`\prod`/`\sec` notation flags), prepush, notes:order (no name-join issues). Built autonomously on one instruction.

- **2026-06-07 (latest) — NDA Maths Differentiation /notes chapter (13th notes chapter, 10th NDA Maths) built reorder-only.** 3 subtopics · 20 concepts (1 foundation + 1 reference) · 85 q · 100% concept-tag coverage. The deciding Step-0 check: the nda-maths Template-A guide references the first two subtopic names verbatim in `principles.ts` (the chain-rule/log drill → "Differentiation Techniques — Chain Rule, Logarithmic, Composite Functions"; the parametric/implicit drill → "Parametric, Implicit, and Higher-Order Derivatives") + CHAPTER_TABLE/strategy/traps prose — so a Phase-D rename would silently break those drill CTAs ([[shipped-chapter-rename-downstream-sync]]); and the 3 DB subtopics already form a coherent teaching arc (Core Techniques → Parametric/Implicit/Higher → Differentiability), so **reorder-only as-is**, friendly display titles, no guide sync. Teaching arc: how-to-differentiate (first principles, standard-derivatives reference table, product/quotient, chain, logarithmic, derivative-wrt-another-function, simplify-inverse-trig-first, functional-equation) → advanced forms (implicit, log-implicit, parametric, higher-order, d²x/dy² reciprocal identity, prove-an-ODE) → differentiability (differentiable⇒continuous foundation, LHD=RHD, modulus corners + x|x| trap, greatest-integer, parameter problems, limit-definition at awkward points). **3 static 2D SVGs** (diff-tangent-slope, diff-modulus-corner, diff-piecewise-join) — graphable but not spatial, so all 2D per [[notes-diagram-decision-rule]]. `diff-` slugs, `\ln` convention. The standard-derivatives **reference** concept is bank-tagged (5 q) — a reference concept must carry questions, not be a no-PYQ primitive ([[notes-self-sufficient-template]]); the differentiable⇒continuous concept is the **foundation** (no featured PYQ — the "derivative-may-not-exist" statement PYQs sit in the Core Techniques subtopic, not Differentiability). Re-filed 1 PYQ (`55c258cc`, cos⁻¹(cos x) slope) from Parametric → Core Techniques (an inverse-trig-simplify question mis-filed). HARD **and** MODERATE solutions both read during Step 0 per the updated workflow. Gates green: notes:lint 0/0 (63 notes), notes:latex OK, notes:coverage clean (only `\sec`/`\infty` notation flags), prepush, notes:order (no name-join issues).

- **2026-06-07 (latest) — MODERATE-solution sweep across all 12 noted chapters: 7 gaps filled (the HARD-only sweep had a real blind spot).** User challenge: "why only HARD?" The data justified it — **3 of the prior sweep's 5 gaps were MODERATE, not HARD** (variance `(n²−1)/12`, monotonic⇒one-one, both MODERATE; found only by luck/probe, not my HARD read). So I ran a MODERATE read across all 12 chapters (delegated to 4 parallel subagents — each read its chapters' notes concepts + queried MODERATE solutions + reported only genuine untaught techniques, keeping the 50k-char solution dumps out of the main context). It found **7 gaps** where HARD-only had found ~none: **(1)** Probability **geometric (area/length-ratio) probability** — a whole missing model (notes defined probability only by discrete counting); new `geometric-probability` concept + retagged 2 q. **(2)** 3D **line-of-intersection direction = n₁×n₂** (3 q across 3 subtopics — DC&Ratios `perpendicular-parallel-lines`, Straight-Line `line-equation-forms`, Plane `plane-through-intersection`); taught in all three + retagged. **(3)** Sequence `gp-sum-infinite` **factual error** (claimed continued fractions "reduce to" the GP formula — wrong) fixed + new `self-referential-continued-fractions` concept. **(4)** Matrices **adj(Aᵀ)=(adjA)ᵀ** → `adjoint-properties`. **(5)** Sequence **common terms of two APs = AP with d=LCM** → new concept. **(6)** Statistics **MD≈⅘SD** empirical relation → `sum-of-deviations-empirical`. **(7)** Matrices **Hermitian/skew-Hermitian** → `special-matrix-types` catalog. Clean (no MODERATE gaps): Functions, Vectors, NDA-IndefInt, Binomial, Sound, E&M, MHT-IndefInt. All retags were concept_slug-only within the same subtopic (no rehash). notes:lint/latex 0/0. **Lesson → workflow updated:** read **HARD + MODERATE** solutions (MODERATE is the largest bucket and where named shortcuts hide); EASY stays probe-only. See [[notes-coverage-probe]] + the step-0 workflow note.

- **2026-06-07 (latest) — #2 coverage sweep across the remaining 9 noted chapters: 8 clean, 1 thin gap filled.** Ran the probe + manual HARD-solution read on Vectors, Probability, 3D Geometry, Sequence & Series, NDA Indefinite Integration, Binomial, NDA Sound, Electricity & Magnetism, MHT-CET Indefinite Integration. **8 of 9 clean** — every HARD technique maps to a concept (verified non-gaps incl. alien cofactors, infinite-ladder self-similar `R∞`, Vandermonde, AGP/HP/Vieta, all integration-substitution families). Only **Probability** had a real gap: the 3-event **"at least two of A,B,C" = ΣP(pairwise) − 2P(A∩B∩C)** identity (and "exactly two" = −3) wasn't taught (only the 2-event "exactly one of A,B" was). Added both to the `probability-identity-statements` concept (bounds-on-probability) + practice + a coefficient trap; re-filed the PYQ `499c5d5e` from its mis-fit "Independent Events" subtopic → "Bounds on Probability" and retagged it (it's an identity-statement question, not an independence one; subtopic_id not in content_hash → no rehash). Deferred the "coin tossed till two heads → geometric series" one-off (user: skip). **Sweep lesson:** the probe macro channel was near-useless here (all notation/Greek noise — `\perp`, `\Omega`, `\ln`-vs-`\log`); every real finding this sweep + the prior two came from the **manual HARD read**, which also caught a self-inflicted false alarm (an unfiltered query pulled MHT-CET Sound, a non-noted chapter, as "untagged Sound"). The token probe finds absent *formulas*; semantic/technique coverage still needs the human read. notes:lint/latex 0/0.

- **2026-06-07 (latest) — Matrices notes revisited via #2: 2 thin technique gaps filled.** Read every HARD solution + the probe. Chapter is very well covered (alien cofactors `ΣaᵢⱼCₖⱼ=0`, circulant `a³+b³+c³−3abc`, factor theorem, Vandermonde, cube-roots, matrix polynomials all present). Only two techniques (1 HARD PYQ each) were genuinely untaught: **(a)** binomial-coefficient determinants via **Pascal's identity** (`C₃→C₁+C₂−C₃`) — added a `binomial-coefficient-determinants` concept to `determinants-evaluation-properties` + retagged `d4a4b8f6` off its mis-fit `sum-of-determinants` tag; **(b)** the **matrix-determinant lemma** `det(I+AAᵀ)=1+AᵀA` — added a bullet + practice + trap to the existing `det-products-scalar-powers` concept (too niche for its own, sits where `det(AAᵀ)` already lives; `a013b015` stays tagged there). notes:lint/latex 0/0. (The probe's macro channel didn't surface these — they're the semantic/cross-notation class only the manual HARD-solution read catches, exactly #2's job.)

- **2026-06-07 (latest) — Functions notes revisited via #2 (author-from-solutions); one gap filled + probe sharpened.** Ran `npm run notes:coverage nda-maths functions` + read every flagged solution. Chapter held up — 21 concepts cover the techniques; the only genuine miss: the criterion **"a strictly monotonic function is one-one"** (used in `99f007c4`'s bijective-on-(−1,1) solution: `x|x|` strictly increasing ⇒ bijection; `cos(πx)` non-monotone ⇒ not one-one/onto) was nowhere in the notes. Added it to `funcs-one-one-onto-bijective` as a definition bullet (framed **calculus-free** — `x|x|` is `−x²` then `x²`, both increasing) + a practice set + a non-monotone trap (`99f007c4` already tagged here). Other flags triaged to non-gaps: `\ln`/`\cos` (notation / incidental — log domain + ln-odd already covered), and `[d/dx]` (cross-chapter calculus: `f'(1)`, ln-increasing-via-derivative — Functions notes correctly don't teach differentiation). **Probe fix (B):** `DERIV_RE` now requires the Leibniz denominator to also start with `d` (`\frac{d…}{d…}`), so a fraction with a variable numerator `d` (`\frac{d}{x}`) is no longer a false `[d/dx]` — verified the real determinant derivative (`\frac{d^2}{dx^2}`) is still detected (Matrices det-properties stays covered). notes:lint/latex 0/0.

- **2026-06-07 (latest) — Matrices notes: added "Differentiating a Determinant" concept + cleaned its one PYQ.** User asked whether the bank has derivative-of-determinant / matrix-derivative questions. Probe + SQL: exactly ONE (`1cb6a8fe`, NDA 2026 HARD — `d²/dx²|...|` at x=0); zero matrix-calculus questions (not an NDA topic). The row-by-row differentiation technique (`d/dx` of a determinant = SUM of determinants each with one row differentiated; constant rows drop out) was NOT in the notes → genuine thin gap. Added a `differentiating-a-determinant` concept to `determinants-evaluation-properties.ts` (rule + 2×2 worked/self-check + the PYQ featured), retagged `1cb6a8fe` off its mis-fit `singular-and-determinant-equations` tag, and rewrote its hand-wavy solution to the clean method (at x=0 the twice-differentiated row 1 becomes `[6,−1,0]` = row 2 → det 0; solution not in `content_hash`, no rehash). **Probe footnote:** the coverage probe only weakly pointed here (a `\sin`/`\cos` flag) because the giveaway macro `\frac{d…}` is on the common-macro allowlist — a known blind spot when the signal macro is common. notes:lint/latex 0/0.

- **2026-06-07 (latest) — Tooling (`npm run gate` + `npm run stats`) + Statistics notes gap-fill (`special-case-variance`).** Three small items. **(1) `scripts/gate.sh` / `npm run gate [target]`** — runs a gate (default prepush) and prints a `GATE_RESULT: PASS/FAIL` sentinel as its LAST line, so the result survives `| tail`/grep even though a pipe masks the exit code (the trap that produced a false-green push earlier this week — [[gate-exit-code-masked-by-tail]]). **(2) `scripts/stats.ts` / `npm run stats`** — read-only reporter (head-count queries, no 1000-row cap) for the header lines that drift: live PUBLIC bank size by exam/subject (confirmed 10,264), taxonomy counts, /notes chapter (12) + diagram (75) counts. It surfaced that the header's "179 ch · 671 sub" is the *cleaned-13-subjects* scope while the live DB is **258 ch · 934 sub** (the gap is JEE's ~79 ch / ~263 sub auto-created across Papers 1–10) — relabel, don't "correct". **(3) Statistics /notes gap-fill** — a user-flagged export showed a solution using `Var(first n natural numbers) = (n²−1)/12`, which the notes didn't teach (the linear-transformation half WAS taught; the natural-numbers-variance half was a genuine gap — contrast the earlier `det(B⁻¹)` case which was already present). Added a `special-case-variance` concept to `dispersion.ts` (mirrors the existing `special-case-means` in central-tendency): `σ²(1..n)=(n²−1)/12`, `σ²(AP)=d²(n²−1)/12`; retagged the 2 affected PYQs (dual-tag, kept existing). A general "notes-coverage probe" was then BUILT (`scripts/notes-coverage.ts` / `npm run notes:coverage [subjectRoute] [chapterSlug]`, default nda-maths statistics) — per-subtopic diff of math in PUBLIC solutions vs notes bodies (macros + operand-abstracted structural fragments; the macro channel is high-signal, fragments are noisy generic-arithmetic). Tuning gotcha: notes `formula.latex` + reference-table cells are raw LaTeX OUTSIDE `\(...\)`, so the notes-vocab builder must add them explicitly or it false-flags (a `\prod`/GM false positive caught this). Run on Statistics → macro channel clean, no new gaps (validated it would've flagged the `(n²−1)/12` class pre-fill); the other 11 noted chapters remain to run. Triage-aid, not a gate. notes:lint/latex 0/0.

- **2026-06-07 — Word-export fix: math accents (\bar, \hat, \vec, \dot) + \overline now render properly.** User screenshot: the mean bar in an exported `.docx` showed as a tiny detached mark floating above-right of `x`, not a bar. Root cause (verified by dumping the OMML): `mml2omml` maps temml's single-base accents (`\bar`/`\hat`/`\vec`/`\dot`/`\tilde`, all `<mover>` in MathML) to an over-LIMIT **`<m:limUpp>`** — Word draws a limit at reduced size, detached above the base. And `\overline` maps to a top-only **`<m:borderBox>`** which renders invisibly under our injected math defaults (the same failure as the documented `\underline` borderBox). Both are now repaired by a new post-process **`wrapAccents`** in [ommlBuilder.ts](src/lib/export/ommlBuilder.ts), chained after `wrapMatrixDelimiters` (same render-only post-process pattern as the matrix-delimiter + underline fixes — **no DB/bank writes**; existing papers regenerate correctly on next download): a `<m:limUpp>` whose `<m:lim>` is exactly one recognised accent char → `<m:acc>` with the matching COMBINING char (overline U+0305, circumflex U+0302, arrow U+20D7, dot U+0307, tilde U+0303); a top-only `<m:borderBox>` (i.e. `\overline`) → `<m:bar pos=top>`. (A bottom-only borderBox — `\underline` in math — is deliberately LEFT as-is: the `\underline` text bypass + the preserved borderBox fallback are a separate intentional design — see `tests/underline-roundtrip.test.ts`.) A `<m:limUpp>` with any other lim content (real `\lim`/`\overset`/`\overbrace`) and `\overrightarrow` (already a correct stretchy `<m:groupChr>`) are left untouched. Scope was broad — PUBLIC field-occurrences: `\bar` 127, `\hat` 485, `\vec` 625, `\overline` 46, `\dot` 4 (`\overrightarrow` 125 already fine). +8 TDD cases in `tests/omml-builder.test.ts` (38 total in that file). Browser/KaTeX rendering was never affected — export-only.

- **2026-06-06 — Bank-wide control-char LaTeX repair + /notes run-on-definition bulleting.** Two readability fixes off user review of the new Functions notes. **(1) Bank corruption (DB, no code):** a rendered card showed `KaTeX parse error: Expected '\right'` — root cause was an ingestion step that processed string escapes, turning LaTeX commands whose backslash is followed by `r`/`t` into **control characters**: `\right`/`\rho`/`\rightarrow` → `CR`, `\text`/`\times`/`\tan`/`\textit` → `TAB`. A bank-wide scan (raw SQL — NOT supabase-js, which silently 1000-row-caps; the dry-run that missed it is the textbook PostgREST 1000-row-cap trap — see "Recurring pitfalls") found the true scope is **small**: 6 CR-command + 26 TAB-command field/option occurrences; backspace/formfeed 0 (so `\frac` was safe), and the 256 CR rows that are just benign CRLF line-endings + all LF-before-letter cases (real `\n\n` newlines / GFM tables) were correctly left alone. Fixed with `regexp_replace(col, chr(13)||'([a-zA-Z])', '\\r\1')` + the `chr(9)→\t` analogue across text/context/solution + options. **content_hash recomputed for the 5 rows whose text/option changed** (those fields are in the hash via `norm` which collapses whitespace; context/solution are not) using the real `contentHash` helper by explicit-id fetch. Verified 0 remaining. **(2) /notes run-on definitions (TS):** 9 `definition` fields that packed 4–7 inline-math identities into one semicolon-joined prose line (the "wall" the user flagged) → converted to `- ` bulleted lists with bold labels (RichText renders `<ul>`, per the existing convention for property enumerations). Concepts: Matrices `det-products-scalar-powers` + `inverse-properties` + `transpose-rules`; Vectors `cross-product-algebra-and-properties` + `perpendicularity-test`; Probability `min-max-of-combined-probabilities` + `probability-identity-statements`; Matrices `adjoint-properties`; MHT-CET `identity-simplification`. A scan confirmed the bold-led / numbered-procedure definitions (3D Geometry etc.) were already fine and left untouched. notes:latex + notes:lint 0/0; markup-only (TDD-exempt per [[tdd-calibration]]).

- **2026-06-06 — NDA Maths Functions /notes chapter (12th notes chapter, 9th NDA Maths) built reorder-only.** 5 subtopics · 21 concepts (2 foundations) · 109 q · 100% concept-tag coverage (115 tags incl. 6 intended dual-tags). Bank shape: Domain/Range 48 (mostly EASY) · Composition/Inverse 28 (the HARD pocket, 7 of 11 chapter HARDs) · Functional Equations 18 · Definition/Classification 8 · Greatest Integer 7. **Reorder-only, NO Phase-D rename/merge** — the deciding constraint (checked first): the shipped nda-maths Template-A guide references all four named subtopics verbatim in `principles.ts` (the "Sets, Functions & Relations" principle drills) + `strategy.ts` (Tier-B `mustDrill`/`skipSubtopics`) + CHAPTER_TABLE, so any rename would silently break those drill CTAs ([[shipped-chapter-rename-downstream-sync]]); the 5 DB subtopics also already form a coherent teaching arc, so display titles + `subtopicOrder` sufficed. Teaching order: Definition/Classification → Domain/Range → Composition/Inverse → Greatest Integer → Functional Equations (the core `mustDrill` pair early, niche topics last). **6 static 2D SVGs** (mapping-diagram for injective/surjective/bijective · domain-range-graph · even-odd-symmetry · composition-machine · inverse-reflection-line · greatest-integer-staircase) — Functions is graphable but not spatial, so all static, none 3-D, per [[notes-diagram-decision-rule]]. `funcs-` prefixed subtopic slugs for global uniqueness. Set-sibling co-location handled, incl. dual-tagging the 3 cross-concept sets (domain+range, range+periodicity, composition+inverse) identically across siblings. Gates green: notes:lint 0/0 (incl. PYQ-dup check #5 + 100% tag coverage), notes:latex OK, notes:order populated the 5 subtopics' order_index. Built end-to-end in one session following the Notes editorial workflow.

- **2026-06-06 — /browse subtopics + chapter-scoped questions sort by TEACHING ORDER (migration 0029), for the ~11 noted chapters only.** User flagged that the /browse filter sidebar listed a chapter's subtopics by question-count desc (Matrices & Determinants showed "Determinant Properties (59)" first), not in the order the chapter is taught. Root cause: `mergeAndSortFacets` ([src/lib/questions/facets.ts](src/lib/questions/facets.ts)) sorted purely by count, and **subtopics had no order column** (chapters have `order_index`; subtopics never did). Teaching order existed only in each /notes chapter's `subtopicOrder`, which /browse never read. **Shipped, scoped to "notes-only, rest unchanged"** (user's explicit call):
  - **Migration 0029** adds `subtopics.order_index int` — **NULLABLE, default NULL** (deliberately NOT `0` like chapters): NULL = "no teaching order" and sorts LAST in both the JS facet sort (`?? Infinity`) and the SQL question sort (`nullsFirst:false`). A `0` default would sort an un-ordered subtopic ahead of a position-1 one. So the ~168 chapters outside notes keep byte-identical count-desc behaviour.
  - **`scripts/sync-subtopic-order.ts`** (`npm run notes:order`, idempotent, `--dry`) derives order_index from `NOTES_CHAPTERS` → each chapter's `subtopicOrder` (position → 1-based index), joining notes `subtopicName` → DB subtopic by name. LOUD on the name-join caveat ([[shipped-chapter-rename-downstream-sync]]): reports any notes slug with no live DB subtopic AND any DB subtopic left unordered. First run: 11 chapters, 55 subtopics ordered, 0 issues. Added as the post-ship step in the Notes editorial workflow (also re-syncs after a Phase-D re-carve).
  - **Sidebar:** `mergeAndSortFacets` sort precedence is now `orderIndex asc (null last) → count desc → name`. Subtopics pass `orderIndex`; **chapters pass nothing** → all tie on the order term → identical to before (chapter ordering deliberately untouched, honouring "rest as-is"). TDD: 4 new cases in `tests/facets.test.ts`.
  - **Questions:** when a chapter is filtered, `query.ts` leads the sort with the subtopic's teaching order, then the existing `created_at desc → source_row → id`. The unfiltered /browse default (newest-first) is untouched (a global subtopic-order sort would meaninglessly interleave unrelated chapters). DB-integration assert in `tests/query-order.test.ts` (order_index `[1,2,null]`, proving NULLs-last).
  - **PostgREST gotcha (cost ~30 min, worth banking):** ordering the PARENT result by an embedded to-one column does NOT work via supabase-js's `.order(col, { referencedTable })` — that orders the embedded child *collection*, a no-op for a to-one embed (probe returned unsorted). The working form is to pass the **embed-alias column string** `.order("subtopic(order_index)", { ascending, nullsFirst:false })` with NO `referencedTable` — supabase-js forwards it as PostgREST's top-level `order=subtopic(order_index)` spec (uses the SELECT *alias* `subtopic`, not the table name `subtopics`). Verified live: `1×33 2×22 3×59 4×20 5×28 6×8` = exact Matrices teaching order. See [[postgrest-order-by-embedded-column]].

- **2026-06-04 — /notes mastery-check + /guide worked-example cards now render GFM pipe-tables.** Bug (user-reported, screenshot): the `SubtopicMasteryCheckpoint` + `/guide` worked-example cards rendered bank passage/stem content through plain `KatexRenderer`, so a GFM pipe-table in a question's `context`/`text` (the Probability Q112 contingency table, surfaced on a CET notes mastery card) showed as raw `| … |` markup. Fix: `WorkedExampleCard` ([src/app/guide/_components/WorkedExampleCard.tsx](src/app/guide/_components/WorkedExampleCard.tsx)) swaps `KatexRenderer` → the table-aware `BlockText` for `example.context` + `example.text` ONLY — mirroring `/browse`; options + solution stay on `KatexRenderer` (the v1 no-tables-in-options contract). `BlockText` fast-paths non-table text to byte-identical `KatexRenderer` output, so the 13 linearized Statistics questions are unaffected. Pure render-wiring (table parsing already TDD'd in `parseTableBlocks`, so no new test per [[tdd-calibration]]); typecheck + full prepush green, merged to `main` (`028836a`). **Surface lesson:** bank `text`/`context` now flows through `BlockText` on FOUR surfaces — /browse expanded card, editor Preview, docx export, and the /notes+/guide `WorkedExampleCard`; a future content-render capability on those fields must cover all four. (A separate idea this session — printable per-chapter `/notes` PDF booklets via a "Download PDF" button — was scoped, sampled (51-pp MHT-CET Indefinite Integration preview), and **parked** in ROADMAP.md, not built.)

- **2026-06-04 — MHT-CET Indefinite Integration /notes reworked for flow + coverage (user: the chapter "doesn't progress logically" and "doesn't cover all the concepts").** Both defects confirmed against the Std-12 board chapter + a fresh 121-PYQ bank audit. **(1) Flow:** the page rendered Trig II BEFORE Rational & Partial Fractions, so the chapter's hardest pocket (Weierstrass / divide-by-cos²) needed the completing-the-square + arctan/arcsin/log machinery taught only in the LATER subtopic — and the `subtopicOrder` array silently CONTRADICTED the intro prose (which already numbered Rational/PF as movement 4, Trig II as 5). Fixed by swapping `subtopicOrder` alone (no slug/name change → zero Phase-D / guide-sync cost). **(2) Coverage: +6 concepts, all bank-validated** — Foundations `linear-argument-rule`; Substitution `reciprocal-power-substitution` (split off the `u-substitution-basics` catch-all to hold the signature HARD take-out-power / `t=x±k/x` family, 7 q incl. the 6× near-dup `x²(x⁴+1)^{3/4}`); Trig I `standard-trig-integrals` (∫tan/cot/sec/cosec, foundation) + `inverse-trig-argument`; Trig II `product-of-sines` + `trig-to-partial-fraction` (carved out of an overloaded `weierstrass` tag that was absorbing 3 distinct techniques). Enhancements: `divide-by-cos²` gained the `tan x = t` double-angle route; `standard-quadratic-forms` gained the `sec⁻¹` + `∫f'/√f` forms. **DB: ~13 `question_concept_tags` re-tagged — `concept_slug` only, `subtopic_slug` unchanged** — so the rework is migration-cheap (no PK invalidation, no guide/browse cross-ref breakage); distribution rebalanced (`u-sub-basics` 20→16, `weierstrass` 7→2; every bank-anchored concept keeps ≥1 tag). **Lesson:** a `reference`-variant concept must be bank-tagged — a no-PYQ teaching primitive (the four ∫trig results have no pure PYQ) must be a `formula`-variant FOUNDATION (no `pyqExampleId` → lint-exempt), NOT a reference table (notes-lint warns: reference = the flat-fact-recall bucket, never a primitive). Gates: notes:lint 0/0, notes:latex OK, prepush green (1091 tests). Ordering + overloaded-tag + shipped-chapter-review lessons → [[notes-structure-pedagogy-first]]; reference-must-be-tagged reconfirmed → [[notes-self-sufficient-template]].

- **2026-06-04 — Solution cleanup: NDA Maths, 28 "closed" chapters swept for stealth wrong-keys — 21 key flips.** Ran /solution-cleanup across every NDA Maths chapter except Binomial Distribution + Indefinite Integration (just done). The `matches_option_disagrees` probe read **0** (the 2026-05-27 audit had cleaned self-contradictions) — but the **`REVIEW:`-marker + hedge probes surfaced 43 questions whose own derivation reached a different option than the key**, which slipped past the 2026-05-27 audit because its disagree-probe matches a **case-sensitive** `Matches option` while these hedged in lowercase / `REVIEW:`. Fresh re-derivation of all 43 → **20 key flips** (16 no-PDF, 4 PDF-verified). Then **hardening the disagree-probe to be case-insensitive** (`~* 'matches option'` + `(?i)`) surfaced a **21st** flip — Height & Distance Q100/2019 B→A — which carried NO `REVIEW:`/hedge marker and so had escaped even the marker sweep (its solution derived option A, then wrote "Official key is B"). Flips:
  - **App of Derivatives** Q65/2022 A→B (`sin4x+2x` has 2 extrema in (0,π/2)); Q94/2021 A→B (only stmt 2: `sin x` not increasing on all of (0,π)).
  - **Applications of Integration** Q60/2022 B→C (`(8/3)k²=24⇒k=3`); Q85/2020 B→C (area `2/3`).
  - **Binary Numbers** Q54/2022 C→B (`(x−y)²+xy=x²−xy+y²=9=(1001)₂`).
  - **Circles** Q59/2022 B→C (centre (2,3) on `3x−4y+6=0`); Q55/2017 A→B (`x²+y²+3x+12y+2=0`); Q60/2026 (set S6) A→B (diameter `5√10`).
  - **Conics** Q85/2022 A→B (`2c=√2`).
  - **Functions** Q67/2022 A→D (`f(2)=4`).
  - **Inverse Trig** Q37/2022 B→C (`x=1/√2`).
  - **Lines** Q60/2025 D→A (sum of intercepts `=2`); Q52/2020 D→C (`2x−3y=5`); Q56/2017 A→B (ratio `3:4`); Q87/2022 B→A (sum of \|intercepts\| `=2`; PDF-confirmed).
  - **Trig Equations** Q43/2020 A→B (`cot(α+β)=1/3`).
  - **Vectors** Q70/2017 A→B (`λ=3`).
  - **Height & Distance** Q100/2019 B→A (`z²=h²(cot²y−cot²x)` = option A; right angle at A ⇒ `OB²=OA²+AB²`).
  - **Quadratic Equations** Q4/2017 C→A (strict `<√5` excludes `k=±3` → open interval; PDF-confirmed stem).
  - **Definite Integration** set S7/2019: Q94 B→C (`I₁=π`) + Q95 D→A (`I₁+I₂=2π`) — both keys in the 2-item set were wrong; PDF-confirmed.
  **PDF-driven storage fixes (key unchanged):** Quad Q3/2025 stem `2/√3`→`2√3` (paper-confirmed; key B=6 holds); Binary Q6/2025 stem dropped a spurious trailing digit (`12345678987654321=111111111²`) + options were plain decimal not `(·)₂` (key D holds). **Printed-paper preserves (key + honest note):** Def-Int Q81/2023 — computed `(π+2)/(π−2)` is not among the options (sibling Q82 `8I₁²=π⁴` confirms `I₁`), official key B preserved; App-Deriv Q77/2022 — printed `m+8n` is indeterminate (=option D) while official key B=0 = the intended `m−8n`, preserved + note. ~19 further AGREE/keep-key de-hedge rewrites. **No PUBLIC-count change** (flips only). All Phase-5 probes return 0; all 22 hash-bearing rows recompute. **Lesson:** "closed" means closed *for the probes that ran*. BOTH gaps mattered here: the `REVIEW:`-marker sweep + fresh re-derivation caught 20, and once the disagree-probe was made **case-insensitive** it caught a 21st that had no hedge marker at all. The `/solution-cleanup` skill's disagree-probe was hardened to case-insensitive (`.claude/commands/solution-cleanup.md`, this session) so future runs catch lowercase `matches option` on the first pass. Reconfirms [[audit-probe-symmetry]]; source-PDF reads used the local-first Word/PyMuPDF pipeline ([[gdrive-pdf-fetch]]).

- **2026-06-04 — NDA Maths Binomial Distribution /notes chapter (8th NDA Maths notes chapter) built end-to-end autonomously + 2 stealth wrong-key flips.** Followed the /notes workflow start to finish on one user instruction ("do it without my help"). Bank: 30 PUBLIC q · 2 DB subtopics (15 each) · 3 HARD / 19 MOD / 8 EASY. **Kept the 2 DB subtopics as-is** — they already form a clean teaching arc (compute P(X=k) / moments-and-back-solve), so step-0 of the workflow concluded **no Phase-D reshape** and therefore **no shipped-guide-sync risk** (the deciding check before authoring). 14 concepts (12 bank-anchored + 2 foundations); set siblings S7 + S24 co-located on one concept each. **Diagrams: 4, all 2D** — binomial is algebraic, so a 3D figure would be slop per [[notes-diagram-decision-rule]]: the load-bearing one is `binomial-pmf-interactive` (n/p button-controls + live mean marker, built on the HistogramBinSlider pattern) + 3 static (binomial-coefficient-tree → why the coefficient is C(n,k); binomial-tail-shading → "at least k" as a summed tail; binomial-mean-spread → np centre + √(npq) band). **2 stealth wrong-keys flipped during the bank read** even though NDA Maths is "audit-closed" — a notes build re-derives every featured/drill question, the same way the 2026-06-02 Indefinite Integration notes pass surfaced 3 (reconfirms [[audit-probe-symmetry]]): 1ffbd6f2 "at least one of 6 workers" A→B (665/729) + 4204be5b set-S24 k-value B→C (k=1/4) — both solution-confirmed (the latter by sibling Q108); `content_hash` recomputed via the exact `contentHash` fn, solutions de-hedged; **bank count unchanged** (flips only, no deletes). Process note: a PowerShell here-string mangled the commit and the chained `branch -d` then deleted the feature branch, so the commit landed straight on `main` (still correct + gated + pushed) — captured in [[git-merge-to-main-flow]]. Gates green: notes:lint 0/0, notes:latex OK, full prepush exit 0 (1044 tests).

- **2026-06-04 — JEE Papers 11-26 triage + compilation-mode pipeline + Papers 18, 11 PUBLIC (JEE 718).** Resuming JEE ingestion past Paper 10 surfaced that the `Paper 11-16.docx` files are **NON-STANDARD mixed-subject compilations**, not 90-q shift papers — my block-count triage falsely called them "defective/missing Maths" (the position-based `subjectForNumber` mislabelled the Maths block as Chemistry). **Shipped durable fix:** `extract.ts --compilation` (decide MCQ-vs-numerical by option-parsing, not position) + optional per-question `classification.subject` (content-based, overrides position at commit). **Key-reliability finding:** the compilation papers are unofficial with **unreliable answer keys + no Maths worked-solutions** — user chose "verify EVERY key"; Paper 11 had **2 wrong keys** (Q15→D, Q48→B, both verified) + 1 corrupted stem (Q53 kept PRIVATE). **DONE + PUBLIC:** Paper 18 (clean shift, 60/60) + Paper 11 (compilation, 59/60). **Paper 12 staged PRIVATE** (54 MCQ classified by content; 1 deduped vs P11; NOT yet verified/no solutions). **RESUME POINT (full per-paper map + playbook in [[jee-mains-ingestion]]):** (1) finish Paper 12 — verify all 54 keys, author 26 solutions, fix garbled stems, flip PUBLIC; (2) Papers 13-16 same compilation+audit process; (3) clean shift papers 19-23, 25, 26 + Maths-key-resolution papers 17, 24. Compilation papers are content-audit-level effort each (~2 wrong keys + a garbled stem + 26 from-scratch solutions). See [[jee-paper-compilation-mode]].

- **2026-06-04 — Custom domain + rebrand: "Question Bank" → "PYQ Vault" at https://www.pyqvault.com.** Bought `pyqvault.com` on GoDaddy (the available exact-keyword `.com` after scanning ~30 — "PYQ" matches what aspirants search; pyqforge/examvault/etc. all taken), pointed it at the existing Vercel `question-bank` project on **Hobby** (custom domains + free SSL work on Hobby; commercial Razorpay use will need **Pro** later). DNS at GoDaddy = single `A @ → 216.198.79.1` + `CNAME www → <unique>.vercel-dns-NNN.com` (delete GoDaddy's two parking `A @` records + edit the default `www` CNAME — "conflicts with another record" = editing the existing row, not adding; DNS is per-domain so other domains are untouched). www set primary, apex 308→www. Then the app-side follow-ups: **(1)** added the new domain to **Supabase Auth** redirect URLs + Site URL (mandatory or sign-in breaks); **(2)** swapped 11 hardcoded `question-bank-sage.vercel.app` refs → `https://www.pyqvault.com` (4 `SITE_URL` consts drive canonical/metadataBase/sitemap/robots/JSON-LD; 7 OG-image footer labels) — a redirect alone is NOT enough because canonical/OG/sitemap are *declared in the HTML* and read by crawlers, not redirected; **(3) rebrand** "Question Bank" → "PYQ Vault" across 17 files (`SITE_NAME` cascades to title/template/applicationName/OG/twitter; + AppHeader wordmark, login/signup, Razorpay checkout `name`, JSON-LD publisher "LWS Pune — PYQ Vault", OG-image titles, default export filename; per-page titles trimmed to "Premium"/"Your account" so the root template adds the brand once not twice). Left lowercase "question bank" prose (the corpus, e.g. "2,160-question bank") unchanged. Description refreshed: "MHT-CET, NDA and JEE Main **live**; IPMAT, CUET, NEET coming soon". See [[project-domain-brand]].

- **2026-06-03 — JEE Mains Papers 5-10 (2021) batch-ingested + PUBLIC; JEE now 599 q PUBLIC (Papers 1-10, 600 committed).** Six papers in one session via the mature pipeline; ~126 AI-authored solutions bank-wide (grounded in read figures / re-derivation, unicode-free). Three pipeline-hardening changes (all TDD/gate-clean, committed): **(1) subject-by-number** — `lib.ts` now derives subject from the question number (1-30 Phy / 31-60 Chem / 61-90 Maths) instead of parsing PART headers, after Paper 6's Chemistry header failed to match and mislabelled Q31-60 Physics (`subjectForNumber` + test); **(2) option-supply** — `commit.ts` lets `optionOverrides` supply all four options for a needs_review row whose options didn't parse (Paper 7 Q77 statement-style options split by a display-math block); **(3) skip-unplannable** — `attach-images.ts` warns+skips a record whose image layout `planFor` can't resolve (Paper 5 Q41's 2-scheme + 4-option layout) instead of throwing, so the rest of the paper still attaches. Recurring per-paper work this batch: **garbled dropped-digit values** (the source's leading-char-drop pattern — e.g. "0 cm"→20 cm, ".01×10⁵"→1.01×10⁵, "to  A"→3 A) back-solved from options/answers; **match-list / IE tables that didn't extract** restored as GFM tables (P6 Q34, P8 Q34, P10 Q45); **multi-figure stems** composited into one labelled image (P5 Q36 aromatic structures, P5 Q41 two reaction schemes — same Pillow technique as P4 Q44); **all-"1." solution-doc numbering** (P8 Physics) → answers + solutions supplied positionally; **stem-located LaTeX artifacts** (split `\(…\)`, trailing `\`, mangled `\begin{matrix}` piecewise) fixed via `stemOverride`+`resync` (these escape `attach-solutions`' solution-only KaTeX guard but `validate-db` catches them). `validate-db`'s soft "incomplete?" flags on text-only LCR / ammonolysis questions are false positives (stem mentions a circuit/reaction but the options are self-contained). Per-paper effort ranged ~40 min (clean papers 9/10) to ~90 min (Paper 8's all-"1." remap + 27 authored). Full state + per-paper notes in [[jee-mains-ingestion]]; NEXT: Papers 11-26.

- **2026-06-03 — JEE Mains Paper 4 (2021) ingested + PUBLIC; JEE was 239 q PUBLIC (Papers 1-4).** Clean run — 0 needs_review / 0 no_answer_key, the mature pipeline absorbed every common quirk; 60/60 solutions (45 source + 15 authored, 6 image-option organics grounded in `Read`-ing the extracted figures). 15 new chapters/subtopics auto-created (reused live taxonomy via a dump-before-classify step). 3 stem overrides via `resync` (no re-commit): Q63 malformed `\left(\right.` delimiter, Q74 stray trailing `\ ` before `\)` (a STEM artifact → escapes attach-solutions' solution-only KaTeX guard, caught by `validate-db`), and **Q44 — the recurring multi-figure-stem failure, now with a reusable FIX**: the source renders N compounds as N separate images but only the first attaches to the single `image_url` (attach-images warns "N stem figures, only the first is attached"). **Resolution: composite all N figures into ONE labelled image** (Python+Pillow — scale ×2, side-by-side on white, draw `(I)/(II)/(III)/(IV)` beneath each, trim faint source label-fragments; upload via the bank's `uploadImage` in a one-off tsx script, set `image_url`, stem references the figure). Beats both the text-rewrite (works even when structures aren't nameable) and the P3-Q37 PRIVATE route. Full state + technique in [[jee-mains-ingestion]]. Per-paper effort held ~40 min.

- **2026-06-03 — JEE Mains Papers 2-3 + scale-up pipeline hardening + exam-picker wiring; Papers 1-3 now 180 q PUBLIC.** Wired `jee-mains` into `EXAM_REGISTRY` (guides/notes null → fall back to indexes; `/browse` filter was already DB-driven) so JEE is a pickable exam. Then ran the first scale-up paper (`2021-p2`), which stress-tested the pipeline and forced real hardening (all committed, 40 tests): **(segmenter/parser)** anchor a number alone on its line; keep matrix `\\` row separators (the hard-break strip was eating them); skip `(a)-(ii)` match-codes when finding option markers; **`findDuplicateSolutionNumbers`** warns on a mis-numbered soln block (Paper 2's soln had two "1." → Map last-wins silently **wrong-keyed Q1** + left Q2 keyless — only re-derivation caught it); dropped `sec` from auto-upright (collides with "m/sec"). **(per-paper overrides)** `PaperData` gained `stemOverrides` + `answerOverrides`; `isCommittable` recovers needs_review/no_answer_key rows instead of dropping them. **(robustness)** attach-images plans by option-emptiness not status (match-list-as-image → stem figure); cleanup-latex gained a KaTeX-validated **de-glue** (`\inR`→`\in R`, validates the prefix macro in isolation so multi-macro zones resolve). **THE re-commit-after-cleanup HAZARD:** cleanup-latex mutates committed text+hash, so re-running `commit` inserts **duplicates** (the recomputed hash no longer matches) — fixed by deleting dups and adding **`scripts/jee/resync.ts`** (re-applies overrides to committed rows + recomputes hash via direct UPDATE, no re-commit). **Validation gap (caught by a human /browse review, not the validator):** "0 KaTeX-broken" ≠ "renders clean" — KaTeX only checks complete segments, so a leaked trailing `\` (~19 stems, both papers) and **split `\(…\)` delimiters across options** (each option opened the next one's math, Q62/Q69) passed it; `validate-db` now scans for both + markup leaks. Solutions: user chose **full parity** — Paper 2 is 60/60 (16 authored, organic/circuit ones grounded in the read images). Pace: one more paper (P3) end-to-end, then batch. Per-paper effort is ~45-60 min (classify + a few overrides + ~16 authored + quirk-fixing) — NOT a hands-off loop. **Paper 3 (2021-p3) then ran fully autonomously (user authorized no-approvals) and was clean — the hardening absorbed every common quirk (no answer-key corruption, no number-alone misses). It added the last recurring artifact auto-repair: `cleanup-latex` now rebalances split `\(…\)` delimiters per field (drop a trailing dangling `\(`; prepend `\(` when a field starts with a `\)`), fixing all 15 of P3's artifacts in ONE pass vs P2's manual overrides — this pattern recurs every paper. P3 reached 60/60 solutions; Q37 is structurally incomplete (its reaction figure didn't extract — passes every render/leak/artifact check yet is unanswerable), **set PRIVATE** (179 JEE PUBLIC) with a tracking note in `papers/2021-p3.json`. This drove a 5th `validate-db` scan — a soft **completeness check** (visual-ref stem + no image + no display-math) that flags exactly this class.** See [[jee-mains-ingestion]] + [[docx-pandoc-ingestion]].

- **2026-06-03 — JEE Mains ingestion: new exam, 2021 Paper 1 pilot (60 MCQ) live + a parameterized `scripts/jee/` pipeline.** First new exam since the MHT-CET/NDA era. User had DOCX papers (`C:\tmp\PYQPs\JEE_Mains\`) and asked "do it in chat, not Excel/UI?" — the real answer was **neither**: the hard 95% is *extraction* (OMML→LaTeX, image extract, classify into a new taxonomy), which is mechanism-independent, so a one-off **scripted extractor → the existing `commitStaged` pipeline** keeps every invariant (dedup / taxonomy auto-create / content_hash) instead of re-implementing them in chat. **pandoc** (`--extract-media -t markdown`) converts the papers' OMML equations to faithful LaTeX *and* extracts figures inline — the linchpin (a local tool, not an app dep). Shipped end-to-end across the session: seeded `JEE Mains` exam + 3 subjects (Physics/Chemistry/Maths); built the pipeline TDD-first (`scripts/jee/lib.ts` pure helpers, 34 tests); committed 60 MCQ PRIVATE with a per-paper `upload_jobs` row; attached 41 images + solutions; flipped PUBLIC. **v1 choices:** MCQ-only (Section B numericals skipped), with images, fresh JEE taxonomy (47 ch · 57 sub auto-created), pilot-first. **13 image-only source solutions** were **AI-authored** after *reading the extracted reaction-scheme images* (not guessed) — flagged as authored, not source-derived. Then **parameterized per-paper** (`config.ts` + `papers/<paperId>.json` + `<paperId>` args; `classification.ts` retired) for the scale-up. Durable lessons → [[docx-pandoc-ingestion]] (the reusable pattern) + full state/IDs in [[jee-mains-ingestion]]. Key gotchas: KaTeX validation only catches *math* — raw image markdown / `<!-- -->` / attribute leaks need a separate text scan; Section A/B split is **positional** (each part = 20 MCQ + 10 numerical), not option-count; attach steps must scope by `source_file` since question numbers (Q1-90) repeat across papers.

- **2026-06-03 — Solution cleanup: MHT-CET · Chemistry · probe-flagged surface + numeric-chapter deep sweep (23 key flips).** The last bank-wide audit frontier. Same wrong-key-prone ingestion batch as MHT-CET Physics. Two passes: (A) subject-wide content probes across all 29 chapters (the ingestion left honest `REVIEW:` self-doubt markers — 24 flagged, many explicitly noting key disagreement); (B) a **stealth-key sweep** re-deriving every HARD + numeric MODERATE across the 8 numeric chapters (Solutions, Kinetics, Electrochem, Ionic Eq, Thermo, Solid State, Basic Concepts, Structure of Atom) — because the `matches_disagrees` probe only catches *self-contradicting* solutions, not stealth keys where solution+key agree but both are wrong ([[audit-probe-symmetry]]). The sweep was high-yield: HARD ran ~22% wrong-key, and most stealth flips were cases where **the bank's own solution already derived the correct option then deferred to a wrong key**. 23 flips:
  - Coordination Q72 `fd443733` D→A (EAN [Cu(NH₃)₄]²⁺ = 35). Basic Concepts Q55 `d4d4a9fe` C→B (P_O₂ = 1/5·10 = 2 bar). Alcohols Q53 `6010f82f` D→C (methoxyethane bp ~8 °C is the gas, not diethyl ether). Aldehydes Q63 `66f9b427` C→A (R₂Cd + acyl chloride → acetophenone, not benzophenone). Basic Org Q61 `b773543a` B→A (0.5 mol C → 11.2 dm³). Group 1&2 Q56 `b01d0eac` D→C (Brass = Cu+Zn; D was Duralumin). Polymer Q76 `9f40fb54` D→A (PAN has no amide; all nylons do). Redox Q77 `7f49d25c` C→D ("per **one mole** oxidising agent" = 1 e⁻ — bank over-corrected the official key). Kinetics Q83 `14be257d` D→A (rate signs → 2y+Z→3x). Thermo Q85 `6871cfcd` D→C (ΔU = 200−100 = +100 J).
  - HARD: Kinetics `78930803` A→B (t½ ∝ 1/k → 900×12.25 = 11025 min; bank dropped a decimal). Thermo Q84 `e0e22d34` B→D (ΔH = ΔU+PΔV = 432+200 = 632 J). Solid State Q92 `c774b4bf` A→C (1.4×10²²; solution said C). Solid State `c65fb279` A→C (anions ccp + cations ⅓ tet voids → A₂B₃). Basic Concepts Q65 `e4c012e1` D→A (same mass, const T: P∝1/M → H₂, not Cl₂).
  - MODERATE: Basic Concepts `933b91b1` A→C (KClO₃ for 5.6 dm³ O₂ = 20.4 g; solution said C). Thermo Q62 `fee5b5c4` A→D (ΔHf **of NH₃** is per-mole = −42.5; key A was the 2-mole value). Solid State Q67 `18f17834` D→A (Po SC density = 9.15 g/cm³, matches real Po; bank's 9.74 a miscalc). Electrochem Q71+Q94 `ccfc3f91`+`b2e08a34` C→A ×2 (Zn‖Cd E°cell = −0.403−(−0.763) = +0.36 V; **2 duplicate copies both wrong-keyed C**). Kinetics `34233a31` B→C (rate expr correct form has −d[N₂]/dt). Ionic Eq `327a0991` B→D (Ba(OH)₂ Ksp at pH 12 = 5×10⁻⁷; solution computed it).  Solutions Q51 `a1d8433c` D→C (NaCl ΔH_hyd = 4−790 = −786 kJ; solution derived it).
  - All Phase-5 checks green: every flipped row's keyed label matches its rewritten solution's "Matches option X"; all 23 `content_hash`es recomputed; 0 residual hedge phrases. **MHT-CET Chemistry PUBLIC count unchanged at 1,553** (flips only, no deletes). **Still open** (NOT closed): ~12 **Bucket-2 items needing the MHT-CET source PDF** (computed value not among options / garbled options / broken stems — e.g. Thermo Q55 CO₂-state stem, Solutions Q67 Kb, the duplicate-conflicting Thermo Q62 `213f0e72`/`75902b62` keyed A vs B); **4 image-based rows** whose options extracted as literal "Option A/B/C/D" (Coordination Q70, Electrochem Q68, Ionic Eq Q69, Modern Periodic Table Q100 placeholder — user flagged these as image questions, recover from source not delete); **many intra-chapter duplicate rows** (Thermo/Kinetics/Electrochem/Solutions have 2–3 copies of several PYQs) pending a dedup pass; and the **recall-heavy organic/inorganic chapters' non-flagged questions** (identity-swap wrong-key risk a probe can't see) were only spot-checked via the probe set, not deep-audited. See [[content-audit-progress]] + [[mhtcet-extraction-error-genres]].
  - **Dedup pass (2026-06-03, same session):** normalized-fingerprint scan found **24 exact-duplicate groups** (full stem + options + key identical — same PYQ recurring across exam years) → **deleted one copy of each, 1,553 → 1,529** (one was the cross-chapter Benzonitrile→benzaldehyde Stephen reduction filed under both Aldehydes and Amines; kept the Aldehydes copy). Clean cascade (no concept/principle tags, no set membership, 0 orphan options). **Deferred:** 5 **conflicting-key pairs** (same question + options, DIFFERENT keys — at least one wrong-keyed: chiral-molecule `697850b8`(A)/`b38624c9`(C) → C correct; oxalate-donor-atoms `788af003`(D=4)/`d0b6f96c`(B=2) → B correct since oxalate is bidentate; ΔH°=TΔS° `0f91cb00`/`db330d20` → both correct, options A≡C duplicate; plus 2 image-based pairs cyclopentanol-IUPAC + secondary-benzylic-alcohol that need the figure) — user chose to defer ALL 5 pending source rather than delete the wrong-keyed copy; AND **32 same-stem/different-option groups** (reformatted-option dupes vs genuinely-different image-stem questions — need per-row inspection). The dedup query (normalize text+options to `[^a-z0-9]`-stripped lowercase, group by full fingerprint) is the reusable pattern for the next subject.
  - **Recall-chapter deep sweep (2026-06-03, same session) — CLOSES the MHT-CET Chemistry audit.** Deep-read all 21 recall-heavy organic/inorganic chapters (Coordination, Aldehydes, Amines, Alcohols, Halogen Derivatives, Biomolecules, Polymers, Transition Elements, Chemical Bonding, the 6 Group/Periodic-Table chapters, Redox, Surface Chem, Basic Org, Alkenes, Aromatic, Alkanes, Alkynes) — judging each key against the chemistry directly (identity-swap wrong-keys a probe can't see). **8 more wrong-key flips** (total session **31**): Aldehydes `5f9da8f2` C→B (dimethylcadmium + acetyl chloride → propanone, sibling-confirmed) + `8ce34ca5` D→C (3-ethyl-5-hydroxybenzoic acid, alphabetical-locant rule) · Halogen `199132d1` A→C (NAS difficulty = m-nitrochlorobenzene, meta-NO₂ doesn't activate; sibling-confirmed) · Alcohols `b3fc2d38` D→C (cross-chapter dup of `8ce34ca5`) + `9f3b36bd` A→B (3-methoxy-1,1-dimethylcyclobutane, lowest-locant-set {1,1,3}; sibling-confirmed) · Biomolecules `dc9abd35` B→A (sucrose+lactose give equal glucose, not lactose+maltose) · Redox `6c63994e` A→B (Mn²⁺+ClO₃⁻ balance, x=2) · Aromatic `a356e8c4` D→C (aniline+NaNO₂/HCl at low temp → diazonium chloride, not phenol). **Pattern confirmed:** recall chapters are ~0.5–1% wrong-key (Bonding, Transition Elements, all 6 Group/Periodic, Alkynes, Group-1&2, Surface Chem, Polymers, Amines were 100% clean) — far below the numeric chapters' ~22% HARD rate; the flips clustered in *reaction/IUPAC-naming* questions, never in pure fact-recall. Several flips were **sibling-confirmed** (an identical or parallel question in the same chapter carried the correct key) — the strongest stealth-key signal. **Still deferred** (need source/image): `7692fb49` (sodium propanoate decarboxylation, ethane not in options) · `7d3fa448` (toluene/KMnO₄, LLM-reconstructed stem) · `12bef5c4` (empirical CH₂N solid but MW hinges on a "50 mL" datum → C₁₆H₃₂N₁₆ vs keyed C₂H₄N₂) · `6c31e7d9` (sol-gel last step) · the cyclopentanol-IUPAC image family. **MHT-CET Chemistry audit now substantively closed** (probe surface + numeric deep sweep + recall deep sweep); only the ~12 source-needed items + 5 conflicting dup-pairs + 32 diff-option groups remain, all requiring the MHT-CET PDFs/figures. All 8 recall flips verified (keyed label = solution conclusion, hashes recomputed). Bank-wide audit now **~100% of the 9,550-q bank** at the no-source-needed surface.
  - **Source-PDF verification pass (2026-06-03, same session) — resolves the deferred items via the local MHT-CET papers.** The papers live as text-extractable DOCX in `C:\tmp\PYQPs\MHT-CET\{2021..2025}\` (per-shift `(ques)/(ans).docx` for 2023–24). **New reusable pipeline:** the math/structures are embedded as *images/equations*, so DOCX text-extraction loses option values — instead convert DOCX→PDF via **Word COM** (PowerShell `New-Object -ComObject Word.Application` → `Documents.Open` → `SaveAs(…,17)`; LibreOffice absent) then PyMuPDF-render the page region → `Read` the PNG. The per-shift `*_QuestionBank.xlsx` are NOT independent — they ARE the bank's ingestion source (identical garbled options + same key), so only the rendered exam paper is authoritative. **Critical finding:** this paper set is a **coaching reproduction** ("Gyanam Test Generator, Kota") riddled with printed typos — so the bank's extraction was largely *faithful* and most "errors" are in the source papers, splitting the deferred items two ways. **(A) Bank extraction errors → FIXED (5):** `7d3fa448` stem was an LLM hallucination ("toluene+KMnO₄") — real reaction is **phenol + CrO₃ → p-benzoquinone**, key D was right (stem fixed); `9729764e` [A]₀ "1.2"→**0.2** (paper value), key B now derives 0.5; `54aa1bd1` molar mass "30"→**150** (paper value), key A now derives Kb 1.95; `b9e6360a` "edge length 2 Å"→**a³·N_A=16.2** (the paper's actual given), key A now derives 63.18; `a737ac25` fully reconstructed — garbled "first orbit n=1" placeholder was really **"radius of first orbit of Li²⁺"** (=17.63 pm), so stem+4 options rewritten, key D→**C**, and **moved from Coordination → Structure of Atom** (it was misfiled too). **(B) Printed-paper defects → PRESERVE official key + honest solution note (12):** `4a7984dc` (printed CO₂(s)/½O₂(s) state-symbol typo), `bd9fee07` (option (a) prints "41.6" — a 10× typo for 416), `5994f5c6`/`81535d42`/`4734e779`/`2e625047`/`4bfdcf1f` (printed value/exponent typos where the computed answer isn't among options), `7692fb49` (ethane — the real product — genuinely absent from the printed options), `213f0e72` (OF₂ ΔU, also deduped vs `75902b62`), `d709a35d` (V₂=3.5 not in options), `12bef5c4` (printed 50 mL ⇒ C₁₆H₃₂N₁₆, inconsistent with official C₂H₄N₂), `3baa65be` (correct pH=10 absent from options). **(C) Unreconstructable broken rows → PRIVATE (3):** `facb7658`+`e960b7b4` (placeholder "Option A/B/C/D" extraction failures), `5009cb32` (admitted filler row). **Net DB:** 5 stem/option fixes (hashes refreshed) + 12 preserve-notes + 1 OF₂ dedup + 3 → PRIVATE. **MHT-CET Chemistry 1,529 → 1,525 PUBLIC** (−1 dedup, −3 PRIVATE). **Still deferred:** 6 image-based IUPAC-naming rows (4 cyclopentanol + 2 secondary-benzylic — official keys stand, the structures don't decisively overturn them) + the 5 user-deferred conflicting-key dup-pairs. The DOCX→PDF Word-COM render pipeline is the reusable technique for any future MHT-CET source check ([[mhtcet-source-docx-render]]).
- **2026-06-03 — Word-export fix: matrices/determinants now render with stretchy brackets (`<m:d>` delimiters).** Bug: delimited matrix environments (`pmatrix` `(`, `bmatrix` `[`, **`vmatrix` `|` determinants**, `Bmatrix` `{`, `Vmatrix` `‖`) rendered in the `.docx` with **tiny, detached, single-line-height brackets beside the grid** instead of enclosing it. Root cause (verified by dumping `document.xml` from a real export): `mml2omml` converts the matrix grid to a proper OMML `<m:m>` but emits the surrounding stretchy fence operators as plain `<m:r><m:t>(</m:t></m:r>` text runs flanking it — never an OMML delimiter object, so Word has nothing to stretch. Fix: one pure helper `wrapMatrixDelimiters(omml)` in [ommlBuilder.ts](src/lib/export/ommlBuilder.ts), called from `latexToOmml` after `sanitizeOmmlForXml`, that rewrites the `fence-run + <m:m> + fence-run` shape into `<m:d><m:dPr><m:begChr/><m:endChr/><m:ctrlPr/></m:dPr><m:e><m:m>…</m:m></m:e></m:d>` with `begChr`/`endChr` taken from the captured fence chars (so `vmatrix` → real determinant bars). Only a fence run **immediately adjacent** to `<m:m>` is consumed → ordinary `f(x)` math and fence-less `\begin{matrix}` are untouched (negative tests lock this). **Render-only — no DB writes, no bank edits**; existing papers/keys render correctly on the next download (export regenerates from live data). +7 TDD cases in [omml-builder.test.ts](tests/omml-builder.test.ts) (995 → 1002 tests). Mirrors the existing "mml2omml limitation → post-process the OMML string" pattern already used for underline-bypass + XML-escape.
- **2026-06-03 — LaTeX cleanup: NDA · Chemistry · whole subject (262 q; 69 formula-bearing across 10 chapters).** The standard latex-cleanup probes were already **0** — the bank's chemistry formulae were de-facto **unicode-subscript** (49 q, e.g. `H₂O`), which the probe regex tolerates (it checks superscripts, not subscripts). The real state was a 3-way mix: 49 unicode-subscript + 17 ASCII `->` arrows + 13 bare-digit (`FeCl2`) + a few already-LaTeX. User chose **full-LaTeX canonicalization**: every formula → `\(\text{El}_n\)` with `\to` arrows, `\cdot` for hydrates, `\rightleftharpoons` for equilibria, charges as `^{n+}`. Converted across stems + options + solutions; **23 rows had text/option changes (hash-refreshed, all 23 verified), 46 were solution-only** (no hash impact). **No key flips** — mechanical formatting only (no `is_correct` touched). All Phase-5 probes (standard unicode/balance + chem arrow/subscript/bare-digit) return **0**. **Canonical chem form going forward = LaTeX `\(\text{}_n\)` + `\to`** — unicode `→` can't be the target (it trips the latex-cleanup probe), and this unifies the previously-mixed conventions. DB edits already live; this logs them.

- **2026-06-02 — Solution cleanup: NDA · Mathematics · Indefinite Integration (40 PUBLIC q).** Probes flagged 5; a full HARD + MODERATE/EASY re-derivation sweep found **3 wrong-key flips + 1 PDF-confirmed broken-stem set + 2 garbled-prose rewrites**. This chapter sat inside the 2026-05-27 "NDA bank-wide closed" audit — all three flips are **stealth wrong-keys** (the bank solution agreed with the wrong key, so the matches-option probe never fired; caught only by fresh derivation, two via a hand sweep the probes didn't surface). Key flips:
  - Q76 2022 NDA 2 · `f9259cbb`: A→C. \(\int\frac{(\cos x)^{1.5}-(\sin x)^{1.5}}{\sqrt{\sin x\cos x}}=2\sqrt{\sin x}+2\sqrt{\cos x}\); stored key A dropped the factor 2.
  - Q85 2021 NDA 1 · `4ce9c717`: B→D. \(\int\frac{dx}{\sec x+\tan x}=\ln|\sec x+\tan x|-\ln|\sec x|\); the stored solution swapped \(\int\sec x\) and \(\int\tan x\), landing on the negated key B.
  - Q81 2019 NDA 2 · `84d75bd9`: A→D. \(\int\frac{dx}{2x^2-2x+1}=\tan^{-1}(2x-1)\); stored key A \(=\tan^{-1}(2x-1)/2\) — an extra ½ from a \(\frac12\cdot2=\frac12\) arithmetic slip (half the true value).
  - Q100 2024 NDA 2 · `8afc550b`: D→A (part of the broken-stem set below).
  **Broken-stem set (S20, 2024 NDA 2, Q99+Q100) — PDF-confirmed** against `Maths_2024_NDA2.pdf` (Drive, Set A): printed integrand is \((x^2-1)/\sqrt{x^2+1}\) — the bank had **dropped the √** (stored \((x^2+1)\)), making the printed \(U\!\cdot\!V-3\ln(U+V)\) form impossible (\(2\int\frac{x^2-1}{x^2+1}=2x-4\tan^{-1}x\), no product/log). Fixed the shared context to \(\sqrt{x^2+1}\); with \(U=x,\,V=\sqrt{x^2+1}\): Q99 \(|U^2-V^2|=1\) (key B kept; restored the \(|\cdot|\) the bank dropped from the stem), Q100 \(U\!\cdot\!V=\sqrt{x^2+x^4}\) so **D→A** (the bank had ALSO dropped the √ from all four options — restored them; C reconstructed as \(\tfrac12\sqrt{x^2+x^4}\) from the scalar-distractor pattern, OCR garbled there). The set sibling rule applied (both items in S20 fixed together; context propagated to both).
  Prose-only rewrites (key already correct): Q91 2026 NDA 1 · `8501bf79` (A=1/5, had a "…wait…Let me redo" fragment); Q85 u+v 2025 NDA 2 · `9967b15a` (=dv/dx, had "eˣcosx·0…"/"Actually" noise). No cluster bias (wrong keys spread A/B/A/D). All Phase-5 probes return 0; all 5 content_hashes match recomputed. NDA Maths PUBLIC count unchanged at 2,160. (Lesson reinforced: [[audit-probe-symmetry]] — a "closed" chapter can still hide stealth wrong-keys a probe can't see; the HARD re-derivation sweep is what caught Q85 + Q81.)

- **2026-06-02 — NDA Maths Indefinite Integration /notes chapter (NDA Maths's 7th notes chapter) — built pedagogy-first, reorder-only.** 4 subtopics · 22 concepts (6 foundation) · 40 q · 100% concept-tag coverage; all gates green, pushed to main.
  - **Bank shape:** 40 PUBLIC q · ~22.5% HARD · 4 DB subtopics (Substitution 17, Standard Forms 13, Partial Fractions 7, By Parts 3) · 6 passage-sets. Only 6 EASY q in the whole chapter — the NDA reliably forces a simplify/substitute/decompose step before a standard formula appears.
  - **Reorder-only, NO Phase-D reshape/rename** — the cleanest precedent-following (cf. Matrices). The deciding constraint: the shipped nda-maths Template-A guide's `principles.ts` references all 4 subtopic names verbatim, so a rename would silently break its principle-drill CTAs ([[shipped-chapter-rename-downstream-sync]]). Kept DB names exactly; used friendly display titles + `subtopicOrder` only.
  - **Pedagogy-first ordering** (easiest tool first): Foundations & Standard Forms → Substitution → By Parts → Partial Fractions. Foundations (antiderivative+C, standard table, linearity) folded as un-tagged foundation concepts into the first taught subtopic — the standard pattern.
  - **Diagram-light** per [[notes-diagram-decision-rule]] (algebraic chapter): reused the one existing `antiderivative-family` SVG, **no new diagrams** (matches Matrices / Sequence & Series).
  - **Forward-method trap handled:** the eˣcos/eˣsin **cyclic pair** questions live in *Standard Forms* (taught first) but are by-parts results. Taught there as **standard results verified by differentiation** (a current-level tool — forward *pointer*, not forward *method*), with the by-parts derivation deferred to By Parts. Keeps [[notes-concept-content-alignment]] clean; notes-lint check #5 raised no duplication warnings (authored examples use different numbers than featured PYQs).
  - **Conventions:** `ii-`-prefixed subtopic slugs for global uniqueness vs the MHT-CET Indefinite Integration sibling (which owns `substitution`/`integration-by-parts`); `\ln` (NDA/UPSC papers + this bank's solutions) vs MHT-CET's `\log`; exports namespaced `NDA_INDEFINITE_INTEGRATION_*` to avoid clashing with the sibling's `INDEFINITE_INTEGRATION_*`.

- **2026-06-02 — MHT-CET "Sequences and Series" notes DEFERRED (bank too thin); bank hygiene fixed instead.** Asked to build MHT-CET Sequence & Series notes pedagogy-first. Analysis killed it: the chapter is **8 PUBLIC q** (2nd-smallest of 26 MHT-CET Maths chapters), of which only ~2 are genuinely on-topic series (power-sums limit + a log-base sum), **3 are misfiled roots/quadratic** questions, and 3 are incidental "in A.P./H.P." trig (one a duplicate). Structural, not fixable by tagging — MHT-CET Maths is ~80% Std-12, and Sequences & Series is a Std-11 topic. notes-lint needs ≥1 tagged question per bank-anchored concept; a 25-concept chapter (the NDA shape) would be ~23 concepts with zero bank evidence. **Verdict:** the teaching arc is identical to the shipped NDA Sequence & Series notes — reuse those for the shared 11th-std math, don't duplicate a 25-concept chapter for a 2-q bank. User chose "fix the bank, defer notes." Established the **bank-coverage gate** for /notes ([[mhtcet-sequences-notes-deferred]], [[notes-self-sufficient-template]]): count + sample-read the bank before proposing a chapter.
  - **Bank-hygiene fix executed** (one atomic transaction): created a new **Quadratic Equations** chapter (MHT-CET Maths, order_index 26 — a real Std-11 chapter that had no home in the bank) + subtopic "Roots — Nature, Vieta's Relations, and Conditions", and moved the 3 misfiled roots questions (`848eaa9b` cubic nature-of-roots · `724832d8` min Σ-of-squares-of-roots · `431afc97` Vieta related-roots) into it. **Deduped** the tan⁻¹-in-AP near-duplicate: deleted `c30de073` (Sequences copy — wrong chapter + mis-set MODERATE) and kept `b8ac18fc` (Inverse Trig, HARD, correct home; different `content_hash`es → no collision). **Renamed** the Sequences subtopic "… AP, Roots, and Logarithmic Sums" → "Sequences and Series — AP, HP, Logarithmic and Power Sums" (Roots left). Net: MHT-CET Maths 26 → 27 ch · 1,588 → 1,587 q · 71 → 72 sub; Sequences & Series 8 → 4 q; 0 orphans, kept copy verified intact.
  - **Deferred (flagged, not touched):** `5044c336` (sin(θ−α),sinθ,sin(θ+α) in H.P. → cos²θ) is a probable **stealth wrong-key** — its `(1−2cos²α)/2` answer didn't reconcile with its Trig-II sibling `b358ae2f` (cos2θ → `(1−4cos²α)/2`), and it's arguably misfiled to Sequences (really trig). Logged in ROADMAP "Content quality audits" for the next MHT-CET pass. The matches-option probe never flagged it (the stealth-wrong-key gap).

- **2026-06-02 — NDA Maths Sequence & Series /notes chapter (9th notes chapter) — built pedagogy-first; bank re-carved 4 → 5 subtopics.** 5 subtopics · 25 concepts · 89 q · 100% concept-tag coverage. The bank lumped GP + HP + the three means + the AP↔GP↔HP interconversion genre into one 37-q subtopic; with 0 concept tags the re-carve was free ([[reclassification-sql-pattern]]).
  - **Phase D — split the lumped subtopic into 3.** Renamed the 2 kept subtopics (Arithmetic Progressions, Special Series and Special Sums), created 3 (Geometric Progressions / Harmonic Progressions and the Three Means / Interrelating AP, GP and HP), folded the 2-q Vieta's subtopic into Interrelating + deleted it, moved the 1 arithmetic-geometric q from AP → Special Series. Chapter name unchanged; all multi-q passage sets stayed inside AP (set-sibling-safe). `seq-` prefixed slugs for global uniqueness. Sizes: AP 42 / GP 19 / HP&Means 5 / Interrelating 15 / Special 8.
  - **The Interrelating page carries the signature hard genre** — log bridge (GP→AP), reciprocal bridge (HP→AP), three-term conditions, Vieta's. 1 new static SVG (am-gm-hm-means) — diagram-light per [[notes-diagram-decision-rule]] since the chapter is algebraic.
  - **Guide-sync ([[shipped-chapter-rename-downstream-sync]]):** the nda-maths Template-A guide referenced the old GP/HP + AP subtopic names in `principles.ts` / `compounds.ts` / `strategy.ts`; the split let each drill repoint more precisely (GP three-term → Geometric Progressions; AM≥GM≥HM → HP&Means; AP+GP compounds → Interrelating). guide live-taxonomy + cross-link-integrity tests green.
  - **Caught one transcription error during tagging** — a featured `pyqExampleId` UUID was off by a few hex chars (notes-lint check #2 / coverage diff surfaced it); fixed before INSERT. notes:lint 0/0 (incl. #5 PYQ-dup), notes:latex clean, full prepush green, pushed to main.

- **2026-06-02 — NDA Physics Electricity & Magnetism /notes chapter (8th notes chapter) — built pedagogy-first as the whole E&M DB chapter, not a "Current Electricity" split.** The user asked for "Current Electricity" notes; NDA Physics has no such chapter (it lives inside "Electricity and Magnetism", 93 q). Offered split-the-chapter vs do-the-whole-chapter — user chose **whole chapter** (zero shipped-guide chapter-level churn) + **re-carve subtopics for a clean teaching arc**.
  - **Phase 0 — DB re-carve, 6 → 9 subtopics (free).** 0 concept tags + 0 passage sets existed, so the re-carve cost nothing ([[reclassification-sql-pattern]]): renamed 4 in place, created 3, kept 2, moved 28 of 93 q. New subtopics: Electrostatics · Electric Current and Ohm's Law · Resistance and Resistivity · Combination of Resistors · Electrical Power, Energy and Heating · Cells, EMF and Kirchhoff's Laws · Magnetism and Magnetic Effects of Current · Magnetic Force and Fleming's Rules · Electrical Devices. Chapter NAME unchanged ⟹ chapter-level guide/browse links held.
  - **Phase 1 — shipped-guide sync ([[shipped-chapter-rename-downstream-sync]]).** The nda-physics Template-C guide referenced the OLD subtopic names in `playbooks`/`strategy`/`trends`/`playbook-details`/CHAPTER_TABLE; synced all to the new names + refreshed stale "Resistance & Circuits 26 q @ 42% HARD" → "Combination of Resistors 16 q @ 38%". `guide-nda-physics-playbooks` live-taxonomy test green.
  - **Phase 2 — authoring.** 42 concepts; reference-variant cards for the recall-heavy facts (devices, AC/DC, magnetic materials), formula variants elsewhere. **`em-` prefixed subtopic slugs** to stay globally unique as concept-tag keys. **Cells/EMF/Kirchhoff kept thin (3 q) by design** — NDA barely tests it, but it's a coherent teaching unit; its EMF/internal-resistance concept is a foundation (no featured PYQ — its only PYQ is image-dependent → drill). Image-dependent PYQs (I-V graph, circuit figures) are drills, never featured cards (the worked-example card can't render images). 7 new static SVGs.
  - **Phase 4 — 93 concept tags, 100% coverage, 0 mismatched** (every tag's `subtopic_slug` matches the question's real DB subtopic). notes:lint check #5 caught 2 worked-examples that reused their featured PYQ's exact numbers → fixed to distinct problems.
  - Pushed to main; full prepush gate green (117 test files, all 9 pages SSG-prerendered).

- **2026-06-02 — Solution cleanup: MHT-CET · Physics · whole subject (24 chapters, 1,577 PUBLIC q).** 44 flagged → **20 key flips** + 6 AGREE rewrites + 3 stem-extraction fixes + 10 keep-key honest-note rewrites + 1 figure-method rewrite; 4 held for review. Very high wrong-key rate, **clustered in the May 2023 / May 2024 HARD batch** (Rotational Dynamics alone: 6 flips + 2 extraction errors of 8 flagged) — many wrong-keyed solutions literally derived the correct answer then wrote "following the answer key", pointing to one bad ingestion batch. Key flips (old→new):
  - AC: `c4d0f63c` Q43 C→A (V_L=I√(L/C)=25 V); `df311966` Q31 A→B (series R-C: reducing C and f both dim the bulb).
  - Current Electricity: `81a42a6d` Q50 B→D (G=700 Ω from 3=(G+2000)/(G+200)).
  - Dual Nature: `741d2e89` Q13 D→B (v∝√KE → 1:2).
  - EM Induction: `8c131940` Q42 B→D (√(8000/8)=√1000≈32:1).
  - Gravitation: `4232e62c` Q42 C→A (h=R → g/4 → T₂/T₁=2).
  - KTG: `2830b74d` C→B (T_H=320 K=47°C; PDF-confirmed stem).
  - Fluids: `49c44194` C→A (terminal v∝r² → VR²/r²; PDF-confirmed stem).
  - Optics: `a6d25d67` Q46 B→A (red → larger f → M decreases).
  - Oscillations: `e0231cdd` Q31 B→A (lower g at equator → shorter wire); `8f669e83` Q9 D→A (t=T/12).
  - Rotational Dynamics: `0f518fb9` Q36 D→B (ring tangent-in-plane I=3mL³/8π²); `1f10ec95` Q3 A→C (vertex-axis ∥ opp side, I=3mL²/2); `a9b04986` Q19 D→C (v̄=r√(αθ/2)); `2c28ea38` Q2 C→D (E∝m/r → 12:1); `cd4eb78d` Q26 B→A (τ=Iα=0.15 N·m); `a3b372f1` Q49 B→C (H-frame, 4Ml²/3).
  - Sound: `ca1ea3e5` Q32 D→B (open-pipe 2nd overtone = 3rd harmonic, λ=2(l+2e)/3).
  - Thermodynamics: `2824f441` Q37 D→B (P/4·4^1.5=2P).
  - Wave Optics: `0aa8d55d` D→C... (key A→C; λ/4→K/2 ⇒ I_max=K).
  - **3 duplicate-question collisions:** `df311966`, `0f518fb9`, `81a42a6d` are byte-identical to already-correctly-keyed twins (`3c4ae354`, `a6fd439f`, `ee779d8b`) — the flip both confirms correctness and creates a hash collision, so their `content_hash` was left stale per the [[duplicate-question-dedup-collision]] workaround. **Follow-up dedup pass needed** to delete one of each pair.
  - **Stem-extraction fixes (text corrected, key kept):** `5c3e286b` Superposition velocity 36→**336 m/s** (PDF-confirmed via MHT-CET 2025 19-Apr S-II paper, the only MHT-CET QP in Drive); `262c2b45` Sound 6→**56 m/s** + `7ca0edf1` AC π/6→**π/3** (key back-solves uniquely, same dropped-digit genre).
  - **10 keep-key honest-note rewrites** (computed value not among printed options / options garbled, no source paper): `88c2cf7e` `96eceee1` `0c1c1661` `74198b7c` `ca672d99` `2f11d104` `ebe4c03c` `cd33e11c` `9b203c0c` `dc188227` — solutions now derive correctly and flag the printed defect; keys preserved.
  - **4 held items resolved (2026-06-02, via user screenshots):** `61c9ed23` EMI Q28 A→**D** ("per turn" = total/N = MI/Nt); `a32ad300` Semicond Q12 C→**D** (Y=A̅+B·C → (1,1) for all-low and all-high); `241c3e10` Osc Q23 **D confirmed** (fig b = series → T_b=√2 T_a, fig c = parallel → T_c=T_a/√2, so T_b=2T_c); `0eac7d7e` Osc Q43 **key C kept + stem fixed** (mean-position speed 2→12 cm/s, second speed √6→6 cm/s — only consistent reading for x=2√3). So the subject's total is **22 key flips**.
  - **Dedup pass (2026-06-02):** the 3 duplicate pairs were deduped — deleted `df311966`/`0f518fb9` (kept their correctly-keyed twins) and `ee779d8b` (a Galvanometer Q50 **misfiled under Magnetic Fields**; kept `81a42a6d` in the correct Current Electricity chapter + refreshed its hash). MHT-CET Physics PUBLIC count 1,577 → **1,574**. All kept rows verify against recomputed `content_hash`.
  All Phase-5 content probes now return **0**; every hash-bearing edit verifies against recomputed `content_hash`.
- **2026-06-02 — LaTeX cleanup: MHT-CET · Physics · whole subject (24 chapters, 1,577 PUBLIC q).** 39 questions touched (Bucket A, mechanical): straggler unicode → LaTeX (`Ω`→`\(\Omega\)`, `μ`→`\(\mu\)`, `²³`→`\(^n\)`, `×`→`\(\times\)`, `→`→`\(\to\)`, `cm⁻¹`→`cm\(^{-1}\)`) across qtext/solution/options; hashes refreshed on the 13 rows whose text/options changed. No unbalanced delimiters or matrix envs anywhere — subject was already clean (2.8% flagged). 9 deferred to /solution-cleanup: 7 hedge/REVIEW/corrupted-stem solutions (incl. `7678e0af` garbled NAND truth-table, `1f10ec95` Rotational MI wrong-key candidate) + 2 clean-LaTeX wrong-key candidates outside the unicode set (`c4d0f63c` AC Q43 soln→A/key C, `0f518fb9` Rotational Q36 soln→B/key D — both 2024 May HARD). Phase 5 probes return 0 except the 7 deferred (1 qtext + 6 solution).
- **2026-06-02 — In-app /notes concept reporting + admin triage (mirrors question-reports).** Logged-in users report a concept from its card (incorrect-content / confusing-explanation / typo / broken-visualization / wrong-example / other); admins triage at /dashboard/notes-reports with an open-count badge. Built TDD-first; migration 0028 + lib/notes-reports + 2 API routes + ReportConceptDialog + triage page; +19 tests (980 total); gate + advisors green.
  - **The load-bearing decision — org resolution for org-less content.** The user chose "org-scoped like questions" over a global model, but the schema then forced the interesting part: the ENTIRE taxonomy (exams/subjects/chapters/subtopics) has **no `org_id`** — only questions do — and a concept is TS editorial with no DB row at all. So a concept genuinely has no owning org. Resolved by routing the report to **the org that owns the bank questions the concept teaches**: `createConceptReport` resolves the subtopic via `resolveTaxonomy`, takes the `org_id` of a PUBLIC question in it, and denormalizes it. Reliable because every notes subtopic has bank questions by construction (works for tag-less foundation concepts too), unambiguous today (one org owns all PUBLIC questions), and a clean parallel to the question-report model. A DB test asserts the report lands on the content org, NOT the reporter's throwaway org.
  - **ISR preserved by client-side auth.** `/notes` pages are ISR-cached (cookie-free anon client). The browse `ReportQuestionDialog` gets `isLoggedIn` from the server — doing that on notes would force dynamic rendering and kill the cache on every page. Instead `ReportConceptDialog` resolves login **client-side** (browser supabase `getUser` on mount); the build confirms free notes pages stayed SSG-prerendered (●) even though the server-component `ConceptUnitCard` now renders the client dialog. Anon → sign-in prompt + concept-prefilled mailto.
  - **Identity + reuse.** Concept identity is the globally-unique `(subtopic_slug, concept_slug)` pair (same key as `question_concept_tags`); `getConceptIdentity` validates it against `NOTES_CHAPTERS` so a tampered POST for a non-existent concept is rejected (404). The `report_status` enum + status validators are REUSED from 0024 (lifecycle is identical) — only the category enum is new.
  - Also fixed a pre-existing dead mailto: the question-report fallback pointed at `report@questionbank.lwspune.org` (doesn't exist) → `connect.lwspune@gmail.com` (matches the Footer). Both report dialogs now consistent.

- **2026-06-02 — Bank-wide /notes worked-example de-duplication + a lint guard to keep it that way.** A concept-content alignment pass across the other 6 shipped chapters uncovered that the duplication caught on Matrices was **systemic**: the worked examples had been seeded from their featured PYQ's exact numbers in the *majority* of bank-anchored concepts. Caught only by DB-diffing each `pyqExampleId`'s live text against its `authoredExample`.
  - **Scale.** ~80 worked examples / self-checks re-authored (hand-verified) across Sound, Probability, Statistics, 3D Geometry, Vectors, Indefinite Integration — plus the backlog the new lint then surfaced (Matrices & Determinants 14, 3D Geometry 7 the LLM audit had missed). Two finding classes: **(A) wrong/misleading** examples — fixed always (3D `plane-through-three-points` taught intercept-form not the cross-product method; `dcs-from-a-line` orphaned `l⁴+m⁴+n⁴`; `sphere-and-axes` garbled `r/√2`) — vs **(B) redundant-but-correct** (right concept + method, same numbers as the PYQ below it).
  - **Structural fixes.** Indefinite Integration: **subtopic-order swap** (Rational/Partial-Fractions before Trig II) so the Weierstrass / divide-by-cos² examples meet the arctan-quadratic machinery before they need it — clean reorder, no concept moves, no DB-tag cost. Vectors `scalar-projection` forward-method (uses the component dot product a subtopic early): fixed with an **inline dot-product gloss, NOT a concept move** — relocating it broke notes-lint because projection questions live in the Magnitude/Projection *taxonomy* subtopic and a featured PYQ must sit in the same one ([[notes-concept-content-alignment]]).
  - **PYQ retags (2).** Statistics `arithmetic-mean-raw`'s featured discard-problem → `mean-replacement-correction`; Vectors `triple-product-cyclic`'s unit-vector-sinθ PYQ → `cross-product-magnitude`.
  - **The durable guard — notes-lint check #5.** Fetches each PYQ's text and WARNs when a worked example re-uses its distinctive numbers (subscripts stripped — they're indices, not magnitudes; superscript powers kept). Non-blocking. Reversed the "deliberately not lintable" stance for the *number-heavy* slice; concept-illustration / forward-method / arithmetic-correctness remain the manual read-through (no gate checks math — a wrong fresh example is worse than a correct duplicate). It immediately found ~21 dups the manual+agent audit missed. CLAUDE.md "Notes editorial workflow" Step 1 + 4 updated (author the worked example FIRST then attach a *different* PYQ; prefer a second real PYQ over an invented one). See [[notes-concept-content-alignment]] + [[audit-scope-separation]].

- **2026-06-02 — NDA Maths Matrices & Determinants /notes chapter (7th) + concept-content alignment rule + CI/pre-push gap closed.** Built pedagogy-first; 170 q · 35 concepts · 100% tag coverage.
  - **The chapter.** 6 subtopics (matrix-operations · special-matrices · determinants-evaluation-properties · special-determinants · cofactors-adjoint-inverse · linear-systems), 34 formula + 1 reference concept. Largest+hardest notes chapter (31% HARD). **Pedagogy-first manifested as a decision NOT to reshape** — the bank's 6 subtopics already tracked the teaching spine, so reordered via `subtopicOrder` with no Phase-D (contrast 3D Geometry, which needed a split). **Diagram-light** (3 static schematics) — the diagram decision rule predicting the right outcome for an algebraic chapter. 2 cross-subtopic passage sets co-located (S11→cofactors, S14→special-matrices).
  - **Concept-content alignment rule (the user caught it on a rendered page).** The "scalar multiplication" concept's worked example was actually matrix *multiplication* (taught later, didn't illustrate the concept); a sweep found the "evaluating-determinants" example row-reduced (taught 2 concepts later) AND duplicated its own featured PYQ. Both fixed. Codified the rule in the Notes editorial workflow (Step 1 + Step 4 manual read-through) + [[notes-concept-content-alignment]]: worked examples must teach THEIR concept using only techniques taught by then (forward-POINTERS in prose are fine; forward-METHOD is not); worked example must differ from the featured PYQ; retag PYQs whose dominant skill is a later concept (`66be5b2d` moved equality→multiplication). Semantic — deliberately not a notes-lint rule.
  - **CI ≠ pre-push.** CI red-failed on the Matrices push: `notes-index-helper.test.ts` used "Matrices & Determinants" as a *no-notes* example, which shipping the chapter flipped to true. **Pre-push runs only typecheck+lint+build; CI adds `npm test` + notes:lint + notes:latex** — so the test break passed pre-push and only CI caught it. Fixed the test (synthetic negative cases now) + added "run full `npm test` before ship" to workflow Step 4. See [[prepush-excludes-npm-test]].

- **2026-06-02 — NDA Maths 3D Geometry /notes chapter (6th chapter) + diagram decision-rule codified.** Shipped pedagogy-first in 2 commits; gates green (notes:lint, notes:latex, prepush, guide-nda-maths-taxonomy).
  - **The chapter.** 5 subtopics · 29 concepts (27 formula + 2 reference) on the teaching arc coordinates → direction cosines → line → plane → sphere, authored against the 89-q bank. **Phase-D split:** the bank lumped lines + planes into one 25-q subtopic; pedagogically distinct, so "Lines and Planes in 3D" was re-carved into "The Straight Line in 3D" (11 q) + "The Plane" (14 q) (NDA Maths 109 → 110 sub), and the nda-maths guide principle drill + CHAPTER_TABLE focus prose synced in the same change (the [[shipped-chapter-rename-downstream-sync]] discipline). 89 `question_concept_tags` inserted (100% coverage, all 12 passage sets co-located).
  - **Diagrams — first chapter to start static, then upgraded.** Initial ship leaned static-2D on a chapter that's the most spatial in the bank; a follow-up commit converted octants / line-plane / plane-normal to **drag-to-rotate** (the Vectors `makeProject(az, el)` turntable) and added 2 new rotatable figures (angle-between-lines, sphere centre/radius), keeping sphere↔plane tangency a static cross-section (clearer in 2-D). 6 new SVGs total → 51 bank-wide.
  - **The rule that came out of it (answer to "should diagrams be default-on?"): NO — selective by decision rule, not mandatory.** A diagram on a non-spatial concept is decorative slop + a needless client-island; real hit-rate is ~40–60% on visual chapters, ~0% on recall chapters. Codified the 4-step rule in the Notes editorial workflow (Step 1) + [[notes-diagram-decision-rule]]: spatial? → 3-D? rotate : 2-D static; cap to load-bearing concepts. The miss this session (static-first on a 3-D chapter) is exactly what the rule now prevents.

- **2026-06-01 — Notes/browse UX polish: "Back to notes" pill + slimmer Hero + Present-mode promo removed.** Three small UI changes, 3 atomic commits, all gates green.
  - **"Back to notes" return mechanism.** Clicking a notes "Drill N more →" link used to drop the student on /browse with no way back. Now the drill link carries `from`/`fromLabel`; a floating `BackToNotes` pill (bottom-LEFT, opposite the CartPill) returns them to the **exact concept anchor** (`…/subtopic#concept-slug`, lands via the card's existing `id={slug}` + `scroll-mt-20`). Captured into sessionStorage (`qb:returnTo:v1`) so it **survives pagination + filtering** (those navigations drop the param). Open-redirect guarded by `isSafeReturnPath` (only `/notes/` + `/guide/`). New: `src/lib/browse/returnTo.ts` (pure, TDD `tests/return-to.test.ts`, 16 tests) + `src/app/browse/BackToNotes.tsx` (client). Wired: `buildBrowseUrl` (+`from`/`fromLabel`), `ConceptUnitCard` (+`backHref`/`backLabel` props), `NotesSubtopicPage` (threads per-concept + subtopic-wide targets), `browse/page.tsx` (mounts the pill). **Guides not yet wired** — the /browse pill infra supports any `from`, but each guide `BrowseLink` would need a one-line `from=` per call site (deferred).
  - **Browse Hero slimmed** — dropped the "Available now / Coming soon" exam pill rows (redundant with the sidebar exam filter + header exam pill) + the unused `Sparkles` import + the two exam-list consts; tightened padding. Keeps the `<h1>`, one-line sub, and the 2-stat band. ~40% shorter on the unfiltered /browse. (Continues the "stale scaffolding decays" line — [[stale-scaffolding-decay]].)
  - **"Built for the digital board" Present-mode promo removed** — the callout on the chapter-landing renderer (`NotesChapterLanding`) + the three subject-index pages (nda-maths/nda-physics/mht-cet-maths) per user "not needed / too much real estate". The Present mode feature itself (note button + `NotePresenter`) is untouched.

- **2026-06-01 — UI / brand pass: indigo design-token system + selective accent + scannability/micro-interaction polish.** Shipped in 5 atomic commits; +4 tests net (`peakIndices` helper), all gates green + browser-verified in light/dark. The app was the stock shadcn slate (no accent, near-black primary) — generic. Now it has an identity without losing the dense utilitarian feel.
  - **Why indigo + why selective.** User picked indigo (over the LWS-set emerald #4fd1a5 or a deep teal) specifically to **unify with `/notes`**, whose content palette is already indigo. Application is **additive/selective**, NOT a `--primary` repurpose: a new brand token lights up the chrome the eye lands on; near-black `--primary` stays the workhorse. The load-bearing rule banked in [[brand-indigo-tokens]]: the `/notes` + `/guide` content colors are semantics and are off-limits to the brand pass.
  - **Token system** (`globals.css` + `tailwind.config.ts`): `--brand`/`--brand-foreground` (fill) + `--brand-accent` (text/icon, indigo-600 light / indigo-400 dark — two tokens because fill vs small-text have different AA needs), `--ring` → brand, `brand` Button variant.
  - **Applied to:** active nav (`PrimaryNav`), header `BookOpen` mark, `StatBlock` numbers, exam-pill emphasis + a "Switch exam…" tooltip (`ExamPill`), hero gradient + exam badges (`Hero` — killed the one hardcoded-emerald leak), and `brand`-variant CTAs on `/nda` + `/pricing` + the DownloadDialog primary/trigger.
  - **Browse hero** reworked to a 2-stat band (`questions · Free`); the **`FilterRecipes` quick-filters feature was removed entirely** (component + `recipes.ts` helper + test) per user feedback that it cluttered the empty state. (The earlier 3-stat "subjects" idea was dropped — `subjectOpts` is empty bank-wide without an exam filter, so the count read "0".)
  - **Table scannability:** new pure `src/lib/guide/peakValues.ts` (`peakIndices`, TDD'd) drives a peak-cell highlight; `DriftTable` heatmap recolored to brand; `ConceptWeightTable` bars + heaviest-concept highlight; generic `ReferenceTables` zebra rows; mobile right-edge fade affordance on the scrollable tables.
  - **Micro-interactions:** `QuestionCard` expanded body fades in. (Planned sidebar skeleton already existed in `browse/loading.tsx`.)

- **2026-06-01 (later) — Tech-debt pass: guide component de-dup + resolver registry + cross-link & billing test coverage.** Four scoped items off a planned tech-debt analysis; +29 tests (910 → 939), zero new deps, all gates green.
  - **Guide components: 12 parallel files → 2 generics.** The 8 `RelatedXPlaybooks` + 4 `XReferenceTables` components differed only by an import path + one `href` literal (verified by `diff`). Collapsed to a generic `RelatedPlaybooks` (props `guidePath`/`items`/`slugs` + optional `pathSegment`/`heading`/`intro` — the maths principles variant passes `pathSegment="principles"`; `slug` is optional on `RelatedItem` + filter-narrowed because maths `TOP_11.slug` is `string | undefined`) and a generic `ReferenceTables` (props `guidePath`/`clusters`). All 12 call sites rewired; the 11 dead files deleted only AFTER the full suite was green (see [[verify-before-deleting]]). **This reverses the prior "don't parameterise the parallels until forced" decision** — the trigger was passed at 8.
  - **`questionResources.ts` resolver → data-driven registry.** The 6 chapter-keyed subjects now route via a `CHAPTER_KEYED_GUIDES` array + a map-building loop instead of 6 parallel `const`s + 6 switch cases; Maths / English / single-page landings stay special-cased. Adding a chapter-keyed subject is a one-row change. Behaviour-preserving (the 41-test `question-resources.test.ts` stayed green).
  - **Closed two untested surfaces.** `tests/cross-link-integrity.test.ts` (17) locks the resolver routing (the registry regression guard), the reference-table `playbookSlug`→catalog links (previously unchecked anywhere), and live Economics/CA subject resolution. `tests/billing-webhook.test.ts` (9, real-HMAC + mocked grant) + `tests/billing-grant.integration.test.ts` (3, real DB) cover the Razorpay value path — order.paid routing, idempotent grant (the 23505 path that blocks a webhook+verify double-grant), expired-grant → no access — **before** Razorpay goes live.
  - **Deferred (still open tech debt):** the 1,154-line `EditQuestionForm` god-component split + `docxBuilder` helper extraction (Tier-2, lower urgency) and the `/ → /dashboard → /browse` double-redirect (Tier-3) — see ROADMAP.

- **2026-06-01 — Premium paywall shipped end-to-end (4 phases): self-serve signup → entitlements → comp-access UI → notes preview-gate → Razorpay checkout.** Built test-first across the session; 2 migrations (0026, 0027), +42 tests (910 total), zero new npm deps. Driver: the user wants signups so a few *future* notes chapters can be paywalled, plus the ability to comp their own students.
  - **The load-bearing axis decision — entitlement is per-USER, independent of orgs.** The whole RLS model is org-based, but a paying/comped student has NO org. So `public.entitlements` (migration 0026) keys on `auth.users(id)`, not org. A row grants a `scope` ('all' = full premium; or a future content key), with `source` ∈ `razorpay | comp | manual` and optional `expires_at`. The gate asks only "is there an active entitlement?" — **source-agnostic**, so comp grants and paid grants write the SAME table. RLS: users SELECT own; **no write policy → service-role-only writes** (webhook + comp UI). Pure `isEntitlementActive`/`hasActiveScope` ([src/lib/entitlements/access.ts](src/lib/entitlements/access.ts)) + `userHasAccess` (RLS-scoped, fail-closed). Verified via JWT simulation (owner sees own row, others see 0).
  - **Phase 0 — self-serve students** ([src/app/signup/](src/app/signup/)): Google OAuth (primary) + email/password. **Confirm-email OFF** — payment is the real gate, and Google sidesteps Supabase's built-in email throttle (~2/hr, dev-only; custom SMTP needed for prod password-resets later). `AppHeader`/`UserMenu` now render the account menu for org-less users (role `null` → "Student"); `/dashboard` redirects org-less visitors to `/browse` (dropped the stale "ask your administrator" card). Client-safe validators extracted to [src/lib/auth/credentials.ts](src/lib/auth/credentials.ts) (out of the service-role members module). New `GoogleSignInButton`.
  - **Phase 2 — comp access** ([/dashboard/entitlements](src/app/dashboard/entitlements/)): admins grant/revoke free premium by email (`source:'comp'`), **require-signup-first** (no user auto-create → no password handling). Mirrors the members admin pattern (service-role helpers + action-discriminator route).
  - **Phase 3 — notes preview-gate**: `NOTES_CHAPTERS` gains `tier?: "free" | "paid"` + `paidScope` + `previewConceptCount`. On a paid chapter the landing + first N concepts (default 2) per subtopic stay public + **indexable** (the concept TOC lists ALL names), the rest gate behind `NotesPaywall`. **All 5 current chapters are FREE — machinery is dormant.** CONTRACT: a paid chapter's `[subtopicSlug]/page.tsx` MUST `export const dynamic = "force-dynamic"` + drop `revalidate`/`generateStaticParams` — the gate reads session cookies, so a statically-cached anon prerender would leak the preview. **notes-lint enforces this** (static guard, no DB). Verified live by temporarily flagging Sound paid: anon got 2 preview cards + wall with the locked card absent from HTML but its TOC name kept; free chapters unchanged.
  - **Phase 4 — Razorpay** (migration 0027): **₹999 / 365-day one-time pass** (NOT recurring — avoids RBI e-mandate complexity + the +0.99% subscription fee; renew = buy again). No SDK: Orders API via `fetch` + Basic auth, signatures via `node:crypto` HMAC, hosted `checkout.js` on the client. **Dual idempotent grant** — `/api/billing/verify` (client success → instant access) + `/api/billing/webhook` (`order.paid`, raw-body HMAC, authoritative). Idempotency enforced by a unique index on `(provider_ref) WHERE source='razorpay'` (0027) — verified a duplicate insert → 23505. `/pricing` + `/account` pages; "Account & access" in the user menu.
  - **OPEN (only thing left to transact):** Razorpay account not yet created. Checkout returns 503 until 4 env vars are set in Vercel + `.env.local`: `RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET`, `NEXT_PUBLIC_RAZORPAY_KEY_ID` (= key id), `RAZORPAY_WEBHOOK_SECRET`; plus an `order.paid` webhook → `/api/billing/webhook`. Test mode needs no KYC; KYC gates only LIVE keys. (As of 2026-06-01 the user's existing Razorpay login appeared to be a partner-linked/Route account — "Registered By: CREATOR ECONOMY TECH", limited nav — which may not expose standalone API keys; a fresh direct razorpay.com account may be needed.) Full state in [[project-paywall-build]].

- _For entries dated **2026-05-08 through 2026-05-30**, see [`DECISIONS_HISTORY.md`](DECISIONS_HISTORY.md) — four archived batches: 111 entries (2026-05-08→25) + 26 per-chapter NDA Maths audit entries (2026-05-27) + 27 infra/cleanup entries (2026-05-26→27) + 9 entries (2026-05-28→30: notes template + Probability/Sound/MHT-CET-IndefInt chapters + diagram program + ConceptUnit union + MHT-CET Maths audit closure, archived 2026-06-01). Foundations (M1–M3, 2026-05-08) remain above; only 2026-06 onwards stays in the active log._

## Pending work

The chronological decisions log above captures *why* shipped choices were made; for what's pending, see [`ROADMAP.md`](ROADMAP.md) at the project root. It consolidates every "deferred", "future scope", and "explicitly chose not to build" annotation scattered through this file plus open product questions (e.g., the `principle_tags text[]` column proposal). When an item ships, move its entry into the Decisions log here with a `2026-MM-DD —` prefix and a "Why" sentence so the two files don't drift.

## Cleaned subjects · post-delta sizing

At-a-glance sizing of the cross-subject taxonomy cleanup (chapter + subtopic technique-level canonicals — see Decisions log entries dated 2026-05-13 onward for per-subject narratives). For per-chapter **content-correctness** audit status (orthogonal to taxonomy structure), see [[content-audit-progress]] — **NDA Mathematics fully closed (31 of 31 chapters, all 2,160 q)** as of 2026-05-27, ~22.6 % bank-wide.

**Contract:** numbers below reflect **live counts as of the most recent cleanup pass per subject** (not historical "cleanup-pass-time" snapshots). They match live DB exactly the moment a subject ships, then drift again with the next upload — re-cleanup (Phase D delta) re-syncs them. After any upload batch, treat any subject in this table as stale until SQL-verified or re-cleaned. The live bank-size line at the top of this file is the always-current total. See [[project-docs-staleness]].

**Done — 13 of 13 subjects · 179 ch · 9,546 q · 671 sub** (this is the **cleaned-13-subjects scope, NOT the live DB total** — `npm run stats` reports the live aggregate, currently **258 ch · 934 sub · 10,264 PUBLIC q**; the difference is JEE Mains' ~79 ch / ~263 sub auto-created across Papers 1–10, tracked separately and not part of this cleanup register) (live-verified 2026-06-03; per-row figures below are the per-subject cleanup snapshots and may sum slightly under the live aggregate — SQL-verify before quoting. MHT-CET Chemistry 1,553 → 1,529 → 1,525 on 2026-06-03 — 24 exact-duplicate rows deduped in its solution-cleanup dedup pass, then −1 OF₂ dedup −3 broken rows set PRIVATE in the source-PDF verification pass; NDA Maths re-Phase-D'd 2026-05-17; all 9 other NDA subjects on 2026-05-18 after fresh uploads; MHT-CET Maths re-Phase-D'd 2026-05-23; MHT-CET Physics first cleanup 2026-05-23; MHT-CET Chemistry first cleanup 2026-05-27; NDA Maths Probability re-merged 113 → 109 sub on 2026-05-29 for the notes pedagogy split; MHT-CET Maths Indefinite Integration re-carved 4 → 6 sub on 2026-05-30 for its notes chapter, +2 net → 71 sub; NDA Maths 3D Geometry "Lines and Planes" split into "The Straight Line in 3D" + "The Plane" on 2026-06-02 for its notes chapter, 109 → 110 sub; NDA Physics "Electricity and Magnetism" re-carved 6 → 9 subtopics on 2026-06-02 for its notes chapter, 49 → 52 sub; NDA Maths Sequence & Series re-carved 4 → 5 sub on 2026-06-02 for its notes chapter — split the lumped GP/HP/AM-GM-HM subtopic into 3 + folded Vieta's, 110 → 111 sub; MHT-CET Maths gained a new **Quadratic Equations** chapter on 2026-06-02 (26 → 27 ch · 71 → 72 sub) holding 3 roots questions reclassified out of "Sequences and Series", and one tan⁻¹-in-AP duplicate was deduped from Sequences (1,588 → 1,587 q)):

| Subject | Chapters | Questions | Subtopics |
|---|---|---|---|
| NDA Mathematics | 31 | 2,160 | 111 |
| MHT-CET Mathematics | 27 | 1,587 | 72 |
| MHT-CET Physics | 24 | 1,574 | 91 |
| MHT-CET Chemistry | 29 | 1,525 | 121 |
| NDA English (GAT) | 8 | 900 | 29 |
| NDA Physics | 14 | 449 | 52 |
| NDA Chemistry | 12 | 262 | 47 |
| NDA Biology | 9 | 190 | 39 |
| NDA Geography | 7 | 345 | 36 |
| NDA History | 4 | 260 | 22 |
| NDA Polity | 4 | 90 | 14 |
| NDA Economics | 1 | 24 | 3 |
| NDA Current Affairs | 8 | 180 | 31 |

**No subjects pending.** Bank-wide subtopic taxonomy is now technique-canonical end-to-end. The post-upload ritual still applies — fresh uploads can re-fragment subtopics, requiring Phase D delta cleanup; see [[post-upload-ritual]].

Workflow: inline chapter-by-chapter in chat (no scripts), descending q-count first, source-source collision-loop for bulk merges, per-question reclassification for catch-all chapters ≤~50 q. Patterns + SQL templates: `[[reclassification-sql-pattern]]` + `[[taxonomy-inline-iteration]]`.

## Notes editorial workflow

Authoring a new chapter of `/notes` content. The loop is locked-in as of the Phase 2 ship (concept drills come from the DB, not TS). Order matters because some steps depend on earlier ones.

### 0. Decide the chapter structure — pedagogy-first, bank is a guide

The chapter's subtopic breakdown and concept order are decided from the **teacher/student point of view**: the content must read as a coherent, logical learning progression (foundations → core method → applications → advanced). **The bank's subtopic taxonomy is a GUIDE, not the organizing principle** — the bank is structured for *drilling/filtering on /browse*, so it tells you *what* is tested and *how much* (coverage + emphasis), NOT how to *sequence the teaching*. It frequently splits one pedagogical idea across several subtopics.

Where the bank's split is finer or differently-cut than the teaching arc, **reshape the bank to match the pedagogy** (Phase D merge/rename per [[reclassification-sql-pattern]]) rather than forcing the notes to mirror an incoherent split. The notes `subtopicName` still maps 1:1 to a DB subtopic (lint + concept-tag constraint), so align the DB subtopics to the pedagogical structure *first*, then author 1:1 against the consolidated set.

Concretely: (1) read the bank — sizes, difficulty mix, year spread, real stems **AND the SOLUTIONS** — to learn what to teach + emphasise; **list the distinct solution TECHNIQUES (not just question types) and ensure each maps to a planned concept** — a stem-level read misses technique gaps like "differentiate a determinant" or "variance of natural numbers = (n²−1)/12" that only show up in solutions (both leaked because the chapter was authored from a concept list, not from the solutions). **Read HARD *and* MODERATE solutions** — MODERATE is the largest bucket and is where named shortcuts/formulas actually hide (the 2026-06-07 sweep found 7 gaps, all MODERATE-class: geometric-probability, line-of-intersection = n₁×n₂, MD≈⅘SD, Hermitian, adj(Aᵀ), common-terms-of-two-APs, continued-fraction self-reference; a HARD-only read of those same chapters found ~none). EASY is low-yield (basic methods covered by construction) — leave it to the probe's formula scan; (2) design the subtopic list + concept ramp as a teacher would, for coherent progression; (3) if the bank's split doesn't match, merge/rename DB subtopics so the 1:1 mapping lands on coherent teaching units; (4) proceed to step 1. After authoring, `npm run notes:coverage` (step 4) is the automated backstop for this. **Precedent (2026-05-29, NDA Probability):** the bank stored dice / coins / balls-and-urns / arrangements as 4 separate subtopics that are pedagogically one idea (classical probability = favourable/total on different props); these were merged into "Probability via Counting" so the notes could teach one coherent "Classical Probability" page instead of four repetitive ones. See [[notes-structure-pedagogy-first]].

### 1. Write the TS module

Under `src/app/notes/<exam>/<chapter>/_data/`:

- `chapter.ts` exports `<CHAPTER>_CHAPTER: ChapterNote` with `chapterName` (must match the canonical DB chapter name exactly), `title`, `intro`, and `subtopicOrder: string[]` of stable subtopic slugs.
- One file per subtopic (`<subtopic-slug>.ts`), exporting a `SubtopicNote` with `subtopicName` (canonical DB name), `title`, `oneLineDefinition`, `whyItMatters`, and `concepts: ConceptUnit[]`. **`ConceptUnit` is a discriminated union** (`kind: "formula" | "reference"` is **required**). Both variants share `slug` (stable identity for DB tags), `name`, `intuition`, `definition`, optional `selfCheckExample` (fresh problem, full solution behind one reveal), optional `practiceSet` (Level 1 mastery reps — ~4 short prompt+answer+method items, authored not bank-derived), optional `visualizationSlug` (a static or interactive SVG diagram — build it under `_components/visualizations/`, add its slug to the `VisualizationSlug` union, and add a `case` in `renderVisualization`; all-SVG, no 3D library — see the `_components/` note in [ARCHITECTURE.md](ARCHITECTURE.md)), optional `pyqExampleId` (one featured PYQ), and optional `traps`. Variants differ only on the **core teaching slot**:
  - **`kind: "formula"`** — carries optional `formula?: FormulaSpec` (label + LaTeX + symbol legend) and required `authoredExample` (prompt + step-by-step solution + final answer). Use for technique-driven concepts where the teaching unit is "apply this formula / method to a problem". The pedagogy ramp: intuition → definition → formula? → visualization? → worked example → self-check? → Level-1 practice? → featured PYQ → traps.
  - **`kind: "reference"`** — carries required `table: ReferenceTable` (`columns: string[]` with 2–5 plain-text headers, `rows: ReferenceRow[]` with `cells: string[]` parity-checked against `columns.length`, optional per-row `noteAmber` for amber-tinted callouts, optional per-row `pyqExampleId` for `[Q]` chips, optional table-level `caption`). Use for named-fact recall where the teaching unit is "memorise this table" (frequency bands, transducer chain, ear anatomy, periodic-table-style lookups). The pedagogy ramp: intuition → definition → visualization? → reference table → self-check? → Level-1 practice? → featured PYQ → traps. Self-check + practice on reference variant ARE NOT redundant — they're "spot the wrong row" / "single-row recall" reps.
- **Picking the variant**: read the bank questions for that concept. If the answer hinges on a derivation or formula application → formula. If the answer hinges on memorising a specific named fact ("ultrasonic is > 20 kHz", "cochlea converts pressure to electrical") → reference. A subtopic can (and often should) mix both — Sound's `properties-of-sound-waves` has 2 formula concepts + 1 reference, `sonar-and-ultrasonic` is pure reference, `echo-acoustics-reverberation` is pure formula.
- **Diagrams — selective by decision rule, NOT default-on** (see [[notes-diagram-decision-rule]]). `visualizationSlug` is optional on purpose; a diagram on a non-spatial concept is decorative slop and adds a client-island for no payoff. Real hit-rate even on visual chapters is ~40–60% of concepts (Vectors 17/28, Statistics 12/31, 3D Geometry 6), and ~0% on recall/reference chapters. Per concept, walk the rule: **(1)** Does it have a spatial/geometric mental model? No (formula manipulation, named-fact lookup, counting rule) → no diagram. **(2)** Inherently 3-D (axes, planes, vectors, solids)? → build a **drag-to-rotate turntable** (`makeProject(az, el)` + pointer drag — the Vectors / 3D-Geometry pattern; a fixed projection of a 3-D object is a quiet failure). **(3)** 2-D or a cross-section (triangle, Venn, edge-on tangency)? → **static server-component SVG** (the cross-section is often *clearer* than rotating — e.g. sphere↔plane `p=r`). **(4)** Cap it to the chapter's load-bearing concepts. Add-one mechanics + SVG conventions are in the `_components/` note in [ARCHITECTURE.md](ARCHITECTURE.md).
- **Concept-content alignment — the worked example must teach THIS concept with tools already taught** (see [[notes-concept-content-alignment]]). Every `authoredExample` + `selfCheckExample` must (a) actually illustrate the concept named in the heading, and (b) use only techniques from the **current or earlier** concepts/subtopics in `subtopicOrder` — no forward-reference in the SOLUTION METHOD. The failure mode (shipped twice on Matrices & Determinants): a "scalar multiplication" concept whose worked example was really matrix *multiplication* (taught later); an "evaluate determinants" concept that row-reduced (a property taught two concepts later) AND re-solved its own featured PYQ. Distinctions: a **forward-POINTER** in prose/catalog (stating a consequence the concept exists to deliver, e.g. "skew odd-order ⇒ det 0" before the Determinants subtopic) is **fine**; elementary prior-knowledge (2×2 `ad−bc`) is fine. What's NOT fine is the worked example's *method* needing a not-yet-taught technique when a current-level method exists. Also: the `authoredExample` must be a **different problem from the `pyqExampleId`** (don't solve the same determinant twice), and a `selfCheckExample` shouldn't duplicate another concept's. If a featured/drill PYQ's DOMINANT skill is a later concept, **retag it** to that concept (as `66be5b2d` moved from equality → multiplication), don't teach it early.
  - **Authoring ORDER (prevents the systemic duplication found bank-wide 2026-06-02):** author the worked example FIRST, from scratch, then attach a *different* PYQ as `pyqExampleId` — never reverse-engineer the worked example from the PYQ. The reverse direction is exactly what seeded ~60 "worked example == featured PYQ" duplications across 6 shipped chapters. `npm run notes:lint` now WARNs (heuristically, by number-overlap) when an example re-uses its PYQ's numbers — but it only catches number-heavy cases, so the discipline matters.
  - **Prefer "second real PYQ, worked" over an invented problem when de-duplicating.** A worked example pulled from a *different* tagged bank question is answer-keyed-correct AND distinct — no hand-authored arithmetic to get wrong. Hand-authoring is fine for clean didactic numbers, but **no gate checks arithmetic**: a fresh worked example is only as correct as your verification, whereas a duplicated one was at least provably right. So either re-derive every hand-authored answer independently before ship, or anchor the example to a second real PYQ.
- **Foundations**: prepend a few primitive concepts (no `pyqExampleId`/drill — exempt from notes-lint check 4 **only on formula variant**; reference concepts are never foundations because they are the bucket for the subtopic's flat-fact recall). Give procedural foundations a `practiceSet`; skip it on purely-conceptual ones.
- **Plain-text fields** (`title`, `oneLineDefinition`, `whyItMatters`, `concept.name`, `formula.label`) render WITHOUT KaTeX — use Unicode/prose, no `\(...\)`. `npm run notes:latex` catches leaks.
- **Markup in body fields** (2026-05-29): use `**bold**` for key terms (renders via KatexRenderer / RichText), and in the `definition` field use `- ` line-prefixed **bullets** when the definition is list-shaped (enumerates terms/types/properties) — `RichText` renders them as a `<ul>`. Keep narrative definitions as prose; don't force single statements into bullets. `**`/`- ` are literal in the plain-text fields above — never use them there.
- `index.ts` re-exports the union as `<CHAPTER>_NOTES: Record<slug, SubtopicNote>` and `<CHAPTER>_SLUGS: string[]`.

**No page files to write.** The chapter's `page.tsx` + `[subtopicSlug]/page.tsx` are thin wrappers — clone the Statistics pair, swap the two `getNotesChapterBySlug(...)` args. ALL data-loading + layout lives in the shared `NotesChapterLanding` / `NotesSubtopicPage`.

**The slug is the stable contract** — once a chapter ships, never rename a concept `slug` without a matching UPDATE on `question_concept_tags`. Notes-lint flags orphan slugs as errors but only after they've already broken pages.

### 2. Register in `NOTES_CHAPTERS` (single source of truth)

`src/lib/notes/chapters.ts` is the registry. Append one entry — `{examName, subjectName, subjectRoute, subjectDisplay, chapterSlug, chipLabel, chapter, notes, slugs}` — importing the chapter's `_data` exports. Consumers derive from it automatically: the shared page renderers (`NotesChapterLanding` / `NotesSubtopicPage`), `subtopicSlugRegistry` (concept-tag validation + admin tagging UI), `links/notesIndex` (chip lookups), `links/tagNames` (concept-name lookups), `app/sitemap` (route entries), `/notes/<subjectRoute>/page.tsx` (chapter cards), `scripts/notes-lint` + `scripts/notes-latex-audit` (validation). The only per-chapter files are `_data/*` + the two thin route wrappers.

### 3. Run a chat tagging session

For each chapter, propose a `(question_id, concept_slug)` mapping for every PUBLIC question. Same inline-iteration pattern as the taxonomy cleanup work.

Audit query:
```sql
SELECT q.id, q.difficulty, q.pyq_year, q.set_id, LEFT(q.text, 200) AS text_preview
FROM questions q
WHERE q.chapter_id = '<chapter-uuid>' AND q.visibility = 'PUBLIC'
ORDER BY q.set_id NULLS LAST, q.difficulty, q.pyq_year DESC NULLS LAST;
```

After user approval, INSERT via the standard atomic block:
```sql
INSERT INTO question_concept_tags (question_id, subtopic_slug, concept_slug, tagged_by_llm)
VALUES
  ('<q-uuid>'::uuid, '<subtopic-slug>', '<concept-slug>', true),
  -- ...
ON CONFLICT (question_id, subtopic_slug, concept_slug) DO NOTHING;
```

Mark `tagged_by_llm = true` on LLM-suggested rows so a future admin-review query can find them. The DB row count plus the LLM flag is the audit trail.

### 4. Verify integrity

- `npm run notes:lint` — every `subtopicName` resolves under live taxonomy; every `pyqExampleId` is PUBLIC + in the parent subtopic; every DB tag's `concept_slug` matches a known TS concept (catches typos in either direction); each TS concept has ≥1 DB-tagged question (soft warn for un-tagged concepts); **and (check #5, soft warn) flags when a worked example / self-check re-uses its featured PYQ's distinctive numbers** (the duplication heuristic — number-based, so it catches computational dups and misses word/variable-only ones). Exits non-zero on error; WARNs don't block.
- `npm run notes:latex` — no LaTeX markup in plain-text fields, no unbalanced `\(...\)` / `\[...\]` delimiters. Exits non-zero on either.
- **`npm run notes:coverage <subjectRoute> <chapterSlug>` — coverage triage (NOT a gate).** Diffs the math in the chapter's PUBLIC solutions vs its notes bodies; **triage the macro/technique channel** (the high-signal one — a `[d/dx]`/`\binom`/etc. flag means a solution uses a technique the notes may not teach; structural fragments are noisy, skim only). This is how the `(n²−1)/12` variance + differentiate-a-determinant gaps would have been caught at authoring time. Triage every flag to either "covered (notation variant)" or "real gap → add a concept". See [[notes-coverage-probe]].
- `npm run prepush` — typecheck + lint + build clean.
- **`npm run prepush` covers `npm test`** as of 2026-06-02 (the hook now mirrors CI), so a green pre-push won't red-CI on a test break. Still worth knowing the failure mode: a new notes chapter can flip a literal assertion — `tests/notes-index-helper.test.ts` used "Matrices & Determinants" as a *no-notes* example, which shipping that chapter turned true. Keep negative test cases on synthetic chapter names; real chapters gain notes later ([[prepush-excludes-npm-test]]).
- **Concept-content alignment read-through (manual — not lintable).** `notes:lint` check #5 now auto-WARNs on *number-heavy* worked-example/PYQ duplication, so the manual read-through focuses on what no script catches: (a) the example actually **illustrates the named concept** (not an adjacent one), (b) its method uses only **current/earlier techniques** (no forward-reference per [[notes-concept-content-alignment]]), (c) **word/variable-only** dups the heuristic misses, and (d) **the arithmetic is correct** (a wrong worked example is worse than a duplicated one, and no gate checks math). This caught a misplaced multiplication example on Matrices & Determinants only on a human read of the page. Do it before Ship.

### 5. Ship

ISR-cached `/notes` pages pick up new tags within the 1-hour `revalidate` window. Commit the TS module + the new registry entry; the DB rows are already live from Step 3.

**Then run `npm run notes:order`** (migration 0029) — it copies the chapter's `subtopicOrder` onto `subtopics.order_index` so the new chapter's subtopics render in **teaching order** on /browse (filter sidebar + chapter-scoped question list), not by question-count. It's idempotent and derives purely from `NOTES_CHAPTERS`, so it also re-syncs after a Phase-D re-carve of an already-noted chapter (positions shift). The link is by `subtopicName` → DB name, so the script LOUDLY reports any name drift or any DB subtopic the notes didn't order (which would otherwise sort ahead of its ordered siblings) — fix those before considering the chapter shipped. Chapters without notes keep `order_index` NULL and stay count-desc, untouched.

### Invariants to verify before commit

- **Set-sibling co-location**: questions sharing `set_id` (passage groups) MUST be tagged with the same concept(s). Splitting a passage group breaks `/browse` + Word-export grouping. Audit `set_id` groupings in Step 3 before drafting the mapping.
- **`pyqExampleId` stays in TS**: the one editorial "featured" example per concept is hand-picked and lives in the TS module. Drill UUIDs come from the DB — don't reach back to a TS array.
- **Concept slugs are stable IDs**: never edit a `slug` in a published note. If a rename is genuinely necessary, do it as a one-shot `UPDATE question_concept_tags SET concept_slug = '<new>' WHERE subtopic_slug = '<sub>' AND concept_slug = '<old>'` in the same transaction as the TS edit.

See "Design axes" above for why drills live in the DB while pyqExampleIds + concept definitions live in TS — they're on different axes.

## After a new upload

When a new question-batch lands, run the 6-step ritual in order. The upload pipeline is auto (taxonomy auto-creates, `content_hash` dedup, default PUBLIC visibility, live counts in `/browse` + dashboard + `/guide` TOP_11 + `/notes` drills). What's NOT auto and needs this ritual: **tag coverage, taxonomy fragmentation cleanup, and TS-static editorial copy**.

1. **Sanity-check the batch** (always) — confirm `inserted` vs PUBLIC/PRIVATE counts, PYQ metadata stamped, no missing years
2. **Taxonomy delta cleanup** (if ≥5 new subtopic auto-creates or a new chapter name) — Phase D delta inline; don't restart from scratch. Precedent: 2026-05-17 NDA Maths re-Phase-D (~770 new auto-creates closed in one session)
3. **Principle-tag delta survey** (if new questions in TOP_11 chapter footprints) — incremental per-new-chapter, not whole-bank
4. **Concept-tag delta session** (if new questions in `/notes`-covered subtopics) — also revisit dropped-gap ConceptUnits; bank growth can mature what was previously dropped
4b. **Notes-coverage check** (if new questions in a `/notes`-covered chapter) — `npm run notes:coverage <subjectRoute> <chapterSlug>` and triage the macro/technique channel. New uploads are how a *technique gap* drifts in (a fresh question using a method the notes don't teach); this is the routine catch for it. Add a concept for any real gap.
5. **Editorial copy refresh** (if counts move ≥5–10%) — `whyItMatters`, OVERVIEW constants, CHAPTER_TABLE, strategy %HARD, trends callouts. Mechanical TS edits
6. **Integrity check** (always) — `npm run notes:lint` + `npm run prepush` before commit

Steps 2–5 (incl. 4b) are conditional and can be deferred / queued across sessions. Full procedure + SQL probes + skip-conditions: [[post-upload-ritual]].

## Adding a new RLS-protected table

Follow the `questions`/`options` pattern (see `0002_rls.sql` + `0004_upload_staging.sql`):
1. Enable RLS.
2. Read policy: `org_id = private.current_user_org_id()`.
3. Write policy: same plus `private.current_user_is_admin()` if admin-curated.
4. Add a test in `tests/upload-rls.test.ts` (or new equivalent) proving a TEACHER cannot write and a member of another org cannot read.
5. Run `mcp__supabase__get_advisors` after applying the migration.

## Live infra

- **Supabase project:** `wunvtnqlzjrkvolslbnm` (https://wunvtnqlzjrkvolslbnm.supabase.co)
- **Production:** **https://www.pyqvault.com** (custom domain, live 2026-06-04; apex `pyqvault.com` 308-redirects to `www`) — auto-deploys from `main`. The old `question-bank-sage.vercel.app` still resolves (optionally set to redirect → `www.pyqvault.com` in Vercel → Domains). Canonical URL in code is `https://www.pyqvault.com` (the 4 `SITE_URL` consts: layout/sitemap/robots/GuideJsonLd). DNS at GoDaddy: `A @ → 216.198.79.1` + `CNAME www → <unique>.vercel-dns-NNN.com`. **Vercel plan is Hobby** — fine for the free launch, but move to **Pro before Razorpay goes live** (taking payments = commercial use).
- **Repo:** https://github.com/lwspune/question-bank (public; Vercel project is named `question-bank`)
- **Auth callback URLs allow-listed in Supabase Auth (Authentication → URL Configuration):** `https://www.pyqvault.com/api/auth/callback` + `https://pyqvault.com/api/auth/callback` + the legacy `question-bank-sage.vercel.app` + `localhost:3000`. Site URL = `https://www.pyqvault.com`. (Adding the new domain here is mandatory or OAuth/email sign-in breaks on it.)

## Operations

Day-to-day knobs and where to look when something goes sideways.

### Monitoring

- **Vercel function logs:** dashboard → Project → Logs. The `/api/export`, `/api/sync/mock`, and `/api/upload/*` route handlers all `console.error` on the catch path; surface there.
- **Vercel Analytics:** dashboard → Project → Analytics. Pageviews + visitor counts. Free tier, cookieless. Frontend errors don't surface here yet — if/when noise picks up, consider Sentry.
- **Supabase logs:** dashboard → Logs → API/Postgres. Useful when an export 500s and you want to see the underlying SQL error.
- **Supabase advisor:** run `mcp__supabase__get_advisors` periodically (or after a migration). Two acceptable lints today: `rls_enabled_no_policy` on `public.rate_limits` (intentional, service-role-only access) and `auth_leaked_password_protection` (Supabase auth setting, can be enabled in dashboard).

### When to upgrade tiers

- **Vercel Hobby (current)** is bounded by ~100 GB-h/month of function execution and ~100 GB of bandwidth. The export endpoint is the heavy hitter — when daily traffic crosses ~500 papers, watch the dashboard's bandwidth meter weekly.
- **Supabase Free (current)** is bounded by 500 MB DB + 1 GB storage egress + 5 GB bandwidth. The public bucket of question images is the egress risk; if egress crosses ~80% of the cap mid-month, plan a Pro upgrade.

### Rate limit visibility

The rate-limit table (Phase D) is service-role only and not exposed to the API. To see who's hitting the limit:
```sql
select bucket, count, window_start
from rate_limits
order by count desc
limit 20;
```
Buckets are formatted `export:anon:<ip>` or `export:user:<user_id>`.

### Rotating SYNC_SHARED_SECRET

No code change needed. Set a new value in:
1. Vercel env vars (Question Bank project) → trigger redeploy
2. MHT_CET_AI's env (whatever variable name its publisher uses) → redeploy

The secret is read at request time via `process.env.SYNC_SHARED_SECRET`, so a redeploy on Question Bank's side picks up the new value immediately. Until both sides match, syncs return 401.

### Billing (Razorpay) — env + going live

Checkout is dormant until 4 env vars are set in Vercel + `.env.local`: `RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET`, `NEXT_PUBLIC_RAZORPAY_KEY_ID` (= key id, publishable for the client), `RAZORPAY_WEBHOOK_SECRET`. Without them `/api/billing/order` returns 503 (the `/pricing` Buy button shows "not configured"). Create a webhook in the Razorpay dashboard → `https://www.pyqvault.com/api/billing/webhook`, subscribe to **`order.paid`**, secret = `RAZORPAY_WEBHOOK_SECRET`. **Test mode needs no KYC**; KYC gates only live keys. Going live = swap the 4 vars to `rzp_live_…` values + repoint/add a live webhook (no code change). Change the price/duration in [src/lib/billing/plans.ts](src/lib/billing/plans.ts) (`PLANS`). As of 2026-06-01 the account is **not yet created** — see [[project-paywall-build]].

### Managing members (admins + teachers)

`/dashboard/members` is the admin UI as of 2026-05-26. Add new admins/teachers with name + email + password + role; reset passwords; change roles; remove members. Last-admin protection + self-protection prevent locking yourself out. The page is admin-only; teachers are redirected to /browse.

For one-off ops outside the UI (cron, scripted bulk reset, etc.), the helpers in `src/lib/members/admin.ts` are importable from any server-only context (they use `createSupabaseAdminClient()`). For purely manual SQL reset of a password without the UI:
```sql
update auth.users
set encrypted_password = crypt('new-password', gen_salt('bf'))
where email = 'admin@example.com';
```
Run in the Supabase SQL editor with the service role.

### Flipping visibility on existing questions

The default for new rows is **PUBLIC** (since migration 0022). To take a batch PRIVATE (e.g., a paid-org-only release):
```sql
update questions
set visibility = 'PRIVATE'
where source_file = 'NDA_2024_paper.xlsx';
```
The reverse (flipping PRIVATE → PUBLIC) is rarely needed now since the default is PUBLIC, but the same shape with `set visibility = 'PUBLIC'` still works.

### Setting PYQ year/month/comment for a whole upload

Preferred path: open `/uploads/<jobId>` as the org admin, click **Edit** on the PYQ row, fill any of Year / Month / Comment, save. The PATCH covers all questions linked via `upload_job_id`. For a one-shot SQL backfill (e.g. tagging a batch that was uploaded before Phase A landed):
```sql
update questions
set pyq_year = 2025, pyq_month = 'May', pyq_note = 'Shift I'
where source_file = 'MHT_CET_2025_PCM.xlsx';
```
Use SQL only when you can't reach the upload via the UI (ambiguous `upload_job_id`) — otherwise the UI is auditable.

### Deleting a whole upload

`/uploads/<jobId>` → **Delete upload** removes every question linked via `upload_job_id` (cascades options + question_images via FK), then deletes the storage objects, then deletes the `upload_jobs` row. Pre-0013 questions have `upload_job_id = NULL` and are unreachable via this UI — delete those by `source_file` from SQL if needed:
```sql
delete from questions
where source_file = 'BAD_BATCH.xlsx' and upload_job_id is null;
```
