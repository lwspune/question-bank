# NOTES_HISTORY.md — per-chapter `/notes` detail

Archived from CLAUDE.md's `src/app/notes/` file-layout block to keep the main
file lean (mirrors `DECISIONS_HISTORY.md`). This is the full per-chapter
reference: subtopic breakdowns, concept counts, Phase-D reshapes, visualization
lists, and `question_concept_tags` coverage. CLAUDE.md keeps a one-line-per-chapter
summary + the structural `_types.ts` / `_components/` notes; come here for depth.

The shared machinery (registry, renderers, the authoring workflow, the design
rules) lives in CLAUDE.md — "Notes editorial workflow", the `_components/` note,
and the Decisions log. Chapters below are ordered as in CLAUDE.md's tree.

---

### nda-maths/statistics/
First chapter — 4 subtopics, SSG. `page.tsx` + `[subtopicSlug]/page.tsx` are **thin wrappers** (resolve `getNotesChapterBySlug("nda-maths","statistics")`, delegate to the shared renderers). `_data/chapter.ts` defines `STATISTICS_CHAPTER`; `_data/{central-tendency,dispersion,regression-correlation,frequency-distributions}.ts` each export a SubtopicNote; `_data/index.ts` exposes `STATISTICS_NOTES` + `STATISTICS_SLUGS`. **31 concept units** (6 foundations in central-tendency + 25 bank-anchored) with the full ramp — worked → self-check → Level 1 practiceSet (29 concepts carry one, ~116 reps; the 2 purely-conceptual foundations what-is-data/types-of-data are skipped) → featured PYQ → traps. 12 concepts carry visualizations (originals regression-line-fit / variance-squared-deviations / histogram-bin-slider + High + Medium batch additions). Per-concept drill UUIDs in `question_concept_tags` (**166 tag rows (question × concept pairs) = 100% PUBLIC coverage** of 160 PYQs across 25 bank-anchored concepts; 6 PYQs are dual-tagged).

### nda-maths/vectors/
Second chapter (reshaped 2026-05-22 as teach-from-scratch with a foundation layer; brought to full parity with Statistics 2026-05-29). 5 subtopics, SSG, same thin-wrapper structure. `_data/index.ts` exposes `VECTORS_NOTES` + `VECTORS_SLUGS`; `subtopicOrder` starts with `position-vectors-section` so foundations precede magnitude/dot/cross/geometry. **28 concept units** (6 foundations in Position Vectors + 22 bank-anchored). Same full ramp — self-check + Level 1 practiceSet (27 concepts, 108 reps; the "what is a vector" foundation is skipped as conceptual) — plus 17 visualizations (Vectors carries the most, including the rotatable 3-D figures: right-hand rule, unit normal, direction cosines, torque, triple-product box). Position Vectors title shows "Foundations: Vectors, Operations, and Position" while `subtopicName` stays "Position Vectors and Section" so DB taxonomy + concept tags resolve. Per-concept drill UUIDs in `question_concept_tags` (**96 tag rows = 96/97 PUBLIC coverage** across 22 bank-anchored concepts; 1 PYQ still untagged).

### nda-maths/probability/
Third chapter (shipped 2026-05-29, **pedagogy-first** — the bank was reshaped to match the teaching arc, not vice-versa; see [[notes-structure-pedagogy-first]]). 5 subtopics, SSG, same thin-wrapper structure. `_data/index.ts` exposes `PROBABILITY_NOTES` + `PROBABILITY_SLUGS`. **24 concept units** across the arc *define → count → add → multiply → condition → bound*: Classical Probability & Counting (foundations + 5 counting techniques) · Event Algebra & the Addition Rule · Independent Events · Conditional/Total/Bayes · Bounds on Probability. 10 visualizations (originals dice-sum-grid / venn-two-events / probability-tree + High + Medium batch additions). **Bank Phase-D merge** folded the dice/coins/arrangements/balls subtopics into "Probability via Counting" (162 q preserved, NDA Maths subtopics 113 → 109) so the notes teach one coherent Classical page instead of four repetitive ones. Per-concept drill UUIDs in `question_concept_tags` (**162 tag rows = 100% PUBLIC coverage** of 162 PYQs across 24 concepts; the mis-filed "sock" combinations question is cross-tagged to the Classical page).

### nda-maths/3d-geometry/
Sixth chapter shipped (2026-06-02, **pedagogy-first** — teaching arc coordinates → direction cosines → line → plane → sphere). 5 subtopics, SSG, same thin-wrapper structure. `_data/index.ts` exposes `THREE_D_GEOMETRY_NOTES` + `THREE_D_GEOMETRY_SLUGS`. **The five subtopics:** `coordinates-distance-section` (DB "Distance, Section, and Collinearity in 3D" — foundations: octants, distance, section, centroid, collinearity/shapes) · `direction-cosines-ratios` · `straight-line-3d` (DB "The Straight Line in 3D") · `plane-3d` (DB "The Plane") · `sphere-3d`. **29 concept units** — 27 formula + 2 reference (octant sign-table + axis-DC table). **Bank Phase-D split:** the bank's lumped "Lines and Planes in 3D" (25 q) was re-carved into "The Straight Line in 3D" (11 q) + "The Plane" (14 q) so the teaching units are coherent (NDA Maths subtopics 109 → 110); the nda-maths guide principle drill + CHAPTER_TABLE focus prose were synced in the same change. **6 visualizations** (octants-coordinate-planes, line-plane-intersection, plane-with-normal — drag-to-rotate; angle-between-lines-3d, sphere-centre-radius-3d — new drag-to-rotate; sphere-plane-tangency static cross-section), reusing direction-cosines/section-formula/triangle-centroid/magnitude-right-triangle. Per-concept drill UUIDs in `question_concept_tags` (**89 tag rows = 100% PUBLIC coverage** of all 89 PYQs across 29 concepts, all 12 passage sets co-located).

### nda-maths/matrices-determinants/
Seventh chapter (shipped 2026-06-02, **pedagogy-first as a decision NOT to reshape** — the bank's 6 subtopics already matched the teaching spine, so reordered via `subtopicOrder`, no Phase-D). Largest+hardest notes chapter: **170 q · 31% HARD**. 6 subtopics: `matrix-operations` · `special-matrices` · `determinants-evaluation-properties` (the 59-q core) · `special-determinants` · `cofactors-adjoint-inverse` · `linear-systems`. **35 concept units** — 34 formula + 1 reference (special-matrix catalog). **Diagram-light by the decision rule** (algebraic chapter): 3 static schematics only (`determinant-as-area`, `cofactor-sign-grid`, `sarrus-rule`). Per-concept drills in `question_concept_tags` (**170 tag rows = 100% coverage**; 2 cross-subtopic passage sets co-located — S11→cofactors, S14→special-matrices). Authoring caught 2 concept-content-alignment defects (see [[notes-concept-content-alignment]]): a "scalar multiplication" worked example that was really matrix multiplication, and an "evaluating-determinants" example that row-reduced (taught later) + duplicated its featured PYQ — both fixed.

### nda-physics/sound/
Fourth chapter (shipped 2026-05-30, **first reference-variant chapter** + **first non-Maths /notes subject**; **pedagogy-first redesigned same day** — see Decisions log). 4 subtopics in a coherent *what → measure → behave → use* arc — reshaped from the bank's 5-subtopic split (Phase D collapse: 5 → 4) so the chapter teaches as a narrative top-to-bottom instead of five independent grab-bags. SSG, same thin-wrapper structure under subject route `/notes/nda-physics`. `_data/index.ts` exposes `SOUND_NOTES` + `SOUND_SLUGS`. **The four subtopics:** (1) **`foundations`** — Sound, Perception, and the Ear (11 q · 3 concepts: mechanical-longitudinal-wave, pitch-loudness-quality, human-ear-chain reference) — what sound IS, how we PERCEIVE it, the ear chain that does the conversion; (2) **`wave-equation-and-bands`** — Wave Equation, Speed, Frequency Bands (13 q · 3 concepts: wave-equation-and-units, speed-medium-only, frequency-bands-and-scales reference — merged ultrasonic-band into the frequency-bands reference) — how we MEASURE sound; (3) **`sound-behaviours`** — Reflection, Echo, Reverberation, Beats (5 q · 4 concepts: sound-properties-checklist reference, echo-and-minimum-distance, reverberation-multiple-reflections, beats-formation) — what sound DOES; (4) **`applications`** — SONAR, Transducers, Instruments (5 q · 3 concepts: sonar-and-applications reference, sound-transducers reference, musical-instruments reference — split out from the original ear-and-instruments concept) — how we USE sound. **13 concept units** total — 7 formula variants + 6 reference variants. **5 visualizations** (compression-rarefaction-wave → foundations, beats-envelope → sound-behaviours, ear-anatomy → foundations, frequency-spectrum-strip → wave-equation-and-bands, echo-geometry → sound-behaviours) — all static SVG, server components. Per-concept drill UUIDs in `question_concept_tags` (**34 tag rows = 100% PUBLIC coverage** of 34 PYQs across 13 concepts — the entire chapter bank, no dual-tags). DB Phase D moved 10 questions between subtopics (cochlea to Foundations, beats to Sound Behaviours, microphone + flute + SONAR/bats to Applications, ultrasonic-band Qs into the merged Frequency Bands concept, ultrasonic-can-reflect to Sound Behaviours) and deleted 2 empty subtopics (SONAR and Ultrasonic, Beats Musical Instruments and Human Ear). Two questions had a degenerate set_id (grouped only by Statement-I/II format, no shared passage) — `set_id` unset to standalone during the original tagging session.

### mht-cet-maths/indefinite-integration/
Fifth chapter + **first non-NDA /notes subject** (shipped 2026-05-30). DB subject is **"Maths"** (NOT "Mathematics" — that's NDA's name; the registry/taxonomy lookup must use "Maths" or it resolves nothing). 121 PYQs, the chapter's densest + hardest (~60% HARD). 6 subtopics in a easiest-tool-first arc: **`fundamentals`** (antiderivative + C + standard table + algebraic prep + boundary-value + reconstruct-function, 5 concepts; slug is `fundamentals` NOT `foundations` — see the global-subtopic-slug pitfall) · **`substitution`** (44 q, the workhorse: u-sub, f'/f→log, [f]ⁿf', root subs, exponential subs, 5 concepts) · **`trigonometric-integrals-powers`** (7 q: power-reduction, identity-simplification, 2 concepts) · **`trigonometric-integrals-rational`** (26 q, the hard core: Weierstrass, divide-by-cos², fractional-power-tan-trick, linear-combination-numerator, 4 concepts) · **`rational-and-partial-fractions`** (16 q: standard-quadratic-forms, completing-the-square, linear-numerator-over-quadratic, partial-fraction-decomposition, 4 concepts) · **`integration-by-parts`** (23 q: LIATE, cyclic, eˣ[f+f'], plus 2 syllabus-reference concepts `root-quadratic-standard-results` + `tabular-by-parts` authored WITHOUT a pyqExampleId since the bank has zero of those forms — flagged "not a current bank pattern" in prose, lint-exempt as formula-variant foundations). **1 SVG** (`antiderivative-family`). Phase D re-carve: 4 source subtopics → 6 (rename-4 + create-2, no orphans); image-stub Q131 rebuilt from a user screenshot (∫(3x−2)/((x+1)(x−2)²)). 121 PYQs tagged in `question_concept_tags` (100% coverage). Textbook formula audit (MSBSHTE Ch.3) confirmed bank-tested formulas present; added the px+q numerator-split + √(x²±a²) log-forms (bank-backed) and the ∫√(quadratic) + tabular-by-parts results (syllabus-reference).
