# circle-9 — the book's PRINTED ANSWER KEY, transcribed

**Do not read this file if you are deriving or blind-re-deriving an answer for
this chapter.** It is kept OUT of `config.ts` on purpose: the key was fenced
inside that file's comments and agents read it by accident three times on
2026-08-22 — first because the fence was drawn as a box whose bottom border
read as the fence's end, then because the fenced block sits ABOVE the chapter
entry, so any line-range read around the entry catches its tail. A warning
inside the file everyone is told to open is not a boundary; a separate file is.

Nothing merges this file: `merge.ts` globs `<id>.*.json` only, so a `.md`
cannot be ingested as questions.

It exists so the step-6 cross-check has a transcribed record to diff against
without re-reading the rendered answer pages.

---

   If you are an agent asked to DERIVE or BLIND-RE-DERIVE any answer for
   this chapter, STOP READING at this line and do not scroll past it.
   Reading it contaminates your derivation and destroys the only
   independent check this chapter has.
   Fenced 2026-08-22 BEFORE ingest, on the evidence of Ch.8 Trigonometry:
   the transcription brief tells agents to derive MCQ answers, the
   orchestrator tells them to read this config entry for the block
   inventory, and there the printed key sits. On Ch.8 an agent met the
   key ~50 lines above the sentence saying it must be withheld, derived
   correctly anyway, and honestly refused to call its pass blind. A key
   recorded FOR the cross-check and a key hidden FROM the derivation are
   the SAME BYTES in the SAME FILE — this fence is the only separator.
   (the key runs to the matching 'PRINTED ANSWER KEY ENDS' marker.
    There is deliberately NO box drawn around it: a box's bottom
    border reads as 'the fence ends here' to a human AND to a grep,
    and on 2026-08-22 an agent took exactly that border for the end
    of the fenced region and read the key immediately below it.)
     PS 6.1  1. 20 cm · 2. 5 cm · 3. 32 units · 4. 9 units
     PS 6.2  1. 12 cm · 2. 24 cm
     PS 6.3  (NOTHING - it is entirely "construct the incircle and
             circumcircle of ...", whose answer is the drawing)
     Prob 6  1. (i) A (ii) C (iii) A (iv) B (v) D (vi) C (vii) **D or B**
             2. 2:1 · 4. 24 units
   ⚠ NOTE Problem set 6 Q1(vii): the printed key itself reads "D or B" - the
     BOOK acknowledges two acceptable options. Do not "resolve" that to a
     single letter; blind-re-derive it and, if both genuinely fit, preserve
     the ambiguity with an errata bracket rather than picking one.
   ⚠ A BOOK ANSWER-KEY DEFECT, and it is the interesting kind - the key is
     too GENEROUS rather than wrong. Problem set 6 Q1(vii) asks for the
     distance between two parallel chords of lengths 6 cm and 8 cm in a
     circle of radius 5 cm that are **on opposite sides of the centre**, and
     the printed key reads "D or B". The two chords are at distances
     sqrt(25-9) = 4 and sqrt(25-16) = 3 from the centre. On OPPOSITE sides
     the distance is 4 + 3 = 7 cm (option D); on the SAME side it would be
     4 - 3 = 1 cm (option B). The stem says opposite sides, so **D is the
     only correct answer** and B answers a question the stem excludes. Key D
     and carry an errata bracket rather than reproducing the hedge.
