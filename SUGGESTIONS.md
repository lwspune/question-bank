# Suggestions

A running list of actionable improvements surfaced during /update-docs runs.
Each item is outside the scope of the work that surfaced it. Strike through when done.

---

> **This file was split on 2026-09-16.** The **Backfill ledger** below is the living queue and keeps its home here (scripts and the global CLAUDE.md reference it by this path). Dated review batches from **2026-05 to 2026-08** moved to **[SUGGESTIONS_HISTORY.md](SUGGESTIONS_HISTORY.md)**; the current month stays below. Reason: at 552 KB this file could not be read in one context window, so every question about it paid for all of it.

## Backfill ledger

Standing list of **new learnings that may apply to EXISTING/shipped work** — so they survive across sessions and aren't silently forgotten. Per [[learning-propagation-protocol]]: a candidate here is reworked ONLY after a 360 analysis + explicit user permission. Status: *identified → analysed → awaiting-permission → approved / declined → done*.

| Learning | Existing-work candidate | Status (2026-06-10) |
|---|---|---|
| **The classroom-projection overlay (2026-09-18) makes the `presentMode` prop definitively dead, not merely dormant.** It was left inert on 2026-06-09 when the /notes slide presenter was removed, on the reasoning that ripping out ~20 className ternaries was riskier than leaving them. There is now a DIFFERENT mechanism serving the classroom-board job — one question, an overlay, no slides — so nothing will ever pass this prop again. Measured: **45 references and 33 ternaries across FOUR files, zero callers.** | Delete the prop and collapse each ternary to its read-mode branch in `WorkedExampleCard` (22 refs / 20 ternaries), `WorkedExampleAuthored` (9 / 7), `TrapCallout` (6 / 3), `FormulaBlock` (8 / 3). **The 2026-06-09 entry names only TWO of those four files**, so the shipped record undercounts the surface — anyone scoping this from the decisions log alone would miss half of it. The deferral reason still holds and is the thing to weigh: these are four shipped `/notes` + `/guide` components whose render NO gate in this repo can verify (no jsdom, no Playwright), so the proof is a human reading authored prose pages. Cheap to write, expensive to trust. | identified |
| **The quiz results share is now UTM-tagged but still writes no outbound row, and the mock loop's `share_events` cannot take one. Found 2026-09-18** while fixing the tagging. `0109_share_events.sql` constrains `subject_kind` to `CHECK (subject_kind IN ('mock'))` and `/api/mock/share` hardcodes `subjectKind: "mock"`, so a quiz share has nowhere to land. With the tag in place, inbound arrivals are now attributed correctly (they were being stored as `source: "direct"`); what is still missing is the outbound half, and 0109's own header is the argument for why one without the other is ambiguous — zero signups reads identically whether nobody tapped share or nobody clicked through. | Widen the CHECK in a NEW migration (0109 is applied; append-only) to `IN ('mock','quiz')` — the column was deliberately built as a column rather than an assumption for exactly this — and generalise `/api/mock/share` to take `subjectKind`, or add a sibling route. Small, but it touches a route and a constraint shipped the day before, so it is a deliberate change and not a drive-by. Worth doing only alongside a READER: `share_events` still has none (no dashboard surface, no probe), so rows from a second producer would also accumulate unseen. | identified |
| **`lib/quiz/share.ts` and `lib/mocks/share.ts` duplicate the same URL builder. Found 2026-09-18** when the quiz one was written. Both hardcode `https://www.pyqvault.com`, both set the same `utm_source`/`utm_medium`/`utm_campaign` triple, and they differ only in path segment and campaign name. The quiz module was written as a MIRROR rather than an import on purpose: the mock module shipped 2026-09-17 and reworking shipped code to apply a new learning is exactly what the 360 gate covers. | Extract a shared `buildTaggedShareUrl(path, channel, campaign)`. **Do not name it `buildShareUrl`** — `@/lib/questions/buildShareUrl` already exists for `/browse` filter URLs and the bare name became ambiguous during the 0109 build. Low value on its own (the duplicated core is six lines) and best folded into the `share_events` row above, since both touch the same two modules. Declining is a reasonable outcome. | identified |
| **`/me` has a mock-attempts card and no quiz equivalent, so quiz history is reachable only from the results screen. Found 2026-09-18** while shipping `/quiz/attempts`. `/me` already renders a Mocks card with completed/best/average stats and a "View all N attempts" link; the new quiz history has no such card, and `/account` links neither. | Add a Quizzes card to `/me` reading `getUserQuizAttempts`. Cheap and symmetric, but deliberately deferred: the emitter shipped 2026-09-18 with **zero rows behind it**, so the card would render an empty state for every student until signed-in students start taking quizzes. Revisit once `user_activity where kind='quiz_taken'` is non-trivial — that count is also the honest test of whether the whole feature was worth building. | identified |
| **Five control-byte TAB characters survive across `DECISIONS_HISTORY.md` (four) and `SUGGESTIONS.md` (one, in a `C:/tmp` path), after the 2026-09-15 backfill that cleared this class and deleted its guard. Found 2026-09-18** while scanning a newly-written entry for exactly this corruption. All five are the same defect: a heredoc-authored `\times` collapsed to a literal TAB + `imes`, at file offsets ~590,297-590,579 inside an older archived entry, rendering as `60.2 <TAB>imes 40.2`. The 2026-09-15 (eighth) pass baselined 28 offending paths, 16 of which were gitignored scratch files that existed only on that machine — so a clean checkout was never fully covered, and clearing the ledger removed the allowlist and the trap together. This file is 1.67 MB and was NOT in the surviving scope. | Two candidates, and the FIRST is the real one: (a) restore a repo-wide control-byte probe as a standing gate rather than a one-off baselined list — a list of paths is a claim about one working tree, which is precisely how these four survived; (b) repair the four bytes themselves, which is cosmetic (the text is an archived narrative nobody computes from) and should NOT be done blind, since `\t` is legitimate inside fenced code samples elsewhere in the same file. Do (a) first and let it find (b)'s true scope. | identified |
| **The 317 `/questions` SEO landing pages ship ZERO question cards in their HTML - the content they exist to have indexed lives only in the RSC payload. Found 2026-09-17** while building the formula axis (in a `/concept/[slug]` pilot since scrapped, and confirmed on the `/formula` pages that replaced it), which inherited the same pattern from them. `QuestionList`'s bookmark button calls `useSearchParams()`, and the `<Suspense fallback={null}>` wrapper is documented in that page as "the questions still render on the server, only the bookmark button defers" - **measured, that comment is false**. Stripping every `<script>` block from a prerendered landing (`cbse-11/mathematics/sets.html`) leaves **29.5 KB of DOM out of 172 KB total, with 0 occurrences of "Expand question" and 0 of `katex`**: every card is client-hydrated from the ~143 KB flight payload. Reproduced identically on the new `/formula/adj-adj-a` page - so this is a property of the shared component, not of either page. **The direct marker is in the HTML itself:** a prerendered landing ends in `<template data-dgst="BAILOUT_TO_CLIENT_SIDE_RENDERING">`, which is Next saying the Suspense boundary was abandoned at build. Everything OUTSIDE that boundary does render (35.5 KB of the formula page's 119.7 KB is real DOM - heading, formula, stats, cross-links); it is the question list alone that is missing. The cached-page count is untouched (783 prerendered .html, same as before this work), so it is NOT a caching regression; it is a RENDERING one, landing exactly on the surface whose stated purpose is "a real page to rank instead of a single /browse URL". **What this measurement does NOT establish:** whether Googlebot executes the JS and indexes the questions anyway. It proves only that the served HTML is empty of them. | Do NOT fix by editing `QuestionList` blind - it is shared by `/browse`, `/questions`, `/saved` and now `/formula` (40 more pages, same defect, shipped knowingly rather than fixed blind), and the `useSearchParams()` call is what builds the bookmark sign-in return path. Cheapest candidate: pass that return path in as a PROP from each page (every caller is a server component that already knows its own URL), which removes the client hook and lets the Suspense boundary go. **Measure the real cost FIRST** - check Search Console for whether these 317 pages are indexed WITH their question text. If Google is rendering the JS, the correct fix is to delete the false comment and change nothing else, and a refactor of a component on four surfaces would be pure risk for no gain. | identified |
| **One NDA GAT sitting lost the GFM separator row on EVERY pipe-bearing question, across six subjects, so its match-list tables render as literal pipes today. Found 2026-09-14** during the NDA II 2026 GAT ingest. A transcription band reported it as an English-only problem with 2 affected rows; measuring it properly made it sharper rather than smaller. `GAT_NDA1_2026_QuestionBank.xlsx` (NDA I 2026) carries **12 pipe-bearing rows and ZERO with a `|---|---|` separator** - English 4, Geography 2, Polity 2, History 2, Chemistry 1, Current Affairs 1 - where **every other GAT sitting that carries a table has its separator** (2017-I 8/8, 2018-II 6/6, 2025-II 4/4, 2024-II 2/2, 2023-II 3/3, 2022-II 2/2, 2020-I 1/1, 2025-I 3/3). So it is not a subject-specific slip: that one sitting's ingest dropped the separator wholesale. Per the pipe-table convention in CLAUDE.md, a pipe row with no separator is treated as prose, so a student reading `Read the following pairs: / Active Voice | Passive Voice / I. They laughed at his warnings... | His warnings were laughed at...` sees the raw pipes on `/browse` AND in a teacher's downloaded Word paper. | Insert the separator row after the header row of each of the 12 stems. **`text` IS part of `content_hash`**, so this is NOT an in-place UPDATE: it is a delete-and-re-commit that mints a fresh uuid, and `paper_questions` / `attempt_answers` / `question_concept_tags` / `question_bookmarks` must all be checked for every id first - the same constraint the NCERT Class-11 OMML repair hit. Cheaper alternative worth costing first: these are `pyq` rows whose `.xlsx` source is NOT tracked in this repo, so there is no source of record to correct upstream and the DB is the only copy. Also worth checking whether the 2026 NDA I GAT mock is published and whether any attempt exists against the 12 rows. **Separately NOT this defect, checked and excluded:** `GAT_NDA2_2017_PYQ.xlsx`'s 5 English rows join P/Q/R/S parts with pipes (a different shape, and one GAT_RULES rule 3 says should be newline-separated anyway), and `NDA2_2026_Maths_SetA.pdf`'s 12 pipe rows are absolute values and determinants, where no separator is CORRECT. | identified |
| **`src/lib/text/normalizeNewlines.ts` contained FOUR RAW NUL BYTES and was therefore INVISIBLE to every text probe in the repo. Found 2026-09-12** during the CBSE Chemistry 2024 wave, reported INDEPENDENTLY by two agents on two different papers and verified by the maintainer rather than taken on their word (`file` reported `data`, not text; a byte scan counted 4). The bytes were the deliberate math-mask SENTINEL and **the behaviour was correct** — only greppability was broken: `grep` classified the file as binary and searched nothing in it, so the one file that DEFINES the project's newline contract was silently skipped by `audit:text`, by every `grep -r`, and by the very probes that catch this corruption class elsewhere. **Third instance** after `scripts/bank-paper/english.ts` (2026-08-27) and `scripts/foundation/check-authored.ts` (2026-09-08). | **DONE 2026-09-12**, on explicit approval. The four raw bytes are now the two-character TS escape, and `file(1)` reports `JavaScript source, UTF-8 text` instead of `data`. Behaviour PROVEN identical rather than assumed: a byte-identical copy of the pre-fix file was diffed against the new one over **2,084 inputs — every codepoint 0-255 in eight surrounding shapes, the sentinel spelled by a hostile author, LaTeX commands beginning with the literal sequence, delimiter precedence and idempotence — with 0 differences**, and **1,821 of those inputs genuinely reached past the early return**. A control against a variant using a different sentinel was proven to go RED (3 of 3), so the harness is not blind. ⚠ **The FIRST version of that proof was VACUOUS**: authored through a shell heredoc, which ate one backslash from every input, so the two-character literal became a real newline, nothing reached the code under test, and it reported "0 differences" over 2,079 inputs. Caught only because the sentinel control failed to fire. The rewritten harness builds its inputs from codepoints and REFUSES if fewer than 1,000 are live. 176 tests across all six consumer suites pass, including `upload-literal-newline-guard` (the `commitStaged` rejection path). | done |
| **A repo-wide census found RAW NUL BYTES in SIXTEEN files, THREE of them in shipped `src/` — the class is an order of magnitude wider than its four known instances. Found 2026-09-14** during the NDA II 2026 guide refresh. Noticed only because `grep -rn "NOTES_CHAPTERS" src/lib/notes/*.ts` printed `Binary file ... matches` instead of a line; the census that followed (10,027 files across `src/ scripts/ tests/ supabase/`) **had never been run despite four separate prior occurrences**, and measuring made the problem sharper rather than smaller. **`src/` (shipped): `src/lib/notes/goLinks.ts` (1), `src/lib/quiz/atoms.ts` (1), `src/lib/tags/conceptTags.ts` (1).** **`scripts/`: `jee/lib.ts` (4), `mh-ssc-10-text/fix-italics.ts` (4), `jee/promote-gaps.ts` (2), `practice-paper/check-taxonomy.ts` (2), `reviews/report.ts` (1), `mh-sb-11/_kt_nuc_katex.mjs`, `_kt_nuc_solcheck.mjs`, `_kt_sound_katex.mjs`, `cbse-12-pyq/_tmp/_tmp_2022-55-2-2_check.ts`, `mh-ssc-10-text/data/_sci-animal-classification-10.verify.ts`, `_sci-heredity-10.verify.ts`, `alg-linear-equations-10.rendercheck.ts`, `ncert/_tmp_c12PhyRayOptics_ctrltest.mjs` (1 each).** The inspected instance (`goLinks.ts` line 48) is a composite-key separator in a template literal — ``const nameKey = (a,b) => `${a}<NUL>${b}`;`` — where the author evidently typed `\0` and a raw byte was written instead. **BEHAVIOUR is correct** in that case: a raw NUL is a legal template-literal character and the key separates exactly as intended. **Only greppability breaks** — `grep` classifies each file as binary and searches NOTHING in it, so all sixteen are silently skipped by every `grep -r`, by `audit:text`, and by the very probes meant to catch this class. Two prior instances were fixed one-at-a-time (`scripts/bank-paper/english.ts` 2026-08-27, `scripts/foundation/check-authored.ts` 2026-09-08, `src/lib/text/normalizeNewlines.ts` 2026-09-12). **The census scope was `src/ scripts/ tests/ supabase/`, so root-level docs were never scanned — and THIS LEDGER was a seventeenth instance: writing the entry you are reading emitted a raw byte where the two-character escape `\0` was typed, so `SUGGESTIONS.md` was itself classified as binary and the entry describing unsearchable files was unsearchable. Repaired 2026-09-14.** The census must be re-run over the WHOLE repo, not just the code directories; the standing probe below should be too. | **Do NOT repeat the one-file-at-a-time fix — that pattern is what let this reach sixteen.** Triage by tier: the three `src/` files are shipped app code and each needs behaviour proven identical before and after (the `normalizeNewlines.ts` harness of 2026-09-12 is the model — build inputs from codepoints, never a shell heredoc, and REFUSE if too few inputs reach the code under test); the `_tmp`/`_kt_`/`.verify`/`.rendercheck` files are throwaway probe scratch and several may simply be DELETABLE — check before repairing. **The durable fix is the standing probe, not the sixteen edits**: add a NUL census to `audit:text` (or `prepush`) so a seventeenth cannot appear unseen; that is net-new tooling and needs no permission, unlike the file edits. Zero DB impact, zero `content_hash` impact. **A caution this session earned first-hand:** the initial census was authored through a shell heredoc, which ATE the backslash in `p.replace('\', '/')` and died on a SyntaxError — the documented heredoc-backslash trap hitting the probe rather than the data, exactly as `[[heredoc-backslash-eating]]` warns. Write the census to a FILE. | identified |
| **Two Chemical-Kinetics rows asking whether the units of RATE and RATE CONSTANT coincide are filed on a THIRD subtopic from their own assertion-form twins. Found 2026-09-12** while ruling on order-of-reaction filing. `2023-56-3-1 Q7` and `2025-56-6-1 Q11` ("the unit of rate and rate constant are same for a :", answer = zero order) sit on `Rate of a Chemical Reaction`, while the assertion-form twins making the identical claim (`2023-56-5-1 Q16`, `2024-56-5-1 Q14`, `2024-56-5-2 Q15`, `2024-56-5-3 Q13`) sit on `Integrated Rate Equations`. NCERT puts the units-of-k table in **§3.2.2 Rate Expression and Rate Constant**, i.e. under `Factors Influencing Rate of a Reaction` — so on the spine BOTH current filings are wrong and the two groups also disagree with each other. | Adjudicate the whole units-of-k class at once and file it on `Factors Influencing Rate of a Reaction`. **Deliberately left alone 2026-09-12**: moving only the two rows in front of me would have created a THIRD variant, which is worse than the inconsistency it replaces. All six rows are PRIVATE `pyq`, so nothing shipped is affected, and `subtopic` is not hashed — a cheap UPDATE whenever it is taken. | identified |
| **Four SHIPPED Maths rows record a CBSE keyless/defect void in `_flag` but not in `_cbseVoided`, against the addendum's own rule. Found 2026-09-12** while ruling on void classification. `2025-65-2-1 Q6`, `2025-65-2-2 Q2` and `2025-65-2-3 Q14` each carry the marking scheme's *"No option is correct. Full marks may be awarded for attempting the question."* and correctly set `_noCorrectOption: true`, but leave `_cbseVoided` empty — where `SCIENCE_ADDENDUM.md` rule (a) says a keyless void sets BOTH. `2026-65-3-3 Q33` is the subjective analogue (*"f defined in the question is not a function. So, full marks to be given for attempting the question."*, 5 marks, unconditional) and likewise has none. | Set `_cbseVoided` to the scheme's wording verbatim on all four. Documentation only — `_cbseVoided` is read by NO code and is not hashed, so it cannot move a row's identity or its key. **Not done 2026-09-12**: these are PUBLIC shipped Maths rows and the session's approval was scoped to the Physics void split, so this needs its own 360 + permission. | identified |
| **A live PUBLIC practice row keys a conjunctive adverb as a CONJUNCTION, and its own stored solution refutes the key. Found 2026-09-12** during the LWS-11th derivation, reached INDEPENDENTLY by two agents working on two different papers that could not see each other. Row `cf616452-130f-4b32-868d-3c379e528c56` (source `Oswaal_NDA_GAT_Mock_Test_9.pdf`, NDA English > Grammar > Parts of Speech) asks the part of speech of *Otherwise* in "My parents lent me the money. Otherwise I could not have afforded the trip." It keys **Conjunction** and its solution reads *"it connects the two clauses"* - **but the stem carries a FULL STOP, so those are two SENTENCES, not two clauses**; the stated reason is false on the stem's own punctuation, and the solution is unreviewed prose with no `Matches option X.` line. **The bank also contradicts itself**: sibling row `e681c7b2-08d5-4135-96ef-79f4f6eb59b9` puts *Besides,* in the identical sentence-initial position, keys **Adverb**, and carries a reviewed house-format solution saying *"it cannot be a conjunction because there is a full stop before it."* Adjudicated: **Adverb is correct** - the word is a conjunctive adverb (it follows a sentence boundary, which no coordinating conjunction can cross, and it is freely movable: "I could not otherwise have afforded the trip"). | Flip `cf616452`'s key from Conjunction to Adverb and rewrite its solution in house format. **A key flip changes `content_hash`** - recompute it via the real `contentHash` helper and re-stamp IN PLACE so `paper_questions` is not orphaned; this row is PUBLIC practice, so check `paper_questions` / `attempt_answers` / `question_reviews` first and re-grade any affected attempt (precedent: the 2026-09-07 NDA GAT key fixes). Worth a scoped sweep at the same time for other sentence-initial linking adverbs (however, therefore, moreover, nevertheless, besides, otherwise) keyed as conjunctions - this is likely a class, not one row. | **DONE 2026-09-12.** Approved and applied. The pre-fix class sweep is the useful part and it NARROWED scope rather than widening it: of 16 sentence-initial linking-adverb rows in Parts of Speech, only TWO were wrong. `cf616452` (Otherwise) and `8dc988c7` (Therefore) were flipped to Adverb, solutions rewritten in house format, `content_hash` recomputed with the real helper and re-stamped IN PLACE, and both recorded in `question_reviews` under run `backfill:2026-09-12-lws11-ledger`. **Two candidates were DELIBERATELY NOT touched** and that is the does-it-really-apply guard working: `caa621f1` ("She worked hard yet she did not pass") is ONE sentence where yet genuinely coordinates two clauses, and `cf19b44a` ("Still water runs deep") is a genuine attributive adjective. Blast radius was ZERO attempts and ZERO bookmarks on both fixed rows, so no student mark moved. The script refuses a re-run by asserting the before-state. |
| **Eight duplicate question PAIRS are live in the bank, every one surviving `content_hash` because the two copies differ only in MARKUP or WHITESPACE, never in meaning. Found 2026-09-12** during the LWS-11th dedup gate, surfaced independently by five different dedup agents and then verified row-by-row against the DB (2 of the 9 originally claimed did NOT hold up and are excluded — see below). All pairs share the same correct answer, so **no student can be mis-graded; the harm is inflated chapter counts and both copies surfacing in `/browse`.** **Maths (2, both practice):** `4ea0df6c`/`4eed4f19` (the `^{20}C_0 - ^{20}C_1 + ...` series, differing by `\ldots..` vs `\ldots`) and `b6375fba`/`eaeaa47a` (sum-of-reciprocals, byte-identical stems, options `6/5` vs `\(\frac{6}{5}\)`). **English (1):** `3403a601`/`3df8e481` — the same sentence spelled `bottomline` vs `bottom line`. **Geography Climatology (5):** `18d3294f`/`eeb1e660` (water cycle; answers `(1), (2) and (3)` vs `All of these` — same meaning, different option wording) plus FOUR where a `practice` row duplicates a `pyq` row — `77c5e8fb`/`f599d030`, `5b718a49`/`c5cfefae`, `5177f9a7`/`62e229f9`, `3eb3f757`/`47d0a514` — which is a DISTINCT and arguably worse class, since the practice copy is redundant with a real past-year question and the `/browse` PYQ/Practice toggle shows them as two separate items. **NOT duplicates, checked and excluded:** `60afafee`/`383cfba2` are two DIFFERENT sentences both answering 'Interjection' (content repetition, not duplication), and `79878a00`/`bf6540b4` are a PRIVATE/PUBLIC pair, i.e. the `status:"dup"` mechanism working as designed. | Decide per pair whether to withdraw one copy to PRIVATE (preferred — reversible, keeps ids alive for any paper referencing them) or merge. **Check `paper_questions`, `attempt_answers` and `question_concept_tags` for BOTH ids before touching either** — a copy sitting in a teacher's paper or a student's attempt cannot simply vanish. The practice-duplicates-pyq group needs a product call first: is practice/PYQ overlap intentional? A bank-wide sweep for this class has never been run and would likely find many more than eight; the cheap detector is a normalised-stem GROUP BY that strips LaTeX commands and whitespace. | **PARTIALLY DONE 2026-09-12 — the 8 verified pairs are resolved; a bank-wide sweep then found the population is far larger.** Each pair had one member withdrawn to PRIVATE (visibility only, so ids survive and org staff still see them in their papers). Keep-rule applied uniformly: most downstream history wins (attempts > concept tags > papers), ties on content quality. In all four practice-duplicating-pyq pairs the PYQ carried the attempts and the concept tag, so the practice copy went. **NEWLY MEASURED, NOT YET ACTIONED: 79 duplicate groups / 84 redundant PUBLIC rows remain across NDA — Mathematics 72, English 4, Physics 2, Chemistry 1; Geography is now 0.** Mathematics dominates because LWS reuses a question set across its own mock files (Mock_Test_04/05/06/08); 5 of 5 sampled groups were genuine, differing only in whitespace. **THE DETECTOR MUST KEY ON STEM + SORTED OPTIONS, NEVER THE STEM ALONE** — a stem-only key returned 135 groups and is mostly WRONG, because for split-sentence, rearrangement and "which statement is correct" items the content lives in the OPTIONS and a shared instruction stem is not a duplicate at all. Needs its own permission: 84 rows is a different job from 8. **Status: 8 pairs done; the 84-row sweep is awaiting-permission.** |
| **A subtopic the NCERT ingest DECLARED but which no textbook exercise ever used is INVISIBLE to a live-axis query, and both `validate.ts` and `axis.ts` treated that absence as "this name is invalid". Found 2026-09-10** on the Physics board-PYQ pilot. A subtopic row only reaches the DB once a question is filed on it, so `declared` and `live` are different sets and the tooling only ever read the second. Measured: **Physics hides 7 declared-but-unused subtopics and Chemistry 14** — including `Diffraction` (Wave Optics), `Radioactivity` and `Nuclear Force` (Nuclei), `Physical Properties of Amines`, `Structure of Amines` and `Physical Properties of Diazonium Salts`. Fixed at source: `declaredSubtopics()` in `scripts/cbse-12-pyq/config.ts`, `validate.ts` now ACCEPTS a declared name (reporting it as pending rather than refusing it), and `axis.ts` prints it marked `declared, no rows yet - USE IT if it fits`. Proven both ways: a real `Diffraction` row validates clean, a typo'd `Diffractionn` still goes RED. | **THE GAP FOUR CHEMISTRY AGENTS REPORTED WAS NEVER A GAP.** Waves 1a/1b recorded, and the wave-1b commit message states, that *"Amines has no `Physical Properties` subtopic (it exists under Haloalkanes only - verified against the live DB)"*. The name existed in config the whole time; **my verification queried the live axis, which is exactly the wrong source for the question "is this name allowed?"**, so it confirmed the wrong thing. Same for the diazonium-stability gap. Affected: an unknown SUBSET of the 106 committed Amines rows plus some Coordination Compounds rows, all currently filed on a defensible-but-approximate live subtopic. **Nothing is wrong for a reader today** - every row is on a real subtopic with a correct answer - so this is a filing-precision gap, not a correctness defect. **Identifying which rows move needs per-row judgement** (reading each stem), NOT a mechanical migration. Cheap and fully reversible while the corpus is still git-only: `subtopic` is NOT part of `content_hash`, so re-filing is a plain field edit with no hash churn and no delete-and-re-commit - and it gets materially more expensive once these 1,024 rows are committed to the DB. | **identified 2026-09-10; tooling FIXED (net-new, no permission needed); the Chemistry re-filing is NOT done.** It touches 1,024 previously-committed rows and needs a 360 + explicit permission. Best done BEFORE the Chemistry DB commit, since the same edit afterwards is a DB migration rather than a JSON edit. |
| **A question the board asks in TWO sittings collapses to ONE row, and until 2026-09-10 nothing said so.** `content_hash` is unique on `(org_id, exam_id, content_hash)`, so two identical stems in the same exam CANNOT both exist - which is correct and is the corpus's own "duplicates are a recurrence signal" fact. The gap was REPORTING: `commit.ts` named rows absorbed by a PRE-EXISTING question (the textbook twin case) but was structurally blind to two rows of the SAME batch collapsing into each other, because its test is "did this stem land?" and for a duplicate pair the stem did land - once. `absorbed` therefore read 0 and the only signal was the PRIVATE-stamp count being one short of the authored total, which is easy to misread (I misread it once before chasing it). Fixed at source: `commit.ts` now groups the authored rows by content hash, names every question asked in more than one sitting with BOTH sittings, and reconciles the count explicitly. | **2 shipped rows are affected, both MH HSC Class 12 MATHS**, found by scanning all 437 authored rows (4 collapses total; the other 2 are in Rotational Dynamics and Oscillations, committed 2026-09-10 with the collapse reported and both sittings preserved in the committed `data/*.questions.json`). The shipped pair: `differentiation-12-pyq` - *"If y = x^x, find dy/dx"* asked **March 2016 Q.4(B)(ii)** AND **March 2019 Q.11**; and `indef-integration-12-pyq` - *"Prove that the integral of dx/(a^2-x^2) = ..."* asked **March 2018 Q.5(B)(ii)** AND **2024 Q.23**. In each the bank holds ONE row carrying ONE sitting's year and question number; the other sitting is absent from the DB. **Nothing is wrong with the shipped rows** - the question IS in the bank with a correct answer - so this is a completeness/provenance gap, not a correctness defect, and the recurrence is NOT lost (both sittings are in the committed source of record). **Cheap and low-risk if approved**: `pyq_note` is NOT part of `content_hash`, so recording the second sitting is a plain in-place `UPDATE` with no delete-and-re-commit and no hash churn. The open question is a product one - whether a recurrence marker is worth surfacing to students at all, which the MHT-CET book work argued IS the best feature of a repeat rather than a defect. | DONE 2026-09-10 (approved). The surviving row now names the other sitting in `pyq_note`, via the new `apply-recurrence-notes.ts` — idempotent, and a plain UPDATE because `pyq_note` is not part of `content_hash`. Applied to ALL FOUR collapsed pairs, not only the two shipped Maths ones: `differentiation#2`/`#7`, `indef-integration#20`/`#23`, `rotational-dynamics#35`/`#41`, `oscillations#8`/`#13`. Separately, `commit.ts` now REPORTS a question asked in more than one sitting and reconciles the count, so the collapse can never be silent again; and `unify-duplicate-solutions.ts` resolves the case where two source entries for one collapsed row carry DIFFERENT solutions — toward the text already live, so nothing a student sees can move. |
| **A stored model answer must read as the BOARD ANSWER a student would write, not as a note to whoever is marking it. Codified 2026-09-10** as `scripts/lib/boardAnswerStyle.ts` + `tests/board-answer-style.test.ts`, enforced by `scripts/mh-hsc-12-pyq/promote.ts` (errors block, the `\therefore` convention only warns) and written into that pipeline's `SOLUTION_BRIEF.md`. The calibration is worth keeping: measured across the 153 shipped Maths numericals, `\therefore` before the concluding step is real (135, 88%) while closing on a bolded result line is NOT (0 of 153 — the corpus closes with a sanity check instead), and a first draft that warned on the missing bolded line fired on 43% of the corpus before being corrected. | **4 shipped PUBLIC rows carry the defect the probe now blocks**, all MH HSC Class 12 Maths: `app-derivatives-12-pyq#24` (an "also acceptable" aside offering the marker an alternative) and `trig-functions-12-pyq#1 / #8 / #15` (each naming an option LETTER in prose — "options A and B", "Options A and D", "rules out options A"). The last three are the more valuable fix, because naming a letter in prose is also what makes `audit:keys` misfire, so repairing them closes a live probe-noise source as well. **Cheap and low-risk**: `content_hash` EXCLUDES `solution`, so unlike the bracket item above this is a plain in-place `UPDATE` with no delete-and-re-commit and no hash churn. Note that until it is done, re-promoting either of those two chapters will now be REFUSED by the gate — which is the correct signal, but is friction someone will meet unexpectedly. **10 other pipelines have their own SOLUTION_BRIEF and are NOT yet wired to the shared probe**; extending them is new work, not a backfill. | identified — awaiting permission |
| **pandoc escapes a prose `[...]` as `\[...\]`, which is this project's DISPLAY-MATH delimiter — so an escaped prose bracket renders as typeset maths rather than a bracket. Found 2026-09-10** while cleaning the Physics board-PYQ extraction, where 18 stems carried one (10 of them unbalanced, because pandoc leaves the opener outside the math zone and absorbs the closer into it as `\rbrack`). `stripArtifacts` already unescaped `\_`, `\^`, `\<` and `\>` for exactly this reason; brackets were simply missing from that list. The fix is in `scripts/mh-hsc-12-pyq/lib.ts` and is covered by three new tests. | **Exactly ONE shipped PUBLIC row**: `efbca568-8d1e-4e5b-ae97-e70e31387f35` (MH HSC Class 12 Maths → Application of Derivatives, `question_kind='pyq'`), whose stem ends `on the interval \[0, 5\].` and therefore renders "0, 5" as a display equation instead of a closed interval. **The bank already shows the correct form one row over**: the textbook twin of the same question (`c69c1002-…`, `question_kind='practice'`) stores `\([0, 5]\)` and renders correctly — so this is a defect, not a convention. Blast radius is one row, one field, and the repair is a pure text edit. **BUT `content_hash` covers the stem**, so this cannot be an in-place `UPDATE` without desyncing the row from its hash — it needs the delete-and-re-commit path (`commit.ts` documents the hazard), and the row must first be checked for `paper_questions` / `attempt_answers` / bookmark references. Note the same fix also changes `differentiation-12-pyq#30` in the draft, but that copy is DROPPED by the Maths cross-chapter ledger and never reached the DB. | DONE 2026-09-10 (approved). Urgent by then for a reason worth recording: the extractor had been fixed earlier in the same session, so the SOURCE and the DATABASE disagreed on that stem — and since `content_hash` is stem-derived, the next `commit.ts` would have INSERTED the corrected text as a NEW row instead of skipping, silently giving the chapter a duplicate with one copy broken. Repaired by delete + re-commit + flip after confirming 0 dependent references; the row now reads `[0, 5]` and a bank-wide scan finds 0 remaining escaped prose brackets. The class is closed by the new `resync-stems.ts`, which diffs live rows against the committed source BY CONTENT_HASH — so an OPTION repair counts too, not just a stem — and refuses to delete a row anything references. |
| **96 CBSE Class-12 PHYSICS solution rows carry a two-part scaffolding that no shipped corpus uses, and 16 of them print a MARK SPLIT written for the marker rather than the candidate. Found 2026-09-13** in the first Physics solution wave. Two of the five papers (`2023-55-2-1`, `2023-55-3-1`) open rows with `CBSE's marking scheme:`, close them with `Added here, and not from the marking scheme:`, and in 16 cases print `Marks: ½ for the field formula, ½ for the radius relation, …`. The agent that wrote them said explicitly it was following "the house style already committed on `2023-56-1-1`". **Measured, that style is a 46-row pilot, not a convention: the COMPLETE shipped Maths book — 1,766 solved rows — has ONE row opening with the scheme, ZERO "Added here" and ZERO mark splits**, against Chemistry's 44 of 46. So a 46-row precedent was about to overwrite one set across 1,766. **The exam signal is not lost by writing one voice** — 84 Maths rows (4.8%) mention the scheme INLINE where it carries information (*"The direction must be stated: the marking scheme deducts half a mark if it is left out."*), which is the same fact delivered where a student needs it. The mark splits are separately the thing `scripts/lib/boardAnswerStyle.ts` forbids outright: a stored answer must read as the board answer a student would write, not a note to whoever is marking it. | Rewrite the 96 rows to one voice: collapse the two blocks, keep every equation and every sentence of physics, delete the 16 mark splits, retain the scheme reference inline wherever it carries exam information. **Cheap and low-risk: `solution` is NOT part of `content_hash`, so this is a plain in-place `UPDATE` — no delete-and-re-commit, no hash churn, and the rows are PRIVATE with zero dependents.** The convention itself is already fixed at source (SOLUTION_BRIEF.md now carries the measurement and an explicit "ONE VOICE" rule), so the remaining ~30 waves cannot re-derive the pilot's style — this ledger entry is only about the 96 rows already written. **The 46 Chemistry pilot rows are a SEPARATE call**: they are the origin of the pattern and a different batch, and folding them in silently would be the wrong shape. | **awaiting permission** — user prefers plain prose (2026-09-13) and the measurement agrees; the rewrite itself was not approved before the session paused. |
| **CONSOLIDATED 360 — four backfill classes on the shipped CBSE Class-12 Physics + Chemistry openers. Analysed 2026-09-13** after the wave-4 follower ingest, whose 44 papers act as a free second reading of every opener. **Blast radius measured rather than assumed and it reframes the whole item: all 3,257 CBSE-12 Phy+Chem `pyq` rows are PRIVATE (0 PUBLIC), and they have ZERO references across all nine dependent tables** — `paper_questions`, `attempt_answers`, `question_bookmarks`, `question_concept_tags`, `question_principle_tags`, `question_reviews`, `question_item_stats`, `question_reports`, `embeddings`. **Cost splits on one fact verified in `commit.ts`: `_flag` and `_cbseVoided` NEVER REACH THE DATABASE** (they live only in the JSON), `_figure` reaches it only as the `REQUIRED` collision-guard signal, `subtopic` reaches it but is NOT hashed (in-place UPDATE), and only stem/option text is hashed (DELETE + re-commit, new UUID). **(1) 15 stale taxonomy flags** — re-swept, not quoted; the count had moved from the 14 recorded earlier. All four claimed-missing subtopics are LIVE (`Diffraction` 28 rows · `Physical Properties of Amines` 3 · `Physical Properties of Carboxylic Acids` 1 · `Special Purpose p-n Junction Diodes` 8). **Stale flag ≠ mis-filed row**: `2025-55-4-1` Q15 and Q18 are ALREADY on `Diffraction` and only the note is false, and several Amines rows correctly argue in their own flag text that basicity belongs on `Chemical Reactions of Amines` per NCERT §13.5.1. **(2) transcription fidelity** — `2025-55-4-1 Q12` silently "corrected" the printed misprint *horizonal* → *horizontal* (both followers AND the marking scheme print the defect), which is the one item with DEMONSTRATED harm: the bank holds TWO rows for that one printed question (`a6a139b2…` opener, `c98ee82e…` follower), confirmed by query; `2025-56-1-2 Q29c` types a real sentence dash as a hyphen; `2022-55-5-1` *farther*/*further* is UNRESOLVED (its pages are not rendered). **(3) answer-leaking `_figure` notes** — 1 confirmed (`2025-55-4-1 Q32a` states *"(field into the page)"* beside a velocity arrow, which together determine an induced-current direction). **(4) void classification** — 4 rows from the sibling-agreement audit (2,447 questions in 2+ sets, 2,443 agree): `2022-55-1-1 Q10`, `2024-55-2-1 Q18b`, `2024-55-2-3 Q19b` need `_cbseVoided`; `2026-56-1-1 Q28` needs it promoted from `_flag`. | **DO: 1a (flag text, 15) · 4 (voids, 4) · 3-confirmed (1) · 2a (horizonal, 1).** The first three are pure JSON edits to fields no code reads — entire risk is a bad edit caught by git — and leaving false taxonomy claims in the source is actively harmful, since they are what the next agent reads and one has already propagated a wrong "gap" into six papers. 2a is the only demonstrated harm. **THEN: 1b, re-file ~6-8 rows** in one pass with the stems read; hash-neutral so it is an UPDATE, and several are genuine one-row-two-homes cases where filing on the dominant half is the documented convention — so this needs judgement, not a mechanical migration. **PARTIAL: 2b**, folded into 2a's re-commit batch (identical mechanics, low value alone). **DEFER: 2c** until `2022-55-5-1` is rendered (already on the figure re-render list). **DECLINE: the en-dash class**, and this REVERSES the ledger's own earlier framing of it as "the largest single backfill candidate" (216 U+2013 in one source, 8 rows in one paper). A later finding had already settled it by measuring the opener's own committed file — **12 en-dashes, NONE in a chemical name** — so the corpus has a consistent derivable rule (*normalise chemical-locant dashes to hyphens, keep the real sentence dash as an en-dash*) and the papers themselves are inconsistent (`2-Bromobutane` is a genuine hyphen on the page). That is a house convention, not accumulated error; 2b is the single row that violates it. **DEFER: the wider figure-leak scan.** ⚠ **A probe was built to size this class and is NOT trustworthy** — it returned 8 candidates of which most are notes correctly EXPLAINING why a figure is `REQUIRED` rather than leaking anything, **and it MISSED the one known positive** (its gate required the stem to ask for a "direction"; that stem asks to "mark the relevant values"). Wrong in both directions. Deciding whether a note leaks needs the question and the note read TOGETHER — judgement, not a pattern — so this is best done as a read-through when the 292 figures are cropped and someone is looking at each one anyway. | **DO-set approved and applied 2026-09-13** — see the Decisions log. |
| **389 PUBLIC rows across 28 SHIPPED chapters carry a DERIVED answer with no `derived_model` stamp, so a reader cannot tell them from a keyed one. Found 2026-09-09** while closing NCERT Chemistry, by diffing every `scripts/ncert/data/*.crosscheck.json` row whose verdict is `NO-KEY-ENTRY` (i.e. the book prints no answer, so ours is derived) against the `derived_model` column. The lane has cross-checked 48 chapters producing **621 NO-KEY-ENTRY rows**, and `stamp-provenance.ts` has been run on only 20 of them (**232 rows**, and it stamps exactly the NO-KEY-ENTRY set — verified: Cl-11 Chemistry's 183 stamps reproduce its eight chapters' crosscheck counts row for row). The remainder is **Cl-12 Chemistry 202 across all 10 chapters · Cl-12 Maths 93 across 7 · Cl-11 Maths 93 across 10 · Cl-12 Physics 1 (Nuclei)**; Cl-11 Physics and Cl-11 Chemistry are fully stamped. Worst single chapters: Biomolecules 34, Trigonometric Functions 31, d-Block 28, Application of Derivatives 26, Alcohols 24, Coordination 22, Haloalkanes 22, Amines 21, Sets 20. **The cause is structural, not clerical: `scripts/ncert/flip-public.ts` has no provenance gate**, unlike its State Board sibling which refuses to publish an authored row carrying no stamp — so stamping is a step someone must remember, and it was forgotten 28 times. (An in-session working figure of 404 appears in that day's conversation; **389 is the re-measured, checkable number** and the 28-chapter count is identical either way.) | **389 PUBLIC rows in `cbse-11` + `cbse-12`.** The write itself is trivial and reversible — `derived_model`/`derived_at` are not in any content hash, so nothing re-hashes, no `content_hash` moves, and no paper, mock or `question_reviews` fingerprint is disturbed; it is one UPDATE per chapter driven by the committed crosscheck files, which are the evidence. Blast radius is a rendered provenance line, nothing more. The judgement call is not the mechanics but whether a textbook corpus owes the disclosure at all: the 2026-09-02 State Board entry argues it does *as structured data* (`derived_model`), while separately arguing the CDS-style prose disclosure in `pyq_note` is the WRONG surface for a textbook — and these 389 rows would take the former, not the latter. | **DECLINED 2026-09-09 — user: *"leave them as it is."*** Raised with a 360 (scope · blast radius · does-it-really-apply · risk · cost · recommendation) and declined. **Do NOT re-raise for these 389 rows.** What is still open and is a different item is the forward-looking gate — see the `## 2026-09-09` suggestion of the same date — because without it this count grows with every future NCERT chapter, and the decline covers the rows that exist today, not the absence of the guard. |
| **A PUBLIC CDS QUESTION UNDERLINES THE WRONG WORD, so the stem asks about `licence` while the key and all three distractors define a VENDOR. Found 2026-09-08** while authoring the Cadet Vocabulary book's L-N chapter, when the cluster cross-check refused to match the paper's key to any sensible synonym of *licence*. The row: *"Vendors must have \(\underline{\text{licence}}\)."* with options `A One who drives a car / B One who works in a hospital / C One who is engaged in food serving / D* One engaged in selling`, and the **stored solution says so outright** — *"A vendor is a person who sells things, i.e. one engaged in selling."* So the question is answerable and correctly keyed; only the underline points at the wrong word. This is the `undFirst` silent-no-op class the 2026-08-25 CDS repair pass fixed 12 instances of — **the repair was scoped to the 20 rows identified then, so this one was never in scope** and nothing has swept for it since. | **1 PUBLIC CDS English row**, `d421046b-add2-42c8-8da9-33df62cd999e` (`Eng_CDS_2018_1.pdf`). Severity LOW for a student — the key is right and the sentence has only one plausible target once you read the options — but it is a live defect in a published mock (`cds-2018-i-english`) and it silently corrupts anything that reads the underline as "the word being tested": the Cadet Vocabulary extractor pulled `licence` into Part 2 off it, which would have printed a word the exam never asked. **The fix moves the markup only** — `\underline{}` from `licence` to `Vendors` — so it changes the STEM and therefore `content_hash`, which means the mock's question ref must be preserved by updating in place (the `apply-underline-fixes.ts` pattern, which re-stamps rather than delete-and-recommit) and the fix mirrored to `scripts/cds/` records. Worth pairing with a re-run of `npm run audit:underlines` scoped to CDS: this row proves the 2026-08-25 sweep was not exhaustive, and a standing probe comparing the underlined word against the key's semantic class does not exist. | **Identified 2026-09-08; adjudicated against the stored row and its own solution; NOT fixed.** Shipped PUBLIC content inside a published mock, so it needs a 360 + explicit permission. The vocab book has already withheld `licence` from print (`excluded: true` with the reason in its `note`), so nothing downstream is asserting the false claim while this waits. |
| **SIX PUBLIC MCQs SHIP AN OPTION THAT IS A MARKUP FRAGMENT OR NOTHING AT ALL — and the honest count took three attempts to reach. Found 2026-09-07** during the `NDA2_2026` dedup gate, when an agent flagged `00526aaf` (PUBLIC practice, `NDA_Maths_Mock_Test_03.docx`): its stem TRUNCATES mid-formula (`Which one of the following is correct | (a) $A \cup P(A) = P`) because option (a) leaked into it, and **option A's stored text is literally `$`**. Unanswerable as rendered. **The measurement is the lesson.** A first scan for `length(btrim(text)) <= 1` returned **4,657 rows across 13 exams** — nonsense, because it matches every legitimate single-character option (`0`, `1`, `n`, `pi`). Restricting to actual markup fragments gave **381**. But 375 of those are **JEE image-options**, where empty text is CORRECT and the option is a picture (`o.image_url is not null`) — the class this repo's own JEE audit already labels `IMAGE_OPTIONS`, *"what a drawn option looks like, not a defect"*. The honest figure is **6**. Two of my three measurements were wrong, in the same session in which I logged the ledger entry warning about exactly this. | **6 PUBLIC rows, and severity is LOW because the key is on a valid option in all six** — no student can have been mis-graded; the damage is one unreadable distractor. `c7fe8b61`, `881fc463`, `122be589` (JEE Mains pyq, empty option, 0 papers / 0 answers) - `00526aaf` (NDA practice, option A = `$` **plus a truncated stem**, 0/0) - **`de5122c4` and `707e673d` (NDA pyq, `NDA_Maths_Sep2024_QuestionBank.xlsx`, option text `-`, 5 student answer-events EACH)** — those two are the only ones real students have met. `00526aaf` is the worst of the six because its stem is damaged too, so repairing the option alone leaves it unanswerable. Fixes change `content_hash` (options are hashed), so recompute via the real `contentHash` helper and mirror to any records file. | **Identified 2026-09-07; measured honestly at the third attempt; NOT fixed.** Shipped PUBLIC content, so it needs a 360 + explicit permission. **Worth pairing with a standing probe** — nothing detects this class today: `audit:keys` checks duplicate options and not-exactly-one-correct, but never asks whether an option is READABLE. The probe must exclude image-options or it reports 375 false positives on JEE alone. |
| **A PUBLIC practice row keys an ANGLE THAT MAKES ITS OWN EQUATIONS UNSOLVABLE, and its correct twin sits in the same bank. Found 2026-09-07** during the dedup gate for the `NDA2_2026` test series. The item: *"In \(\triangle PQR\), if \(3\sin P+4\cos Q=6\) and \(4\sin Q+3\cos P=1\), then the angle \(R\) is"*. Squaring and adding gives \(25+24\sin(P+Q)=37\), so \(\sin(P+Q)=\tfrac12\) and \(R=\pi/6\) **or** \(5\pi/6\) — and the second branch is impossible: \(R=5\pi/6\) forces \(P+Q=\pi/6\), whence \(3\sin P+4\cos Q<1.5+4=5.5<6\). **Verified numerically as well as algebraically**: a 400k-point scan of the \(P+Q=5\pi/6\) branch reaches residual **0.000022** (a real solution) while the \(\pi/6\) branch bottoms out at **2.098** (none). | **1 PUBLIC practice row is wrong**: `6308aaf7-9f79-4b13-a4b6-3f8a4e44fb42` (`NDA_Maths_Practice__Trigonometry__Trigonometric_Identities.pdf`) keys **D = 5π/6**; correct is **A = π/6** in that row's own option order. Its twin `20b4af47-ffdb-496b-a68f-1b6a0aa60353` (PRIVATE, `LWS_Maths_Mock_2_20-7_6M.pdf`) keys π/6 **correctly**, so the bank contradicts itself and one copy is provably right — unusually strong evidence. **Blast radius is small and measured: 0 student answer-events, 0 papers for the wrong row** (the correct PRIVATE twin is in 1 paper). No gate sees it — the stored solution names no option letter, so `audit:keys`' solution-contradicts-key probe cannot fire; this is the stealth wrong-key class. The answer is in `content_hash`, so a flip must recompute via the real `contentHash` helper. | **Identified 2026-09-07; adjudicated independently (algebra + numeric scan); NOT fixed.** Shipped PUBLIC content, so it needs a 360 + explicit permission. The key was visible during adjudication, so it also owes a BLIND pass. |
| **THE NDA MATHEMATICS BANK CARRIES SUBSTANTIAL INTERNAL DUPLICATION, and the count depends entirely on the normaliser. Found 2026-09-07** across five independent dedup agents run over the `NDA2_2026` test series, each of which reported duplicate GROUPS among the rows it matched (11, 12, 15 and ~20 respectively) without being asked to look for them. Nearly all are benign re-typings — `\frac` vs `\dfrac`, `sinx` vs `\sin x`, permuted options — that survive `content_hash` because it is computed over exact text. **Every group that was checked agrees on the ANSWER**, and one agent noted the right way to check: compare answer TEXT, not answer LETTER, since one triple keys D, D and B while all three mean **0**. | **Between ~91 groups / 189 rows and ~161 groups / 337 rows (~176 redundant), and the spread is the point.** A stricter key gives 91; an aggressive key that strips all LaTeX commands and non-alphanumerics gives 161 — but that key collapses `\sin^{-1}` and `\tan^{-1}` both to `1`, so it can merge genuinely different questions and is an UPPER bound only. A 4-group sample of the aggressive key's output was 4/4 genuine. **Do not quote either number as fact without adjudicating** — the ledger's own MHT-CET duplicate-option entry made exactly this mistake (82 reported, 49 honest). Two pairs are already adjudicated as effectively exact: `01509836`/`135fb52e` and `0b429c2c`/`c430a6cf`. One pair genuinely DISAGREES on content: `22444457`/`1f87e476` differ in option A's numerator (219 vs 291 / 364), so one is a transcription error. | **Identified 2026-09-07; NOT fixed, and NOT yet honestly counted.** Shipped PUBLIC content. The first step is a real measurement with an adjudicated normaliser, not a cleanup. |
| **AN ENTIRE 10-QUESTION PYQ BLOCK IS KEYED "A" NINE TIMES AND IS WRONG EIGHT TIMES — a broken key column, adjudicated question by question against the printed UPSC booklet. Found 2026-09-07** while grading LWS "PQRS Test 1". `GAT_NDA1_2022_PYQ.xlsx` Q21-Q30 (NDA 2022 (I) GAT, *Ordering of Words in a Sentence*) keys **A on 9 of its 10 rows**; every other source in that chapter sits at 30-60% max share, so this is a defect signature, not chance. All ten were read off `C:/tmp/PYQPs/NDA/GAT_Edited/GAT_2022_NDA1.pdf` (booklet SDFR-F-TAG/44A, printed pp.5-7): **our parts and all four options are byte-faithful to the paper in all ten**, so the defect is in the key column alone. **Only Q27 and Q28 are correct.** Corrected readings: **Q23→C** (QSPR), **Q24→C** (SPRQ), **Q25→B** (RPSQ), **Q26→D** (RPQS), **Q29→C** (RSQP), **Q30→B** (SRQP) — each the unique sequence that yields a grammatical sentence, with the keyed A broken in every case (e.g. Q30's A strands "faith and belief are" at the end). **The booklet prints NO answer key**, so the preserve-the-paper's-key convention does not apply: the stored letters came from a prep-house Answer column, which the 2026-08-26 precedent already treats as a derivation. Q21/Q22 of the same block were corrected the same day (see the Status column). | **6 PUBLIC pyq rows, all in the published `nda-2022-apr-gat` mock** (`91d742bb` Q29 · `2100ffe1` Q24 · `97602b27` Q25 · `f6a63ef9` Q30 · `6c8d9296` Q26 · `56ee0236` Q23). Blast radius measured, not estimated: 4 submitted attempts, 23 answer-events across the six, and a regrade would **RAISE 16 and LOWER 5** — so unlike the three already fixed this is *not* uniformly in students' favour, and `regrade-attempt.ts --allow-lower` would be required. `56ee0236` is also printed Q30 of the `lws-pqrs-test-1` paper, so its records file needs the mirror. **Note the OMR mismatch report cannot find these**: the teacher's own Evalbee key agrees with the bank on Q23, so both are wrong together — a disagreement report only finds where two sources differ, never where they are wrong in the same direction. Method is settled and cheap: `apply-adjudicated-fixes.ts` (assert `from`, flip to `to`, rewrite the solution, recompute `content_hash` in place so `paper_questions` is not orphaned) + `record-key-fixes-*.ts` + `regrade-attempt.ts`. | **Identified 2026-09-07; adjudicated against the printed paper; NOT fixed.** Shipped PUBLIC content AND live student scores, so it needs a 360 + explicit permission — in particular a decision on lowering 5 marks a student has already seen. |
| **THE SAME QUESTION IS IN THE BANK TWICE WITH THE SAME FOUR OPTIONS AND OPPOSITE KEYS, so one of them is provably wrong. Found 2026-09-07** during the dedup gate for the LWS "PQRS Test 1" paper. The item is a P/Q/R/S rearrangement — P "was the victory secured by the women's hockey team", Q "in the arena of sport, perhaps", R "against Australia in the Tokyo Olympics", S "the greatest moment in Indian Olympic history". `56ee0236-206c-4aae-b991-a95392fd8a98` (`GAT_NDA1_2022_PYQ.xlsx` Q23, PUBLIC pyq) keys **A = SPQR**; `563bfed0-8858-4147-a05c-32e92527fbe5` (`Oswaal_NDA_GAT_Mock_Test_5.pdf` Q42, PUBLIC) keys **C = QSPR**. Both print the identical option set A=SPQR / B=RPQS / C=QSPR / D=QRPS, so this is a straight contradiction, not two different questions. **QSPR is the grammatical reading** — "In the arena of sport, perhaps, the greatest moment in Indian Olympic history was the victory secured by the women's hockey team against Australia in the Tokyo Olympics"; SPQR strands "in the arena of sport, perhaps" between "team" and "against Australia". **No gate sees it**: they survive `content_hash` because the two typings differ (curly vs straight apostrophe, `/` separators, a trailing instruction line), and `audit:keys` is scoped to `question_kind='practice'` so it never scans the pyq row. | **2 PUBLIC rows, of which 1 is wrong — almost certainly the PYQ row `56ee0236`.** Evidence beyond the reading: the Oswaal editors independently keyed C, and the bank's own dedup-agent pass reached C from the text alone. Counter-consideration: the repo convention PRESERVES a printed paper's official key even when wrong, because students were marked against it — but `GAT_NDA1_2022_PYQ.xlsx` is a **prep-house Excel**, and the 2026-08-26 precedent established that such an `Answer` column is a derivation rather than a published key, which is what made those four NDA key corrections legitimate. **Settle it against the actual UPSC NDA 2022 (I) GAT paper before flipping anything.** The answer is in `content_hash`, so a flip must recompute via the real `contentHash` helper and mirror back to any records file. Worth handling the duplicate pair in the same pass — the same item also exists twice for Q4 of that paper (`339698ba` / `75d44c46`), though that pair has different option sets and is NOT a key conflict. | **SUPERSEDED / PARTLY DONE 2026-09-07.** The source paper WAS located (`C:/tmp/PYQPs/NDA/GAT_Edited/GAT_2022_NDA1.pdf`, booklet SDFR-F-TAG/44A p.7) and read: it confirms our parts and options and prints no key, so the conflict is settled — **`56ee0236`'s A is wrong and Oswaal's C is right**. It is NOT fixed, because it turned out to be one of eight wrong keys in that block; it is now tracked by the row above, which carries the measured blast radius. The Q4 pair (`339698ba` / `75d44c46`) remains a genuine duplicate but not a key conflict. |
| **A SHIPPED SCRIPT'S WARNING MESSAGE STATES THE SORT ORDER BACKWARDS, and it is the kind of claim a reader will take on trust. Found 2026-09-07** while adding three subtopics for the APJ Biology ingest. `scripts/sync-subtopic-order.ts:146` warns that a DB subtopic absent from a notes `subtopicOrder` is *"left with no teaching order (**will sort ahead of ordered siblings**)"*. It sorts **LAST**. Migration 0029 says so in its own comment ("NULL means 'no teaching order set', which sorts LAST"), `src/lib/questions/facets.ts:23` ranks `orderIndex ?? Infinity`, and `src/lib/questions/query.ts:217` passes `nullsFirst: false`. **The trap is that Postgres defaults ASC to NULLS LAST while a hand-written diagnostic query may say `nulls first` and appear to confirm the warning** — which is exactly how I misread it before checking the sort code. | **One line in one shipped script.** No data is affected and nothing renders wrongly today; the cost is purely that the warning misinforms whoever reads it, and it sits at the precise moment someone is deciding whether an unordered subtopic is a problem. Fix is a one-line message correction to "will sort last, after the ordered siblings". Worth pairing with the open question it exposed: a chapter with /notes cannot give a NEW subtopic a position ahead of its noted ones without a value < 1, because `notes:order` rewrites the noted subtopics to 1..N on every run — so *Biodiversity and Classification* now renders Fungi/Plant/Animal before Systematics/Monera/Protista, which is backwards pedagogically. | **Identified 2026-09-07; NOT fixed.** Shipped script, so it needs permission. The message fix is trivial; the ordering question is a real design call (negative indices vs extending the notes module) and should not be decided in passing. |
| **`verify-commit.ts` REPORTS FOUR FAILURES ON A CORRECT INGEST when a question dedups byte-exactly against a row from a DIFFERENT `source_file`. Found 2026-09-07** ingesting the APJ Biology test. Its Q21 is a verbatim reproduction of NDA II 2018 GAT Q60, so `content_hash` correctly reused the existing PYQ row (`19496b5c`, `NDA2_GAT_2018_PYQ.xlsx`) instead of inserting a copy — `commit-paper` handles this deliberately ("resolved 1/1 skipped exact-dup rows via content_hash"). But every one of `verify-commit`'s checks is scoped by `source_file`, so it reported row-count 49-vs-50, a question_number-set mismatch, a foreign paper question, AND a broken OMR order. **All four are the same artifact and the underlying invariants hold** — verified by hand: the reused row sits at paper position 21, the correct printed slot, and all 49 others have position == printed number. **Why it has never fired before:** a *semantic* dup has different text, so it inserts as its own row; only a *byte-exact* dup collides on the hash, and this is the first paper to contain one. | **One shipped script, `scripts/practice-paper/verify-commit.ts`.** No data is wrong. The risk is that the gate now cries wolf on a legitimate outcome, and the next person either burns time re-deriving it or learns to wave the gate through — which is worse than not having it. The fix is to resolve each paper link's identity rather than assume its `source_file`: treat a link whose row was reused via `content_hash` as satisfying the count/number/ownership checks, and validate OMR parity on paper POSITION order (which is the invariant that actually matters) rather than on the source-scoped row set. Small and self-contained; the records file already knows which `n` deduped. | **FIXED 2026-09-07 — but WITHOUT the permission this ledger asked for; flagged to the user, revert is one commit.** Forced by the next ingest: LWS "PQRS Test 1" is **30 of 30** bank-mirrored (a whole test assembled from existing NDA GAT PYQs), so the gate reported 4 FAILs on a wholly correct ingest and there was no honest way to report the ingest verified. `verify-commit.ts` now resolves each record the same way `commit-paper` does — `source_file` first, then `content_hash` — and prints a `note` naming how many questions are bank-mirrored; the practice-kind and dedup-gate checks are scoped to rows the ingest actually CREATED (a mirrored row's visibility was decided by whoever committed it first and this paper cannot leak it), and OMR parity is now checked on the resolved row identity rather than on `question_number`, which a mirrored row carries from its original paper. A `createPaper:false` fix rode along, mirroring `commit-paper`'s own `commitRecs` rule so the 9 uncommitted dup/flawed records of a bank-only ingest stop reading as missing. **Swept across all 65 registry specs, old build vs new: 0 regressions, 27 papers fail -> pass, 26 still failing on unrelated pre-existing causes.** The 4 FAILs this row describes on `apj-bio-t01` are now 0. |
| **~6 SHIPPED NDA Biology rows are filed under a kingdom subtopic while testing SYSTEMATICS, and 4 more sit in the wrong CHAPTER entirely. Found 2026-09-07** during the APJ Biology dedup gate. Within *Biodiversity and Classification*: the binomial-nomenclature row (`860087ed`), the taxonomic-hierarchy row (`fe8a54b0`), "smallest unit of classification" (`d747b8df`) and "who popularized embryological characters in taxonomy" all sit under **Animal Kingdom Classification**; "Five-Kingdom classification was given by" (`a836af8a`) sits under **Plant Kingdom Classification**; an Amoeba statement-question sits under Animal Kingdom although it is Protista; and `19496b5c` — a cross-kingdom comparison — sits under **Kingdom Fungi**. Cross-chapter: `4aaa72e8` (an **electrocardiogram** question) is under *Pathogens and Diseases*, and `17c7936e` (elephantiasis) + `9d86eac0` (milk curdling) are under **Antibiotics — Discovery**. **This is not accidental drift** — the shipped `/notes` chapter deliberately folds systematics into two kingdom subtopics (`fungi.ts` is titled "The Five Kingdoms and Kingdom Fungi", `animal-kingdom.ts` "Naming, Hierarchy and the Animal Kingdom"), which is exactly why the questions were filed that way. | **~10 PUBLIC rows.** A `Systematics and Nomenclature` subtopic now exists on that chapter (created 2026-09-07 for the APJ ingest), so the in-chapter rows finally have a correct home. Re-filing is a `subtopic_id` UPDATE — **no stem changes, so no `content_hash` moves** and nothing can orphan a paper ref, review row or bookmark. **But it is entangled with the notes:** moving the naming/hierarchy questions out of Animal Kingdom Classification breaks the correspondence its notes module was built on, so the editorial titles and `subtopicOrder` should be revisited in the same pass, per [[shipped-chapter-rename-downstream-sync]]. The 4 cross-chapter rows are independent of the notes and much cheaper. Also seen while reading: `d9f4883b` prints "**Cyas**" for *Cycas* and is a near-duplicate of `a39092fa` which spells it correctly, plus ~7 triplicate clusters (Leishmania, scientific-name match-list, ribosome-lysosome, boiled-egg-in-brine) and several pairs. | **Identified 2026-09-07; NOT fixed.** Shipped PUBLIC content plus a shipped notes chapter, so it needs a 360 + explicit permission. Do the 4 cross-chapter moves first — they are cheap and touch no editorial. |
| **A PUBLIC practice row keys a fact its OWN SOLUTION REFUTES. Found 2026-09-07** during the dedup gate for the LWS "Pressure Belt, POS" paper. `Oswaal_NDA_GAT_Mock_Test_6.pdf` Q120 "Subtropical high-pressure belts are otherwise called as:" keys **B "Roaring forties"**; the correct answer is **A "Horse latitudes"**. The row's own stored solution places the belt "at approximately 30o north and south latitude", while the Roaring Forties are by definition the 40-degree-S westerlies -- a WIND belt, not a pressure belt. **Why no gate sees it:** the solution never names an option letter, so the `audit:keys` solution-contradicts-key probe cannot fire; it is the stealth wrong-key class the 2026-06-03 audit identified, catchable only by re-derivation. | **1 PUBLIC practice row**, `e4d3de0b-e1b1-4e98-a0b4-03a475a3328c`. Only 2 rows bank-wide mention horse latitudes and the other (`d0675957`) does not key the identity, so there is NO sibling to cross-confirm -- the evidence is the unambiguous physics plus the row contradicting itself. Fixing it changes `content_hash` (the answer is in the hash), so recompute via the real `contentHash` helper; the source `.pdf` is not a tracked artifact, so there is no source of record to mirror the fix back to. | **DONE (2026-09-07)** - key flipped B->A + solution rewritten via `apply-adjudicated-fixes.ts` (extended with a `answer` key-flip capability), mirrored to `oswaal-gat-mock-6.records.json`, hash recomputed 8731db68->f54c4304, and recorded in `question_reviews` as `solution_audit`/`key_fixed` under run `key-fix:2026-09-07-lws-pressure-belt-ingest`. Still owes a BLIND pass: the key was visible throughout this adjudication. |
| **AN EATEN BACKSLASH LEAVES A CONTROL CHARACTER INSIDE THE LaTeX, AND EVERY EXISTING CHECK IS BLIND TO IT. Found 2026-09-07 by doing it to myself** while hand-patching a `context` through a `python - <<PY` heredoc. The shell ate one backslash level, so `\text{cm}` resolved to **TAB + "ext{cm}"**. `\t \b \f \v` are all valid LaTeX command starts (`\text`, `\beta`, `\frac`, `\vec`), so this class is systematic, not a one-off. **Why nothing catches it:** the JSON still parses, the `\(…\)` delimiters still balance, `audit:text` checks other classes, and — the load-bearing part — a control-character scan that whitelists TAB as "benign whitespace" cannot see the commonest case. My own validator had exactly that whitelist and returned a clean bill on a corrupt file; the TypeScript compiler, not any probe, is what caught the second instance. | **15 PUBLIC rows across 7 exams**, measured bank-wide over 70,914 questions with `position(chr(N) in …)` (a `LIKE '%\t%'` scan does NOT work — LIKE treats backslash as its own escape). TAB in 3 stems + 11 solutions, plus 3 form-feed and 4 backspace. By exam: **CDS Maths 3** (2018/2019/2022 papers), **Worksheets 3** (Complex Numbers 06-2/06-5/06-7), **MH HSC Cl-12 4** (Mathematical Logic `1.2 Ex 1.2 Q.2 (iv)` — a truth-table header where `\to` became TAB+`o`, so the column reads `q o p` instead of `q → p`; plus 3 in Line and Planes), **JEE Mains 3**, **NDA Physics 1**, **Foundation Physics 1** (`Sound WS1` Q62, `\(f = 100\ \text{Hz}\)` rendering as `100 <TAB>ext{Hz}`). The corresponding source JSONs carry it too (`foundation/data/sound-1.*`, `stateboard/data/logic-12.s12.solutions.json`), so a repair must fix BOTH or the next re-commit reverts it. **Render impact is UNMEASURED** — check a rendered card before claiming any row is broken; it may degrade rather than break. Stems are part of `content_hash`, so those 3 need `repair-paper-text.ts` (in-place update + hash re-stamp), while solution-only edits are a plain UPDATE. | **Identified 2026-09-07; NOT fixed.** Shipped PUBLIC content, so it needs a 360 + explicit permission. A guard is already live in `scripts/ncert/commit.ts` (refuses `[\t\f\v\b\0]` in stem/context/solution, proven to fire on all three) — **the same guard belongs in the shared `scripts/stateboard/lib.ts` core that ncert/mh-sb-9/mh-sb-11 re-export, but adding it there would refuse a re-commit of the 2 foundation + 1 stateboard files above until they are cleaned, so it is gated on this backfill.** |
| **A QUESTION WITH TWO IDENTICAL OPTIONS CANNOT BE ANSWERED, and the defect is invisible to every gate we run. Found 2026-09-07 while building the MHT-CET Maths book.** The user spotted one on screen: `9 May 2023 Shift 2` Q108 prints options `29 / sqrt(29) / 26 / sqrt(26)` and the bank stores `sqrt(29) / sqrt(29) / sqrt(26) / sqrt(26)` — radicals were added to (a) and (c) at transcription, so the key (b) is correct yet identical to (a). Confirmed against the printed paper. `audit:keys` would catch this class but is scoped to `question_kind='practice'`, and MHT-CET is entirely `pyq`. **Measurement trap, paid for twice:** a naive normaliser reports 82 because it strips en-dashes (`-3` and `3` collapse) and interval brackets (`[1,7/3)` and `(1,7/3]` collapse); stripping LaTeX delimiters is then also required or `(4)` and `4` read as different. The honest figure is **49**. | **49 of the 2,136 questions the book prints (2.3%) have duplicate options** — 57 questions have one duplicate pair, 20 have two, 5 have three, before the normaliser correction. Every affected sitting's source booklet and worked answer key is on disk under `C:/tmp/PYQPs/MHT-CET/<year>/`, and the repair is usually ONE option's text, not the key. The method is proven: 7 defects were adjudicated and fixed this way on 2026-09-06 (`scripts/books/apply-source-fixes.ts`), which asserts both current and target state, refuses the batch on any mismatch, and records `question_reviews` under `source_key_crosscheck`. **Do NOT recompute `content_hash`** — that would make a re-upload of the original defective spreadsheet insert a duplicate of the repaired row. | **Identified 2026-09-07; NOT fixed.** Shipped PUBLIC content, so it needs a 360 + explicit permission. Worth pairing with a standing probe: extend `audit:keys` to cover `pyq`, or add a books-scoped duplicate-option check, since nothing detects this class today. |
| **A STRAY DOUBLE BACKSLASH INSIDE AN INLINE MATH ZONE — `\(\\frac{8}{9}\)`. Found 2026-09-06 while adjudicating an MHT-CET Maths answer against its source paper.** `\` is a LaTeX line break, so a zone written `\\frac{...}` renders a break before the fraction rather than the fraction alone. It reached the bank through an .xlsx upload, not a pipeline, so no ingest gate ever saw it. **Note the measurement trap**: a SQL `LIKE '%\\%'` scan returns 0 because LIKE treats backslash as its own escape character — use `strpos over three chr(92) values concatenated`. | **147 options + 46 stems bank-wide carry three consecutive backslashes; 79 options across 25 questions are MHT-CET Maths alone.** Four of those options were repaired in this session because they sat on a question whose ANSWER was also wrong (`17/9` for `sqrt(17)/9`), so the class is confirmed to co-occur with real defects. **NOT a blanket fix**: `\` is legitimate as a row separator inside `\begin{matrix}`, so a sweep must exclude math zones containing an array/matrix environment, and the render impact (KaTeX on web vs temml->OMML in the Word export) is UNMEASURED — it may degrade rather than break. Scope the probe by `source_file` and check a rendered card before claiming any row is broken. | **Identified 2026-09-06; NOT fixed.** Shipped PUBLIC content across several exams, so it needs a 360 + explicit permission. Start by measuring the render, not by editing rows. |
| **Three MHT-CET papers were uploaded twice, and because the two uploads were typed independently `content_hash` deduped only the rows whose typing matched — so each pair left BOTH copies of the rest.** Found 2026-09-06 while closing the held mocks. The pairs are `16thMay2023_Shift2`/`2023_Analysis`, `12thMay2024_Shift2`/`13thMay2024_Shift1` and `2025_Apr_19_S2`/`2025_PCM`; each union is exactly 150, proving one paper. | **~200 near-duplicate rows live and PUBLIC in `/browse`** — 97, 107 and 142 question numbers appear under both labels of their pair, of which 85, 93 and 103 carry a provably identical normalised stem. They survive dedup only because their OPTION text differs enough to change the hash. Cost: MHT-CET's public count is inflated by ~2-3%, and any per-chapter weightage or %HARD figure derived from it is skewed by the same amount — including the numbers in the shipped `/guide/mht-cet-maths`. The mocks are NOT affected (they now merge and de-duplicate at build time). Fixing means choosing a winner per pair and deleting ~200 PUBLIC rows, which needs the better transcription identified per question, not per file. | **Identified 2026-09-06; NOT fixed.** Shipped public content, so it needs a 360 + explicit permission. Note the delete is not mechanical: the two copies differ in option text and neither is automatically the better one. |
| **A CHAPTER BOUNDARY IS NOT A PDF BOUNDARY — a shipped chapter is missing an entire exercise block because the book printed it inside the NEXT chapter's pages. Found 2026-09-03 while reading Ch.11's exercise page for an unrelated cross-check.** Balbharati Std XII Physics prints an `Exercise : Chapter 10` box on **p-13 of `11. Magnetic Materials.pdf`**, carrying five theory questions that belong to Chapter 10. `scripts/stateboard` transcribes each chapter from its OWN pre-split PDF, so nothing ever looked there. The tell is structural and was visible in the data all along: Ch.10 is the only chapter of this book whose `Ex Q.2` has no `(i)-(v)` sub-items, because **its own PDF has no `2. Answer in brief.` block at all** (verified by scanning all 21 of its pages — it goes `Exercises` -> `Choose the correct option` -> straight to numerical Q.2). **No gate can see this**: the chapter committed cleanly, `backfill-sections` reconstructed its outline with 0 orphans, `board:lint` is green, and the five questions simply never existed. | **ONE shipped chapter — `magnetic-fields-current-12-phy` (Magnetic Fields due to Electric Current), 33 q PUBLIC — is missing 5 exercise questions.** The five are: distinguish the forces on a moving charge in a uniform E vs a uniform B; the condition for uniform circular motion in a magnetic field + cyclotron + frequency of revolution; what is special about a radial magnetic field and why it is useful in a moving-coil galvanometer; state Biot-Savart law and apply it to (i) an infinitely long conductor and (ii) a point on the axis of a circular loop; state Ampere's law and explain its uses. Confirmed absent from Ch.10's transcription by grepping its stems (its Q.13/Q.14/Q.17 are cyclotron/galvanometer NUMERICALS, not these). **Not systemic — measured, not assumed:** a scan of all 16 Std XII Physics PDFs for `Exercise : Chapter N` finds exactly one occurrence, this one. The repair is additive (a new band file -> `merge` -> `commit` -> author 5 solutions -> sections -> flip), so it mints only new rows and **cannot orphan an existing paper ref, review row or bookmark** — no stem changes, no `content_hash` moves. Worth adding a standing probe in the same pass, since nothing detects this class today. | **Identified 2026-09-03; NOT fixed.** Shipped PUBLIC content, so it needs a 360 + explicit permission per the learning-propagation protocol. Low risk and low blast radius, but it is net-new content on a published chapter and the five answers would need authoring under the same key-less regime (blind derivation + grounding) as the rest of this lane. |
| **THE P1 GATE DETECTS A DEFECT CLASS ITS REPAIR CANNOT FIX — a live deadlock, and Blueprint Mock 5 is currently BLOCKING on it. Found 2026-09-02 from a rendered card.** `paper-text.ts` says in its own comment that P1 has TWO halves: the literal-word form (`STATEMENT_LABEL` = Statement/Assertion/Reason + a delimiter) and the bare-numeral form (`findStatementLabels`). Only the SECOND half has a repairer. `layoutStatements`' `SEQUENCES` carries three styles - `1.`, `(1)`, `I.` - **all dot- or paren-delimited and none colon-delimited**, so `Statement I:` matches no token, `labels.length < 2`, and it returns `changed: false`. A paper holding such a row is blocked by a BLOCKING rule with no automated way out - exactly the deadlock the module's own comments describe for match-lists, now live for statements. **MEASURED, and it refuted the first hypothesis:** the existing repair fixes **0 of 327** candidates. The cause is NOT the 2026-09-02 exam allow-list (which excluded JEE/NEET/MHT-CET for the bare-numeral false positives) - NDA's 22 rows were fully in scope and are equally unfixable. | **327 PUBLIC rows** - JEE Mains 264 - NEET 29 - NDA 22 - Foundation 8 - MHT-CET 2 - Worksheets 2. Twelve sampled by hand, **zero false positives**: all are 'Statement I: ... Statement II: ...' run together. Independently corroborated from the sources: four separate NDA PYQ pages read this session (2017-II Q105, 2019-II Q114, 2021-I Q94, 2023-I) all print their statements on SEPARATE INDENTED LINES, so flat storage is unfaithful to the paper, not merely ugly. **The fix looks small and carries far less false-positive risk than the numeral styles it would sit beside**: add a colon-delimited literal-word style, which cannot occur as ordinary prose - which is why the exam exclusions were needed for bare numerals and would NOT be needed here. **The stem is part of `content_hash`**, so this is an in-place update + hash re-stamp, NOT a plain UPDATE - use the `repair-paper-text.ts` mechanism, which exists for exactly this and carries the guards (recompute the current hash first, check the new hash for collision in the same org+exam, re-audit through the real rule module). **URGENT SUB-ITEM: `npx tsx scripts/bank-paper/audit-paper-text.ts f918efd4-e03f-4392-8bbf-6f5a4783b698` reports 1 BLOCKING P1 violation on Mock 5 (Binomial Theorem Q51, the JEE 2026 fill). That paper shipped with its asserts reported clean because this gate was never run on its final state.** | **analysed - awaiting-permission** (2026-09-02) |
| **A PRINTED DATA TABLE CAN ARRIVE FLATTENED INTO PROSE, and NO rule detects it. Found 2026-09-02 from a rendered card; all four instances then source-verified against the scanned papers.** The stem still reads well and the question is still answerable, so every gate passes: `audit:text` checks other classes, P2 only fires on match-lists, and there is nothing malformed to find. Two of the four stems ANNOUNCE the missing structure in their own words ('The following table gives...'), which is the strongest internal evidence available. The papers themselves are consistent about which shape is which: **a raw data list is printed as prose and is faithfully stored as prose** (2023-I Q110/Q112, 2021-I Q107 all verified), while an x/f frequency distribution is printed as a bordered table - so the probe must require TWO parallel runs of numbers, not one. | **Exactly FOUR PUBLIC rows, all NDA Mathematics, ALL FROM `.xlsx` UPLOADS** - the defect the standing 'for PYQs refer to the actual PDFs, not the Excel' rule exists to catch. All four read off the printed page: `548cc682` (2019-II Q110, x 1-8 / f 3,15,45,57,50,36,25,9 - **and this row is Mock 5's Q50**), `b76c9357` (2023-I Q111, x 1,2,3,5 / f 4,6,9,7), `5d585188` (2021-I Q105, Number of peas 1-7 / Frequency 4,33,76,50,26,8,1), and the worst, `67d87dff` (2017-II Q114) - a THREE-column table with a spanning `Expenditure (in ₹)` header, where flattening lost the header AND the currency and left three parallel lists for the reader to re-pair by eye. **Restoring these means editing the STEM, which IS part of `content_hash`** - so unlike the orphaned-context repair above this is an in-place update + hash re-stamp via `repair-paper-text.ts`, never a delete-and-re-commit (which would mint fresh uuids and leave Mock 5's ref dangling - a dangling paper ref renders a BLANK question with no error and marks every attempt wrong). Worth adding a standing probe at the same time, since nothing detects this class today. | **analysed - awaiting-permission** (2026-09-02) |
| **A question can be UNANSWERABLE AS STORED because its data table is missing, and the builder's RULE 1 exclusion is structurally blind to it — the filter drops the sibling that survived and admits the one that broke. Found 2026-09-02 while reviewing Blueprint Mock 5; sources located and read the same day.** RULE 1 tests for a context that IS PRESENT, so it cannot see one that is REQUIRED AND MISSING: the predicate points the wrong way for this failure mode. **Two distinct causes, both landing in the same blind spot.** (a) *Displaced in the SOURCE* — in Paper 1 the Q113 table FLOATED out of the left column and landed mid-way through Q119's option block in the right column (a Word anchor accident, visible on the rendered page), so it is adjacent to Q113 in no reading order and the ingest never had it. (b) *Dropped at INGEST* — in Paper 2 the Q80 table is printed directly beneath its own stem and was simply not carried across. | **Exactly TWO rows, both PUBLIC, both `question_kind='practice'`, both LWS mock papers** — measured, not estimated, by two differently-shaped probes that agree: a first probe matched 145 and was almost entirely false positives (*"which of the above statements"* is the standard NDA phrasing for a statement list carried in the stem itself). **SOURCES FOUND 2026-09-02** at `C:\Vilas\LWS_Pune\NDA_Subjects_Content\Test_Series\NDA Math 1 to 10 - Complete Mock\Questions_paper\` (per-mock subfolders; born-digital PDFs with clean text layers, plus the printed answer keys) — so **BOTH tables are recovered and both rows are repairable**. (1) `38a90f9a-4baa-47b4-8aa8-44cdcb79aae8` (Mock_Test_01 Q113): the table is *Class interval 0-5 / 6-10 / 11-15 / 16-20, Frequency 3 / 7 / 6 / 5*. **DO NOT copy Q114's context across, which was this ledger's first suggestion and is WRONG** — that table (0-10…40-50, freq 5/10/20/5/10) puts the median class and the modal class both at 20-30, making statement 1 TRUE and keying (a), while the printed AK says **D**; the recovered table gives median class 11-15 vs modal class 6-10, statement 1 FALSE, statement 2 FALSE, hence D. The wrong table would have made the row answerable AND WRONG, and only the printed key distinguishes them. (2) `f75633b1-a8b0-4626-8aa6-ed9b52fd85e9` (Mock_Test_02 Q80): the table is *x 1 / 2 / 3 / 4, Frequency 2 / 3 / f / 5*. **Both stored keys are already correct** (D and B, matching the papers' own answer keys), and Q119/Q120 options survived the float intact. **The repair is a plain UPDATE of `context`: `contentHash` for an MCQ is question + sorted options + answer and does NOT include `context`** (`src/lib/upload/hash.ts`), so row identity cannot move and no paper, mock ref, review row or bookmark is orphaned. Reversible by setting `context` back to NULL. **Separately worth telling LWS:** Paper 2 Q80 as PRINTED says the mean is 32/8, which forces f = -12 — impossible; our stored 23/8 gives f = 6 = the printed key B, so the bank is right and the paper carries a typo. Still open and NOT closed by this: Mocks 04/05/09/10 hold ZERO context rows against 2-36 elsewhere, and neither probe finds an orphan in them — a question that lost its table AND never says "above" is undetectable by phrasing, so clearing them needs a read. Worth deciding separately whether the builder should gain a POSITIVE probe (stem references external data + no context + no image => refuse), since repairing two rows leaves the blind spot open for the next ingest. | **analysed — awaiting-permission** (2026-09-02) |
| **A disclosure stored in `pyq_note` renders INSIDE the bracket the /browse card draws, so it ends in a visible `]]` — and the field is the wrong home for it anyway. Found 2026-09-02 by looking at a rendered card, after every gate was green.** `QuestionCard.tsx` renders the provenance line as `[{provenance}]`, and `formatProvenance` puts `pyq_note` in it verbatim. CDS General Knowledge stores its derived-answer disclosure as `CDS (I) 2019 — General Knowledge [No official answer key is published for this paper. …]`, so the inner bracket nests. Beyond the cosmetics, the placement is wrong on the merits: `pyq_note` has exactly one consumer, the SOURCE line, and a reader looking at the QUESTION has not seen an answer yet — there is nothing for them to mistake for an official key. The structured signal `derived_model` already exists on these rows and never reaches the UI. | **2,280 PUBLIC CDS General Knowledge rows** (every row of the corpus; all 19 papers 2016-II…2026-I). Also worth checking in the same pass: `mh-ssc-10-text` and `mh-sb-9`, which stamp a `pyq_note` clause under the same pattern — their exact wording has not been measured, so whether they nest is unverified rather than assumed. The repair is a one-field text UPDATE scoped by `source_file`; **`pyq_note` is not part of `content_hash`**, so unlike a stem repair it cannot move row identity, orphan a paper ref or stale a `question_reviews` verdict. The real decision is not the brackets but whether the disclosure belongs on the card at all — and for CDS the answer may genuinely differ from the Physics one, because CDS is EXAM PAPERS where 'official answer key' is a real artifact students hunt for, which is exactly the premise that does not hold for a textbook. | **Identified 2026-09-02 while opening the State Board Physics lane; NOT fixed — user's call to review CDS separately.** The Physics chapter took the opposite decision the same day (`pyq_note` = source only; the derived-answer fact kept as `derived_model`/`derived_at` and the flip-public gate re-keyed onto that column rather than a prose match). Whether CDS should follow is a product judgement, not a cleanup — hence a 360 and explicit permission before touching 2,280 shipped rows. |
| **`pyq_note` is free text typed into an uploaded spreadsheet, and at least one of them is provably wrong** — found while building the MHT-CET mock registry (2026-09-01). `MHT_CET_3rdMay2023_S1_QB.xlsx` stores the note **"3rd May 2nd Shift"**, contradicting its own filename, while `MHT_CET_3rdMay2023_Shift2_QuestionBank.xlsx` already legitimately claims that sitting — so taken at its word the bank has two files for 3rd May Shift 2 and none for Shift 1. Settled against the local source booklets rather than by preferring the more official-looking field: stems unique to that file ("watch glass", "coloured compound") appear ONLY in `3 may shift 1 (ques).docx`, and the Shift-2 file's stems only in `3 may shift 2` — the **FILENAME is right and the note is a typo**. | **~150 live PUBLIC rows carry the wrong shift label** in `pyq_note`. It is provenance metadata, so nothing computes from it today and the mocks deliberately take their labels from `scripts/mocks/mhtcetSittings.ts` instead — but it IS rendered as the source citation on `/browse`, so a student comparing two shifts sees one paper filed under the other's sitting. The repair is a one-row-value `UPDATE` scoped by `source_file`, and — worth checking first — **`pyq_note` is not part of `content_hash`**, so unlike a stem repair this does NOT change row identity and needs no delete-and-re-commit. Also worth a wider sweep while in there: 3 other MHT-CET sources carry undated notes ("Shift 1", "Shift \|\|") whose sitting nobody has established. | **Identified 2026-09-01; NOT fixed.** Shipped content, so it needs a 360 + explicit permission. Low risk and low blast radius, but it is a data edit to live rows and the wider sweep needs the same source-booklet verification this one got — a *plausible* date is exactly what must not be written. |
| **The GAT content rules were written down on 2026-08-26 and made enforceable (`scripts/practice-paper/GAT_RULES.md` + `gat-lint.ts`) — which means every GAT paper ingested BEFORE that date was ingested without them.** Rules 1-3 are structural: statement lists (`1.` `2.` `3.`) each need their own line, `P./Q./R./S.` rearrangement parts each need their own line, and a List I / List II or any two-column pair list must be a GFM pipe table (header row AND `|---|---|` separator, or the renderer treats it as prose). The dominant cause is a one-line normalisation habit — collapsing all whitespace (`\s+` -> `" "`) flattens exactly these three shapes while being correct for ordinary prose, whose source newlines really are just wrap points. | **101 BLOCKING findings across 11 shipped GAT papers**, measured by `gat-lint.ts` after two of its own false positives were found and fixed (a correct match-list table's cells look like a flattened statement list; "which pair is not correctly matched" puts its pairs in the OPTIONS, so there is nothing to tabulate — that pass took the count 131 -> 104 -> 101). Spread: `gat-mock-w09` 20, `gat-mock-3` 20, `gat-mock-w011` 18, `gat-mock-7` 13, `gat-mock-21aug` 8, `gat-mock-w07` 7, `gat-mock-w2` 5, `gat-mock-g1` 5, `gat-mock-4` 4, `gat-mock-w03` 3, `gat-mock-w1` 1. Almost all are rule 1 and rule 2. **Cheap and low-risk in principle** — these are presentation defects in `stem` text, not answer defects — but `content_hash` covers the stem, so a repair changes a row's identity: it needs delete-and-re-commit rather than an UPDATE, which mints fresh ids and would orphan any paper ref or authored-solution pairing. A handful are rule 7 (a stem referencing a figure that does not exist), which are NOT presentation defects and are unpublishable rather than untidy. | **Identified 2026-08-26 while building the lint; NOT fixed.** Shipped work, so it needs a 360 + explicit permission. Note the fix is not uniform: the rule-1/2/3 rows are cosmetic-but-hash-changing, while the rule-7 rows are genuine correctness problems and should be triaged separately rather than swept in the same pass. |
| **`verify-commit.ts` reports a FALSE FAILURE for any paper with `createPaper:false` — found 2026-08-26.** That mode commits only `status:"new"` rows (documented in `gat-mock-g1`'s own config comment: *"dup rows ... are never committed — createPaper:false commits new only"*), but the gate's row-count and question-number checks compare the DB against ALL records in the file. So a correct ingest fails, and the failure is indistinguishable from a real one. | **Reproduced on shipped data**: `gat-mock-g1` fails the same two checks today, so this is pre-existing rather than introduced by the Oswaal Mock 10 ingest, which fails three (row count, question-number set, and the shared-passage set — the third because a cloze member held back as `flawed` legitimately leaves 4 of 5 members carrying the context). The fix is to make the gate aware of the mode: expect `new`-only rows when `createPaper` is false, and scope the passage check to committed rows. **The danger of leaving it is habituation** — a gate that always fails stops being read, which is exactly how a real failure gets waved through. | **Identified 2026-08-26 during the Mock 10 commit; NOT fixed.** Shipped tooling, so it needs a 360 + permission. Mock 10's invariants were verified directly by SQL instead (128 rows, all `practice`, all PRIVATE, 0 null solutions/subtopics/chapters, 0 rows with other than 4 options, 0 with other than 1 correct, 0 duplicate options, 4 cloze rows sharing 1 `set_id`) — which is what the gate would have asserted had it been mode-aware. |
| **The bank contains its own near-duplicate pairs — surfaced 2026-08-26 by a dedup pass that was pointed OUTWARD.** While matching 1,350 Oswaal mock questions against the NDA corpus, the scorer repeatedly returned two bank rows for one incoming question. | **Confirmed pairs so far: troposphere, silver tarnishing, acid rain (one Chemistry row and one Geography row with identical options), and temporary hardness of water.** This is a small sample from a scan that was not looking for it, so the real count is unknown and probably larger. The tooling to measure it already exists — `scripts/practice-paper/dump-bank.ts --subject` plus the same IDF-cosine matcher — pointed at the bank against itself rather than against an incoming paper. Note one of the pairs spans two SUBJECTS, so a per-subject sweep would miss it. | **Identified 2026-08-26; NOT investigated or fixed.** Out of scope for the Oswaal ingest, which only needed to know whether an incoming question was already present. Worth its own measured pass before deciding anything: an intra-bank duplicate is not automatically wrong (the Decisions log records a deliberate keep-duplicates-as-recurrence-signal policy for PYQs), so the question is which of these are genuine redundancy in the PRACTICE corpus. |
| **377 CDS English questions reference a passage that was never stored — found 2026-08-23.** Reading Comprehension 223 of 268 (83%) and Cloze Test 154 of 174 (89%) carry a `context` that is only a stub: `Directions: answer from the passage / Passage / (Passage not stored — refer to the source booklet: CDS (I) 2018 English, items Q51-55.)`. The questions and their directions were transcribed; the text they are ABOUT was not. As stored, all 377 are **unanswerable** — a blind deriver can only abstain, and a student handed one has nothing to read. No gate catches it: `context` is non-null and non-empty, the options are well-formed, exactly one is keyed, and `audit:text` checks long-form defects rather than whether a referenced passage exists. It stayed invisible because only the 45 RC and 20 Cloze rows that DO have passages were ever drawable into a paper. | **377 rows across 55 passages, spanning every sitting 2017-I to 2024-II** (RC 39 passages, Cloze 16). Recoverable rather than lost: all 19 source booklets are on disk at `C:	mp\PYQPs\CDS\2. English` (0.5–21 MB each, 2017-I → 2026-I), `scripts/cds/config.ts` already maps each `source_file` to its PDF, `scripts/cds/render.ts` rasterises them, and **each stub names its own item range**, so the passage-to-question mapping needs no reconstruction. The work is a vision transcription pass over 55 passages, then an update to `context` — which is NOT part of `subjectiveContentHash` for MCQs and not part of `contentHash` at all, so the repair cannot move a row's identity or orphan a paper ref. Worth pairing with a standing probe (`context ilike '%not stored%'`), which currently returns exactly these 377. | **Identified 2026-08-23 while scoping GAT mock 2; NOT fixed — user's call to finish the mock first.** Mock 2 needs only one Cloze passage and a surviving one exists, so the mock is unblocked. Doing it would unlock 377 questions including 39 genuine RC passages, which is what a future GAT mock needs for a real comprehension block — mock 1 consumed the ONLY stored RC passage in the corpus, and mock 2 substitutes Cloze because there is no second one. |
| **A solution that ASSERTS instead of DERIVING is a CORRECTNESS probe, not a readability one — measured 2026-08-25, and the ratio is the finding.** Sweeping NDA Maths for phrases that carry a conclusion with no executed step (`verified numerically`, `evaluating gives`, `it can be shown`, `the printed solution`, `by an appropriate`) flagged **79 of 5,347 PUBLIC solutions**. Rewriting them produced **62 rewrites, 2 legitimate keeps, and 15 REPORTs where the keyed answer could not be reached from the stem at all** — a 19% defect rate among flagged rows. Nearly every REPORT is a **mis-transcribed stem under a CORRECT key**, and the hand-waving was the symptom rather than the disease: the original author could not derive the printed answer from the corrupted stem either, so they named a method and asserted its result. **RULE 4's blind re-derivation cannot see this class** — the key is right, so the row simply AGREEs. One row proves why numeric spot-checks are banned as justification: the true and transcribed relations intersect at exactly one point, and that point is the obvious angle to test (45 degrees), so a check there agrees by coincidence and falsely confirms the wrong stem. | **ALL 14 REPORTed rows were adjudicated against the printed sources 2026-08-26 and the result inverted the prior.** The 14 split exactly by corpus, and the two halves needed OPPOSITE treatment. **10 practice rows: every key was already RIGHT** - 10/10 matched the booklet's own printed answer key, so not one was a wrong answer. Eight were OUR transcription corrupting the stem or the options (a stacked fraction (1/6)sin read as '1 - sin'; concrete vectors i+j-k symbolised into a+b, which is why the question appeared to state no data; 'two options each, namely true and false' read as 'three options each'; one option block shifted so our (A) held (b)'s value), and two are BOOK defects where our text is byte-faithful and the printed page is itself wrong. **4 PYQ rows: every key was WRONG** - all four stems byte-faithful to the scanned UPSC booklets, so the answer was the issue. The decisive fact is that **the booklets carry NO answer key at all** (their tail pages are SPACE FOR ROUGH WORK), so there was no issued key to preserve; the stored key came from a prep-house Excel whose Answer column sits beside plainly LLM-authored solutions - Q34's contains a literal '?' mid-derivation and Q77's ends 'REVIEW: official answer is 0; ... check problem statement'. That is a derivation, not a published key, so correcting it does not override UPSC. Keys moved A->C, C->D, B->D, B->D, each re-derived from the printed stem without consulting the stored key and verified symbolically and numerically (25 checks, 0 failures). **Reconstructions were wrong 3 times out of 4** where a rewriter offered one, which is the standing argument for reading the source rather than reasoning about it. | **DONE 2026-08-26 - all 14 applied, verified and recorded.** 10 stem/option repairs (keys untouched, and the script asserts the key it hashes with is the key it read) + 4 key corrections + 4 rewritten solutions naming the two book defects. Every repaired stem was proved to REACH its stored key before the write (`data/report-adjudication/verify-practice.py`), and the four PYQ answers independently before it (`verify-pyq.py`); before-state snapshots are committed beside them for rollback. `mock_attempts` stores a FROZEN score, so a key move does not re-grade - **one real student had picked C on Q34, the correct answer, and been marked wrong**; re-graded +3.33 via the new `regrade-attempt.ts`, which reuses the app's own `gradeMock` and REFUSES to lower a score without `--allow-lower`. Nobody lost marks. All 14 recorded in `question_reviews` as `blind_rederivation` under run `report-adjudication:2026-08-26-nda-maths`, split 8 `stem_fixed` / 2 `defect_preserved` / 4 `key_fixed` so 'we were wrong' and 'the source was wrong' stay distinguishable. Verified: 14/14 PUBLIC, exactly one correct option, 4 distinct options, solution present; render-check PASS on web and Word (258 math zones, 0 KaTeX, 0 OMML, 0 control chars); `audit:text` clean on all 6 practice sources; `audit:keys` and `audit:omml` flag 16 and 6 rows respectively, **none of them among the 14** (pre-existing, other chapters - logged below). Remaining from this sweep: the same hand-wave markers are unswept in every other subject and exam. **FOLLOW-UP 2026-08-26 — the 16 `audit:keys` rows surfaced by that pass are also RESOLVED, and NOT ONE was a wrong answer.** **11 had two options marked correct and the second mark was never spurious: every one is a genuine MULTIPLE-CORRECT question** (JEE-Advanced-style items reprinted in this booklet, whose stems say 'is/are', 'statement(s)', 'value(s) is (are)'), each second option verified TRUE by solving. The defect is that this bank models exactly one correct option and `question_format` has no multi-correct member, so the only single-answer repair available is to un-mark a TRUE option - which marks a student wrong for a correct answer. Withdrawn to PRIVATE (visibility only; ids, text and paper refs intact, reversible in one UPDATE). **3 flagged SOLN != KEY were PROBE FALSE POSITIVES** - each solution already carried an adjudicated `[Textbook note: the source book keys option D ...]` bracket and the probe read that letter as the solution's own conclusion; reworded to name the option's TEXT, which silences the probe permanently instead of relocating it. **2 duplicate-option rows are BOTH the BOOK's own duplicates**, verified on the page, and were treated differently by HARM: Q2280 duplicates a distractor (key unique - preserved PUBLIC with the defect named) while Q2517 duplicates the ANSWER (a student choosing the twin is right and marked wrong - withdrawn). Measured bank-wide afterwards: **0 PUBLIC rows with more than one correct option across all 13 exams**, and JEE Mains has none by construction (Mains is single-correct; multi-correct is JEE Advanced, which is not ingested). All 16 recorded as `defect_preserved` under run `audit-keys-adjudication:2026-08-26-nda-maths-practice`. **Newly surfaced and NOT fixed: 5 PUBLIC CBSE Class 12 board-PYQ MCQs have four options and ZERO marked correct** - a different defect class in a different corpus, logged as its own candidate below. |
| **An MCQ with FOUR options and ZERO marked correct is invisible to every gate this repo runs - found 2026-08-26.** `audit:keys` is scoped to `question_kind='practice'`, `board:lint` checks section structure, `audit:text` checks long-form text defects, and a NULL solution is legal - so nothing asks the plainest question of all: does this MCQ have an answer? A sweep of the whole bank for `count(is_correct) <> 1` on non-subjective, non-numeric rows found **5 PUBLIC CBSE Class 12 board-PYQ MCQs with four options and no correct option at all**. On `/browse` the reveal shows no answer; in a mock the grader would find no key and mark every attempt wrong with a penalty - the same failure mode the CDS dangling-ref case documents. | **5 rows, all `cbse-12-pyq` (ingested 2026-08-21), all PUBLIC:** 2023 65-3-2 Q10 (probability of A and B), 2023 65-5-1 Q12 (sum of order and degree), 2023 65-2-1 Q18 (LPP corner points), 2025 65-6-1 Q16 (range of |lambda a|), 2025 65-2-1 Q6 (direction angles). Each needs its answer derived and source-verified against that paper's official CBSE marking scheme, which is the corpus's established source of record - so this is per-row adjudication, not a bulk flip. **Worth checking at the same time whether the cause is systematic**: all five are MCQs in a corpus whose ingest notes record that CBSE marking schemes must be read as IMAGES because the text layer yields no math, so a key that failed to parse would plausibly leave exactly this shape. If so the right fix is a commit-time guard that REFUSES an MCQ with no correct option, not five hand fixes. | **Identified 2026-08-26 while resolving the `audit:keys` flags; NOT fixed - different corpus, different source of record, and newly-discovered scope.** Deliberately surfaced rather than bulk-repaired: deriving five board answers without the marking scheme would put a guess where the corpus promises a verified key. The standing probe to add is one SQL line - `count(*) filter (where is_correct) <> 1` over non-subjective, non-numeric PUBLIC rows - which currently returns exactly these 5 and would have caught them at ingest. |
| **Restoring a missing passage makes a previously-underivable key CHECKABLE for the first time — found 2026-08-26 during the CDS passage backfill.** CDS English ships no official answer key, so every key in that corpus is LLM-derived. For the 367 questions whose passage was never transcribed, that derivation was made **without the text the question is about** — the deriver had only the stem and four options. Restoring the passage does not merely make the question answerable by a student; it supplies, for the first time, the evidence needed to check the stored key. Five suspected wrong keys surfaced as a by-product of transcribing seven papers, without anyone looking for them. | **Five rows flagged by transcription agents, each with the passage sentence that contradicts the key.** `2017-2 Q54` — the passage states the iceberg "turns upside down, it appears **green**", option (a) is `green`, stored key is **C = blue**. `2018-1 Q104` — the booklet's option (a) is **`prestige`** ("to regain their independence and old ___ in 1817") while `questions.json` stores `attempt` and keys **C = rebellion**; this is the documented CDS mis-slotted-option class and is a live wrong answer. `2018-2 Q36` — key **D** ("complete failure of the government") is unsupported; the passage's only grounds point at option (c). `2017-2 Q63` and `2021-2 Q18` — arguable rather than clearly wrong, flagged at lower confidence. Also two option-transcription drifts on `2018-1 Q91`/`Q94` (a dropped option, an invented one, and a shifted set) that do not move the answer. | **Identified 2026-08-26 during the passage backfill; NOT fixed — deliberately out of scope.** The backfill's remit is the `passage` field, which is hash-neutral; a key flip or an option repair changes `content_hash`, mints a new row and needs the resync/re-flip sequence, so it is a different repair with a different blast radius. Worth doing as one pass **after** all 58 passages are restored, because the same evidence will surface more: only 7 of 15 papers had been transcribed when these five appeared. The right shape is a blind re-derivation over every passage-dependent question now that the passages exist — which is exactly the control that was unavailable before, and which `question_reviews` can record as `blind_rederivation` to distinguish "we were wrong" from "the source was wrong". |
| **Multi-statement stems render as one run-on line, and the convention is applied inconsistently across the bank.** Found 2026-08-25 while reviewing NDA Maths Blueprint Mock 1. A stem presenting two labelled claims (`Statement-1: ... Statement-2: ...`) reads as a single paragraph unless real newlines separate them — which matters more on a PRINTED paper than on the web, because the student cannot re-flow it. Both surfaces already honour a real newline (`KatexRenderer` sets `whiteSpace: "pre-wrap"`; docx `mathRuns()` splits on the newline character (``) and emits `TextRun({break:1})`), so this is purely a stored-text defect. **Measured NDA Maths PUBLIC: 13 stems carry 2+ inline `Statement-N:` labels, and 7 more already have the newlines** — i.e. the bank disagrees with itself. Separately, 11 rows use tabular language with no GFM table; most are the deliberate linearizations CLAUDE.md records as future-only, so they are NOT automatically defects. | **12 rows outside Mock 1** (6 PYQ + 7 practice, minus the 1 repaired here). The repair is hash-neutral by construction — `contentHash` normalises runs of whitespace (`\s+`) to a single space — so it cannot orphan a row, break `paper_questions` refs, or stale a `question_reviews` verdict, which makes this unusually low-risk. Tooling exists: `scripts/reviews/apply-formatting-fixes.ts` takes a paper id, refuses any edit whose hash moves, and dry-runs by default; it would need widening from a paper to a `source_file` or subject scope. **Before any sweep, the 11 tabular-language rows need triage first** — deciding which are genuine flattened tables and which are the documented Statistics/Match-List linearizations that were deliberately left alone. | **Identified 2026-08-25.** Logged, not fixed: the 12 are shipped PUBLIC rows outside the paper under review, so they go through the normal 360 + permission gate rather than riding along in an unrelated commit. The 1 row inside Mock 1 (Q668) WAS fixed, since it is part of the deliverable being reviewed. |
| **A BLIND RE-DERIVATION CANNOT CATCH A CORRUPTED OPTION SET — measured 2026-08-25, and it invalidates the assumption behind the CDS verification plan.** The blind protocol (solve from stem+options without seeing the key, then compare letters) is this repo's strongest key control. It is silently defeated when the OPTION TEXT is itself mis-transcribed, because the solver derives the right answer, looks for the label carrying that text, and confirms whichever label holds it. On CDS the transcriber had repeatedly copied the correct option's text into the WRONG letter's slot, so the blind pass confirmed the wrong letter — deriving correctly and concluding wrongly. **Proof, not inference: `question_reviews` holds `bank-paper:cds-english-blind-2026-08-23`, 89 rows across 8 papers, every one `verdict='confirmed'` — and one of them, 2021-2 Q59, is a key this session proved wrong (A→C) by reading the printed page.** The blind pass saw our option (a) holding the text "S R Q P", which IS the correct ordering, and confirmed (a); the booklet prints "P Q R S" at (a) and "S R Q P" at (c). | **Transcription fidelity is a PREREQUISITE control, not a parallel one, and the two must be ordered.** Re-deriving keys over a corrupted option set launders a transcription error into a confirmed verdict — which is worse than no review, because the verdict is then cited as evidence. The cheap decisive check is per-question and needs no solving: compare our option SET against the printed page (catches a wrong option) **and separately our label→text ORDER** (catches a swap or rotation, which leaves four distinct strings and is invisible to set comparison, to duplicate detection, and to `audit:keys`). Measured on 2025-2's match-list block: **6 of 20 rows corrupted, only 1 of the 6 had any duplicate**, and 3 carried live wrong keys. Practical consequence for the standing CDS backlog: **the 89 `confirmed` rows from the 2026-08-23 run should be treated as UNVERIFIED until their option sets have been checked against the source**, and any future blind pass on a vision-transcribed corpus should be gated behind an option-fidelity pass. | **Identified 2026-08-25 from a proven counter-example.** The 22 rows found this session are repaired and their keys corrected under assertions (`scripts/cds/apply-key-fixes.ts`); the match-list sections of the remaining papers were swept. Not yet done: re-validating the other 88 rows of the 2026-08-23 blind run, and deciding whether the same ordering rule applies to the other vision-transcribed corpora (the practice bank, Foundation, mh-ssc-10) — none of which has had an option-ORDER check either. |
| **19 CDS English mock tests now GRADE STUDENTS against answer keys nobody has verified.** CDS booklets ship with no official key, so all 2,280 answers are LLM-derived and self-confidence-flagged: **1,423 HIGH · 648 MED · 132 LOW · 77 carrying no marker at all**. `scripts/cds/README.md`'s own gate said "human spot-check before flipping PUBLIC"; on 2026-08-25 that gate was deliberately deferred by product decision so the mocks could ship, and the corpus was flipped PUBLIC as-is. This is a *known, accepted* debt, not an oversight — logged here because the deferral is otherwise recorded only in one conversation. What makes it sharper than an ordinary unverified corpus: a wrong key on `/browse` shows a student a wrong answer, whereas a wrong key in a mock **deducts marks under −1/3 negative marking** and feeds `mock_attempts` scores the student is told to trust. The confidence labels are also self-assessed by the transcribing agent, and the 2026 trial already showed a section split silently flipped 5 answers that would have read HIGH — so HIGH is not a verification, it is an opinion. | **Blind re-derive all 2,280 CDS keys** — the repo's established strongest control (the JEE BLIND lane, Worksheets' dual-blind, the NCERT MCQ passes), where agents solve from stem+options WITHOUT seeing the stored key and disagreements go to a second independent blind pass. That is the only method that catches a *stealth* wrong key, i.e. one whose solution is internally consistent and simply wrong — the 2026-06-03 audit's central finding, and by construction invisible to a read-through. Priority order is measurable: worst papers first by uncertain-row count — **2025-2 (62 uncertain), 2017-1 (60), 2024-2 (58), 2018-1 (55), 2022-2 (53)**; cleanest is 2026-1 (24, of which 15 are merely unmarked). Verdicts MUST be written to `question_reviews` (migration 0074) with `method='blind_rederivation'`, which is exactly the table built for this and which currently holds **zero CDS rows** — so today "not recorded" and "not reviewed" are indistinguishable for this corpus. Note the flip also published 8 `/questions/cds/english/*` landing pages, so the debt is not confined to `/mock`. | **Identified 2026-08-25, deferred BY DECISION (not by omission).** The user was shown the four options (full blind re-derivation / 857 uncertain rows only / 2-paper pilot / ship as-is) and chose ship-as-is with blind derivation to follow. No permission gate applies to *doing* the derivation — it is net-new verification work, not a rework of shipped content — so this can start whenever. Only a resulting KEY FLIP touches shipped rows and needs the usual adjudication. |
| **A live PUBLIC practice question has TWO of its four options marked correct**, found incidentally 2026-08-22 by running `npm run audit:keys -- Trigonometry` while ingesting an unrelated chapter. Not a new learning so much as a standing probe finding something nobody had run the probe against: `audit:keys` is documented as triage to run "after every ingest", but it is scoped by a source substring, so a source nobody has re-ingested since the probe existed has never been scanned. The defect is student-visible — the `/browse` reveal highlights both options — and silent to every other gate, since a solution, a stem and four options are all present and well-formed. | **`4daf50ab-62d5-4374-bc68-1dabed76e8a9`, question_number 1097, PUBLIC, `question_kind='practice'`, source `NDA_Maths_Practice__Trigonometry__Trigonometric_Identities.pdf`** — stem begins `Let \(f: (0, \pi/2) \to \mathbb{R}\) be such that \(f(\cos 4\theta) = …\)`, 4 options, 2 flagged `is_correct`. Fixing it needs SOURCE verification (which of the two the printed booklet keys), not a derivation-only call — this pipeline's own README says to source-verify before flipping a key, and the bank's history is that ~80% of such disputes are bank-side extraction rather than a source error. The wider question is worth more than the single row: **how many other `source_file`s have never had `audit:keys` run against them at all?** That is answerable with one unscoped run (the probe is zero-LLM and structural), and would give a real denominator instead of a one-row anecdote. | **Identified 2026-08-22.** Logged, not fixed — it is shipped PUBLIC content and outside the scope of the chapter being ingested, so it goes through the normal permission gate rather than riding along inside an unrelated commit. Recommend pairing the fix with the unscoped `audit:keys` sweep so the row is repaired in company rather than alone. |
| **A printed answer key recorded in `config.ts` for the step-6 cross-check is the SAME BYTES as a key that must be withheld from the blind derivation — and `scripts/mh-sb-9/TRANSCRIPTION_BRIEF.md` tells every transcription agent to "DERIVE the MCQ answer by solving".** So on any chapter whose config carries an MCQ key, an agent that reads the config entry (which is the normal instruction — the entry holds the block inventory and the canonical subtopic list) is contaminated before it derives anything. Surfaced 2026-08-22 on `trigonometry-9`: the b3 agent met `// Prob 8  1. (i) A (ii) D (iii) C (iv) D` roughly 50 lines above the sentence in the *same comment block* telling it that key must be withheld. It derived all four correctly from scratch, recorded its reasoning, and then **honestly refused to call the pass blind** — the right behaviour, and the only reason this was caught rather than shipped as a false "blind-verified". The failure is silent by construction: a contaminated derivation that agrees with the key is indistinguishable from an independent one. | **Two shipped chapters carry an un-fenced Problem-set MCQ key in their `config.ts` entry: `circle-9`** (`Prob 6  1. (i) A (ii) C (iii) A (iv) B (v) D (vi) C (vii) D or B`, whose own comment says the seven were blind-re-derived) **and `statistics-9`** (`(i) C (ii) B (iii) D (iv) B (v) A (vi) D (vii) B (viii) A (ix) C (x) C`, whose comment explicitly says *"Blind-re-derive before trusting these"*). Whether those chapters' agents were actually pointed at config is unrecorded, so this is a **provenance question, not a known defect** — the keys may well have been derived cleanly. Candidate remedies, cheapest first: (a) add the `AGENT-STOP` fence used on `trigonometry-9` to those two entries — comment-only, zero behavioural change; (b) add one line to `TRANSCRIPTION_BRIEF.md` forbidding agents from reading a key block, which fixes the class rather than two instances; (c) the structural fix — move transcribed keys out of `config.ts` into a `data/<id>.key.json` that the `SCRATCH` regex already excludes from merge and that no agent prompt ever names. Note (c) is the only one that cannot be undone by the next person who writes a key into a comment. | **Identified 2026-08-22.** Fenced on `trigonometry-9` (new work, applied). NOT applied to `circle-9`/`statistics-9` or to the shared brief — both are shipped work and the learning-propagation protocol gates a retroactive edit on a 360 + explicit permission. Blast radius for (a) and (b) is comment text only; (c) touches a shipped pipeline script and deserves its own 360. Worth deciding before the next Part-2 chapter (Ch.6 Circle) is ingested, since that chapter's key is already sitting in the file its agents will be told to read. |
| **A test whose `beforeAll` ignores its seed's error reports the WRONG failure.** `tests/quiz-slug-numbering.integration.test.ts` inserts 12 atoms and never checks the result; when that insert failed transiently during a 246-file parallel run (2026-08-21), `assembleNextQuiz` correctly returned "Only 0 ready" and the test failed asserting something about SLUG NUMBERING, which was never the broken thing. It passes in isolation and passed on a re-run, so it reads as a flake and gets retried rather than fixed. | The 5 quiz integration tests, and any other `beforeAll` that seeds without checking `error`. Checking it turns a confusing assertion failure into "seed failed: <reason>". | OPEN, unowned (2026-08-21) |
| **`contentHash` SORTS the option texts but keys on the answer LETTER, and the sort destroys the pairing the letter refers to.** So two questions that print the SAME four options in a DIFFERENT order and both key (A) hash identically — even though (A) denotes a different thing in each, i.e. they have different correct answers. Surfaced 2026-08-21 with a live instance in the CBSE Class-12 board-PYQ ingest: `2025 65/7/1 Q1` and `65/7/3 Q1` both read "The given graph illustrates :" over the same four inverse-trig options and both key (A) — but 65/7/1's printed (A) is `tan⁻¹ x` above an arctan graph, and 65/7/3's printed (A) is `sec⁻¹ x` above an arcsec graph. `contentHash(stem, sorted(optionTexts), "A")` cannot see the difference. This is narrow — it needs an option SHUFFLE plus a coinciding key letter, which is exactly what a multi-series board paper does — but where it fires it is silent: the upsert drops one question and the survivor keeps a key that may be wrong for the stem a reader sees. | Fixing it means hashing the correct option's TEXT rather than its letter, in `src/lib/upload/hash.ts`. That re-hashes **every MCQ in the bank (~38,000 rows across 11 exams)**, orphans every `image_url` attached by content_hash, and invalidates `data/hash-collisions.json` in three pipelines. It is emphatically NOT a change to make for one question. The cheap, local containment already exists and is working: `scripts/cbse-12-pyq/commit.ts` refuses to commit a figure-dependent row whose hash collides with another paper's until the pair is adjudicated in `data/hash-collisions.json` — which is how this one was caught rather than shipped. | **Identified 2026-08-21.** Logged, not acted on. The one known instance is contained (65/7/3 Q1 held out of the commit, recorded with its evidence) and the surviving row is correct and now carries its own graph. Worth a decision only if a second pipeline hits it — at which point the honest options are a per-exam hash variant or accepting the loss, not a bank-wide re-hash. |
| **`jszip` is a `devDependency` but is imported by RUNTIME server code**, surfaced 2026-08-18 while adding the .pptx export. `src/lib/export/docxBuilder.ts` has imported it since the Word exporter shipped, and it is reached on every `/api/export` request. It works today only because Vercel installs devDependencies during the build and Next's tracing bundles it into the serverless function — so the classification is wrong rather than broken, and the failure it invites is a build run with `npm ci --omit=dev`, which would 500 every download. | One-line move in `package.json`, `devDependencies` → `dependencies`. **The new .pptx builder imports it too**, so the misclassification is now load-bearing for two exporters rather than one. Blast radius is the lockfile plus a reinstall; no source change, no behavioural change on Vercel (the package is already present there). Verify by `npm ci --omit=dev` followed by a `/api/export` request for each of the four kinds. | **Identified 2026-08-18.** Not applied with the .pptx work because `docxBuilder` is shipped code and the learning-propagation protocol gates a retroactive fix on explicit permission — and because a dependency move is exactly the kind of change that should not ride along inside a feature commit. Awaiting permission. |
| **The `/questions` prerender contention fix has DECAYED as the bank grew, and the build is flaky again.** The 2026-08-03 entry closed this class with `experimental.cpus: 4` + `staticPageGenerationTimeout: 180`, verified at "zero of both across two consecutive builds". It is back: a `prepush` run on 2026-08-17 failed the build with **2 pages hitting `canceling statement due to statement timeout`** — `worksheets-11-12/mathematics/{vectors,indefinite-integration}`. Since that fix the bank has grown ~45k → **51,424** rows and the page count 689 → ~703. It is genuinely load-dependent, not a size threshold: the failing pages ROTATE among that exam's largest chapters (2026-08-17 Vectors 664 + Indefinite Integration 474; 2026-08-18 Derivatives 531 + Indefinite Integration), and on the first occasion Derivatives sat *between* the two failures and passed. So a build can fail for reasons unrelated to the changeset, which is the worst property for a gate to have: it trains people to re-run rather than read. **UPDATED 2026-08-18 with a sharper hypothesis than "contention", and it changes which fix is cheapest: standalone builds pass 3/3, builds INSIDE the full gate pass only 1/3.** A standalone build meets an idle database; a gate build starts immediately after ~6 minutes of the test suite hammering that same database. If that is the mechanism, the cheapest mitigation is not touching `generateStaticParams` at all but decoupling the two stages — a pause, or accepting that `prepush`'s build is the noisy one and `needsBuild` already skips it for data-only pushes. **Also note the honest attribution: the 2026-08-18 cbse-11 batch added 925 rows and 10 landing pages, so "pre-existing, not my regression" is too generous — this was already marginal and the batch plausibly tipped it.** | The two heaviest chapters in the bank are now **Worksheets Vectors 664** and **Derivatives 531** — CLAUDE.md's Recurring-pitfalls section still records the largest as 531 against the PostgREST 1000-row cap, so that figure is stale and the real headroom is smaller than documented. Candidate remedies, none costed yet: raise `staticPageGenerationTimeout` again; lower `experimental.cpus`; or narrow `generateStaticParams`' top-40 slice so the heaviest chapters are NOT prerendered (they are the ones that time out, and they render fine on demand). A related smaller finding from the same run: **`get_chapter_last_added` timed out once for a subject named only "Mathematics"**, so that subject's chapters fell back to the BUILD DATE for their sitemap `<lastmod>` — reintroducing, for one subject for one build, exactly the "every page claims it changed today" defect the 2026-08-09 work fixed. | identified 2026-08-17 |
| **A best-effort warning that does not identify WHICH exam it fired for is not actionable.** `src/lib/questions/landing.ts` logs `[landing] last-added lookup failed for ${subject.name}` — but **nine exams have a subject called "Mathematics"**, so the message cannot tell you whose sitemap dates degraded. The surrounding design is right and deliberate (the comment explains the error is surfaced rather than swallowed precisely because a silent `{ data }` destructure is how the original 5.4s timeout went unnoticed); the message just stops one field short of being useful. | One-line change in `listChapterLandings`' `console.warn` to include `exam.slug` alongside `subject.name`. Shipped code on a hot path, so it goes through the normal gate — but it is about as low-risk as a change gets, and without it the ledger entry above cannot even be attributed to an exam. | identified 2026-08-17 |
| **A chapter's MCQ rows get their `solution` from the BLIND MCQ RE-DERIVATION pass, not from the solution-authoring pass** — so a chapter whose blind pass never ran ships MCQs with a correct key and NO working. Surfaced 2026-08-17 while verifying the six-chapter NCERT batch: a per-chapter `solution IS NULL` count read 0 for twelve chapters and **22 for Integrals**. Nothing catches this — `board:lint` checks section structure, `audit:keys` compares a solution's concluded letter against the key (and skips a row that has no solution at all), and the commit reports success because a NULL solution is legal for an MCQ. It is visible only as a count. | **22 PUBLIC MCQ rows in the shipped `Integrals` chapter (`NCERT_12_Maths__Integrals.pdf`) have `solution IS NULL`** — `Ex 7.1 Q21/Q22`, `Ex 7.2 Q38/Q39`, `Ex 7.3 Q23/Q24`, `Ex 7.4 Q24/Q25`, `Ex 7.5 Q22/Q23`, `Ex 7.6 Q23/Q24`, `Ex 7.7 Q10/Q11`, `Ex 7.8 Q21/Q22`, `Ex 7.9 Q10`, `Ex 7.10 Q20/Q21`, `Misc Q38/Q39/Q40`. Integrals was this pipeline's PILOT chapter and its `integrals.q9.mcq-verify.json` covers a single question, so the other 22 never got a blind pass. The other 292 rows in the chapter all carry a solution, so on `/board` and `/browse` these 22 reveal a highlighted correct option and an empty working — inconsistent within one chapter. The five later chapters (`matrices`, `determinants`, `relationsFunctions`, `differentialEquations`, `probability`) and all six of this batch read 0, so the gap is Integrals-only. | **DONE 2026-08-17 (user-approved) — and the 22 never-verified keys are ALL CORRECT.** Ran the recommended recipe (`db:backup` first, since these are shipped PUBLIC rows): `dump-mcq` → blind agent → `mark-mcq-verify --write` → `apply-solutions --apply`. **23/23 blind agreement, 0 mismatches** (23 not 22 — the dump carries every MCQ in the chapter), so the audit half of the fix returned a clean bill on keys nobody had ever checked. Integrals now reads **0 rows with `solution IS NULL`**, down from 22, and the whole exam reads 0. **A hazard the 360 did NOT anticipate, worth recording:** `apply-solutions.ts` overwrites **any** MCQ row present in a `*.mcq-verify.json` file, unconditionally — and `Ex 7.9 Q9` already had a solution carrying a `[Textbook answer-key error: …]` bracket whose source of record is the pilot's own `integrals.q9.mcq-verify.json`. A second file naming that row would have put two files in competition with `readdir` order picking the winner, silently replacing an adjudicated erratum with plain working (the [[sibling-glob-merges-outputs]] shape). Handled by having the blind agent derive Q9 for its verification value but **omit the `solution` field**, which the applier skips — so the row was written once, from the pilot file, with its bracket. Verified after the fact: both Integrals brackets (`Ex 7.6 Q11`, `Ex 7.9 Q9`) intact. **Bonus finding: the blind pass independently re-derived `Ex 7.9 Q9` as 6 (the printed option A), confirming the existing erratum — NCERT's printed key marks D and our stored answer is right — on a third derivation that never opened the key, the DB or the bracket.** One methodological note from the run: `Ex 7.10 Q20`'s numeric quadrature returned 4.0 instead of π (a node-placement artifact from `tan⁵x`'s fifth-order pole) and would have read as "no option matches"; the agent re-checked three other ways rather than reporting a defect — the standing "a CAS returning nothing is a question, never a verdict" rule, applied correctly. `audit:keys`/`audit:text`/`audit:omml` all clean on the chapter afterwards. |
| **A generated manifest whose paths are built with `os.path.relpath(p, os.getcwd())` silently rewrites itself depending on which directory you ran the script from.** Surfaced 2026-08-15: a verification run of `scripts/mh-sb-9/render_solution_diagrams.py pair-lines-12` launched from `scripts/` rewrote every path in the SHIPPED `scripts/stateboard/data/pair-lines-12.solution-images.json`, dropping the leading `scripts/` from all 20 entries — exit 0, no warning, and it only surfaced because `git status` showed a tracked file modified. Fixed in the mh-sb-9 copy by anchoring to the REPO ROOT (`os.path.dirname(os.path.dirname(HERE))`), which reproduces the shipped values exactly and cannot drift. | `scripts/stateboard/render_solution_diagrams.py` carries the identical line and produces the four SHIPPED Class-12 manifests (`pair-lines-12`, `linear-prog-12`, `app-def-integration-12`, `app-derivatives-12`). Their committed values are currently CORRECT — they were generated from the repo root — so this is a latent trap, not a live defect: the next person who re-renders from anywhere else corrupts them. | **Identified 2026-08-15.** One-line change, mechanical, zero behavioural effect when run from the repo root (verified: the mh-sb-9 copy now emits byte-identical manifests from both `scripts/` and the repo root). Not applied to the stateboard copy because that is shipped work — awaiting permission per the learning-propagation protocol. Blast radius is one line in one script; the four manifests would be re-verified unchanged. |
| **A `stem` override without a matching `solution` override can leave the stored solution deriving from the PRE-REPAIR data**, so the row ships a stem and a solution that contradict each other. Surfaced 2026-08-14 in Batch J: Properties of Triangle `02-23`'s stem was repaired `a/c` → `a/b`, but its solution still opened "From sine rule: a/c = …". **No probe catches this** — `audit:keys` only compares a solution's concluded LETTER against the key, and here the letter was right; `audit:text` checks rendering, not agreement. It was found only by reading the committed row back out of the database instead of trusting the commit report. | **~35 stem repairs across the already-shipped Cadetprep Worksheets chapters (Batches A–I) carry no solution override** and are therefore candidates: vectors (16), logarithms (5), circles (5), matrices-determinants (3), derivatives (3), trig-identities (2), plus single rows in functions, indefinite-integration, inverse-trigonometry, complex-numbers, permutations-combinations, quadratic-equations. **This is an UPPER BOUND, not a defect count** — many stem repairs are cosmetic (an extraction artifact removed, a typo fixed, an option-letter reference reworded) and leave the solution perfectly valid. Only *value-changing* stem repairs can produce the contradiction, so the list needs triage before any edit. | **ANALYSED 2026-08-14 — the real number is ONE, not ~35, and it is `circles 02-8`.** Triaged with `out/_stemtriage.ts` (read-only): for each of the **43** stem-overrides-without-a-solution-override it diffs the ORIGINAL source stem against the repaired one, extracts the tokens the repair dropped, and checks whether the live stored solution still cites any of them. Result: **38 cosmetic or clean · 5 flagged · 4 of those 5 false positives** (the flagged tokens were the English article "a", the ubiquitous `\vec`, and the legitimate `4` of a 3-4-5 triangle in an inverse-trig derivation). **`circles 02-8` is genuine and is PUBLIC**: the stem was generalised to *"If a line is tangent to a circle, then the distance from the centre of the circle to that line is:"* while the stored solution still reads *"Circle: \(x^2+y^2-4x-6y+9=0\), Center: (2,3), Radius: \(\sqrt{4+9-9}=2\)…"* — it solves a specific circle the question no longer mentions. **Awaiting permission** for the one-row fix: replace that solution with the general statement (the distance equals the radius, which is why the keyed option is right). Solution-only, so `content_hash` is untouched and it applies with `out/_patchsol.ts`; the override file stays the source of record. **Forward-going work is already protected** — the Batch-J loop reads committed rows back from the DB, and `scripts/worksheets/README.md` step 7 documents the class. |
| **2 errors reported in the EXISTING PUBLIC MH-HSC-12 Maths *textbook* (practice) corpus**, surfaced 2026-08-13 while authoring board-PYQ solutions against that corpus as the reuse source (`scripts/mh-hsc-12-pyq`). Not found by any probe — found because a PYQ asked the same thing and the twin's answer disagreed. | (1) **Trigonometric Functions** — the shipped solution for the triangle \(a=13,b=14,c=15\) prints **cos A = 33/65**, which is actually cos B; the correct value is **3/5**. (2) **Trigonometric Functions**, Properties of Inverse Trig — a row states \(2\tan^{-1}(-1/3) + \cos^{-1}(3/5) = \pi/2\), which is **false**; the identity holds for \(+1/3\). | **DONE 2026-08-13 (user-approved) — but only ONE of the two was real, and re-deriving first is what caught it.** (1) **CONFIRMED and fixed** (`21b3d110`): the formula LABEL was right and the SUBSTITUTION swapped a and b, giving \(\frac{13^2+15^2-14^2}{2(13)(15)}=\frac{198}{390}\) — i.e. cos B. Decisive evidence was internal: parts (ii),(iii),(vi) of the SAME row give \(\sin\frac{A}{2}=\frac1{\sqrt5}\), \(\cos\frac{A}{2}=\frac2{\sqrt5}\) and \(\sin A=\frac45\), which force \(\cos A=1-2\sin^2\frac{A}{2}=\frac35\), so part (i) contradicted its own siblings. Rewritten to \(\frac{14^2+15^2-13^2}{2(14)(15)}=\frac{252}{420}=\frac35\); **`content_hash` verified UNMOVED** (solution is not hashed, so the row updates rather than orphaning). (2) **NOT AN ERROR — already handled correctly.** The row (`988ebac1`) preserves the book's printed \(-1/3\) *and* its solution already opens with `[Textbook misprint: the printed stem shows −1/3; the book's solution proves the result using 1/3.]`, then proves the true identity. That is exactly the preserve-printed-defect-and-flag convention. **Second time this ledger's own warning has paid for itself**: acting on the report unchecked would have "corrected" a correctly-flagged row into a silently-repaired PYQ. Also worth recording from the hunt: a bare `.select()` over this 2,850-row exam silently returned 0 matches (PostgREST 1000-row cap), and a PostgREST `ilike` pattern containing `\t` matched a literal `t` because **backslash is the LIKE escape character** — both made a present row look absent. |
| Theme-coverage gap — a chapter can ship "complete" while a theme (formula/trap) is missing from the notes source ([[quiz-formula-coverage-gap]]) | The ~23 un-quiz-built /notes chapters: (a) **correctness** — hand-authored practiceSet/selfCheck answers never independently re-derived (the Lines `(1,-2)` class of bug); (b) **completeness** — empty `formula.latex` / <12 `traps` (run `npm run quiz:coverage`) | **Analysed** (full audit done). User decision: NO blanket campaign — **fold correctness re-derivation + formula/trap enrichment into per-chapter quiz-building (Wave 2+)**. Targeted correctness pass available on request for any chapter that's a live LWS lesson plan. |
| Matrices is formula- + property-rich; `quiz:coverage` flagged ~10 empty-`formula.latex` concepts | Matrices & Determinants /notes — property quiz + clean formula/property split | **Path A + B DONE 2026-06-10.** Path A: 25 rule-identities enriched from prose → property (2 quizzes, 0 churn). Path B: re-themed the 12 rule-formulas formula→property + re-assembled → clean split (formula=construction 1 quiz; property=ALL rules 3 quizzes); 0 integrity flags. **Only deferred:** the 6 `transpose-rules`/`symmetric-and-skew` *extra* properties reverted in Path A (single→bundle flip — re-author + re-assemble if ever wanted; low value). |
| 2 likely wrong-keys in the EXISTING PUBLIC Foundation "Acids, Bases and Salts" bank, surfaced when an LWS test (already 100% in the bank) was re-derived during `/lws-test-ingest` (2026-06-20) | (1) *"Tooth decay begins at the pH of"* (opts 8.0/5.1/5.8/6.5) — bank keyed **C=5.8**; decay starts BELOW 5.5, so 5.8 shouldn't qualify, **5.1 (B)** is the only sub-5.5 option. (2) *"Bleaching powder is a"* — bank keyed **C=greyish white powder**; standard textbook description is **pale yellow powder (B)**. | **DONE 2026-06-20** (user-approved). Both keys flipped C→B (id `7c9581ef…` tooth decay, `1906ee88…` bleaching powder), `content_hash` recomputed via the real helper (no collisions), explanatory solutions added, exactly-one-correct verified. These were LLM-*derived* keys (Foundation has no printed keys), corrected to the textbook-standard answer. |
| **4 wrong-keys in the EXISTING PUBLIC NDA Maths bank** (a content-audit-"closed" subject), surfaced by blind re-derivation during `/lws-test-ingest` of APJ Maths Mock 5 (2026-06-21) — 3 have the bank's OWN solution already deriving the correct answer, i.e. a key/solution mismatch | (1) `4107821d` (App of Derivatives — extrema of α log\|x\|+βx²+x): keyed **D=(a=2,b=½)** but the correct option is **(a=2,b=−½)** (f′=α/x+2βx+1=0 at x=−1,2 ⇒ β=−½; bank solution even says "Hence (B)"). (2) `69ec37dd` (Circles — two diameters, area 49π): keyed **C=(x²+y²−2x−2y−47=0)** but centre=(1,−1) ⇒ correct is **(x²+y²−2x+2y−47=0)** (bank solution derives the +2y form then mis-keys). (3) `771d677e` (Matrices/Determinants — system x+2y+3z=1, 2x+y+3z=2, 5x+5y+9z=4): keyed **B=infinitely many** but det=3≠0 ⇒ **unique solution (0,−1,1)**; the bank's row-reduction is wrong. (4) `59e05e11` (App of Derivatives — longest decreasing interval of f=2cos²x−1=cos2x): keyed **B=π** but the answer is **π/2** (cos2x decreasing on intervals of length π/2; bank solution treats it like cos x). | **2 of 4 DONE 2026-08-05 (user-approved); the other 2 were ALREADY CORRECT — this ledger row had gone stale.** Re-derived all four before touching anything, per [[audit-probe-symmetry]], and only two still needed a flip: `771d677e` **B→A** (det = 1(9−15)−2(18−15)+3(10−5) = **3 ≠ 0** ⇒ unique solution (0,−1,1), verified against all three equations; the old solution row-reduced correctly then mis-stated the rank as 2) and `59e05e11` **B→C** (f = cos 2x, f′ = −2 sin 2x < 0 on x ∈ (kπ, kπ+π/2) ⇒ length **π/2**, not π — the old solution treated it as cos x). Applied via `scripts/grounding/apply-key-fix.ts ledger-nda-maths-2026-08-05 --apply`; verified exactly-one-correct, 4 options, no `content_hash` collision. **`4107821d` and `69ec37dd` needed NO change** — the first already keys B (a=2, b=−½, confirmed) and the second already keys C with the correct `x²+y²−2x+2y−47=0` text, so they were fixed at some point after this row was written. **Had the row been trusted rather than re-derived, two correct rows would have been "corrected" into errors** — the standing argument for verifying every ledger claim against live data before acting on it. Original note follows: the Mock-5 paper itself is keyed CORRECTLY for all four (3 are PRIVATE dups, `59e05e11`'s analogue n=111 is a NEW PUBLIC-eligible row with the right key), so this ledger item is only about fixing the *shipped bank rows*. Cross-check each official key against a source PDF before flipping per [[audit-probe-symmetry]]. |
| **3 more wrong-keys in the EXISTING PUBLIC NDA Maths bank** (Vectors), surfaced by blind re-derivation during `/lws-test-ingest` of LWS Maths Mock 3 (2026-06-28) — the whole 120-q paper was a dup of the existing bank | (1) `3cbe4d57` (Vectors — median through P, \(\vec{PQ},\vec{PR}\) given): keyed **D=None** but \(\vec{PM}=\tfrac12(\vec{PQ}+\vec{PR})=(1,1,4)\), \(|\vec{PM}|=\sqrt{18}\) which IS printed option **C**. (2) `6741a9d9` (Vectors — P divides AB 2:3 internally): keyed **B** but \(P=(2B+3A)/5=(7,3,4)/5=\tfrac15(7\hat i+3\hat j+4\hat k)=\) option **C**. (3) `87f87b05` (Vectors — \(\vec b=3\hat j+4\hat k\) split parallel/perp to \(\vec a=\hat i+\hat j\), find \(\vec b_1\times\vec b_2\)): keyed **D** but \(\vec b_1=\tfrac32(\hat i+\hat j),\ \vec b_2=(-\tfrac32,\tfrac32,4)\Rightarrow \vec b_1\times\vec b_2=(6,-6,\tfrac92)=\) option **C**. | **DONE 2026-06-28** (user-approved). All three flipped D/B→C in one transaction: `is_correct` moved to option C, solutions cleaned to state "Matches option C" (87f87b05 also had a \(\vec b_2\) sign typo fixed), `content_hash` recomputed via the real helper (no collisions, verified exactly-one-correct after). The Mock 3 Excel was already keyed correctly. Still **awaiting permission** — the 3 genuinely **flawed** dup rows whose correct value isn't among the printed options (Q2 `e3d20dbf` not-collinear, Q11 `a260bb9f` unit-vector not an option, Q20 `bc4bd708` duplicate options A=B + value off by 3×) — these need a source/teacher review, not a simple key flip. |
| **1 wrong-key in the EXISTING PUBLIC NDA Physics bank** (Electrostatics), surfaced cross-checking the APJ GAT Full Mock 6 derived answers against the teacher's official key (2026-06-29) | `8d56de4e` (*"For a material under static conditions, which one is correct?"*): bank keyed **A=insulator can be charged**, but BOTH a perfect conductor and an insulator can hold static charge ⇒ correct is **C=both can be charged** (the official LWS Mock-6 key also says C). | **DONE 2026-06-29** (user-approved). Flipped A→C in one transaction: `is_correct` moved to C, solution rewritten, `content_hash` recomputed via the real helper (OLD_A reproduced the stored hash exactly; no collision on NEW_C), exactly-one-correct verified. **Also flagged for the TEACHER to fix the Mock-6 MASTER key (NOT bank rows):** Q18 (antonym of HANDY keyed *heavy*; should be *unwieldy*/*cumbersome*) + Q55 (lightning keyed D=ground-induction; the discharge answer A is bank-verified + more defensible). The Mock-6 Excel was aligned 100% to the official key per the user's call; these two carry an honest in-solution note + `reviewNote`. |
| **Figure-as-OPTION questions committed as TEXT descriptions can give away the answer + are lossy** — surfaced reviewing the MHT-CET 2025-April ingest ([[figure-image-acquisition]]) | The ~7 PUBLIC MHT-CET 2025-April "which structure is X" rows ingested with `optionImages` text-describe instead of attached figures. Worst: **Q93** (`MHT_CET_2025_Apr_20_S1.docx`, "identify the structural formula of phloroglucinol") — option A text literally reads "…— phloroglucinol", **naming the answer**. Lower-risk: S1 Q79/Q89, S2 Q121, S2-20Apr Q74/Q79 (describe branching/structure, closer to faithful). Also their `content_hash` is weak (placeholder option text). | **DONE 2026-06-26** (user-approved). 360 narrowed it to **Q93 + Q66** (the two that literally name the answer — "…phloroglucinol" / "…neopentane"): re-attached the 4 structure figures each (`optionFigures`) + neutral `(A)(B)(C)(D)` labels; image→option order verified (image12=1,3,5-triol=A; image14=X-shape=neopentane=C); delete→recommit→attach→flip PUBLIC; count unchanged (588/4). The other 5 (Q89/Q74/Q121/Q79×2) **deliberately left as faithful text** — naming alcohols/formulae/regions doesn't trivialise the asked question (guard against over-eager backfill). |
| **1 bank-vs-official-key conflict in the EXISTING PUBLIC NDA Geography bank**, surfaced cross-checking the LWS GAT Full Mock 4 derived answers against the teacher's official key (2026-07-14) | `58d68de7` (*"Which one is known as uplands of delta region?"* — opts A.Bef B.Bils C.Peh D.Chars): bank keyed **D=Chars**, but the LWS GAT Mock 4 official key says **C=Peh** for the SAME question + SAME options. Genuinely obscure deltaic-landform term; needs a source check to decide (also option A "Bef" looks like a truncated "Bhur/Bhabar" typo). The Mock-4 Excel follows the official key (C); the shipped bank row is untouched. | **CLOSED 2026-08-05 — NO CHANGE; the BANK is right and the mock's key is wrong.** NCERT *India: Physical Environment* states it almost verbatim: *"the uplands in the delta are called 'Chars' while marshy areas are known as 'Bils'."* Both terms appear as options here, in exactly those roles, so the question is lifted from that sentence and **D = Chars is correct**. The LWS GAT Mock 4 key (C = Peh) is the error. This is the case the row's own caveat anticipated — an official-looking key is not automatically right on obscure regional terms — and flipping on its word would have replaced an NCERT-verbatim answer with a wrong one. Superseded guidance follows: A `is_correct` move D→C + solution + `content_hash` recompute via the real helper (collision-guarded) IF a source confirms Peh. Verify against a geography source before flipping — the official key is not automatically right on obscure regional terms. |
| **1 wrong-key in the EXISTING PUBLIC NDA Geography bank** (a real NDA-2 2018 GAT PYQ), surfaced re-deriving the APJ GAT Mock 8 river-basin/town match-list during `/lws-test-ingest` (2026-07-21) | `7695c15c` (NDA2_GAT_2018_PYQ.xlsx Q96 — Match List-I River Basin {A.Bhagirathi B.Alaknanda C.Nayar D.Ganga} with List-II Town {1.Lansdowne 2.Narendra Nagar 3.Uttarkashi 4.Pauri}): bank keyed **C=(A-2,B-4,C-1,D-3)**, which requires Ganga→Uttarkashi and Bhagirathi→Narendra Nagar, both false. Correct is **B=(A-3,B-4,C-1,D-2)** — Uttarkashi is on the Bhagirathi (A-3), Nayar→Lansdowne (C-1), Alaknanda→Pauri (B-4), Ganga→Narendra Nagar (D-2). The bank's own solution was self-contradictory ("Bhagirathi past Uttarkashi (A-3)… Matches option C"). Source-verified against the local `GAT_2018_NDA2.pdf` Set-A booklet (exact question + option order confirmed; booklet is question-only, no printed key — but C is geographically impossible, so the answer is provably B). | **DONE 2026-07-21** (user-approved). Flipped C→B in one transaction: `is_correct` moved to option B, self-contradictory solution rewritten, `content_hash` recomputed via the real helper (no collision, exactly-one-correct verified, row stays PUBLIC/pyq). The Mock 8 tagged Excel + committed practice rows were already keyed B. |
| **Clipped stem-named labels may exist in EXISTING PUBLIC Foundation figures** — the clipped-label defect class the NEET audit found (2026-07-02), extrapolated to the only other render-crop pipeline ([[figure-snapcrop-verify]] lesson 6) | Foundation Course has **283 PUBLIC figure-questions**; `extractStemLabels` flags **~107 (38%)** as naming ≥1 label (over-counts — Foundation's Class 9/10 prose has stray caps). Foundation crops **unioned raster image-blocks** (not eyeballed page-bands like NEET), so a label that's part of the figure raster is inside the union → structural risk is **lower** than NEET's. The plausible defect: a marker rendered as positioned text just outside the block union. | **Identified — awaiting permission (do NOT sweep silently).** 360: *scope* = the ~107 label-naming subset (not all 283); *blast radius* = re-crop changes `image_url` only, no key/hash change, but touches shipped PUBLIC content; *does-it-apply* = partially — block-union crops are safer than NEET's page-bands, so expect a far lower hit rate than NEET's 9%; *cost* = one vision-agent pass (~12 agents × ~9 figs) over the 107 subset + per-hit re-crop; *risk/reversibility* = low/reversible (image swap); *recommendation* = **DEFER** to Foundation's next figure touch OR run only if a specific Foundation figure is reported — not worth a standalone 107-figure sweep given the lower structural risk. Method: download the 107 crops + stems, spawn vision agents to cross-check each stem's `extractStemLabels` output against the crop, verify each FLAG against source before re-cropping. |
| **2 wrong-keys (+2 conflicts) in the EXISTING PUBLIC NDA Physics/Geography bank**, surfaced cross-checking APJ GAT Mock 7 against the teacher's official key + a full dedup re-derivation (2026-07-06); the whole Q101–150 GK block is a dup of the bank | (1) `f50d217b` (Physics/Light — rectangle ABCD at 2f/3f before concave mirror, ratio B′C′:A′D′): bank keyed **B=2** but the stem fully determines it (m at 2f =1, m at 3f =½) ⇒ **½ = C** (matches the official NDA key; the LWS Mock-7 key was also wrong here). (2) `76b431ce` (Physics/Light — object far beyond C accelerating toward concave mirror): bank keyed **B=toward focus** but as the object approaches C the image moves F→C ⇒ **A=toward centre of curvature**. **Conflicts (need source/teacher review, NOT asserted):** `613be10a` (two ⊥ mirrors rotated by θ — bank **C=2θ** vs retroreflector-invariance **A=0**; figure-dependent) + `58d68de7` (Geography — "uplands of delta region": bank keyed **Chars/D**, the Mock-7 official key says **Peh/C** — both obscure, unresolved). | **2 clear ones DONE 2026-07-06** (user-approved). Flipped via `scripts/grounding/apply-key-fix.ts gat7-physics --apply`: `f50d217b` B→C (½; the row's own solution had a spurious "×2"), `76b431ce` B→A (image moves F→C; the row's solution had the direction reversed); solutions rewritten, `content_hash` recomputed via the real helper (no collisions), exactly-one-correct verified after. The Mock-7 Excel was already keyed C/A per the user's adjudication. **Still awaiting review — the 2 CONFLICTS** (`613be10a` two-⊥-mirrors 2θ-vs-0, figure-dependent; `58d68de7` "uplands of delta" bank Chars/D vs Mock-7 key Peh/C) need a source/teacher decision, NOT a flip. |
| **1 pre-existing internal contradiction in the EXISTING PUBLIC NDA Geography bank** (two near-identical Ring-of-Fire rows with opposite keys), surfaced by the semantic-dedup scan of the "Interior of Earth & POP 50 Q" LWS test (2026-07-25) — the paper itself is all net-new (49 new / 1 maybe / 0 dup), so this is a bank-internal issue, not paper-derived | `d0b9971a` (Ring of Fire — {1. convergent plate boundaries, 2. seismic & volcanic, 3. deep ocean trenches}) keyed **D (1,2,3)** vs `21fbe2f1` (same cluster, "convergence plate boundaries / active seismic & volcanic / deep trench") keyed **B (2 and 3 only)**. Statement 1 (Ring of Fire ≈ convergent boundaries) is NCERT-standard-correct, so **D is defensible and `21fbe2f1`'s B is the suspected wrong-key.** | **DECLINED 2026-08-05 — NO CHANGE, and the proposed direction was backwards.** The two rows are not peers: **`21fbe2f1` is a REAL NDA-1 2018 GAT PYQ** (`GAT_2018_NDA1_PYQ.xlsx`, `pyq_year` 2018) carrying an official UPSC key, while **`d0b9971a` is an LWS PRACTICE mock** (`NDA_GAT_Practice__LWS_GAT_Full_Mock_3.pdf`, `pyq_year` NULL) whose answer is derived. Flipping the PYQ to match the practice paper inverts the source hierarchy. The PYQ's stored solution also gives a coherent defence of B — the Ring of Fire includes transform boundaries (the San Andreas segment), so "a zone of convergent plate boundaries" is arguably overbroad as an unqualified statement, which is exactly the kind of distinction UPSC tests. **Settling it needs the official NDA-1 2018 GAT key**, which we do not have; absent that, house convention is preserve-the-official-key-and-note, never flip toward a weaker source. Not a contradiction requiring action: two differently-sourced questions may legitimately key differently. Superseded guidance follows: A `is_correct` move B→D on `21fbe2f1` + solution reconcile + `content_hash` recompute via the real helper (collision-guarded). Confirm both rows' exact stems/options against a source before flipping per [[audit-probe-symmetry]]; they may be legitimately distinct (e.g. one qualifies "convergent" differently). |
| **Systematic vision-ingestion corruption in the EXISTING PUBLIC NDA-Maths *practice* bank** (the 3D folder → Vectors + 3D Geometry, and Definite Integration), surfaced reviewing the 3 LWS "Mock 4 / Mock 4 (B) / 6M Mock" draft papers (2026-07-14). The practice-PDF vision pipeline ([[practice-pdf-vision-ingestion]]) mangled stems/options/keys; of 311 distinct maths q across those papers, **30 were defective — 28 bank-extraction errors (source question valid + correctly keyed, but a stem/option/flag got corrupted) + 2 source defects** (1987 lines-equal-angles keyed 2 not 4; 2091 circle-radius keyed √37 not √7; +1997 a source stem/key inconsistency). | The 30 in these 3 papers were only the ones those papers happened to sample. The **full** `NDA_Maths_Practice__3D__Vectors` (Q1795–1969), `__3D__3D_Geometry` (Q1970–2100), and `__Calculus__Definite_Integration` (Q2749–2885) practice chapters (source `C:\tmp\Practice\Maths\04. 3D\` + `05. Calculus\`) almost certainly carry many more of the same corruptions — a full source-verified re-audit of those 3 chapters (and likely the other practice chapters from the same ingest) is warranted. | **The 30 DONE 2026-07-14** (user said "proceed"). Each source-verified against the rendered source PDF + answer-key PDF, then applied via `scripts/practice/apply-source-fixes.ts` (general applier: stem/option-text edits + key move + collision-guarded `content_hash` recompute); all 30 re-verified in-DB (exactly-one-correct, 0 dup options, correct-text matches source). **Broader per-chapter re-audit: the SCRAMBLE/DUP/wrong-key-with-contradicting-solution class is now SWEPT bank-wide + guarded (DONE 2026-07-25).** The LWS Mock 5 review generalized into a zero-LLM `npm run audit:keys` probe (`scripts/practice/audit-keys.ts`) + **140 source-verified fixes across every NDA-Maths practice chapter** (159→17 faithful residual) + **54 Foundation dup fixes**, all applied via `apply-source-fixes.ts` with transcription sync (content_hash-idempotent); merges through `495c3b9`. See the 2026-07-25 Decisions entry + [[practice-pdf-vision-ingestion]]. **Still NOT done — the deeper class:** a full stem-FIDELITY re-transcription of each chapter (a wrong stem coefficient/value that keeps a *consistent* key+solution won't trip the structural probe), per the original method (render every chapter's Q+solution+key pages, vision-transcribe, diff, classify extraction-error-vs-source-defect). |
| **`/saved` still gates its per-question Edit affordance on `Boolean(member)`, which went stale at migration 0056** — spotted while wiring `canEdit` correctly into the paper editor (2026-07-15) | [`src/app/saved/page.tsx:23`](src/app/saved/page.tsx#L23) computes `const canEdit = Boolean(member)` and passes it to `QuestionCard`, so an org TEACHER sees an Edit link on every saved question. Content editing has been **superadmin-only** since 0056 — `/browse` was repointed to `getSessionSuperadmin()` ([browse/page.tsx:67](src/app/browse/page.tsx#L67)) but `/saved` was missed. Cosmetic only: the `/questions/[id]/edit` route itself is `requireSuperadmin`-guarded, so a teacher clicking it is bounced — there is **no privilege leak**, just a dead affordance. | **Identified — awaiting permission.** 360: *scope* = one line in `/saved`; *blast radius* = nil (prop only; the route guard is the real boundary and already correct); *does-it-apply* = yes, same 0056 rule `/browse` follows; *cost* = ~2 min (swap to `getSessionSuperadmin()`, mirroring browse); *risk/reversibility* = trivial/reversible; *recommendation* = **do it on the next `/saved` touch** — a teacher hitting a dead Edit link is mildly confusing but harmless, so it doesn't justify its own PR. |
| **Two atom-quality defects in the ~120 shipped daily quizzes** — non-standalone stems (dangle an undefined concrete object) + correct-option "tells", found auditing Matrices & Determinants (2026-07-06, [[quiz-atom-standalone-and-option-tell]]) | The ~120 pushed daily quizzes across all 30 noted NDA Maths chapters (+ HP), each harvested from /notes practiceSet/selfCheck, so both classes likely recur. M&D alone had **5 non-standalone + 17 option-tells of 218** (~10%). | **Identified — awaiting permission (do NOT sweep silently).** 360: *scope* = the ~120 `quizzes.questions` snapshots + their source atoms; *blast radius* = in-place patch of `options[answer]`/`correct`/`stem` + the snapshot element, then re-push (UUIDv5 → updates the same nda-tracker draft) — no key/hash change, but touches shipped content (some public via `public_slug`); *does-it-apply* = yes, same harvest source; *cost* = per-chapter detector pass + targeted fixes (heavy if manual across 120); *risk/reversibility* = low/reversible (backup + byte-verify); *recommendation* = **DEFER** — fold a detector into `quiz:lint` FIRST (see 2026-07-06 dated entry) so the audit is mechanical, then run per-chapter on a touch, not a 120-quiz manual read. Method: reuse the M&D detector (correct-option prose-word-count vs distractors + punct scan for `—`/`(=`/`(not`/`(rank`; standalone = stem names a matrix/determinant/system with no inline values). **UPDATE 2026-07-06: detector BUILT (`flagStem`+`flagOptionTell` in `quiz:lint`) + full snapshot AUDIT run** over all 180 shipped quizzes / 2565 q → **2 non-standalone stems + 67 correct-option tells across 48 quizzes / 19 chapters; only 1 flagged quiz is PUBLIC (`vectors-computation-1`).** Highest tell counts: HP 9, Limits&Continuity 9, 3D-Geo 8, Vectors 7, M&D 6. ~10–15% of tell flags are LaTeX-wrapping false positives (a distractor wraps the same aside inside `\text{}`); the 2 stems + most tells are genuine. **Still awaiting permission to FIX** — fixing = in-place patch of atom+snapshot + re-push per chapter (some public), a human dismissing the FPs. |
| **9 PUBLIC JEE Maths rows carry a leaked `CHEMISTRY`/`MATHEMATICS` subject banner at the end of the stem** — found 2026-07-30 while opening the Physics ingestion, when the pilot's last Physics question (a NAT) ended in "CHEMISTRY" | `segmentQuestions` skipped `PART-`/`SECTION` headers but not the **bare** `**CHEMISTRY**` banner the 2025/2026 sittings print between subject blocks, so it was absorbed into the PRECEDING question — into the **stem** when that question is a NAT (no option markers to catch it), into **option (d)** when it's an MCQ. **83 rows corpus-wide** in `out/*.records.json`; 37 Physics + 37 Chemistry are not yet committed (and are now fixed at source), leaving **9 already-committed PUBLIC Maths rows** — the last Maths question of a shift in the 2025 sittings (`2025-apr02` Q75, `2025-apr03` Q75, etc.). Cosmetic: a stray uppercase word trailing the stem; the question, options and key are all intact. | **Identified — awaiting permission.** Fixed FORWARD already (`SUBJECT_BANNER` guard in `scripts/jee/lib.ts` + a regression test), so the class cannot grow. 360: *scope* = 9 PUBLIC Maths rows, `text` (or option `d`) only — enumerate with the same whole-line regex, do not guess; *blast radius* = trimming the banner changes `text` ⇒ **`content_hash` recompute, collision-guarded** (same shape as the literal-`\n` repair, which found 0 collisions across 24 rows); row ids unchanged so papers/mocks/bookmarks are unaffected; *does-it-apply* = **yes, unambiguously** — no JEE stem legitimately ends in a bare subject name, and unlike the Pariksha pipe-tables there is no judgement call about intent; *cost* = trivial, one guarded script reusing `apply-key-fix.ts`'s hash machinery; *risk/reversibility* = low + reversible; *recommendation* = **fold into the next JEE Maths touch** rather than a standalone PR — it's a cosmetic trailing word on 9 of 3,677 Maths rows, so it doesn't justify its own run, but it should not be forgotten either. |
| **7 PUBLIC rows author a table with pipes but NO `\|---\|` separator row, so it renders as pipe soup on every surface** — found 2026-07-27 by the new `npm run audit:text` probe on its first bank-wide run (32,289 rows), while fixing the literal-`\n` report | **MHT_CET_12thMay2024_Shift1.xlsx Q81** is a genuine 2-column data table (`\| Solid \| E gap \|` → 5.47/0.0/1.12/0.67 eV) missing its separator — the worst of the seven, since the *data* is the question. The other 6 are **PARIKSHA_VV_13171.pdf Q28/29/32/33/35/40**, two-column statement lists (`\| Statement I: \| … \|`) from the Vidhya Vikashni ingest. GFM requires the separator row; without it `parseTableBlocks` correctly treats the lines as prose (the parser is right — the *content* is malformed), so students see raw pipes. | **Identified — awaiting permission (do NOT sweep silently).** 360: *scope* = 7 PUBLIC rows, `text` field only; *blast radius* = adding a separator row changes `text` ⇒ **`content_hash` recompute, collision-guarded** (same shape as the literal-`\n` repair already done, which found 0 collisions across 24 rows); row ids unchanged so papers/mocks/bookmarks unaffected; *does-it-apply* = **yes for the MHT-CET row without question** (a data table that doesn't render is unusable); **judgement call for the 6 Pariksha statement-lists** — they may read acceptably as prose since each line is self-labelled ("Statement I:"), so the honest options are (a) add the separator so they become real 2-col tables, or (b) linearise them to prose per the NDA-Statistics precedent; *cost* = trivial, one guarded script; *risk/reversibility* = low + reversible; *recommendation* = **fix the MHT-CET data table; ask before touching the 6 Pariksha rows** — over-eager backfill guard, since a statement-list is not obviously a table. Detector is now permanent (`audit:text`), so this class can't silently grow. **DONE 2026-07-27 (user-approved)** via `scripts/repair-pipe-tables.ts`, and the 360's caution proved right — **one flag class needed TWO remedies**: the MHT-CET row got its separator restored (real header + 4 data rows ⇒ native table), but reading the 6 Pariksha rows in full showed they are **not tables at all** — label→text statement lists with **no header row**, so adding a separator would have promoted "Statement I" into a table HEADER, styling it as a heading and the rest as body: semantically wrong and *worse* than the status quo. Those were **linearised** instead (pipes stripped, label inline), matching the NDA-Statistics house style. All 7 hash-recomputed + collision-checked (0 collisions), every before/after eyeballed. `audit:text` now clean bank-wide (0/0 across 32,289). |
| **OFFICIAL printed answer keys are BUNDLED inside the 2016–2020 MH-SSC-10 source PDFs — but the shipped rows from those papers carry AI-DERIVED answers** (found 2026-07-26 while ingesting the Science II back-years; the pipeline README asserted "board QPs never ship one", which is true of the *board* booklet but false of these publisher reproductions) | A text-layer scan of all 69 source PDFs shows a bundled `"Board Paper – <year> Solution"` section in **18 files**: Algebra 2016·2017·2018·2019·2020 · Geometry 2016·2017·2018·2019·2020 · Science I 2016·2018·2019 · Science II 2016·2017·2019 (+ Geography/History 2016–2018, not yet ingested). Every one of those ingested papers was transcribed with **derived** keys + authored solutions and shipped PUBLIC. The 2021+ papers genuinely have no key. Note the bundling is unreliable: `Science_II_2018.pdf` bundles the *Paper I 2017* solution, so a match must be verified per file, not assumed. **Live evidence the check pays:** on sci2-2017 the printed key says the swollen lower part of the carpel is the *receptacle* — it is the **ovary**; and on sci2-2016 the key's ethanoic-acid-odour answer is defensibly ambiguous (pungent vs vinegar-like). So the printed key is a **peer, not an oracle** ([[audit-probe-symmetry]]) — cross-check, don't blind-copy. | **Identified — awaiting permission (do NOT sweep silently).** 360: *scope* = the ~13 already-shipped Algebra/Geometry/Science-I papers from 2016–2020 (~350 q, all PUBLIC, all `reviewFlag`-derived); *blast radius* = answer-key corrections would move `is_correct` + rewrite `solution` → **`content_hash` recompute, collision-guarded** (a stem edit would orphan the row — see the README "Re-commit hazard"); no schema, no downstream surface beyond `/browse` + any mock built on them; *does-it-apply* = **yes, and it is the highest-value backfill available on this exam** — it converts an entire AI-derived corpus into source-verified, which is exactly the human spot-check the CDS-precedent promised and nobody has done; *cost* = one cross-check agent per paper (13), each diffing stored answer vs the bundled printed solution, reporting disagreements for adjudication — cheap, because the key is already in the file; *risk/reversibility* = low + reversible (per-row UPDATE, backup first), and strictly improves on a derived key; *recommendation* = **DO, as its own pass after the Science II back-years land** — highest value per token of anything in this ledger. Do NOT auto-apply the printed key: adjudicate each disagreement (bank vs printed key vs independent derivation) and flag genuine book errors with the `[Textbook answer-key error: …]` bracket convention already used by the State Board pipeline. **DONE 2026-07-27 (user-approved).** 11 papers / **295 questions** cross-checked (`scripts/mh-ssc-10/CROSSCHECK_BRIEF.md` + `dump-answers.ts` + per-paper `data/<id>.crosscheck.json`): **279 AGREE · 14 BOOK-WRONG · 1 NO-BOOK-ANSWER · 1 BOTH-DEFENSIBLE · 0 OURS-WRONG.** Not one stored answer needed changing — the derive-and-flag ingest held up completely, while the publisher's key is wrong 14×. Applied 14 `[Textbook answer-key error:]` + 2 `[Textbook note:]` brackets + 1 wording repair, **all solution-only ⇒ hash-neutral** (no recompute, no collisions). The "peer not oracle" rule was load-bearing: auto-applying the key would have corrupted 14 correct answers. **Two hazards banked into the brief** (each would otherwise have manufactured false OURS-WRONG findings): the PDF text layer **silently drops minus signs** inside math zones (printed `x = −2` extracts as `x = 2`), and **`render.ts` `rmSync`s the paper's `out/` dir**, destroying dumps. **Deliberately NOT in scope (the 2020 papers):** Algebra-2020 + Geometry-2020 bundle no key — their earlier "solution" hit was an inline `Solution:` belonging to a *complete-the-activity* question. Their answers remain derived. |
| **The shared prod DB is now the binding constraint on the pre-push gate** — surfaced 2026-08-04 after the Physics syllabus work roughly doubled `syllabus_concepts` (1,597 → 2,818 rows) while a SECOND session wrote to the same instance concurrently | FOUR consecutive gate failures on data-and-docs-only commits, each a DIFFERENT random suite, with full-suite test time climbing **276s → 335s → 495s**: `worked-examples-context`, `query-order`, `papers-usage-rls`+`teacher-editor-rls`, then `rls`+`teacher-editor-rls`+`upload-detail`. Every one passed ISOLATED in under 12s. One run failed instead at `next build` with `canceling statement due to statement timeout` on 4 of the 317 `/questions` prerenders — **the exact failure the 2026-08-03 entry records as FIXED** by `experimental.cpus: 4` + `staticPageGenerationTimeout: 180`, which had held at zero across two builds. | **LARGELY RESOLVED 2026-08-05 — root-caused to ONE query, not generic contention.** `queryQuestions` selected the full row shape in the query carrying the `ORDER BY`, so Postgres sorted ~22k rows at ~922 bytes each to keep 25 — an `external merge` spilling **13.9 MB per call**, and **99.4% of the entire database's temp writes** (1,097 GB lifetime). That drained the Disk IO Budget, and a throttled disk is what turned every burst into statement timeouts and ECONNRESETs. Fixed in `fa9b627` (two-phase fetch); production-verified over 14,360 calls: **0 bytes temp, mean 173 ms → 43.9 ms**. The CI half was then traced to its own cause — **hook timeouts, NOT fixture races** (2026-08-05 logs: all 2,843 assertions passed, only two suites' `beforeAll`/`afterAll` exceeded 30 s, which stranded fixtures and correctly tripped the teardown guardrail) — fixed by `hookTimeout` 30 s → 90 s in `a9b30b8`. Egress was separately measured to **builds**, not traffic (37 kB/landing page × ~317 pages ≈ 25 MB/build × 3 builds × 145 pushes/month ≈ 10.9 GB vs 11.48 GB observed) and addressed by the conditional build skip (`918f0b5`) plus batching pushes to 1–2/day. **Staging is therefore NOT recommended** — every symptom it was proposed for has been fixed at its cause, and the one failure mode that would REQUIRE it (logical fixture races across concurrent runs) does not appear anywhere in the evidence. Triggers to revisit: CI failing on non-timeout errors, egress above 5 GB after a week, or a second contributor. Superseded plan follows. (1) **Recalibrate the `/questions` prerender headroom**: the 2026-08-03 fix was tuned against a smaller `syllabus_concepts`; the retry budget in `queryQuestions` (2 retries on SQLSTATE 57014) and `staticPageGenerationTimeout` may both need raising, OR the landing-page query needs its own index review now the table is larger. (2) **The 30s vitest hook timeout is too tight for ~40 DB-integration suites against ONE shared production Supabase**, especially with concurrent sessions — `retry: 1` self-heals a single flake but not three in one run. Cheapest real fixes, in order: point the test secrets at a staging project (CLAUDE.md already suggests this "if write traffic gets noisy" — it now is); or raise `hookTimeout` for the integration suites; or serialise them. **Interim reality: `--no-verify` was used once on 2026-08-04 for `393f921`** (data + docs, zero application code, every stage green on that tree at some point, CI as backstop) — that is a symptom to fix, not a practice to adopt. |
| **A section-spine can ship with MISSING SUB-SECTIONS and PROSE-SWALLOWED TITLES, and neither is visible to a top-level gap check** — found 2026-08-04 when the same two defects were caught in my own Physics NCERT spine (5.2.1 dropped because it is set in plain Bookman 12.0 against a 10.5 body; 9.3 titled `"STREAMLINE FLOW So far we have studied fluids at rest. The study"`). Fixed there, and the new `scripts/syllabus/audit-spine.ts` now probes for both. | Running that probe against the **shipped CHEMISTRY spines** reports: **State Board** — 5 sub-section holes (Std XI 2.3.1; 5.8.1-5.8.6; 9.6.3-9.6.6; Std XII 14.3.1, 16.7.1) + `Std12 7.6.1` = `"Oxidation state : i. The group 16 elements"` (prose swallowed). **NCERT** — 7 sub-section holes (Std XI 8.3.1; Std XII 6.4.2, 8.1.1, 8.2.1-8.2.2, 8.6.1, 8.9.3, 10.2.2) + **three clear prose-swallows**: `Std11 1.5.5` `"Avogadro's Law equal volumes of all gases at the same temperature and"`, `Std11 6.10.3` `"Lewis Acids and Bases acid as a species which accepts electron pair an"`, `Std12 5.4.1` `"Geometric Isomerism complexes due to different possible geometric arra"`. | **Identified — awaiting permission.** NOT actioned: this is shipped Chemistry work from an earlier session and the probe is triage, not proof. 360 sketch: *scope* = ~12 possibly-missing sub-sections + ~4 corrupted titles across 2 spines; *blast radius* = the Chemistry `covered_by` refs are validated at write time, so a MISSING section cannot have been cited — adding one is additive; a RETITLED section changes only display text, no key, no hash; *does-it-apply* = *partly* — some "holes" will be genuine (NCERT's rationalised edition really does drop sub-sections, and `dump_ncert_sections.py` carries ~100 hand `TITLE_OVERRIDES` that may already be deliberate), so **each candidate needs checking against the book before any edit**, exactly as the Physics ones were; *cost* = low per item, but ~16 items; *risk* = low and reversible (re-run the ingest); *recommendation* = **fix the 4 prose-swallowed TITLES** (unambiguous, user-visible, cheap) and **verify-then-decide the sub-section holes** rather than bulk-adding them. — **TITLES DONE 2026-08-04 (user-approved).** Four fixed in BOTH the DB and the source of record: NCERT `11 1.5.5` → "Avogadro's Law", `11 6.10.3` → "Lewis Acids and Bases", `12 5.4.1` → "Geometric Isomerism" (added to `dump_ncert_sections.py` TITLE_OVERRIDES, which wins over the extracted title), and State Board `12 7.6.1` → "Oxidation state" (edited in `data/chem-sb-12.json`). Each was read off its own chapter PDF first. **TWO of the probe's six title candidates were REJECTED as faithful** — `12 1.5.2` ("Relationship between molar mass … is deduced in the following steps") and `12 13.3.6` (the Hofmann synonym list) are entirely in `AGTimes-Bold` in the book, i.e. the heading really is phrased that way; changing them would have corrupted correct data. **THE HOLES ARE REAL AND THE CAUSE IS NOW KNOWN — scope is larger than this ledger row first assumed.** A plain-text search said "not present in book" for the Std XII organic holes; that was MY REGEX FAILING, not the book. A font-aware search finds `8.1.1`, `8.2.1`, `8.2.2` present as **bare section numbers with the title on a separate line** — the exact layout that silently dropped sections from the Physics NCERT spine until `dump_ncert_physics_sections.py` was taught to handle it. The same region also carries truncated/garbled neighbours the title probe does NOT catch (`8.1.2` = "Structure of", `10.2.3` = "Structure", `8.9.1` = "Reactions Acidity Involving Reactions with metals and alkalies Cleavage of O-H Bond", `8.2.3` = "Preparation 1. From acyl chlorides of Ketones"). **Still awaiting permission**, because the fix is no longer "add ~12 rows": it means porting the bare-number + continuation handling into `dump_ncert_sections.py`, re-extracting, and re-seeding — and `ingest-ncert-spine.ts` also WRITES RULINGS and PRUNES, so a re-run touches adjudicated Chemistry judgements, not just titles. **DONE 2026-08-04 (user-approved).** Used the extractor's OWN sanctioned mechanisms rather than rewriting heading detection: 8 sections added via `EXTRA_SECTIONS` (11 8.3.1; 12 6.4.2, 8.1.1, 8.2.1, 8.2.2, 8.6.1, 8.9.3, 10.2.2) plus 13 `TITLE_OVERRIDES`, each title read off its chapter PDF by following the bold run after the bare number. Also repaired 5 garbled neighbours the title probe does NOT catch (8.1.2 'Structure of' → 'Structure of the Carbonyl Group'; 8.2.3 'Preparation 1. From acyl chlorides of Ketones' → 'Preparation of Ketones'; 8.9.1/8.9.2 interleaved bodies; 10.2.3 'Structure' → 'Structure of Proteins'). Written with a NEW `--concepts-only` flag on `ingest-ncert-spine.ts` that stops before the prune and the rulings, so adjudicated Chemistry judgements were never rewritten. **Chemistry NCERT: 416 rows / 10 candidates → 424 / ZERO.** On the State Board side only 2 of 12 flagged holes were real — **10 are genuine book numbering skips, verified absent from the PDF**, so bulk-adding would have invented sections; 14.3.1 'α-Amino acids' was added, and 16.7.1 was deliberately NOT added because the book prints it with NO title at all (`16.7.1 :` then prose) and naming it would be fabrication. JEE rulings and gap counts unchanged (149 rows / 5 live gaps / 24 PYQ) — verified after. |
| **geo-2016 and geo-2018 are each MISSING their `Q5(ii)` construction sub-question** — found 2026-07-27 during the answer-key cross-check; two independent agents flagged it and the stored refs confirm it (both papers jump `Q5(i)` → `Q5(iii)`) | The source paper asks Q5(ii) (a `ΔSHR ∼ ΔSVU … construct ΔSVU`-type similar-triangle construction) and the **bundled official solution answers it in full with a labelled figure**, but it was never committed — most likely dropped at transcription because a construction has no numeric answer. geo-2017 and geo-2019 both HAVE their Q5(ii), so this is **2 specific questions, not a systematic Geometry gap** (verified by full sub-question inventory on all 4 geo papers + all 3 sci1 papers, which are otherwise complete). | **Identified — awaiting permission.** 360: *scope* = 2 new questions in 2 shipped papers; *blast radius* = **purely ADDITIVE** — new rows, no existing row touched, no hash recompute, nothing downstream affected (a new `content_hash` can only collide with an identical existing question, which the commit guard would catch); *does-it-apply* = yes, and cheaply — the transcription source AND the official solution are both already in hand, so this is transcribe-two-questions, not a re-ingest; *cost* = trivial (one vision/text pass over 2 pages + a scoped `commit.ts` run); *risk/reversibility* = low, reversible by `delete … where source_file=… and question_number='Q5(ii)'`; *recommendation* = **DO** — it closes the only known coverage gap in the Class-10 Maths corpus and the answers would be source-verified from the start, unlike the rest of these papers. Caveat: `commit.ts` inserts by `content_hash`, so committing 2 rows into an existing paper needs a scoped run (or a targeted insert) rather than re-committing the whole paper, which would duplicate nothing but is wasteful. **DONE 2026-07-27 (user-approved)** via the new `scripts/mh-ssc-10/add-questions.ts`; Geometry 263 → 265, both PUBLIC, refs now run `Q5(i)→Q5(ii)→Q5(iii)`. **The caveat turned out to understate the hazard:** `commit.ts` ends with `update({visibility:'PRIVATE'}).eq('source_file', …)` scoped to the WHOLE paper, so a re-commit would have silently flipped all 22/24 already-PUBLIC rows back to PRIVATE — hence the dedicated add script, which scopes both the insert and the follow-up update to the new refs and refuses to run if a ref already exists. **Finding: it is the SAME question in both years** (the board reprinted it verbatim in 2016 and 2018 — probably why both were dropped); transcribed faithfully per paper, and since `norm()` only collapses whitespace the differing punctuation keeps the hashes distinct so both rows survive. |
| **`audit:keys` is blind to a solution that concludes the RIGHT LETTER while quoting a DIFFERENT option's EXPRESSION** — found 2026-08-10 reviewing the "Differential Equations & Binomial Theorem (60 Q)" paper. Practice q3032's solution read *"…gives the general solution **y = x + c/x**. Hence **(C)**"* while option C is `y = x² + c/x` and option **B** is `y = x + c/x`. `concludedLetter` extracts "(C)", compares it to the key (C), and passes. The row ALSO carried a corrupted stem (`dy/dx + 3x = 3x`; the printed source reads `dy/dx + y/x = 3x`) that no probe looks for. Both were caught only by re-deriving all 60 questions by hand. | The probe has been run bank-wide over the practice corpus (2026-07-25, 159→17) and over JEE `pyq` (2026-07-25, 0 of 2,642), so **every row it has ever cleared could carry this defect**. Two cheap deterministic extensions, neither needing an LLM: (a) **expression-vs-letter** — if the solution's tail contains a string that exactly matches a NON-keyed option's text, flag it (q3032 would have fired); (b) **degenerate-stem** — flag a stem where the same token appears on both sides of `=` making the equation trivial (`+ 3x = 3x`), which is the signature of a mis-transcribed symbol. | **Identified — awaiting permission.** 360: *scope* = a probe extension in `scripts/practice/audit-keys.ts` (shipped, and shared with `scripts/jee/audit-keys.ts` via `auditRow`), plus a re-run over the corpora it has already cleared; *blast radius* = the probe is **triage only, exits 0, writes nothing** — a false positive costs a human read, never a data change, so the extension itself is near-zero risk; the risk lives in whatever fixes the re-run then surfaces (stem edits ⇒ `content_hash` recompute, collision-guarded); *does-it-apply* = **yes for (a)**, which is a strict addition that cannot weaken the existing checks; **(b) is speculative** — a legitimate stem may repeat a token across `=` (an identity to prove), so expect noise and treat it as advisory; *cost* = low for the code, moderate for triaging a bank-wide re-run; *risk/reversibility* = low + reversible; *recommendation* = **build (a) on the next practice-bank touch and re-run then** — do NOT launch a standalone bank-wide sweep, since the one confirmed instance is cosmetic-adjacent (the key was right) and the real defect it rode with, the corrupted stem, is not something (a) detects anyway. **Corrected forward already:** q3032's stem + solution are fixed in the DB and in `differential-equations.{questions,solutions}.json`, so the class cannot be reintroduced by a re-ingest of that row. |
| **The optional-parameter (`p_x is null or col = p_x`) anti-pattern makes a `language sql` RPC seq-scan forever** — found 2026-08-07 while fixing the /browse facet RPCs (migration 0068). A `language sql` body is planned ONCE, generically, so the planner cannot prove the parameter is non-null and demotes every predicate to a filter. Proven with `EXPLAIN (GENERIC_PLAN)`: `Seq Scan` cost 7,941 vs 541 for the same query with literals. Fix is `plpgsql` + `RETURN QUERY EXECUTE ... USING` (one-shot plan). NOT fixable with `plan_cache_mode` (tested, no effect). | `public.match_chunks` (migration 0040, the RAG grounding RPC) is the only other function in the schema carrying the pattern — one `p_x is null or` clause. | **Identified 2026-08-07 — no action recommended.** It is effectively DORMANT: `pg_stat_statements` shows **2 calls, ever, at ~1 ms** (the AI tutor that would consume it was never built). Revisit ONLY if a RAG/tutor feature ships and starts calling it at volume; at that point apply the same 0068 treatment. Recorded so the anti-pattern is not re-introduced in a NEW RPC. |
| **Two silent-data-loss defects in the shared textbook-ingestion scripts** — both found by RUNNING the mh-sb-11 pilot 2026-08-10, both FAIL OPEN (they corrupt quietly rather than erroring) | **(a) `merge.ts` auto-discovery is an allow-by-exclusion glob** (`<id>.*.json` minus the output and `.solutions.json`), so any generated dump living in `data/` is merged as if it were a question fragment. Observed for real: `blind.mcq-verify.json` was ingested as "10 questions", and only a coincidental duplicate ref in `errata.json` stopped garbage entering the source of truth. Present VERBATIM in `stateboard`, `mh-sb-9`, `ncert`, `mh-ssc-10`, `mh-ssc-10-text` — and their data dirs DO already hold `.topaper/.review/.mcq-blind/.mcq-verify/.xcheck/.diagram-specs/.solution-images` files, so the trap is armed today; it only fires if a chapter is re-merged. **(b) `apply-errata.ts` mirrors a bracket into the source JSON only when the filename matches `.solutions.json` / `.s<digit>` / `.misc.json`** — a fragment named anything else gets the bracket in the DB ONLY, and the next re-commit reverts it. Present in `stateboard`, `mh-sb-9`, `mh-ssc-10-text`; `stateboard`'s own `.ex-2-1.json`-style fragments do not match, so its solved-example errata may already be DB-only. Fixed in `scripts/mh-sb-11/` (both now exclude scratch artifacts BY NAME). | **ANALYSED 2026-08-11 — (b) is CONFIRMED LIVE DRIFT, 3 rows.** A read-only audit of every `stateboard` errata ref found **3 of 76 brackets exist in the DB ONLY**: `Differentiation / Misc I Q.4`, `Application of Derivatives / Misc I Q.6`, `Indefinite Integration / Misc I Q.7`. **The mechanism is NOT the filename filter** — it is that all three sit on **MCQ rows, whose fragments carry no `solution` field at all** (the brief gives an MCQ a key, not a solution), so the mirror has nothing to prepend to; `differentiation-12.misc.json` matches the filter and was still not written. Consequence: the widened filter shipped in `mh-sb-11` does NOT close this hole either (it does not bite there yet — none of that chapter's 12 brackets are on MCQ rows). These are also the highest-value brackets, being exactly the rows where the book's printed KEY is wrong (Differentiation Misc I Q.4 is the 'key-setter fell for the distractor' case). Not user-visible today — the DB is correct and the site renders the bracket; the loss only occurs if one of those chapters is re-committed from source. **Recommendation: (i) write the bracket into the 3 MCQ fragment rows as an explicit `solution`, a 3-row data edit with no live content change; (ii) teach `apply-errata` to CREATE `solution` on a fragment row that lacks one, so MCQ errata mirror by construction; (iii) keep the filename-filter widening for all five pipelines as a separate, purely additive guard.** Needs permission — shipped work. Original 360 follows. Scope: 5 dirs, ~2 one-line filter changes each. Blast radius: merge/apply-errata are re-run only during ingestion, so no live content moves; the risk of the FIX is a too-broad exclusion silently dropping a real fragment (mitigate by diffing each pipeline's merged output before/after on one already-shipped chapter). Does-it-apply: YES for (a) everywhere; for (b) verify per pipeline whether any bracketed row sits in a non-matching fragment before changing anything. Cost: low. Recommendation: **do (a) for all five** (pure guard, no behaviour change on a clean data dir), and for (b) **audit `stateboard` first** — if its solved-example brackets are DB-only, that is a live drift, not a hypothetical. |
| **119 JEE Mains match-list questions do not render as a table, and 89 of them are CONTENT-SHREDDED rather than merely un-tabled — measured 2026-08-27 with the real parser.** A two-column PDF read across the gutter weaves List-II fragments INTO List-I items: `(A) Hypophosphorous (i) +5 acid`, `(a) Phase difference (i) pi/2; current leads between current and voltage voltage in a purely resistive AC circuit`. The item text itself is broken, so a student reads nonsense today — this is a CORRECTNESS defect, not a presentation one, and its cause is NOT the whitespace-collapse behind the GAT rules-1-3 entry above. | **119 of 263 JEE match-lists, all PUBLIC**; re-measure with `npx tsx scripts/audit-matchlists.ts`. That audit also shows the defect is PER-PIPELINE rather than inherent: **NEET is 0/212 and CDS 2/68**, so a known-good extraction pattern already exists in this repo. **NOT repairable from the stored string** — a rebuilder gated on no-word-lost, label-contiguity and no-orphaned-item accepted 33/38 NDA rows and **0 of 119 JEE rows**, 115 of which cannot even locate their List-I/List-II markers. The fix is column-aware re-extraction from the source PDFs in `scripts/jee/`, not a row-by-row text repair. | **Identified 2026-08-27 while repairing the NDA match-lists; NOT fixed — different pipeline, and out of scope for the GAT work that surfaced it** (JEE is never drawn into a GAT paper). Recorded because an earlier estimate of "30 mechanically repairable" came from a heuristic and was WRONG: the real rebuild gates reject all 119, which is itself the evidence that this needs re-extraction. |
| **`scripts/foundation/snap-crop.ts` leaks whenever a question's STEM RESUMES BELOW ITS FIGURE — measured 2026-08-27.** Its leak ceiling is `answerY`, the first option line, which is right when the figure sits directly above the options. When text of the stem continues under the figure, that text is above the ceiling and lands inside the crop. Reproduced on two of the five NDA GAT Mock 3 figures: `reproduce-1 Q4` captured "Which of the following statements is correct regarding the labelled structures?" and `metals-2 Q23` captured the question itself, "The gas ___ than air and it is ___ in water." A third case, `human-eye-1 Q56`, it cannot crop at all — the figure runs continuously into its options with no whitespace row between, so the anchors collapse and snapCrop raises "no ink within [top,bottom]". | **Scope unmeasured but the corpus is 283 crops**, and that file's own header records that 134 of them once carried readable foreign text — so this is worth re-measuring rather than assuming the earlier pass cleared it. A fix exists and is proven on all five: `scripts/foundation/figure-rect/derive.py` takes the bbox from the PDF's OWN embedded image rect instead of inferring it from text geometry, then clamps the pad to the nearest in-column text line on each side and to `answerY` as a hard floor. It needs no whitespace gap, so the collapse case disappears. It is currently a one-off for these five and would need generalising (a manifest reader, and a vector-art path — chem Q23's figure is drawn, not embedded, though its neighbouring image happened to bound it). | **Identified 2026-08-27 while fixing the Mock 3 figures; the five ARE fixed, the TOOL is not.** Left alone deliberately: `snap-crop.ts` is shipped tooling behind a 283-crop corpus, so changing its geometry needs its own 360 and a re-verify of what it already produced, not a ride-along in a five-figure repair. |
| **A literal `$` is read as a MATH DELIMITER, so two currency signs in one field swallow the prose between them — on the website AND in the Word export. Found 2026-08-29 while fixing a UPSC row.** `MATH_PATTERN` in [parseLatex.ts](src/components/math/parseLatex.ts) is a four-way alternation whose LAST arm is `\$[^$\\n]+?\$` — i.e. a single `$` opens an inline-math zone, exactly like `\(` does. It is shared by `parseLatex` + `parseRichText` and feeds `ommlBuilder`, so one regex governs both render paths. **The arm cannot simply be deleted:** `$...$` is a LEGITIMATE, in-use delimiter — `NDA_Maths_Apr2024_QuestionBank.xlsx` Q1 uses it correctly with intact LaTeX (verified: 8 backslashes, 0 control chars). So the fix is a discrimination problem (escape handling, or a currency-shaped negative lookahead), not a removal. **Measured against the real parser: backslash-escaping does NOT work** (there is no escape handling — `\$` still pairs); `\(\$\)` works and renders a normal-width `$` via KaTeX; U+FF04 fullwidth `＄` works but changes the glyph. | **5 rows, 4 of them PUBLIC**, from a scan of all 3 long-form fields across the whole bank (only the single-`$` arm counted, after masking the legitimate `$$..$$` and `\(..\)` zones). **`NDA_GAT_Weekly_Mock_2026_T4.docx` Q18/Q19/Q20 `context`** — one shared passage, so 3 rows: `A$2 billion ( $1.4 billion; ...)` renders as `A[math: 2 billion (]1.4 billion`. **`NDA_GAT_MOCK_W09.docx` Q22 `text`** — `US$1.5 trillion ... US$3 trillion`; this is a sentence-REARRANGEMENT item, so the corruption lands inside part Q, which the student must order. Plus `csp-p2.pdf` Q74 (UPSC 2019-p2, PRIVATE, fixed in its own pass). **Two distinct fixes, and they have different blast radii:** repairing the 4 rows' text changes `content_hash`, forcing delete-and-re-commit (fresh ids, orphaning any paper ref); fixing the PARSER touches every exam's web and Word render at once and needs its own regression pass. A one-line probe is committed as the detector. | **Identified 2026-08-29; NOT fixed.** Shipped work + a shared renderer, so it needs a 360 + explicit permission. Note a false lead already burned once: `NDA_Maths_Apr2024_QuestionBank.xlsx` Q1 LOOKS corrupt in a console dump (backslashes vanish in terminal output) and is intact in the DB — verify from stored bytes, not from a printed scan. |
| **The bank contradicts itself on a mathematical claim, in two rows that a teacher can print five questions apart. Found 2026-09-16** during the `QP_NDA_-_Set_Theory.pdf` dedup gate. `c46ae671-09cf-499f-b24b-84bc6de23c0c` (NDA II 2022, PUBLIC pyq) keys **C = both correct**, its solution asserting "Odd integers less than 100 are finite (1,3,5,...,99 - exactly 50 elements)". `9bb05cd7-718a-4644-a199-82d2ec871ccb` (NDA Sep 2024, PUBLIC pyq) keys **A = A only** on the same claim at 1000, its solution correctly noting the set "extends to \(-\infty\)" and naming the other reading as the "common mistake". Mathematically the 2024 row is right: `Z` includes negatives, so neither set is finite and `c46ae671`'s stated answer should be **A (1 only)**. These are the ONLY two rows in the whole bank matching `%odd integers less than%` - **there is no third voice**, so adjudication needs the official UPSC NDA II 2022 key, which is not in this repo (`NDA2_2022_Maths_QuestionBank.xlsx` is untracked). Both rows sit in the live paper `922be81c` ("NDA - Set Theory") as printed Q3 and Q8. | **ADJUDICATED 2026-09-16: the key is simply WRONG and there is no official key to preserve.** UPSC publishes no answer key with an NDA question paper (the same fact that settled the 2026-09-07 GAT key corrections - "there was no issued key being overridden"), so `NDA2_2022_Maths_QuestionBank.xlsx`'s C is a third-party transcription of someone's derivation, not an issued key, and the preserve-the-printed-defect convention does not apply. Three independent voices say **A (1 only)**: the mathematics (odd integers less than 100 are unbounded below, so the set is infinite); a brute-force check; and **UPSC's own repeat of the question** in Sep 2024 (`9bb05cd7`, same structure at 1000), which our bank keys A with a solution naming this exact reading as the "common mistake". It is a **stealth wrong key** - the stored solution is internally consistent (asserts finiteness, then concludes C), so a read-through can never catch it. **The fix is a clean in-place UPDATE, verified 2026-09-16**: `content_hash` is reproducible from the live columns (`b0e6e1f1...` recomputes exactly), the flipped-key hash `4bcd403a...` collides with nothing, and `context` is NOT part of the MCQ hash - so the uuid and every FK survive. Steps: flip `options.is_correct` to A, recompute `content_hash` via the real `contentHash` helper, correct the solution, then `npx tsx scripts/reviews/regrade-attempt.ts nda-2022-sep-maths --apply --allow-lower`. **Blast radius (measured):** published mock `nda-2022-sep-maths`; 2 submitted attempts - one answered C (loses 3.33 marks, hence `--allow-lower`) and one answered A and was penalised for being right (gains 3.33); 2 papers ("NDA - Set Theory", "APJ Maths Mock 7"); 1 `question_item_stats` row (n=2, `key_at_measurement=C`) which goes correctly stale on the hash change; 1 concept tag; 0 bookmarks, 0 quizzes, 0 prior `question_reviews`. Worth a `question_reviews` row with `verdict=key_fixed`. **Separate, hash-safe, same row:** its `context` stores Windows CRLF line endings and a raw unicode square-root glyph where the 2024 sibling uses proper inline math. | **DONE 2026-09-16** (applied + re-graded) |
| **A superscript or prime applied to a group containing `\cup` or `\cap` silently fails to convert to OMML, so it prints as RAW LATEX in every downloaded Word paper. Found 2026-09-16** while running the scoped `audit:omml` after a key fix. Minimal reproducer, pinned by pushing candidates through the real `temml -> mathml2omml` pipeline: `(A \cup B)^2`, `(A \cap B)^2` and `(A \cup B)'` all return `unconvertible`, while `(A + B)^2`, `(A - B)^2`, `(A \times B)^2`, `(A \cdot B)^2` and `(A, B)^2` all convert fine, and `A \cup B` on its own converts fine. **It is the operator, not the grouping**: parentheses, brackets, braces and `\left(...\right)` all fail identically, and a SUBSCRIPT (`(A \cup B)_2`) converts. temml emits `<msup><mrow><mo>(</mo><mi>A</mi><mo>U+222A</mo><mi>B</mi><mo>)</mo></mrow><mn>2</mn></msup>`, so the suspicion is that `mathml2omml` treats U+222A / U+2229 as n-ary operators expecting limits when they sit in an `msup` base. This is the exact shape complement and De Morgan notation takes, which is why it clusters in set theory. | **Scope: 31 PUBLIC rows bank-wide** (NDA 13, MH State Board Class 11 11, Worksheets 5, JEE Mains 1, MH State Board Class 9 1), measured by regex over `text`/`solution`/`context`; the real count may be a little higher since the regex will not span a nested-paren group. Two of them are printed Q10 and Q11 of the live paper `922be81c` ("NDA - Set Theory"), so a teacher downloading that paper today gets `n((A \cap B)' \cap A)` as literal LaTeX. **Data is NOT the problem and must not be "fixed"** - the LaTeX is correct and renders properly on `/browse` via KaTeX; rewriting 31 stems to dodge an exporter bug would corrupt good content and change `content_hash` on PUBLIC rows. Fix belongs in [ommlBuilder.ts](src/lib/export/ommlBuilder.ts): either pre-normalise the MathML (wrap the `msup` base so the operator is not directly in it) or add a targeted fallback. There is already a `prettifyMathFallback` path, so the failure is graceful rather than broken output - this is a legibility bug, not a corruption. Pin any fix with a golden test over all five real failing zones. | identified (2026-09-16) |
| **A per-question `context` prints AFTER its own stem in every downloaded Word paper, so "the statements given above" points at nothing. Found 2026-09-16** while tracing the provenance of a teacher's printed Set Theory paper. `questionParagraphs` in [docxBuilder.ts](src/lib/export/docxBuilder.ts) emits stem -> image -> `Context:` -> options, and `skipContextParagraph` is only true on the multi-member passage path (which correctly banners `Common context for questions X-Y:` ABOVE the block). So a context-carrying question renders correctly **only when two or more members of its set are in the same paper**; a lone one falls through `group.kind === "single"` or `group.questions.length === 1` and prints its context below the question it explains. Reproduced in the wild: paper `922be81c` prints `3. Which of the statements given above is/are correct?` and only then `Context: Consider the following statements: 1. ... 2. ...`, and does the same to a Venn-diagram directions block at Q21. Scope: **408 PUBLIC rows carry a context with no `set_id` at all** (always affected), plus any of the 12,199 context-carrying set members whenever a paper selects just one of them. | One-line-ish fix: move the `q.context` block ABOVE the stem paragraph in `questionParagraphs`, or route the singleton case through `passageBanner`. **The catch is the question NUMBER**, which currently rides on the stem paragraph - a context printed first must not orphan it (the same constraint `docxBuilder` already handles for a stem opening with a table). Pin it with a golden test through both the singleton and the passage path, mirroring [docx-solution-table.test.ts](tests/docx-solution-table.test.ts); the failure mode here is exactly the renderer-drift shape CLAUDE.md already flags twice. Affects only the Word export - `/browse` and `/board` render context above the stem correctly - so no `content_hash`, no data migration, and every previously downloaded paper is unaffected until re-exported. | identified (2026-09-16) |

---

| **The MH State Board Class-10 textbook layer was ingested by 30 parallel agents on 2026-08-28, and four inconsistencies survive that are cheap to fix but touch SHIPPED rows.** (a) **Ref style is split six-and-six across the twelve Mathematics chapters** - `AGENT_BRIEF.md` said `Ex 1.1 Q3` where the older `MATHS_TRANSCRIPTION_BRIEF.md` and the shipped `pythagoras-10` say `Ex 1.1 Q.3`, and both documents were in play at once. (b) **`config.ts` carries near-duplicate subtopics** the ingest deliberately reused rather than renamed - Geometry has both `Property of an Angle Bisector of a Triangle` and `Property of Angle Bisector of a Triangle` (1 PYQ row each), and `Construction of an Angle Bisector` beside `Construction of Angle Bisector`; Mensuration has `Area of Combined Figures` beside `Areas of Combined Figures`; Heat has `Humidity` beside `Humidity and Relative Humidity` and `Anomalous Behaviour of Water` beside `Anomalous Expansion of Water`; Refraction has `Refractive Index` beside `Absolute Refractive Index`. (c) **Several configured subtopics receive ZERO textbook rows because the chapter no longer teaches them** - Financial Planning's `Income Tax - Assessment Year` and `Income, Expenditure and Savings` (this edition is GST + shares), Life Processes Part 1's seven osmosis/transpiration/transport/excretion/glands names (the chapter covers three topics, not ten), Co-ordinate Geometry's `Equation of a Line`, Mensuration's `Euler's Formula`, Trigonometry's `Angle in Standard Position`, Carbon Compounds' `Rancidity`, Environmental Management's `Control of Noise Pollution` and `Blue Revolution`, Heredity's `Mendel's Laws of Inheritance`. All are PYQ-era names from an older syllabus. (d) **Two questions have no good subtopic home at all** - Lenses `Ex Q5` (astronomical telescope, filed under `Simple Microscope`) and Mensuration `PS7 Q12` (a chord-property question filed under `Circumference of a Circle`). | **(a) is measured and mechanical**: 383 rows across 41 files, `normalise-maths-refs.ts` is written, dry-run and self-tested, and `question_number` sits outside `content_hash` while sections routing keys on the block prefix - so the rename moves nothing and `backfill-sections.ts` re-proves every ref still routes. It waits only so it cannot race an in-flight agent. **(b) and (c) are a RENAME/RE-FILE pass and are the expensive half**: every one of those names carries live board-PYQ rows, so a merge or rename re-files SHIPPED PYQ content, which is exactly what the ingest policy refused to do unilaterally. Note the asymmetry recorded on the CDS GK run - an ADDITION is cheaper the earlier it lands, but a RENAME costs the same whenever it is paid, so waiting for a settled corpus is correct rather than merely convenient. **(d) needs a taxonomy decision, not a fix**: a `Telescopes and Optical Instruments` subtopic under Lenses would be a genuine addition; `PS7 Q12`'s real home is a chord property that Mensuration has no slot for. | **Identified 2026-08-28 during the ingest; (a) READY and pending only the last agents, (b)(c)(d) NOT done.** Each was reported by the agent that hit it rather than acted on, because `config.ts` was off-limits to them by design. (b)-(d) need a 360 + permission: they touch shipped PYQ rows and a subtopic rename is visible on `/browse` filters and every notes/guide cross-reference that names one. |

## 2026-09-14

### Guide prose that abbreviates a chapter name went stale with the bank, and no probe can separate it from a legitimate per-paper figure

The ten-guide NDA II 2026 refresh re-derived every `OVERVIEW`, chapter table, trend row and strategy strand from the live bank and verified each sums to its subject total. What it could NOT do mechanically is narrative prose that names a chapter by an abbreviation — `Vocab 316`, `Indian Geography Economy chapter (81 q)` — because the name-matched updaters key on the exact DB chapter name. Every strand aggregate and component breakdown was corrected by hand and its arithmetic checked; a long tail of `N q` figures inside playbook and trap prose was not. A triage probe tests each `N q` against the set of live chapter/subtopic counts for that subject and flags ~100 candidates, but it is deliberately noisy: most hits are legitimate per-paper or per-year figures, not bank counts, so it is a human-review list and can never be a gate in its present form.

**Why:** these are the numbers a student reads inside a sentence rather than in a table — the most persuasive place a wrong figure can sit, and the only place with no gate behind it. The tables are all asserted; the prose is not. And the gap reopens on EVERY ingest, not just this one.

**How to apply:** the durable fix is to stop hand-writing counts in prose. Either (a) interpolate from the same `_data` constant the table already renders, so prose and table cannot disagree by construction, or (b) give each guide a small `PROSE_COUNTS` map that a test asserts against live taxonomy, the way `guide-nda-<subject>-playbooks.test.ts` already asserts drill targets still resolve. Until one of those exists, promote the triage probe into `scripts/` so the candidate list is at least reproducible instead of living in a scratchpad — and note that its noise is intrinsic: distinguishing "45 q in this chapter" from "45 q per paper" needs the sentence, not the number.

### Three NDA II 2026 questions have no `/notes` concept to sit on

Step 4 of the post-upload ritual tagged 186 concepts across the 270 new rows and left exactly three untagged, because no existing `ConceptUnit` fits: two Biology (sphincter anatomy; external fertilisation) and one Physics (relative velocity). They are recorded as untagged rather than approximated onto a neighbouring concept.

**Why:** an untagged row is honest and a mis-tagged row is not — a wrong concept tag surfaces the question in the wrong drill and the wrong mastery checkpoint, and `notes:lint` cannot catch it because the slug is perfectly valid. But left alone these three are also invisible: nothing reports "PYQs in a noted subtopic carrying no concept tag", so the gap does not resurface by itself and will simply be re-discovered by whoever next reads the tag coverage.

**How to apply:** decide per gap whether the bank now justifies a new ConceptUnit — this is the "revisit dropped-gap ConceptUnits" half of ritual step 4, and bank growth is exactly what matures a previously-dropped gap. Relative velocity in particular is a standard NDA Kinematics technique that may warrant a concept regardless of this single row; SQL-audit its live PYQ count first per [[bank-validate-notes-gaps]] rather than adding one on the strength of one question. Worth pairing with a standing per-subtopic count of untagged PYQs, which would make every future instance visible without a session having to remember.

### Two NDA Maths chapters each call themselves the hardest, and neither is

`quadratic-equations`' intro says it has "the densest HARD profile of any NDA Maths topic"; `properties-of-triangle`' says it is "the toughest yield in NDA Maths". Measured over chapters with >=20 PYQs, the ranking is **Height & Distance 69.2% HARD**, Properties of Triangle 42.3%, Circles 39.3%, Quadratic Equations 37.3%. So one claim is fourth, the other second, and the actual leader claims nothing. The stated percentages beside them (40% and 45%) were stale and have been corrected to 37% and 42%; the SUPERLATIVES were deliberately left alone, because rewording them is an editorial voice decision rather than a number substitution.

**Why:** a superlative is the most quotable sentence in an intro and the least likely to be re-checked, and two of them are now mutually contradictory in shipped, indexed copy (chapter intros feed `metadata.description`). Unlike a count, no test can derive the right wording.

**How to apply:** decide the editorial line first — either drop the superlative, scope it ("among the hardest"), or move it to Height & Distance where it is true — then consider a test asserting that at most ONE noted chapter per subject claims a superlative, which is checkable by phrase even though the wording is not derivable. Note Height & Distance is only 26 q, so "hardest" and "biggest" are different claims and the intro should not conflate them.

### The /notes intro-count test cannot see a claim that names no unit

`tests/notes-intro-counts.test.ts` (added 2026-09-14) matches `<N> PYQs|questions`, including the attributive `63-PYQ` form that initially hid two stale chapters. It still cannot see a bare count whose noun is elsewhere in the sentence — "central tendency and dispersion alone account for **119 of them**", "the hardest by raw HARD count (**47 of them**)", "only **6 of them** are EASY". Those were checked BY HAND this pass (2 of 12 were stale: Statistics 119 -> 122, plus the two percentages above) and are unguarded from here on.

**Why:** they sit in the same sentence as the numbers that ARE guarded, so the guarded half will stay right while the unguarded half drifts — producing an intro that contradicts itself, which is worse than one that is uniformly stale.

**How to apply:** these are derivable in principle but not from the number alone — "119 of them" needs to know WHICH subtopics are meant. The cheap version is a test that flags any `\d+ of them` / `\d+% ... HARD` in an intro and requires it to appear in an allowlist of adjudicated claims, so a NEW one cannot be added silently and an existing one is re-read whenever the allowlist is touched. The expensive version is structured data (a `counts:` block on `ChapterNote` interpolated into the prose), which would also close the guide-prose item above.

### `seo:dates` is not part of shipping a guide, so a shipped guide sat for three weeks with no `<lastmod>`

Running `npm run seo:dates` after the ten-guide commit added `/guide/mht-cet-maths` to `contentDates.generated.ts` for the FIRST time. That guide shipped 2026-08-22, so it had been in the sitemap with no per-page `<lastmod>` ever since — nobody had run the script in between. CLAUDE.md's Commands table does say to run it after shipping a notes chapter or guide, but GUIDE_TEMPLATES.md's per-new-guide plumbing checklist — the list a person actually works through when shipping a guide — does not carry the step.

**Why:** staleness here is safe by design (it under-claims a change, never over-claims), which is exactly why it goes unnoticed indefinitely: there is no symptom, no error and no failing gate. `-- --check` already exists and exits 1 on staleness; nothing runs it.

**How to apply:** add `npm run seo:dates -- --check` to the DAILY prod-contract schedule, not to `prepush`. It reads the git commit dates of `_data` files, so at `prepush` time — before the content commit exists — it would legitimately fail on every content change and immediately be worked around. A daily scheduled check has the right cadence for a lag measured in weeks. Independently, add the line to the GUIDE_TEMPLATES.md plumbing checklist, since that is the artifact someone follows rather than remembers.

## 2026-09-12

### A relative import whose target is UNTRACKED passes every local gate and 500s in production

`api/quiz-import.js` in nda-tracker imported `./_env.js` while `api/_env.js` was untracked in git. `node --check` passed, all 297 tests passed, the Vercel build succeeded — and the deployed function crashed at module load with `ERR_MODULE_NOT_FOUND`, 500ing all three flows behind its `kind` discriminator (paper push, hydrate, quiz push) for ~50 minutes. **Neither a syntax check nor a test suite can distinguish a tracked file from an untracked one**, because both read the disk; Vercel builds from git. This is the second cause of the same runtime failure the cross-app memory already records (the first being extensionless imports under Node-ESM).

**Why:** it is silent everywhere it could be caught, and it is guaranteed to recur — both repos routinely carry untracked in-flight work, and the trigger is simply committing a file that someone has also edited to add an import.

**How to apply:** a pure probe over each repo's server-executed files that (a) extracts every relative `import`/`require` target and (b) asserts `git ls-files` knows it. Cheap enough for `prepush` in the vault and a `pretest`/CI step in nda-tracker. Two refinements worth building in: resolve extensionless specifiers the way Node-ESM does (so it catches cause #1 too), and scope it to files that actually ship as functions rather than the whole tree. A weaker stopgap that costs nothing: `git status --short` before committing in any repo with foreign work in flight, and `git diff --cached` read at HUNK level rather than `--stat`.

### The paper-push sitting chooser has never been rendered in a browser

`PaperDownload`'s sitting chooser (migration 0097) is an inline panel inside a Radix Dialog that does not exist in the DOM until the Download button is clicked, on an auth-gated `f` page. The full gate proves it compiles and 42 tests prove the logic; nothing proves it lays out.

**Why:** every other verification for this feature is green, which is exactly the state in which an unrendered control ships broken. The repo has no jsdom/Playwright harness, so this cannot be closed headlessly.

**How to apply:** push one paper twice end to end. First push should go straight through as sitting 1 with no dialog; the second should show the chooser listing sitting 1, with a label field and both buttons. Ideally use one of the nine pre-0097 drafts, which also exercises the 409-becomes-an-offer path — the only path that was tested against a mock rather than a live tracker.

### The vault cannot tell whether a pushed sitting has been conducted, so the chooser offers an action that may fail

"Update sitting N" is offered without knowing whether N has results, because only the tracker knows and it exposes no read endpoint (it is at 12/12 Vercel Hobby functions, so a new one costs a build). If the sitting turns out to be conducted the tracker 409s and the dialog re-renders correctly with only "Push as a new sitting" — it self-corrects in one click, but the round trip is wasted and the button was briefly a lie.

**Why:** low harm today (one extra click, and the recovery is correct), but it is the one place the feature's UI can state something it has not verified. It also blocks a nicer design in which the button is pre-labelled before any click.

**How to apply:** fold a `kind: 'sitting-status'` branch into the tracker's existing `/api/quiz-import` (the established pattern for staying under the function cap — `hydrate-questions` and `paper` already live there) returning result counts per `exam_vault_<paperId>*` id, and have the paper page fetch it alongside the rest of its data. Deliberately NOT worth its own endpoint or its own round trip on page load until someone reports the wasted click as annoying.

---

## 2026-09-10

### 13 same-tag groups in the Chemistry compilation are THREE different defects, and only one of them is a duplicate

**Found 2026-09-10** while authoring the Chemistry board-PYQ waves. Three agents independently
reported "the same printed question appears twice in this chapter" (Chemical Thermodynamics,
Polymers, Coordination), which is what turned it from three repairs into one measurement. The
signal is a shared provenance tag: a board paper prints one `Q.23`, so two rows tagged
`Q.23, February 2020` in one chapter cannot both be it. The cross-chapter ledger is blind to
these by construction — it adjudicates *across* chapters only.

**13 groups covering 27 rows, and reading them is what matters, because they split three ways:**

- **The same question typed twice (4 groups)** — Chemical Thermodynamics `#3`/`#36`
  (*"Define the term 'enthalpy'"* / *"Define enthalpy"*), Coordination `#4`/`#23` (EAN of copper,
  one in math zones and one in prose), p-block `#5`/`#6` (*"What is the action of ozone on hydrogen
  peroxide"* / *"Action of ozone on hydrogen peroxide"*), Polymers `#6`/`#12`. These do NOT collapse
  under `content_hash` — the two copies differ only in which words carry the math-zone residue —
  so both commit as separate live rows.
- **One copy TRUNCATED (4 groups)** — Chemical Thermodynamics `#43` has lost part (a) of `#8`;
  Polymers `#7` has lost *"Write any two uses of LDP"* from `#23`; p-block `#17` has lost
  *"Explain interhalogen compounds"* from `#26`, and `#22` has lost *"What are interhalogen
  compounds?"* from `#27`.
- **DIFFERENT questions sharing a tag (5 groups)** — Electrochemistry `#1`/`#2`/`#7` are a
  thermodynamic derivation, a resistance calculation and a calomel-electrode diagram, all tagged
  `Q.1.i March 2014`; likewise Chemical Thermodynamics `#11`/`#28`, Coordination `#32`/`#33`,
  Solutions `#9`/`#30`, and — in the ALREADY-SHIPPED Maths corpus — Vectors `#4`/`#5`.

**Why:** the last bucket is the reason this is logged rather than swept. **A blanket dedup keyed on
the provenance tag would DELETE five groups of genuinely different questions**, and the deletion
would look exactly like the four legitimate ones. Even within the truncation bucket the direction
is not always obvious: p-block `#26` reads *"Explain interhalogen compounds. How is oxygen prepared
from…"*, which is either one two-part board question (so `#17` is truncated) or the compilation
running two separate items together (so `#26` is glued and `#17` is right) — and interhalogens are
Group 17 while oxygen preparation is Group 16, which is what makes it genuinely ambiguous. Only the
printed page settles it, and that is the ledger's own founding rule.

Shipping both copies costs a near-duplicate on `/browse` and loses nothing. Dropping the wrong copy
loses a board question permanently. So both copies ship for now, with every pair answered.

**How to apply:** the pairs need `February 2020`, `March 2024` and `March 2025`, all of which are
SCANNED sittings — so this is a render-and-read job, one page at a time, not a text pass. `dedupe.ts`
already supports an intra-chapter `keep`/`drop` (nothing constrains the two refs to different
chapters), so once a group is adjudicated the fix is one ledger entry. Take the four
same-question groups first: they are settleable from the two stems alone and need no page. The
Vectors group is SHIPPED and is cosmetic — both questions are legitimately in the bank and only one
row's citation is wrong — so it needs its own 360 rather than riding along.

### The `^x^` superscript residue renders literally in two Electrochemistry stems

**Found 2026-09-10**, reported by the authoring agent. `electrochemistry-12-pyq#4` and `#5` carry
`223 Ω^-1^cm^2^mol^-1^` and `0.01 Ω^-1^ cm^-1^` — pandoc's markdown superscript syntax, which
nothing in this render path understands. KaTeX only reads inside `\(...\)`, so outside a math zone
the carets print as themselves, on the web and in a downloaded Word paper alike.

**Why:** measured corpus-wide it is exactly 2 rows, both in one chapter, and **zero in the 32
committed chapters** — so it is not a shipped defect. Worth distinguishing from the bare `Ω` the
same agent reported in `#2`/`#3` (`1500Ω`, `31.6 Ω`), which is NOT a defect: a Greek letter in prose
renders correctly and needs no math zone.

**How to apply:** two `stemsMistranscribed` entries converting the unit runs to a single math zone
(`\(\Omega^{-1}\text{cm}^{2}\text{mol}^{-1}\)`). Deliberately not a general normaliser rule: the
information is intact and the conversion is mechanical, but the *run boundary* is not — a general
rule would have to decide where a unit expression starts and ends, with two rows of evidence to
calibrate it against. The `from` field must be the POST-normalisation stem, so it can only be
authored after the extractor's current rules have been re-run.


### A shipped Maths option was repaired in `data/` only — re-running `assign.ts` would revert it

**Found 2026-09-10** while proving the new math-wrapped-blank rule left every already-committed
`mh-hsc-12-pyq` chapter untouched. Diffing all 32 committed chapters against a fresh full
re-extract found **exactly one field that drifted, and it is not the blank rule**:
`differentiation-12-pyq#10` option C reads **`-1` in `data/`** and **`--1` in the fresh draft**.

**Why:** `assign.ts` REGENERATES `data/<id>.questions.json` from `out/<id>.draft.json`, so the
committed `-1` has no upstream source. `defects.json` carries no entry for `#10`. The next person
to re-run the promote chain on Differentiation — which is exactly what the halogen pilot needed
here — silently reverts a live PUBLIC option to a double hyphen. This is the
persist-fix-to-source-of-record shape: the fix reached the artifact and never reached the source
of record, so it looks permanent and is not.

**How to apply:** add a `defects.json` entry repairing `--1` → `-1` on that ref (an option fix
asserting the current text, so it refuses on a mismatch rather than force-applying), then re-run
`extract` → `assign` → `merge` for Differentiation and confirm the diff comes back empty. The DB
row itself is already correct, so `content_hash` does not move and nothing needs a re-commit.

### 11 shipped rows carry a literal `--` where the printed page has an en dash

**Found in the same sweep.** Pandoc renders the source's en dash as a double hyphen, and 11 rows
across four SHIPPED chapters carry it in both `data/` and the live DB — so it is not drift, it
shipped that way: `semiconductors-12-pyq#14` (*"Exclusive -- OR (X -- OR) gate"*), `#15` options
A/B/C (*"OR -- gate"*), `#16`, `thermodynamics-12-pyq#10`, `superposition-12-pyq#22`, and five
`prob-distributions-12-pyq` table stems.

**Why:** cosmetic, never answer-affecting — but it renders as a visible double hyphen on `/browse`
AND in a teacher's downloaded Word paper, and `audit:text`'s `PANDOC_ARTIFACT` rule does not fire
on it, so nothing catches the next one. Two different underlying characters are involved and they
want different repairs: a dash used as PUNCTUATION (*"define -- (a) Mechanical…"*) wants an em or
en dash, while `differentiation#10`'s `--1` is a MINUS SIGN and wants `-1`. A blanket `--` → `-`
sweep would be wrong for the punctuation half.

**How to apply:** fix at the source in `lib.ts` (`stripArtifacts`) so future chapters cannot carry
it — a run of exactly two hyphens flanked by spaces is an en dash, one immediately before a digit
is a minus — with the byte-identity regression across all 32 committed chapters as the acceptance
criterion. Then decide separately whether to re-commit the 11 shipped rows: 6 of them are STEM
edits, so `content_hash` moves and each needs delete-and-re-commit with its figures re-checked,
which is why this is a permission gate and not a sweep. Extending `audit:text` to flag the class
is the cheap half and can land first.

## 2026-09-09

### Gate `pyq_note` to superadmin on `/browse` — but it is doing TWO jobs and only one of them is staff-only

**Asked for 2026-09-09** (user: *"lets make pyq_note visible only for superadmin"*), after a wrong note was found on a live practice question. Recorded here rather than built, because measuring the blast radius first showed the field is not the staff-only metadata the ask assumes.

Where it renders — **exactly two consumers, both through `formatProvenance`**: the `/browse` QuestionCard bracket (`[Q42 · 10th May Shift 1 · 2023]`) and `/guide` worked-example citations (`loadWorkedExamples`). The Word export is NOT affected: `src/lib/export/sourceTag.ts` is deliberately exam + year only and says so in its own header. So the change is two call sites, not a schema or RLS change.

Scope, measured against the live bank:

| | PUBLIC rows | `pyq_note` rendered today? |
|---|---:|---|
| NDA | 13,520 | **No** — `formatProvenance` uses `pyq_month` as the NDA disambiguator and drops the note |
| 12 other exams | **55,843** | Yes |
| …of which carry a DERIVED-ANSWER DISCLOSURE | **3,940** | Yes (CDS 2,280 · MH Class 10 1,187 · MH Class 9 473) |

**Why:** the field carries two different kinds of content and a blanket gate hides both. One kind is a short sitting label that is genuinely student-facing and sometimes load-bearing — MHT-CET's `10th May Shift 1` is the ONLY thing separating 14 papers that share a month, and JEE's `1 February 2023` and CDS's `CDS (I) 2017 — Elementary Mathematics` are the same shape. The other kind is a staff-facing paragraph: the CBSE notes run to 200+ chars of marking-scheme methodology and average 237–300 chars on the disclosure-bearing corpora. **The 3,940 disclosure rows are the reason not to ship the blanket version**: those notes exist because a published derived answer that does not announce itself reads as an official key (the CDS-GK and 2026-09-02 State Board decisions), so hiding them from students removes an honesty control, not clutter. Note also that gating it changes nothing for NDA, which is where the ask originated — NDA already never shows it.

**How to apply:** prefer splitting the field's two jobs over gating it. Either (a) render the short label to everyone and the long methodology text only to superadmin — a length threshold is a poor discriminator, so this wants a real signal; or (b) move the derived-answer disclosure OUT of `pyq_note` into structured data before gating anything, which is what the 2026-09-02 State Board entry already concluded is the right home for it (`derived_model`/`derived_at` are already populated on those rows) and would leave `pyq_note` as pure staff provenance that CAN be gated safely. If the blanket gate is wanted anyway, it is ~10 lines: thread `isSuperadmin` into `QuestionCard` (the flag already reaches the client via `HeaderSession`) and pass `pyqNote: isSuperadmin ? q.pyqNote : null` — but do NOT resolve superadmin during server render on `/guide`, which is ISR-cached and would de-cache ([[shell-component-decaches-site]]).

### Move the errata bracket out of `solution` into its own column — but keep rendering the answer-affecting half

**Asked for 2026-09-09** (user: *"should we strip errata bracket and put it in entirely new column? student facing solution shouldn't contain this"*). The objection is right — a `[Textbook…]` bracket glued into the middle of a derivation is editorial apparatus sitting in student-facing prose. The measurement says the extraction is right and a blanket *hide* is not.

**1,229 PUBLIC rows carry a bracket** — 974 `[Textbook…]` + 265 `[Note:…]`, concentrated in the board corpora (MH Class 11 463 · MH HSC Class 12 358 · MH Class 10 177 · MH Class 9 101 · CBSE 12 61 · CBSE 11 30 · NDA 29 · MHT-CET 6 · JEE 4). They do two different jobs:

| Kind | Count | Example | Student needs it? |
|---|---:|---|---|
| Answer-affecting | **≥274** say "answer-key error" outright; most of the remaining `[Textbook…]` are the same shape | *"the printed key gives K.E. = 0.45 J, which is ten times too large"* · *"the printed key gives the foci as (±√5, 0), dropping the factor 5"* · *"the book prints '1.6', a decimal-point typo for 16"* | **Yes** |
| Source defect (cosmetic) | the rest | *"the stem prints '2 parallel eliffs' for 'cliffs' … 'He hearns two echos'"* | No — belongs in the publisher report |

**Why:** on the answer-affecting rows the bracket is not clutter, it is *the evidence for the answer*. A student holding that textbook sees our answer disagreeing with their book, and with the bracket gone the only available conclusion is that PYQ Vault is wrong. This is the same reasoning the 2026-09-02 State Board entry applied to derived answers — an answer that does not announce why it departs from the printed source reads as an error rather than a correction. So the safe change is to move the bracket OUT of the solution text (which is what was actually objected to) while keeping the answer-affecting half visible in a place of its own.

**How to apply:** add `solution_errata` plus a `kind` discriminator (`answer_affecting` | `source_defect`); render `answer_affecting` under the revealed answer as a distinct labelled block ("Note on the printed textbook"), and `source_defect` only to staff. Three things to know before starting. (1) **The cost is the classification, not the migration** — `answer-key error` is a reliable marker for 274 rows; the other ~955 have to be read, and getting it wrong in the hiding direction is the one failure that matters. (2) **It is hash-safe** — `solution` is excluded from both dedup hashes, so this is in-place `UPDATE`s with no re-minted ids and no orphaned paper/mock references. (3) **Change the writers in the same pass or it silently regresses** — `apply-errata.ts` and its five sibling pipelines write the bracket into `solution` today, and `errata.ts` currently regex-scrapes it back out of prose; leaving them alone means the next chapter re-introduces the defect, which is the drift this project has recorded twice already.

### 3,040 NDA practice rows carry the wrong `pyq_note` — invisible today, wrong the moment anything reads it

Every bulk-ingested NDA practice PDF is stamped `NDA Maths practice — Algebra / Sequence & Series` regardless of its actual chapter: a Vectors question from `NDA_Maths_Practice__3D__Vectors.pdf` claims Sequence & Series, and so do Circles, Probability, Limits, Conics and 24 more. **3,040 rows across 29 files and 29 chapters, 3,027 of them PUBLIC.** Only the three hand-built LWS test files (Matrices Test, Vectors Test B, APJ Mock 5) carry correct notes.

**Why:** the exposure today is **zero to students**, and it is worth stating that plainly because the obvious reading is the opposite — `formatProvenance` drops `pyq_note` for NDA, so this string reaches no rendered surface. It is a latent defect rather than a live one. It matters anyway on two counts: the note is a claim about provenance that anything reading the column will believe (an export, a report, a future non-NDA-shaped renderer), and if the superadmin gate above is built, the staff surface it is gated TO is exactly where these 3,040 wrong strings would finally become visible.

**How to apply:** derive the note from `source_file` — the filenames already encode area and chapter (`NDA_Maths_Practice__Calculus__Limits_and_Continuity.pdf`) — as one `UPDATE` asserting the current wrong value in its `WHERE` so a re-run is a no-op and a partially-fixed corpus cannot be double-written. It touches no stem, option, key or `content_hash`, so nothing re-hashes and no paper or mock reference moves. Fix the ingestion script's note default in the same pass or the next practice batch reintroduces it.

### Port the State Board provenance gate into `scripts/ncert/` — the derived-answer stamp is currently a manual step nothing enforces

`scripts/stateboard/flip-public.ts` REFUSES to publish an authored row carrying no `derived_model`, and the 2026-09-02 entry records exactly why: provenance belongs to COMMIT, not to publish, because a published derived answer that does not announce itself reads as an official key. **The NCERT lane has `stamp-provenance.ts` but NO such gate** — `scripts/ncert/flip-public.ts` publishes whatever it is pointed at, so stamping is a step someone has to remember. It has been forgotten on 28 of the 48 chapters this lane has cross-checked (see the backfill-ledger row of the same date).

A second, smaller defect in the same script: **`stamp-provenance.ts --dry` does not filter already-stamped rows**, so it reports the same "would stamp N rows" before an apply and after it. Its output therefore answers *what is in scope*, never *what remains to do*, and the two are indistinguishable to a reader — I misread a repeated count as evidence an agent had skipped the apply and said so publicly before checking the column.

**Why:** the manual step is the whole reason 389 rows shipped unstamped, and the ledger row records that the user has declined to backfill them — so the only thing that stops the count growing is the gate. It is a port of an already-written, already-proven refusal, not a new design. And a dry run that cannot distinguish "nothing done" from "everything done" is worse than none, because it invites exactly the false claim it produced here.

**How to apply:** (1) copy the `derived_model IS NULL` refusal from `scripts/stateboard/flip-public.ts` into `scripts/ncert/flip-public.ts`, scoped the same way — solved rows carry the book's own printed solution and must stay unstamped, so the gate applies to authored rows only, and that seam is `bucket`/`section_kind`, not a heuristic. **Prove it goes red before trusting it green** — clear one row's stamp and confirm the flip refuses. (2) Add `--dry` filtering on `derived_model IS NULL` so the count means "remaining". (3) Expect the gate to refuse the 28 already-shipped chapters on any future re-flip; that is correct behaviour, and the ledger row is where the decision to leave them lives.

### 4,800 PUBLIC MCQ rows ship with a correct key and NO working — measure whether each corpus intended one

Measured 2026-09-09 across the whole bank: `visibility='PUBLIC'` MCQ rows with a NULL or empty `solution` number **4,800**, concentrated in **Foundation Course 2,659** (Chemistry 961 · Physics 902 · Biology 796), **NEET 1,769** (Physics 696 · Botany 406 · Chemistry 406 · Zoology 261), **MH State Board Class 11 Maths 132**, **JEE Mains 163** (Maths 120 · Physics 43), **MH State Board Class 9 Maths 59** and **NDA Maths 18**. CBSE Class 11 and Class 12 read **zero**, which is the useful control: it is evidence the NCERT lane's fix for this class held after the Integrals repair.

**Why:** nothing detects it. `board:lint` checks section structure, `audit:keys` SKIPS a row with no solution by construction, and a NULL solution is legal in the schema — so the only symptom is a count nobody runs. This repo has shipped the defect once already (NCERT Integrals, 22 rows, closed 2026-08-17) and it recurred in NCERT Chemistry Cl-11 ch5 this session. **But the number is a measurement, not yet a finding**: for a PYQ MCQ the correct option may be the whole product, and for the two large corpora the answer is genuinely unknown from here — the Foundation worksheets ship no printed key at all, while the NEET source booklets DO carry worked solutions that were apparently never stored, which would make that 1,769 a lossy ingest rather than a design choice.

**How to apply:** triage by corpus before repairing anything — for each of the six, read the pipeline's own README/config and answer "was a solution ever intended here?". Only then decide. The board and NCERT corpora are the unambiguous half: a `/board` row whose reveal shows nothing is a defect by that surface's own contract. Add the one-line count as a standing probe either way, so the next occurrence surfaces as a number rather than as a chapter someone happens to read.

### `mark-mcq-verify` records a `blind_rederivation` verdict it has no way to know was blind

`scripts/ncert/dump-mcq.ts` correctly withholds `is_correct`, and that is the whole of the tool's protection. It is not enough: on this lane a **whole-chapter agent has already derived those same MCQ answers while transcribing**, so handing it the blind dump makes it compare its answer to its own — agreement is guaranteed, the run cannot go red, and `mark-mcq-verify` then writes a `question_reviews` row claiming the strongest evidence class this repo has. Caught 2026-09-09 and fixed in the *brief* (it now permits exactly one focused verifier, reading only rendered pages, as the sole sub-agent); the *tool* still cannot tell the two situations apart.

**Why:** an overstated review label does not merely misinform a reader — `classifyForReview` keys off `method`, so a false `blind_rederivation` **suppresses the later genuine blind pass**. That is worse than no review at all, and it is the exact failure `question_reviews` was built to make legible. The same session produced the mirror-image case from the other direction: I put a printed key's ANSWER into a chapter agent's prompt, which made its pass non-blind before it began.

**How to apply:** make the tool require what the discipline currently supplies. Options, cheapest first: (a) `mark-mcq-verify` refuses unless the verify file declares a `verifierContext` distinct from the chapter's transcription run, and records the weaker `source_key_crosscheck` when it is not; (b) `dump-mcq` writes a nonce into the dump that only a freshly-spawned verifier can echo back. Either way the rule to encode is the general one — **withholding the key does not create blindness when the same context produced the answer** — and it applies to every pipeline here that has a `dump-mcq`, not only NCERT.

## 2026-09-07

### 30 repeat groups in the MHT-CET Maths book are flagged and un-adjudicated

`npx tsx scripts/books/collapse-duplicates.ts --book=mht-cet-maths` collapses only groups the core calls SAFE; 30 of its 118 carry a `review` reason and were deliberately left alone — 16 answer-conflict, 18 chapter-or-subtopic-split, 1 figure (a group can carry several). Read them with `npx tsx scripts/books/duplicate-report.ts --review`.

**Why:** refusing a group costs nothing — both copies then print and neither claims the other's sitting — so this is untidiness rather than a defect. The upside of clearing it is small and concrete: each resolved group removes one duplicated question from the printed book, and the answer-conflict ones may surface further bank defects the way the first seven did. Note three of the flags are known false positives (a trailing full stop, and LaTeX grouping braces such as `x^{2}` vs `x^2`), left in because tightening the key moves it towards the dangerous direction of two different answers comparing equal.

**How to apply:** work the answer-conflict ones first and only against the printed paper, never against the other copy. The 18 taxonomy splits are a different question — the same question filed under two chapters or subtopics is a bank-taxonomy inconsistency the book merely surfaced, and fixing it means moving a question, not curating the book.

### The other 26 chapters of the MHT-CET Maths book have never been looked at

The book is fully built across all 27 chapters — 2,228 rows synced, 92 collapsed, 2,136 printing, 0 dangling refs, 0 non-PUBLIC rows, 0 malformed keys — but only **Vectors** has been rendered and read. Everything else rests on the smoke test and the DB checks.

**Why:** every `/books` route is superadmin-gated and `force-dynamic`, so `next build` never executes one; `scripts/books/smoke.ts` drives the loaders and `render-print.tsx` renders the print view, and neither proves a chapter LAYS OUT. The one defect that got through both this session — the print view rendering no provenance at all for a maths question — was found by looking at the page, not by any check.

**How to apply:** render two or three heavy chapters rather than all 26 — Line and Plane (205), Applications of Derivative (183), Indefinite Integration (159) — with `npx tsx --tsconfig scripts/books/tsconfig.render.json scripts/books/render-print.tsx --book=mht-cet-maths --chapter=<slug>`. Check the contents-table ranges reconcile against the printed numbers, and that no KaTeX zone failed.

### The `.docx` export has never been built, for either book

`/books` can assemble, curate, render and print a book; it cannot export one. The print view's own header says *"the Word file is authoritative"*, so strictly neither book is deliverable yet — print-to-PDF is the interim share path. Deferred on book 1 (2026-08-31, "we will do through script later") and still open.

**Why:** it is now the largest remaining gap in the feature, and it has a specific failure mode worth pre-empting. `numberChapter` and `chapterContents` in `src/lib/books/print.ts` are pure and already produce the printed numbers, the contents ranges and the answer-key rows from ONE pass — which is what stops a book shipping with every question present, every answer present, and each pointing at the other's neighbour. An exporter that recomputes any of that reintroduces exactly the divergence those functions exist to prevent, and this repo has already recorded the docx-vs-web renderers drifting apart twice.

**How to apply:** call `numberChapter`/`chapterContents` rather than reimplementing them, and reuse `src/lib/export/docxBuilder.ts`'s OMML pipeline and its `patchZip` post-processing. Two known traps in that path: a table in a long-form field must go through `parseTableBlocks` on EVERY renderer or it prints as raw pipes, and `docxBuilder.ts:158` asserts set siblings share one subtopic, which is false for the English book (95 counterexamples) though harmless for MHT-CET, where no sets exist at all.

## 2026-09-03

### Dual Nature's 12 numerical rows carry no constants preamble — the fix is a delete-and-re-commit

Ch.14's Exercises open straight at Q.1 with no shared constants block, and **all 12 of its numerical rows (`Ex Q.7`, `Q.9`–`Q.18`) have `context: null`** while needing \(h\), \(c\), \(e\), \(m_e\), \(m_p\) or \(m_n\). A student on `/browse` sees one question and no constants, so the row is under-determined as displayed. The authoring agent named the constants inside each solution as a mitigation and flagged this explicitly — but that is a mitigation, not a fix: the student still cannot *attempt* the question from the stem.

**Why:** `context` is part of `subjectiveContentHash`, so this genuinely cannot be fixed in place — adding it changes each row's identity. It is the same defect already logged against Ch.15 Structure of Atoms and Nuclei, which means the Physics lane now has **two** chapters with it, and the shared cause is that the constants-preamble rule in `PHYSICS_AGENT_BRIEF.md` §3 only fires when the *book* prints a preamble. Where the book prints none but the questions still need constants, nothing catches it.

**How to apply:** decide first whether this is worth a re-commit at all — the cost is 12 rows deleted and re-committed with fresh UUIDs, which invalidates their `question_reviews` rows and requires re-running sections, solutions, provenance and the flip for those rows (the Oscillations `Ex Q.1 (v)` precedent documents the full chain). If yes, do Ch.14 and Ch.15 in one pass. If no, close it explicitly as accepted rather than leaving it open. Either way, consider widening the brief: "if a numerical needs a constant the stem does not supply, put it in `context` — whether or not the book prints a preamble."

### Five Ch.10 questions are printed inside the Ch.11 PDF and are absent from the bank entirely

p-13 of `11. Magnetic Materials.pdf` carries a separate blue box titled **`Exercise : Chapter 10`** holding five theory questions (force on a moving charge in E vs B fields, cyclotron, radial magnetic field, Biot–Savart, Ampère's law). They belong to Ch.10 Magnetic Fields due to Electric Current — and **Ch.10's own per-chapter PDF does not contain that page**, so the Ch.10 agent could never have seen them. Ch.10 shipped with 33 questions and no `Answer in brief` block at all, which is the corroborating tell.

**Why:** these five are lost from the bank unless someone acts deliberately — no gate can see a question that exists only in another chapter's PDF, and the mis-split is a property of the publisher's per-chapter files rather than anything the pipeline did. Recorded in `config.ts` under `magnetic-materials-12-phy` so the next person on Ch.11 does not ingest them into the wrong chapter.

**How to apply:** transcribe the five from `11. Magnetic Materials.pdf` p-13 with `chapterName: "Magnetic Fields due to Electric Current"` and refs in Ch.10's `Ex Q.2 (i)`… namespace, then commit into the Ch.10 `source_file` so they join that chapter. Check first whether Ch.10's exercise numbering already uses `Q.2` for something else — if it does, these need their own ref prefix. Then re-run sections + `board:lint` for Ch.10, since its block structure gains an "Answer in brief" block it currently lacks.

### Verify (or drop) the two unverified EM Induction solved-example misprint claims

The EM Induction agent reported two printed defects in the book's own worked solutions that I deliberately did **not** bracket, because I never inspected those pages myself: `Solved Ex.12.5` printing an italic Latin `u` where π belongs in \(B = \mu_0 N i/(2\pi R)\), and `Solved Ex.12.6` printing `20·10⁻⁴ m` for a solenoid length of 20 cm (the agent's argument being that the printed `0.1257 mH` follows only from 0.20 m).

**Why:** after two agents on this lane made checkably false claims about their own actions on the same day, I stopped asserting a book defect on an agent's word alone. Both claims are plausible and specific — and both concern *shipped, PUBLIC* worked solutions, so if `12.5` really does print π as `u` then students are reading a corrupted formula in a model answer. Leaving them unadjudicated is the one loose end in an otherwise fully-gated chapter.

**How to apply:** render `12. Electromagnetic Induction.pdf` pages p-14 and p-16 at high zoom and read the two lines directly. If confirmed: `12.6`'s is answer-affecting in its working (the final value is right), so it takes a corrected working + `[Textbook misprint: …]` via the now-ported `apply-solved-fixes.ts` + `apply-errata.ts`; `12.5`'s final \(L = 2.414\times10^{-3}\) H is correct, so it is preserve-and-bracket. If not confirmed, say so — a retracted claim is a result, and the Electrostatics agent's own retracted `k₃` misread is the precedent.

### 132 shipped mh-sb-11 Maths MCQs have a correct key and NO working — cause fixed, rows not backfilled

`scripts/mh-sb-11/dump-mcq.ts` printed a contract asking the verifying agent for a **`why`** field, while `apply-solutions.ts`'s MCQ branch read **`m.solution`** and did `if (!m.solution) continue`. So a verify file written to the printed contract updated **nothing**, and said so as `updated solution on 0 mcq row(s)` — which reads like *there were none to update*. Found twice independently on 2026-09-03 (by me while shipping the Physics pilot, and by the Electrostatics ingest agent on its own run). **The cause is fixed** — the applier now falls back to `why` with a <60-char "too thin to ship" refusal, the contract now says the text is published verbatim, and `apply-mcq-solutions.ts` reads the verify file directly. The already-shipped rows are untouched.

**Why:** measured, the blast radius is smaller than it first looks and is confined to ONE exam. `Maharashtra State Board Class 11` holds 237 MCQ rows of which **132 are `solution IS NULL` (55.7%)**; 34 of the 237 are the new Physics chapters and all carry solutions, so the 132 sit entirely in the **18 shipped Maths chapters**. Every sibling lane is clean — `Maharashtra HSC Class 12` reads 294 MCQs / 0 null and `Class 10` reads 153 / 0 — because `scripts/stateboard` has no `dump-mcq.ts` at all and never used the `why` convention, so **no backport is owed**. User-visible effect: on `/browse` and `/board` those 132 rows reveal a highlighted correct option and an empty working, inconsistent with the other 105 MCQs in the same chapters. This is the same defect class as the NCERT Integrals entry above (22 rows, fixed 2026-08-17), at six times the scale.

**How to apply:** identical recipe to the Integrals fix, which came back 23/23 clean — so treat it as an audit with a repair attached, not merely a cosmetic backfill. Per chapter: `db:backup` first (these are shipped PUBLIC rows) → `dump-mcq` → a genuinely blind agent → `mark-mcq-verify --write` → `apply-mcq-solutions --apply`. **Two hazards the Integrals run surfaced and this one inherits:** (i) a row whose existing solution carries a `[Textbook …]` erratum bracket must have its `solution` field OMITTED from the new verify file, or the applier silently replaces an adjudicated erratum with plain working — `apply-mcq-solutions.ts` already refuses to overwrite a non-empty solution, which closes this, but `apply-solutions.ts` does not; (ii) a blind pass may disagree with a shipped key, which is a finding to adjudicate against the printed page, not a flip to apply. Needs permission — shipped content.

## 2026-09-02

### Make `flip-public` refuse a `derivedAnswers` chapter that has never been cross-checked

`Rotational Dynamics` (Std XII Physics) reached one command from PUBLIC with its **mandatory step-6 answer-key cross-check never run**, and nothing in the pipeline could see it. Every positive signal was green: 40/40 rows committed and sectioned, all solutions applied, 6/6 MCQ keys blind-verified with 0 mismatches, `board:lint` green, three audits clean. `flip-public.ts` gates on derived-answer PROVENANCE only — it has no notion of whether the chapter was ever diffed against the book's printed `[Ans: …]` values. It was caught by hand, by noticing that all 8 already-shipped Physics chapters carry `data/<id>.book-answers.json` + `data/<id>.errata.json` and this one carried neither. Running the gate then found **2 real book-key errors in 11 keyed rows**, so it was not ceremony.

**Why:** a skipped step raises no error and produces no wrong value — only a missing file — so it is invisible to every gate this repo has. The same hole exists for every `derivedAnswers` chapter still to come (7 more in Std XII, 14 in Std XI) and, in the general form, for every one of the ~15 ingestion pipelines here. The cost of missing it is publishing content whose answers were never checked against the only key the source provides.

**How to apply:** in `scripts/stateboard/flip-public.ts`, extend the existing `assertProvenance` guard (or add a sibling) so that when `ch.derivedAnswers` is set it also requires `data/<id>.book-answers.json` to exist — and, better, that its row count is non-zero and every row carries a non-empty `our_solution`. Refuse with a message naming `dump-book-answers.ts`. Keep it a NO-OP for chapters without the flag so the 15 shipped Maths chapters are unaffected. An `errata.json` must NOT be required — a genuinely clean chapter legitimately has none, so requiring it would train people to write empty files. Consider the generic version too: a `--verify-artifacts` mode that diffs a pending chapter's file suffixes against a named shipped sibling's.

### Add `_kt_*` to `.gitignore` — 9 scratch files sit untracked in `scripts/stateboard/`

`.gitignore` line 98 covers `scripts/*/_tmp_*`, added after an earlier session swept an agent's in-flight probe into a commit. The Physics lane instead produced `_kt_`-prefixed scratch (`_kt_db_render_check.ts`, `_kt_numerics.py`, `_kt_mcq_check.py`, `_kt_pairing_check.py`, `_kt_q24_q18.py`, `_kt_ex34_convention.py`, `_kt_gen_diagram.py`, `_kt_sections_integrity.ts`, `_kt_strip_inline_bracket.py`), none of which is ignored. `scripts/reviews/_tmp/` is likewise unignored — the pattern is `_tmp_*`, with an underscore, so a plain `_tmp/` directory does not match.

**Why:** they are one `git add <dir>` away from being committed, and `_kt_sections_integrity.ts` / `_kt_db_render_check.ts` are TypeScript inside a typechecked tree, so a broken one turns the gate red for everyone. This exact class already cost a session once (`scripts/mh-ssc-10-text/out/`).

**How to apply:** add `scripts/*/_kt_*` and `scripts/*/_tmp/` beside the existing `scripts/*/_tmp_*` in `.gitignore`. Then decide per file whether any deserves promotion to a committed, named helper — `_kt_sections_integrity.ts` and `_kt_pairing_check.py` look like real checks that were never given a home, and the shipped precedent (`gen_semiconductor_diagram_specs.py`, `verify-fluids-12-phy.py`) is that a genuine generator or verification script IS committed as evidence.

### Land or discard the two unrelated in-flight jobs sitting uncommitted on `main`

After the Physics commit (`af341962`), the working tree still carries two independent bodies of uncommitted work: a **board chapter-order feature** (`package.json` +`board:order`, new `scripts/board/`, `src/app/board/BoardReader.tsx`, `src/app/board/[examSlug]/page.tsx`, `src/lib/board/query.ts`, `tests/board-sections.test.ts`, new `tests/board-chapter-order.test.ts`) and a **practice-paper ocean-parity task** (`scripts/practice-paper/config.ts`, `data/ocean-test-1.records.json`, `verify-ocean-parity.ts`). Neither was touched this session — the Physics commit staged explicit paths precisely to leave them alone.

**Why:** unlike the Physics data, this is `src/` code with tests, so it is the kind of work that silently rots against a moving codebase and makes the next `git add` risky (the documented failure mode: `git add -A` sweeping a prior session's work into an unrelated commit). `tests/board-sections.test.ts` is modified, so the board test file currently in the tree is not the one on `main`.

**How to apply:** decide per job — finish and commit on its own branch, or `git stash`/discard. If keeping, run `npm run prepush` first: it touches `src/` so it is build- and test-relevant, unlike the data-only Physics commit. Note there are **2 commits pending push** on `main` as of 2026-09-02.
